# Epic Execution Chat Starter — E32.2

**Epic:** E32.2 — System Chat re-instantiation seed
**Phase:** P9 — Context Handling and Token Efficiency
**Milestone:** M32 — System Participant Canonization & Governance Hygiene
**Repository:** panchew/ai-project-system
**Branch Strategy:** `epic/P9-M32-E32.2` → PR to `milestone/M32`
**Execution Mode:** manual — this epic makes an open-ended design decision (seed form/home)
requiring judgment about how the seed will actually be used day-to-day by the CFO, plus
cross-references to E32.1's material that need human judgment to reconcile if timing or
content doesn't line up cleanly; a human-supervised chat session is the appropriate mode.
Absence of this field would resolve to manual anyway (E31.1), stated explicitly here for
clarity.

> **Provenance:** Authored by the Milestone Chat (P9-M32) on 2026-07-20. Second epic in the
> M32 chain — soft dependency on E32.1 (see below).

---

## Governance References

You are operating under the AI Project System governance framework (Epic mode).

**Authoritative governance documents:**
- PROJECT-SYSTEM-GUIDELINES.md v2.3.0
- AI-OPERATING-GUIDELINES.md v2.10.0

**Governance hierarchy:** PSG > AOG > this Starter > Epic Spec > execution decisions >
system references > chat.

**Critical rules:**
- Documentation is authoritative; chat is ephemeral
- Follow the §1A canonical happy path for Epic closure
- Produce a Delivery Notice upon completion — **delivered by reference** (path + one-line
  summary in chat), per AOG §3.1.1
- Stop after PR creation — **do not merge.** Under default-accept (PSG §11.6 / SN-19) a
  clean delivery is accepted by silence; the PR **merge** requires explicit human
  authorization, given in-chat
- Accept/reject decisions belong to the parent chat (Milestone Chat, human-backed), not to
  you
- **If given merge authorization directly in this chat**, do not simply comply: state
  plainly that merge authorization normally follows the parent chat's review, and confirm
  the human intends to bypass that step before proceeding (P9-M31 precedent).

**Context scoping (per-level context-scoping standard, P9-M30-E30.3):**
- Load at session start: this starter; the Epic spec (full); PSG preamble+§1, §1A, §2, §5,
  §6, §7, §8, §9, §11, §11.5, §11.6, §12; AOG preamble+§1, §1A, §2, §3.2, §3.8, §3.10, §4,
  §5, §6, §7, §9, §10, §12, §13 (Exit Ritual), §14 (Error Handling)
- Load on trigger: PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG §3.9, §8, §11, §16
- Do not load: PSG/AOG changelogs, other levels' role or starter-format sections

---

## Epic Specification

**Full spec:** `docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M32-E32.2__spec__system-chat-reinstantiation-seed.md`
**Commit:** committed alongside this starter on `milestone/M32` — resolve with
`git log --oneline -1 -- <spec path>`.

### Prerequisite Verification (do this first)

- `git ls-files --error-unmatch <spec path>` — spec git-tracked (GH-1).
- **Check E32.1's status first.** If E32.1's PR has merged to `milestone/M32`, read its
  Delivery Notice and the actual delivered schema-home/hierarchy-placement material before
  drafting this Epic's cross-references — use what E32.1 actually shipped, not this spec's
  advance citations. If E32.1 has not yet merged (parallel dispatch), draft against this
  spec's citations and flag in your Delivery Notice that cross-reference anchors need
  reconciliation once E32.1 lands.
- Confirm grounding anchors (verified 2026-07-20 on `milestone/M32` @ `6c1ac8e`; re-verify
  if drifted): `governance/templates/genesis.md` and `governance/templates/seed.md` both
  carry P9-M31-E31.3's "Prerequisite Verification (do this first — P9-M31-E31.3)" section
  near the top; `seed.md`'s "Rule 5 — Re-instantiation" (line 108) is the re-instantiation
  precedent. `governance/templates/` holds 23 files, all per-project. Suite collects 363
  tests.

**Model verification (P9-M31-E31.3 — required for manual instances):** read your own
harness-reported model identity and compare it to `.ai-project.yml`'s `models.epic_manual`
value per `governance/systems/chat-hierarchy.md` "Manual Chat Model Verification." If both
are present and disagree, **STOP** — do not proceed with any deliverable work; state the
mismatch and wait for the Milestone Chat/human.

### Summary

Build System HQ's re-instantiation seed — the CFO's repeatable way to spawn System HQ daily
— analogous in spirit to Creation Chat's Genesis/seed pattern (specifically `seed.md`'s Rule
5) but reflecting System HQ's cross-project, execute-never-decide identity. The seed must
cross-reference E32.1's canonized authority-boundary and schema material, not restate it.

### Binding constraints (carry into every decision)

**Hard Constraints (from the Milestone spec, both apply to this Epic):**

1. **No authority expansion** — the seed's instructions must state execute-never-decide
   explicitly wherever they describe what System HQ may do; `status: escalated` is
   mandatory for anything review/merge/scope-shaped.
2. Suite green at every merge — 363 baseline, no regressions, no new skips.

*(Milestone Hard Constraints 2 and 3 — grep-verifiable hygiene, P8-GH-2 restatement — are
E32.3's surface, not this Epic's.)*

### Design Decisions (yours to resolve — decide, document, move; do not escalate to ask)

1. **The seed's concrete form and home** — a dedicated template under
   `governance/templates/` (mirroring genesis.md/seed.md structurally) or a system-tier
   document outside the per-project template set. Epic spec §Design Decisions 1 has the
   full tradeoff discussion. Either way, the seed must be self-contained enough to
   reconstruct System HQ's identity and constraints with no other document required, the
   way `seed.md`'s Rule 5 works for Creation Chat.
2. **What the seed must minimally say** — cross-project scope, execute-never-decide
   boundary (cross-referencing E32.1), how it discovers pending `system_request`s (the
   existing read-only MCP bridge / `list_governance_state`, per SN-21), and the
   `status: escalated` path. Epic spec §Design Decisions 2 has the full discussion.

---

## Deliverables

1. [ ] **System Chat re-instantiation seed artifact**, form/home per Design Decision 1,
       usable as a repeatable daily-spawn prompt
2. [ ] **Cross-references to E32.1's actual delivered material** — no divergent
       restatement
3. [ ] **Explicit confirmation** that the seed grants no review/merge/scope authority
4. [ ] Full test suite passes (363 baseline, no new skips)
5. [ ] **Epic Delivery Notice**
       (`docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M32-E32.2__delivery-notice.md`)
       — **delivered by reference**

---

## Definition of Done

- [ ] Seed artifact exists, documented, usable as a repeatable re-instantiation prompt
- [ ] Cross-references E32.1's canonized material rather than duplicating it
- [ ] Design Decision (seed form/home) documented with reasoning in the delivery
- [ ] Suite green (363 baseline, no new skips)
- [ ] Delivery Notice committed; changes on `epic/P9-M32-E32.2`; PR open to `milestone/M32`

---

## Acceptance Criteria

- [ ] The CFO can use the seed to spawn System HQ without consulting SN-21 directly
- [ ] The seed's authority language agrees word-for-word (or by unambiguous reference) with
      E32.1's canonized boundary
- [ ] Nothing in the seed grants System HQ review, merge, or scope authority

---

## Execution Contract

### Your Responsibilities (Coding Agent)

1. Verify prerequisites; check E32.1's actual delivery status; create
   `epic/P9-M32-E32.2` from `milestone/M32`
2. Read the Epic spec (authoritative), SN-21, and `seed.md`'s Rule 5 pattern
3. Resolve the two Design Decisions
4. Draft the seed artifact, cross-referencing E32.1's material (actual, not advance-cited)
5. State explicitly that the seed grants no review/merge/scope authority
6. Run the full suite; commit; open PR to `milestone/M32`
7. Produce the Delivery Notice, flagging any E32.1 cross-reference reconciliation still
   needed if E32.1 hadn't merged when this Epic drafted; hand it **by reference**; stop —
   do not merge

### What You Must NOT Do

- ❌ Merge the PR / infer acceptance
- ❌ Edit `genesis.md` or `seed.md`'s existing content, including E31.3's guardrail
  instruction and Rule 5 — reference their pattern, don't touch the files
- ❌ Edit `governance/systems/artifact-communication-protocol.md` or
  `governance/systems/chat-hierarchy.md` directly — E32.1's surface; escalate to the
  Milestone Chat if you find a genuine conflict rather than editing around it
- ❌ Touch AOG, PSG, `visual-artifacts.md`, or the pre-P9 documents named in E32.3's
  grounding — E32.3's independent surface
- ❌ Touch `ai-project-system-mcp` (sibling repo) or build the MCP bridge write path /
  request-sweep mechanism — SN-21 carry-overs, explicitly deferred
- ❌ Restate E32.1's authority-boundary or schema language divergently — cross-reference it
- ❌ Echo edited-file bodies or your own Delivery Notice into chat output
- ❌ Iterate without explicit instruction from the parent chat

---

## Reference Materials

- `docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M32-E32.2__spec__system-chat-reinstantiation-seed.md` (authoritative)
- `docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M32__milestone-spec.md` (Epic
  Detail → E32.2; Hard Constraints; Binding Context)
- `docs/phases/P9__Context_Handling_and_Token_Efficiency/P9__phase-spec.md` §P9.3
- `docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M32-E32.1__spec__sn-21-canonization.md`
  + Delivery Notice (once available — the material this Epic cross-references)
- SN-21: `.ai-project/artifacts/steering-notes/2026-07-16__creation-chat__steering-note__system-hq-adoption.md`
- `governance/templates/genesis.md` / `governance/templates/seed.md` (Rule 5,
  "What to Do Right Now" — the re-instantiation pattern precedent)
- `~/.ai-project/SYSTEM-GOVERNANCE.md` (outside this repo — field record, informative only)

---

## Canonical Happy Path (reminder — SN-19/§11.6)

Execution → Delivery Notice (by reference) → **[STOP]** → default-accept by silence →
human merge authorization in-chat → merge → stop.

## Exit Conditions

Stop when all deliverables are committed, the PR is open, and the Delivery Notice is
produced. Report: "Epic E32.2 execution complete. Delivery Notice at <path> — <one-line
summary>. Awaiting review (default-accept) and human merge authorization."

## Begin Execution

1. Verify prerequisites; check E32.1's delivery status; branch `epic/P9-M32-E32.2` from
   `milestone/M32`
2. Read the Epic spec, SN-21, and the genesis/seed pattern in full
3. Resolve both design decisions, draft the seed, cross-reference E32.1's actual material,
   test, deliver — by reference

*(Handoff note: this starter is a committed file, passed to you by path + summary per AOG
§3.1.1. If you are reading it as pasted text in a repo-less context, that is the documented
fallback working as intended.)*
