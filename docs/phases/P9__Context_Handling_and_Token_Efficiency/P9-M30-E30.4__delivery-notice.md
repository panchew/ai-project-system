---
type: delivery-notice
epic: E30.4
status: delivered
delivery_date: 2026-07-18
---

# Epic Delivery Notice — E30.4

**Epic:** E30.4 — Reference-don't-display reconciliation (SN-23)
**Status:** Execution Complete, Delivered for Review
**Delivery Date:** 2026-07-18
**PR:** [#139](https://github.com/panchew/ai-project-system/pull/139) → `milestone/M30`

## Execution Summary

Both CFO-ratified SN-23 decisions are implemented across every governance surface
that mandated artifact-body echo — and the sweep found **two mandating surfaces
beyond the spec's named four** (`governance/systems/epic-execution-chat-starter.md`
DELIVERY FORMAT block; `governance/agents/governance.agent.md` delivery
instruction), which were edited coherently because the acceptance criterion's
grep must come back clean over all of `governance/`.

The rule now lives in **one home**: AOG §3.1.1 (v2.10.0), generalized from
"Epic Execution Chat Starter Format" into the normative artifact-handoff rule —
a committed, git-tracked artifact passes **by reference** (IDE-attach + one-line
intent, or the canonical reference line `<type + id> — <path> — <status>`,
defined once in-section); **producer half:** never display a body you wrote to
file; **consumer half:** read once, selectively (frontmatter + Summary + DoD/QA
suffices under PSG §11.6). The four-backtick fenced full-body form is
**retained, demoted** to the marked repo-less fallback (SN-23 Decision 2 —
platform agnosticism preserved; paste is not deleted, and each edited surface
carries a findable "Fallback — no repo access?" pointer). Every other surface
cites §3.1.1 in one sentence — no paraphrases (the P6 E25.4/E25.5 divergence
failure mode explicitly avoided).

**Surfaces edited (8 files + evidence note):** AOG §3.1.1 + changelog
(v2.9.0→v2.10.0); `governance/templates/{epic,milestone,phase}-execution-chat-starter.md`
(usage steps 4–5 + in-body Delivery format; **E30.3's scoping/load-list and
dedup content untouched** — handoff lines only, rebased on merged #138);
`governance/systems/artifact-communication-protocol.md` §Manual Mode + §Purpose
annotation (v1.2.0→v1.3.0 + changelog row); `governance/EPIC-EXECUTION-CHAT-STARTER.md`
§What Happens Next step 1; `governance/systems/epic-execution-chat-starter.md`
DELIVERY FORMAT (last_updated bumped); `governance/agents/governance.agent.md`
(v2.0.0→v2.1.0).

**Before/after evidence (E30.1 mechanism's tokenizer, o200k proxy, G10 ±10–15%):**
`.ai-project/artifacts/reference/token-measurement/echo-cost-note.md` — real M30
bodies, not estimates: full-starter echo cost 2,691–3,627 proxy tokens vs 41–50
for the reference line (median **−3,160 per handoff**, 54–77×); Delivery-Notice
paste 1,401–1,975 vs 44–47 (median **−1,815**, 32–45×); the M30 Milestone-Chat
history's three starter echoes ≈9,700 proxy tokens carried at cache rates on
every call. **Honest bound stated per SN-23:** ingestion happens once,
selectively — eliminated is the duplication, not ingestion; the table is an
upper bound. Billed-median effects **forward-looking only**; **G14** recorded
(historical billed echo share not isolable retroactively). SN-23's word-count
figures superseded by tokenizer figures in the note.

## Design Decisions (spec §Design Decisions, resolved)

1. **Rule home = AOG §3.1.1, generalized.** Every mandating surface already
   cited §3.1.1 as its canonical rule, so one edit repoints all of them; AOG is
   the behavioral document chats actually load (the protocol doc is
   schema/transport reference). Scoping load-lists (E30.3) were not modified —
   the conditional-load rule covers §3.1.1 at handoff time via each template's
   Delivery-format pointer.
2. **Reference format:** `<Artifact type + id> — \`<repo-relative path>\` —
   <status, one clause>` — defined once in §3.1.1; all surfaces cite it.
3. **Evidence note home:** standalone `echo-cost-note.md` beside the E30.1–E30.3
   artifacts — appending to E30.3's `context-scoping.md` would have edited an
   artifact the spec's do-not-touch list protects.
4. **Version bumps:** AOG minor (2.10.0, normative-rule change per its
   changelog convention); protocol minor (1.3.0 + changelog row);
   governance.agent.md frontmatter 2.1.0; systems mirror has no version row —
   `last_updated` bumped. README's governance listing intentionally left for
   phase closure (PSG §5C owns it; mid-phase drift is the established norm).

## Definition of Done — verified

- [x] No governance surface mandates full-body echo of a committed artifact —
      acceptance grep over `governance/` returns only AOG §3.1.1's marked
      fallback item
- [x] Paste documented as repo-less fallback on every surface that previously
      mandated it (findable "Fallback — no repo access?" note at each)
- [x] One no-echo rule + canonical reference format in one home; all surfaces
      cite it
- [x] Mechanism-produced before/after numbers committed with honest bound
      (`echo-cost-note.md`), G10 bars, G14 gap
- [x] Version bumps + changelog rows per each document's convention
- [x] Suite green: **307 passed / 0 failed, no new skips** (no template-text
      assertions existed; verified before editing)
- [x] Delivery Notice committed; all changes on `epic/P9-M30-E30.4`;
      PR [#139](https://github.com/panchew/ai-project-system/pull/139) open to
      `milestone/M30`

## Notes for Review

- E30.3 hard gate was verified merged (#138, `25b5c57`) before branching.
- This epic **practiced its own rule**: no edited-file bodies or this notice's
  body were echoed into chat; handoff is by reference line.
- Extent-vs-spec: the two extra surfaces (systems mirror, agent file) are the
  only additions beyond the spec's named list; both are restatements of the same
  mandate and were required by the acceptance grep. Nothing else was touched —
  PSG, `.ai-project.yml`, `bin/`, schemas, and the §11.6 acceptance model are
  unchanged.

**Awaiting review (default-accept, PSG §11.6) and human merge authorization
(SN-19).**
