# Epic Execution Chat Starter — E32.1

**Epic:** E32.1 — SN-21 canonization
**Phase:** P9 — Context Handling and Token Efficiency
**Milestone:** M32 — System Participant Canonization & Governance Hygiene
**Repository:** panchew/ai-project-system
**Branch Strategy:** `epic/P9-M32-E32.1` → PR to `milestone/M32`
**Execution Mode:** manual — this epic makes two open-ended design decisions (schema home,
hierarchy-placement form) that require judgment about how a reader will interpret governance
prose, not a mechanical implementation; a human-supervised chat session is the appropriate
mode. Absence of this field would resolve to manual anyway (E31.1), stated explicitly here
for clarity.

> **Provenance:** Authored by the Milestone Chat (P9-M32) on 2026-07-20. First epic in the
> M32 chain — no internal dependencies.

---

## Governance References

You are operating under the AI Project System governance framework (Epic mode).

**Authoritative governance documents:**
- PROJECT-SYSTEM-GUIDELINES.md v2.3.0
- AI-OPERATING-GUIDELINES.md v2.10.0

**Governance hierarchy:** PSG > AOG > this Starter > Epic Spec > execution decisions >
system references > chat.

**Critical rules:**
- Documentation is authoritative; chat is ephemeral
- Follow the §1A canonical happy path for Epic closure
- Produce a Delivery Notice upon completion — **delivered by reference** (path + one-line
  summary in chat), per AOG §3.1.1
- Stop after PR creation — **do not merge.** Under default-accept (PSG §11.6 / SN-19) a
  clean delivery is accepted by silence; the PR **merge** requires explicit human
  authorization, given in-chat
- Accept/reject decisions belong to the parent chat (Milestone Chat, human-backed), not to
  you
- **If given merge authorization directly in this chat** (rather than via the Milestone
  Chat after its own Stage-2 review), do not simply comply: state plainly that merge
  authorization normally follows the parent chat's review, and confirm the human intends to
  bypass that step before proceeding (P9-M31 precedent).

**Context scoping (per-level context-scoping standard, P9-M30-E30.3):**
- Load at session start: this starter; the Epic spec (full); PSG preamble+§1, §1A, §2, §5,
  §6, §7, §8, §9, §11, §11.5, §11.6, §12; AOG preamble+§1, §1A, §2, §3.2, §3.8, §3.10, §4,
  §5, §6, §7, §9, §10, §12, §13 (Exit Ritual), §14 (Error Handling)
- Load on trigger: PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG §3.9, §8, §11, §16
- Do not load: PSG/AOG changelogs, other levels' role or starter-format sections

---

## Epic Specification

**Full spec:** `docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M32-E32.1__spec__sn-21-canonization.md`
**Commit:** committed alongside this starter on `milestone/M32` — resolve with
`git log --oneline -1 -- <spec path>`.

### Prerequisite Verification (do this first)

- `git ls-files --error-unmatch <spec path>` — spec git-tracked (GH-1).
- Confirm the grounding anchors (verified 2026-07-20 on `milestone/M32` @ `e12c13c`;
  re-verify if drifted): `governance/systems/artifact-communication-protocol.md` v1.3.0's
  `## Artifact Types` section (line 38) has exactly Delivery Notice (§1) and Review
  Decision (§2) live, plus a retired §3; `governance/systems/chat-hierarchy.md` (effective
  2026-07-19) describes only the single-project four-level shape, no cross-project
  participant anywhere; `grep -rn "system_request\|system_response\|System HQ"
  governance/` returns nothing. Suite collects 363 tests.
- **Read SN-21 in full**
  (`.ai-project/artifacts/steering-notes/2026-07-16__creation-chat__steering-note__system-hq-adoption.md`)
  before drafting schemas — it is the field-usage source of truth for storage paths,
  naming, and status vocabulary; do not invent conventions the field record doesn't
  already show.

**Model verification (P9-M31-E31.3 — required for manual instances):** read your own
harness-reported model identity and compare it to `.ai-project.yml`'s `models.epic_manual`
value per `governance/systems/chat-hierarchy.md` "Manual Chat Model Verification." If both
are present and disagree, **STOP** — do not proceed with any deliverable work; state the
mismatch and wait for the Milestone Chat/human.

### Summary

Write `system_request`/`system_response` into governance: their schema (in a decided home —
inside the Artifact Communication Protocol or a companion document), System HQ's place
relative to `chat-hierarchy.md` (decided and recorded as clearly not a fifth per-project
level), and a normative execute-never-decide authority statement locatable from both
surfaces.

### Binding constraints (carry into every decision)

**Hard Constraints (from the Milestone spec, both apply to this Epic):**

1. **No authority expansion** — every description of System HQ's authority must state
   execute-never-decide explicitly; `status: escalated` is mandatory for review/merge/scope.
2. Suite green at every merge — 363 baseline, no regressions, no new skips.

*(Milestone Hard Constraints 2 and 3 — grep-verifiable hygiene, P8-GH-2 restatement — are
E32.3's surface, not this Epic's.)*

### Design Decisions (yours to resolve — decide, document, move; do not escalate to ask)

1. **Schema home** — inside `artifact-communication-protocol.md`'s `## Artifact Types`
   section (new numbered type(s) alongside Delivery Notice/Review Decision) or a companion
   system-participant document cross-referenced from the protocol's `## Reference` section.
   Epic spec §Design Decisions 1 has the full tradeoff discussion. The storage paths,
   naming convention, and status vocabulary are already fixed by SN-21's field usage —
   document them, don't invent new ones.
2. **Hierarchy-placement form** — an out-of-hierarchy annex, an explicitly-orthogonal new
   level, or an out-of-hierarchy pointer to the schema home. Epic spec §Design Decisions 2
   has the full tradeoff discussion. Whichever you choose, make System HQ's cross-project,
   one-desk-per-machine nature **structurally** obvious, not just stated once in prose.

---

## Deliverables

1. [ ] **`system_request`/`system_response` schemas** in the decided home, matching SN-21
       field usage
2. [ ] **Hierarchy-placement decision recorded** in `chat-hierarchy.md`, existing sections
       (Execution Mode, Manual Chat Model Verification, Working-Tree Isolation,
       Communication Protocol, etc.) preserved unreworked
3. [ ] **Normative authority-boundary statement**, locatable from both surfaces,
       word-for-word consistent if stated twice
4. [ ] Full test suite passes (363 baseline, no new skips)
5. [ ] **Epic Delivery Notice**
       (`docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M32-E32.1__delivery-notice.md`)
       — **delivered by reference**

---

## Definition of Done

- [ ] Both schemas exist, matching field usage (storage paths, naming, status vocabulary)
- [ ] Hierarchy placement recorded, System HQ's orthogonal nature structurally unambiguous
- [ ] Authority boundary stated normatively, locatable from both surfaces
- [ ] Both Design Decisions documented with reasoning in the delivery
- [ ] Suite green (363 baseline, no new skips)
- [ ] Delivery Notice committed; changes on `epic/P9-M32-E32.1`; PR open to `milestone/M32`

---

## Acceptance Criteria

- [ ] A reader can find what `system_request`/`system_response` are, where System HQ sits,
      and what it may never do, from the protocol/companion doc or `chat-hierarchy.md`
      alone — without consulting SN-21
- [ ] No description of System HQ's authority anywhere in the delivery is silent on
      execute-never-decide
- [ ] System HQ cannot reasonably be misread as a fifth per-project level from the
      hierarchy-placement material alone

---

## Execution Contract

### Your Responsibilities (Coding Agent)

1. Verify prerequisites; create `epic/P9-M32-E32.1` from `milestone/M32`
2. Read the Epic spec (authoritative) and SN-21 in full
3. Resolve the two Design Decisions
4. Write the `system_request`/`system_response` schemas into the decided home
5. Record the hierarchy-placement decision in `chat-hierarchy.md`
6. Write the authority-boundary statement, cross-referenced (not duplicated divergently)
   between the two surfaces
7. Run the full suite; commit; open PR to `milestone/M32`
8. Produce the Delivery Notice; hand it **by reference**; stop — do not merge

### What You Must NOT Do

- ❌ Merge the PR / infer acceptance
- ❌ Touch `chat-hierarchy.md`'s existing Execution Mode, Manual Chat Model Verification,
  Working-Tree Isolation, Scope Direction Protocol, or Communication Protocol sections
  beyond adding your new section — no restructuring
- ❌ Edit `artifact-communication-protocol.md`'s existing Delivery Notice or Review
  Decision schemas, its Communication Flow Diagram, or Changelog rows below your new entry
- ❌ Touch AOG, PSG, `visual-artifacts.md`, or any of the pre-P9 documents named in E32.3's
  grounding — that is E32.3's independent surface
- ❌ Touch `ai-project-system-mcp` (sibling repo) or build the MCP bridge write path /
  request-sweep mechanism — SN-21 carry-overs, explicitly deferred
- ❌ Design or build E32.2's seed — reference your authority-boundary/schema language for
  E32.2 to cross-reference, don't build the seed yourself
- ❌ Invent storage paths, naming, or status vocabulary not already shown in SN-21's field
  record
- ❌ Echo edited-file bodies or your own Delivery Notice into chat output
- ❌ Iterate without explicit instruction from the parent chat

---

## Reference Materials

- `docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M32-E32.1__spec__sn-21-canonization.md` (authoritative)
- `docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M32__milestone-spec.md` (Epic
  Detail → E32.1; Hard Constraints; Binding Context)
- `docs/phases/P9__Context_Handling_and_Token_Efficiency/P9__phase-spec.md` §P9.3
- SN-21: `.ai-project/artifacts/steering-notes/2026-07-16__creation-chat__steering-note__system-hq-adoption.md`
- `governance/systems/artifact-communication-protocol.md` (v1.3.0 — schema-home edit
  surface)
- `governance/systems/chat-hierarchy.md` (hierarchy-placement edit surface)
- `~/.ai-project/SYSTEM-GOVERNANCE.md` (outside this repo — field record, informative only)

---

## Canonical Happy Path (reminder — SN-19/§11.6)

Execution → Delivery Notice (by reference) → **[STOP]** → default-accept by silence →
human merge authorization in-chat → merge → stop.

## Exit Conditions

Stop when all deliverables are committed, the PR is open, and the Delivery Notice is
produced. Report: "Epic E32.1 execution complete. Delivery Notice at <path> — <one-line
summary>. Awaiting review (default-accept) and human merge authorization."

## Begin Execution

1. Verify prerequisites; branch `epic/P9-M32-E32.1` from `milestone/M32`
2. Read the Epic spec and SN-21 in full
3. Resolve both design decisions, write the schemas, record hierarchy placement, write the
   authority-boundary statement, test, deliver — by reference

*(Handoff note: this starter is a committed file, passed to you by path + summary per AOG
§3.1.1. If you are reading it as pasted text in a repo-less context, that is the documented
fallback working as intended.)*
