# Epic Execution Chat Starter — E31.2

**Epic:** E31.2 — Agentic paid-vs-local decision logic
**Phase:** P9 — Context Handling and Token Efficiency
**Milestone:** M31 — Dual-Mode Working Levels & Model Guardrail
**Repository:** panchew/ai-project-system
**Branch Strategy:** `epic/P9-M31-E31.2` → PR to `milestone/M31`
**Execution Mode:** agentic — dispatched via `bin/ai-project-orchestrator` →
`bin/run-dev-agent` per the mode model this epic itself operates within
(`governance/systems/chat-hierarchy.md`, "Execution Mode: Manual vs. Agentic", E31.1). If
this session is instead being run manually (human-supervised chat reading this starter
directly), that is equally valid — no field present on *this specific instance's* starter
would mean manual; the field above declares the Milestone Chat's intended mode for this
epic's execution, not a claim about how you are actually being read.

> **Provenance:** Authored by the Milestone Chat (P9-M31) on 2026-07-19. Second epic in the
> M31 chain — hard-blocked on E31.1, which is now merged (`milestone/M31` @ `31eff27`,
> PR #140). E31.3 may run before or after this epic per the Milestone Chat's sequencing
> call; nothing in this epic blocks starting E31.3 in parallel, but verify no shared-file
> contention before doing so (both touch test files; neither touches the other's primary
> surface).

---

## Governance References

You are operating under the AI Project System governance framework (Epic mode).

**Authoritative governance documents:**
- PROJECT-SYSTEM-GUIDELINES.md v2.3.0
- AI-OPERATING-GUIDELINES.md v2.10.0

**Governance hierarchy:** PSG > AOG > this Starter > Epic Spec > execution decisions >
system references > chat.

**Critical rules:**
- Documentation is authoritative; chat is ephemeral
- Follow the §1A canonical happy path for Epic closure
- Produce a Delivery Notice upon completion — **delivered by reference** (path + one-line
  summary in chat), per AOG §3.1.1
- Stop after PR creation — **do not merge.** Under default-accept (PSG §11.6 / SN-19) a
  clean delivery is accepted by silence; the PR **merge** requires explicit human
  authorization, given in-chat
- Accept/reject decisions belong to the parent chats (human-backed), not to you

**Context scoping (per-level context-scoping standard, P9-M30-E30.3):**
- Load at session start: this starter; the Epic spec (full); PSG preamble+§1, §1A, §2, §5,
  §6, §7, §8, §9, §11, §11.5, §11.6, §12; AOG preamble+§1, §1.1, §2, §3.2, §3.8, §3.10, §4,
  §5, §6, §7, §9, §12, §14, §15 (Exit Ritual), §16 (Error Handling)
- Load on trigger: PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG §3.9, §8, §13, §17
- Do not load: PSG/AOG changelogs, other levels' role or starter-format sections; E31.1's
  full spec (its Delivery Notice + demonstration evidence, referenced below, are enough
  context — do not re-read the whole E31.1 chain)

---

## Epic Specification

**Full spec:** `docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M31-E31.2__spec__agentic-paid-vs-local-decision-logic.md`
**Commit:** committed alongside this starter on `milestone/M31` — resolve with
`git log --oneline -1 -- <spec path>`.

### Prerequisite Verification (do this first)

- `git ls-files --error-unmatch <spec path>` — spec git-tracked (GH-1).
- **E31.1 merged is the hard gate:** confirm `milestone/M31` contains
  `governance/systems/chat-hierarchy.md`'s "Execution Mode" section and PR #140's merge
  commit. It does as of this starter's authoring (`31eff27`) — re-verify if time has
  passed.
- Confirm the grounding anchors (verified 2026-07-19 on `milestone/M31` @ `31eff27`;
  re-verify if drifted): `bin/ai-project-orchestrator:17-23` `DEFAULT_MODELS` still
  hardcodes `gpt-4o`/`claude-3-5-sonnet`/`qwen2.5-coder:7b`; `.ai-project.yml:29-34`
  `models:` block still reads `claude-opus-4-8` (×3) + `qwen2.5-coder:14b` (×2);
  `tests/test_model_config.py` still covers `epic_dev` only (4 tests); `handle_epic_execution`
  (~line 320) still does a bare config lookup with no policy citation and no
  local-unavailable handling. Suite collects 307 tests.

### Summary

Fix the four gaps M30's closure handoff identified in the one dispatch surface that
actually runs today (`handle_epic_execution` → `run-dev-agent`, epic-level only): align
`DEFAULT_MODELS` with the refreshed `models:` block, make the dev/qa model resolution
traceable to policy rows P5–P7, extend the consistency guard to all five `models:` keys
plus a policy↔block divergence check, define and test local-unavailable behavior, and
produce real row-P5 run evidence (or an explicit GPU-contention gap record).

### Binding constraints (carry into every decision)

**Hard Constraints (verbatim from the Milestone spec, all five carried; #3 and #4 are this
epic's direct surface):**

1. Refusal means refusal — any local-unavailable handling you add must fail loudly, never
   scroll past as a soft warning.
2. No agentic default — you are not changing whether any instance is agentic, only how an
   already-agentic epic dispatch picks a model.
3. **Policy consumed, not re-authored** — apply rows P5–P7 as recorded; a genuine defect
   routes through the Change Discipline via the Milestone Chat, never silent divergence.
   Guard logic treats policy↔block divergence as an error.
4. **Local loadability never assumed** — every local-model code path in the epic-execution
   dispatch needs defined, tested "not loadable now" behavior.
5. Suite green at every merge — 307 baseline, no regressions, no new skips.

**Scope-honesty constraint (from the Epic spec's grounding):** only epic×execution has a
dispatch mechanism today. Do not write code or delivery prose implying a general per-level
decision engine — state plainly that "per task" means P5–P7 at the one surface that exists.

### Design Decisions (yours to resolve — decide, document, move; do not escalate to ask)

1. **What the decision logic concretely is** — traceability-only (comments/docstrings
   citing P6/P7) vs. traceability + a runtime divergence assertion. Epic spec §Design
   Decisions 1 lays out both with tradeoffs.
2. **Local-unavailable handling design** — a distinguished exit/signal from
   `run-dev-agent`, an escalation record from `handle_epic_execution` (reuse
   `generate_escalation_report`, ~line 430), or both layered. Must be testable against an
   unreachable endpoint, not just "some exception happens."
3. **Consistency-guard extension shape** — extend `tests/test_model_config.py` or add a
   sibling file; cover all five `models:` keys; add a policy↔block divergence check via a
   narrow, documented parse of the policy file's mapping table (no general markdown
   parser).
4. **Row-P5 run-evidence location** — `.ai-project/artifacts/agentic-runs/<id>/` (existing
   convention) or beside `model-routing-policy.md`. If Ollama is contended throughout, the
   gap record goes in the same place instead.

---

## Deliverables

1. [ ] **Policy-traceable epic-level dispatch decision** — `dev_model`/`qa_model`
       resolution cites policy rows P6/P7 explicitly
2. [ ] **`DEFAULT_MODELS` aligned** with the refreshed `models:` block; `grep -rn
       "gpt-4o\|claude-3-5-sonnet\|qwen2.5-coder:7b" bin/` returns nothing
3. [ ] **Consistency-guard tests extended to all five `models:` keys**; policy↔block
       divergence test fails the suite on mismatch
4. [ ] **Local-unavailable handling implemented and tested** — distinguished signal +
       documented, non-silent behavior; a test against an unreachable endpoint
5. [ ] **Row-P5 run evidence** committed (real local-lane run) or the explicit
       GPU-contention gap record
6. [ ] Full test suite passes (307 baseline, no new skips)
7. [ ] **Epic Delivery Notice**
       (`docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M31-E31.2__delivery-notice.md`)
       — **delivered by reference**

---

## Definition of Done

- [ ] Policy-applying decision logic implemented; no hardcoded falsified name in the
      runtime path (grep-verifiable)
- [ ] Guard tests cover all `models:` keys; policy↔block divergence fails the suite
- [ ] Local-unavailable behavior defined and tested
- [ ] Run evidence (or the explicit gap record) committed
- [ ] Suite green (307 baseline, no new skips)
- [ ] Delivery Notice committed; changes on `epic/P9-M31-E31.2`; PR open to `milestone/M31`

---

## Acceptance Criteria

- [ ] Phase criterion "agentic mode carries a paid-vs-local decision mechanism applying the
      recorded policy" met and demonstrated by run evidence or gap record
- [ ] `grep` over `bin/` shows no `gpt-4o`, `claude-3-5-sonnet`, or `qwen2.5-coder:7b`
- [ ] Delivery states plainly that per-task decision logic means epic×execution only
      (P5–P7) — no overclaim of coverage for levels with no dispatch mechanism
- [ ] A local-unavailable run demonstrably produces the documented distinguished behavior

---

## Execution Contract

### Your Responsibilities (Coding Agent)

1. Verify prerequisites — **E31.1 merged is the hard gate**; create `epic/P9-M31-E31.2`
   from `milestone/M31`
2. Read the Epic spec (authoritative) in full
3. Resolve the four Design Decisions
4. Align `DEFAULT_MODELS`; add policy-row traceability to `handle_epic_execution`
5. Implement + test local-unavailable handling
6. Extend the consistency guard to all five keys + policy↔block divergence
7. Produce row-P5 run evidence (real local inference) or the explicit gap record
8. Run the full suite; commit; open PR to `milestone/M31`
9. Produce the Delivery Notice; hand it **by reference**; stop — do not merge

### What You Must NOT Do

- ❌ Merge the PR / infer acceptance
- ❌ Build dispatch for Phase/Milestone/HQ levels — out of scope (no mechanism exists)
- ❌ Touch `chat-hierarchy.md`, the starter templates, or `.ai-project.yml`'s `models:`
  values (already correct)
- ❌ Re-author or re-derive any policy row — cite, never restate or edit
  `model-routing-policy.md`
- ❌ Auto-fallback a local-unavailable task to paid without an explicit per-instance
  choice — that is itself an undeclared mode change
- ❌ Claim run evidence from a mocked model response
- ❌ Echo edited-file bodies or your own Delivery Notice into chat output
- ❌ Iterate without explicit instruction from the parent chat

---

## Reference Materials

- `docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M31-E31.2__spec__agentic-paid-vs-local-decision-logic.md` (authoritative)
- `docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M31__milestone-spec.md` (Epic
  Detail → E31.2; Hard Constraints)
- `docs/phases/P9__Context_Handling_and_Token_Efficiency/P9__phase-spec.md` §P9.2
- E31.1 references (do not re-read the full chain): its
  [Delivery Notice](P9-M31-E31.1__delivery-notice.md) and
  [Demonstration Evidence](P9-M31-E31.1__demonstration-evidence.md) — the
  dispatch-exists-for-Epic-only gap this epic relies on
- `.ai-project/artifacts/reference/token-measurement/model-routing-policy.md` (rows
  P5–P7 + the `.ai-project.yml` mapping table — read-only evidence base)
- `bin/ai-project-orchestrator` (lines 17–23 `DEFAULT_MODELS`; ~320 `handle_epic_execution`;
  ~430 `generate_escalation_report`)
- `bin/run-dev-agent` (exit-code contract, ~line 229)
- `tests/test_model_config.py` (current `epic_dev`-only coverage)
- SN-22: `.ai-project/artifacts/steering-notes/2026-07-17__creation-chat__steering-note__p9-direction.md`
- GitHub issue #126 (GPU contention — reference only)

---

## Canonical Happy Path (reminder — SN-19/§11.6)

Execution → Delivery Notice (by reference) → **[STOP]** → default-accept by silence →
human merge authorization in-chat → merge → stop.

## Exit Conditions

Stop when all deliverables are committed, the PR is open, and the Delivery Notice is
produced. Report: "Epic E31.2 execution complete. Delivery Notice at <path> — <one-line
summary>. Awaiting review (default-accept) and human merge authorization."

## Begin Execution

1. Verify E31.1 is merged (hard gate); branch `epic/P9-M31-E31.2` from `milestone/M31`
2. Read the Epic spec in full
3. Resolve design decisions, align `DEFAULT_MODELS`, add traceability, implement +
   test local-unavailable handling, extend guard tests, produce run evidence, deliver —
   by reference

*(Handoff note: this starter is a committed file, passed to you by path + summary per AOG
§3.1.1. If you are reading it as pasted text in a repo-less context, that is the documented
fallback working as intended.)*
