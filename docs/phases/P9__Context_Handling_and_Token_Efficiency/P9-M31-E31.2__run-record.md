---
type: run-record
epic: E31.2
phase: P9
milestone: M31
status: converged
last_updated: 2026-07-19
---

# Run Record — P9-M31-E31.2-PROVE (Row-P5 Run Evidence)

## What Ran

The proving-vehicle epic `P9-M31-E31.2-PROVE`, executed through `bin/ai-project-orchestrator`'s
real Dev-QA loop, dev role and QA role both on `local:qwen2.5-coder:14b` — this Epic's
policy-correct resolution of rows P6/P7, not the pre-fix `epic_qa: local:qwen2.5-coder:7b`
value E31.1-PROVE's own trigger still names. No mocks, no scripted substitution. Goal:
write a one-line output file, exercising the fixed dispatch path end to end.

## Trigger (verbatim, `.ai-project/queue/04_epic.json`)

```json
{
  "epic_id": "P9-M31-E31.2-PROVE",
  "epic_spec_path": "docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M31-E31.2-PROVE__spec__row-p5-run-evidence.md",
  "sandbox_env": "alpine:latest",
  "dev_command": "./bin/run-dev-agent",
  "validation_command": "test \"$(cat docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M31-E31.2-PROVE__output.md)\" = \"row-p5-run-evidence-E31.2: local lane dispatch verified\"",
  "run_dev_initially": true
}
```

## Execution Route (required disclosure)

**Local-execution fallback, not the Docker sandbox** — the identical, already-documented
route E26.3 and E31.1-PROVE used, for the same reason (this host's Docker cannot reach the
Ollama endpoint or the runner binary). The orchestrator was invoked with a curated `PATH`
(symlinks to `git`, `python3`, and `cat` only — `cat` is what the validation command's
shell needs; its absence in the first two dispatch attempts below produced a false QA
failure that had nothing to do with this Epic's own code, see Attempts) so its own
`subprocess.run(["docker", ...])` raises `FileNotFoundError`, triggering the orchestrator's
already-implemented local-fallback branch:

```bash
env -i HOME="$HOME" USER="$USER" PATH="<curated-dir: git, python3, cat>" \
    LOCAL_AGENT_RUNNER="<wrapper-script>" LANG=C.UTF-8 LC_ALL=C.UTF-8 \
    python3 bin/ai-project-orchestrator
```

`LOCAL_AGENT_RUNNER` pointed at a one-line wrapper (`local-agent-runner` is not installed
as a console script on this host, same as E26.3/E31.1):

```sh
#!/bin/sh
PYTHONPATH="/home/panchew/soft-dev/local-agent-runner" exec /usr/bin/python3 -m local_agent_runner "$@"
```

**Ran against `local-agent-runner`'s `main` branch (commit `860895a`)** — unlike E26.3,
no branch pin was required: `main` already carries `--context` (the dependency that
forced E26.3 onto `milestone/M3`), confirmed via `python3 -m local_agent_runner --help`
before dispatch.

## What This Run Exercises (E31.2's own surface, not a generic re-run of E26.3/E31.1)

- **`DEFAULT_MODELS` alignment:** the dispatch never touched a falsified fallback name —
  confirmed separately by `tests/test_model_config.py`'s CI-time guard, and consistent
  with the "Hybrid Model Mapping" the orchestrator printed at dispatch (`local:qwen2.5-
  coder:14b` for both roles).
- **Policy-traceable resolution:** `dev_model`/`qa_model` came from `handle_epic_execution`'s
  policy-cited lookup (rows P6/P7), not a re-derived value.
- **The local-availability preflight (Hard Constraint 4), confirmed-available branch:**
  `bin/run-dev-agent` probed the real local Ollama endpoint's `/api/tags` before every one
  of the dev invocations below and found `qwen2.5-coder:14b` genuinely pulled — the check
  passed silently each time (no exit-5 anywhere in this record). The local-*un*available
  branch (exit 5, escalation) is covered by real HTTP calls against an unreachable port and
  a stub reporting the tag absent in `tests/integration/test_run_dev_agent_adapter.py`
  (`test_local_unavailable_when_endpoint_unreachable`,
  `test_local_unavailable_when_model_not_pulled`) — deliberately not re-proven here, since
  Ollama was reachable throughout this session's execution window (Non-Goals: a gap record
  is for when it isn't).

## Attempts and Outcomes

Three dispatch attempts were required before clean evidence; the first two failed for a
reason **external to this Epic's code** — a test-harness gap in this run's own curated
`PATH`, not a defect in `handle_epic_execution` or `bin/run-dev-agent`. Recorded for the
same honesty discipline E26.3 set.

| Attempt | Curated `PATH` | Result | Cause |
|---|---|---|---|
| 1 | `git`, `python3` (no `cat`) | All 3 loop attempts: QA exit 1 regardless of dev's exit code (0 or 2) | The QA validation command's `$(cat ...)` found no `cat` on `PATH` inside the local-fallback subprocess — command substitution returned empty, `test` always failed. Confirmed by re-running the validation command manually against the on-disk file, which passed. Not a real QA failure. |
| 2 | `git`, `python3` (no `cat`; DoD text clarified to discourage the read-back loop below) | Same as attempt 1 | Same missing-`cat` cause, isolated by manually verifying file content matched exactly after every dev run. |
| 3 (final) | `git`, `python3`, `cat` added | **Attempt 1/3: QA passed — converged** | `cat` now resolvable; QA's real content check ran correctly. |

A secondary, genuine (not harness-caused) observation surfaced across attempts 1-2's dev
invocations and recurred on the converging attempt: `qwen2.5-coder:14b` frequently
re-issues `write_file` with identical, already-correct content instead of terminating,
exhausting the runner's 10-iteration ceiling (`STATUS_MAX_ITERATIONS_EXCEEDED`, dev exit 2)
even though the file is correct. This is a pre-existing `local-agent-runner`/model
convergence characteristic, not something E31.2's dispatch-fix surface controls or claims
to fix (out of scope: `local-agent-runner` is a read-only reference, per Technical
Constraints).

### Final (converging) attempt — details

- **Dev exit code:** 2 (`STATUS_MAX_ITERATIONS_EXCEEDED`) — the file was written correctly
  well before the 10-iteration ceiling; the model never emitted a clean terminating call.
- **QA (`validation_command`) exit code:** 0 — passed on the first loop attempt.
- **Iterations:** 10 (ceiling reached) · **Tokens:** 837 · **Duration:** 21,978 ms
  (`run-metadata.json`).
- **`transcript.json`'s `final_answer`:** `row-p5-run-evidence-E31.2: local lane dispatch
  verified` — correct prose, present, but (per the transcript's own `status` field)
  reached only after the iteration ceiling, not via a clean stop.
- **`docs/phases/.../P9-M31-E31.2-PROVE__output.md` content (model-produced):**
  `row-p5-run-evidence-E31.2: local lane dispatch verified` — exact match, verified
  byte-for-byte.

**This is the identical structural finding E26.3's run-record already documented**
(`P7-M26-E26.3__run-record.md`, "`final_answer` was explicitly not consulted for the
success determination"): dev's own exit code said "did not converge," yet the task was, in
fact, done correctly, and the QA `validation_command` exit code — the only signal this
Epic's design treats as authoritative — correctly recognized success regardless. E31.2
changes nothing about that principle; this run reconfirms it still holds under the fixed
dispatch path.

## Artifacts (git-tracked)

- `.ai-project/artifacts/agentic-runs/P9-M31-E31.2-PROVE/transcript.json` — the converging
  attempt's full tool-call transcript (runner-produced, passed through unaltered)
- `.ai-project/artifacts/agentic-runs/P9-M31-E31.2-PROVE/context.md` — exact scoped context
  handed to the runner (spec + starter only; governance corpus never included)
- `.ai-project/artifacts/agentic-runs/P9-M31-E31.2-PROVE/run-metadata.json` — adapter's own
  run record (model, runner, endpoint, exit code, timing)
- `docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M31-E31.2-PROVE__output.md` —
  the model-produced deliverable, committed by the orchestrator itself (`5309b0c`), not
  hand-authored
- `docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M31-E31.2-PROVE__spec__row-p5-run-evidence.md`
  and its Epic Execution Chat Starter — the proving vehicle's own declaration
  (`Execution Mode: agentic`)

## Revisit-Trigger Cross-Link

Per `model-routing-policy.md` row P5's revisit trigger ("M31 dual-mode runs at epic
level") — this run, plus E31.1-PROVE's, are two such dual-mode runs now on record; row P5
remains "paid frontier today" pending a Milestone-level (not Epic-level) decision on
whether accumulated local-lane evidence changes that default. Not this Epic's call to make
(Non-Goals: "re-derive, extend, or re-debate M30's policy rows").

## Notes for the record

- The two failed attempts are disclosed above precisely because they could be
  misread as a dispatch-mechanism defect if left out — they were not; both were this
  run's own test-harness gap (`cat` missing from a hand-curated `PATH`), independently
  confirmed by manually re-running the exact validation command against on-disk file
  content after each attempt.
- Six real, non-mocked dev-command invocations are on record across all three attempts
  (two per attempt: initial + on-failure retry, except the converging attempt which
  succeeded on its first loop iteration) — none scripted or substituted.
