---
project: ai-project-system
phase: P9
milestone: M32
epic: E32.3
type: delivery-notice
status: delivered
last_updated: 2026-07-20
---

# Epic Delivery Notice — E32.3

**Epic:** E32.3 — Governance hygiene reconciliation (P8-GH-1, P8-GH-3)
**Branch:** `epic/P9-M32-E32.3` → PR to `milestone/M32`
**Delivered:** 2026-07-20
**Suite:** 363 passed, 0 failed, 0 skipped (baseline 363 — no regressions, no new skips)

---

## Summary

Two P8 documentation debts closed. P8-GH-1: AOG §16.5 and `governance/guides/visual-artifacts.md`
no longer describe this repo as opted out of `visual_artifacts` — both now state the actual state
(`enabled: true` since E29.2, with `test_helper_generates_against_endpoint` running for real
against the live local endpoint). P8-GH-3: the vestigial "Phase Delivery Notice" phrase is purged
from all six pre-P9 planning documents; the P8 closure declaration's carry-forward record is
preserved verbatim. P8-GH-2 is restated below, unresolved, per Hard Constraint 3.

No config, no code, no test changes. Documentation only. E32.1's and E32.2's surfaces untouched.

---

## Prerequisite drift found at execution time (reported, not silently absorbed)

The spec's Grounding (taken at `milestone/M32` @ `1605a8f`) expected the `Phase Delivery Notice`
grep to return **twelve** files. At execution it returned **thirteen**. The two additional files
are this Epic's **own** spec and starter, committed to `milestone/M32` after the grounding grep was
taken — both are P9-own quoted discussion of the carry-forward item, so both correctly fall under
the "do not touch" rule. The six purge targets and the single preserve target matched the spec
exactly. No reconciliation with the parent was required; recorded here for the audit trail.

---

## Acceptance check 1 — P8-GH-1 (literal output)

Command and result **after** the edits:

```
$ grep -rn "stays opted out" governance/
(no matches)

$ grep -rn "opted out|opt-out" governance/ --include=*.md | grep -i "source repo|this repo|source repository"
(no normative stale hits)
```

**One remaining hit exists and is deliberately preserved.** `governance/AI-OPERATING-GUIDELINES.md`
line 1045 restates the old opt-out rationale inside the **`## Changelog`** section — the historical
v2.7.0 entry recording what that version changed at the time. It is preserved for the same reason
the P8 closure declaration is: it is a dated historical record, not live guidance, and rewriting it
would falsify the version history. The Epic spec's own Technical Constraints scope AOG edits to
"§16.5 only (do not edit other AOG sections)", which independently places the changelog out of
scope. **Flagging it explicitly** because the DoD is worded as "returns nothing" — every *normative*
governance surface returns nothing, but a literal repo-wide grep on the phrase still shows this one
changelog line. If the Milestone Chat reads the DoD as requiring the changelog entry to change too,
that is a parent-chat call and a one-line follow-up; this Epic did not make it unilaterally.

## Acceptance check 2 — P8-GH-3 (literal output)

```
$ grep -rln "Phase Delivery Notice" docs/ governance/templates/
docs/phases/P8__Visual_Artifacts_Activation/P8__phase-closure-declaration.md
docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M32-E32.3__delivery-notice.md
docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M32-E32.3__epic-execution-chat-starter.md
docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M32-E32.3__spec__governance-hygiene-reconciliation.md
docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M32__milestone-execution-chat-starter.md
docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M32__milestone-spec.md
docs/phases/P9__Context_Handling_and_Token_Efficiency/P9__phase-execution-chat-starter.md
docs/phases/P9__Context_Handling_and_Token_Efficiency/P9__phase-spec.md
```

All six pre-P9 purge targets are gone. What remains is exactly the intended set: the P8 closure
declaration (the preserved carry-forward record) plus P9's own planning documents, all
quoted-discussion-only. `governance/templates/` returned no hits at any point — no template carried
the copy-paste risk. Note that **this Delivery Notice is itself in that list** — it quotes the phrase
to report on the purge, which is the same quoted-discussion category as the spec and starter, not a
live requirement statement. The output above is the grep re-run against the final committed tree.

---

## Changes

**P8-GH-1 (2 files)**

- `governance/AI-OPERATING-GUIDELINES.md` §16.5, third bullet — the "stays opted out because…"
  framing removed entirely, not hedged. The universal rule (no project commits generated binaries)
  is retained and now stated as holding *independently* of whether a project has the capability
  enabled, with this repo cited as an enabled project that still commits no generated output.
- `governance/guides/visual-artifacts.md` line ~48 callout — same correction.
- `governance/guides/visual-artifacts.md` §6 "Testing without a live endpoint" — **reframed, not
  deleted.** The no-live-endpoint guidance is preserved and now split into the two paths that
  actually exist in `tests/integration/test_visual_artifacts_helper.py`: the `enabled: false` skip
  (accurate for other projects) and `AI_PROJECT_SKIP_LIVE_ENDPOINT_TESTS=1` (for contributors/CI on
  a project that has it on). A closing sentence states that in *this* repo the test runs for real by
  default. Both mechanisms were verified against the test source, not assumed.

**P8-GH-3 (6 files, 9 occurrences)**

- `P6-M25__milestone-spec.md` (×2), `P6__phase-execution-chat-starter.md`,
  `P7-M28__milestone-spec.md` (×2), `P8-M29__milestone-spec.md` (×2),
  `P8__phase-execution-chat-starter.md` — the phrase replaced by PSG §5C's real closing artifact,
  the **Phase Closure Declaration**.
- `P5-M22__milestone-spec.md` (×1) — handled differently on purpose. P5 **did** produce a genuinely
  separate `P5__phase-delivery-notice.md`, which still exists in the repo (verified). Replacing that
  reference with "Phase Closure Declaration" would have made a true historical statement false, so
  the sentence now cites the actual file by path and notes that §5C later canonized the Phase
  Closure Declaration. Accurate, and not a grep match.

---

## P8-GH-2 — restated, not resolved (Hard Constraint 3)

> **P8-GH-2 — Machine-local hosting limitation (Low).** The storage backend provisioned to complete
> E29.3's bindings is local only — `file://` links resolve on this machine and nowhere else (dead on
> GitHub, dead for any other reader). **Accepted by CFO ruling for this phase; revisit only if
> cloud-reachable hosting is ever actually needed for this project.**

Status unchanged: **deferred**, on its recorded trigger. Source record
(`docs/phases/P8__Visual_Artifacts_Activation/P8__phase-closure-declaration.md`, Carry-Forward row
P8-GH-2) is untouched. This restatement is reachable from the M32 Milestone Closure Declaration via
this Delivery Notice; it was not silently dropped, and no hosting work was scoped in.

---

## Definition of Done

- [x] P8-GH-1 stale-opt-out framing gone from all normative `governance/` surfaces (one historical
      changelog line preserved and flagged above)
- [x] "Phase Delivery Notice" grep returns only the P8 closure declaration's carry-forward record
      plus P9's own quoted discussion
- [x] P8-GH-2's deferred status + trigger restated and locatable
- [x] Suite green — 363 passed, no new skips
- [x] Delivery Notice committed; changes on `epic/P9-M32-E32.3`; PR open to `milestone/M32`

## Acceptance Criteria

- [x] Both grep checks pass cleanly (with the changelog caveat surfaced, not buried)
- [x] No historical record altered — `P8__phase-closure-declaration.md` shows zero diff
- [x] `visual-artifacts.md` §6's no-live-endpoint guidance remains present and accurate, and is now
      more accurate than before (both real skip paths documented)

---

**Awaiting review (default-accept, PSG §11.6 / SN-19) and human merge authorization.**
