# Epic Execution Chat Starter — E30.4

**Epic:** E30.4 — Reference-don't-display reconciliation (SN-23)
**Phase:** P9 — Context Handling and Token Efficiency
**Milestone:** M30 — Token Measurement & Model-Tier Audit
**Repository:** panchew/ai-project-system
**Branch Strategy:** `epic/P9-M30-E30.4` → PR to `milestone/M30`

> **Provenance:** Authored by the Milestone Chat (P9-M30) on 2026-07-18 under P9 phase
> spec **v1.1.0** (mid-flight amendment `e490394` executing HQ's SN-23 triage). Sequenced
> **after E30.3's merge** — hard prerequisite (shared template surfaces). This starter
> was handed to you **by reference** (SN-23 Decision 1, practiced ahead of codification);
> you are reading the committed file, which was always the record.

---

## Governance References

You are operating under the AI Project System governance framework (Epic mode).

**Authoritative governance documents:**
- PROJECT-SYSTEM-GUIDELINES.md v2.3.0
- AI-OPERATING-GUIDELINES.md v2.9.0 (your primary edit target — §3.1.1)
- P9 phase spec v1.1.0 (the amendment that created this epic)

**Governance hierarchy:** PSG > AOG > this Starter > Epic Spec > execution decisions >
system references > chat.

**Critical rules:**
- Documentation is authoritative; chat is ephemeral
- Follow the §1A canonical happy path for Epic closure
- Produce a Delivery Notice upon completion — **delivered by reference** (path +
  one-line summary in chat), practicing this epic's own rule
- Stop after PR creation — **do not merge.** Under default-accept (PSG §11.6 / SN-19) a
  clean delivery is accepted by silence; the PR **merge** requires explicit human
  authorization, given in-chat
- Accept/reject decisions belong to the parent chats (human-backed), not to you

---

## Epic Specification

**Full spec:** `docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M30-E30.4__spec__reference-dont-display.md`
**Commit:** committed alongside this starter on `milestone/M30` — resolve with
`git log --oneline -1 -- <spec path>`.

### Prerequisite Verification (do this first)

- `git ls-files --error-unmatch <spec path>` — spec git-tracked (GH-1).
- **E30.3 merged is the hard gate:** confirm E30.3's PR has merged to `milestone/M30`
  (its scoping standard + template edits present). **If not merged, STOP — do not
  branch; report to the parent chat.** A parallel branch on the shared template files
  guarantees conflicts.
- Confirm the echo-surface anchors (all verified 2026-07-18; E30.3's merge may shift
  template line numbers — re-verify): AOG §3.1.1 (~lines 90–125) fenced-block mandate +
  "Copy the entire chat starter…" convention;
  `governance/templates/{epic,milestone,phase}-execution-chat-starter.md` boilerplate
  (lines 23/24/24 pre-E30.3); `governance/systems/artifact-communication-protocol.md:408`
  paste mandate (line 14 = the original transcription-error problem);
  `governance/EPIC-EXECUTION-CHAT-STARTER.md:83` "paste it into the Milestone Chat".
- SN-23 on master:
  `.ai-project/artifacts/steering-notes/2026-07-18__creation-chat__steering-note__reference-dont-display.md`.
- Suite collects 307 tests; `grep -rn "Copy the entire chat starter" tests/` to find any
  template-text assertions before editing.

### Summary

Implement SN-23's two ratified decisions across the three governance surfaces that still
mandate artifact-body echo: AOG §3.1.1 (+ the three starter templates), the protocol's
§Integration with Manual Mode, and EPIC-EXECUTION-CHAT-STARTER's paste line. Result:
reference-first handoff (path + one-line summary; IDE-attach where available) is
canonical; paste survives as the documented repo-less fallback; a producer/consumer
no-echo rule exists in one home; before/after per-handoff numbers come from E30.1's
mechanism with SN-23's honest bound stated.

### Binding constraints (carry into every decision)

- **SN-23 Decisions 1–2 verbatim:** disk artifacts pass by reference, never by paste or
  full-body display; paste remains documented as the repo-less fallback. Both are
  CFO-ratified — implement, don't re-debate; deleting the fallback fails review.
- **Hard Constraint** (Milestone spec, embedded verbatim in the Epic spec — read it
  there): every claim measured or gap-recorded; for this epic, before/after =
  mechanism-tokenized real M30 echo bodies vs the reference form, **honest bound
  stated** (ingestion once, selectively — not never), G10 error bars, billed-median
  effects forward-looking only.
- **One rule, cited everywhere** — write the no-echo rule once in its decided home;
  every edited surface gets a sentence + pointer, not a paraphrase.
- **Preserve E30.3's landed template content** — rebase your handoff edits on it; do
  not rework its load-list changes.
- **No code, no PSG, no `.ai-project.yml`, no schema/acceptance-model changes.**

### Design Decisions (yours to resolve — decide, document, move)

1. **No-echo rule's home** (AOG §3.1.1 generalized / new AOG subsection / protocol doc).
2. **Canonical reference format** (path + one line: type, id, status) — defined once.
3. **Evidence-note location** (rec:
   `.ai-project/artifacts/reference/token-measurement/echo-cost-note.md`, or a section
   in E30.3's scoping standard if it fits).
4. **Version bumps** per each document's own convention (AOG v2.9.0 → likely v2.10.0).

---

## Deliverables

1. [ ] AOG §3.1.1: reference-first mandate, fenced form demoted to repo-less fallback;
       version bump + changelog
2. [ ] Three starter templates' boilerplate → reference handoff + fallback wording
       (rebased on E30.3)
3. [ ] Protocol §Integration with Manual Mode → reference canonical, paste fallback;
       version row
4. [ ] `governance/EPIC-EXECUTION-CHAT-STARTER.md:~83` → reference handoff
5. [ ] No-echo rule (producer + consumer halves) in its home, canonical format defined
6. [ ] Before/after evidence note via E30.1's mechanism (honest bound, G10 bars)
7. [ ] Full test suite passes (307 baseline, no new skips; template assertions updated
       coherently)
8. [ ] Epic Delivery Notice
       (`docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M30-E30.4__delivery-notice.md`)
       — **delivered by reference**

---

## Definition of Done

- [ ] No governance surface still mandates full-body echo of a committed artifact
      (phase spec v1.1.0 acceptance criterion)
- [ ] Paste documented as repo-less fallback on every surface that previously mandated
      it
- [ ] One coherent no-echo rule + reference format; all surfaces cite it
- [ ] Mechanism-produced before/after numbers committed, honest bound stated
- [ ] Version bumps + changelog rows per document convention
- [ ] Suite green (307 baseline, no new skips)
- [ ] Delivery Notice committed; changes on `epic/P9-M30-E30.4`; PR open to
      `milestone/M30`

---

## Acceptance Criteria

- [ ] Post-E30.4 governance, read alone, tells a parent chat to hand a committed
      starter by path + one-line summary — and tells a repo-less adopter where the
      paste fallback lives
- [ ] `grep` finds no surviving unconditional paste/display mandate outside marked
      fallback context
- [ ] The evidence note quantifies eliminated per-handoff duplication with the SN-23
      honest bound
- [ ] E30.3's template content preserved (rebase, not rework)

---

## Execution Contract

### Your Responsibilities (Coding Agent)

1. Verify prerequisites — **E30.3 merged is the hard gate**; then create
   `epic/P9-M30-E30.4` from `milestone/M30`
2. Read the Epic spec (authoritative) + SN-23; check template-text test assertions
3. Resolve the four Design Decisions; write the no-echo rule once
4. Edit the four surfaces + three templates coherently; bump versions per convention
5. Produce the before/after evidence note from E30.1's mechanism
6. Run the full suite; commit; open PR to `milestone/M30`
7. Produce the Delivery Notice; hand it **by reference**; stop — do not merge

### What You Must NOT Do

- ❌ Merge the PR / infer acceptance
- ❌ Delete or bury the paste fallback (SN-23 Decision 2 is ratified)
- ❌ Echo edited-file bodies or your own Delivery Notice into chat output — practice
  the rule
- ❌ Rework E30.3's template content, touch PSG/`.ai-project.yml`/`bin/`, or change
  schemas/acceptance flow
- ❌ Claim billed savings as achieved; skip or route around template-text tests
- ❌ Iterate without explicit instruction from the parent chat

---

## Reference Materials

- `docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M30-E30.4__spec__reference-dont-display.md` (authoritative)
- SN-23: `.ai-project/artifacts/steering-notes/2026-07-18__creation-chat__steering-note__reference-dont-display.md`
- P9 phase spec v1.1.0 (E30.4 entry, acceptance criterion, triage row)
- Edit surfaces: `governance/AI-OPERATING-GUIDELINES.md` §3.1.1;
  `governance/templates/{epic,milestone,phase}-execution-chat-starter.md`;
  `governance/systems/artifact-communication-protocol.md` (lines 14, ~408);
  `governance/EPIC-EXECUTION-CHAT-STARTER.md` (~83)
- Evidence: `.ai-project/artifacts/reference/token-measurement/` (report §2.1–2.2,
  README/G10, `bin/measure-token-burn`, E30.3's scoping standard once merged)

---

## Canonical Happy Path (reminder — SN-19/§11.6)

Execution → Delivery Notice (by reference) → **[STOP]** → default-accept by silence →
human merge authorization in-chat → merge → stop.

## Exit Conditions

Stop when all deliverables are committed, the PR is open, and the Delivery Notice is
produced. Report: "Epic E30.4 execution complete. Delivery Notice at <path> —
<one-line summary>. Awaiting review (default-accept) and human merge authorization."

## Begin Execution

1. Verify E30.3 is merged (hard gate); branch `epic/P9-M30-E30.4` from `milestone/M30`
2. Read the Epic spec + SN-23; verify anchors post-E30.3
3. Edit, measure, test, deliver — by reference

*(Handoff note: this starter is a committed file — per SN-23 it is passed to you by
path + summary, not pasted. If you are reading it as pasted text in a repo-less
context, that is the documented fallback working as intended.)*
