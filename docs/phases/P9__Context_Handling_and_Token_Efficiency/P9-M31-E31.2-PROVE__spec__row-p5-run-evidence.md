---
project: ai-project-system
phase: P9
milestone: M31
epic: E31.2-PROVE
type: spec
status: proving-vehicle
last_updated: 2026-07-19
---

# P9-M31-E31.2-PROVE — Row-P5 run evidence (proving vehicle, not a real epic)

**Not a scoped deliverable epic.** This is a minimal proving vehicle, in the tradition of
`P7-M26-E26.3-PROVE` and `P9-M31-E31.1-PROVE`, whose sole purpose is to be dispatched for
real through `bin/ai-project-orchestrator`'s Dev-QA loop — now carrying E31.2's
policy-traceable `dev_model`/`qa_model` resolution and local-unavailable preflight check —
so that Deliverable 5 (row-P5 run evidence) is a real, non-mocked run rather than a
described hypothetical.

This run exercises the fixed dispatch path itself: `DEFAULT_MODELS` aligned with the
policy, `dev_model` resolved via `handle_epic_execution`'s policy-traceable lookup
(citing P6), and `bin/run-dev-agent`'s new local-availability preflight passing silently
against a genuinely reachable Ollama endpoint with the resolved model actually pulled —
the "local loadability never assumed, and here it's confirmed" case (Hard Constraint 4).

## Definition of Done

- [ ] Create a file at
      `docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M31-E31.2-PROVE__output.md`
      whose content is exactly one line: `row-p5-run-evidence-E31.2: local lane dispatch verified`.
      Make exactly one `write_file` call with that content, then give your final answer
      immediately. Do not read the file back to verify it, and do not write it more than once.
