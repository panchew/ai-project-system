# Epic Execution Chat Starter — E30.3

**Epic:** E30.3 — Evidence-driven context-load reduction
**Phase:** P9 — Context Handling and Token Efficiency
**Milestone:** M30 — Token Measurement & Model-Tier Audit
**Repository:** panchew/ai-project-system
**Branch Strategy:** `epic/P9-M30-E30.3` → PR to `milestone/M30`

> **Provenance:** Authored by the Milestone Chat (P9-M30) on 2026-07-17, after E30.1's
> and E30.2's acceptance and merge — this spec was deliberately deferred until the
> measurement evidence existed, and its extent is sized by that evidence (the spec's
> Sizing Decision section is the record). Last epic in the M30 chain.

---

## Governance References

You are operating under the AI Project System governance framework (Epic mode).

**Authoritative governance documents:**
- PROJECT-SYSTEM-GUIDELINES.md v2.3.0
- AI-OPERATING-GUIDELINES.md v2.9.0
- ai-project-yml-spec.md v2.4.0 (bumped by E30.2)

**Governance hierarchy:** PSG > AOG > this Starter > Epic Spec > execution decisions >
system references > chat.

**Critical rules:**
- Documentation is authoritative; chat is ephemeral
- Follow the §1A canonical happy path for Epic closure
- Produce a Delivery Notice upon completion
- Stop after PR creation — **do not merge.** Under default-accept (PSG §11.6 / SN-19) a
  clean delivery is accepted by silence (no Review Decision on the happy path); the PR
  **merge** requires explicit human authorization, given in-chat (no Delivery
  Authorization artifact)
- Accept/reject decisions belong to the parent chats (human-backed), not to you

---

## Epic Specification

**Full spec:** `docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M30-E30.3__spec__context-load-reduction.md`
**Commit:** committed alongside this starter on `milestone/M30` — resolve the exact hash
with `git log --oneline -1 -- <spec path>` before relying on it.

### Prerequisite Verification (do this first)

- `git ls-files --error-unmatch <spec path>` — confirm the spec is git-tracked (GH-1).
- Confirm both prerequisite epics are merged and tracked:
  `git ls-files --error-unmatch .ai-project/artifacts/reference/token-measurement/token-burn-dataset.json`
  and `... /audit-report.md` and `... /model-routing-policy.md`. **If any is absent,
  STOP — the sizing evidence is not on the branch.**
- Confirm the grounding anchors: dataset §3 per-call medians (epic 76,135 → phase
  169,003) and §4 packs (29–37K); report §4 findings 7–9; `.ai-project.yml:3` still
  cites v2.3.1 (your hygiene fix target); `governance/templates/` exists; suite
  collects 307 tests. If any anchor has drifted, re-verify before editing — the spec's
  grounding is dated 2026-07-17.

### Summary

Deliver the measured slice of context-load reduction: a per-level context-scoping
standard (what each chat level should load and why), implemented pack reduction at
minimum for the Milestone and Phase levels with **before/after numbers from E30.1's
mechanism**, and a committed bounded-scope finding handing the levers documents cannot
pull (mixed-session length, history/tool-result/harness share) to M31 and the Phase Chat
as recommendations. Plus one authorized hygiene line: `.ai-project.yml:3` → v2.4.0.

### Binding constraints (carry into every decision)

**Hard Constraint (verbatim from the Milestone spec):**

> **The frontier-vs-local policy and the `models:` refresh MUST be derived from the
> captured measurements — not from pre-existing assumptions.** Real captured data is the
> bar; estimates do not count (phase acceptance criterion). If a measurement cannot be
> captured for some level or task type, **record the gap explicitly** in the report
> rather than substituting a guess — a policy row grounded in a recorded gap is
> acceptable; a policy row grounded in an unlabeled assumption is not. This constraint
> governs E30.2 directly and sizes E30.3: no reduction work may be scoped from the prior
> 24K/157K estimates alone.

- **Every reduction claim shows before/after numbers from E30.1's mechanism** (Milestone
  spec, verbatim). Pack-level claims only; billed-median improvement is framed as
  forward-looking, never as achieved.
- **The sizing decision is fixed in the Epic spec** (bounded middle path: governance-pack
  slice implemented, non-governance share documented). Deviations are recorded against
  it in the Delivery Notice with evidence — never silently.
- **Measure composition before cutting** — cut lists derive from per-component numbers,
  not intuition.
- **G10 error bars stated** wherever proxy counts appear; proxy is never presented as
  billed.
- **PSG/AOG normative content untouched**; `.ai-project.yml` untouched beyond line 3.

### Design Decisions (yours to resolve — decide, document, move; do not escalate to ask)

1. **Scoping-standard home** — recommended
   `.ai-project/artifacts/reference/token-measurement/context-scoping.md` (keeps the
   E30.1→E30.3 evidence chain in one directory); your call, documented and cross-linked.
2. **Reduction lever per level** — candidates in rough leverage order: trim what
   starters instruct chats to load; section-targeted reads instead of whole documents;
   deduplicate starter↔spec verbatim embedding (measure it first — likely the biggest
   single win). Justify each choice against your measured composition table.
3. **Before/after presentation** — extend `bin/measure-token-burn`'s pack definitions to
   the new scoping (documented manifest change; regenerate + commit the dataset files in
   the same delivery; mechanism contract otherwise unchanged) **or** a separate
   committed before/after table citing the mechanism. Either way: reproducible by rerun.

---

## Deliverables

1. [ ] **Per-level context-scoping standard** (home per Design Decision 1): load /
       don't-load / why per level, with the per-component composition table as its
       evidence
2. [ ] **Implemented pack reduction** for at least Milestone + Phase levels, applied to
       the surfaces the standard names (starter templates / load-list prose)
3. [ ] **Before/after pack measurements** from E30.1's mechanism (Direction B),
       committed and reproducible
4. [ ] **Bounded-scope finding**: what share of per-call context lies beyond document
       control, with recommendations addressed to M31/Phase Chat (session hygiene per
       G7/§2.2; segmentation as future measurement work) — findings, not implemented
       work
5. [ ] **Hygiene:** `.ai-project.yml:3` cites v2.4.0 (one line, comment only)
6. [ ] Full test suite passes (307 baseline, no new skips)
7. [ ] **Epic Delivery Notice**
       (`docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M30-E30.3__delivery-notice.md`)

---

## Definition of Done

- [ ] The delivered extent matches the spec's recorded Sizing Decision (or the Delivery
      Notice documents the evidence-backed deviation)
- [ ] Scoping standard + implemented milestone/phase reduction + bounded-scope finding
      delivered
- [ ] Every reduction claim backed by before/after numbers from the E30.1 mechanism
- [ ] `.ai-project.yml:3` cites v2.4.0
- [ ] Full test suite passes (307 baseline, no new skips)
- [ ] Delivery Notice committed; all changes on `epic/P9-M30-E30.3`; PR opened to
      `milestone/M30`

---

## Acceptance Criteria

- [ ] Either measurable context-load reduction exists with before/after evidence, or the
      repo records why none was justified — **no third state** (Milestone spec verbatim)
- [ ] The scoping standard answers "what should a chat at level X load, and why" for
      every level, from committed analysis — or records the gap
- [ ] No claim exceeds its evidence: pack numbers measured (with G10 error bars);
      billed-median improvement forward-looking only

---

## Technical Constraints

- **In scope:** the scoping standard; `governance/templates/` starter templates +
  load-list prose where the lever targets them; `bin/measure-token-burn` pack
  definitions iff Design Decision 3 takes that route (+ regenerated dataset, same
  delivery); `.ai-project.yml` line 3 only.
- **Do NOT touch:** PSG/AOG normative content (content-neutral verbatim relocation with
  redirects is the only sanctioned exception, and the Delivery Notice must prove
  neutrality); `.ai-project.yml` beyond line 3; `bin/run-dev-agent`;
  `bin/ai-project-orchestrator` (`DEFAULT_MODELS` is M31's task); the E30.2
  policy/report files (cross-links beside them are fine); M30's already-delivered
  session starters beyond the lever's named surfaces.

---

## Execution Contract

### Your Responsibilities (Coding Agent)

1. Create branch `epic/P9-M30-E30.3` from `milestone/M30`
2. Verify prerequisites (spec + both prerequisite epics' artifacts git-tracked; anchors
   as cited)
3. **Measure first:** build the per-level pack-composition table (per-component proxy
   counts) — the cut list falls out of it
4. Write the scoping standard (Design Decision 1); pick and justify the lever per level
   (Design Decision 2)
5. Implement the reduction on the named surfaces; produce before/after numbers (Design
   Decision 3)
6. Write the bounded-scope finding with its M31/Phase recommendations
7. Apply the `.ai-project.yml:3` hygiene line
8. Run the full suite; commit to `epic/P9-M30-E30.3`; open a PR to `milestone/M30`
9. Produce the Epic Delivery Notice; stop — **do not merge** (human merge authorization
   is in-chat, SN-19/§11.6)

### What You Must NOT Do

- ❌ Merge the PR / infer acceptance
- ❌ Claim any reduction without mechanism-produced before/after numbers, or present
  proxy counts as billed counts
- ❌ Cut content before measuring composition, or resize the epic's extent without
  recording the evidence-backed deviation
- ❌ Rewrite PSG/AOG normative content, build retrieval/caching infrastructure, extend
  segmentation into `bin/measure-token-burn`, or touch the orchestrator
- ❌ Touch `.ai-project.yml` beyond line 3
- ❌ Iterate without explicit instruction from the parent chat

---

## Reference Materials

- `docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M30-E30.3__spec__context-load-reduction.md` (your Epic spec — authoritative; Sizing Decision section)
- `docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M30__milestone-spec.md` (Epic Detail → E30.3; Hard Constraint; no-third-state criterion)
- `.ai-project/artifacts/reference/token-measurement/` — dataset (§3 medians, §4 packs),
  audit report (§4 findings 7–9, §2.2, §8), README (G10 error bars, mechanism)
- `bin/measure-token-burn` (Direction B pack definitions — Design Decision 3 surface)
- `governance/templates/` (starter templates — the inheritance surface)
- `.ai-project.yml` line 3 (hygiene target)
- E30.1/E30.2 Delivery Notices (blind spots and carried observations)
- SN-22: `.ai-project/artifacts/steering-notes/2026-07-17__creation-chat__steering-note__p9-direction.md`

---

## Canonical Happy Path (reminder — SN-19/§11.6)

Execution → Delivery Notice → **[STOP]** → default-accept by silence (Review Decision =
exception path only) → **human merge authorization in-chat** → merge → stop. **You own
the execution and the Delivery Notice; then you stop.**

## Epic Delivery Notice Template

**File:** `docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M30-E30.3__delivery-notice.md`

```markdown
---
type: delivery-notice
epic: E30.3
status: delivered
delivery_date: <YYYY-MM-DD>
---

# Epic Delivery Notice — E30.3

**Epic:** E30.3 — Evidence-driven context-load reduction
**Status:** Execution Complete, Delivered for Review
**Delivery Date:** <YYYY-MM-DD>

## Execution Summary
[Composition findings; levers chosen per level + why; before/after numbers (with G10
error bars); scoping-standard home; the bounded-scope finding's headline; extent vs the
spec's Sizing Decision (match or documented deviation)]

## Deliverables Completed
- [ ] Scoping standard committed (location: …) with composition table
- [ ] Pack reduction implemented (milestone + phase minimum; surfaces: …)
- [ ] Before/after measurements committed (method: …) — reproducible
- [ ] Bounded-scope finding + M31/Phase recommendations
- [ ] .ai-project.yml:3 → v2.4.0
- [ ] Suite green (307 baseline, no new skips)
- [ ] Delivery Notice

## Definition of Done Status
[Confirm each — incl. extent-vs-sizing-decision and claims-vs-evidence checks]

## Branch and PR Information
- **Branch:** `epic/P9-M30-E30.3`
- **PR:** [Link]
- **Target:** `milestone/M30`

## Notes
[New gap records (if any, continuing the G-series); recommendations recorded but not
implemented; anything the Milestone Closure Declaration should carry]

## Awaiting Review
Default-accept (PSG §11.6): clean delivery accepted by silence; merge awaits explicit
human authorization in-chat (SN-19).

---
**Coding Agent stopped per AI-OPERATING-GUIDELINES.md canonical happy path.**
```

---

## Exit Conditions

Stop when all deliverables are committed, the PR is open to `milestone/M30`, and the
Delivery Notice is produced. Then report: "Epic E30.3 execution complete. Epic Delivery
Notice produced. Awaiting review (default-accept) and human merge authorization."

## Begin Execution

1. Create branch `epic/P9-M30-E30.3` from `milestone/M30`
2. Verify prerequisites (both prerequisite epics' artifacts on the branch is the hard
   gate); read the Epic spec — especially its Sizing Decision — and report §4
3. Measure composition; write the standard; implement; measure again; open the PR;
   deliver

Copy the entire chat starter above and paste into the Governance Agent (Epic mode) to begin execution.
