---
type: delivery-notice
epic: E30.1
status: delivered
delivery_date: 2026-07-17
---

# Epic Delivery Notice — E30.1

**Epic:** E30.1 — Token-burn instrumentation
**Status:** Execution Complete, Delivered for Review
**Delivery Date:** 2026-07-17

## Execution Summary

**Mechanism chosen: hybrid A + B + C-lite** (the spec's anticipated honest answer,
confirmed by probing):

- **(A) Harness usage logs** for paid spend: `bin/measure-token-burn` parses the 72
  Claude Code session JSONL files on the CFO's machine (window 2026-06-12 → 2026-07-17),
  deduplicates usage by API message id (Claude Code writes one line per content block —
  verified 43 lines → 17 messages in a sample; counting lines would double-bill),
  attributes each session to chat level and task type by documented deterministic rules
  (starter-paste markers first, git-branch fallback flagged lower-confidence;
  role/artifact-write signals for task type), and commits aggregates only.
- **(B) Corpus tokenization** for governance overhead: PSG/AOG/yml-spec, the current
  per-level starters+specs, and `governance/` directory totals, tokenized with tiktoken
  `o200k_base` as a documented proxy (error bars recorded, G10).
- **(C-lite) Local-run extraction**: ollama output tokens (404) extracted from the
  committed `P7-M26-E26.3-PROVE` transcript; its `context.md` proxy-tokenized (671).
  `bin/run-dev-agent` was **deliberately not modified** — forward instrumentation is
  recorded as future work rather than risking the runner's contract for zero backward
  gain; its exit-code/artifact-path contract is untouched by construction.

**Dataset location (stable, documented):**
`.ai-project/artifacts/reference/token-measurement/` — `README.md` (mechanism doc),
`token-burn-dataset.json` (machine-readable, E30.2's input), `token-burn-dataset.md`
(human-readable). Both dataset files are generated by the committed script.

**Matrix coverage:** all 20 level × task-type cells accounted for — measured cells:
epic×execution (38 sessions), milestone×planning (2), milestone×review (1),
phase×planning (1), hq×planning (3); plus level totals for creation (5 sessions) and
mixed/unattributed buckets reported whole. Every other cell carries a numbered record:
**structural** (G1 epic planning/review live in parent chats; G3 only Epic chats
execute) or **gap** (G2 epic closure bundled in execution; G4–G6 review/closure
embedded in mixed sessions at milestone/phase/HQ, creation task-split invisible to
repo-write signals; G7 mixed-session split; G8 this-machine-only coverage; G9 local
prompt tokens unrecorded by the runner; G10 tokenizer-proxy error bars). Paid vs local
spend are separate sections and never merged; the by-model table gives the frontier
mix; billed context-load medians (first-turn and per-call) are provider-reported
numbers.

## Deliverables Completed

- [x] Mechanism documented (reasoning + blind spots) — token-measurement `README.md`;
      code committed and runnable (`bin/measure-token-burn`, stdlib + optional tiktoken)
- [x] Dataset committed (location: `.ai-project/artifacts/reference/token-measurement/`)
      — matrix, paid/local split, corpus overhead separable
- [x] Gap records for every uncaptured cell (G1–G10, in the dataset itself)
- [x] Suite green (307 passed, no new skips; `bin/run-dev-agent` untouched)
- [x] Delivery Notice (this document)

## Definition of Done Status

- **Mechanism documented with design reasoning and blind spots** — done (README:
  decision rationale per direction, attribution rules, dedup rule, six blind-spot
  entries).
- **Real captured data covering the matrix, corpus overhead separable** — done: every
  number traces to provider-reported usage blocks, the runner transcript, or a labeled
  proxy tokenization; corpus overhead is its own section (per-level packs + billed
  context medians). No cell derives from the prior 24K/157K estimates.
- **Every uncaptured cell recorded as an explicit gap** — done (G1–G10; the matrix
  renders the gap id in each empty cell).
- **Location stable, documented, consumable by E30.2** — done; README's "Contract with
  E30.2" section maps each Milestone-spec audit question to dataset sections, verified
  question-by-question.
- **Full test suite passes** — 307 passed, 0 failed, no new skips.
- **Delivery Notice committed; changes on `epic/P9-M30-E30.1`; PR open to
  `milestone/M30`** — this commit + PR below.

## Branch and PR Information

- **Branch:** `epic/P9-M30-E30.1`
- **PR:** [#136](https://github.com/panchew/ai-project-system/pull/136)
- **Target:** `milestone/M30`

## Notes

Blind spots E30.2 must know (full list in README): attribution is rule-inferred, not
labeled at source — every dataset row records which rule fired, and branch-fallback
rows are lower-confidence; sessions are the attribution atom (no within-session
segmentation — that is what makes G2/G4/G7 gaps rather than numbers); coverage is this
machine's harness directory only (any claude.ai-web Creation/HQ work is invisible,
G8); Direction B numbers are o200k proxy values for proportions, while §§1–3 paid
numbers are exact billed counts; the capture includes the live E30.1 session itself,
so a recapture moves recent rows slightly. No pacing-driven gaps occurred: the paid
capture measured already-existing sessions only, so the CFO-pacing valve was never
needed and no new paid spend was initiated by this Epic.

## Awaiting Review

Default-accept (PSG §11.6): clean delivery accepted by silence; merge awaits explicit
human authorization in-chat (SN-19).

---
**Coding Agent stopped per AI-OPERATING-GUIDELINES.md canonical happy path.**
