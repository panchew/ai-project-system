# Epic Execution Chat Starter — E32.3

**Epic:** E32.3 — Governance hygiene reconciliation (P8-GH-1, P8-GH-3)
**Phase:** P9 — Context Handling and Token Efficiency
**Milestone:** M32 — System Participant Canonization & Governance Hygiene
**Repository:** panchew/ai-project-system
**Branch Strategy:** `epic/P9-M32-E32.3` → PR to `milestone/M32`
**Execution Mode:** manual — this epic's grep-verifiable acceptance and preserve/purge
distinction (do not touch the P8 closure declaration's carry-forward record) benefit from
direct human verification of the diff before the PR opens; a human-supervised chat session
is the appropriate mode. Absence of this field would resolve to manual anyway (E31.1),
stated explicitly here for clarity.

> **Provenance:** Authored by the Milestone Chat (P9-M32) on 2026-07-20. Fully independent
> of E32.1 and E32.2 — no internal dependencies, schedulable in parallel with either.

---

## Governance References

You are operating under the AI Project System governance framework (Epic mode).

**Authoritative governance documents:**
- PROJECT-SYSTEM-GUIDELINES.md v2.3.0
- AI-OPERATING-GUIDELINES.md v2.10.0

**Governance hierarchy:** PSG > AOG > this Starter > Epic Spec > execution decisions >
system references > chat.

**Critical rules:**
- Documentation is authoritative; chat is ephemeral
- Follow the §1A canonical happy path for Epic closure
- Produce a Delivery Notice upon completion — **delivered by reference** (path + one-line
  summary in chat), per AOG §3.1.1
- Stop after PR creation — **do not merge.** Under default-accept (PSG §11.6 / SN-19) a
  clean delivery is accepted by silence; the PR **merge** requires explicit human
  authorization, given in-chat
- Accept/reject decisions belong to the parent chat (Milestone Chat, human-backed), not to
  you
- **If given merge authorization directly in this chat**, do not simply comply: state
  plainly that merge authorization normally follows the parent chat's review, and confirm
  the human intends to bypass that step before proceeding (P9-M31 precedent).

**Context scoping (per-level context-scoping standard, P9-M30-E30.3):**
- Load at session start: this starter; the Epic spec (full); PSG preamble+§1, §1A, §2, §5,
  §6, §7, §8, §9, §11, §11.5, §11.6, §12; AOG preamble+§1, §1.1, §2, §3.2, §3.8, §3.10, §4,
  §5, §6, §7, §9, §12, §14, §15 (Exit Ritual), §16 (Error Handling)
- Load on trigger: PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG §3.9, §8, §13, §17
- Do not load: PSG/AOG changelogs, other levels' role or starter-format sections

---

## Epic Specification

**Full spec:** `docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M32-E32.3__spec__governance-hygiene-reconciliation.md`
**Commit:** committed alongside this starter on `milestone/M32` — resolve with
`git log --oneline -1 -- <spec path>`.

### Prerequisite Verification (do this first)

- `git ls-files --error-unmatch <spec path>` — spec git-tracked (GH-1).
- **Re-run both target greps before editing anything** (the spec's citations are as of
  `milestone/M32` @ `1605a8f`, 2026-07-20, and may drift):
  `grep -n "stays opted out" governance/AI-OPERATING-GUIDELINES.md` should hit AOG §17.5
  (~lines 948–953); `grep -rln "Phase Delivery Notice" docs/ governance/templates/` should
  return exactly twelve files — six purge targets, five P9-own quoted-discussion files
  (leave untouched), and the P8 closure declaration (leave untouched). If the counts or
  files differ from this spec's Grounding, stop and reconcile against current state before
  proceeding — do not blindly apply the spec's file list if the repo has moved.
- Confirm `.ai-project.yml:52` reads `enabled: true` (the actual current state the edits
  must match). Suite collects 363 tests.

**Model verification (P9-M31-E31.3 — required for manual instances):** read your own
harness-reported model identity and compare it to `.ai-project.yml`'s `models.epic_manual`
value per `governance/systems/chat-hierarchy.md` "Manual Chat Model Verification." If both
are present and disagree, **STOP** — do not proceed with any deliverable work; state the
mismatch and wait for the Milestone Chat/human.

### Summary

Close two P8 documentation debts: AOG §17.5 and `visual-artifacts.md` still describe this
repo as opted out of `visual_artifacts` when `enabled: true` has been the real, tested state
since E29.2 (P8-GH-1); six pre-P9 planning documents still use a vestigial "Phase Delivery
Notice" phrase PSG §5C's actual closure sequence never names (P8-GH-3). Restate P8-GH-2's
deferred status alongside — confirm, don't resolve.

### Binding constraints (carry into every decision)

**Hard Constraints (from the Milestone spec, applicable to this Epic):**

1. Not applicable — no System HQ authority description in this Epic's surface.
2. **Grep-verifiable hygiene** — acceptance is a clean grep on both items; a partial fix
   that leaves one surface wrong does not close either item.
3. **P8-GH-2 restated, not resolved** — confirm its deferred status and trigger remain
   recorded, don't touch the hosting work itself.
4. Suite green at every merge — 363 baseline, no regressions, no new skips.

### Design Decisions

**None.** This Epic's scope is fully specified — exact files, exact line ranges, exact
preserve/purge distinction. There is no open judgment call; verify and correct against the
fixed target.

---

## Deliverables

1. [ ] **AOG §17.5 corrected** (~lines 948–953) — stale opt-out framing removed, current
       accurate status stated
2. [ ] **`visual-artifacts.md` corrected** (line ~48 callout, §6 ~line 360) — this repo's
       current reality stated; other-environment guidance reframed, not deleted
3. [ ] **Six pre-P9 documents purged** of "Phase Delivery Notice" phrasing (list in Epic
       spec Grounding) — P8 closure declaration's carry-forward entry left untouched
4. [ ] **P8-GH-2's deferred status + trigger restated**, reachable from this Milestone's
       closure record
5. [ ] Full test suite passes (363 baseline, no new skips)
6. [ ] **Epic Delivery Notice**
       (`docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M32-E32.3__delivery-notice.md`)
       — **delivered by reference**, including the literal fresh-grep output for both
       acceptance checks

---

## Definition of Done

- [ ] `grep` for the P8-GH-1 stale-opt-out framing across `governance/` returns nothing
- [ ] `grep` for "Phase Delivery Notice" across `docs/`/`governance/templates/` returns
      only the P8 closure declaration's intentional carry-forward record
- [ ] P8-GH-2's deferred status + trigger restated and locatable
- [ ] Suite green (363 baseline, no new skips)
- [ ] Delivery Notice committed; changes on `epic/P9-M32-E32.3`; PR open to `milestone/M32`

---

## Acceptance Criteria

- [ ] Both grep checks named in the phase spec's own acceptance criteria pass cleanly
- [ ] No historical record (P8's closure declaration) is altered
- [ ] `visual-artifacts.md` §6's guidance for other projects/environments without a live
      endpoint remains present and accurate

---

## Execution Contract

### Your Responsibilities (Coding Agent)

1. Verify prerequisites; re-run both target greps to confirm current state; create
   `epic/P9-M32-E32.3` from `milestone/M32`
2. Read the Epic spec (authoritative) in full
3. Correct AOG §17.5
4. Correct `visual-artifacts.md`'s callout and §6 (reframe, don't delete the
   other-environment guidance)
5. Purge the six pre-P9 documents; leave the P8 closure declaration and P9's own docs
   untouched
6. Restate P8-GH-2's deferred status and trigger, reachable from this Milestone's closure
   record
7. Re-run both greps; confirm clean; run the full suite; commit; open PR to `milestone/M32`
8. Produce the Delivery Notice with literal grep output; hand it **by reference**; stop —
   do not merge

### What You Must NOT Do

- ❌ Merge the PR / infer acceptance
- ❌ Edit `docs/phases/P8__Visual_Artifacts_Activation/P8__phase-closure-declaration.md` in
  any way — it is the preserved historical record
- ❌ Edit any of P9's own planning documents' "Phase Delivery Notice" occurrences —
  verified quoted-discussion-only; editing them is a false-positive fix
- ❌ Delete `visual-artifacts.md` §6's no-live-endpoint guidance — reframe its scope (this
  repo vs. others), don't remove the substance
- ❌ Touch `.ai-project.yml`, `bin/ai-project-visual`, or any code path
- ❌ Touch `governance/systems/artifact-communication-protocol.md`,
  `governance/systems/chat-hierarchy.md`, `governance/templates/genesis.md`, or
  `governance/templates/seed.md` — E32.1's/E32.2's surfaces
- ❌ Invent additional purge scope beyond the six documents and templates carrying the same
  copy-paste risk if found — do not go looking for unrelated hygiene issues
- ❌ Echo edited-file bodies or your own Delivery Notice into chat output
- ❌ Iterate without explicit instruction from the parent chat

---

## Reference Materials

- `docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M32-E32.3__spec__governance-hygiene-reconciliation.md` (authoritative)
- `docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M32__milestone-spec.md` (Epic
  Detail → E32.3; Hard Constraints; Binding Context)
- `docs/phases/P9__Context_Handling_and_Token_Efficiency/P9__phase-spec.md` §P9.3
- `docs/phases/P8__Visual_Artifacts_Activation/P8__phase-closure-declaration.md`
  (Carry-Forward table — source record; preserve, do not edit)
- `governance/AI-OPERATING-GUIDELINES.md` §17.5 (edit target)
- `governance/guides/visual-artifacts.md` (edit target)
- The six pre-P9 documents listed in the Epic spec's Grounding (edit targets)
- `.ai-project.yml` (line 52)

---

## Canonical Happy Path (reminder — SN-19/§11.6)

Execution → Delivery Notice (by reference) → **[STOP]** → default-accept by silence →
human merge authorization in-chat → merge → stop.

## Exit Conditions

Stop when all deliverables are committed, the PR is open, and the Delivery Notice is
produced. Report: "Epic E32.3 execution complete. Delivery Notice at <path> — <one-line
summary>. Awaiting review (default-accept) and human merge authorization."

## Begin Execution

1. Verify prerequisites; re-run both target greps; branch `epic/P9-M32-E32.3` from
   `milestone/M32`
2. Read the Epic spec in full
3. Correct AOG §17.5 and `visual-artifacts.md`, purge the six pre-P9 documents, restate
   P8-GH-2, re-verify both greps clean, test, deliver — by reference

*(Handoff note: this starter is a committed file, passed to you by path + summary per AOG
§3.1.1. If you are reading it as pasted text in a repo-less context, that is the documented
fallback working as intended.)*
