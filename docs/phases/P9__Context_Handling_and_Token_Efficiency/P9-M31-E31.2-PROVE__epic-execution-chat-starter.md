# Epic Execution Chat Starter — E31.2-PROVE

**Epic:** E31.2-PROVE — Row-P5 run evidence (proving vehicle, not a real epic)
**Phase:** P9 — Context Handling and Token Efficiency
**Milestone:** M31 — Dual-Mode Working Levels & Model Guardrail
**Repository:** panchew/ai-project-system
**Branch Strategy:** `epic/P9-M31-E31.2` (committed alongside E31.2 itself — this proving
vehicle is not a separate branch/PR)
**Execution Mode:** agentic — this instance is declared agentic per
`governance/systems/chat-hierarchy.md`'s "Execution Mode" section; it is dispatched,
unattended, via `bin/ai-project-orchestrator` → `bin/run-dev-agent` against
`local:qwen2.5-coder:14b` (dev, policy row P6) / `local:qwen2.5-coder:14b` (QA, policy
row P7) — the corrected mapping this Epic's `DEFAULT_MODELS` alignment and
policy-traceable resolution produce (E31.1-PROVE's own trigger predates this fix and
still names the stale `qwen2.5-coder:7b` QA value; this run is evidence the fix is real).

---

> **Provenance:** authored by the Epic Chat executing P9-M31-E31.2, solely to produce real
> (not simulated) row-P5 run evidence for that Epic's Deliverable 5 — in the tradition of
> `P7-M26-E26.3-PROVE` and `P9-M31-E31.1-PROVE`. This is not a scoped deliverable epic and
> has no Definition of Done beyond its own proving-vehicle spec
> (`P9-M31-E31.2-PROVE__spec__row-p5-run-evidence.md`).

## What this instance demonstrates

That `bin/ai-project-orchestrator`'s `handle_epic_execution`, after this Epic's fix,
resolves `dev_model` to the policy-correct `local:qwen2.5-coder:14b` (row P6) with no
falsified fallback in reach, that `bin/run-dev-agent`'s local-availability preflight check
passes silently against a genuinely reachable local Ollama endpoint with that exact tag
pulled (Hard Constraint 4's "confirmed available" branch — the local-unavailable branch is
covered by unit tests, not this run), and that the Dev-QA loop still converges end-to-end
through the corrected dispatch path — real inference, no mocks.

## Begin Execution

Dispatch via `bin/ai-project-orchestrator` against the trigger naming
`epic_spec_path: docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M31-E31.2-PROVE__spec__row-p5-run-evidence.md`.
No human-chat execution occurs for this instance — its Execution Mode is agentic, per the
declaration above.
