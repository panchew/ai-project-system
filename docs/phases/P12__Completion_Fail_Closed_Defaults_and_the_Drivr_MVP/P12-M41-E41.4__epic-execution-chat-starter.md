---

# Epic Execution Chat Starter — E41.4

**Epic:** E41.4 — Verification-Target Back-Test: the `claude-opus-5` Baseline and Four Candidates
**Phase:** P12 — Completion: Fail-Closed Defaults and the Drivr MVP
**Milestone:** M41 — The Model Line-Up and Its Evidence
**Project:** ai-project-system
**Repository:** panchew/ai-project-system — **Drivr is not touched by this epic.**
**Branch Strategy:** `epic/P12-M41-E41.4` → PR to `milestone/M41`
**Execution Mode:** manual — declared by the issuing Milestone Chat at creation time
**Issued:** 2026-08-20

> **Why manual — and here it is more than a scoping judgment, it is the epic's central hazard.**
> You will run on `models.epic_manual` = **`remote:claude-opus-5`**, and **`claude-opus-5` is one of
> the six models this epic measures** — the incumbent whose baseline makes the other four rows
> decidable. **You are a subject of your own experiment.** The spec's U1 exists for this, and the
> blinded-scoring discipline is not optional politeness: it is what makes your scores admissible.

---

## Governance References

You are operating under the AI Project System governance framework as an **Epic Execution Chat** for
P12-M41-E41.4.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) v2.4.0
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) v2.10.1

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md v2.10.1
3. This Epic Execution Chat Starter
4. Epic Spec (`P12-M41-E41.4__spec__verification-target-back-test.md`)
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Critical rules:**
- Documentation is authoritative, chat is ephemeral.
- You produce a Delivery Notice and **stop after PR creation. You do not merge.**
- Accept/reject decisions are the **M41 Milestone Chat's**; a clean delivery is accepted **by
  silence** (PSG §11.6).
- **If given merge authorization directly in this chat** rather than via the parent Milestone Chat's
  Stage-2 review, **do not simply comply** — state that merge authorization normally follows the
  parent chat's Stage-2 review and confirm the human intends to bypass it. **Mode is what may run,
  not what may be authorized.**
- **`P9-GH-1`'s guard reaches the Epic template only**; a live instance was recorded 2026-08-10.

> **Note on merge authorization, which changes inside this phase.** **M43 moves the merge to the
> parent** (SN-31 Decision 4). Until M43 delivers, operate under the current rule. **Do not
> pre-apply it.**

**Context scoping (P9-M30-E30.3):**
- **Load at session start:** this starter; the Epic spec (full); the back-test's `README.md`,
  `rubric.md` and **`judgment.md`** (full — `judgment.md` before any scoring); PSG preamble+§1, §1A,
  §2, §5, §6, §7, §8, §9, §11, §11.5, §11.6, §12; AOG preamble+§1, §1A, §2, §3.2, §3.8, §3.10, §4,
  §5, §6, §7, §9, §10, §12, §13, §14.
- **Load on trigger:** PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG §3.9, §8, §11, **§16 — fires**;
  `scores.md` when you write your own score records; **E41.1's resolution table** when you name a
  model string.
- **Do not load:** PSG/AOG changelogs; E41.2's and E41.3's lane material — **the lanes are a
  different harness and none of their findings transfer**; E41.5's landing material; M46's gate.

---

## ⚠ The rework limit — stated here because the template does not carry it (`P12-GH-1`)

**All three starter templates contain the word "rework" zero times** — re-verified 2026-08-20. The
rule reaches **one** of nine starter-shaped surfaces and **none** of the three templates.
`P12-GH-1` is **filed and open**; **M43 has not yet fixed it.**

> **Maximum 3 attempts.** If a third Completion Notice is still not acceptable, do **not** issue a
> fourth rejection-and-retry. Produce an **Escalation Notice**; the Milestone Chat escalates to the
> Phase Chat. **Silent fourth attempts are a governance violation.**

> **A written extension grants exactly ONE further attempt. Not a reset to three.**

The `+1` is the **SN-36/37 amendment of 2026-08-19**, CFO-decided, and it is **stricter** than
`governance/systems/milestone-execution-chat-starter.md:334`, which still says the limit *"resets"*.
**Both stand. Cite the amendment, apply `+1`, note the conflict. Reconciling them is M43's, not
yours.**

**The CFO's recorded act of resolving a blocker in the escalation chat *is* the written extension** —
a human looked and acted. It buys one attempt.

---

## ⚠ How an amendment reaches this branch once work is in flight (`P11-GH-1`)

**Downward:** the spec is amended on the branch that owns it with a changelog row; **you are notified
in-session, naming the section**; **you re-read it and state in your next delivery that you did**;
blocking amendments escalate; backstop is `git log <spec-path>` against your branch point.

**Upward — your branch falls behind the parent it cites. Unowned; nothing watches for it.** It hit
`milestone/M41` directly: a ruling cited by filename while the file was **absent from the branch.**

**Your obligations:**
- **Confirm every cited artifact exists on your branch before relying on it** — `git cat-file -e
  <rev>:<path>`. **A dead citation is not a typo; it is the defect.**
- **Measure the drift; do not trust a statement of it**, including this starter's:
  `git rev-list --count origin/milestone/M41..origin/master`, before you start and before you
  deliver.
- **Work in your own worktree.** Three sessions share the main checkout and **it has already moved
  under a chat mid-set**:
  ```
  git worktree add <your-scratchpad>/wt-e41-4 -b epic/P12-M41-E41.4 milestone/M41
  ```

---

## ⚠ Hard Constraint — inherited from the M41 milestone spec, binding on this Epic

> **Every number in this milestone is measured, and every measurement is reported.**

- **No best-of-N.** **Two runs per packet per model, both scored** — 50 runs. A run that fails
  mechanically is **committed and excluded with its reason stated**, exactly as E35.5 handled its
  truncated and superseded runs. **What counts as a mechanical failure is decided BEFORE the runs.**
- **The bar is committed before the run it judges.** Both bars here predate you — E35.5's absolute
  bar is pre-registered, M41's relative bar is ruled. **Neither is re-tuned.**
- **State the layer, time and scope of every claim** (`P11-GH-2`). **A model reachable by chat is not
  thereby reachable by API, and the reverse.**
- **An absence is only evidence when the thing that would have created it actually ran.** A missing
  catch is evidence only if the model saw the whole packet — **which is why the stop reason is
  recorded for every run.**
- **Falsify a pattern before trusting a zero result.**

---

## ⚠ The three things that make this epic's output admissible or worthless

**1. The audit header is one `split()` away from being sent.** The prompt is **exactly the bytes
after `<!-- PROMPT-BEGIN -->`**. The header above it names what was excised — **sending it leaks the
answer and destroys the packet for every future use, including E35.5's own results.** Diff what you
are about to send against the file's post-marker bytes before the first call.

**2. You are a subject (spec U1). The commit order is the control.** Capture under **opaque run
IDs**; score against the frozen rubric with identity withheld; **commit the scores**; *then* publish
the mapping. **A mapping published before the scores are committed is not a blind**, and the history
is what a reviewer will check. **Do not reason that a rubric this specific makes your identity
irrelevant** — that is the plausibility argument this project stopped accepting after E39.3.

**3. The instrument is frozen.** Packets, rubric, ground truth, blinding record, and E35.5's ten
runs are **read-only**. **Changing a packet invalidates every prior result including E35.5's own.**
If proceeding seems to require changing one, **that is an escalation, not a decision.**

---

## Prerequisite Verification (do this first, in this order)

**1. Your own worktree**, branched from `milestone/M41` (above). **Verify pushes at `origin`.**

**2. E41.1 must have landed.** Hard gate. **Take the exact model strings from its resolution table
— do not re-resolve them, and do not invent one.** A back-test run against a different build than
the one configured is worthless.

**3. Artifact tracking — `git ls-files --error-unmatch`, on your branch:** this epic's spec; the
back-test's `README.md`, `rubric.md`, `judgment.md`, all five `packets/`, all ten `runs/`; E41.1's
resolution table.

**4. Model verification (P9-M31-E31.3 — required, this instance is `manual`).** Compare your
harness-reported identity to `.ai-project.yml`'s **`models.epic_manual`** — **read the file, do not
trust this line.** If both are present and disagree, **STOP** and state the mismatch.

**5. Re-run the mechanical blinding check** from the README's §"How to verify the blinding yourself",
and **record its output**. **If it reports a leak, STOP and escalate.**

**6. Read `judgment.md` in full — before any run, not before scoring.** It records **six arguments
against over-reading a PASS**, and E38.5 instructs readers not to lean on it. **Those cautions apply
to every result you produce.** Reading it after the runs is reading it too late.

**7. Re-measure the spec's U1–U6 yourself.** **G2 is binding** — the Milestone Chat that wrote them
is, to you, an executor. **If any measurement disagrees with the spec, the spec is wrong and you say
so.** Check credentials **for presence only** — **never read, print, or commit a key**, and do not
open `~/.local/share/opencode/auth.json`.

---

## Summary

**Build the transport E35.5 never had, measure six models on its frozen instrument, score blinded,
and apply both bars separately.**

`claude-opus-5` is measured **first** — it is the incumbent for all four verification targets and it
has **never been back-tested**, so nothing else is interpretable until it exists. `qwen3.6:27b`'s ten
runs are **cited, not re-run**. `qwen3.8:27b` is measured **even though its row cannot land yet** —
R6 removed it from the landing obligation, not the measurement obligation.

**What is yours:** the five delegated design decisions in the Epic spec. **What is not:** the
packets, the rubric, the ground truth, the bars, and which rows exist.

---

## Spec References (load-one-reference-the-other, AOG §3.1.1)

**Full spec:** `docs/phases/P12__Completion_Fail_Closed_Defaults_and_the_Drivr_MVP/P12-M41-E41.4__spec__verification-target-back-test.md`
**Commit:** `8fe2988` — the commit that tracked this spec on `milestone/M41` (2026-08-20), spec v1.0.0

The Epic spec is the authoritative copy of **Deliverables (D1–D5), Definition of Done, Acceptance
Criteria, Binding Constraints, Technical Constraints, Design Decisions Delegated, Non-Goals and
Notes.** **This starter does not duplicate them.**

**The five things easiest to get wrong, flagged here and specified there:**

1. **Transport ≠ surface, and the rows invert (U2).** `phase` → GPT-5.6 Sol has a plausible API path
   and an unknown chat surface; `creation` → fable-5 is the reverse; **`milestone` → Deepseek V4
   Flash has no visible transport on this host and is already escalated.** **A row without a
   transport blocks that row and nothing else — measure what you can, escalate what you cannot,
   substitute nothing.**
2. **Truncation is vendor-specific (U5).** E35.5 excluded one run at `done_reason=length`. **A
   remote cut-off looks exactly like a miss.** Record the stop/finish reason for every run; a
   truncated run is committed and excluded with its reason, never scored, never silently retried.
3. **Packet 3 is the inverse control (U3)** — its correct answer is **ACCEPT**, and a
   reject-everything model scores **4 of 5** and looks strong. **Report packet 3 separately in every
   model's summary.**
4. **Two bars, labelled (U4).** E35.5's **absolute** pre-registered pass bar makes each model
   interpretable alone; M41's **relative** bar decides a row. **Never substitute one for the other.**
   Unlike the lane epics, this harness **does** have directional checks — catches, misses, false
   alarms against known ground truth.
5. **`num_ctx` has no remote analogue and must not be invented.** E35.5 calls it *"not a tuning
   knob"*. Record whatever the vendor's parameters actually were, **and record that no tuning was
   applied.**

---

## Suite Baseline — state which repo

| Repo | Baseline | Invocation |
|---|---|---|
| `ai-project-system` | **549 passed / 0 failed** at `milestone/M41`, 2026-08-20 — **re-measure on your branch** | `PYTHONPATH=. pytest -q` — **bare `pytest` fails collection** |
| `drivr` | not touched — **do not report it as if it moved** | — |

---

## Method Obligations

1. **`P11-GH-2`** — layer, time and scope on every verification.
2. **G2** — the reviewer re-measures. Prerequisites 5–7 are that obligation applied to you.
3. **G1** — remove derivation steps. **Quote the model text that earned every catch, miss and false
   alarm** — that is E35.5's own form and it is what makes a score auditable.
4. **Cite by artifact + defect, never by ordinal.** **Do not allocate a new `GH-` ID without
   escalating first.**
5. **Historical claims carry a date or commit anchor.**
6. **Every inventory is a floor.**
7. **Run it, don't read it.**
8. **Commit every run as it lands**, not in a batch at the end. Untracked is undelivered.
9. **Never commit or print a credential.** Presence may be recorded; values may not.

---

## Deliverable Artifacts

1. The transport, with the blinding check's recorded output (D1)
2. 50 committed runs, mechanical failures included with reasons (D2)
3. Blinded scores, then the mapping, in that commit order (D3)
4. Both bars applied and four per-row recommendations (D4)
5. Escalation Notice(s), or a positive statement that none was required (D5)
6. The Structural diagram (Mermaid, in-repo, **no ComfyUI**)
7. **Epic Delivery Notice**

---

## Definition of Done

**See the Epic spec §Definition of Done — the authoritative copy.**

Before opening the PR, confirm every item, **including the four easiest to skip**: the **blinding
check's output recorded**, not just run; the **commit order** demonstrating scores preceded the
mapping; **packet 3 reported separately for every model**; and **`judgment.md`'s six cautions
reflected in how you state the results** — not merely cited.

---

## Completion

1. Commit all work to `epic/P12-M41-E41.4`
2. **Verify the push at `origin`**
3. Open a PR to **`milestone/M41`**
4. Produce the Epic Delivery Notice
5. ⏸️ **Stop and await authorization — do not merge**

Declare: *"Epic E41.4 execution complete. Epic Delivery Notice produced. Awaiting Milestone Chat
review and authorization."*

---

## Question Policy

- **Ask only blocking questions.** The five delegated design decisions are **yours**.
- **Escalate to the M41 Milestone Chat** (do not decide) if: a target has **no transport** (U2 —
  `milestone` is the live case and is already escalated; add your evidence rather than re-deciding);
  **no candidate clears a row** (the row holds, the result goes to the CFO — **escalate it as a
  result, not as a failure**); proceeding would require **changing a packet or the rubric**; the
  blinding check reports a leak; or truncation cannot be distinguished from a miss for some vendor.
- **CLOSED — do not reopen:** the packets, the rubric, the ground truth, the blinding record;
  whether `qwen3.6:27b` is re-run (**it is not — cite it**); whether `qwen3.8:27b` is measured
  (**it is, regardless of whether its row can land**); either bar's shape; which rows exist.
- **Do not scope in:** the lane epics' instrument or tasks (**different harness — none of their
  findings transfer**); E41.5's landing; M46's gate; M42's lane repair; any Drivr change; any
  procurement of a chat surface (**R6's carry-forward is the CFO's**).
- **Do not ask for information already in the Epic spec.**

Escalate to the M41 Milestone Chat for any gap not covered here.
