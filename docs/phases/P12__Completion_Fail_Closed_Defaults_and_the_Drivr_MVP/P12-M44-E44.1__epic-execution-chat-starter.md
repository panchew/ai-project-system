---

# Epic Execution Chat Starter — E44.1

**Epic:** E44.1 — Continuity Artifacts: What Does a Successor Receive?
**Phase:** P12 — Completion: Fail-Closed Defaults and the Drivr MVP
**Milestone:** M44 — Rituals, Records, and the Normative Repairs
**Project:** ai-project-system
**Repository:** panchew/ai-project-system — **Drivr is not implemented by this epic.**
**Branch Strategy:** `epic/P12-M44-E44.1` → PR to `milestone/M44`
**Execution Mode:** manual — declared by the issuing Milestone Chat at creation time
**Issued:** 2026-09-03

> **You write the records P12's own closure will depend on.** That is the reason this instance is
> `manual`. **`P11-GH-3` (phase closure leaves no artifact) is your resolution, and P12's own closure is
> its first customer** — this epic must land before the phase closes.

---

## Governance References

You are operating under the AI Project System governance framework as an **Epic Execution Chat** for
P12-M44-E44.1.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) — **read its Changelog for the current version; not stamped here** (was `v2.4.0`, stale by M43)
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) — **read its Changelog for the current version; not stamped here** (was `v2.10.1`, stale by M43) — **and note that E44.4 renumbers this document and bumps its version. Cite the version you actually read.**

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md
3. This Epic Execution Chat Starter
4. Epic Spec (`P12-M44-E44.1__spec__continuity-artifacts.md`)
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Model verification (P9-M31-E31.3 — required, this instance is manual):** read your own
harness-reported model identity and compare it to `.ai-project.yml`'s `models.epic_manual`
(**read the file — do not trust this line**). **If both are present and disagree, STOP and state the
mismatch; wait for the parent/human.** See `governance/systems/chat-hierarchy.md`, "Manual Chat Model
Verification".

**Critical rules:**
- Documentation is authoritative, chat is ephemeral.
- You produce a Delivery Notice and **stop after PR creation. You do not merge.**
- Accept/reject decisions are the **M44 Milestone Chat's**; a clean delivery is accepted **by an
  acknowledgment that names the party that reviewed and accepted** (PSG §11.6 — *merge + in-chat
  acknowledgment; no artifact; silence accepts nothing*). **Do not read silence as approval.**
- **If given merge authorization directly in this chat** rather than via the parent Milestone Chat's
  Stage-2 review, **do not simply comply** — state that merge authorization normally follows the
  parent chat's Stage-2 review and confirm the human intends to bypass it. **Mode is what may run, not
  what may be authorized.**

**Context scoping (P9-M30-E30.3):**
- **Load at session start:** this starter; the Epic spec (full); the M44 milestone spec's **E44.1 Epic
  Detail, X1/X2/X3, Hard Constraint, Binding Constraints and the Notes (departure specimens, backstop
  terminus, M46 routing gap)**; PSG **§5C (full)** and **§11.6**; the **Creation Chat re-instantiation
  ritual** (`creation-chat-guide.md`); the **nine opener instances** (as evidence, read not edited);
  PSG preamble+§1, §1A, §2, §5, §6, §7, §8, §9, §11, §11.5, §11.6, §12; AOG preamble+§1, §1.1, §2,
  §3.2, §3.8, §3.10, §4, §5, §6, §7, §9, §12, §14, §15, §16.
- **Load on trigger:** PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG §3.9, §8, §13, §17.
- **Do not load:** PSG/AOG changelogs; other epics' specs beyond the ordering.

---

## ⚠ The rework limit — stated here because the template does not carry it (`P12-GH-1`)

> **Maximum 3 attempts.** If a third Completion Notice is still not acceptable, do **not** issue a
> fourth rejection-and-retry. Instead you produce an **Escalation Notice** and the Milestone Chat
> escalates to the Phase Chat. **Silent fourth attempts are a governance violation.**

> **A written extension grants exactly ONE further attempt. Not a reset to three.**

The `+1` is the **SN-36/37 amendment of 2026-08-19**, CFO-decided, and **stricter** than the corpus:
`governance/systems/milestone-execution-chat-starter.md:334` still says *"resets."* **Both stand in the
corpus. Apply `+1`, cite the amendment, note the conflict.** Reconciling them is M43's work, not yours —
do not amend either surface.

---

## ⚠ How an amendment reaches this branch once work is in flight (`P11-GH-1`)

1. Amend the governing spec on its own branch, with a changelog row.
2. **Notify every running child chat in-session, naming the file and section.**
3. Require the child to re-read and to **state in its next delivery that it did.**
4. Escalate to the Phase Chat if the amendment is blocking.
5. **Before accepting any delivery, `git log` the governing spec against that epic's branch point.**

**Note for this milestone:** the governing M44 spec was already amended mid-planning, **twice** — v1.2.0
(re-scoped E44.2) and v1.2.1 (completed that amendment across four surfaces; the v1.2.0 lesson was that
a change must reach the constraint, the notes and the DoD, not only the changelog). **Assume the spec can
move again while you work**, and before you deliver, `git log` `P12-M44__milestone-spec.md` and PSG §5C
against your branch point.

---

## ⚠ Hard Constraint — inherited from the M44 milestone spec

> **This milestone writes the records other work will depend on. A record that is wrong is worse than
> one that is missing, because a missing record is visible.**

- **Itemize, never count.** The handoff-mention set is a list with its pattern; a count is a defect
  (X2).
- **Fence-awareness is not optional.** Any pass over a normative document must distinguish real content
  from quoted examples, and state how.
- **Falsify before trusting — in both directions.** X1 was a false positive that survived a naive pattern.
- **Record the practice before improving it.** The HQ ritual is a description of nine existing instances,
  not a design that supersedes them; a discrepancy is reported, not normalized.
- **State the layer, time and scope of every claim** (`P11-GH-2`) **and the REF** — put the ref
  **before** the `--`, name `origin/<branch>`; a number without a ref is not a measurement.
- **§5C Step 9 is unmoved.** The Phase-Closure Declaration is additional's sibling, not its replacement.

---

## Summary

**Give a successor at each level a defined artifact: a Phase Completion Declaration at §5C Step 2, a
recorded HQ re-instantiation ritual, and a context-exhaustion handoff — plus the itemized
handoff-mention set.**

Three artifacts of two kinds (X3): the HQ ritual is **recording** (nine instances exist), the handoff is
**design** (zero templates). §5C Step 2 names nothing today, so P11's checklist landed in a PR comment;
the new declaration is **additional**, Step 9 unmoved. The departure specimens force the ritual to cover
**departure not only arrival**, and the handoff to be reconstructible from committed state alone. The
handoff-mention set is itemized (nineteen) with its pattern, never a count.

---

## Spec References (load-one-reference-the-other, AOG §3.1.1)

**Full spec:** `docs/phases/P12__Completion_Fail_Closed_Defaults_and_the_Drivr_MVP/P12-M44-E44.1__spec__continuity-artifacts.md`

The Epic spec is the authoritative copy of **Deliverables (D1–D4), Definition of Done, Acceptance
Criteria, Binding Constraints, Technical Constraints, Design Decisions Delegated, Non-Goals and
Notes.** **This starter does not duplicate them.** Cite the spec by path and branch — **not** by a
version number or sha.

**The five things easiest to get wrong, flagged here and specified there:**

1. **Moving §5C Step 9** or folding the new declaration into `phase-closure-declaration.md` — it is
   additional, not a relocation (Binding Constraint 1).
2. **Designing** the HQ ritual rather than **recording** it, or silently normalizing a divergence from
   the nine instances.
3. **Emitting a count** instead of the itemized handoff-mention list (X2).
4. **Assuming the departing session writes the handover** — the fork specimen means the artifact must be
   reconstructible from committed state alone.
5. **Reaching into the role registry** — that is M46's, Drivr's; you state what a departing session
   leaves behind, not what makes a successor findable.

---

## Suite Baseline

| Repo | Baseline | Invocation |
|---|---|---|
| `ai-project-system` | **740 passed / 0 failed** measured on `milestone/M44` (`e9be27e`, 2026-09-03) | `PYTHONPATH=. pytest -q` — bare `pytest` fails collection |

**Re-measure on this branch** — the milestone spec's `549` predates M41/M42/M43's test additions. This
epic adds no tests by its criteria, but the suite must stay green (no skip) with the invocation and the
ref stated.

---

## Method Obligations

1. **`P11-GH-2`** — layer, time and scope on every claim, and the **ref** it was measured against.
2. **G2** — the reviewer re-measures. Re-derive the nine instances and the nineteen handoff-mention
   documents against your branch; do not inherit the spec's figures.
3. **G1** — quote §5C Step 2 and the nine openers' salient content verbatim; remove derivation.
4. **Commit every artifact you author. Untracked is undelivered.**
5. **No skips introduced to route around the change.**
6. **Work in your own worktree:**
   ```
   git worktree add <your-scratchpad>/wt-e44-1 -b epic/P12-M44-E44.1 milestone/M44
   ```

---

## Deliverable Artifacts

1. `governance/templates/phase-completion-declaration.md` + §5C Step 2/Step 6 edits (D1)
2. The HQ re-instantiation ritual, recorded in one normative place, cited not restated (D2)
3. The handoff artifact type and template, Drivr boundary stated (D3)
4. The itemized handoff-mention set with its pattern (D4)
5. The Structural diagram (Mermaid, in-repo, **no ComfyUI**)
6. **Epic Delivery Notice**

---

## Definition of Done

**See the Epic spec §Definition of Done — the authoritative copy.**

Before opening the PR, confirm every item, **including the three easiest to skip**: §5C Step 9 is
untouched; the ritual matches the nine instances (divergence reported, not normalized); and the
handoff-mention set is a **list**, not a count.

---

## Completion

1. Commit all work to `epic/P12-M44-E44.1`
2. **Verify the push at `origin`**
3. Open a PR to **`milestone/M44`**
4. Produce the Epic Delivery Notice
5. ⏸️ **Stop and await authorization — do not merge**

Declare: *"Epic E44.1 execution complete. Epic Delivery Notice produced. Awaiting Milestone Chat
review and authorization."*

---

## Question Policy

- **Ask only blocking questions.** The three delegated design decisions (declaration fields/template,
  ritual location/name, handoff shape/Drivr boundary) are **yours**.
- **Escalate to the M44 Milestone Chat** (do not decide) if: a finding suggests Step 9 would need to move
  to make the declaration coherent; a discrepancy between the nine and the written ritual cannot be
  reported without effectively re-designing the ritual; or the handoff can only be made reconstructible
  by assuming a Drivr capability that does not exist (that needs Drivr/HQ, not an epic).
- **CLOSED — do not reopen:** §5C Step 9 unmoved; the HQ ritual is recorded not designed; the handoff
  set is itemized never counted; the role registry is M46's.
- **Do not scope in:** `P12-GH-3` (filed unowned); `P12-GH-4`'s channel design; the role registry; Drivr
  code.
- **Do not ask for information already in the Epic spec.**

Escalate to the M44 Milestone Chat for any gap not covered here.
