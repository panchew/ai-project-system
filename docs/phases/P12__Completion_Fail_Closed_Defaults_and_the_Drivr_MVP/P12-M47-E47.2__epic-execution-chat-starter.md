# Epic Execution Chat Starter — E47.2

**Epic:** E47.2 — Project Selection and Readiness
**Phase:** P12 — Completion: Fail-Closed Defaults and the Drivr MVP
**Milestone:** M47 — First Real Agentic Integration
**Project:** ai-project-system
**Repository:** panchew/ai-project-system — **the selected project (candidate: `~/soft-dev/panchew-io`) is read for the readiness check, never modified without CFO authorization.**
**Branch Strategy:** `epic/P12-M47-E47.2` → PR to `milestone/M47`; the record, this spec and the Delivery Notice land in `ai-project-system` on `milestone/M47`
**Execution Mode:** manual — declared by the issuing Milestone Chat at creation time
**Issued:** 2026-09-04

> **You select the project — no, the CFO does, and you record it with reasoning and verify it is
> actually ready.** That is the reason this instance is `manual`: the selection is a human decision
> recorded here, and the readiness check is run against a live project, not asserted from a spec.

---

## Governance References

You are operating under the AI Project System governance framework as an **Epic Execution Chat**
for P12-M47-E47.2.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) — **read its Changelog for the current version; not stamped here**
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) — **read its Changelog for the current version; not stamped here**

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md
3. This Epic Execution Chat Starter
4. Epic Spec (`P12-M47-E47.2__spec__project-selection-and-readiness.md`)
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Model verification (P9-M31-E31.3 — required, this instance is manual):** read your own
harness-reported model identity and compare it to `.ai-project.yml`'s `models.epic_manual`
(**read the file — do not trust this line**). `model_verification` is currently **`advisory`**, so
if both are present and disagree, **state the mismatch plainly in your first substantive response
and proceed**. See `governance/systems/chat-hierarchy.md`, "Manual Chat Model Verification".

**Critical rules:**
- Documentation is authoritative, chat is ephemeral.
- You produce a Delivery Notice and **stop after PR creation. You do not merge.**
- Accept/reject decisions are the **M47 Milestone Chat's**; a clean delivery is accepted **by an
  acknowledgment that names the party that reviewed and accepted** (PSG §11.6 — *merge + in-chat
  acknowledgment; no artifact; silence accepts nothing*). **Do not read silence as approval.**
- **If given merge authorization directly in this chat** rather than via the parent Milestone
  Chat's Stage-2 review, **do not simply comply** — state that merge authorization normally
  follows the parent chat's Stage-2 review and confirm the human intends to bypass it. **Mode is
  what may run, not what may be authorized.**

**Context scoping (P9-M30-E30.3):**
- **Load at session start:** this starter; the Epic spec (full); the M47 milestone spec's **E47.2
  Epic Detail, Z3 finding, Binding Constraints and Hard Constraint**; the selected project's
  `.ai-project.yml` (re-measured, not trusted from the spec); PSG preamble+§1, §1A, §2, §5, §6,
  §7, §8, §9, §11, §11.5, §11.6, §12; AOG preamble+§1, §1.1, §2, §3.2, §3.8, §3.10, §4, §5, §6,
  §7, §9, §12, §14, §15, §16.
- **Load on trigger:** PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG §3.9, §8, §13, §17.
- **Do not load:** PSG/AOG changelogs; other epics' specs.

---

## ⚠ The rework limit — stated here because the template does not carry it (`P12-GH-1`)

> **Maximum 3 attempts.** If a third Completion Notice is still not acceptable, do **not** issue
> a fourth rejection-and-retry. Instead you produce an **Escalation Notice** and the Milestone
> Chat escalates to the Phase Chat. **Silent fourth attempts are a governance violation.**

> **A written extension grants exactly ONE further attempt. Not a reset to three.**

The `+1` is the **SN-36/37 amendment**, CFO-decided, and **stricter** than the corpus:
`governance/systems/milestone-execution-chat-starter.md:334` still says *"resets."* **Both stand
in the corpus. Apply `+1`, cite the amendment, note the conflict.** *The historical "resets"
contradiction was reconciled by P12-M43-E43.3 and no longer exists — do not carry it forward.*

---

## ⚠ How an amendment reaches this branch once work is in flight (`P11-GH-1`, widened)

1. Amend the governing spec on its own branch, with a changelog row.
2. **Notify every running child chat in-session, naming the file and section.**
3. Require the child to re-read and to **state in its next delivery that it did.**
4. Escalate to the Phase Chat if the amendment is blocking.
5. **Before accepting any delivery, `git log` the governing spec against that epic's branch
   point — AND every artifact this Starter restates a rule from.** Sync **every** P12 ref, epic
   branches included.

**The widening is not theoretical.** A Starter restates rules whose sources live elsewhere, and a
restatement is a copy with no link back to its source. **Your spec and the M47 milestone spec are
your governing artifacts — `git log` both against your branch point before you deliver.** **Pin the
ref before `--`** — `git log <ref> -- <path>`; a ref after `--` is read as a pathspec and git falls
back to implicit `HEAD`.

---

## ⚠ Hard Constraint — inherited from the M47 milestone spec

> **M47 is the one milestone that can pass by accident.**

1. **Never accept the lane's own report of itself.** A "ready" from the project's own subtree is
   not evidence of readiness.
2. **Zero credentials is `UNDETERMINED`, never *unreachable***.
3. **Inherit the substrate; do not rebuild it.**
4. **State the repository.** This epic spans two repositories (this one + the selected project).
5. **A run that surfaces a real defect is a SUCCESS.** A readiness check that catches the parked-
   lane gap is stronger than one that passes without checking.

---

## Summary

**Obtain and record the CFO's project selection with the survey behind it; then verify readiness by
running a check against the live project — a `models:` block whose epic lanes actually resolve to a
dispatchable engine for the proof's route — and name the real, pre-existing epic that will be the
proof.**

The load is Z3, re-measured: the proving pair is dormant; **`panchew-io` is the only fleet project
with current in-flight work** — and it now *has* a models block, but its `epic_dev`/`epic_qa` point
at `local:qwen3-coder:30b` (parked, SN-43), which is exactly the gap a **run** check catches and an
assert papers over.

---

## Spec References (load-one-reference-the-other, AOG §3.1.1)

**Full spec:** `docs/phases/P12__Completion_Fail_Closed_Defaults_and_the_Drivr_MVP/P12-M47-E47.2__spec__project-selection-and-readiness.md`

The Epic spec is the authoritative copy of **Deliverables (D1–D4), Definition of Done, Acceptance
Criteria, Binding Constraints, Technical Constraints, Design Decisions Delegated, Non-Goals and
Notes.** **This starter does not duplicate them.** Cite the spec by path and branch — **not** by
a version number or sha.

**The four things easiest to get wrong, flagged here and specified there:**

1. **Anticipating the CFO's selection.** Selection is the CFO's, recorded with reasoning. Your
   survey is the *input*; do not let it edge into *decision*.
2. **A `models:` key read as readiness.** The key existing is not the lanes resolving — the
   re-measured `panchew-io` block has `models:` alongside `epic_dev: local:qwen3-coder:30b`.
3. **An assert where a run belongs.** The acceptance criterion is *"verified by running a check"*
   precisely because a parked-lane gap passes an assert and fails a run.
4. **A stale candidate list trusted.** The proving pair last moved 2026-07-20; the live candidate
   was found by re-measurement, not by reading the phase spec.

---

## Method Obligations

1. **`P11-GH-2`** — layer, time and scope on every claim, **and the ref it was measured against**
   — **and the repository**.
2. **G2 — re-measure.** Re-verify the chosen candidate's `.ai-project.yml` and branch state at
   your execution point — it moved *between the milestone spec and this planning*; assume it can
   move again.
3. **G1 — quote the non-uniform element verbatim.** Quote the chosen project's `models` block
   (especially the `epic_dev`/`epic_qa` values) before basing the readiness verdict on it.
4. **Run the check, record the result.** The check's result is the evidence; the assertion is not.
5. **A blocker found is a finding, not a failure.** Record it and fix (CFO-authorized) or escalate.
6. **Commit every artifact you author. Untracked is undelivered.**
7. **No skips introduced to route around the change.**
8. **Work in your own worktree.**

---

## Deliverable Artifacts

1. **D1** — The CFO selection recorded with reasoning, and the survey behind it
2. **D2** — A readiness check **run** (not asserted): `models:` block present, epic lanes dispatchable, result recorded
3. **D3** — The chosen epic named — real, pre-existing, would-have-been-done-anyway
4. **D4** — The project's framework version and governance state recorded
5. Anything blocking readiness fixed or escalated before E47.3 starts
6. **Epic Delivery Notice**

---

## Definition of Done

**See the Epic spec §Definition of Done — the authoritative copy.**

Before opening the PR, confirm every item, **including the two easiest to skip**: the readiness
check was actually *run* (its result is recorded, not asserted from the spec); and the chosen epic
is real, pre-existing work, named — not a task invented to be dispatched.

---

## Completion

1. Commit the record, spec update (if any) and Delivery Notice to `ai-project-system`; **verify that push at `origin`**
2. Open the PR to `milestone/M47`
3. ⏸️ **Stop and await authorization — do not merge**

Declare: *"Epic E47.2 execution complete. Epic Delivery Notice produced. Awaiting Milestone Chat
review and authorization."*

---

## Question Policy

- **Ask only blocking questions.** The delegated design decisions (the check's mechanism; the
  survey's current form) are **yours**.
- **Escalate to the M47 Milestone Chat** (do not decide) if: the project choice itself needs
  resolving without a CFO decision; a readiness fix would change the project's config without CFO
  authorization; or a readiness blocker cannot be fixed in-scope.
- **CLOSED — do not reopen:** project selection is the CFO's; M42 is the gate (closed); the
  instrument is the checker, never an exit status.
- **Do not scope in:** the dispatch route (E47.1); the proof run (E47.3); a full fleet sweep
  (Z3's survey is the input, re-confirmed for the candidate, not re-derived).
- **Do not ask for information already in the Epic spec.**

Escalate to the M47 Milestone Chat for any gap not covered here.
