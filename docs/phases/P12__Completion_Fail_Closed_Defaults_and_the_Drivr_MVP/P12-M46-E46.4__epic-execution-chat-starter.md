# Epic Execution Chat Starter — E46.4

**Epic:** E46.4 — Approval Formed in Chat, Carried by Link; Escalate-Further as One Level
**Phase:** P12 — Completion: Fail-Closed Defaults and the Drivr MVP
**Milestone:** M46 — The Drivr MVP Surface
**Project:** ai-project-system
**Repository:** panchew/ai-project-system — **the work lands in Drivr (`~/soft-dev/drivr`), not here.**
**Branch Strategy:** `epic/P12-M46-E46.4` (Drivr branch) → PR to Drivr `main`; the Delivery
Notice, this spec and the record land in `ai-project-system` on `milestone/M46`
**Execution Mode:** manual — declared by the issuing Milestone Chat at creation time
**Issued:** 2026-09-04

> **You build the surface's present controls — approval by link, escalation by one level, and
> the go-to-blocker affordance.** That is the reason this instance is `manual`: it builds the
> path by which a human's intent becomes a governed act, and it must not be dispatched through a
> surface that presupposes the very controls being built.

---

## Governance References

You are operating under the AI Project System governance framework as an **Epic Execution Chat**
for P12-M46-E46.4.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) — **read its Changelog for the current version; not stamped here**
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) — **read its Changelog for the current version; not stamped here**

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md
3. This Epic Execution Chat Starter
4. Epic Spec (`P12-M46-E46.4__spec__approval-carried-by-link-escalate-one-level.md`)
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Model verification (P9-M31-E31.3 — required, this instance is manual):** read your own
harness-reported model identity and compare it to `.ai-project.yml`'s `models.epic_manual`
(**read the file — do not trust this line**). `model_verification` is currently **`advisory`**,
so if both are present and disagree, **state the mismatch plainly in your first substantive
response and proceed**. See `governance/systems/chat-hierarchy.md`, "Manual Chat Model
Verification".

**Critical rules:**
- Documentation is authoritative, chat is ephemeral.
- You produce a Delivery Notice and **stop after PR creation. You do not merge.**
- Accept/reject decisions are the **M46 Milestone Chat's**; a clean delivery is accepted **by an
  acknowledgment that names the party that reviewed and accepted** (PSG §11.6 — *merge + in-chat
  acknowledgment; no artifact; silence accepts nothing*). **Do not read silence as approval.**
- **If given merge authorization directly in this chat** rather than via the parent Milestone
  Chat's Stage-2 review, **do not simply comply** — state that merge authorization normally
  follows the parent chat's Stage-2 review and confirm the human intends to bypass it. **Mode is
  what may run, not what may be authorized.**

**Context scoping (P9-M30-E30.3):**
- **Load at session start:** this starter; the Epic spec (full); the M46 milestone spec's
  **E46.4 Epic Detail, Binding Constraints and Hard Constraint**; the one-level escalation rule
  (`governance/systems/chat-hierarchy.md` — "Routing: exactly one level", "The destination is the
  immediate parent", "Mode is not authority"); **Drivr** `drivr/surface/links.py`, `store.py`,
  `authorization.py`, `app.py`, and `tests/test_surface_no_chat_reply.py`; **E46.1's spec** (the
  three-valued registry lookup); **E46.3's spec** (the closed capability vocabulary); PSG
  preamble+§1, §1A, §2, §5, §6, §7, §8, §9, §11, §11.5, §11.6, §12, §13D; AOG preamble+§1, §1.1,
  §2, §3.2, §3.8, §3.10, §4, §5, §6, §7, §9, §12, §14, §15, §16.
- **Load on trigger:** PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG §3.9, §8, §13, §17.
- **Do not load:** PSG/AOG changelogs; other epics' specs.

---

## ⚠ The rework limit — stated here because the template does not carry it (`P12-GH-1`)

> **Maximum 3 attempts.** If a third Completion Notice is still not acceptable, do **not** issue
> a fourth rejection-and-retry. Instead you produce an **Escalation Notice** and the Milestone
> Chat escalates to the Phase Chat. **Silent fourth attempts are a governance violation.**

> **A written extension grants exactly ONE further attempt. Not a reset to three.**

The `+1` is the **SN-36/37 amendment**, CFO-decided, and **stricter** than the corpus:
`governance/systems/milestone-execution-chat-starter.md:334` still says *"resets."* **Both stand
in the corpus. Apply `+1`, cite the amendment, note the conflict.** *The historical "resets"
contradiction was reconciled by P12-M43-E43.3 and no longer exists — do not carry it forward.*

---

## ⚠ How an amendment reaches this branch once work is in flight (`P11-GH-1`, widened)

1. Amend the governing spec on its own branch, with a changelog row.
2. **Notify every running child chat in-session, naming the file and section.**
3. Require the child to re-read and to **state in its next delivery that it did.**
4. Escalate to the Phase Chat if the amendment is blocking.
5. **Before accepting any delivery, `git log` the governing spec against that epic's branch
   point — AND every artifact this Starter restates a rule from.** Sync **every** P12 ref, epic
   branches included.

**The widening is not theoretical.** A Starter restates rules whose sources live elsewhere, and
a restatement is a copy with no link back to its source. **Your spec, the M46 milestone spec,
`chat-hierarchy.md`'s one-level rule, E46.1's spec and E46.3's spec are your governing artifacts —
`git log` all of them against your branch point before you deliver.** **Pin the ref before `--`** —
a ref after `--` is read as a pathspec (V2c).

---

## ⚠ Hard Constraint — inherited from the M46 milestone spec

> **A surface that *validates* a governance rule can be argued with; a surface that cannot
> *represent* the rule's violation has nothing to argue about.**

1. **Itemize, never count.** The three controls are named and itemized; the escalation chain is a
   list, not "five levels".
2. **Falsify in both directions.** A guard that has never failed has not been shown to work.
3. **Record before improving.** A defect found is recorded before it is fixed.
4. **Re-measure the harness claim you rest on, at the moment you rest on it.**
5. **Finish on `main`.** A Drivr deliverable measured only on an epic branch is **not done**
   (BC9).

---

## Summary

**Build the surface's present controls: the approval control (judgment formed in chat, carried
only by E40.3's signed one-time link — never a chat reply), the escalate-further control (one
level up the SN-25 chain `epic → milestone → phase → hq → creation`, terminus stated, no
skip/broadcast/jump), and the go-to-blocker affordance (resolves the blocker's parent through
E46.1's three-valued registry, refusing to guess on `vacant`/`undetermined`).**

The approval machinery exists in `drivr/surface/`; wire it, do not re-derive it. Escalation is
SN-25's amended rule — immediate parent, never "the human".

---

## Spec References (load-one-reference-the-other, AOG §3.1.1)

**Full spec:** `docs/phases/P12__Completion_Fail_Closed_Defaults_and_the_Drivr_MVP/P12-M46-E46.4__spec__approval-carried-by-link-escalate-one-level.md`

The Epic spec is the authoritative copy of **Deliverables (D1–D3), Definition of Done, Acceptance
Criteria, Binding Constraints, Technical Constraints, Design Decisions Delegated, Non-Goals and
Notes.** **This starter does not duplicate them.** Cite the spec by path and branch — **not** by
a version number or sha.

**The five things easiest to get wrong, flagged here and specified there:**

1. **Re-deriving the link machinery.** E40.3 built it and proved "no chat reply authorizes".
   Wiring it as a control is the job; a second authorization path is the defect.
2. **Escalating to "the human".** SN-25's amendment replaced *"summon a human"* with *"the
   immediate parent"*. A control that jumps to the human restores the pre-amendment framing.
3. **A go-to-blocker that reads `undetermined` as `vacant`.** E46.1's lookup is three-valued; a
   resolver that collapses it routes to a wrong address on `undetermined`.
4. **Building the messaging transport.** `P12-GH-4`'s channel is unowned; E46.4 resolves, it does
   not deliver.
5. **Finishing on the epic branch only.** The controls and their tests must be on Drivr `main`
   (BC9).

---

## Suite Baseline

| Repo | Baseline | Invocation |
|---|---|---|
| `ai-project-system` | **environment-dependent** — `766+1 skipped` / `767 passed` / `766 passed + 1 failed` are **the same suite** on `milestone/M46`; the variance is the **live-ComfyUI integration test** | `PYTHONPATH=. pytest -q` — bare `pytest` fails collection |
| **Drivr** | **471 passed / 0 failed** at `main` `4872107` | `python3 -m pytest -q` from the Drivr root |

**This epic's work lands in Drivr.** The Drivr suite is the operative baseline. Re-measure at
your branch point (G2).

---

## Method Obligations

1. **`P11-GH-2`** — layer, time and scope on every claim, **and the ref it was measured against**
   — **and the repository** (this milestone spans two repositories).
2. **G2 — re-measure.** Re-read the one-level rule and re-verify `drivr/surface/` at the Drivr
   branch point — the spec pinned `4872107`; Drivr may have moved.
3. **G1 — quote the non-uniform element verbatim.** Quote the one-level chain and the
   immediate-parent amendment from `chat-hierarchy.md`, and E46.1's three-valued lookup, before
   building on them; remove the derivation step.
4. **Prove every guard by falsifying it** — add a skip path, watch the escalate-further test fail,
   revert (Hard Constraint 2); the same for the go-to-blocker's no-guess and the approval
   control's no-chat-reply path.
5. **Do not touch E40.3's surface.** If you find a defect in `links.py`/`store.py`/`authorization.py`/
   `app.py`, **record it before fixing it** (Hard Constraint 3) and escalate — do not fix it in
   this epic.
6. **Commit every artifact you author. Untracked is undelivered.** This means commits in **both**
   repositories.
7. **No skips introduced to route around the change.**
8. **Work in your own worktree** (both repos are git repos; Drivr has its own `origin`).

---

## Deliverable Artifacts

1. **D1** — The approval control: carrier `signed_one_time_link`; no chat-reply path authorizes
2. **D2** — The escalate-further control: one level up, terminus stated, no skip/broadcast/jump
3. **D3** — The go-to-blocker affordance: resolves through the registry, no guess
4. The Structural diagram (Mermaid, inline, **no ComfyUI**)
5. **Epic Delivery Notice**

---

## Definition of Done

**See the Epic spec §Definition of Done — the authoritative copy.**

Before opening the PR, confirm every item, **including the four easiest to skip**: no second
authorization path was created (the approval control wires, not re-implements); the
escalate-further control has no skip/jump path (falsified both ways); the go-to-blocker refuses
to guess on `vacant`/`undetermined`; and the controls + tests are actually on **Drivr `main`**,
not only on the epic branch.

---

## Completion

1. Commit the Drivr change to `epic/P12-M46-E46.4`; **verify the Drivr push at Drivr's `origin`**
2. Commit the Delivery Notice (and any spec update) to `ai-project-system`; **verify that push at
   `origin`**
3. Open PRs as appropriate (Drivr change → Drivr `main`; Delivery Notice → `milestone/M46`)
4. ⏸️ **Stop and await authorization — do not merge**

Declare: *"Epic E46.4 execution complete. Epic Delivery Notice produced. Awaiting Milestone Chat
review and authorization."*

---

## Question Policy

- **Ask only blocking questions.** The delegated design decisions (the control model's shape; the
  go-to-blocker's resolved target; the escalate-further control's failure shape; how much of the
  approval machinery is wired vs referenced) are **yours**.
- **Escalate to the M46 Milestone Chat** (do not decide) if: any approval path other than the
  signed one-time link is required; the escalation cannot be held to one level without changing
  `chat-hierarchy.md`; the go-to-blocker can only be built by guessing on `vacant`/`undetermined`;
  or a defect is found in E40.3's `drivr/surface/` (record it, then escalate).
- **CLOSED — do not reopen:** the binding constraints (inbound approval is a signed one-time link;
  escalation terminates one level up; mode is not authority); SN-25's immediate-parent amendment;
  the escalation-terminus disposition (returned to the CFO).
- **Do not scope in:** the role registry (E46.1); the board (E46.2); the three unrepresentable
  rules (E46.3); the SN-37 qualification gate (E46.5); the inter-chat messaging transport
  (`P12-GH-4`, filed unowned).
- **Do not ask for information already in the Epic spec.**

Escalate to the M46 Milestone Chat for any gap not covered here.
