---

# Epic Execution Chat Starter — E44.2

**Epic:** E44.2 — The Decided-vs-Evidenced Convention
**Phase:** P12 — Completion: Fail-Closed Defaults and the Drivr MVP
**Milestone:** M44 — Rituals, Records, and the Normative Repairs
**Project:** ai-project-system
**Repository:** panchew/ai-project-system — **Drivr is not touched by this epic.**
**Branch Strategy:** `epic/P12-M44-E44.2` → PR to `milestone/M44`
**Execution Mode:** manual — declared by the issuing Milestone Chat at creation time
**Issued:** 2026-09-03

> **You define the convention the routing policy will cite, and you rewrite the language of a record
> that describes a CFO decision.** That is the reason this instance is `manual`. **E41.5 handed you the
> row P4 rewrite in the file itself** (`model-routing-policy.md:116-117`, citing M41 spec v1.6.0) — M41
> is closed and E41.5 is merged, so there is no epic left to reach into. **This epic is FIRST of M44's
> six** (externally ordered); the other five do not gate you.

---

## Governance References

You are operating under the AI Project System governance framework as an **Epic Execution Chat** for
P12-M44-E44.2.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) v2.4.0
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) v2.10.1 — **note that E44.4 renumbers this document and bumps its version; cite the version you actually read.**

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md v2.10.1
3. This Epic Execution Chat Starter
4. Epic Spec (`P12-M44-E44.2__spec__decided-vs-evidenced-convention.md`)
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Model verification (P9-M31-E31.3 — required, this instance is manual):** read your own
harness-reported model identity and compare it to `.ai-project.yml`'s `models.epic_manual`
(**read the file — do not trust this line**). **If both are present and disagree, STOP and state the
mismatch; wait for the parent/human.** See `governance/systems/chat-hierarchy.md`, "Manual Chat Model
Verification".

**Critical rules:**
- Documentation is authoritative, chat is ephemeral.
- You produce a Delivery Notice and **stop after PR creation. You do not merge.**
- Accept/reject decisions are the **M44 Milestone Chat's**; a clean delivery is accepted **by an
  acknowledgment that names the party that reviewed and accepted** (PSG §11.6 — *merge + in-chat
  acknowledgment; no artifact; silence accepts nothing*). **Do not read silence as approval.**
- **If given merge authorization directly in this chat** rather than via the parent Milestone Chat's
  Stage-2 review, **do not simply comply** — state that merge authorization normally follows the
  parent chat's Stage-2 review and confirm the human intends to bypass it. **Mode is what may run, not
  what may be authorized.**

**Context scoping (P9-M30-E30.3):**
- **Load at session start:** this starter; the Epic spec (full); the M44 milestone spec's **E44.2 Epic
  Detail (v1.2.0 amendment), Binding Constraints and Hard Constraint**; the **row P4 notes in
  `model-routing-policy.md` (`:46-118`)**; the **R6 ruling** and the **SN-40..46 ruling**; PSG
  preamble+§1, §1A, §2, §5, §6, §7, §8, §9, §11, §11.5, §11.6, §12; AOG preamble+§1, §1A, §2, §3.2,
  §3.8, §3.10, §4, §5, §6, §7, §9, §10, §12, §13, §14.
- **Load on trigger:** PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG §3.9, §8, §11, §16.
- **Do not load:** PSG/AOG changelogs; other epics' specs beyond the ordering.

---

## ⚠ The rework limit — stated here because the template does not carry it (`P12-GH-1`)

> **Maximum 3 attempts.** If a third Completion Notice is still not acceptable, do **not** issue a
> fourth rejection-and-retry. Instead you produce an **Escalation Notice** and the Milestone Chat
> escalates to the Phase Chat. **Silent fourth attempts are a governance violation.**

> **A written extension grants exactly ONE further attempt. Not a reset to three.**

The `+1` is the **SN-36/37 amendment of 2026-08-19**, CFO-decided, and **stricter** than the corpus:
`governance/systems/milestone-execution-chat-starter.md:334` still says *"resets."* **Both stand in the
corpus. Apply `+1`, cite the amendment, note the conflict.** Reconciling them is M43's work, not yours —
do not amend either surface.

---

## ⚠ How an amendment reaches this branch once work is in flight (`P11-GH-1`)

1. Amend the governing spec on its own branch, with a changelog row.
2. **Notify every running child chat in-session, naming the file and section.**
3. Require the child to re-read and to **state in its next delivery that it did.**
4. Escalate to the Phase Chat if the amendment is blocking.
5. **Before accepting any delivery, `git log` the governing spec against that epic's branch point.**

**Note for this milestone:** the governing M44 spec was already amended mid-planning (v1.2.0,
`e9be27e`) — the Phase Chat inverted E44.2's premise from *specify* to *execute*. That amendment took
this exact path. **Assume the spec can move again while you work**, and before you deliver, `git log`
`P12-M44__milestone-spec.md` and `model-routing-policy.md` against your branch point.

---

## ⚠ Hard Constraint — inherited from the M44 milestone spec

> **This milestone writes the records other work will depend on. A record that is wrong is worse than
> one that is missing, because a missing record is visible.**

- **Itemize, never count.** Where a set is named (the rows/faces the convention covers), state the list
  and the pattern, not a count.
- **Fence-awareness is not optional.** Any pass over a normative document (grep, renumber,
  cross-reference) must distinguish real content from quoted examples, and state how.
- **Falsify before trusting — in both directions.** A naive pattern that returns a plausible answer is
  as dangerous as one that returns zero. Prove any guard by falsifying it.
- **Record the practice before improving it.** E41.5 already recorded *"beside the row, per the file's
  own convention"* — where a convention already exists, describe it before changing it.
- **State the layer, time and scope of every claim** (`P11-GH-2`) **and the REF** — put the ref
  **before** the `--`, name `origin/<branch>`; a number without a ref is not a measurement.
- **No CFO-enumerated value moves.** You rewrite row P4's **language**, not a value. Changing a value
  the CFO enumerated is an escalation, not this epic.

---

## Summary

**Define, once, how the record shows a decision apart from its configuration and its evidence — and
perform the row P4 rewrite E41.5 handed over.**

`P12-GH-3`'s shape at the configuration layer: a reader assumes the file matches the ruling, and the
divergence lives in prose where no guard reaches. E44.2 defines the convention covering **both faces** —
decision-ahead-of-configuration (R6, historical) and configuration-ahead-of-evidence (SN-41, live) —
rewrites row P4's `why` cell to describe an **allowance-decided** value (discharging the handoff at
`model-routing-policy.md:116`), disposes the guard gap (the tier cells sit outside
`test_policy_mapping_agrees_with_yml_block`'s parse), and states what makes the facts distinguishable
**without inference**. **No CFO-enumerated value moves.**

---

## Spec References (load-one-reference-the-other, AOG §3.1.1)

**Full spec:** `docs/phases/P12__Completion_Fail_Closed_Defaults_and_the_Drivr_MVP/P12-M44-E44.2__spec__decided-vs-evidenced-convention.md`

The Epic spec is the authoritative copy of **Deliverables (D1–D4), Definition of Done, Acceptance
Criteria, Binding Constraints, Technical Constraints, Design Decisions Delegated, Non-Goals and
Notes.** **This starter does not duplicate them.** Cite the spec by path and branch — **not** by a
version number or sha.

**The five things easiest to get wrong, flagged here and specified there:**

1. **Moving a value instead of the language.** Only row P4's `why` cell is in scope; any CFO-enumerated
   value is out (escalation, not this epic).
2. **Making E41.5's note read as a competing account.** It is the record the rewrite closes; the two
   must read as one.
3. **Skipping the guard gap.** It is a binding criterion (D3) — disposed or stated-absent, never left.
4. **Covering only one face.** The convention must cover decision-ahead-of-configuration **and**
   configuration-ahead-of-evidence, or name which it covers and why.
5. **Emitting a coverage count.** The set of rows/faces is a list, not a number.

---

## Suite Baseline

| Repo | Baseline | Invocation |
|---|---|---|
| `ai-project-system` | **740 passed / 0 failed** measured on `milestone/M44` at `e9be27e` (2026-09-03) | `PYTHONPATH=. pytest -q` — bare `pytest` fails collection |

**Re-measure on this branch** — the milestone spec's `549` predates M41/M42/M43's test additions.
If you add a guard (D3), show it green (and red when the drift is reintroduced), name it, and state the
invocation and the ref.

---

## Method Obligations

1. **`P11-GH-2`** — layer, time and scope on every claim, and the **ref** it was measured against.
2. **G2** — the reviewer re-measures. Re-derive the row P4 facts against your branch; do not inherit the
   milestone spec's figures.
3. **G1** — quote the mapping row's *"set by allowance decision"* attribution and E41.5's note verbatim;
   remove derivation.
4. **Prove any guard by falsifying it** — reintroduce the drift, watch the check fail, restore it.
5. **Commit every artifact you author. Untracked is undelivered.**
6. **No skips introduced to route around the change.**
7. **Work in your own worktree:**
   ```
   git worktree add <your-scratchpad>/wt-e44-2 -b epic/P12-M44-E44.2 milestone/M44
   ```

---

## Deliverable Artifacts

1. The convention, defined once, both faces, location stated (D1)
2. Row P4's language rewritten to describe an allowance-decided value (D2)
3. The guard gap disposed — guard or stated absence (D3)
4. The distinguishability-without-inference statement (D4)
5. The Structural diagram (Mermaid, in-repo, **no ComfyUI**)
6. **Epic Delivery Notice**

---

## Definition of Done

**See the Epic spec §Definition of Done — the authoritative copy.**

Before opening the PR, confirm every item, **including the three easiest to skip**: no CFO-enumerated
value moved; E41.5's note reads as consistent with the rewrite (not competing); and the guard gap was
**disposed** (falsified if a guard was added).

---

## Completion

1. Commit all work to `epic/P12-M44-E44.2`
2. **Verify the push at `origin`**
3. Open a PR to **`milestone/M44`**
4. Produce the Epic Delivery Notice
5. ⏸️ **Stop and await authorization — do not merge**

Declare: *"Epic E44.2 execution complete. Epic Delivery Notice produced. Awaiting Milestone Chat
review and authorization."*

---

## Question Policy

- **Ask only blocking questions.** The two delegated design decisions (the convention's form/location,
  and guard-vs-stated-absence) are **yours**.
- **Escalate to the M44 Milestone Chat** (do not decide) if: a finding suggests a CFO-enumerated value
  would need to move to make the record coherent (that is not this epic — it is an escalation); a
  finding suggests the row P4 facts are **not** as re-measured; or E41.5's note cannot be made to read
  consistently with the rewrite without superseding it.
- **CLOSED — do not reopen:** E44.2 executes the rewrite; the three carried rows are historical not
  pending; E41.5's note is not superseded; both faces are covered; no CFO-enumerated value moves.
- **Do not scope in:** `P12-GH-3` (still filed unowned); the dispatch lanes; `chat-hierarchy.md`
  (E44.3's); `.ai-project.yml` values; `bin/`; `~/soft-dev/drivr`.
- **Do not ask for information already in the Epic spec.**

Escalate to the M44 Milestone Chat for any gap not covered here.
