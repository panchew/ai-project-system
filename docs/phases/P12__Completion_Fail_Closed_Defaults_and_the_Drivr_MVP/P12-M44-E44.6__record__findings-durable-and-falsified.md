---
project: ai-project-system
phase: P12
milestone: M44
epic: E44.6
type: record
status: delivered
last_updated: 2026-09-03
spec_executed: P12-M44-E44.6__spec__findings-durable-in-owning-tier.md (v1.0.1)
branch: epic/P12-M44-E44.6
---

# E44.6 Execution Record — D1: The Four Observed Defects, Itemized, and the Mechanical Guards

## Layer, time and scope (`P11-GH-2`)

This record is written on `epic/P12-M44-E44.6`, 2026-09-03, branched from
`origin/milestone/M44` @ `9fff3da` (verified merge-base identical to `milestone/M44`).
Its claims are about `tests/test_findings_durable_in_owning_tier.py` on this branch,
read and **run directly** (`PYTHONPATH=. pytest -q tests/test_findings_durable_in_owning_tier.py`);
its defect-status claims are about the current `milestone/M44`-based corpus on
**2026-09-03**. It says nothing about Drivr, which this epic does not touch.
References are stated per claim.

## Model verification (P9-M31-E31.3 — required; advisory per SN-40)

Harness-reported model identity: **`opencode-go/deepseek-v4-flash-vision-exp`**.
`.ai-project.yml` `models.epic_manual`: **`remote:deepseek-v4-flash`** (read, not
trusted from the starter). **Both present and DISAGREE** in two respects: the route
segment (`opencode-go` vs the config's route-less `remote:`), which the config's own
comment records as deliberate ("`remote:<name>` records WHICH MODEL, NOT WHICH
ROUTE"), and the model suffix (`vision-exp`). Per `model_verification: advisory`
(SN-40) the mismatch is stated plainly and the instance proceeds. No mismatch-caused
halt is required.

## The four observed defects, itemized — never a count (`P11-GH-2`, G1)

Issue #192 Finding 2 observed four defects. Each is itemized below with its current
**live status on this branch** (re-measured, not inherited from the 2026-08-10
assessment — G2), which guard owns it, and the falsification. **The list is the
finding; no count is emitted.**

### Defect #1 — AOG duplicate `§13`/`§14` + out-of-order sections (E44.4's D5)

- **Observed defect:** `AI-OPERATING-GUIDELINES.md` had two `§13`s and two `§14`s,
  sections ordered `1, 1A, 2–9, 13, 14, 10, 11, 12, 13, 14, 16, 15`, and two sections
  titled "Error Handling".
- **Checkable by:** heading uniqueness + monotonicity.
- **Ruled (2026-09-03, phase level):** E44.4's D5 owns this defect, in full —
  uniqueness **and** monotonicity. **E44.6 builds nothing for it.** Its contribution is
  to record the finding here and cite E44.4's check (`tests/test_aog_section_numbering.py`,
  D5) as the coverage. One detector, one check, one falsification — a second E44.6
  check would re-import the fragile fence detector and double the surface where a
  desync at `:134` can lie.
- **Live status on this branch:** **FIXED by E44.4** — the AOG's real `## ` sections
  are `1..n` on this branch. Verified by running E44.4's D5 green test against the
  artifact: `tests/test_aog_section_numbering.py::test_aog_sections_are_1_to_n_with_unique_numbers_and_titles`
  PASSES on this branch (`PYTHONPATH=. pytest -q tests/test_aog_section_numbering.py`).
- **E44.6 does not rebuild it** (Binding Constraint / Hard Constraint record-only).

### Defect #2 — `.ai-project.yml` schema contract (framework_version / models:)

- **Observed defect:** `.ai-project.yml` missing `framework_version`; and
  `bin/ai-project-init` wrote neither `framework_version` nor `models:`.
- **Checkable by:** schema validation.
- **Guard:** the schema-contract guard in `tests/test_findings_durable_in_owning_tier.py`
  — `yml_contract_errors()` asserts the five §4-required fields are present; the
  `models:` block is present and carries all seven recognized keys, each a well-formed
  `remote:|local:` route; and `framework_version`, when present, is a pinned semver
  per rule 26 (absence is valid — P10-GH-1 made it optional).
- **Live status on this branch:** the schema-validation **mechanism** already exists
  (E38.3 / P10-GH-5: `bin/ai-project-validate` + `tests/test_ai_project_validate.py`).
  The repository's own `.ai-project.yml` **is** §4-valid (re-measured: the validator's
  `validate_file` returns `valid: True`) and carries all seven `models:` keys
  (re-measured: `hq, phase, milestone, epic_dev, epic_qa, creation, epic_manual`). It
  carries **no** `framework_version` — which is **valid** (rule 26). The
  **`bin/ai-project-init` producer side** (it writes neither `framework_version` nor
  `models:`) is **ONLY RECORDED here, not fixed** — this epic is a checks epic, not a
  producer fix; the guard enforces the contract on a shipped config, so a regression
  back to an incomplete `models:` block fails the suite.
- **Falsification (in both directions, on the real corpus):**
  - Clean (real `.ai-project.yml`): `yml_contract_errors()` → `[]`.
  - Reintroduce the defect (drop `epic_qa` from the `models:` block): → `["'models:' block incomplete — missing keys: epic_qa"]`.
  - Reintroduce a non-pinned `framework_version` (`framework_version: latest`): → flagged.
  - Reintroduce a missing required field (drop `governance.ref`): → flagged.
  - Restore: → `[]`.

### Defect #3 — E36.1 claimed 4 bare `SN-23` references; actual was 17 (occurrence count)

- **Observed defect:** E36.1's delivery notice v1.0 claimed **four** bare `SN-23`
  references, all "inside the new allocation section"; the actual count was
  **seventeen**. The undercount came from **line-oriented** counting dropping any line
  that held a dated form (each new changelog row held both forms on one line, hiding
  all thirteen).
- **Checkable by:** occurrence count.
- **Guard:** the occurrence-count guard — `undated_sn23_citation_lines()` scans the two
  citation-bearing normative documents (AOG, chat-hierarchy.md) and reports any
  **un-dated `SN-23 … Decision` citation** in the normative body. It is
  occurrence-oriented (a line holding both a dated and a bare form still surfaces the
  bare form) and exempts changelog rows (E36.1's recorded non-citation classes) and
  bare-identifier prose mentions (E36.1's "names the identifier itself" class).
- **Live status on this branch:** the specific count error is **FIXED in E36.1 v1.1**
  (corrected to seventeen, occurrence-oriented, with the reasoning extended to all
  seventeen). The **class** (an un-dated `SN-23` citation in a citation-bearing body)
  was never mechanically guarded, and it is **live as a recurrence surface**. The
  guard is green on this branch (no un-dated `SN-23 … Decision` citation in either
  body).
- **Falsification (both directions, on the real AOG):**
  - Clean: `undated_sn23_citation_lines(AOG)` → `[]`.
  - Reintroduce the defect (undo E36.1's date-qualification on a real citation line):
    `SN-23 (2026-07-18) Decision 2, ratified` → `SN-23 Decision 2, ratified` → the
    guard reports `[124]`.
  - Restore: → `[]`.

### Defect #4 — Count-error tally collision at "nine" (E38.3/E38.5, stale base of eight)

- **Observed defect:** two different chats each independently recorded the *same*
  count-error ordinal, "nine", each correctly adding one to the same recorded base of
  eight, neither aware of the other. E38.3's is `ai-project-yml-spec.md` §4 rule 3
  ("All four required fields" listing five); E38.5's is Finding 1's ceiling row
  overstated by 58,247 bytes. M38's reconciliation (v1.1.4) recorded the remedy:
  **cite instances by artifact + defect, never by ordinal**; any total is a floor
  carrying its date, base and scope.
- **Checkable by:** identifier uniqueness.
- **Guard:** the identifier-uniqueness guard — `instance_identifier_issues()` flags two
  records sharing a non-None ordinal (the shape of the "stale base" collision) and two
  records sharing an id; plus a corpus check that the P11-GH-1 carry-forward note does
  not cite an instance by a bare ordinal.
- **Live status on this branch:** the specific collision is **RECONCILED** (M38 v1.1.4;
  the tally is a floored record, not a running integer — E38.6's Finding 1 remedy is
  in force). The **class** (a shared ordinal colliding; an instance cited by ordinal,
  reproducing the defect) is still guardable and **live as a recurrence surface**.
- **Falsification (both directions):**
  - Distinct ordinals (reconciled-shape): `["E38.3:rule3",8], ["E38.5:ceiling",9]` → `[]`.
  - Reintroduce the stale-base collision (both at ordinal `9`): → `["duplicate ordinal 9: 'E38.3' and 'E38.5'"]`.
  - Restore: → `[]`.

## The guards, named, and the falsification demonstration

`tests/test_findings_durable_in_owning_tier.py` — **15 tests**. Each guard has both a
real-corpus green test and a mutation falsification test that reintroduces the defect,
observes the guard fail, and restores (green). The three guards are named, never
counted as a bare total:

1. `yml_contract_errors(text)` — the `.ai-project.yml` **schema contract** (defect #2).
2. `undated_sn23_citation_lines(text)` — the **occurrence-count** citation guard
   (defect #3).
3. `instance_identifier_issues(records)` — the **identifier-uniqueness** guard
   (defect #4); plus the corpus ordinal check on the carry-forward note.

**Falsification — observed red, both directions (stale base / reintroduced defect → red;
restore → green).** Run on this branch:
`PYTHONPATH=. pytest -q tests/test_findings_durable_in_owning_tier.py` → **15 passed /
0 failed**. The live one-off mutation runs captured above (in §Defect #2/#3/#4
falsification blocks) each show the clean state green, the reintroduction red, and the
restore green — the guards are proven, not asserted.

## What was verified, not inherited

| Claim | How verified | Result |
|---|---|---|
| AOG is `1..n` on this branch (defect #1 fixed) | Ran E44.4's D5 real-corpus test | Green |
| `.ai-project.yml` is §4-valid on this branch | `bin/ai-project-validate` `validate_file` → `valid: True` | Valid |
| `.ai-project.yml` carries all seven `models:` keys | YAML parse of the artifact | All seven present |
| No un-dated `SN-23 … Decision` citation in AOG or chat-hierarchy bodies | `undated_sn23_citation_lines()` on each | None |
| The note must not cite an instance by bare ordinal | `test_guard_reads_the_carry_forward_note_without_ordinals` | Green |
| No amendment reached this branch since the branch point | `git log 9fff3da..HEAD -- <milestone spec; E44.6 spec; starter; PSG; AOG; opening ruling>` | Only this epic's own commits |

## Changelog

| Version | Date | Change |
|---|---|---|
| 1.0.0 | 2026-09-03 | Initial D1 record: the four observed defects itemized (never a count), each with its live status re-measured (G2), its owning guard, and its two-direction falsification on the real corpus. Defect #1 recorded-and-cited to E44.4's D5 (never rebuilt); #2/#3/#4 guarded by `test_findings_durable_in_owning_tier.py` (15 tests). Bin/ai-project-init's producer side of defect #2 recorded as out of scope (checks epic, not a producer fix). |
