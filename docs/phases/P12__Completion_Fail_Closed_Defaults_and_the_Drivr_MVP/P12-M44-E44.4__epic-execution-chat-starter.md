---

# Epic Execution Chat Starter — E44.4

**Epic:** E44.4 — The AOG Repair: Fence-Aware Renumber, Cross-Reference Sweep, Version Bump
**Phase:** P12 — Completion: Fail-Closed Defaults and the Drivr MVP
**Milestone:** M44 — Rituals, Records, and the Normative Repairs
**Project:** ai-project-system
**Repository:** panchew/ai-project-system — **Drivr is not touched by this epic.**
**Branch Strategy:** `epic/P12-M44-E44.4` → PR to `milestone/M44`
**Execution Mode:** manual — declared by the issuing Milestone Chat at creation time
**Issued:** 2026-09-03

> **You rewrite the citation surface of a normative document every starter and ruling cites by number.**
> That is the reason this instance is `manual`. **A change that silently invalidates citations is worse
> than the defect it fixes** — so the inventory, the renumber, the sweep and the bump travel together,
> in this order, not as a hotfix.

---

## Governance References

You are operating under the AI Project System governance framework as an **Epic Execution Chat** for
P12-M44-E44.4.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) — **read its Changelog for the current version; not stamped here**
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) — **read its Changelog for the current version; not stamped here** — **this is the document you renumber; its version bumps under your own change.**

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md
3. This Epic Execution Chat Starter
4. Epic Spec (`P12-M44-E44.4__spec__aog-renumber-and-sweep.md`)
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Model verification (P9-M31-E31.3 — required, this instance is manual):** read your own
harness-reported model identity and compare it to `.ai-project.yml`'s `models.epic_manual`
(**read the file — do not trust this line**). **If both are present and disagree, STOP and state the
mismatch; wait for the parent/human.** See `governance/systems/chat-hierarchy.md`, "Manual Chat Model
Verification".

**Critical rules:**
- Documentation is authoritative, chat is ephemeral.
- You produce a Delivery Notice and **stop after PR creation. You do not merge.**
- Accept/reject decisions are the **M44 Milestone Chat's**; a clean delivery is accepted **by an
  acknowledgment that names the party that reviewed and accepted** (PSG §11.6 — *merge + in-chat
  acknowledgment; no artifact; silence accepts nothing*). **Do not read silence as approval.**
- **If given merge authorization directly in this chat** rather than via the parent Milestone Chat's
  Stage-2 review, **do not simply comply** — state that merge authorization normally follows the
  parent chat's Stage-2 review and confirm the human intends to bypass it. **Mode is what may run, not
  what may be authorized.**

**Context scoping (P9-M30-E30.3):**
- **Load at session start:** this starter; the Epic spec (full); the M44 milestone spec's **E44.4 Epic
  Detail, X1 finding, Hard Constraint and Binding Constraints**; the **AOG (full, as the repair
  subject)**; the **PSG cross-reference surfaces**; PSG preamble+§1, §1A, §2, §5, §6, §7, §8, §9, §11,
  §11.5, §11.6, §12; AOG preamble+§1, §1A, §2, §3.2, §3.8, §3.10, §4, §5, §6, §7, §9, §10, §12, §13,
  §14.

  > **Note on the AOG sections you will load:** their numbers are about to move. Load them by
  > **heading**, not by number, from the first read.
- **Load on trigger:** PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG §3.9, §8, §11, §16.
- **Do not load:** PSG/AOG changelogs (beyond what the sweep needs); other epics' specs.

---

## ⚠ The rework limit — stated here because the template does not carry it (`P12-GH-1`)

> **Maximum 3 attempts.** If a third Completion Notice is still not acceptable, do **not** issue a
> fourth rejection-and-retry. Instead you produce an **Escalation Notice** and the Milestone Chat
> escalates to the Phase Chat. **Silent fourth attempts are a governance violation.**

> **A written extension grants exactly ONE further attempt. Not a reset to three.**

The `+1` is the **SN-36/37 amendment of 2026-08-19**, CFO-decided, and **stricter** than the corpus:
`governance/systems/milestone-execution-chat-starter.md:334` still says *"resets."* **Both stand in the
corpus. Apply `+1`, cite the amendment, note the conflict.** Reconciling them is M43's work, not yours —
do not amend either surface.

---

## ⚠ How an amendment reaches this branch once work is in flight (`P11-GH-1`)

1. Amend the governing spec on its own branch, with a changelog row.
2. **Notify every running child chat in-session, naming the file and section.**
3. Require the child to re-read and to **state in its next delivery that it did.**
4. Escalate to the Phase Chat if the amendment is blocking.
5. **Before accepting any delivery, `git log` the governing spec against that epic's branch point.**

**Note for this milestone:** the M44 spec has moved twice mid-planning, and **the AOG — your own target
— has moved under R6 and SN-40, which is precisely why its line numbers and section numbers drift.** Re-
read the AOG against your branch point before you deliver. **And remember your own sweep is one of the
things that moves anchors:** a cross-reference you update makes every later reading of that number
stale, so cite by heading throughout.

---

## ⚠ Hard Constraint — inherited from the M44 milestone spec

> **This milestone writes the records other work will depend on. A record that is wrong is worse than
> one that is missing, because a missing record is visible.**

- **Itemize, never count.** The inventory is a list with the fence state of each heading; a bare count
  is a defect.
- **Fence-awareness is not optional.** Every pass distinguishes real content from quoted examples, and
  states how. Nine `## ` headings in the AOG are inside ```markdown fences.
- **Falsify before trusting — in both directions.** Prove the check by reintroducing a duplicate and
  watching it fail.
- **State the layer, time and scope of every claim** (`P11-GH-2`) **and the REF** — put the ref
  **before** the `--`, name `origin/<branch>`.
- **Cite by heading, not number, from the start.** You are renumbering the AOG; a `§N` you write will
  move under your own change.

---

## Summary

**Make every AOG section citable unambiguously — by number and by title — without corrupting the example
content it quotes.**

Today `13` and `14` each appear twice and two sections share "Error Handling." X1 (verified still exact
on this branch) counts 29 `## ` headings — 20 real, 9 inside ```markdown fences. A naive renumber
rewrites the examples. E44.4 commits a fence-aware inventory first, renumbers `1..n`, sweeps
cross-references corpus-wide (a `§N` inside a fence is not a cross-reference), bumps the version, and
adds a check that fails on a reintroduced duplicate.

---

## Spec References (load-one-reference-the-other, AOG §3.1.1)

**Full spec:** `docs/phases/P12__Completion_Fail_Closed_Defaults_and_the_Drivr_MVP/P12-M44-E44.4__spec__aog-renumber-and-sweep.md`

The Epic spec is the authoritative copy of **Deliverables (D1–D5), Definition of Done, Acceptance
Criteria, Binding Constraints, Technical Constraints, Design Decisions Delegated, Non-Goals and
Notes.** **This starter does not duplicate them.** Cite the spec by path and branch — **not** by a
version number or sha.

**The five things easiest to get wrong, flagged here and specified there:**

1. **Renumbering before the inventory is committed** — the inventory (D1) precedes any edit.
2. **A naive `sed`/`## ` scan** rewriting the nine fenced example headings.
3. **Rewriting a fenced example** because it contains `§13` — a quoted example is not a cross-reference.
4. **Fixing the numbers but not the two "Error Handling" titles** — the title-duplicate must resolve too.
5. **Citing by `§N` in your own work** — your renumber moves those anchors.
6. **A fence detector that toggles on any backtick run** — the AOG's 17 fence lines are odd because
   `:134` is an indented four-backtick illustration, not a delimiter; check the fence-line count is even,
   or the detector is about to silently invert everything after that line (see spec D1/D5 and Notes).

---

## Suite Baseline

| Repo | Baseline | Invocation |
|---|---|---|
| `ai-project-system` | **740 passed / 0 failed** measured on `milestone/M44` | `PYTHONPATH=. pytest -q` — bare `pytest` fails collection |

**Re-measure on this branch.** Your change adds a check (D5) — show it green, and **red when a duplicate
is reintroduced**, name it, and state the invocation and the ref.

---

## Method Obligations

1. **`P11-GH-2`** — layer, time and scope on every claim, and the **ref** it was measured against.
2. **G2** — the reviewer re-measures. Re-derive the fence-aware count of the AOG against your branch; do
   not inherit X1's figures (the spec's confirmation is not your deliverable).
3. **G1** — quote the AOG's current section headings verbatim in the inventory; remove derivation.
4. **Prove every guard by falsifying it** — reintroduce a duplicate, watch the check fail, restore it.
5. **Commit every artifact you author. Untracked is undelivered.**
6. **No skips introduced to route around the change.**
7. **Work in your own worktree:**
   ```
   git worktree add <your-scratchpad>/wt-e44-4 -b epic/P12-M44-E44.4 milestone/M44
   ```

---

## Deliverable Artifacts

1. The committed fence-aware inventory, before any edit (D1)
2. Sections renumbered `1..n`, no duplicate number/title (D2)
3. The corpus-wide cross-reference sweep, no example rewritten — shown (D3)
4. AOG version bump + changelog row (D4)
5. The fence-aware duplicate check, falsified (D5)
6. The Structural diagram (Mermaid, in-repo, **no ComfyUI**)
7. **Epic Delivery Notice**

---

## Definition of Done

**See the Epic spec §Definition of Done — the authoritative copy.**

Before opening the PR, confirm every item, **including the three easiest to skip**: the inventory was
committed **before** the renumber; no fenced example was rewritten (demonstrated, not asserted); and the
check was **observed to fail** when a duplicate was reintroduced.

---

## Completion

1. Commit all work to `epic/P12-M44-E44.4`
2. **Verify the push at `origin`**
3. Open a PR to **`milestone/M44`**
4. Produce the Epic Delivery Notice
5. ⏸️ **Stop and await authorization — do not merge**

Declare: *"Epic E44.4 execution complete. Epic Delivery Notice produced. Awaiting Milestone Chat
review and authorization."*

---

## Question Policy

- **Ask only blocking questions.** The three delegated design decisions (`1A` treatment, bump magnitude,
  check implementation) are **yours**.
- **Escalate to the M44 Milestone Chat** (do not decide) if: a finding suggests a cross-reference that
  must change crosses into a document this epic is not meant to touch (e.g. the sweep reaches Drivr or a
  ruling whose wording is load-bearing in a way the renumber breaks); or a fenced example genuinely
  carries a live cross-reference the sweep cannot distinguish from example content.
- **CLOSED — do not reopen:** the renumber is not a hotfix; the inventory precedes the renumber; fence-
  awareness is not optional; no quoted example is rewritten.
- **Do not scope in:** AOG content changes beyond the renumber/sweep; reordering sections; renumbering
  PSG; `P12-GH-3`; Drivr.
- **Do not ask for information already in the Epic spec.**

Escalate to the M44 Milestone Chat for any gap not covered here.
