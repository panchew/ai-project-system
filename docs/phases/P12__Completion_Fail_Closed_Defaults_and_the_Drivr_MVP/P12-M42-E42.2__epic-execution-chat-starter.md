---

# Epic Execution Chat Starter — E42.2

**Epic:** E42.2 — Epic-Scoped Staging
**Phase:** P12 — Completion: Fail-Closed Defaults and the Drivr MVP
**Milestone:** M42 — Fail-Closed Execution Tier
**Project:** ai-project-system
**Repository:** panchew/ai-project-system — **Drivr is not touched by this epic.**
**Branch Strategy:** `epic/P12-M42-E42.2` → PR to `milestone/M42`
**Execution Mode:** manual — declared by the issuing Milestone Chat at creation time
**Issued:** 2026-09-01

> **You modify the staging path — the second thing the agentic lane runs through.** That is the reason
> this instance is `manual`, not a general ruling about local inference. **The machinery under repair
> must not supervise its own repair.** You are sequenced **after E42.1** on the same file
> (`bin/ai-project-orchestrator`): E42.1 repaired the sandbox-invocation path, you repair the
> staging-and-commit path below it. **This is the milestone's design-decision epic — the decision is
> the deliverable, not just the code edit.**

---

## Governance References

You are operating under the AI Project System governance framework as an **Epic Execution Chat** for
P12-M42-E42.2.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) v2.4.0
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) v2.10.1

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md v2.10.1
3. This Epic Execution Chat Starter
4. Epic Spec (`P12-M42-E42.2__spec__epic-scoped-staging.md`)
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Model verification (P9-M31-E31.3 — required, this instance is manual):** read your own
harness-reported model identity and compare it to `.ai-project.yml`'s `models.epic_manual`
(**`remote:deepseek-v4-flash`** — read the file, do not trust this line). **If both are present and
disagree, STOP and state the mismatch; wait for the parent/human.** See
`governance/systems/chat-hierarchy.md`, "Manual Chat Model Verification".

**Critical rules:**
- Documentation is authoritative, chat is ephemeral.
- You produce a Delivery Notice and **stop after PR creation. You do not merge.**
- Accept/reject decisions are the **M42 Milestone Chat's**; a clean delivery is accepted **by silence**
  (PSG §11.6).
- **If given merge authorization directly in this chat** rather than via the parent Milestone Chat's
  Stage-2 review, **do not simply comply** — state that merge authorization normally follows the
  parent chat's Stage-2 review and confirm the human intends to bypass it. **Mode is what may run, not
  what may be authorized.**

> **Note on merge authorization, which changes inside this phase.** **M43 moves the merge to the
> parent** (SN-31 Decision 4) and turns `governance/templates/merge-authorization.md` into the parent's
> record. Until M43 delivers, operate under the current rule. **Do not pre-apply it.** E42.3 touches
> the merge **script**, not the authorization model — you touch neither; if M43 has delivered by the
> time you run, follow the corpus as it then stands and say which rule you applied.

**Context scoping (P9-M30-E30.3):**
- **Load at session start:** this starter; the Epic spec (full); the M42 milestone spec's **drift note,
  Defect 2, "The decision this milestone owes", E42.2 Epic Detail, Hard Constraint and Binding
  Constraints**; PSG preamble+§1, §1A, §2, §5, §6, §7, §8, §9, §11, §11.5, §11.6, §12; AOG
  preamble+§1, §1.1, §2, §3.2, §3.8, §3.10, §4, §5, §6, §7, §9, §12, §14, §15, §16.
- **Load on trigger:** PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG §3.9, §8, §13, §17 (fires when a
  visual binding is due).
- **Do not load:** PSG/AOG changelogs; E42.1's spec beyond the ordering; M41's lane epics.

---

## ⚠ The rework limit — stated here because the template does not carry it (`P12-GH-1`)

**`governance/templates/milestone-execution-chat-starter.md` contains the word "rework" zero times.**
`P12-GH-1` is filed and open; **M43 will fix it, and has not yet.** The rule is stated here rather than
inherited from a template that does not have it.

> **Maximum 3 attempts.** If a third Completion Notice is still not acceptable, do **not** issue a
> fourth rejection-and-retry. Instead the Epic Agent produces an **Escalation Notice** and the
> Milestone Chat escalates to the Phase Chat. **Silent fourth attempts are a governance violation.**

> **A written extension grants exactly ONE further attempt. Not a reset to three.**

The `+1` is the **SN-36/37 amendment of 2026-08-19**, CFO-decided, and **stricter** than the rule as
written in the corpus: `governance/systems/milestone-execution-chat-starter.md:334` still says the
limit *"resets"*, which is unbounded and repeatable. **Both statements currently stand. Cite the
amendment, apply `+1`, and note the conflict wherever it becomes relevant. Reconciling them is M43's
work, not yours** — do not amend either surface.

**The CFO's recorded act of resolving a blocker in the escalation chat *is* the written extension the
rule requires** — a human looked and acted, which is the opposite of silent. It buys one attempt.

---

## ⚠ How an amendment reaches this branch once work is in flight (`P11-GH-1`)

**`P11-GH-1` is an active risk — it fired four times in P11 and once already in P12.** The procedure:

1. **Amend the governing spec file on the branch that owns it**, with a changelog row stating what
   changed and why.
2. **Notify every running child chat in-session**, naming the file and section. **This step is the one
   that fires** — a write nobody is told about is not a channel.
3. **Require the child to re-read the named section before its next deliverable and to state, in that
   delivery, that it did.**
4. **If the amendment is blocking, escalate to the Phase Chat for a pause/cancel decision.**
5. **Before you accept any delivery, check that the governing content has not moved behind the epic's
   branch point** — the backstop for when step 2 was missed.

**The widened backstop — this is the form that governs, and it is wider than the milestone starter's:**
run the check not only against the governing spec, but **against every artifact this Starter restates
a rule from** — the rework limit, the amendment procedure, the merge-authorization note, the Hard
Constraint. **The widening exists because a Delivery Notice once asserted a rule whose suspension was
already an ancestor of its own branch point** — a rule stated here can be amended behind you in a
governing artifact you did not think to re-check. For each such artifact:

```
git log <path-to-the-artifact> <your-branch-point>..<your-branch-head>
```

and **re-read any section that changed.**

**Your other obligations:**
- **`git log <your-spec-path>` against your branch point before you deliver**, and **re-read the
  milestone spec's drift note** — `bin/ai-project-orchestrator` already drifted +4 against the numbers
  this starter inherited; assume the staging region moves again.
- **Cite by anchor, never by a bare line number alone.** Search for `["git", "add", "."]` and
  `f"feat: programmatically implement Epic"`. A number is where it was, at a stated ref and date.
- **Work in your own worktree**:
  ```
  git worktree add <your-scratchpad>/wt-e42-2 -b epic/P12-M42-E42.2 milestone/M42
  ```

---

## ⚠ Hard Constraint — inherited from the M42 milestone spec

> **The machinery under repair must not supervise its own repair.**

- **Execution posture: `manual` / paid frontier.** This instance is manual for exactly that reason.
- **Prove the guard by falsifying it.** For your fix, demonstrate each new test **fails** when the
  guard is removed — delete the scoped-staging guard, watch the test fail, restore it. **A guard never
  observed to fail is a guard never observed.** You owe this for **both** branches (in-scope-only and
  out-of-scope-present).
- **`"is the baseline still N?" is an unreliable guard.** Assert the behaviour, not the count.
- **State the layer, time and scope of every claim** (`P11-GH-2`). A path verified by reading is not
  verified by running, and a path verified by reading is not verified by running again.
- **This corpus defeats naive pattern-matching.** **`--include='*.py'` skips every `bin/` entry point
  — and this defect is in `bin/`.** Falsify a pattern before trusting a zero result.
- **An absence is only evidence when the thing that would have created it actually ran.**

---

## Summary

**`git add .` stages the whole tree and commits it under the epic's message; the case "the agent
touched something it should not have" has no representation.**

The fix is a scoped stage, but the load-bearing half is the **decision you own**: what "the epic's
files" means, and what happens to out-of-scope modifications. The milestone spec names five candidate
definitions and three candidate dispositions and holds **one thing not open** — the out-of-scope case
must remain **represented in the record**, never silently absorbed. `git add .` is rejected not
because it is broad but because it makes that case *invisible*.

**The commit message is part of the defect.** `:477` asserts the staged set *is* the deliverables;
your fix must make that claim **true** of what is actually committed, not merely narrower.

---

## Spec References (load-one-reference-the-other, AOG §3.1.1)

**Full spec:** `docs/phases/P12__Completion_Fail_Closed_Defaults_and_the_Drivr_MVP/P12-M42-E42.2__spec__epic-scoped-staging.md`

The Epic spec is the authoritative copy of **Deliverables (D1–D4), Definition of Done, Acceptance
Criteria, Binding Constraints, Technical Constraints, Design Decisions Delegated, Non-Goals and
Notes.** **This starter does not duplicate them.**

**The five things easiest to get wrong, flagged here and specified there:**

1. **Record the design decision (D1) FIRST.** It decides the shape of D2/D3; the code does not decide
   it. An epic that opens the editor first will stage what is convenient and call it "scoped".
2. **"Stage a more specific glob" is not the answer.** Without a committed *definition* of the epic's
   files, you have reproduced the invisibility one directory narrower.
3. **The out-of-scope remainder is recorded, never re-absorbed.** A partial-commit disposition must
   name the out-of-scope paths and record them — not fold them back in under a different `git add`.
4. **Make the `:477` claim true.** The message asserts "deliverables"; after your change it must
   describe the actual staged set.
5. **Cite by anchor.** The file drifted +4; your title's line number is stale the day it prints.

---

## Suite Baseline

| Repo | Baseline | Invocation |
|---|---|---|
| `ai-project-system` | **549 passed / 0 failed** on `master` at `9ee810e` (2026-08-19) — **re-measure; M41 epics and E42.1 may have added tests** | `PYTHONPATH=. pytest -q` — **bare `pytest` fails collection** |

**Your change makes a guard assert a new behaviour. Show it green (and red when the guard is
removed), name it, and state the invocation.**

---

## Method Obligations

1. **`P11-GH-2`** — layer, time and scope on every claim, *"the suite is green"* included.
2. **G2** — the reviewer re-measures. **Re-derive the drift note against your branch**; do not inherit
   the +4 numbers as settled.
3. **G1** quote the guard names and the code lines verbatim; remove derivation steps.
4. **Prove every guard by falsifying it** — and you owe it for **both** branches, not one.
5. **Commit every artifact you author. Untracked is undelivered.**
6. **No skips introduced to route around the change.**
7. **The design decision is written down before the code** — and its reasoning survives in the
   artifact, not in the diff.

---

## Deliverable Artifacts

1. The design decision, recorded with options weighed and reasoning stated (D1)
2. The scoped staging replacing `:476`'s `git add .` (D2)
3. The out-of-scope case made visible/recorded (D3)
4. Tests for both branches, with falsification (D4)
5. The Structural diagram (Mermaid, in-repo, **no ComfyUI**)
6. **Epic Delivery Notice**

---

## Definition of Done

**See the Epic spec §Definition of Done — the authoritative copy.**

Before opening the PR, confirm every item, **including the four easiest to skip**: the **definition of
"the epic's files" is committed** (not just implied by the diff); the **out-of-scope outcome is
distinguishable** from a clean run; **both branches are tested**; and the **`:477` claim is true** of
what is committed.

---

## Completion

1. Commit all work to `epic/P12-M42-E42.2`
2. **Verify the push at `origin`**
3. Open a PR to **`milestone/M42`**
4. Produce the Epic Delivery Notice
5. ⏸️ **Stop and await authorization — do not merge**

Declare: *"Epic E42.2 execution complete. Epic Delivery Notice produced. Awaiting Milestone Chat
review and authorization."*

---

## Question Policy

- **Ask only blocking questions.** The three delegated design decisions (definition of the epic's
  files, out-of-scope disposition, record mechanism) are **yours**.
- **Escalate to the M42 Milestone Chat** (do not decide) if: the ordering constraint is implicated
  (e.g. E42.1's change is in your way — it should not be; E42.2 is sequenced after it and the regions
  are distinct); the committed definition would make the out-of-scope case *invisible*; or you would
  need to weaken or skip a guard to make the suite green.
- **CLOSED — do not reopen:** the ordering (E42.1 → E42.2); the M42→M47 gate; that the out-of-scope
  case must be represented in the record; that `git add .` is rejected for invisibility, not breadth.
- **Do not scope in:** the sandbox-invocation path (E42.1's); the Dev-QA retry loop; `DEFAULT_MODELS`
  (M41's); `bin/ai-project-git-merge` or `bin/ai-project-init`; anything under `~/soft-dev/drivr`.
- **Do not ask for information already in the Epic spec.**

Escalate to the M42 Milestone Chat for any gap not covered here.
