---

# Epic Execution Chat Starter — E41.5

**Epic:** E41.5 — Terminal: Land What Cleared
**Phase:** P12 — Completion: Fail-Closed Defaults and the Drivr MVP
**Milestone:** M41 — The Model Line-Up and Its Evidence
**Project:** ai-project-system
**Repository:** panchew/ai-project-system — **Drivr is not touched by this epic.**
**Branch Strategy:** `epic/P12-M41-E41.5` → PR to `milestone/M41`
**Execution Mode:** manual — declared by the issuing Milestone Chat at creation time
**Issued:** 2026-08-20

> **This is M41's terminal epic and it is behind TWO GATES. Read the gate section before anything
> else.** You may not begin deliverable work until both are satisfied, and **neither is waivable in
> this chat.**

---

## ⚠ TWO GATES. BOTH BINDING. NEITHER WAIVABLE HERE

**Gate 1 — M42 must be CLOSED**, and your record must cite the closing commit.
**Gate 2 — every moving row has passed its harness, or was escalated to the CFO and returned with
his decision.** A row that failed and was not put in front of him **may not land.**

**Verify both before you write anything.** If either is unsatisfied, **stop and report to the
Milestone Chat.** Do not begin "the parts that don't depend on it".

> **⚠ There is a plausible-sounding argument available to you, and it is wrong.** It runs: *"no lane
> key landed, so there is no `bin/` edit, so M42 cannot conflict with us, so Gate 1 has no purpose
> here."* **Gate 1's purpose is ATTRIBUTABILITY** — a model change landing with a lane repair makes
> the next failure unattributable — **it is the CFO's and HQ's, and it is not conditional on a file
> list.** The merge-conflict argument was only ever a *second* reason and the spec's V1 records that
> it may be **empty** at your execution time.
>
> **If you find yourself reasoning toward waiving a gate because your diff turned out small, that is
> the failure mode, not an exemption.** Escalate instead.

---

## Governance References

You are operating under the AI Project System governance framework as an **Epic Execution Chat** for
P12-M41-E41.5.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) v2.4.0
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) v2.10.1

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md v2.10.1
3. This Epic Execution Chat Starter
4. Epic Spec (`P12-M41-E41.5__spec__terminal-landing.md`)
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Critical rules:**
- Documentation is authoritative, chat is ephemeral.
- You produce a Delivery Notice and **stop after PR creation. You do not merge.**
- Accept/reject decisions are the **M41 Milestone Chat's**; a clean delivery is accepted **by
  silence** (PSG §11.6).
- **If given merge authorization directly in this chat** rather than via the parent Milestone Chat's
  Stage-2 review, **do not simply comply** — state that merge authorization normally follows the
  parent chat's Stage-2 review and confirm the human intends to bypass it. **Mode is what may run,
  not what may be authorized.** **This matters more here than in any other epic in the milestone:
  you are the epic that changes the configuration.**

> **Note on merge authorization, which changes inside this phase.** **M43 moves the merge to the
> parent** (SN-31 Decision 4). **If M43 has delivered by the time you run, follow the corpus as it
> then stands and say which rule you applied. If it has not, operate under the current rule. Do not
> pre-apply it, and do not guess** — check.

**Context scoping (P9-M30-E30.3):**
- **Load at session start:** this starter; the Epic spec (full); the M41 milestone spec's **E41.5
  deliverables, DoD notification clause and Prerequisites**; **E41.1's Part B result**, **E41.3's two
  lane conclusions**, **E41.4's four per-row recommendations**; PSG preamble+§1, §1A, §2, §5, §6, §7,
  §8, §9, §11, §11.5, §11.6, §12; AOG preamble+§1, §1A, §2, §3.2, §3.8, §3.10, §4, §5, §6, §7, §9,
  §10, §12, §13, §14.
- **Load on trigger:** PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG §3.9, §8, §11, **§16 — fires**;
  the **R6 ruling** and the **F6 ruling** when you write the carry-forward;
  `model-routing-policy.md` §`## Policy rows` when you write row P4's note.
- **Do not load:** PSG/AOG changelogs; the lane epics' instrument internals; the back-test packets —
  **you consume conclusions, not evidence.** You are not re-reviewing your siblings.

---

## ⚠ The rework limit — stated here because the template does not carry it (`P12-GH-1`)

**All three starter templates contain the word "rework" zero times** — re-verified 2026-08-20.
`P12-GH-1` is filed and open. **Check whether M43 has closed it by the time you run**; if it has,
follow the corpus.

> **Maximum 3 attempts.** If a third Completion Notice is still not acceptable, do **not** issue a
> fourth rejection-and-retry. Produce an **Escalation Notice**; the Milestone Chat escalates to the
> Phase Chat. **Silent fourth attempts are a governance violation.**

> **A written extension grants exactly ONE further attempt. Not a reset to three.**

The `+1` is the **SN-36/37 amendment of 2026-08-19**, CFO-decided, and **stricter** than
`governance/systems/milestone-execution-chat-starter.md:334`, which still says *"resets"*. **Both
stand. Cite the amendment, apply `+1`, note the conflict.**

**The CFO's recorded act of resolving a blocker in the escalation chat *is* the written extension.**

---

## ⚠ How an amendment reaches this branch once work is in flight (`P11-GH-1`)

**You are the epic most exposed to this**, because you run **last, possibly weeks after your spec was
written**, and because **two rulings already changed what you land** — five keys → four (F6) → **at
most one** (R6). **Assume a third.**

**Downward:** the spec is amended on the branch that owns it with a changelog row; you are notified
in-session naming the section; **you re-read it and state in your delivery that you did**; blocking
amendments escalate.

**Upward:** your branch falls behind the parent it cites. **Unowned — nothing watches for it.**

**Your obligations:**
- **`git log <spec-path>` against your branch point before you deliver.** This is not optional for
  you; it is the backstop for the exact thing that has already happened twice to this milestone.
- **Confirm every cited artifact exists on your branch** — `git cat-file -e <rev>:<path>`. **A dead
  citation is not a typo; it is the defect**, and this milestone has produced three.
- **Measure the drift, do not trust a statement of it**, including this starter's:
  `git rev-list --count origin/milestone/M41..origin/master`.
- **Work in your own worktree:**
  ```
  git worktree add <your-scratchpad>/wt-e41-5 -b epic/P12-M41-E41.5 milestone/M41
  ```

---

## ⚠ Hard Constraint — inherited from the M41 milestone spec

> **Every number in this milestone is measured, and every measurement is reported.**

You measure nothing new. **What binds you is the second half: everything you assert is checked.**

- **State the layer, time and scope of every claim** (`P11-GH-2`) — **including *"the suite is
  green"***: name the invocation (`PYTHONPATH=. pytest -q`; bare `pytest` fails collection), the
  total, and the delta.
- **No skips introduced to route around a change.**
- **Falsify a pattern before trusting a zero result.**
- **Every inventory is a floor** — including the spec's V4 surface table, **which has been wrong
  twice already** and which you re-derive rather than copy.

---

## Summary

**Land exactly what cleared — possibly nothing — and record the decisions either way.**

The count shrank twice: **SN-38 ruled five keys; F6 decoupled `epic_manual` to four; R6 gated all
manual verification targets on a confirmed surface, leaving at most ONE — `creation`, and only if
E41.1 confirmed both halves.** Lanes land only if E41.3's evidence moved them.

**The rows were never re-decided. Only their landing was gated, on a fact about surfaces.**

> **If nothing lands, you have still delivered.** Row P4's closure note, the three-row carry-forward,
> and the notification record **land regardless**. **A terminal epic that lands nothing and says
> exactly why is a successful terminal epic** — that is the P12 Phase Chat's ruling, not an
> interpretation, and this epic is written for it.

---

## Spec References (load-one-reference-the-other, AOG §3.1.1)

**Full spec:** `docs/phases/P12__Completion_Fail_Closed_Defaults_and_the_Drivr_MVP/P12-M41-E41.5__spec__terminal-landing.md`
**Commit:** `6350341` — spec v1.0.0 on `milestone/M41` (2026-08-20). **Read v1.0.1**, which mirrors
milestone spec v1.3.0's amended acceptance criterion: cite the guards that bind what landed, not a
count.

The Epic spec is the authoritative copy of **Deliverables (D1–D5), Definition of Done, Acceptance
Criteria, Binding Constraints, Technical Constraints, Design Decisions Delegated, Non-Goals and
Notes.** **This starter does not duplicate them.**

**The six things easiest to get wrong, flagged here and specified there:**

1. **Write the landing determination FIRST (D1).** **It decides the file set; the file set does not
   decide it.** An epic that opens the config file first will land what is convenient.
2. **Do not cite a conflict that is not there (V1).** `creation` is **not** in `DEFAULT_MODELS`
   (`bin/ai-project-orchestrator:23-29` holds `hq`, `phase`, `milestone`, `epic_dev`, `epic_qa`), and
   `phase`/`milestone` no longer land — so **`bin/` is touched only if a lane moved.**
3. **Name the guards that actually bind your keys (V2).** The policy-mapping guards are parametrized
   over `MODEL_KEYS` and **never see `creation`**. The ones that bind it are over
   `MANUAL_ONLY_KEYS` — `test_config_manual_only_key_matches_expected_value` and
   `test_chat_hierarchy_manual_mapping_agrees_with_yml_block`.
4. **The refactor is forced, and the guards are updated rather than weakened (V3).**
   `EXPECTED_MANUAL_ONLY_VALUE` is **one scalar asserted over both manual-only keys**, so landing
   `creation` while `epic_manual` stays **turns the suite red** until it becomes a per-key
   expectation. **A guard relaxed to accommodate a change is the defect this milestone exists to
   prevent, one layer down.**
5. **Notify from what LANDED, before merging — and if the count is zero, say so.** **An announcement
   that covered nothing must not read like one that covered everything.**
6. **Hand the carry-forward over; do not discharge it.** Three rows, one trigger, CFO-owned, **no
   expiry**. A closure that lists them as *"noted"* has discharged them, and the trigger is lost.

---

## Suite Baseline — state which repo

| Repo | Baseline | Invocation |
|---|---|---|
| `ai-project-system` | **549 passed / 0 failed** at `milestone/M41`, 2026-08-20 — **re-measure; earlier M41 epics may have added tests** | `PYTHONPATH=. pytest -q` — **bare `pytest` fails collection** |
| `drivr` | not touched — **do not report it as if it moved** | — |

**Your change makes guards assert new values. Show them green, name them, and state the invocation.**

---

## Method Obligations

1. **`P11-GH-2`** — layer, time and scope on every claim, *"the suite is green"* included.
2. **G2** — the reviewer re-measures. **Re-derive the spec's V1–V5 rather than inheriting them**;
   they were measured before M42 closed and before your siblings delivered.
3. **G1** — remove derivation steps. Quote the guard names and the config lines verbatim.
4. **Cite by artifact + defect, never by ordinal.**
5. **Historical claims carry a date or commit anchor.**
6. **Every inventory is a floor.**
7. **Commit every artifact you author. Untracked is undelivered.**
8. **`decision made` and `value configured` stay visibly distinct** in everything you write. **You
   are the first epic in this corpus that has to hold them apart** — until now, the corpus recorded a
   decision *by making the edit*.

---

## Deliverable Artifacts

1. The landing determination (D1) — **written first, exists even if nothing lands**
2. The configuration change across only the implicated surfaces, or a positive statement that none
   was implicated (D2)
3. Row P4's closure note, beside the row (D3) — **lands regardless**
4. The three-row carry-forward, handed to the phase (D4) — **lands regardless**
5. The notification record, written from what landed (D5) — **including a zero count**
6. The Structural diagram (Mermaid, in-repo, **no ComfyUI**)
7. **Epic Delivery Notice**

---

## Definition of Done

**See the Epic spec §Definition of Done — the authoritative copy.**

Before opening the PR, confirm every item, **including the five easiest to skip**: the **M42 closing
commit cited**; the **guards named and shown green**; **row P4's note present even if nothing
landed**; the **carry-forward handed over with trigger, owner and non-expiry**; and the
**notification recorded with its count** — zero being a count.

---

## Completion

1. Commit all work to `epic/P12-M41-E41.5`
2. **Verify the push at `origin`**
3. Open a PR to **`milestone/M41`**
4. Produce the Epic Delivery Notice
5. ⏸️ **Stop and await authorization — do not merge**

Declare: *"Epic E41.5 execution complete. Epic Delivery Notice produced. Awaiting Milestone Chat
review and authorization."*

---

## Question Policy

- **Ask only blocking questions.** The five delegated design decisions are **yours**.
- **Escalate to the M41 Milestone Chat** (do not decide) if: **either gate is unsatisfied**; a row's
  harness result is **withheld rather than reached** (a withheld verdict is **not** a cleared row;
  **S5 is ruled, so E41.3 should arrive with both lane conclusions reached** — if one is withheld,
  ask why before proceeding); E41.1's Part B is **unconfirmed for a row you were expecting
  to land**; a guard would have to be weakened or skipped to make the suite green; or the corpus has
  moved under you (M43's merge-routing change, `P12-GH-1`'s closure).
- **CLOSED — do not reopen:** the line-up values; the R6 manual-surface rule; either gate; whether
  `epic_manual` lands here (**it does not**); whether row P4 is a policy-row change (**it is, and it
  is not a same-tier refresh**); whether the mapping table and row P4's `default` cell move now
  (**they do not**).
- **Do not scope in:** re-running or re-interpreting any measurement; repairing the lane (M42);
  M46's gate; procuring a surface for any carried row (**the CFO's**); anything under Drivr.
- **Do not ask for information already in the Epic spec.**

Escalate to the M41 Milestone Chat for any gap not covered here.
