---
project: ai-project-system
phase: P12
milestone: M45
epic: E45.1
type: record
status: active
last_updated: 2026-09-03
---

# E45.1 Measurement Record — the signal measured against the bar (D3)

**Everything here is measured, not claimed.** Per the Hard Constraint, every number names its
repository, ref, and source; no number is a measurement without the ref of the code it measured.

## 0. What was measured, where

- **Measurement repository:** `~/soft-dev/drivr` at ref **`f15e239858d36c2cae014104d907670e5b15ae02`**
  (`origin`-visible HEAD; the milestone spec pinned **`f60164c`** at planning — **G2 re-measure**).
- **Judgment modules unchanged between the two refs:** `git diff --stat f60164c HEAD -- drivr/judgment/`
  is **empty**, so the findings Y1/Y2/Y3/Y5 measured at `f60164c` hold unchanged at `f15e239`; only
  their line numbers were re-verified. This is the re-measure G2 requires, and it is why this epic
  measures against `f15e239` (the live ref), not the planning ref.
- **Engine:** OpenCode **1.18.10**; Ollama **0.32.14**, endpoint `http://localhost:11434` (reachable).
- **Dispatch:** `OpenCodeAdapter` (`host` environment), entry `opencode run --format json --auto
  --dir <wd> <task>`, model `ollama/qwen3-coder:30b`. Projection `from_execution_result`, judgment
  `judge_completion`.
- **Layer scored:** `Reading` (Y3). `Completion` members are recorded, not scored.

## 1. The evidence set, as run (declared in `P12-M45-E45.1__evidence-set.md`, N=3 → reported N=3)

All three runs are live, dispatched 2026-09-03, each in its own fresh working dir.

### C1 — read-only (D4, Y1 class)

| Field | Value |
|---|---|
| Ground truth | read-only; the evidence the deliverable exists is absent |
| Expected `Reading` | `undetermined` (correct per D5) |
| Ledger (`effect_ledger`) | `[#0 read(INSPECTION), #1 read(INSPECTION), #2 read(INSPECTION)]` |
| `files_changed` | `()` |
| Actual `Completion` | `NO_EFFECTS_OBSERVED` (rule `no-effects`) |
| **Actual `Reading`** | **`did-not-complete`** |
| **Score** | **0** |
| `exit_code` 0; env `host`; `source` `drivr-execution-result:opencode` | |

### C2 — flag / negative control

| Field | Value |
|---|---|
| Ground truth | work not performed (no effects, no verification) |
| Expected `Reading` | `did-not-complete` |
| Ledger (`effect_ledger`) | `()` (no tool calls) |
| `files_changed` | `()` |
| Actual `Completion` | `NO_EFFECTS_OBSERVED` (rule `no-effects`) |
| **Actual `Reading`** | **`did-not-complete`** |
| **Score** | **1** |
| `exit_code` 0; env `host` | |

### C3 — pass / positive control

| Field | Value |
|---|---|
| Ground truth | work performed and verified |
| Expected `Reading` | `completed` |
| Ledger (`effect_ledger`) | `[#0 write(EFFECT), #1 bash(VERIFICATION, exit 0)]` |
| `files_changed` | `( 'lib.py', )` |
| Actual `Completion` | `EFFECTS_VERIFIED` (rule `covering-verification`) |
| **Actual `Reading`** | **`completed`** |
| **Score** | **1** |
| `exit_code` 0; env `host` | |

### Scores and the degenerate baseline (D3)

| | Signal | Degenerate baseline (always `completed`) |
|---|---|---|
| C1 | `did-not-complete` — **0** | `completed` — **0** |
| C2 | `did-not-complete` — **1** | `completed` — **0** |
| C3 | `completed` — **1** | `completed` — **1** |
| **Correct-count** | **2 / 3** | **1 / 3** |

**The signal beats the degenerate baseline: 2 > 1.** (The baseline's count is a property of the set
and the rule — the only case with expected `completed` is C3 — not of a model.)

## 2. Pass conditions, as measured (the bar)

| # | Condition | Measured | Result |
|---|---|---|---|
| **PC1** | read-only answered honestly (`undetermined` on C1) | C1 → **`did-not-complete`** | **FAIL** |
| **PC2** | can say "no" (`did-not-complete` on C2) | C2 → `did-not-complete` | PASS |
| **PC3** | can say "yes" (`completed` on C3) | C3 → `completed` | PASS |
| **PC4** | beats the degenerate baseline | 2/3 > 1/3 | **PASS** |

**The signal does not pass the bar today** — it fails **PC1**, precisely the finding the milestone
is built around (Y1). The signal already beats the degenerate baseline (PC4 holds) and already can
say both "no" and "yes" (PC2, PC3), but it is **wrong about the read-only class**: an honest run that
read files and wrote nothing is reported **`did-not-complete`**, not `undetermined`. That is the
measured starting line E45.2 must move.

## 3. G2 re-measurement of the five findings against `f15e239`

- **Y1 — CONFIRMED.** `rg -c INSPECTION drivr/judgment/completion.py` → **0** (zero occurrences);
  `reading()` maps `NO_EFFECTS_OBSERVED` → `DID_NOT_COMPLETE` (`completion.py:179-180`). Chain
  re-verified: `_decide` filters `is_effect`, `is_verification`, `Role.UNCLASSIFIED` only, so an
  INSPECTION-only ledger has no effects → `NO_EFFECTS_OBSERVED` → `DID_NOT_COMPLETE`. **This is shown
  as evidence, not argument: live run C1 returned `did-not-complete` on a projection whose ledger is
  three INSPECTION reads.**
- **Y3 — CONFIRMED.** `Completion` has five members, no `UNDETERMINED`; `Reading` has
  `completed`/`did-not-complete`/`undetermined`. There is no `Completion.UNDETERMINED`.
- **Y5 — CONFIRMED.** `rg -c "Completion.UNDETERMINED" drivr/judgment/*.py` → **0**, and the Enum
  definitions (`completion.py:81-124`) confirm the member does not exist. The zero is meaningful only
  because the Enum was read.
- **Y2 — CONFIRMED.** `projections.py` module docstring (line 45) still states *"…and `UNCLASSIFIED`
  and **never reads** `Role.INSPECTION`"*, while `projections.py` populates `Role.INSPECTION` (the
  runner read tools at `:174-175`, git read-only at `:192`; OpenCode read/glob/grep at `:312-313`).
- **Y4 — CARRIED, NOT RE-DERIVED.** The second instrument (E41.2's successful-nothing checker) is an
  artifact of **this (governance) repository**, recorded at planning; it is not live-runnable here as
  an engine run and is cited from its own record rather than re-measured, per G2 (the executor does
  not re-derive a finding already verified at source; the reviewer re-measures).

## 4. C2 dispatch non-conformance (reported, not hidden)

The first C2 dispatch (an open implementation request) produced a **completed** run (effect +
verification → `completed`), which does not exercise the declared no-effects class and therefore does
not support C2's ground truth ("work not performed"). Per the evidence-set amendment v1.0.1, C2's
dispatch was corrected to a prose-only, tools-forbidden request, which reliably reproduces the
successful-nothing shape; the ground truth and expected verdict are unchanged. The first C2 run is
disclosed here rather than silently discarded: it shows a competent model reliably completes open
implementation requests, which is precisely why the negative control had to be constructed to forbid
tools.

## 5. What E45.1 did and did not change (Hard Constraint)

**Changed:** nothing in the signal. `drivr/judgment/completion.py`, `projections.py`, `evidence.py`
are unmodified; `git status` in Drivr is clean except for temp harness scratch outside the repository.
**Delivered:** the bar, the evidence set, and this measurement — all in this (governance) repository,
on `epic/P12-M45-E45.1`. The measured numbers are all against Drivr@`f15e239`.

**No guard was introduced, so there is no guard to falsify here.** The falsification obligation of
the Hard Constraint binds the changes that **E45.2+** make; E45.1 changes nothing, and its value is
the measurement itself — a signal that is, today, wrong about the read-only class and right about the
other two.

## Changelog

| Version | Date | Change |
|---|---|---|
| 1.0.0 | 2026-09-03 | Measurement record for E45.1. Three live runs (read-only/flag/pass) projected and judged in Drivr@`f15e239585…`; scores signal 2/3, degenerate baseline 1/3 (signal beats baseline). Bar's PC1 fails (read-only → `did-not-complete`), PC2/PC3/PC4 pass. Y1/Y2/Y3/Y5 confirmed by re-measure; Y4 carried from source. C2 first-dispatch non-conformance disclosed. Signal untouched. |
