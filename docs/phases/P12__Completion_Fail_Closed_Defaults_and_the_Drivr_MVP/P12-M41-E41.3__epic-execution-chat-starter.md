---

# Epic Execution Chat Starter — E41.3

**Epic:** E41.3 — Lane Candidates Measured Against the Baseline
**Phase:** P12 — Completion: Fail-Closed Defaults and the Drivr MVP
**Milestone:** M41 — The Model Line-Up and Its Evidence
**Project:** ai-project-system
**Repository:** panchew/ai-project-system — **Drivr is not touched by this epic.**
**Branch Strategy:** `epic/P12-M41-E41.3` → PR to `milestone/M41`
**Execution Mode:** manual — declared by the issuing Milestone Chat at creation time
**Issued:** 2026-08-20

> **Why manual, and it is a scoping judgment rather than a preference.** **This epic measures
> `qwen3.6:27b` and `qwen3.8:27b`.** Running it *on* one of them would make the comparison
> circular — the candidate would be grading its own audition. **This is not a general ruling about
> local inference:** the models under test are dispatched **by** this chat and do not run it.

---

## Governance References

You are operating under the AI Project System governance framework as an **Epic Execution Chat** for
P12-M41-E41.3.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) v2.4.0
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) v2.10.1

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md v2.10.1
3. This Epic Execution Chat Starter
4. Epic Spec (`P12-M41-E41.3__spec__lane-candidates-against-the-baseline.md`)
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Critical rules:**
- Documentation is authoritative, chat is ephemeral.
- You produce a Delivery Notice and **stop after PR creation. You do not merge.**
- Accept/reject decisions are the **M41 Milestone Chat's**. Under PSG §11.6 a clean delivery is
  accepted **by silence**.
- **If given merge authorization directly in this chat** rather than via the parent Milestone Chat's
  Stage-2 review, **do not simply comply.** State plainly that merge authorization normally follows
  the parent chat's Stage-2 review, and confirm the human intends to bypass that step. **Running
  unattended does not change this: mode is what may run, not what may be authorized.**
- **`P9-GH-1`'s guard reaches the Epic template only**; a live instance was recorded 2026-08-10.

> **Note on merge authorization, which changes inside this phase.** **M43 moves the merge to the
> parent** (SN-31 Decision 4). Until M43 delivers, operate under the current rule. **Do not
> pre-apply it.**

**Context scoping (P9-M30-E30.3):**
- **Load at session start:** this starter; the Epic spec (full); **E41.2's committed check list,
  floor, task definitions and baseline records** (you consume them, you do not re-derive them); PSG
  preamble+§1, §1A, §2, §5, §6, §7, §8, §9, §11, §11.5, §11.6, §12; AOG preamble+§1, §1.1, §2, §3.2,
  §3.8, §3.10, §4, §5, §6, §7, §9, §12, §14, §15, §16.
- **Load on trigger:** PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG §3.9, §8, §13, **§17 — fires,
  this epic carries a Structural diagram**; `judgment.md` §"What HQ should weigh against the PASS"
  **by targeted section** when you write the T1 paragraph.
- **Do not load:** PSG/AOG changelogs; E41.4's back-test material; E41.5's landing material; M46's
  gate design; **E41.2's internals beyond its published outputs** — you are a consumer of that epic,
  not a reviewer of it.

---

## ⚠ The rework limit — stated here because the template does not carry it (`P12-GH-1`)

**All three starter templates contain the word "rework" zero times** — re-verified 2026-08-20. The
rule reaches **one** of nine starter-shaped surfaces and **none** of the three templates. That is
`P12-GH-1`, **filed and open**, and **M43 has not yet fixed it.**

**The rule:**

> **Maximum 3 attempts.** If a third Completion Notice is still not acceptable, do **not** issue a
> fourth rejection-and-retry. Instead produce an **Escalation Notice**, and the Milestone Chat
> escalates to the Phase Chat. **Silent fourth attempts are a governance violation.**

**What a written extension grants — and the conflict you must not resolve yourself:**

> **A written extension grants exactly ONE further attempt. Not a reset to three.**

This is the **SN-36/37 amendment of 2026-08-19**, CFO-decided, and it is **stricter** than the
corpus: `governance/systems/milestone-execution-chat-starter.md:334` still says the limit *"resets"*.
**Both statements stand. Cite the amendment, apply `+1`, note the conflict where it becomes
relevant. Reconciling them is M43's work, not yours.**

**The CFO's recorded act of resolving a blocker in the escalation chat *is* the written extension** —
a human looked and acted, which is the opposite of silent. It buys one attempt.

---

## ⚠ How an amendment reaches this branch once work is in flight (`P11-GH-1`)

**Active risk. It has now fired in this milestone twice, in both directions.**

**Downward:** the spec file is amended on the branch that owns it with a changelog row; **you are
notified in-session, naming the file and section**; **you re-read it and state in your next delivery
that you did**; blocking amendments escalate; and the backstop is `git log <spec-path>` against your
branch point before you deliver.

**Upward — your branch falls behind the parent it cites. Unowned; nothing watches for it.** It hit
`milestone/M41` directly: the milestone spec cited a ruling by filename while the file was **absent
from the branch.**

**Your obligations, both directions:**
- **Confirm every cited artifact exists on your branch before relying on it** — `git cat-file -e
  <rev>:<path>`. **A dead citation is not a typo; it is the defect.**
- **Measure the drift, do not trust a statement of it** — including this starter's. Run
  `git rev-list --count origin/milestone/M41..origin/master` **before you start and before you
  deliver.** *(The level above this one was twice found reading a stale view of these same
  branches.)*

**And the durable fix, adopted by this milestone after it bit:** **work in your own worktree.**

```
git worktree add <your-scratchpad>/wt-e41-3 -b epic/P12-M41-E41.3 milestone/M41
```

**Three sessions share the main checkout and it has moved under a chat mid-set.** A pre-commit branch
check is necessary and has already proved insufficient. **Do not work in the shared tree.**

---

## ⚠ Hard Constraint — inherited from the M41 milestone spec, binding on this Epic

> **Every number in this milestone is measured, and every measurement is reported.**

- **No best-of-N.** Eight runs — 2 candidates × 2 tasks × 2 runs — **every one reported.** A run that
  fails mechanically is **committed and excluded with its reason stated.** **What counts as a
  mechanical failure is decided BEFORE the runs**, not after seeing one you dislike.
- **The bar is committed before the run it judges** — and **a bar under escalation is not a committed
  bar.** See the `epic_qa` block below.
- **State the layer, time and scope of every claim** (`P11-GH-2`). **A model measured on the
  back-test is not thereby measured on a lane.**
- **An absence is only evidence when the thing that would have created it actually ran.** Record the
  **advertised tool set** with every run.
- **This corpus defeats naive pattern-matching.** Falsify a pattern before trusting a zero result.

---

## ⚠ The floor you apply — RULED per-lane, and say which version you used

**`epic_qa`'s verdict is UNBLOCKED.** E41.2's **S5** — that the absolute floor's `files changed > 0`
term is **unreachable by construction** on the `epic_qa` lane, because `bin/run-qa-agent` **refuses
to dispatch** under a mutating tool set (`:336-344`) — was escalated and **the CFO ruled it on
2026-08-20**:

| Lane | Floor |
|---|---|
| **`epic_dev`** | `tool rounds > 0` **AND** `files changed > 0` — **unchanged** |
| **`epic_qa`** | `tool rounds > 0` **AND** `claims resolve against files that exist`; **`files changed` recorded but NOT scored** |

`.ai-project/artifacts/rulings/2026-08-20__ai-project-system-hq__ruling__s5-per-lane-qualification-floor.md`

- **Apply it to both rows and reach both verdicts.** The prior *"may not apply an unresolved floor"*
  restriction is **discharged** — the bar is committed now.
- **STATE WHICH VERSION OF THE FLOOR YOU APPLIED.** A run judged against the pre-ruling floor and one
  judged against the ruled floor **are not comparable results**, and your numbers will be read beside
  E41.2's.
- **⚠ Criterion 2 is untouched.** **Reaching the floor by enabling a mutating tool on the QA lane is
  refused in advance.** A floor cleared that way is **not a cleared floor** — record that no run
  reached its floor by that route.
- **This is not a relaxation.** `files changed > 0` on `epic_qa` was **a constant false, not a strict
  bar** — zero discriminating power. Every recorded historical failure still fails: E39.3 fails
  `rounds > 0` **and** `claims resolve` independently.

---

## Prerequisite Verification (do this first, in this order)

**1. Your own worktree.** See the `P11-GH-1` block above. Branch `epic/P12-M41-E41.3` from
`milestone/M41`, in a worktree of your own. **Verify pushes at `origin`** — `git log -1 <branch>`
reads a local ref and proves nothing.

**2. E41.1 and E41.2 must both have landed.** E41.1 is the **hard gate** (resolved, reachable,
routable strings; both 27b declared in `opencode.json`). E41.2 supplies **the instrument, the
committed check list and floor, the two task definitions, and the two incumbent baselines.**
**A comparison against an unpublished baseline is not a comparison.**

**3. Artifact tracking — `git ls-files --error-unmatch`, on your branch:** this epic's spec; E41.2's
instrument, check list and task definitions; E41.1's resolution table; the M41 milestone spec.

**4. Model verification (P9-M31-E31.3 — required, this instance is `manual`).** Compare your own
harness-reported identity to `.ai-project.yml`'s **`models.epic_manual`** — **read the file, do not
trust this line**, because M41 is the milestone that changes those values. **If both are present and
disagree, STOP** and state the mismatch.

**5. Substrate, before any run:**

```
docker --version                      # absent -> STOP and escalate (unsandboxed fallback)
curl -s http://localhost:11434/api/tags   # both 27b present, with digests
curl -s http://localhost:11434/api/ps     # expect {"models":[]} — a clean start
```

**6. Prove the instrument still agrees with E41.2's baselines.** Replay at least one of E41.2's
recorded cases and confirm the figures reproduce. **If they do not, STOP and escalate — the
comparison is void before it starts.** Do not repair the instrument; it is E41.2's.

**7. Re-measure the spec's T1–T5 yourself.** **G2 is binding**, and the Milestone Chat that wrote
them is, to you, an executor. **If any measurement disagrees with the spec, the spec is wrong and you
say so.**

---

## Summary

**Run E41.2's instrument, on E41.2's tasks, against E41.2's baselines, with two candidates — and
reach two independent conclusions.**

**What is already decided and is not yours:** the instrument, the tasks, the check list, the floor,
the bar's shape, and the separate treatment of the two lanes. **What is yours:** the five delegated
design decisions in the Epic spec — decide, document, proceed.

**Holding a row is a legitimate outcome.** `epic_dev` is **discovery** — the incumbent owns this
project's only mergeable-work evidence and a candidate must **earn** the row; parity is not a reason
to move. **Delivering "no change" with evidence is delivering.**

---

## Spec References (load-one-reference-the-other, AOG §3.1.1)

**Full spec:** `docs/phases/P12__Completion_Fail_Closed_Defaults_and_the_Drivr_MVP/P12-M41-E41.3__spec__lane-candidates-against-the-baseline.md`
**Commit:** `993d906` — spec **v1.1.0** on `milestone/M41` (2026-08-20). **Read v1.1.0** — the
S5 ruling made the qualification floor **per-lane**, so the `epic_qa` verdict is no longer withheld
and you must state which version of the floor you applied

The Epic spec is the authoritative copy of the **Deliverables (D1–D4), Definition of Done, Acceptance
Criteria, Binding Constraints, Technical Constraints, Design Decisions Delegated, Non-Goals and
Notes.** **This starter does not duplicate them.**

**The five things easiest to get wrong, flagged here and specified there:**

1. **Do not import a reputation (T1).** `qwen3.6:27b`'s `PASS 4/5, zero false alarms` was earned
   **single-turn and tool-free** — `judgment.md`'s caution 4. The back-test **could not have
   observed** tool rounds, files changed or claims resolving. **The candidate arrives with zero lane
   evidence: not credit, and not a handicap either.** Say so in the record.
2. **Control residency (T2).** All three models exceed this box's 16 GB VRAM and **it cannot hold
   two.** One resident at a time; record `/api/ps` **before and after** every run. A fast run may
   simply be a warm one.
3. **The counters are floors, not scores (T3).** *More tool rounds is not better* — E33.2 Run B took
   10, wasted 3, and hit `max_iterations_exceeded` with the work done. **"Strictly better on at least
   one" attaches to the task's directional check** (E41.2 spec v1.0.2). **If no directional check
   exists, STOP AND ESCALATE — never break a tie with an impression of quality.**
4. **Never score the exit code.** The clean exit belongs to the run that did nothing.
5. **Distinguish measured equivalence from inability to compare.** Both end in *the row holds*.
   **They are different results and must not read alike** — a reader has to be able to tell which
   happened.

---

## Suite Baseline — state which repo

| Repo | Baseline | Invocation |
|---|---|---|
| `ai-project-system` | **549 passed / 0 failed** at `milestone/M41`, 2026-08-20 — **re-measure on your branch** | `PYTHONPATH=. pytest -q` — **bare `pytest` fails collection** |
| `drivr` | not touched — **do not report it as if it moved** | — |

If you add tests, name the total, the delta, and attribute the delta to named tests — **and confirm
each new test is actually collected.** A rotted guard is invisible while the total still looks green.

---

## Method Obligations

1. **`P11-GH-2`** — layer, time and scope on every verification.
2. **G2** — the reviewer re-measures. Prerequisites 6 and 7 are that obligation applied to you.
3. **G1** — remove derivation steps. Quote counts verbatim.
4. **Cite by artifact + defect, never by ordinal.** **Do not allocate a new `GH-` ID without
   escalating first.**
5. **Historical claims carry a date or commit anchor.**
6. **Every inventory is a floor.**
7. **Run it, don't read it.**
8. **Commit every artifact you author. Untracked is undelivered.**
9. **Committed transcripts and E41.2's instrument are READ-ONLY.**

---

## Deliverable Artifacts

1. Eight run records with full per-run conditions (D1)
2. Two per-row applications of the relative bar, both reached, `epic_qa` against the ruled per-lane floor with the version stated (D2)
3. The split reasoning, with E39.1's tempering (D3)
4. Escalation Notice(s), or a positive statement that none was required (D4)
5. The Structural diagram (Mermaid, in-repo, **no ComfyUI**)
6. **Epic Delivery Notice**

---

## Definition of Done

**See the Epic spec §Definition of Done — the authoritative copy.**

Before opening the PR, confirm every item, **including the four easiest to skip**: **all eight runs
reported** including any excluded ones with their reasons; **`/api/ps` recorded around each run**;
the **`epic_qa` verdict reached against the ruled floor**, with the version stated and Criterion 2
recorded intact; and **the T1 paragraph** —
that the back-test result was not used as lane evidence, and why.

---

## Completion

1. Commit all work to `epic/P12-M41-E41.3`
2. **Verify the push at `origin`**
3. Open a PR to **`milestone/M41`**
4. Produce the Epic Delivery Notice
5. ⏸️ **Stop and await authorization — do not merge**

Declare: *"Epic E41.3 execution complete. Epic Delivery Notice produced. Awaiting Milestone Chat
review and authorization."*

---

## Question Policy

- **Ask only blocking questions.** The five delegated design decisions are **yours**.
- **Escalate to the M41 Milestone Chat** (do not decide) if: **no candidate clears a row** (the row
  holds and the result goes to the CFO — **this is a correct outcome, escalate it as a result, not as
  a failure**); the instrument disagrees with E41.2's recorded baselines; a task cannot be run
  identically; **no directional check exists** so the bar cannot be applied (T3); Docker becomes
  unavailable; or no engine resolves. **S5 is ruled — do not re-open it.**
- **CLOSED — do not reopen:** the instrument, the tasks, the check list, the floor's *shape*; the
  separate treatment of the two lanes; whether a model's judgment may be load-bearing (**it may
  not** — E39.1); whether exit status counts as work (**it does not**); whether `qwen3.6:27b`'s
  back-test transfers to this lane (**it does not** — T1).
- **Do not scope in:** E41.4's back-test or its transport; E41.5's landing; M46's gate; M42's lane
  repair; the `qwen3-coder:30b` context overpack; any Drivr change; **any repair to E41.2's
  instrument.**
- **Do not ask for information already in the Epic spec.**

Escalate to the M41 Milestone Chat for any gap not covered here.
