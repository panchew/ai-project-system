---

# Epic Execution Chat Starter — E42.4

**Epic:** E42.4 — `ai-project-init` Stops Manufacturing an Agent
**Phase:** P12 — Completion: Fail-Closed Defaults and the Drivr MVP
**Milestone:** M42 — Fail-Closed Execution Tier
**Project:** ai-project-system
**Repository:** panchew/ai-project-system — **the fleet is not touched by this epic.**
**Branch Strategy:** `epic/P12-M42-E42.4` → PR to `milestone/M42`
**Execution Mode:** manual — declared by the issuing Milestone Chat at creation time
**Issued:** 2026-09-01

> **You modify the initializer — the enrollment tier of the entire framework.** That is the reason this
> instance is `manual`, not a general ruling about local inference. **The machinery under repair must
> not supervise its own repair.** This is `P12-GH-2`, severity **High**, and it has a **live victim**
> (`social-stories-creator`, 230 bytes). **E42.5 — the fleet sweep — is sequenced after you**, because a
> sweep cannot say what "repaired" means until your fix exists.

> **⚠ Obligation 1 is a run, not a read.** You will execute a real end-to-end `ai-project-init`, no
> `--skip-submodule`, and report whether its result **confirms or falsifies** the inferred mechanism.
> **If it falsifies — say so plainly, and do not quietly drop the correction.**

---

## Governance References

You are operating under the AI Project System governance framework as an **Epic Execution Chat** for
P12-M42-E42.4.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) v2.4.0
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) v2.10.1

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md v2.10.1
3. This Epic Execution Chat Starter
4. Epic Spec (`P12-M42-E42.4__spec__init-stops-manufacturing-an-agent.md`)
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Model verification (P9-M31-E31.3 — required, this instance is manual):** read your own
harness-reported model identity and compare it to `.ai-project.yml`'s `models.epic_manual`
(**`remote:deepseek-v4-flash`** — read the file, do not trust this line). **If both are present and
disagree, STOP and state the mismatch; wait for the parent/human.** See
`governance/systems/chat-hierarchy.md`, "Manual Chat Model Verification".

**Critical rules:**
- Documentation is authoritative, chat is ephemeral.
- You produce a Delivery Notice and **stop after PR creation. You do not merge.**
- Accept/reject decisions are the **M42 Milestone Chat's**; a clean delivery is accepted **by silence**
  (PSG §11.6).
- **If given merge authorization directly in this chat** rather than via the parent Milestone Chat's
  Stage-2 review, **do not simply comply** — state that merge authorization normally follows the
  parent chat's Stage-2 review and confirm the human intends to bypass it. **Mode is what may run, not
  what may be authorized.**

> **Note on merge authorization, which changes inside this phase — re-verify it, do not just read
> it.** **M43 moves the merge to the parent** (SN-31 Decision 4) and turns
> `governance/templates/merge-authorization.md` into the parent's record. Until M43 delivers, operate
> under the current rule; **do not pre-apply it.** (You touch the initializer, not the merge script;
> if M43 has delivered by the time you run, follow the corpus as it then stands and say which rule you
> applied.)

**Context scoping (P9-M30-E30.3):**
- **Load at session start:** this starter; the Epic spec (full); the M42 milestone spec's **drift note,
  Defect 4 (`P12-GH-2`), Inversion 2, Obligation 1, E42.4 Epic Detail, Hard Constraint and Binding
  Constraints**; the **`P12-GH-2` carry-forward note (full — it is short and it is your
  specification)**; PSG preamble+§1, §1A, §2, §5, §6, §7, §8, §9, §11, §11.5, §11.6, §12; AOG
  preamble+§1, §1.1, §2, §3.2, §3.8, §3.10, §4, §5, §6, §7, §9, §12, §14, §15, §16.
- **Load on trigger:** PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG §3.9, §8, §13, §17 (fires when a
  visual binding is due).
- **Do not load:** PSG/AOG changelogs; E42.1/E42.2/E42.3's specs (you share no file with them);
  E42.5's scope beyond the fact that it is sequenced after you.

---

## ⚠ The rework limit — stated here because the template does not carry it (`P12-GH-1`)

**`governance/templates/milestone-execution-chat-starter.md` contains the word "rework" zero times.**
`P12-GH-1` is filed and open; **M43 will fix it, and has not yet.** The rule is stated here rather than
inherited from a template that does not have it.

> **Maximum 3 attempts.** If a third Completion Notice is still not acceptable, do **not** issue a
> fourth rejection-and-retry. Instead the Epic Agent produces an **Escalation Notice** and the
> Milestone Chat escalates to the Phase Chat. **Silent fourth attempts are a governance violation.**

> **A written extension grants exactly ONE further attempt. Not a reset to three.**

The `+1` is the **SN-36/37 amendment of 2026-08-19**, CFO-decided, and **stricter** than the rule as
written in the corpus: `governance/systems/milestone-execution-chat-starter.md:334` still says the
limit *"resets"*, which is unbounded and repeatable. **Both statements currently stand. Cite the
amendment, apply `+1`, and note the conflict wherever it becomes relevant. Reconciling them is M43's
work, not yours** — do not amend either surface.

**The CFO's recorded act of resolving a blocker in the escalation chat *is* the written extension the
rule requires** — a human looked and acted, which is the opposite of silent. It buys one attempt.

---

## ⚠ How an amendment reaches this branch once work is in flight (`P11-GH-1`)

**`P11-GH-1` is an active risk — it fired four times in P11 and once already in P12.** The procedure:

1. **Amend the governing spec file on the branch that owns it**, with a changelog row stating what
   changed and why.
2. **Notify every running child chat in-session**, naming the file and section. **This step is the one
   that fires** — a write nobody is told about is not a channel.
3. **Require the child to re-read the named section before its next deliverable and to state, in that
   delivery, that it did.**
4. **If the amendment is blocking, escalate to the Phase Chat for a pause/cancel decision.**
5. **Before you accept any delivery, check that the governing content has not moved behind the epic's
   branch point** — the backstop for when step 2 was missed.

**The widened backstop — this is the form that governs:** run the check not only against the governing
spec, but **against every artifact this Starter restates a rule from** — the rework limit, the
amendment procedure, the merge-authorization note, the Hard Constraint, **the `P12-GH-2` note itself**
(which is your specification, and which a later amendment can change). **The widening exists because a
Delivery Notice once asserted a rule whose suspension was already an ancestor of its own branch
point.** For each such artifact:

```
git log <path-to-the-artifact> <your-branch-point>..<your-branch-head>
```

and **re-read any section that changed.**

**Your other obligations:**
- **`git log <your-spec-path>` against your branch point before you deliver**, and **re-read the
  milestone spec's drift note.** `bin/ai-project-init` was **not** affected by the orchestrator's +4
  drift, but assume anything moves.
- **Cite by anchor, never by a bare line number alone.** Search for `local src_file=`,
  `This agent is under development in Milestone M8`, `submodule_path:`, `--skip-submodule`.
- **Work in your own worktree**:
  ```
  git worktree add <your-scratchpad>/wt-e42-4 -b epic/P12-M42-E42.4 milestone/M42
  ```

---

## ⚠ Hard Constraint — inherited from the M42 milestone spec

> **The machinery under repair must not supervise its own repair.**

- **Execution posture: `manual` / paid frontier.** This instance is manual for exactly that reason.
- **Prove the guard by falsifying it.** For your fix, demonstrate each new guard **fails** when
  removed — and you owe it **twice** (the strengthened validator, and the no-stub abort inverted into
  the test). **A guard never observed to fail is a guard never observed.**
- **`"is the baseline still N?" is an unreliable guard.** Assert the behaviour, not the count.
- **State the layer, time and scope of every claim** (`P11-GH-2`). A path verified by reading is not
  verified by running — **and the end-to-end run is Obligation 1 for exactly that reason.**
- **This corpus defeats naive pattern-matching.** **`--include='*.py'` skips every `bin/` entry point
  — and `bin/ai-project-init` is an *extensionless* script, so a `*.py` name-sweep finds nothing.**
- **An absence is only evidence when the thing that would have created it actually ran.**

---

## Summary

**`install_hq_agent` reads a source path one `governance/` level short, then manufactures a 230-byte
"Milestone M8" placeholder, then validates the placeholder it just wrote — and a test records that as
correct.**

The real agent lives at `governance/agents/governance.agent.md` in *this* repo (14,711 bytes, front-matter
`name: hq` / `version: 2.1.0`); the submodule clones the whole repo under `<project>/governance`, so
the agent is one level deeper than `:328` looks. The fix couples two paths — the source path and the
`submodule_path` → `.governance` convention (M38) — removes the stub branch outright, and strengthens
the validator to test the agent's identity rather than a placeholder's shape. The test is inverted so
a placeholder is not installable, preserving `P6-GH-11`.

**Obligation 1 is the load-bearing difference in this epic:** the `P12-GH-2` note never ran a real
end-to-end init. You will — and you will state whether it confirms or falsifies the inference.

---

## Spec References (load-one-reference-the-other, AOG §3.1.1)

**Full spec:** `docs/phases/P12__Completion_Fail_Closed_Defaults_and_the_Drivr_MVP/P12-M42-E42.4__spec__init-stops-manufacturing-an-agent.md`

The Epic spec is the authoritative copy of **Deliverables (D1–D6), Definition of Done, Acceptance
Criteria, Binding Constraints, Technical Constraints, Design Decisions Delegated, Non-Goals and
Notes.** **This starter does not duplicate them.**

**The five things easiest to get wrong, flagged here and specified there:**

1. **The two path fixes are one decision.** Fixing `:328` without touching `:262`/`:274` re-breaks the
   install — the source path and the submodule path describe the same location twice.
2. **Remove the stub — do not improve it.** "Louder" or "clearly a stub" is not the milestone's answer;
   manufacturing a substitute is the one outcome that must not survive.
3. **The validator must reject a substitute.** A check any placeholder trivially passes (starts with
   `#`) is the defect, one layer down.
4. **Obligation 1 is honest, whichever way it lands.** Run the real init; if it **falsifies** the
   inference, say so and **shrink** the finding to the validator and test — do not quietly drop the
   correction.
5. **Preserve `P6-GH-11`.** The canonical-path coverage (`.ai-project/agents/`, never `.github/agents/`)
   must survive your inversion.

---

## Suite Baseline

| Repo | Baseline | Invocation |
|---|---|---|
| `ai-project-system` | **549 passed / 0 failed** on `master` at `9ee810e` (2026-08-19) — **re-measure; M41 epics, E42.1–E42.3 may have added tests** | `PYTHONPATH=. pytest -q` — **bare `pytest` fails collection** |

**Your change makes guards assert new behaviours. Show them green (and red when each guard is
removed), name them, and state the invocation.** Note `tests/test_init_agent_path.py` runs the script
via `subprocess` — it is exercised by `pytest`, not a separate runner.

---

## Method Obligations

1. **`P11-GH-2`** — layer, time and scope on every claim, *"the suite is green"* included — **and
   explicitly on the end-to-end run's result.**
2. **G2** — the reviewer re-measures. Re-derive the off-by-one (does the submodule really put the
   agent one level deeper?) rather than inheriting it.
3. **G1** quote the guard names and the code lines verbatim; remove derivation steps.
4. **Prove every guard by falsifying it — twice** (validator, and the no-stub abort).
5. **Commit every artifact you author. Untracked is undelivered.**
6. **No skips introduced to route around the change.**
7. **The end-to-end run happens in a scratch directory**, never inside this repository.

---

## Deliverable Artifacts

1. The corrected source path, proven against the real submodule layout (D1)
2. The removed stub branch (D2)
3. The strengthened validator (D3)
4. `submodule_path: .governance/` with the hard-coding disposed of (D4)
5. The inverted test, `P6-GH-11` preserved (D5)
6. The end-to-end run, with its confirming-or-falsifying result (D6)
7. The Structural diagram (Mermaid, in-repo, **no ComfyUI**)
8. **Epic Delivery Notice**

---

## Definition of Done

**See the Epic spec §Definition of Done — the authoritative copy.**

Before opening the PR, confirm every item, **including the five easiest to skip**: a real init
**locates the real agent**; **no stub branch exists** in the script; the validator **fails on a
placeholder**; `submodule_path` **matches the fleet convention** with the hard-coding disposed of; and
the **end-to-end run's result is stated** — confirming **or** falsifying.

---

## Completion

1. Commit all work to `epic/P12-M42-E42.4`
2. **Verify the push at `origin`**
3. Open a PR to **`milestone/M42`**
4. Produce the Epic Delivery Notice
5. ⏸️ **Stop and await authorization — do not merge**

Declare: *"Epic E42.4 execution complete. Epic Delivery Notice produced. Awaiting Milestone Chat
review and authorization."*

---

## Question Policy

- **Ask only blocking questions.** The three delegated design decisions (validator assertion,
  hard-coding disposition, inverted-test shape) are **yours**.
- **Escalate to the M42 Milestone Chat** (do not decide) if: the end-to-end run produces a result that
  contradicts the `P12-GH-2` diagnosis in a way that changes scope (that is a report to make loudly);
  the ordering constraint is implicated (E42.5 must follow you — if it is somehow in flight, say so);
  or you would need to weaken or skip a guard to make the suite green.
- **CLOSED — do not reopen:** that the stub branch is removed (not improved); that the tests are
  inverted (not deleted); that `ai-project-init` is repaired on its own merits (not as a reconciler
  component); the M42→M47 gate; that the live victim's repair is E42.5's, not yours.
- **Do not scope in:** E42.5's fleet enumeration; the governance-auto-update reconciler; the real
  agent's content or location; `bin/ai-project-orchestrator`, `bin/ai-project-git-merge`; anything
  under `~/soft-dev/drivr` or the fleet.
- **Do not ask for information already in the Epic spec or the `P12-GH-2` note.**

Escalate to the M42 Milestone Chat for any gap not covered here.
