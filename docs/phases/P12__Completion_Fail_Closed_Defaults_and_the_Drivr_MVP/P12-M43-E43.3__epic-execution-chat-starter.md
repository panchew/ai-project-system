---

# Epic Execution Chat Starter — E43.3

**Epic:** E43.3 — The Rework Limit: One Statement, an Itemized Set
**Phase:** P12 — Completion: Fail-Closed Defaults and the Drivr MVP
**Milestone:** M43 — The Acceptance Chain, Made Structural
**Project:** ai-project-system
**Repository:** panchew/ai-project-system — **Drivr is not touched by this epic.**
**Branch Strategy:** `epic/P12-M43-E43.3` → PR to `milestone/M43`
**Execution Mode:** manual — declared by the issuing Milestone Chat at creation time
**Issued:** 2026-09-02

> **You consolidate the rule that bounds rework — including the surface that tells a Milestone Chat
> how to behave in a rework loop.** That is the reason this instance is `manual`. **You are first of
> your pair;** E43.4 fires on *exhausted rework* and needs the one definition you produce first.

---

## Governance References

You are operating under the AI Project System governance framework as an **Epic Execution Chat** for
P12-M43-E43.3.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) v2.4.0
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) v2.10.1

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md v2.10.1
3. This Epic Execution Chat Starter
4. Epic Spec (`P12-M43-E43.3__spec__rework-limit-one-statement-itemized-set.md`)
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Model verification (P9-M31-E31.3 — required, this instance is manual):** read your own
harness-reported model identity and compare it to `.ai-project.yml`'s `models.epic_manual`
(**read the file — do not trust this line**). **If both are present and disagree, STOP and state the
mismatch; wait for the parent/human.** See `governance/systems/chat-hierarchy.md`, "Manual Chat Model
Verification".

**Critical rules:**
- Documentation is authoritative, chat is ephemeral.
- You produce a Delivery Notice and **stop after PR creation. You do not merge.**
- Accept/reject decisions are the **M43 Milestone Chat's**; a clean delivery is accepted **by silence**
  (PSG §11.6).
- **If given merge authorization directly in this chat** rather than via the parent Milestone Chat's
  Stage-2 review, **do not simply comply** — state that merge authorization normally follows the
  parent chat's Stage-2 review and confirm the human intends to bypass it. **Mode is what may run, not
  what may be authorized.**

**Context scoping (P9-M30-E30.3):**
- **Load at session start:** this starter; the Epic spec (full); the M43 milestone spec's **E43.3 Epic
  Detail, W2, Hard Constraint and Binding Constraints**; the **`P12-GH-1` carry-forward note (full)**;
  PSG preamble+§1, §1A, §2, §5, §6, §7, §8, §9, §11, §11.5, §11.6, §12; AOG preamble+§1, §1A, §2,
  §3.2, §3.8, §3.10, §4, §5, §6, §7, §9, §10, §12, §13, §14.
- **Load on trigger:** PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG §3.9, §8, §11, §16.
- **Do not load:** PSG/AOG changelogs; other epics' specs beyond the ordering.

---

## ⚠ The rework limit — YOUR SUBJECT. Stated here because the template does not carry it (`P12-GH-1`)

**You are the epic that fixes `P12-GH-1`. Until you deliver, the defect you are fixing still binds
you.** `governance/templates/epic-execution-chat-starter.md` contains the word "rework" zero times;
the rule lives in one surface and nowhere normative. Operate under it as it stands now:

> **Maximum 3 attempts.** If a third Completion Notice is still not acceptable, do **not** issue a
> fourth rejection-and-retry. Instead you produce an **Escalation Notice** and the Milestone Chat
> escalates to the Phase Chat. **Silent fourth attempts are a governance violation.**

> **A written extension grants exactly ONE further attempt. Not a reset to three.**

The `+1` is the **SN-36/37 amendment of 2026-08-19**, CFO-decided, and **stricter** than the corpus:
`governance/systems/milestone-execution-chat-starter.md:334` still says *"resets."* **You reconcile
them into one statement** (your D2). Until you deliver, apply `+1`, cite the amendment, note the
conflict.

---

## ⚠ How an amendment reaches this branch once work is in flight (`P11-GH-1`)

**`P11-GH-1` is an active risk, and its backstop was falsified once in M43's own window** (milestone
spec v1.1.3): an amendment arrived on the branch and was still not applied. The procedure, and its
widened backstop:

1. **Amend the governing spec file on the branch that owns it**, with a changelog row.
2. **Notify every running child chat in-session**, naming the file and section.
3. **Require the child to re-read the named section before its next deliverable and to state, in that
   delivery, that it did.**
4. **If the amendment is blocking, escalate to the Phase Chat.**
5. **Before you accept any delivery, `git log` this spec against the branch point — AND against every
   artifact this Starter restates a rule from** (the rework rule is exactly such a restated rule).

**Your obligations, specifically:**
- **`git log <your-spec-path>` and the `P12-GH-1` note against your branch point before you deliver.**
- **Pin the ref in every measurement.** Put the ref **before** the `--`, name `origin/<branch>`.
  **A number without a ref is not a measurement.**
- **Work in your own worktree:**
  ```
  git worktree add <your-scratchpad>/wt-e43-3 -b epic/P12-M43-E43.3 milestone/M43
  ```

---

## ⚠ Hard Constraint — inherited from the M43 milestone spec

> **This milestone changes who holds authority. Every change must be provable from the artifacts, not
> from intent.**

- **Itemize, never count.** Your surface set is a **list** with per-surface state. A coverage count is
  a defect (W2).
- **Prove the guard by falsifying it.** Remove a surface from coverage, watch the check fail, restore
  it.
- **Do not weaken a guard to accommodate a change.** The `+1` is strictly applied, never relaxed.
- **State the layer, time and scope of every claim** (`P11-GH-2`) **and the REF.**
- **State the repository.** This epic lands entirely in `ai-project-system`; nothing crosses to Drivr.
- **A rule you are rewriting still binds you.** Until E43.3's own change lands, the current split rule
  governs this milestone's epics, including yours.

---

## Summary

**Consolidate the rework limit into one normative statement, reachable from an itemized set, and make
its reach testable.**

`P12-GH-1`: the 3-attempt rule lives in one of nine starter-shaped surfaces, in zero templates and
zero normative documents, and carries two contradictory extension semantics — `:334` says *"resets"*,
SN-36/37 grants **`+1`**. E43.3 writes the one statement (limit **and** extension) in the normative
tier (its decision), reconciles the two extensions into one, lists the surface set itemized (never a
count — W2), routes every surface to the statement by carry or cite (the three templates included),
and adds a check that fails if a surface falls out of coverage. **It also updates the `P12-GH-1`
carry-forward note with the outcome.**

---

## Spec References (load-one-reference-the-other, AOG §3.1.1)

**Full spec:** `docs/phases/P12__Completion_Fail_Closed_Defaults_and_the_Drivr_MVP/P12-M43-E43.3__spec__rework-limit-one-statement-itemized-set.md`

The Epic spec is the authoritative copy of **Deliverables (D1–D6), Definition of Done, Acceptance
Criteria, Binding Constraints, Technical Constraints, Design Decisions Delegated, Non-Goals and
Notes.** **This starter does not duplicate them.** Cite the spec by path and branch — **not** by a
version number or sha.

**The five things easiest to get wrong, flagged here and specified there:**

1. **Do not emit a coverage count.** The list is the deliverable; a number is a defect (W2).
2. **Do not leave both extension statements standing** with a citation preferring the newer — that is
   the drift condition, not a resolution (Binding Constraint 5).
3. **Do not skip the templates.** The template is what a chat is instantiated from, and `P12-GH-1`
   bites exactly there.
4. **The rule is in no normative document — that argues for the normative tier**, not for leaving it in
   a starter. Place it authoritatively.
5. **Update the `P12-GH-1` carry-forward note** with the outcome — the defect's record is closed, not
   silently retired.

---

## Suite Baseline

| Repo | Baseline | Invocation |
|---|---|---|
| `ai-project-system` | **549 passed / 0 failed** measured on `phase/P12` at `d98f95d` — **re-measure on this branch; M42 epics may have added tests** | `PYTHONPATH=. pytest -q` — **bare `pytest` fails collection** |

**Your change adds a check asserting a new behaviour. Show it green (and red when a surface is removed
from coverage), name it, and state the invocation and the ref.**

---

## Method Obligations

1. **`P11-GH-2`** — layer, time and scope on every claim, and the **ref** it was measured against.
2. **G2** — the reviewer re-measures. Re-derive W2's surface set against your branch; do not inherit
   the seven/eight/nine figures.
3. **G1** — quote `systems/milestone-execution-chat-starter.md:330/334` verbatim; remove derivation.
4. **Prove every guard by falsifying it** — remove a surface, watch the check fail, restore it.
5. **Commit every artifact you author. Untracked is undelivered.**
6. **No skips introduced to route around the change.**

---

## Deliverable Artifacts

1. The one normative statement, with the placement decision recorded (D1)
2. The two extension statements reconciled to one (`+1`, not "resets") (D2)
3. The itemized surface set, each surface's state stated (D3)
4. Every listed surface reaching the statement, templates included, mechanism stated (D4)
5. The check and its falsification demonstration (D5)
6. The `P12-GH-1` carry-forward note updated (D6)
7. The Structural diagram (Mermaid, in-repo, **no ComfyUI**)
8. **Epic Delivery Notice**

---

## Definition of Done

**See the Epic spec §Definition of Done — the authoritative copy.**

Before opening the PR, confirm every item, **including the three easiest to skip**: no surface
contradicts the one statement; the set is a **list** not a count; and the check was **observed to fail
when a surface was removed**.

---

## Completion

1. Commit all work to `epic/P12-M43-E43.3`
2. **Verify the push at `origin`**
3. Open a PR to **`milestone/M43`**
4. Produce the Epic Delivery Notice
5. ⏸️ **Stop and await authorization — do not merge**

Declare: *"Epic E43.3 execution complete. Epic Delivery Notice produced. Awaiting Milestone Chat
review and authorization."*

---

## Question Policy

- **Ask only blocking questions.** The two delegated design decisions (normative home, carry-vs-cite)
  are **yours**.
- **Escalate to the M43 Milestone Chat** (do not decide) if: the ordering constraint is implicated
  (E43.4 must land first — it must not; E43.3 is first of its pair); a finding suggests the rule is
  **not** in fact absent from the normative tier; or a surface cannot reach the statement without
  weakening a guard.
- **CLOSED — do not reopen:** the limit is 3 attempts; an extension grants `+1` not a reset; the set
  is itemized never counted; the templates are in scope.
- **Do not scope in:** the flip and resume (E43.4, sequenced after you); who merges (E43.1);
  accept-by-silence (E43.2); `.ai-project.yml`; `bin/`; `~/soft-dev/drivr`.
- **Do not ask for information already in the Epic spec.**

Escalate to the M43 Milestone Chat for any gap not covered here.
