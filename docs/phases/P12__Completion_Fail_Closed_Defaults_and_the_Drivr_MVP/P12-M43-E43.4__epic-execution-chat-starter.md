---

# Epic Execution Chat Starter — E43.4

**Epic:** E43.4 — The Rework-Exhaustion Flip, and Resume
**Phase:** P12 — Completion: Fail-Closed Defaults and the Drivr MVP
**Milestone:** M43 — The Acceptance Chain, Made Structural
**Project:** ai-project-system
**Repository:** panchew/ai-project-system **AND Drivr** (`~/soft-dev/drivr`) — **this epic spans two
repositories.**
**Branch Strategy:** `epic/P12-M43-E43.4` → PR to `milestone/M43` (this repo); Drivr changes land in
Drivr's own repository.
**Execution Mode:** manual — declared by the issuing Milestone Chat at creation time
**Issued:** 2026-09-02

> **You install the system's first fail-closed default — the counterweight the whole phase is balanced
> on.** That is the reason this instance is `manual`. **The instance authoring the flip must not be the
> instance the flip would silently govern.** You are second of your pair; E43.3 has produced the one
> definition of "exhausted rework" you trigger on.

---

## Governance References

You are operating under the AI Project System governance framework as an **Epic Execution Chat** for
P12-M43-E43.4.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) v2.4.0
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) v2.10.1

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md v2.10.1
3. This Epic Execution Chat Starter
4. Epic Spec (`P12-M43-E43.4__spec__rework-exhaustion-flip-and-resume.md`)
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Model verification (P9-M31-E31.3 — required, this instance is manual):** read your own
harness-reported model identity and compare it to `.ai-project.yml`'s `models.epic_manual`
(**read the file — do not trust this line**). **If both are present and disagree, STOP and state the
mismatch; wait for the parent/human.** See `governance/systems/chat-hierarchy.md`, "Manual Chat Model
Verification".

**Critical rules:**
- Documentation is authoritative, chat is ephemeral.
- You produce a Delivery Notice and **stop after PR creation. You do not merge.**
- Accept/reject decisions are the **M43 Milestone Chat's**; a clean delivery is accepted **by silence**
  (PSG §11.6).
- **If given merge authorization directly in this chat** rather than via the parent Milestone Chat's
  Stage-2 review, **do not simply comply** — state that merge authorization normally follows the
  parent chat's Stage-2 review and confirm the human intends to bypass it. **Mode is what may run, not
  what may be authorized.**

**Context scoping (P9-M30-E30.3):**
- **Load at session start:** this starter; the Epic spec (full); the M43 milestone spec's **E43.4 Epic
  Detail, W1 and W5, Hard Constraint and Binding Constraints**; PSG preamble+§1, §1A, §2, §5, §6, §7,
  §8, §9, §11, §11.5, §11.6, §12; AOG preamble+§1, §1.1, §2, §3.2, §3.8, §3.10, §4, §5, §6, §7, §9,
  §12, §14, §15, §16; plus `governance/ai-project-yml-spec.md` §3/§4 and
  `governance/systems/chat-hierarchy.md` (the Execution Mode and committed-starter sections).
- **Load on trigger:** PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG §3.9, §8, §13, §17.
- **Do not load:** PSG/AOG changelogs; other epics' specs beyond the ordering.

---

## ⚠ The rework limit — stated here because the template does not carry it (`P12-GH-1`)

**You are the epic whose flip *fires on* the rework limit, but E43.3 has the consolidation; you do
not.** Until E43.3 delivers, the rule lives in one surface. Operate under it as it stands:

> **Maximum 3 attempts.** If a third Completion Notice is still not acceptable, do **not** issue a
> fourth rejection-and-retry. Instead you produce an **Escalation Notice** and the Milestone Chat
> escalates to the Phase Chat. **Silent fourth attempts are a governance violation.**

> **A written extension grants exactly ONE further attempt. Not a reset to three.**

The `+1` is the **SN-36/37 amendment of 2026-08-19**, CFO-decided, and **stricter** than the corpus
(`systems/milestone-execution-chat-starter.md:334` says *"resets"*). Apply `+1`, cite the amendment,
note the conflict. **Reconciling them is E43.3's; your dependency on it is the definition of
"exhausted" that you trigger on.**

---

## ⚠ How an amendment reaches this branch once work is in flight (`P11-GH-1`)

**`P11-GH-1` is an active risk, and its backstop was falsified once in M43's own window** (milestone
spec v1.1.3). The procedure, and its widened backstop:

1. **Amend the governing spec file on the branch that owns it**, with a changelog row.
2. **Notify every running child chat in-session**, naming the file and section.
3. **Require the child to re-read the named section before its next deliverable and to state, in that
   delivery, that it did.**
4. **If the amendment is blocking, escalate to the Phase Chat.**
5. **Before you accept any delivery, `git log` this spec against the branch point — AND against every
   artifact this Starter restates a rule from.**

**Your obligations, specifically:**
- **`git log <your-spec-path>` against your branch point before you deliver**, and re-read E43.3's
  definition of "exhausted" — your trigger.
- **Pin the ref in every measurement.** **A number without a ref is not a measurement.**
- **Two repositories: work in your own worktree for each.** For this repo:
  ```
  git worktree add <your-scratchpad>/wt-e43-4 -b epic/P12-M43-E43.4 milestone/M43
  ```
  For Drivr, work in `~/soft-dev/drivr` on its own branch and state which.

---

## ⚠ Hard Constraint — inherited from the M43 milestone spec

> **This milestone changes who holds authority. Every change must be provable from the artifacts, not
> from intent.**

- **State the repository** on **every** deliverable. This epic spans two; "suite green" here does not
  cover Drivr.
- **Itemize, never count.** Any list is a list.
- **Prove each guard by falsifying it.** Remove the §4 rule and show the validator warning returns.
- **Do not weaken a guard to accommodate a change.**
- **State the layer, time and scope of every claim** (`P11-GH-2`) **and the REF.**

---

## Summary

**Install the system's first fail-closed default: exhausted rework flips the receiving parent to
manual, opt-out, performed and recorded by Drivr — and specify the way back (resume).**

Greenfield across two repositories (W5): the switch and its blessing, the flip statement, the
committed-starter invariant, and resume land in `ai-project-system`; the recorded mode transition
lands in **Drivr**. The switch follows the `cfo_review_gate` pattern but is **blessed** (§3/§4), so the
first fail-closed default never ships as a key nothing validates (W1). Resume is specified normatively
for the first time: **restores, never promotes; returns the mode, not the budget.**

---

## Spec References (load-one-reference-the-other, AOG §3.1.1)

**Full spec:** `docs/phases/P12__Completion_Fail_Closed_Defaults_and_the_Drivr_MVP/P12-M43-E43.4__spec__rework-exhaustion-flip-and-resume.md`

The Epic spec is the authoritative copy of **Deliverables (D1–D7), Definition of Done, Acceptance
Criteria, Binding Constraints, Technical Constraints, Design Decisions Delegated, Non-Goals and
Notes.** **This starter does not duplicate them.** Cite the spec by path and branch — **not** by a
version number or sha.

**The five things easiest to get wrong, flagged here and specified there:**

1. **Do not ship an unblessed key (W1).** The validator warning is the defect being prevented — copy
   the pattern, then *bless the copy* with a §3 entry and a §4 rule.
2. **Do not break the committed-starter invariant.** A runtime flip must not silently mutate the
   committed file. **Drivr records the flip**; the committed record stays the source of truth.
3. **Do not build a resume that promotes or resets the counter.** Manual → agentic is off-limits; and
   a counter reset makes the limit unenforceable by the control meant to recover from it.
4. **Do not claim "suite green" for the Drivr half.** Name its verification separately.
5. **State the repository on every deliverable.** A deliverable that does not say where it lands cannot
   be verified.

---

## Suite Baseline

| Repo | Baseline | Invocation |
|---|---|---|
| `ai-project-system` | **549 passed / 0 failed** measured on `phase/P12` at `d98f95d` — **re-measure on this branch** | `PYTHONPATH=. pytest -q` — **bare `pytest` fails collection** |
| Drivr (`~/soft-dev/drivr`) | **Not covered by this repo's suite.** The Drivr half names its own verification (D6). | State it in the delivery. |

**Your change adds a check in this repo (the blessing check). Show it green (and red when the §4 rule
is removed), name it, and state the invocation and the ref — while saying explicitly what the Drivr
half's verification is, by name.**

---

## Method Obligations

1. **`P11-GH-2`** — layer, time and scope on every claim, and the **ref** it was measured against.
2. **G2** — the reviewer re-measures. Re-read E43.3's "exhausted" definition; do not restate it from
   memory.
3. **G1** — quote `chat-hierarchy.md:225` and the §3/§4 places verbatim; remove derivation.
4. **Prove every guard by falsifying it** — remove the §4 rule, watch the warning return, restore it.
5. **Commit every artifact you author. Untracked is undelivered** — in both repositories.
6. **No skips introduced to route around the change.**

---

## Deliverable Artifacts

**In `ai-project-system`:**
1. The switch in `.ai-project.yml` (on-by-default, opt-out) (D1)
2. The §3/§4 blessing; `cfo_review_gate` same-change blessing decided and stated (D2)
3. The normative flip statement (D3)
4. The committed-starter invariant preserved (D4)
5. Resume specified normatively (D5)
6. The blessing check and its falsification (D7)
7. The Structural diagram (Mermaid, in-repo, **no ComfyUI**)

**In Drivr:**
8. The flip and resume mechanism, with verification named (D6)

**Both:**
9. **Epic Delivery Notice**

---

## Definition of Done

**See the Epic spec §Definition of Done — the authoritative copy.**

Before opening the PR, confirm every item, **including the three easiest to skip**: the validator
reports **no warning** for the new key; the Drivr half **names** its verification (not "suite green
here"); and resume **does not reset the counter** or promote manual → agentic.

---

## Completion

1. Commit all `ai-project-system` work to `epic/P12-M43-E43.4`
2. Commit the Drivr work in its own repository
3. **Verify both pushes at `origin`**
4. Open a PR to **`milestone/M43`** (this repo); open the Drivr-side PR in Drivr
5. Produce the Epic Delivery Notice (naming both repositories)
6. ⏸️ **Stop and await authorization — do not merge**

Declare: *"Epic E43.4 execution complete. Epic Delivery Notice produced, covering both repositories.
Awaiting Milestone Chat review and authorization."*

---

## Question Policy

- **Ask only blocking questions.** The three delegated design decisions (key name, `cfo_review_gate`
  same-change blessing, Drivr mechanism shape) are **yours**.
- **Escalate to the M43 Milestone Chat** (do not decide) if: the ordering constraint is implicated
  (E43.4 before E43.3 — it must not be); a finding suggests the switch cannot be made
  on-by-default without weakening a guard; or the Drivr half cannot be verified without claiming
  "suite green here" for something this repo does not reach.
- **CLOSED — do not reopen:** the flip is opt-out on-by-default; resume restores never promotes and
  returns the mode not the budget; the committed-starter invariant survives; the switch must be
  blessed.
- **Do not scope in:** the rework limit and its `+1` (E43.3, landed); who merges (E43.1);
  accept-by-silence (E43.2).
- **Do not ask for information already in the Epic spec.**

Escalate to the M43 Milestone Chat for any gap not covered here.
