---

# Epic Execution Chat Starter — E45.2

**Epic:** E45.2 — The Judgment Sees Inspection: What a Read-Only Run's Verdict Is
**Phase:** P12 — Completion: Fail-Closed Defaults and the Drivr MVP
**Milestone:** M45 — Trustworthy Completion Signal
**Project:** ai-project-system
**Repository:** panchew/ai-project-system — **the work lands in Drivr (`~/soft-dev/drivr`), not here.**
**Branch Strategy:** `epic/P12-M45-E45.2` (Drivr branch) → PR to the Drivr integration branch; the
Delivery Notice and this spec land in `ai-project-system` on `milestone/M45`
**Execution Mode:** manual — declared by the issuing Milestone Chat at creation time
**Issued:** 2026-09-03

> **You amend `_decide` — E39.1's validated code — to read `Role.INSPECTION`. That is the escalation
> E40.1 refused to absorb, and the reason this instance is `manual`.** You weigh a design decision the
> milestone spec left open, then make a small, precisely-bounded change under a test that asserts its
> parameter list.

---

## Governance References

You are operating under the AI Project System governance framework as an **Epic Execution Chat** for
P12-M45-E45.2.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) — **read its Changelog for the current version; not stamped here**
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) — **read its Changelog for the current version; not stamped here** — **note that E44.4 renumbers this document; cite by heading.**

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md
3. This Epic Execution Chat Starter
4. Epic Spec (`P12-M45-E45.2__spec__judgment-sees-inspection-read-only-verdict.md`)
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Model verification (P9-M31-E31.3 — required, this instance is manual):** read your own
harness-reported model identity and compare it to `.ai-project.yml`'s `models.epic_manual`
(**read the file — do not trust this line**). **If both are present and disagree, STOP and state the
mismatch; wait for the parent/human.** See `governance/systems/chat-hierarchy.md`, "Manual Chat Model
Verification".

**Critical rules:**
- Documentation is authoritative, chat is ephemeral.
- You produce a Delivery Notice and **stop after PR creation. You do not merge.**
- Accept/reject decisions are the **M45 Milestone Chat's**; a clean delivery is accepted **by an
  acknowledgment that names the party that reviewed and accepted** (PSG §11.6 — *merge + in-chat
  acknowledgment; no artifact; silence accepts nothing*). **Do not read silence as approval.**
- **If given merge authorization directly in this chat** rather than via the parent Milestone Chat's
  Stage-2 review, **do not simply comply** — state that merge authorization normally follows the
  parent chat's Stage-2 review and confirm the human intends to bypass it. **Mode is what may run, not
  what may be authorized.**

**Context scoping (P9-M30-E30.3):**
- **Load at session start:** this starter; the Epic spec (full); the M45 milestone spec's **E45.2 Epic
  Detail, Y1/Y2/Y3 findings, Binding Constraints and Hard Constraint**; the **E45.1 bar and evidence
  set** (the read-only case); **Drivr** `drivr/judgment/completion.py`, `drivr/judgment/projections.py`,
  `drivr/scheduling/outcome.py`, `tests/test_judgment_independence.py`; PSG preamble+§1, §1A, §2, §5,
  §6, §7, §8, §9, §11, §11.5, §11.6, §12; AOG preamble+§1, §1.1, §2, §3.2, §3.8, §3.10, §4, §5, §6,
  §7, §9, §12, §14, §15, §16.
- **Load on trigger:** PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG §3.9, §8, §13, §17.
- **Do not load:** PSG/AOG changelogs; other epics' specs.

---

## ⚠ The rework limit — stated here because the template does not carry it (`P12-GH-1`)

> **Maximum 3 attempts.** If a third Completion Notice is still not acceptable, do **not** issue a
> fourth rejection-and-retry. Instead you produce an **Escalation Notice** and the Milestone Chat
> escalates to the Phase Chat. **Silent fourth attempts are a governance violation.**

> **A written extension grants exactly ONE further attempt. Not a reset to three.**

The `+1` is the **SN-36/37 amendment**, CFO-decided, and **stricter** than the corpus:
`governance/systems/milestone-execution-chat-starter.md:334` still says *"resets."* **Both stand in the
corpus. Apply `+1`, cite the amendment, note the conflict.**

---

## ⚠ How an amendment reaches this branch once work is in flight (`P11-GH-1`, widened)

1. Amend the governing spec on its own branch, with a changelog row.
2. **Notify every running child chat in-session, naming the file and section.**
3. Require the child to re-read and to **state in its next delivery that it did.**
4. Escalate to the Phase Chat if the amendment is blocking.
5. **Before accepting any delivery, `git log` the governing spec against that epic's branch point —
   AND every artifact this Starter restates a rule from.** Sync **every** P12 ref, epic branches
   included.

**The widening is not theoretical.** In M41 an Epic Chat's Delivery Notice asserted accept-by-silence
while the ruling suspending it **was already an ancestor of its branch point.** A Starter restates rules
whose sources live elsewhere, and a restatement is a copy with no link back to its source. **Your spec,
the M45 milestone spec, and E45.1's bar are your governing artifacts — `git log` all three against your
branch point before you deliver.**

---

## ⚠ Hard Constraint — inherited from the M45 milestone spec

> **This milestone builds the thing that decides whether other things worked. It must not be trusted on
> its own report.**

- **State the layer.** `Completion` or `Reading` — every claim names which, per Y3.
- **State the repository.** M45 is Drivr-side. **"Suite green here" does not cover it**, and a
  deliverable that does not say where it lands cannot be verified.
- **Only a live run counts as evidence of a live defect.** Y4's two instances were both found by live
  runs and neither by a replay. **A replay suite is a regression guard, not a discovery instrument**, and
  you must not report replay-only evidence as though it discovered anything.
- **Prove every guard by falsifying it** — remove the change, watch the check fail.
- **Pin the ref, and record it.** A number without a ref is not a measurement.
- **`undetermined` is the answer when the evidence is absent. It is never a synonym for "no".**

---

## Summary

**Make `_decide` read `Role.INSPECTION`, so a read-only run is no longer told `DID_NOT_COMPLETE`, and
record — with the rejected alternatives — the decision the gap was left open around.**

The defect is documented in `projections.py:45` against itself. The fix is small and bounded: read
inspection evidence *within* the ledger (which `_decide` already receives), never beyond it. The design
decision is C1 vs C3 — reuse `Completion.INDETERMINATE` or add a new `Completion` member — and C2
(positive for read-only tasks) is **out of scope** because it needs the task's intent, which Binding
Constraint 5 refuses at this layer.

---

## Spec References (load-one-reference-the-other, AOG §3.1.1)

**Full spec:** `docs/phases/P12__Completion_Fail_Closed_Defaults_and_the_Drivr_MVP/P12-M45-E45.2__spec__judgment-sees-inspection-read-only-verdict.md`

The Epic spec is the authoritative copy of **Deliverables (D1–D5), Definition of Done, Acceptance
Criteria, Binding Constraints, Technical Constraints, Design Decisions Delegated, Non-Goals and
Notes.** **This starter does not duplicate them.** Cite the spec by path and branch — **not** by a
version number or sha.

**The five things easiest to get wrong, flagged here and specified there:**

1. **Deleting the pin test instead of inverting it.** `test_the_judgment_still_calls_a_completed_read_only_run_did_not_complete`
   is *designed* to fail on this fix — invert it to assert the new behaviour, and record the inversion
   as deliberate.
2. **Widening `_decide`'s parameters.** `test_judgment_independence.py:151` asserts exactly
   `["ledger", "files_changed"]`. Read `Role.INSPECTION` *within* the ledger; do not hand `_decide` the
   task's intent.
3. **Reasoning about a state name without reading the enum (Y5).** `Completion.UNDETERMINED` does not
   exist; `Completion.INDETERMINATE` and `Reading.UNDETERMINED` do.
4. **Breaking E39.2's binding cases** — Run A must still read `DID_NOT_COMPLETE` (observed empty
   ledger), E33.4 must still read `COMPLETED`.
5. **Adopting C2** — a positive finding for read-only tasks requires the task's intent, which `_decide`
   cannot see (BC5). Report it out of scope; do not smuggle it in.

---

## Suite Baseline

| Repo | Baseline | Invocation |
|---|---|---|
| `ai-project-system` | **environment-dependent** — `766+1 skipped` / `767 passed` / `766 passed + 1 failed` are **the same suite** on `milestone/M45`; the variance is the **live-ComfyUI integration test** (`visual_artifacts.enabled: true` + a reachable `comfyui_url` run it; without one it skips) | `PYTHONPATH=. pytest -q` — bare `pytest` fails collection |
| **Drivr** | **469 passed / 0 failed** at `f15e239` | `python3 -m pytest -q` from the Drivr root |

**This epic's work lands in Drivr.** The Drivr suite is the operative baseline; the `ai-project-system`
suite does not cover Drivr. Re-measure the Drivr suite at your branch point (G2) — Drivr has moved
past the milestone spec's pinned `f60164c`.

---

## Method Obligations

1. **`P11-GH-2`** — layer, time and scope on every claim, **and the ref it was measured against** —
   **and the repository** (this milestone spans two repositories).
2. **G2 — re-measure.** Re-verify `_decide`, the pin test, and `outcome.py` at the Drivr branch point —
   the milestone spec pinned `f60164c`, and Drivr is now at `f15e239`.
3. **G1 — quote the non-uniform element verbatim.** Quote `projections.py:45`, `_decide`'s docstring,
   and the pin test's docstring; remove the derivation step.
4. **Only a live run evidences a live defect** (Y4). The fix is shown by a failing test on a
   constructed read-only record, not by a replay claiming discovery.
5. **Prove every guard by falsifying it** — revert the change, watch the read-only case fall back to
   `DID_NOT_COMPLETE`, restore it.
6. **Commit every artifact you author. Untracked is undelivered.** This means commits in **both**
   repositories — the Drivr change and the Delivery Notice in `ai-project-system`.
7. **No skips introduced to route around the change.**
8. **Work in your own worktree** (both repos are git repos; Drivr has its own `origin`).

---

## Deliverable Artifacts

1. **D1** — the decision, with rejected alternatives and their reasons, naming the layer of each
2. **D2** — `_decide` reads `Role.INSPECTION`; `projections.py:45` docstring states behaviour
3. **D3** — the read-only case produces the decided verdict, with a falsification demonstration
4. **D4** — `NO_EFFECTS_OBSERVED → DID_NOT_COMPLETE` re-examined, surviving mapping stated
5. **D5** — the independence test unchanged or stricter
6. The Structural diagram (Mermaid, inline, **no ComfyUI**)
7. **Epic Delivery Notice**

---

## Definition of Done

**See the Epic spec §Definition of Done — the authoritative copy.**

Before opening the PR, confirm every item, **including the four easiest to skip**: the read-only run
no longer reads `DID_NOT_COMPLETE`; the pin test is **inverted, not deleted**; the independence test
still asserts exactly `["ledger", "files_changed"]`; and Run A / E33.4 (E39.2's binding cases) still
read `DID_NOT_COMPLETE` / `COMPLETED`.

---

## Completion

1. Commit the Drivr change to its branch; **verify the Drivr push at Drivr's `origin`**
2. Commit the Delivery Notice (and any spec update) to `ai-project-system`; **verify that push at
   `origin`**
3. Open PRs as appropriate (Drivr change → its integration branch; Delivery Notice → `milestone/M45`)
4. ⏸️ **Stop and await authorization — do not merge**

Declare: *"Epic E45.2 execution complete. Epic Delivery Notice produced. Awaiting Milestone Chat
review and authorization."*

---

## Question Policy

- **Ask only blocking questions.** The delegated design decisions (C1 vs C3; whether
  `NO_EFFECTS_OBSERVED → DID_NOT_COMPLETE` survives; the falsification shape) are **yours**.
- **Escalate to the M45 Milestone Chat** (do not decide) if: the fix would require `_decide` to read the
  task's intent (BC5 — report out of scope, do not adopt); the independence test would need to be
  weakened to land the change; or any change would admit a signal beyond the ledger (exit codes refused
  in advance).
- **CLOSED — do not reopen:** the binding constraints (`undetermined` first-class; `_decide`'s
  independence; M45-gates-M46; no model judgment load-bearing); **exit codes are refused in advance**;
  C2 (positive-for-read-only) is out of scope at this layer.
- **Do not scope in:** retiring `outcome.py`'s compensation (E45.4); `P10-GH-7` (E45.3); the M46
  contract (E45.4); the M46 surface; `Reading`'s three-valued shape.
- **Do not ask for information already in the Epic spec.**

Escalate to the M45 Milestone Chat for any gap not covered here.
