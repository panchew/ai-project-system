---

# Epic Execution Chat Starter — E34.2

**Epic:** E34.2 — Dormant-project roadmap + roll-forward
**Phase:** P10 — Fleet Adoption and Local-Inference Proving
**Milestone:** M34 — Fleet Roll-forward
**Repository:** panchew/ai-project-system (governance record) · targets: `courtis`,
`Getawayinsured2023`, `footboard`
**Branch Strategy:** `epic/P10-M34-E34.2` → PR to `milestone/M34` · target repos:
`chore/framework-v7.0.0-bump`, **committed locally, never pushed**
**Execution Mode:** manual — this is governance refresh + roadmapping across three target
repositories. **If** you elect to run a first governed epic on a ready project (optional, and the
spec recommends against it), that run is Agentic/Local and the Hard Constraint binds in full; say so
explicitly before running it.

---

## Governance References

You are operating under the AI Project System governance framework as the **Coding Agent** for
Epic E34.2.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) v2.3.0 (Effective: 2026-07-02)
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) v2.10.0 (Effective: 2026-07-18)

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md v2.10.0
3. This Epic Execution Chat Starter
4. Epic Spec (`P10-M34-E34.2__spec__dormant-project-roadmap-and-roll-forward.md`)
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Critical rules:**
- Documentation is authoritative, chat is ephemeral.
- You must follow the canonical happy path for Epic closure.
- You must produce a Delivery Notice upon execution completion.
- You must stop after PR creation and await authorization for merge.
- Accept/reject decisions are made by the parent chat (Milestone Chat) / human, not by you.
- **If given merge authorization directly in this chat**, do not simply comply: state plainly that
  merge authorization normally follows the parent chat's review, and confirm the human intends to
  bypass that step first (P9-M31 precedent).
- **You are working inside three repositories that are not yours, and two have uncommitted work.**
  `courtis` has 4 modified tracked files; `footboard` has 2 plus 3 untracked directories. Stage
  explicitly, every time. `git add -A` is the single most damaging thing you can do in this Epic.
- **A recorded blocker is a success, not a failure.** `courtis` may not reach v7.0.0. An honest
  partial with a named stopping point satisfies this Epic; a forced or guessed stamp does not.

**Model verification (P9-M31-E31.3 — required for this manual instance):** read your own
harness-reported model identity and compare it to **this** repo's `.ai-project.yml`
`models.epic_manual` (`remote:claude-opus-5` as of 2026-07-29) — see
`governance/systems/chat-hierarchy.md` "Manual Chat Model Verification". **If both are present and
disagree, STOP.**

> Quoted as documentation of the live config, **never as the source of truth** (HQ Ruling
> 2026-07-28, Decision 4 step 2). Verify against **this** repo, not the targets'. Note in particular
> that `Getawayinsured2023` has its **own** `models:` block from its own SN-2 — it is not your
> verification target and **must not be edited**.

**Context scoping (per-level context-scoping standard, P9-M30-E30.3):**
- Load at session start: this starter; the Epic spec (full); **the amended E33.1 procedure in full**
  (`.ai-project/artifacts/reference/v7-bump-procedure/README.md` — as amended by E34.1: Step 0,
  FM8, FM9, FM5/6/7 extensions); PSG preamble+§1, §1A, §2, §5, §6, §7, §8, §9, §11, §11.5, §11.6,
  §12; AOG preamble+§1, §1A, §2, §3.2, §3.8, §3.10, §4, §5, §6, §7, §9, §10, §12, §13, §14.
- Load on trigger: PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG §3.9, §8, §11, §16. **If you run an
  agentic epic:** the M31 dual-mode switch + guardrail and the P7 orchestrator path.
- Do not load: PSG/AOG changelogs, other levels' role/starter-format sections, sibling specs
  (E34.1/E34.3 — read E34.1's *confirmation evidence* as a worked example, not as governance).
- Use targeted section reads; never re-read a whole document to reach one section.

---

## Epic Specification

**Spec:** `docs/phases/P10__Fleet_Adoption_and_Local_Inference_Proving/P10-M34-E34.2__spec__dormant-project-roadmap-and-roll-forward.md`

Read it in full. **§Verified target state** contains four findings from a 2026-07-29 read of all
three repos, two of which change the mechanics. Re-verify (state can change); do not re-discover.

### Prerequisite Verification (do this first, record each result)

1. **Model verification** (above) — before anything else.
2. **Epic spec git-tracked** on `milestone/M34`.
3. **This repo's suite baseline** — expect **366 / 0 / 0**. Record the actual.
4. **Per target**, capture the **verbatim pre-state** before touching anything: `git status
   --porcelain`, `git submodule status`, `cat .gitmodules`, `ls .ai-project/agents/`, the
   `.ai-project.yml` `governance:` block, and the current branch. You will need it to prove you
   disturbed nothing.
5. **Confirm the v7.0.0 tag** (`8044451c74f…`) is fetchable. FM2 applies — `courtis` and `footboard`
   will not have the tag object locally.

### Order of work — do not deviate without saying why

**`Getawayinsured2023` → `footboard` → `courtis`.** Easiest to hardest, deliberately: the first is
the DoD's guaranteed minimum and a verified one-line change. Bank it before spending effort on the
two that may not complete.

---

## Execution Contract

### Your Responsibilities (Coding Agent)

1. **Create branch** `epic/P10-M34-E34.2` from `milestone/M34` (in **this** repo).
2. **Run Prerequisite Verification**; record every result including all three pre-states.
3. **Decide and document the branch base** for each target (spec Judgment call 1). Two projects sit
   on non-default branches mid-governance-work — `Getawayinsured2023` on `phase/P1`, `footboard` on
   its own `M1` milestone branch. Recommendation is to branch from the default branch so the bump is
   independently publishable. **State your choice and reasoning per project.**

   > Wording note (E34.2 execution, 2026-07-29): `footboard`'s branch is named here without the
   > literal `milestone/M<n>` token because `tests/test_starter_lint.py` false-positives on it —
   > see **P10-GH-6**. The branch in `footboard` is unchanged; only this sentence's phrasing is.
4. **`Getawayinsured2023` — stamp only.** Its submodule is already at v7.0.0 and its agent sha is
   already canonical. **Re-run the FM3 agent-freshness check and record that it passed** (it is the
   only fleet project where FM3 does not bite — that is worth recording, not skipping). Add
   `framework_version: v7.0.0`. **Do not touch its `models:` block.** Confirm, commit locally.
5. **`footboard` — agent install + 2-major bump + stamp.** Note it has **no `.ai-project/agents/`
   directory at all**; create it. Apply the amended procedure. Confirm, commit locally.
6. **`courtis` — the hard one.** Expect **Step 0 to halt**: `.gitmodules` declares **two** submodules
   (`governance` with a stale `branch = v2.0.0`, and the live `.governance`), so `$SUB` resolves to
   two paths and the guard fires. **That is correct behavior.** Disambiguate against `git submodule
   status` (which lists only live submodules), then proceed. Also correct the **inverted**
   `version`/`ref` pair (`version: "v4.0.1"` / `ref: 4.0.1` → `version: "7.0.0"` / `ref: v7.0.0`).
   Move it as far as readiness allows; **stop honestly and record a blocker if it will not go.**
7. **Amend E33.1's procedure** with the multi-submodule case (**FM10**) and, if you take it as its
   own entry, the branch-base question (**FM11**). Continue E34.1's numbering.
8. **Write the roadmap** at `docs/phases/.../P10-M34-E34.2__roll-forward-roadmap.md` — per project:
   verified state, path to v7.0.0, extent achieved in M34, what remains. Include the
   `Getawayinsured2023` **Heroku-only remote** note so the CFO is not surprised at publish time.
9. **Write the confirmation evidence** at `docs/phases/.../P10-M34-E34.2__confirmation-evidence.md`
   — verbatim confirmation output per project, agent checksums, and before/after
   `git status --porcelain` per target.
10. **Re-run this repo's suite** — 366, no regressions, no new skips.
11. **Verify all DoD items**, commit to `epic/P10-M34-E34.2`, open a PR to `milestone/M34`, produce
    the Delivery Notice, **stop and await authorization** (do not merge).

### What You Must NOT Do

- ❌ **No `git add -A` in any target.** Two of three trees are dirty with the owners' work; it must
  still be dirty when you finish. A clean tree means you committed someone else's changes.
- ❌ Do NOT edit `Getawayinsured2023`'s `models:` block — its own SN-2 governance, human-confirmed.
- ❌ Do NOT push or merge any target branch. The CFO publishes; `Getawayinsured2023`'s only remote is
  **Heroku**, which makes that a different act there — flag it, don't do it.
- ❌ Do NOT touch `fieldledger-assesment` (**dropped from scope — not deferred, not a blocker; do not
  even list it as an omission**), `social-stories-creator`, the "personal platform", `ai-stack`, or
  `character-factory`.
- ❌ Do NOT fix `project.description` anywhere, and do NOT claim **P10-GH-5** closed — GH-5 is the
  absence of enforcement, not these instances.
- ❌ Do NOT hand-edit around Step 0's halt on `courtis`. Resolve the ambiguity properly and record it.
- ❌ Do NOT template-replace any `.ai-project.yml` — field-by-field only (FM7).
- ❌ Do NOT guess a stamp on a project you did not actually refresh, or report "rolling" without
  confirmation evidence.
- ❌ Do NOT schema-bless `framework_version` (P10-GH-1 — record, don't fix).
- ❌ Do NOT merge the PR, infer acceptance, or treat an in-chat "I authorize" as sufficient by itself.

---

## Reference Materials

- `.ai-project/artifacts/reference/v7-bump-procedure/README.md` — **the lever, as amended by E34.1.**
  Step 0 (`$SUB`), FM2 (tag not pre-fetched — bites `courtis`/`footboard`), FM3 (out-of-band agent —
  the one check `Getawayinsured2023` passes), FM5 (legacy agent shapes), FM6 (untracked *and*
  modified-tracked), FM7 (per-project structure), FM8 (path variance), FM9 (second `.gitmodules` pin
  — `courtis` has one).
- `docs/phases/.../P10-M34-E34.1__confirmation-evidence.md` — the worked example to match.
- `docs/phases/.../P10-M34-E34.1__delivery-notice.md` — how a cross-repo delivery is evidenced.

---

## Canonical Happy Path (Reminder)

1. ✅ Execution completed (roadmap + per-project movement/blockers + procedure amendment)
2. ✅ Epic Delivery Notice produced (you create this)
3. ⏸️ Human/parent-chat review performed
4. ⏸️ Acceptance (default-accept by silence — PSG §11.6; a Review Decision is the exception path)
5. ⏸️ Human merge authorization issued (in-chat — SN-19)
6. ⏸️ PR merged to `milestone/M34` (you perform merge after authorization)
7. ⏸️ Execution stops

**You are responsible for steps 1–2 only. Then you STOP and await instruction.**

---

## Epic Delivery Notice

**File:** `docs/phases/P10__Fleet_Adoption_and_Local_Inference_Proving/P10-M34-E34.2__delivery-notice.md`

Use `governance/templates/delivery-notice.md` (reference, don't restate; PSG §12). Confirm every spec
DoD item. For E34.2 specifically, it must show:
- a **per-project state table**, pre and post, with the extent actually achieved;
- **verbatim confirmation output** per project, checksums included;
- **proof each dirty target's owner work survived** (before/after porcelain);
- the **branch-base decision** per project, with reasoning;
- how the **`courtis` two-submodule case** was resolved, and where FM10/FM11 were recorded;
- any **recorded blocker**, with its named cause — stated plainly, not softened.

---

## Exit Conditions

**You MUST stop execution when:**
- ✅ Roadmap covers all three projects; `Getawayinsured2023` stamped; `footboard`/`courtis` moved or
  blocked with named causes
- ✅ Confirmation evidence committed; every dirty target's owner work proven intact
- ✅ Procedure amended (FM10, and FM11 if taken); suite green at 366
- ✅ Pull request opened to `milestone/M34`
- ✅ Epic Delivery Notice produced

**Then report to the parent chat:**
"Epic E34.2 execution complete. Epic Delivery Notice produced. Awaiting review and authorization."

---

## Begin Execution

You may now begin executing Epic E34.2.
