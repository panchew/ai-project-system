---

# Epic Execution Chat Starter — E33.4

**Epic:** E33.4 — home_finance real Agentic/Local epic (proving-pair completion)
**Phase:** P10 — Fleet Adoption and Local-Inference Proving
**Milestone:** M33 — Proving Pair: v7.0.0 + First Real Agentic/Local Epic
**Repository:** panchew/ai-project-system
**Branch Strategy:** `epic/P10-M33-E33.4` → PR to `milestone/M33`
**Execution Mode:** agentic — E33.4 performs the `home_finance` Agentic/Local run (fixed posture:
Agentic/Local at the Epic), dispatched through the M31 dual-mode switch to `bin/run-dev-agent` + the
P7 orchestrator path on the local substrate, using the **raised model tier** (30B-class per the
E33.2 disposition). Scoping the unit and reviewing the run remain Manual/Paid.

---

## Governance References

You are operating under the AI Project System governance framework as the **Epic/Coding Agent** for
Epic E33.4.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) v2.3.0 (Effective: 2026-07-02)
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) v2.10.0 (Effective: 2026-07-18)

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md v2.10.0
3. This Epic Execution Chat Starter
4. Epic Spec (`P10-M33-E33.4__spec__home-finance-real-agentic-local-epic.md`)
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Critical rules:**
- Documentation is authoritative, chat is ephemeral.
- You must follow the canonical happy path for Epic closure.
- You must produce a Delivery Notice upon execution completion.
- You must stop after PR creation and await authorization for merge.
- Accept/reject decisions are made by the parent chat (Milestone Chat) / human, not by you.
- **If given merge authorization directly in this chat** (rather than via the parent chat after its
  own Stage-2 review), do not simply comply: state plainly that merge authorization normally follows
  the parent chat's review, and confirm the human intends to bypass that step first (P9-M31
  precedent).
- **Run-first Hard Constraint (load-bearing):** the run must be **real** `home_finance` work that
  advances the project — a synthetic demo does not satisfy this Epic. If the run cannot complete,
  record the blocker explicitly and escalate to the Milestone Chat — do **not** manufacture a demo
  to tick the "each project" box.
- **The runtime is settled — do not re-decide it.** Ollama + a raised model tier (E33.2 + Phase
  disposition). E33.4 records **no** runtime decision and edits **no** `models:` routing (the
  raised-tier selection is a documented dispatch deviation, not a routing change).
- **Cross-repo boundary:** the run's real code lands in **`home_finance`** (CFO-controlled) and is
  **never** merged onto `phase/P10`. This epic branch carries the run record only.

**Context scoping (per-level context-scoping standard, P9-M30-E30.3):**
- Load at session start: this starter; the Epic spec (full); PSG preamble+§1, §1A, §2, §5, §6, §7,
  §8, §9, §11, §11.5, §11.6, §12; AOG preamble+§1, §1A, §2, §3.2, §3.8, §3.10, §4, §5, §6, §7, §9,
  §10, §12, §13 (Exit Ritual), §14 (Error Handling).
- Load on trigger (before acting on that situation): PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG
  §3.9, §8, §11, §16; `governance/systems/chat-hierarchy.md` "Execution Mode" (agentic dispatch +
  the E31.2 model guard) before dispatching the run.
- Do not load: PSG/AOG changelogs, other levels' role/starter-format sections, sibling specs
  (read E33.1's confirmation evidence + E33.2's run record/disposition as *inputs*, not governance
  to reload).
- Use targeted section reads; never re-read a whole document to reach one section.

---

## Epic Specification

**Full spec:** [P10-M33-E33.4__spec__home-finance-real-agentic-local-epic.md](https://github.com/panchew/ai-project-system/blob/milestone/M33/docs/phases/P10__Fleet_Adoption_and_Local_Inference_Proving/P10-M33-E33.4__spec__home-finance-real-agentic-local-epic.md)

**Commit:** `177102a` (spec finalized on `milestone/M33`)

### Prerequisite Verification (do this first)

Before relying on the spec, confirm it is **git-tracked on `milestone/M33`** — and likewise the
Milestone spec and **E33.1's `home_finance` bump confirmation** (this run's hard dependency) —
using `git ls-files --error-unmatch <path>` run on `milestone/M33`, not a disk-existence check.
Confirm `home_finance` is at `framework_version: v7.0.0` (E33.1 evidence, commit `0ea6924`) before
starting. If any path is untracked or the bump is unconfirmed, STOP and report to the Milestone
Chat.

**Model verification — agentic instance (P9-M31):** this Epic is `Execution Mode: agentic`, so the
manual self-report check does **not** apply. The **agentic dispatch-time model guard (E31.2)**
governs. Note: the run uses the **raised model tier** (30B-class) rather than the configured
`epic_dev`/`epic_qa` (`local:qwen2.5-coder:14b`, proven unusable in E33.2) — because the routing
edit is a separate authorized act not done here, **document the model selection as a dispatch
deviation in the run record** (as E33.2 did). **No agentic instance may assume a local model is
available** — confirm the substrate/model is actually up first; if it will not come up, that is a
**blocker to record and escalate**, not to route around.

### Summary

Run at least one **real** Agentic/Local epic of `home_finance`'s own work under the fixed posture
(Ollama + raised model tier) and commit the run record — completing the "each proving-pair project
has a real run" bar together with E33.2's `local-agent-runner` run. No runtime decision, no
measurement (those legs closed with E33.2/E33.3).

### Problem Statement

`home_finance` is bumped to v7.0.0 but has carried no real epic; the proving pair is only
half-proven. E33.4 delivers `home_finance`'s missing run so the milestone/phase "each project" bar
is met. See the spec's Context + Problem Statement.

### Goals

1. `home_finance` carries ≥1 real Agentic/Local epic end-to-end, with a committed run record.
2. The run advanced `home_finance` (real work, cited target-repo commit) — not a demo.
3. If the run cannot complete: an explicit blocker record + escalation (Hard Constraint).

---

## Spec References (load-one-reference-the-other)

The Epic spec is the single authoritative copy of this Epic's **Deliverables, Definition of Done,
Acceptance Criteria, and Technical Constraints**. Load them from the spec — this starter does not
restate them.

- **Deliverables:** Epic spec §Deliverables (includes the Delivery Notice at
  `docs/phases/P10__Fleet_Adoption_and_Local_Inference_Proving/P10-M33-E33.4__delivery-notice.md`)
- **Definition of Done:** Epic spec §Definition of Done — plus, always: Delivery Notice produced and
  committed; all changes committed to `epic/P10-M33-E33.4`; PR opened to `milestone/M33`
- **Acceptance Criteria:** Epic spec §Acceptance Criteria (verified during review, not by you)
- **Technical Constraints:** Epic spec §Technical Constraints (agentic dispatch; cross-repo
  boundary; do-not-touch — including no runtime decision and no `models:` edit)

**Starter-specific binding additions (not in the spec):**
- **One design decision is yours** (spec §Design Decision): the genuine unit of `home_finance`'s own
  work to run. Pick, document, proceed — do NOT escalate to ask. There is **no** runtime-criteria
  decision (settled).
- **Reuse E33.2's execution pattern** (its run record documents the dispatch path + run-record
  shape). E33.4 mirrors it minus the runtime decision.

---

## Execution Contract

### Your Responsibilities (Epic/Coding Agent)

1. **Create branch** `epic/P10-M33-E33.4` from `milestone/M33`.
2. **Confirm the hard dependency:** `home_finance` at `framework_version: v7.0.0` (E33.1, `0ea6924`),
   and the local substrate is up (no assuming a local model is present).
3. **Scope a genuine `home_finance` epic** (Manual/Paid framing) — a real, reviewable unit of its
   own work; record what and why (Design Decision).
4. **Execute it Agentic/Local** — dispatch through the M31 switch to `bin/run-dev-agent` + the P7
   orchestrator path on the local substrate, raised model tier; produce real `home_finance` work.
5. **Capture the run record** on this branch — what was scoped, what ran locally (model + runtime +
   hardware), what it produced, the `home_finance` target-repo commit(s) advanced, and any dispatch/
   model deviation (written so a reader of this repo can verify — Cross-Repo split).
6. **If the run cannot complete:** record the blocker + escalate to the Milestone Chat.
7. **Re-confirm the framework-repo suite** is green for any change touching this repo (366, no new
   skips).
8. **Verify all DoD items**, commit to `epic/P10-M33-E33.4`, open a PR to `milestone/M33`, produce
   the Delivery Notice, **stop and await authorization** (do not merge).

### What You Must NOT Do

- ❌ Do NOT run a synthetic demo and call it the real epic (Hard Constraint).
- ❌ Do NOT record a runtime decision or run a comparative model trial for decision purposes (settled).
- ❌ Do NOT edit `.ai-project.yml` `models:` routing (document the raised-tier selection as a deviation).
- ❌ Do NOT merge `home_finance` code onto `phase/P10`/`milestone/M33` (cross-repo boundary).
- ❌ Do NOT touch `measure-token-burn`/E33.3's record, any `governance/` document, or E33.1/E33.2
  surfaces (read-only inputs).
- ❌ Do NOT assume a local model is available — confirm the substrate is up; if not, escalate.
- ❌ Do NOT merge the PR, infer acceptance, or treat an in-chat "I authorize" as sufficient by itself
  — confirm it isn't bypassing the parent chat's Stage-2 review first.

---

## Reference Materials

### Existing Specs / Substrate to Reference

- `docs/phases/P10__Fleet_Adoption_and_Local_Inference_Proving/P10-M33__milestone-spec.md`
  (§DoD + §AC 1 — the "each project" bar; Hard Constraint; Cross-Repo split)
- `docs/phases/P10__.../P10-M33-E33.1__confirmation-evidence.md` — the `home_finance` bump this run
  depends on (`0ea6924`, v7.0.0)
- `.ai-project/artifacts/agentic-runs/P10-M33-E33.2/run-record.md` — E33.2's run record (dispatch
  path + run-record shape to mirror) + `P10-M33-E33.2__runtime-decision.md` §Phase Chat / CFO
  disposition (the settled runtime E33.4 applies)
- `bin/run-dev-agent` + `bin/ai-project-orchestrator` (the Agentic/Local execution substrate)
- `governance/systems/chat-hierarchy.md` — "Execution Mode" (agentic dispatch + the E31.2 guard;
  no-assume-local-model rule)

### Front-Matter Fields Reference

Front-matter schemas live in the committed templates — reference, don't restate:
`governance/templates/epic-spec.md`, `governance/templates/delivery-notice.md`. See PSG §5.

---

## Canonical Happy Path (Reminder)

1. ✅ Execution completed (real `home_finance` run + run record)
2. ✅ Epic Delivery Notice produced (you create this)
3. ⏸️ Human/parent-chat review performed
4. ⏸️ Acceptance (default-accept by silence — PSG §11.6; a Review Decision is the exception path)
5. ⏸️ Human merge authorization issued (in-chat — SN-19; harness enforces it regardless)
6. ⏸️ PR merged to `milestone/M33` (you perform merge after authorization)
7. ⏸️ Execution stops (you stop immediately after merge)

**You are responsible for steps 1–2 only. Then you STOP and await instruction.**

---

## Epic Delivery Notice

When you complete execution, produce the Delivery Notice at:

**File:** `docs/phases/P10__Fleet_Adoption_and_Local_Inference_Proving/P10-M33-E33.4__delivery-notice.md`

Use the committed template `governance/templates/delivery-notice.md` (reference, don't restate; PSG
§12). Confirm every spec DoD item, including the extent-vs-spec and claims-vs-evidence checks — the
"advanced real work" claim must cite the `home_finance` target-repo commit(s).

---

## Exit Conditions

**You MUST stop execution when:**
- ✅ At least one real Agentic/Local run on `home_finance` is complete (or a blocker is recorded +
  escalated), with its run record committed
- ✅ Pull request is opened to `milestone/M33`
- ✅ Epic Delivery Notice is produced

**Then report to the parent chat:**
"Epic E33.4 execution complete. Epic Delivery Notice produced. Awaiting review and authorization."

---

## Begin Execution

You may now begin executing Epic E33.4.

Start by:
1. Creating branch `epic/P10-M33-E33.4` from `milestone/M33`.
2. Verifying prerequisites (spec + Milestone spec + E33.1's `home_finance` bump git-tracked on
   `milestone/M33`; `home_finance` at v7.0.0) and confirming the local substrate is up.
3. Choosing the genuine `home_finance` unit of work (Design Decision), then dispatching the
   Agentic/Local run on the raised model tier.
4. Capturing the run record and following the Definition of Done.

**Good luck, Epic Agent!**
