---

# Epic Execution Chat Starter — E33.2

**Epic:** E33.2 — First real Agentic/Local epic on the pair + runtime decision
**Phase:** P10 — Fleet Adoption and Local-Inference Proving
**Milestone:** M33 — Proving Pair: v7.0.0 + First Real Agentic/Local Epic
**Repository:** panchew/ai-project-system
**Branch Strategy:** `epic/P10-M33-E33.2` → PR to `milestone/M33`
**Execution Mode:** agentic — E33.2 performs the Agentic/Local run (the fixed posture:
Agentic/Local at the Epic). The run is dispatched through the M31 dual-mode switch to
`bin/run-dev-agent` + the P7 orchestrator path on the local inference substrate. Scoping the
target-project unit and reviewing the runtime decision remain Manual/Paid (the Milestone Chat's
Stage-2).

---

## Governance References

You are operating under the AI Project System governance framework as the **Epic/Coding Agent**
for Epic E33.2.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) v2.3.0 (Effective: 2026-07-02)
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) v2.10.0 (Effective: 2026-07-18)

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md v2.10.0
3. This Epic Execution Chat Starter
4. Epic Spec (`P10-M33-E33.2__spec__first-real-agentic-local-epic-and-runtime-decision.md`)
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Critical rules:**
- Documentation is authoritative, chat is ephemeral.
- You must follow the canonical happy path for Epic closure.
- You must produce a Delivery Notice upon execution completion.
- You must stop after PR creation and await authorization for merge.
- Accept/reject decisions are made by the parent chat (Milestone Chat) / human, not by you.
- **If given merge authorization directly in this chat** (rather than via the parent chat after its
  own Stage-2 review), do not simply comply: state plainly that merge authorization normally follows
  the parent chat's review, and confirm the human intends to bypass that step first (P9-M31
  precedent).
- **Run-first Hard Constraint (load-bearing here):** the runtime decision MUST derive from a real
  run that advances the target project — a synthetic demo does not satisfy this Epic. If a real run
  cannot complete, record the blocker explicitly and escalate to the Milestone Chat — do **not**
  substitute an abstract decision or hand-waved numbers (spec §Hard Constraint).
- **Cross-repo boundary:** the run's real code lands in the **target repo** (`home_finance` or
  `local-agent-runner`, CFO-controlled) and is **never** merged onto `phase/P10`. This epic branch
  carries the run record + runtime decision only (spec §Cross-Repo Record/Evidence Split).

**Context scoping (per-level context-scoping standard, P9-M30-E30.3):**
- Load at session start: this starter; the Epic spec (full); PSG preamble+§1, §1A, §2, §5, §6, §7,
  §8, §9, §11, §11.5, §11.6, §12; AOG preamble+§1, §1.1, §2, §3.2, §3.8, §3.10, §4, §5, §6, §7, §9,
  §12, §14, §15 (Exit Ritual), §16 (Error Handling).
- Load on trigger (before acting on that situation): PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG
  §3.9, §8, §13, §17; `governance/systems/chat-hierarchy.md` "Execution Mode" (agentic dispatch +
  the E31.2 model guard) before dispatching the run.
- Do not load: PSG/AOG changelogs, other levels' role or starter-format sections, sibling specs
  (E33.1/E33.3).
- Use targeted section reads; never re-read a whole document to reach one section. PSG and AOG
  remain fully authoritative — a triggered situation requires its section loaded before acting.

---

## Epic Specification

**Full spec:** [P10-M33-E33.2__spec__first-real-agentic-local-epic-and-runtime-decision.md](https://github.com/panchew/ai-project-system/blob/milestone/M33/docs/phases/P10__Fleet_Adoption_and_Local_Inference_Proving/P10-M33-E33.2__spec__first-real-agentic-local-epic-and-runtime-decision.md)

**Commit:** `fc9043f` (spec finalized on `milestone/M33`)

### Prerequisite Verification (do this first)

Before relying on the spec, confirm it is **git-tracked on `milestone/M33`** — and likewise the
Milestone spec and **E33.1's merged bump evidence** (this run's hard dependency) — using
`git ls-files --error-unmatch <path>` run on `milestone/M33`, not a disk-existence check. Confirm
E33.1 is merged (its confirmation evidence is on `milestone/M33`) and both proving-pair targets are
at `framework_version: v7.0.0` before starting — the run needs a bumped target. If any path is
untracked or the bump is unconfirmed, STOP and report to the Milestone Chat.

**Model verification — agentic instance (P9-M31):** this Epic is `Execution Mode: agentic`, so the
manual self-report check does **not** apply. Instead, the **agentic dispatch-time model guard
(E31.2)** governs: the dispatch verifies the run against `.ai-project.yml`'s `models.epic_dev` /
`models.epic_qa` (currently `local:qwen2.5-coder:14b`). **No agentic instance may assume a local
model is available** — confirm the local inference substrate/model is actually up before relying on
it (`governance/systems/chat-hierarchy.md`, Execution Mode). If the substrate will not come up, that
is a **blocker to record and escalate**, not a condition to route around.

### Summary

Run at least one **real** Agentic/Local epic of a proving-pair project's own work under the fixed
posture, commit the run record, and record the **Ollama-vs-llama.cpp+Qwen3.6** runtime decision with
the run's own reasons (quality, throughput, loadability, review burden). This settles P10's one open
substrate risk.

### Problem Statement

Adoption is asserted, not demonstrated, and the fleet's local-inference substrate is unresolved. One
real run must produce both a demonstrated governed epic and a runtime decision traceable to its own
observations. See the spec's Problem Statement.

### Goals

1. At least one real Agentic/Local epic run on a proving-pair project, with a committed run record.
2. The run advanced the target project (real work, cited target-repo commits) — not a demo.
3. The Ollama-vs-llama.cpp+Qwen3.6 decision recorded with the run's own reasons across quality,
   throughput, loadability, review burden.
4. If a run cannot complete: an explicit blocker record + escalation (Hard Constraint).

---

## Spec References (load-one-reference-the-other)

The Epic spec is the single authoritative copy of this Epic's **Deliverables, Definition of Done,
Acceptance Criteria, and Technical Constraints**. Load them from the spec — this starter does not
restate them.

- **Deliverables:** Epic spec §Deliverables (includes the Delivery Notice at
  `docs/phases/P10__Fleet_Adoption_and_Local_Inference_Proving/P10-M33-E33.2__delivery-notice.md`)
- **Definition of Done:** Epic spec §Definition of Done — plus, always: Delivery Notice produced and
  committed; all changes committed to `epic/P10-M33-E33.2`; PR opened to `milestone/M33`
- **Acceptance Criteria:** Epic spec §Acceptance Criteria (verified during review, not by you)
- **Technical Constraints:** Epic spec §Technical Constraints (agentic dispatch; cross-repo
  boundary; do-not-touch — including no `models:` edit)

**Starter-specific binding additions (not in the spec):**
- **Two design decisions are yours to resolve** (spec §Design Decisions): (1) which proving-pair
  project's first real epic to run, and (2) the runtime decision criteria across the four
  dimensions. Pick, document, proceed — do NOT escalate to ask. The only escalation trigger is a
  real run that cannot complete.
- **Record the decision; do not change the routing.** This Epic records the runtime decision. It
  does **not** edit `.ai-project.yml`'s `models:` block — any routing change is a separate
  authorized act (spec §Non-Goals).

---

## Execution Contract

### Your Responsibilities (Epic/Coding Agent)

1. **Create branch** `epic/P10-M33-E33.2` from `milestone/M33`.
2. **Confirm the hard dependency:** E33.1 merged, both targets at `framework_version: v7.0.0`, and
   the local substrate is up (no assuming a local model is present).
3. **Scope a genuine target-project epic** (Manual/Paid framing) — a real, reviewable unit of the
   chosen proving-pair project's own work; record what and why (spec §Scope of Work 1; Design
   Decision 1).
4. **Execute it Agentic/Local** — dispatch through the M31 dual-mode switch to `bin/run-dev-agent` +
   the P7 orchestrator path on the local substrate; produce real target-repo work.
5. **Capture the run record** on this branch — what was scoped, what ran locally (model + runtime +
   hardware), what it produced, throughput/loadability observations, and the target-repo commit(s)
   advanced (written so a reader of this repo can verify — Cross-Repo split).
6. **Record the runtime decision** with the run's own reasons across quality, throughput,
   loadability, review burden.
7. **If the run cannot complete:** record the blocker + escalate to the Milestone Chat (in place of
   a substituted decision).
8. **Re-confirm the framework-repo suite** is green for any change touching this repo (363, no new
   skips).
9. **Verify all DoD items**, commit to `epic/P10-M33-E33.2`, open a PR to `milestone/M33`, produce
   the Delivery Notice, **stop and await authorization** (do not merge).

### What You Must NOT Do

- ❌ Do NOT run a synthetic demo/benchmark and call it the real epic (Hard Constraint).
- ❌ Do NOT edit `.ai-project.yml`'s `models:` block or any routing (record the decision, don't apply
  it).
- ❌ Do NOT merge the run's target-repo code onto `phase/P10`/`milestone/M33` (cross-repo boundary).
- ❌ Do NOT do E33.3's measurement work, touch `measure-token-burn`, or any `governance/` document.
- ❌ Do NOT rebuild the dual-mode switch / guardrail (P9/M31 — applied, not edited).
- ❌ Do NOT assume a local model is available — confirm the substrate is up; if not, escalate.
- ❌ Do NOT merge the PR, infer acceptance, or treat an in-chat "I authorize" as sufficient by itself
  — confirm it isn't bypassing the parent chat's Stage-2 review first.

---

## Reference Materials

### Existing Specs / Substrate to Reference

- `docs/phases/P10__Fleet_Adoption_and_Local_Inference_Proving/P10-M33__milestone-spec.md`
  (Hard Constraint; Cross-Repo split; Epic Detail → E33.2)
- `bin/run-dev-agent` + `bin/ai-project-orchestrator` (the Agentic/Local execution substrate)
- `governance/systems/chat-hierarchy.md` — "Execution Mode" (agentic dispatch + the E31.2 model
  guard; no-assume-local-model rule)
- `.ai-project/artifacts/agentic-runs/P7-M26-E26.3-PROVE/` (run-record **shape** reference — a PROVE
  demo, NOT a real epic; does not satisfy the Hard Constraint)
- E33.1 confirmation evidence (`P10-M33-E33.1__confirmation-evidence.md`) — the bumped targets this
  run depends on
- Local-model setup reference: https://quesma.com/blog/qwen-36-is-awesome/ (Qwen3.6 27B Q8_0 on
  llama.cpp; ~32 tok/s at ~42 GB)

### Front-Matter Fields Reference

Front-matter schemas live in the committed templates — reference, don't restate:
`governance/templates/epic-spec.md`, `governance/templates/delivery-notice.md`. See PSG §5.

---

## Canonical Happy Path (Reminder)

1. ✅ Execution completed (real run + run record + runtime decision)
2. ✅ Epic Delivery Notice produced (you create this)
3. ⏸️ Human/parent-chat review performed (Manual/Paid — the runtime decision is confirmed here)
4. ⏸️ Acceptance (default-accept by silence — PSG §11.6; a Review Decision is the exception path)
5. ⏸️ Human merge authorization issued (in-chat — SN-19; harness enforces it regardless)
6. ⏸️ PR merged to `milestone/M33` (you perform merge after authorization)
7. ⏸️ Execution stops (you stop immediately after merge)

**You are responsible for steps 1–2 only. Then you STOP and await instruction.**

---

## Epic Delivery Notice

When you complete execution, produce the Delivery Notice at:

**File:** `docs/phases/P10__Fleet_Adoption_and_Local_Inference_Proving/P10-M33-E33.2__delivery-notice.md`

Use the committed template `governance/templates/delivery-notice.md` (reference, don't restate; PSG
§12). Confirm every spec DoD item, including the extent-vs-spec and claims-vs-evidence checks — for
E33.2, the claims-vs-evidence check is load-bearing: every runtime reason must trace to a run
observation, and the "advanced real work" claim must cite the target-repo commit(s).

---

## Exit Conditions

**You MUST stop execution when:**
- ✅ At least one real Agentic/Local run is complete (or a blocker is recorded + escalated), with its
  run record + runtime decision committed
- ✅ Pull request is opened to `milestone/M33`
- ✅ Epic Delivery Notice is produced

**Then report to the parent chat:**
"Epic E33.2 execution complete. Epic Delivery Notice produced. Awaiting review and authorization."

---

## Begin Execution

You may now begin executing Epic E33.2.

Start by:
1. Creating branch `epic/P10-M33-E33.2` from `milestone/M33`.
2. Verifying prerequisites (spec + Milestone spec + E33.1's merged bump git-tracked on
   `milestone/M33`; both targets at v7.0.0) and confirming the local substrate is up.
3. Choosing the proving-pair project + the real unit of work (Design Decision 1), then dispatching
   the Agentic/Local run.
4. Capturing the run record + runtime decision, and following the Definition of Done.

**Good luck, Epic Agent!**
