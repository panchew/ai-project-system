---
artifact_type: delivery_notice
artifact_version: 1.0
timestamp: 2026-07-30T23:55:00Z
issuer_chat: Epic Agent (P10-M35-E35.4)
issuer_role: Epic Agent
status: delivered
epic_id: P10-M35-E35.4
milestone_id: P10-M35
phase_id: P10
project_name: ai-project-system
merge_details:
  pr_number: 163
  pr_url: https://github.com/panchew/ai-project-system/pull/163
  merge_commit: <pending — human-authorized merge, not yet performed>
  merge_timestamp: <pending>
  merge_strategy: merge
  target_branch: milestone/M35
duration:
  start_date: 2026-07-30
  end_date: 2026-07-30
  elapsed_days: 0
final_artifacts:
  - name: Chat Hierarchy — "The execution matrix (ratified)" (new subsection)
    path: governance/systems/chat-hierarchy.md
    type: spec
  - name: Chat Hierarchy — "Mode is not authority" (new subsection; normative home of the rule)
    path: governance/systems/chat-hierarchy.md
    type: spec
  - name: Chat Hierarchy — E35.3 handback statement converted to a citation
    path: governance/systems/chat-hierarchy.md
    type: spec
  - name: E35.4 Delivery Notice
    path: docs/phases/P10__Fleet_Adoption_and_Local_Inference_Proving/P10-M35-E35.4__delivery-notice.md
    type: report
completion_notice_timestamp: 2026-07-30T23:55:00Z
review_decision_timestamp: <pending — parent Milestone Chat Stage-2>
---

# Delivery Notice: P10-M35-E35.4 — Execution matrix ratification + mode-is-not-authority

## Summary

The ratified execution matrix (HQ Ruling on SN-25, Decision 4) is recorded normatively in
`governance/systems/chat-hierarchy.md`'s Execution Mode section, together with the constraint that
makes the restoration safe — **mode is not authority** — now the corpus's single normative home for
that rule. Documentation only: no mechanism was built, no configuration changed, and **nothing in
this repository runs differently the day this merges.**

The table itself was transcription, and it verified clean on the first comparison. The delivery is
the three reconciliations around it, each of which prevents the table from contradicting the section
it lands in.

Work is committed to `epic/P10-M35-E35.4` and PR #163 is open to `milestone/M35`. **Not merged** —
awaiting the parent Milestone Chat's Stage-2 review and human merge authorization.

## Merge Details

- PR Number: #163
- PR URL: https://github.com/panchew/ai-project-system/pull/163
- Merge Commit: *pending — merge is human-authorized (SN-19 / PSG §11.6); not performed*
- Target Branch: `milestone/M35`
- Merge Strategy: merge
- Merge Time: *pending*

*(P10-GH-4 applies: these `merge_details` are unfillable at Delivery Notice time by construction —
the notice is produced before the authorized merge.)*

## Duration

- Started: 2026-07-30
- Completed: 2026-07-30
- Total Time: under one session (estimate was 0.5 day)

## Prerequisite Verification (starter §Prerequisite Verification)

| # | Check | Result |
|---|---|---|
| 1 | Spec git-tracked on `milestone/M35` | **PASS** — `git ls-files --error-unmatch` resolved the spec path; branch head `1f31abc` |
| 2 | Suite baselined before any edit | **PASS** — **366 passed / 0 failures / 0 skips** (24.12s), the expected figure; adopted as-is |
| 3 | Matrix compared cell by cell against `phase/P10` source | **PASS** — byte-identical (see below) |
| 4 | Phase spec's Note-on-Decision-2 footnote read in its own file | **PASS** — read at `phase/P10` §Ratified Decisions, lines 639–647; the supersession record agrees with it, not paraphrased from the Epic spec |
| 5 | `chat-hierarchy.md` Execution Mode section read in full | **PASS** — incl. "The two Execution Modes" with "What exists mechanically today", "Creation Chat and HQ Chat: Manual-Only, Permanently", "Manual Chat Model Verification", and E35.3's handback subsection |

**Model verification (P9-M31-E31.3):** harness self-report `claude-opus-5`; `.ai-project.yml`
`models.epic_manual: remote:claude-opus-5`. **Match — no mismatch, execution proceeded.**

## Cell-by-cell matrix comparison (DoD item 1)

Compared mechanically rather than by eye: the table was extracted from
`git show phase/P10:…/P10__phase-spec.md` (§P10.3, lines 267–273) and `diff`ed against the delivered
table at `governance/systems/chat-hierarchy.md:120–126`. **`diff` returned empty — byte-identical,
including the em-dash and both bold spans.**

| Row | Level | Execution Mode | Inference locality | Match |
|---|---|---|---|---|
| 1 | Creation | `Manual only (permanent, SN-22)` | `Remote` | ✅ exact |
| 2 | HQ | `Manual only (permanent, SN-22)` | `Remote` | ✅ exact |
| 3 | Phase | `Agentic or manual` | `Remote` | ✅ exact |
| 4 | Milestone | `Agentic or manual` | `Remote — **local under evaluation**` | ✅ exact |
| 5 | Epic | `Agentic or manual` | `Local or remote (in force, E34.3)` | ✅ exact |

Header row and separator also identical. **No divergence found, so no escalation was triggered.**

Additionally verified: the phase spec is **identical on `phase/P10` and `milestone/M35`**
(`git diff --stat phase/P10 milestone/M35 -- …/P10__phase-spec.md` → empty), so the source
comparison holds regardless of which branch a reviewer reads it on. The ratified table in the HQ
Ruling (Decision 4) carries the same five rows.

## Surface decision and reasoning

**Chosen: `governance/systems/chat-hierarchy.md`, inside the Execution Mode section** — the spec's
recommendation, taken without modification. Two new `###` subsections were placed **after §"The two
Execution Modes" and before §"Declaration mechanism"**, exactly the suggested shape.

Reasoning: all three reconciliations are claims *about text in that section* — E31.1's dispatch-gap
paragraph sits directly above the matrix, and the Creation/HQ manual-only section and the Manual
Chat Model Verification pointer precedent are both a short scroll below. Writing the matrix anywhere
else would mean asserting things about a section it does not sit in, which is how contradictions
survive review.

**Marking:** inline dated notes — `*(Added P10-M35-E35.4, 2026-07-30.)*` — per that document's own
precedent (E31.1, E31.3, G7 and E35.3 all mark this way). **No `version` field and no `## Changelog`
were added** (P10-GH-8, deferred). The existing front-matter `effective_date: 2026-07-20` was left
unchanged, consistent with E35.3, which likewise did not move it — the absence of a versioning
mechanism here *is* P10-GH-8.

## The three reconciliations, as delivered (DoD items 2–4)

### 1. Creation/HQ — describes how they run, not a declaration they accept

> **The Creation and HQ rows describe how those levels run; they are not a declaration those levels
> accept.** "Manual only" in those two cells means: manually, always, with no per-instance override
> possible. It does **not** mean Creation or HQ carries a manual Execution Mode declaration. Neither
> level's opener gains an Execution Mode field, then or now — see "Creation Chat and HQ Chat:
> Manual-Only, Permanently" below, whose effect these two rows restate and do not amend.

The reconciliation is written out, not left to the reader to perform. `chat-hierarchy.md:131–135`.

### 2. Locality column — a pointer, not an authority

> **The locality column is a pointer, not an authority.** Inference locality is decided by
> `.ai-project/artifacts/reference/token-measurement/model-routing-policy.md` rows P1–P7, under that
> file's own **§Change discipline** (*"Policy rows change only with new cited evidence … never by
> assumption"*). That file is authoritative on locality and **wins on any divergence** with this
> column — the same relationship this document's "Manual Chat Model Verification" section below
> already has with it. A reader asking which locality applies at a level is answered by the policy
> rows, not finally by this table, which summarises them as of the ratification date.

`chat-hierarchy.md:137–143`. The corpus gains no second authority on routing.

### 3. E31.1's dispatch gap — intact, quoted, and visible

> **Restoring the mode is not building the dispatcher.** … the "What exists mechanically today"
> paragraph above — that the dispatch path is implemented for the **Epic** level only, and that
> *"no dispatch mechanism yet consumes a Phase/Milestone agentic declaration; wiring the
> orchestrator to those levels is future implementation work"* — **remains true after this
> ratification and is unmodified by it.** The matrix restores a *possibility*, not a default. No
> Phase or Milestone agentic run can be dispatched today, and nothing in this repository runs
> differently because the matrix was recorded.

`chat-hierarchy.md:145–153`. **E31.1's own paragraph was not edited, softened, or marked resolved** —
verify at `chat-hierarchy.md:102–110`, unchanged in this diff. It is quoted verbatim ten lines
below itself, so restoration cannot be read as availability.

## Mode-is-not-authority: where it is normative (DoD items 5–6)

**Normative home: `governance/systems/chat-hierarchy.md` §"Mode is not authority" (line 165)**, its
own subsection, marked in its dated note as *"**This is the normative home of this rule.** Other
sections of this document cite it rather than restating it, so the corpus holds one statement that
cannot drift against a second."*

The acts are named, in bold, as their own paragraph:

> **Until ruled otherwise, authority-bearing acts — Stage-2 acceptance and merge authorization —
> still require the human's key, whatever Execution Mode the instance is running in.**

The framing is carried: *"**Mode is what may run; gates are what may be decided without a key.**"*
So is why it binds hardest at Milestone — where Stage-2 accept authority lives, the level whose
errors propagate into merges and the reason row P4 reads paid frontier — with the operative
sentence: *"A Milestone instance declared agentic may run its review work unattended; it may not
accept a delivery or authorize a merge on its own signature."*

**Relationship to E35.3 — one home, the other citing it.** E35.3's handback subsection ("The
resolving intervention is authority-bearing", line 406) previously carried an independent
restatement of the rule, including its own copy of the Stage-2/merge sentence. That restatement is
now a citation:

> **This does not widen what the parent may authorize.** *Mode is not authority* — **cited here, not
> restated:** see "Mode is not authority" above, which is that rule's normative home (HQ Ruling on
> SN-25, Decision 4). Applied to handback: an instance running unattended holds exactly the authority
> its level always held, so a handback resolution is a **direction to the child**, not a new power
> for the resolver. *(Citation form adopted P10-M35-E35.4, 2026-07-30; the rule this section states
> was unchanged by that Epic.)*

E35.3's *applied* meaning for handback is fully preserved; only the duplicated normative sentence is
gone. This follows the citation pattern E35.3 itself established one subsection later ("cited here
rather than restated — a fourth copy is a fourth thing that can drift"). **The corpus now contains
exactly one statement of the rule.**

### One addition beyond literal transcription, flagged for review

The subsection closes with a **corollary** the acceptance criteria appeared to require:

> **Corollary, for the reader who knows the default-accept model.** PSG §11.6's accept-by-silence
> turns a parent's *silence* into acceptance. That model presumes a manual instance, where the
> human's key is present at the session by construction; the matrix does not extend it to an
> unattended one. An agentic instance's silence is not the silence §11.6 speaks of, and does not by
> itself accept a delivery. This follows from the rule above rather than adding to it — no new gate
> is created here, and none is removed.

**Why it is here:** acceptance criterion 1 is that *a reader cannot come away believing agentic mode
at any level grants acceptance or merge authority.* A reader who knows PSG §11.6 would otherwise
reason that an agentic Milestone instance accepts a clean delivery by doing nothing at all — which
would defeat the criterion by exactly the silent route the Epic exists to close.

**Its status, stated honestly:** this is a deduction from Decision 4's ruled text (acceptance
requires the human's key; an unattended instance has no key), not new ruled text, and it is marked
in-line as following from the rule rather than adding to it. **If the Milestone Chat reads it as new
normative content rather than a corollary, it should be struck or referred upward** — the rest of
the delivery does not depend on it.

## SN-23 Decision #2 supersession (DoD item 7)

Recorded at `chat-hierarchy.md:154–163`. Agreement with the phase spec's **Note on Decision 2**
footnote, checked clause by clause against the footnote in its own file:

| Phase spec footnote (`phase/P10`, lines 639–647) | Delivered record | Agrees |
|---|---|---|
| Decision 2's text "preserved verbatim … not reopened" | *"Decision 2's own text is preserved verbatim in the P10 phase spec's Ratified Decisions and is **not reopened**."* | ✅ |
| "Execution Mode axis is superseded": Phase and Milestone may now run agentically or manually | *"Its **Execution Mode axis is superseded** — Phase and Milestone may now run agentically or manually…"* | ✅ |
| "restoring the P9-M31-E31.1 baseline that SN-23 had narrowed for P10's start" | *"…restoring the P9-M31-E31.1 baseline that SN-23 had narrowed for P10's start."* | ✅ |
| "locality axis stands, with Milestone × local inference now under a directed evidence evaluation (§P10.3, E35.5)" | *"Its **locality axis stands**, with Milestone × local inference now under a directed evidence evaluation (P10-M35-E35.5) rather than settled either way…"* | ✅ |
| "recorded explicitly so it is a decision, not drift" | *"The supersession is recorded explicitly so that it is a decision rather than drift."* | ✅ |
| "mode restoration does not confer authority" | §"Mode is not authority", the subsection immediately following | ✅ |

**E35.5 is not pre-empted:** the record states the cell *"is neither opened nor closed, and 'under
evaluation' records that the question is live and directed, not that it is unanswered by neglect"* —
it says an evaluation is directed and says nothing about what it will find.

## Empty-diff confirmation (DoD item 8)

`git diff --stat milestone/M35 -- <paths>` returned **empty** for all of:

| Path | Diff | Required by |
|---|---|---|
| `.ai-project/artifacts/reference/token-measurement/model-routing-policy.md` | **empty** | Spec Out-of-scope; starter ❌ #1 — no row P4 decision, no new rows |
| `.ai-project.yml` | **empty** | Spec Out-of-scope; Milestone spec (no config changes in M35) |
| `docs/…/P10__phase-spec.md` | **empty** | Spec Out-of-scope — agree with the footnote, do not add to it |
| `bin/ai-project-orchestrator` | **empty** | Hard Constraint — nothing built |

`git status` shows **one** modified source file in the implementation commit:
`governance/systems/chat-hierarchy.md` (+90 / −5). **Hard Constraint satisfied — no mechanism, no
dispatch wiring, no mode switch, no policy row touched.**

## No version field, no changelog (DoD item 9)

`chat-hierarchy.md` front matter remains `type / status / effective_date` with **no `version:` key**,
and the document has **no `## Changelog` section**. Marking is by inline dated note. P10-GH-8 stays
open and untouched by this Epic.

## Test results (DoD item 10)

| | Passed | Failed | Skipped |
|---|---|---|---|
| **Before** (baseline, `milestone/M35`) | 366 | 0 | 0 |
| **After** (`epic/P10-M35-E35.4`) | 366 | 0 | 0 |

`python3 -m pytest tests/ -q` → `366 passed in 23.95s`. **No regressions, no new skips, no change in
count** — expected for a documentation-only delivery.

## Definition of Done — verification

- [x] Execution matrix recorded normatively and **matches the phase spec exactly** — verified by
      `diff`, byte-identical, all five rows and both columns
- [x] **Creation/HQ reconciliation written** — cells describe how the levels run; no opener gains an
      Execution Mode field
- [x] **Locality column marked a pointer**, `model-routing-policy.md` named authoritative on
      divergence
- [x] **E31.1's dispatch-gap statement stands** — quoted verbatim next to the matrix, its own
      paragraph unmodified
- [x] **Mode-is-not-authority stated explicitly**, naming Stage-2 acceptance and merge authorization
      as the acts requiring the human's key
- [x] **Relationship to E35.3 explicit** — one normative home; E35.3's statement is now a citation
- [x] **SN-23 Decision #2 supersession recorded**, agreeing clause by clause with the phase spec's
      footnote
- [x] **`model-routing-policy.md`, `.ai-project.yml`, phase spec untouched** — empty diffs, stated
      above
- [x] **No `version` field, no `## Changelog`** in `chat-hierarchy.md`; inline dated note used
- [x] **Full suite green at 366**, no regressions, no new skips
- [x] **Delivery Notice produced and committed** (this document)
- [x] **Pull request opened to `milestone/M35`** — PR #163

## Acceptance Criteria — verification

- [x] **A reader cannot come away believing agentic mode grants acceptance or merge authority** — the
      rule is its own subsection, the acts are named in bold, and the §11.6 corollary closes the one
      remaining silent route
- [x] **…believing Creation or HQ takes an Execution Mode declaration** — the describes-not-declares
      reconciliation is written adjacent to the rows, and forward-references SN-22's permanent section
- [x] **…believing a Phase or Milestone agentic run can be dispatched today** — E31.1's gap is quoted
      intact, and the record says so in terms: *"No Phase or Milestone agentic run can be dispatched
      today"*
- [x] **A reader looking for inference locality is sent to `model-routing-policy.md`** — the column
      is marked a pointer that loses on divergence
- [x] **A reader can see SN-23 Decision 2 was superseded on one axis by decision**, with the other
      axis intact and the Milestone cell under directed evaluation

## Notes for the parent chat

1. **The corollary in §"Mode is not authority" is the one judgment call in this delivery**, flagged
   above with its reasoning and its status. It is the only text here that is not a transcription or a
   direct restatement of ruled material.
2. **Nothing changed operationally.** The matrix restores a possibility; no starter was rewritten, no
   instance's declared Execution Mode changed, and no dispatcher exists for Phase or Milestone. The
   Epic's own corollary — *"nothing in this repo should run differently the day after it merges"* —
   holds.
3. **P10-GH-8 remains open** and was deliberately not addressed: two more dated inline notes now sit
   in a document with no version field, which is more evidence for the gap rather than less.

## Chat Closure

This Epic Chat (P10-M35-E35.4) has completed execution. Work is committed to
`epic/P10-M35-E35.4`; PR #163 is open to `milestone/M35` and **has not been merged.**

**Next Step:** Parent Milestone Chat (P10-M35) performs Stage-2 review. Acceptance is by silence
unless a Review Decision is issued (PSG §11.6). Merge is human-authorized (SN-19) and will be
performed by this chat only after that authorization — and, if authorization arrives here rather
than via the parent chat's review, only after confirming the bypass is intended.
