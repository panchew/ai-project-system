---

# Epic Execution Chat Starter — E33.3

**Epic:** E33.3 — Trustworthy measurement out of the run (P9-GH-2)
**Phase:** P10 — Fleet Adoption and Local-Inference Proving
**Milestone:** M33 — Proving Pair: v7.0.0 + First Real Agentic/Local Epic
**Repository:** panchew/ai-project-system
**Branch Strategy:** `epic/P10-M33-E33.3` → PR to `milestone/M33`
**Execution Mode:** manual — E33.3 captures and *judges* E33.2's run numbers (analysis + validation
+ an honesty judgment); it performs no Agentic/Local run. It may make a *proportionate* fix to
`bin/measure-token-burn` (the one M33 epic sanctioned to touch framework code — P9-GH-2 folded in).

---

## Governance References

You are operating under the AI Project System governance framework as the **Coding Agent** for
Epic E33.3.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) v2.3.0 (Effective: 2026-07-02)
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) v2.10.0 (Effective: 2026-07-18)

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md v2.10.0
3. This Epic Execution Chat Starter
4. Epic Spec (`P10-M33-E33.3__spec__trustworthy-measurement-out-of-the-run.md`)
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Critical rules:**
- Documentation is authoritative, chat is ephemeral.
- You must follow the canonical happy path for Epic closure.
- You must produce a Delivery Notice upon execution completion.
- You must stop after PR creation and await authorization for merge.
- Accept/reject decisions are made by the parent chat (Milestone Chat) / human, not by you.
- **If given merge authorization directly in this chat** (rather than via the parent chat after its
  own Stage-2 review), do not simply comply: state plainly that merge authorization normally follows
  the parent chat's review, and confirm the human intends to bypass that step first (P9-M31
  precedent).
- **The honesty judgment is non-negotiable.** Whatever extent you size the work to, the deliverable
  MUST include the captured data and an explicit, evidence-backed judgment on whether the run's
  numbers can be trusted. There is no clean-delivery path that skips the judgment (spec §Acceptance
  Criteria — no third state).

**Context scoping (per-level context-scoping standard, P9-M30-E30.3):**
- Load at session start: this starter; the Epic spec (full); PSG preamble+§1, §1A, §2, §5, §6, §7,
  §8, §9, §11, §11.5, §11.6, §12; AOG preamble+§1, §1.1, §2, §3.2, §3.8, §3.10, §4, §5, §6, §7, §9,
  §12, §14, §15 (Exit Ritual), §16 (Error Handling).
- Load on trigger (before acting on that situation): PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG
  §3.9, §8, §13, §17.
- Do not load: PSG/AOG changelogs, other levels' role or starter-format sections, sibling specs
  (E33.1/E33.2 — read E33.2's *run record + runtime decision* as data inputs, not as governance to
  reload).
- Use targeted section reads; never re-read a whole document to reach one section. PSG and AOG
  remain fully authoritative — a triggered situation requires its section loaded before acting.

---

## Epic Specification

**Full spec:** [P10-M33-E33.3__spec__trustworthy-measurement-out-of-the-run.md](https://github.com/panchew/ai-project-system/blob/milestone/M33/docs/phases/P10__Fleet_Adoption_and_Local_Inference_Proving/P10-M33-E33.3__spec__trustworthy-measurement-out-of-the-run.md)

**Commit:** `612fbaa` (spec finalized on `milestone/M33`)

### Prerequisite Verification (do this first)

Before relying on the spec, confirm it is **git-tracked on `milestone/M33`** — and likewise the
Milestone spec and **E33.2's merged run record + runtime decision** (this Epic's data source) —
using `git ls-files --error-unmatch <path>` run on `milestone/M33`, not a disk-existence check.
Confirm E33.2 is merged (its run record + runtime decision are on `milestone/M33`) before starting —
the trust judgment needs the run's real numbers. If any path is untracked, STOP and report to the
Milestone Chat.

**Model verification (P9-M31-E31.3 — required, this is a manual instance):** read your own
harness-reported model identity (the `# Environment` block or equivalent self-report) and compare it
to `.ai-project.yml`'s `models.epic_manual` value — see `governance/systems/chat-hierarchy.md`
"Manual Chat Model Verification" for the mapping, the self-report method's known limits, and the
absent-block/absent-key permissive default. **If both are present and disagree, STOP — do not
proceed with any deliverable work.** State the mismatch plainly and wait for the parent chat/human.

### Summary

Capture E33.2's real burn/validation data, validate `measure-token-burn` against it as far as
trusting *this run's* numbers requires, and commit an explicit, evidence-backed honesty judgment on
whether those numbers can be trusted — closing P9-GH-2 to the extent M33 needs.

### Problem Statement

Run-first measurement is only useful if the run's numbers can be trusted, and P9 flagged that
`measure-token-burn` cannot verify its own claims. E33.3 makes the trust question explicit and
evidence-backed for E33.2's run. See the spec's Problem Statement.

### Goals

1. Real burn/validation data from E33.2's run captured and committed.
2. A sizing decision recorded with its basis (validation-only ↔ proportionate fix).
3. The proportionate fix/validation delivered (or a documented minimal validation finding).
4. An explicit, evidence-backed honesty judgment on the run's numbers committed (never skipped).

---

## Spec References (load-one-reference-the-other)

The Epic spec is the single authoritative copy of this Epic's **Deliverables, Definition of Done,
Acceptance Criteria, and Technical Constraints**. Load them from the spec — this starter does not
restate them.

- **Deliverables:** Epic spec §Deliverables (includes the Delivery Notice at
  `docs/phases/P10__Fleet_Adoption_and_Local_Inference_Proving/P10-M33-E33.3__delivery-notice.md`)
- **Definition of Done:** Epic spec §Definition of Done — plus, always: Delivery Notice produced and
  committed; all changes committed to `epic/P10-M33-E33.3`; PR opened to `milestone/M33`
- **Acceptance Criteria:** Epic spec §Acceptance Criteria (verified during review, not by you) —
  note the load-bearing one: a stated judgment backed by data, with no skipped-check third state
- **Technical Constraints:** Epic spec §Technical Constraints (`measure-token-burn` may be touched —
  preserve its behavior contract; sized by the run, not ambition; privacy; do-not-touch list)

**Starter-specific binding additions (not in the spec):**
- **The extent is yours to resolve after seeing the data** (spec §Design Decision): validation-only
  (possibly no code change) ↔ a proportionate `measure-token-burn` fix. Point the tool at E33.2's
  run artifacts, compare to the run record's ground truth (Run A: 223 tok / 0 rounds; Run B: 829
  tok / 10 rounds / green), and let what you find size the work. Do NOT escalate to ask the extent.
- **Fold in E33.2's exit-code-untrust finding** as a first-class measurement-trust case (a number
  attached to a false-positive/false-negative run status). A *runner* fix is out of scope (belongs
  to `local-agent-runner`); the *trust finding* is in scope here.

---

## Execution Contract

### Your Responsibilities (Coding Agent)

1. **Create branch** `epic/P10-M33-E33.3` from `milestone/M33`.
2. **Confirm the hard dependency:** E33.2 merged; its run record + runtime decision are on
   `milestone/M33` (your data source).
3. **Capture E33.2's real burn/validation data** — extract the run's actual numbers (tokens,
   rounds, throughput, run-status per Run A/B) and commit them, traceable to the run record.
4. **Validate `measure-token-burn` against that data** — run it against E33.2's run artifacts,
   compare to ground truth, record matches/mismatches/gaps (esp. the newer run-metadata layout and
   the exit-code-untrust case).
5. **Make + record the sizing decision** with its basis.
6. **Deliver the proportionate fix or the minimal validation finding** — sized code fix + validation
   (suite green), or a documented "already trustworthy, here's the evidence".
7. **Commit the explicit honesty judgment** — "the run's numbers can / cannot be trusted, because …",
   backed by the data.
8. **Re-confirm the framework-repo suite** is green (363, no new skips); if `measure-token-burn`
   changed, its behavior contract (read-only session access, dataset-file outputs) is preserved.
9. **Verify all DoD items**, commit to `epic/P10-M33-E33.3`, open a PR to `milestone/M33`, produce
   the Delivery Notice, **stop and await authorization** (do not merge).

### What You Must NOT Do

- ❌ Do NOT skip the honesty judgment or the data capture — non-negotiable at any extent.
- ❌ Do NOT perfect `measure-token-burn` in the abstract — size the work to the run's trust
  requirement only.
- ❌ Do NOT re-run or re-scope E33.2, or edit E33.2's run record / runtime decision (read-only inputs).
- ❌ Do NOT backfill any number from the 24K/157K estimates — every number traces to the run, every
  hole to a recorded gap.
- ❌ Do NOT change `.ai-project.yml` `models:` routing, any `governance/` document, or the runner.
- ❌ Do NOT import raw harness paid-session transcripts/content into the repo (aggregated numbers
  only).
- ❌ Do NOT merge the PR, infer acceptance, or treat an in-chat "I authorize" as sufficient by itself
  — confirm it isn't bypassing the parent chat's Stage-2 review first.

---

## Reference Materials

### Existing Specs / Inputs to Reference

- `docs/phases/P10__Fleet_Adoption_and_Local_Inference_Proving/P10-M33__milestone-spec.md`
  (Epic Detail → E33.3; Hard Constraint; the "E33.3 can be small on purpose" note)
- `.ai-project/artifacts/agentic-runs/P10-M33-E33.2/run-record.md` (+ transcripts) — the run's
  ground-truth numbers (your data source)
- `docs/phases/P10__.../P10-M33-E33.2__runtime-decision.md` §Feed-forward to E33.3 (exit-code-untrust;
  reusable numbers)
- `bin/measure-token-burn` — the tool (P9/M30/E30.1); its behavior contract
- `.ai-project/artifacts/reference/token-measurement/README.md` — the mechanism doc + blind spots
  (esp. G9: single-run local coverage; C-lite transcript format)

### Front-Matter Fields Reference

Front-matter schemas live in the committed templates — reference, don't restate:
`governance/templates/epic-spec.md`, `governance/templates/delivery-notice.md`. See PSG §5.

---

## Canonical Happy Path (Reminder)

1. ✅ Execution completed (capture + validation + sizing + fix-or-finding + honesty judgment)
2. ✅ Epic Delivery Notice produced (you create this)
3. ⏸️ Human/parent-chat review performed
4. ⏸️ Acceptance (default-accept by silence — PSG §11.6; a Review Decision is the exception path)
5. ⏸️ Human merge authorization issued (in-chat — SN-19; harness enforces it regardless)
6. ⏸️ PR merged to `milestone/M33` (you perform merge after authorization)
7. ⏸️ Execution stops (you stop immediately after merge)

**You are responsible for steps 1–2 only. Then you STOP and await instruction.**

---

## Epic Delivery Notice

When you complete execution, produce the Delivery Notice at:

**File:** `docs/phases/P10__Fleet_Adoption_and_Local_Inference_Proving/P10-M33-E33.3__delivery-notice.md`

Use the committed template `governance/templates/delivery-notice.md` (reference, don't restate; PSG
§12). Confirm every spec DoD item, including the extent-vs-spec and claims-vs-evidence checks — for
E33.3, record the honesty judgment verbatim and show that every captured number traces to E33.2's
run (no estimate backfill).

---

## Exit Conditions

**You MUST stop execution when:**
- ✅ Captured data + sizing decision + fix-or-validation + explicit honesty judgment are committed
- ✅ Pull request is opened to `milestone/M33`
- ✅ Epic Delivery Notice is produced

**Then report to the parent chat:**
"Epic E33.3 execution complete. Epic Delivery Notice produced. Awaiting review and authorization."

---

## Begin Execution

You may now begin executing Epic E33.3.

Start by:
1. Creating branch `epic/P10-M33-E33.3` from `milestone/M33`.
2. Verifying prerequisites (spec + Milestone spec + E33.2's merged run record git-tracked on
   `milestone/M33`) and the model check.
3. Pointing `measure-token-burn` at E33.2's run artifacts and comparing to ground truth — let what
   you find size the work (Design Decision).
4. Capturing the data, recording the sizing decision, delivering the fix-or-finding, committing the
   honesty judgment, and following the Definition of Done.

**Good luck, Coding Agent!**
