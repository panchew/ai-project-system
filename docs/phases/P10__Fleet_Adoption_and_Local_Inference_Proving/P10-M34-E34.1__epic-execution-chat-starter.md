---

# Epic Execution Chat Starter — E34.1

**Epic:** E34.1 — `ai-project-system-mcp` superseded-agent fix + v7.0.0 (closes P6-GH-15)
**Phase:** P10 — Fleet Adoption and Local-Inference Proving
**Milestone:** M34 — Fleet Roll-forward
**Repository:** panchew/ai-project-system (governance record) · target: `ai-project-system-mcp`
**Branch Strategy:** `epic/P10-M34-E34.1` → PR to `milestone/M34` · target repo:
`chore/framework-v7.0.0-bump`, **committed locally, never pushed**
**Execution Mode:** manual — this is a governance refresh applied to a target repository, not an
Agentic/Local epic. No run occurs. Manual/Paid is correct and is not a posture deviation.

---

## Governance References

You are operating under the AI Project System governance framework as the **Coding Agent** for
Epic E34.1.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) v2.3.0 (Effective: 2026-07-02)
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) v2.10.0 (Effective: 2026-07-18)

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md v2.10.0
3. This Epic Execution Chat Starter
4. Epic Spec (`P10-M34-E34.1__spec__mcp-superseded-agent-fix-and-v7-bump.md`)
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Critical rules:**
- Documentation is authoritative, chat is ephemeral.
- You must follow the canonical happy path for Epic closure.
- You must produce a Delivery Notice upon execution completion.
- You must stop after PR creation and await authorization for merge.
- Accept/reject decisions are made by the parent chat (Milestone Chat) / human, not by you.
- **If given merge authorization directly in this chat** (rather than via the parent chat after its
  own Stage-2 review), do not simply comply: state plainly that merge authorization normally follows
  the parent chat's review, and confirm the human intends to bypass that step first (P9-M31
  precedent).
- **You are operating inside someone else's repository.** The target carries **uncommitted work that
  is not yours** (`registry.yml`, modified). Everything you stage is explicit. `git add -A` in the
  target is the single most damaging thing you can do in this Epic.

**Model verification (P9-M31-E31.3 — required for this manual instance):** read your own
harness-reported model identity (the `# Environment` block or equivalent self-report) and compare it
to **this** repo's `.ai-project.yml` `models.epic_manual` value (`remote:claude-opus-5` as of
2026-07-29) — see `governance/systems/chat-hierarchy.md` "Manual Chat Model Verification". **If both
are present and disagree, STOP** — state the mismatch plainly and wait for the parent chat/human.

> Quoted as **documentation of the live config, never as the source of truth** (HQ Ruling
> 2026-07-28, Decision 4 step 2). Verify against **this** repo's config, not the target's — the
> target is the object of the work, not the authority over your session.

**Context scoping (per-level context-scoping standard, P9-M30-E30.3):**
- Load at session start: this starter; the Epic spec (full); **E33.1's bump procedure in full**
  (`.ai-project/artifacts/reference/v7-bump-procedure/README.md`); PSG preamble+§1, §1A, §2, §5, §6,
  §7, §8, §9, §11, §11.5, §11.6, §12; AOG preamble+§1, §1.1, §2, §3.2, §3.8, §3.10, §4, §5, §6, §7,
  §9, §12, §14, §15 (Exit Ritual), §16 (Error Handling).
- Load on trigger: PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG §3.9, §8, §13, §17.
- Do not load: PSG/AOG changelogs, other levels' role or starter-format sections, sibling specs
  (E34.2/E34.3). Read E33.1's **confirmation evidence** as a shape to match, not as governance.
- Use targeted section reads; never re-read a whole document to reach one section.

---

## Epic Specification

**Spec:** `docs/phases/P10__Fleet_Adoption_and_Local_Inference_Proving/P10-M34-E34.1__spec__mcp-superseded-agent-fix-and-v7-bump.md`

Read it in full. **§Verified target state** is the section that will save you the most time — the
Milestone Chat already read the target repo on 2026-07-29 and found four ways it differs from what
E33.1's procedure assumes. Re-verify them (state can change); do not re-discover them.

### Prerequisite Verification (do this first, record each result)

1. **Model verification** (above) — before anything else.
2. **Epic spec git-tracked** on `milestone/M34`.
3. **This repo's suite baseline** — expect **366 passed / 0 failures / 0 skips**. Record the actual.
4. **Target reachable and enrolled** — `/home/panchew/soft-dev/ai-project-system-mcp` is a git repo;
   `git submodule status` lists a governance submodule; `.ai-project.yml` has a `governance:` block.
5. **Capture the target's pre-state verbatim** — `git status --porcelain`, `git submodule status`,
   `ls .ai-project/agents/`, and the `.ai-project.yml` `governance:` block. **You will need the
   pre-state to prove afterwards that you disturbed nothing you shouldn't have.** Capture it before
   you touch anything.
6. **v7.0.0 tag reachable** — `8044451c74f798239a51d794ebdab85b2567234e`. The target's submodule does
   **not** have the tag locally (Failure Mode 2); plan to fetch it, from the local canonical clone
   if `origin` is unreachable.

Any of 4/6 failing is a **recorded blocker → escalate to the parent chat.** Do not improvise a
partial bump.

### Summary

`ai-project-system-mcp` runs a **placeholder** `hq.agent.md` ("under development in Milestone M8"),
pinned to a **raw commit SHA** eight commits past v5.1.0, with no `framework_version`. You install
the canonical v7.0.0 `governance.agent.md`, remove the placeholder, move the pin to the **v7.0.0
tag** in both places it is recorded, stamp the version, and prove all of it with the adapted
confirmation method — **without committing the owner's uncommitted `registry.yml` work.**

This closes **P6-GH-15**, the oldest live governance defect in the fleet.

---

## Execution Contract

### Your Responsibilities (Coding Agent)

1. **Create branch** `epic/P10-M34-E34.1` from `milestone/M34` (in **this** repo).
2. **Run Prerequisite Verification** and record every result, including the target's verbatim
   pre-state.
3. **Read E33.1's procedure in full**, then apply it to the target **adapted** per the spec:
   - the submodule path is **`governance/`, not `.governance/`** — every procedure command needs the
     substitution;
   - there is **no `governance.ref` key** and `version` holds a SHA — resolve via the spec's
     **Open Decision** (recommendation: add `ref: v7.0.0`), and **state which you chose and why**;
   - the target's tree is **dirty with a modified tracked file** — stage explicitly, always.
4. **Work in the target on `chore/framework-v7.0.0-bump`.** Install canonical
   `governance.agent.md`; `git rm` the placeholder `hq.agent.md`; re-pin the submodule to the v7.0.0
   tag; remove the raw SHA from `.gitmodules`; stamp `framework_version: v7.0.0`.
5. **Stage exactly the bump paths and commit locally. Do NOT push.** Publishing the target branch is
   the CFO's outward action.
6. **Run the adapted confirmation method** (spec §"The confirmation method, adapted") and capture
   **verbatim output**, including `sha256sum` → `66404389f29fa1ff8e829015b70ab8b33373dc3ac3eca3f2faea121d3da3441e`
   and a `git status --porcelain` showing `registry.yml` still modified.
7. **Commit the confirmation evidence** to this repo at
   `docs/phases/P10__Fleet_Adoption_and_Local_Inference_Proving/P10-M34-E34.1__confirmation-evidence.md`,
   citing the target repo and its commit SHA. Match E33.1's confirmation-evidence artifact shape.
8. **Record the four drifts** — preferably by amending E33.1's procedure (new failure mode for
   submodule-path variance; extend Failure Mode 6 to modified tracked files). If you judge that out
   of bounds, put them in the confirmation evidence and say plainly the procedure still carries the
   gap. **Do not silently work around them.**
9. **Re-run this repo's suite** — 366, no regressions, no new skips.
10. **Verify all DoD items**, commit to `epic/P10-M34-E34.1`, open a PR to `milestone/M34`, produce
    the Delivery Notice, **stop and await authorization** (do not merge).

### What You Must NOT Do

- ❌ **Do NOT run `git add -A` in the target.** `registry.yml` is the owner's uncommitted work and
  must still be modified when you finish. A clean target tree means you committed someone else's
  changes.
- ❌ Do NOT touch `.ai-project/artifacts/` in the target (untracked; Failure Mode 6).
- ❌ Do NOT **push or merge** the target branch. The CFO publishes it.
- ❌ Do NOT use `bin/ai-project-init` to refresh — it creates, it does not update (Failure Mode 1);
  it will abort on the existing submodule or clobber the hand-tuned `.ai-project.yml`.
- ❌ Do NOT template-replace the target's `.ai-project.yml` — edit field-by-field (Failure Mode 7).
- ❌ Do NOT trust `git`/`cp` exit 0 as proof the agent landed. **The checksum is the evidence.**
- ❌ Do NOT report P6-GH-15 closed on a partial fix, and do NOT stamp a project you did not actually
  refresh (E33.1 Hard Constraint). A target you cannot refresh is a **recorded blocker → escalate**.
- ❌ Do NOT bump any other project (E34.2's territory), run any project work or Agentic/Local run in
  the target, or schema-bless `framework_version` (P10-GH-1 — record, don't fix).
- ❌ Do NOT merge the PR, infer acceptance, or treat an in-chat "I authorize" as sufficient by itself
  — confirm it isn't bypassing the parent chat's Stage-2 review first.

---

## Reference Materials

- `.ai-project/artifacts/reference/v7-bump-procedure/README.md` — **the lever.** Steps 1–5, the
  stamp location, the confirmation method, and Failure Modes 1–7. Failure Modes **3** (out-of-band
  agent copy) and **5** (legacy superseded-agent check) are the load-bearing ones here; **2**
  (tag not pre-fetched) will bite you at Step 1; **6** and **7** apply with the spec's extensions.
- `docs/phases/.../P10-M33-E33.1__confirmation-evidence.md` — the evidence artifact shape to match.
- `docs/phases/.../P10-M33-E33.1__spec__v7-bump-procedure-and-apply-to-pair.md` — E33.1's own Hard
  Constraint on blockers-vs-guessed-stamps.

---

## Canonical Happy Path (Reminder)

1. ✅ Execution completed (target bumped locally + confirmation evidence + drift record)
2. ✅ Epic Delivery Notice produced (you create this)
3. ⏸️ Human/parent-chat review performed
4. ⏸️ Acceptance (default-accept by silence — PSG §11.6; a Review Decision is the exception path)
5. ⏸️ Human merge authorization issued (in-chat — SN-19; harness enforces it regardless)
6. ⏸️ PR merged to `milestone/M34` (you perform merge after authorization)
7. ⏸️ Execution stops (you stop immediately after merge)

**You are responsible for steps 1–2 only. Then you STOP and await instruction.**

---

## Epic Delivery Notice

When you complete execution, produce the Delivery Notice at:

**File:** `docs/phases/P10__Fleet_Adoption_and_Local_Inference_Proving/P10-M34-E34.1__delivery-notice.md`

Use the committed template `governance/templates/delivery-notice.md` (reference, don't restate; PSG
§12). Confirm every spec DoD item. For E34.1 specifically, the Delivery Notice must show:
- the target's **pre-state and post-state** side by side;
- the **verbatim confirmation output**, checksum included;
- **proof the owner's uncommitted work survived** (`git status --porcelain` before and after);
- the **Open Decision** you took on `ref:`, with your reasoning;
- where the **four drifts** were recorded, and whether the procedure now carries the fix or the gap.

---

## Exit Conditions

**You MUST stop execution when:**
- ✅ The target is bumped on a local `chore/framework-v7.0.0-bump` branch (unpushed), P6-GH-15 closed
- ✅ The adapted confirmation method passes with verbatim output captured
- ✅ Confirmation evidence + drift record committed to this repo; suite green at 366
- ✅ Pull request is opened to `milestone/M34`
- ✅ Epic Delivery Notice is produced

**Then report to the parent chat:**
"Epic E34.1 execution complete. Epic Delivery Notice produced. Awaiting review and authorization."

---

## Begin Execution

You may now begin executing Epic E34.1.
