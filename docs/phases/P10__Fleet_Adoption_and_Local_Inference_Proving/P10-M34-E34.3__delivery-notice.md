---
artifact_type: delivery_notice
artifact_version: 1.0
timestamp: 2026-07-28T22:40:00Z
issuer_chat: Epic Agent (P10-M34-E34.3)
issuer_role: Epic Agent
status: delivered
epic_id: P10-M34-E34.3
milestone_id: P10-M34
phase_id: P10
project_name: ai-project-system
merge_details:
  pr_number: 156
  pr_url: https://github.com/panchew/ai-project-system/pull/156
  merge_commit: <pending — human-authorized merge, not yet performed>
  merge_timestamp: <pending>
  merge_strategy: merge
  target_branch: milestone/M34
duration:
  start_date: 2026-07-28
  end_date: 2026-07-28
  elapsed_days: 0
final_artifacts:
  - name: Agentic epic-lane routing change (models.epic_dev / models.epic_qa)
    path: .ai-project.yml
    type: implementation
  - name: Orchestrator no-config fallback (DEFAULT_MODELS)
    path: bin/ai-project-orchestrator
    type: implementation
  - name: Policy rows P6/P7 + mapping rows + row-vs-mapping distinction note
    path: .ai-project/artifacts/reference/token-measurement/model-routing-policy.md
    type: spec
  - name: .ai-project.yml Specification v2.7.0 (value/documentation refresh + sizing note)
    path: governance/ai-project-yml-spec.md
    type: spec
  - name: models block divergence guard (expected value + docstring)
    path: tests/test_model_config.py
    type: implementation
  - name: E34.3 Carry-Forward Note
    path: docs/phases/P10__Fleet_Adoption_and_Local_Inference_Proving/P10-M34-E34.3__carry-forward-note.md
    type: report
  - name: E34.3 Delivery Notice
    path: docs/phases/P10__Fleet_Adoption_and_Local_Inference_Proving/P10-M34-E34.3__delivery-notice.md
    type: report
completion_notice_timestamp: 2026-07-28T22:40:00Z
review_decision_timestamp: <pending — parent Milestone Chat Stage-2>
---

# Delivery Notice: P10-M34-E34.3 — Apply the settled runtime choice (`models:` routing edit)

## Summary

The routing edit M33 settled and deliberately deferred is made. `models.epic_dev` / `models.epic_qa`
move `local:qwen2.5-coder:14b` → **`local:qwen3-coder:30b`** across the full coupled surface set,
with the change traced to the runs that settled it.

**This Epic applied an already-ratified decision.** No new decision, no new capability, no new
schema, and — per its own Execution Mode — **no Agentic/Local run**. It is the Epic that makes the
*next* run safe.

Suite green at **366 passed / 0 failures / 0 skips**, identical to baseline. The divergence guard in
`tests/test_model_config.py` — the real acceptance gate — passes on the coupled change, which is the
evidence the application was complete rather than partial.

Commit `a80033e` on `epic/P10-M34-E34.3`; PR [#156](https://github.com/panchew/ai-project-system/pull/156)
to `milestone/M34`.

---

## Prerequisite Verification (recorded, as instructed)

| # | Check | Result |
|---|---|---|
| 0 | **Model verification** (E31.3 guardrail) — harness self-report vs `.ai-project.yml` `models.epic_manual` | Harness reports **`claude-opus-5`**; config `epic_manual` = **`remote:claude-opus-5`**. **Match** — proceeded. `epic_manual` was verified **before** any edit and was never touched. |
| 1 | Epic spec git-tracked on `milestone/M34` | ✅ `git ls-files --error-unmatch` returned the path. |
| 2 | **Baseline suite before touching anything** | ✅ **366 passed / 0 failures / 0 skips** — matches the expected figure exactly. No adjusted baseline adopted. |
| 3 | **Model tag is real, not plausible** — `curl -s http://localhost:11434/api/tags` | ✅ `qwen3-coder:30b` present: **30.5B params, Q4_K_M, 18.6 GB**. Matches the spec's planning-time capture exactly. (Also present: `qwen3.6:27b` at Q4_K_M — confirmed *not* the parked Q8_0 stack, and deliberately not touched.) |
| 4 | Cited evidence present on the branch | ✅ `P10-M33-E33.2__runtime-decision.md`, `agentic-runs/P10-M33-E33.2/run-record.md`, `agentic-runs/P10-M33-E33.4/run-record.md` — all three git-tracked. |

No blocker arose. No escalation was required.

---

## Surface classification — every hit, live vs. historical

The spec's line numbers were treated as hints, not addresses: the string was re-grepped repo-wide.
**79 tracked files** contained `qwen2.5-coder:14b` at the `milestone/M34` baseline. **Five are live
surfaces; 74 are historical record or non-policy fixtures.**

### LIVE — edited (5 files)

| File | What moved | Why it is live |
|---|---|---|
| `.ai-project.yml` | `models.epic_dev`, `models.epic_qa`; provenance comment added; spec pointer → v2.7.0 | The configuration `bin/ai-project-orchestrator` reads at dispatch. The authority the E31.3 guardrail reads. |
| `bin/ai-project-orchestrator` | `DEFAULT_MODELS["epic_dev"]`, `DEFAULT_MODELS["epic_qa"]` | **The no-config fallback.** A project with no `models:` block — or a yml that fails to parse — falls through to this constant. This is what the Milestone acceptance criterion's *"no longer **defaults** to"* actually reaches; leaving it would have handed the falsified model to every unconfigured adopter. |
| `.ai-project/artifacts/reference/token-measurement/model-routing-policy.md` | **Rows P6, P7** (Decision + why + gaps + revisit-trigger columns) and their two "Mapping to `.ai-project.yml`" rows; plus a new note distinguishing this row change from the same-day HQ mapping refresh | The recorded policy. Rows P6/P7 name the model in their own Decision column, so this is a **row change** and the file's own Change discipline governs it. |
| `governance/ai-project-yml-spec.md` | §3.1 schema comments; §3.4 field-table defaults **and** examples; format-constraint local example; new agentic-lane sizing note; **v2.6.0 → v2.7.0** + changelog entry; "Introduced In" line | The adopter-facing contract. Its default rows *define* the default for adopting repositories. |
| `tests/test_model_config.py` | `EXPECTED_EPIC_DEV`; module docstring; one test renamed | The divergence guard. Its expected value is a live assertion, not a record. |

### HISTORICAL — deliberately left alone (74 files)

Rewriting any of these would falsify the record. **Precedent:** the 2026-07-28 HQ Ruling moved `hq`
to `claude-opus-5` and left `audit-report.md`'s `claude-opus-4-8` standing — *"Historical evidence
(audit-report, dataset, P9 artifacts) is not retconned."* The same rule is applied here.

| Bucket | Count | Reasoning |
|---|---|---|
| `docs/phases/P7__.../` — specs, starters, delivery notices, run record, escalation notices | 15 | P7-M26 is where `epic_dev` *moved to* the 14b (E26.2) and first ran on it. These documents record what was true then. |
| `docs/phases/P9__.../` — specs, starters, delivery notices, run records, closure declaration | 17 | P9-M30/M31 derived the policy and ran the dual-mode work on the 14b. Recorded history. |
| `docs/phases/P10__.../` — M33 specs/starters/delivery notices, M34 milestone spec, **this Epic's own spec and starter** | 10 | These name the 14b as *the thing being removed*. Editing them would erase the epic's own rationale. |
| `.ai-project/artifacts/agentic-runs/**` — `context.md`, transcripts, run-metadata, run records | 13 | Captured run evidence. The single most damaging category to rewrite — these are the cited evidence for this very change. |
| `.ai-project/artifacts/closure-declarations/**` | 4 | Signed, timestamped closure records (P7-M26, P9-M30, P9-M31, P10-M33). |
| `.ai-project/artifacts/reference/token-measurement/**` (excl. the policy) — `audit-report.md`, `e33-2-run-measurement-trust.md`, both `token-burn-dataset.{md,json}` pairs | 6 | Dated measurement evidence with a stated window. `audit-report.md` is the direct HQ precedent above. |
| `.ai-project/artifacts/rulings/`, `steering-notes/`, `escalation-notices/`, `hq-openers/` | 6 | Issued governance instruments. Immutable once filed. |
| `tests/test_measure_token_burn_local.py` | 1 | P7-era **and** E33.2 Run A fixtures that replicate actual captured transcripts. Changing them would make the tests assert against runs that never happened. |
| `tests/test_ai_project_version_script.py` | 1 | Docstring narrating the P7-M26-E26.3 live run. History, not a definition. |
| `tests/integration/test_run_dev_agent_adapter.py` | 1 | **Judgment call, stated as instructed:** `STUB_MODEL_TAG` and the inline tags are arbitrary strings in adapter plumbing tests against a stub server. **Left unchanged** — nothing breaks, and per the spec they carry no policy meaning either way. |

**Residual-string note (so the DoD grep is not misread):** `grep -n 'qwen2.5-coder:14b' .ai-project.yml`
still returns **one** hit — line 37, inside the *provenance comment* recording the change. The
`models:` **block itself is clean**, which is what the DoD specifies. This mirrors the HQ Ruling
precedent exactly, which left `claude-opus-4-8` named in the same comment block as change
provenance. Likewise the policy rows, the yml spec, and the guard's docstring name the old value in
prose *because* they explain the move.

---

## Before / after suite figures

| | Result |
|---|---|
| **Before** (baseline on `milestone/M34`, before any edit) | **366 passed, 0 failures, 0 skips** — 24.71s |
| **After** (full suite, `epic/P10-M34-E34.3`) | **366 passed, 0 failures, 0 skips** — 24.77s |

No regressions. No new skips. No test relaxed, skipped, or rewritten to accommodate the change —
`tests/test_model_config.py` was edited only to move its **expected value**, which is the guard's
input, not its logic. Every divergence assertion (`config ↔ DEFAULT_MODELS`, `policy ↔ config`,
`policy ↔ DEFAULT_MODELS`, across all five keys) passes unmodified on the new value, which is the
positive evidence that the application was **coupled and complete** rather than partial.

---

## P7 honesty statement (required)

**Gap G11 remains open. No QA-lane evidence is claimed by this Epic.**

`epic_qa` changed value, and that is the claim most easily over-read from this diff. The runner
dispatches dev and QA from a single model in practice — E33.2's run record states it plainly:
*"Run A is the configured `epic_dev`/`epic_qa` model"* — so **M33 produced no isolated QA-role run**.
E33.2 and E33.4 are **dev-lane** evidence.

Row P7 therefore did **not** move on new QA-lane evidence. It moved on **exactly the reasoning it
already carried** — *gap-grounded interim default: the one local model with any run evidence*
(audit report §7.2 standard) — with its referent updated from the falsified `qwen2.5-coder:14b` to
`qwen3-coder:30b`. **G11 was not dropped from P7's gap column**; it is restated there as open, and
P7's revisit trigger remains its own unfired condition: *the first isolated QA-role run*.

P7's recorded revisit trigger (*"First M31 QA-role run"*) **did not fire**, and the row records that
it did not.

---

## Definition of Done — verification

| DoD item | Status |
|---|---|
| `grep -n 'qwen2.5-coder:14b' .ai-project.yml` returns nothing **in the `models:` block** | ✅ Block clean. One hit remains in the provenance comment (line 37) — see residual-string note above; HQ-precedent-consistent. |
| `models.epic_dev` / `epic_qa` = confirmed 30B-class tag, verified against `/api/tags` | ✅ `local:qwen3-coder:30b`; verified live, not assumed. |
| `DEFAULT_MODELS` no longer defaults either epic lane to the 14b | ✅ Both moved; the string no longer appears anywhere in `bin/ai-project-orchestrator`. |
| Policy rows **P6 and P7** updated with cited evidence; **G11 shown open** on P7; rows P1–P5 untouched | ✅ Both rows rewritten with citations; G11 explicit; P1–P5 byte-identical (verify in the diff). |
| `governance/ai-project-yml-spec.md` version bumped + changelog entry describing a value/documentation refresh | ✅ **v2.6.0 → v2.7.0**; entry states plainly that no field, key, or validation rule changed. |
| Change cites E33.2's runtime decision and the E33.2/E33.4 run records | ✅ In the commit message, the PR body, policy row P6, the yml-spec changelog, the `.ai-project.yml` comment, and the carry-forward note. |
| Loadability envelope recorded with the change | ✅ 18.6 GB Q4_K_M exceeds 16 GB VRAM; partial RAM offload (12.9 GB VRAM / 21.4 GB total); ~9.4 tok/s vs the 14b's 12.2 — recorded in five places, framed as the ratified trade. |
| No historical record rewritten | ✅ 74 historical files untouched — see classification. Diff touches exactly 6 files (5 live + 1 new note). |
| **Full suite green — 366, no regressions, no new skips** | ✅ 366/0/0 before and after. |

## Acceptance Criteria

1. **Dispatch no longer routes to the falsified model — from config *and* from the no-config default
   path.** ✅ Both surfaces moved; the second is why the orchestrator constant was in scope.
2. **A reader can get from the edited value back to the runs without asking anyone.** ✅ Every edited
   surface carries the citation inline.
3. **Rows P6/P7 satisfy Change discipline on their face.** ✅ P6: new cited evidence with the
   falsification stated (the row's own prior value produced zero work on a real epic). P7: honest
   gap-grounded interim with G11 open. Both carry **fresh** revisit triggers — not the ones this
   change consumed.
4. **Suite green at 366 with no new skips.** ✅

---

## Judgment calls made, stated for review

1. **Format-constraint example (`ai-project-yml-spec.md`) updated** although it explicitly disclaims
   being a default. The starter made this conditional on the line asserting a default — it does not.
   Updated anyway, because changelog **2.2.0 and 2.4.0 both moved this example in lockstep** with an
   epic-lane value change, and leaving the falsified model as the lead local example invites adopters
   to copy it. Easily reverted if the Milestone Chat disagrees.
2. **`qwen2.5-coder:14b` was NOT added to `test_model_config.py`'s `FALSIFIED_NAMES`.** Considered
   and declined. That list asserts a **global** prohibition across all seven keys — a stronger claim
   than the policy rows this Epic is authorized to move. The 14b is a working, tool-calling model
   that proved *under-powered for agentic epic work on this stack* (SN-3 markdown-plan mode), which
   is not the same as `llama3:8b`'s contract-verified tool-call incapacity. Adding it would have been
   deciding, and *this Epic applies; it does not decide.* **Flagged for the Milestone Chat** as a
   possible follow-on hardening.
3. **`model-routing-policy.md` frontmatter left unchanged** (`last_updated: 2026-07-17`, P9-M30-E30.2
   provenance) — following the HQ Ruling's precedent on the same file eight days later, which also
   left it untouched. The file's provenance records authorship, not last edit.
4. **A distinguishing note was added to the policy** contrasting the two 2026-07-28 changes
   (mapping refresh vs. row change). Not strictly required, but the spec's Note 2 warns these will be
   conflated — the file now defends itself.

---

## Not touched, deliberately

The five **paid-frontier** keys (HQ: *"`epic_dev`/`epic_qa` are untouched … Do not conflate the
two"*) · policy rows **P1–P5** · the **runtime** question (Ollama stands) · **`qwen3.6:27b`**
(present on the host at Q4_K_M — *not* the parked Q8_0 reference stack, and not this Epic's
business) · **`framework_version`** schema-blessing (**P10-GH-1** — recorded, not fixed) · any
routing relocation into openers or per-level-per-project mechanism (HQ Decision 4 declined it;
Drivr's domain in P11) · any target repository (E34.1/E34.2 territory) · any Agentic/Local run.

---

## Carry-forward

- **DISCHARGED:** the M33 Closure Declaration's *"M34 should make the routing change"*.
- **STILL RECORDED, NOT FIXED:** **P10-GH-1** (`framework_version` unschema'd) — carries to the P10
  phase closure declaration alongside P10-GH-2 and P10-GH-3.
- **Open gaps unchanged by this Epic:** **G11** (zero captured QA-role runs), **G12** (no quality
  signal), **G9** (local input tokens unmeasured), and exit-code untrust on this stack.

Full reasoning: [`P10-M34-E34.3__carry-forward-note.md`](P10-M34-E34.3__carry-forward-note.md) —
which records that discharging a carry-forward is not the same as closing a gap.

---

## Sequencing reminder for the parent chat

E34.3 must **land before any E34.2 agentic run**. Until this PR merges, `milestone/M34` still routes
agentic epic dispatch to the model E33.2 proved emits exit 0 with zero work — the exact trap this
Epic exists to close.

---

## Chat Closure

Epic E34.3 execution is complete through **step 2 of the canonical happy path** (execution +
Delivery Notice). The PR is **open and unmerged**.

⏸️ **Awaiting Stage-2 review by the parent Milestone Chat (P10-M34), acceptance, and human merge
authorization.** Per the Epic Execution Chat Starter, an in-chat *"I authorize"* is not treated as
sufficient by itself — if merge authorization arrives here rather than via the parent chat's own
Stage-2 review, this chat will say so and confirm the bypass is intended before acting (P9-M31
precedent).

**Next Step:** Parent Milestone Chat performs Stage-2 review.
