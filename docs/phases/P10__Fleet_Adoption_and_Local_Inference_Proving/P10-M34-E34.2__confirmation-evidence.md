---
project: ai-project-system
phase: P10
milestone: M34
epic: E34.2
type: confirmation-evidence
status: complete
last_updated: 2026-07-29
---

# E34.2 — Confirmation Evidence

Verbatim output from the three target repositories, captured at
`/home/panchew/soft-dev/{Getawayinsured2023,footboard,courtis}` on 2026-07-29. Every "bumped" /
"stamped" claim in the delivery notice and the roadmap resolves to a block in this file.

**Exit-code untrust applies throughout.** `git` and `cp` returning 0 prove nothing about what landed.
The evidence is the **checksum**, the **submodule SHA**, the **grep of the stamped file**, and the
**before/after porcelain**.

Canonical constants, independently re-verified against this repo's `v7.0.0` tag:

```
$ git rev-parse 'v7.0.0^{commit}'
8044451c74f798239a51d794ebdab85b2567234e
$ git show v7.0.0:governance/agents/governance.agent.md | sha256sum
66404389f29fa1ff8e829015b70ab8b33373dc3ac3eca3f2faea121d3da3441e  -
$ git show v7.0.0:governance/agents/governance.agent.md | wc -c
14711
```

---

## Prerequisite verification

| # | Check | Result |
|---|---|---|
| 1 | Model verification (E31.3) | **PASS** — harness identity `claude-opus-5` == `.ai-project.yml` `models.epic_manual: remote:claude-opus-5` |
| 2 | Epic spec git-tracked on the M34 milestone branch | **PASS** |
| 3 | This repo's suite baseline | **FAIL as specified — 365 passed / 1 failed / 0 skipped, not 366/0/0.** See below. |
| 4 | Per-target verbatim pre-state | **captured** — all three, below |
| 5 | v7.0.0 tag fetchable | **PASS**; FM2 confirmed live on `footboard` and `courtis` (tag absent locally until fetched) |

### Check 3 — the stated baseline was falsified, and the cause was the Epic's own starter

The spec and starter both give the baseline as **366 / 0 / 0**. Actual, on `milestone/M34` before any
work:

```
E           Failed: Milestone branch-name typo(s) detected in Epic starters:
E             docs/phases/P10__Fleet_Adoption_and_Local_Inference_Proving/P10-M34-E34.2__epic-execution-chat-starter.md:111 -> milestone/M1 (not a known milestone)
E           Fix the milestone number (likely a stray extra digit).

tests/test_starter_lint.py:130: Failed
=========================== short test summary info ============================
FAILED tests/test_starter_lint.py::test_no_branch_name_typos_in_starters
1 failed, 365 passed in 23.90s
```

**The failure pre-dated this Epic's work** — proven by the epic branch's tree being byte-identical to
`milestone/M34` at the time (`git diff --stat milestone/M34` → empty). It was introduced by commit
`973a7f5`, which added the E34.2 starter.

**Root cause is a false positive in the guard, not a typo.** `tests/test_starter_lint.py` derives the
set of real milestones **only from Epic-starter filenames** (`known_milestones()` /
`FILENAME_RE`). This repo has no `P#-M1-E#.#__epic-execution-chat-starter.md` file, so **M1 is
invisible to the guard even though M1 is a real milestone of this repo** —
`docs/phases/P1__.../P1-M1-E1.1__spec__project-tracker-integration-system.md` and its completion
record both exist. The starter's line 111 was additionally referring to **`footboard`'s** branch, not
this repo's, which the guard has no notion of either.

**Action taken: recorded, not fixed.** Changing the guard is a framework-repo (capability) change,
which FM4 of the bump procedure already establishes is **out of scope for adoption epics**. So the
starter's wording was adjusted to avoid emitting the literal token (the branch in `footboard` is
unchanged; only the sentence's phrasing is), and the underlying defect is recorded as **P10-GH-6** —
see `P10-M34__carry-forward-note__P10-GH-6-starter-lint-false-positive.md`.

Suite after that wording change:

```
366 passed in 23.91s
```

---

## `Getawayinsured2023` — stamp only

### Pre-state (verbatim, before anything was touched)

```
$ git rev-parse --abbrev-ref HEAD
phase/P1
$ git log --oneline -1
4a56b81 P1-M2/M3: add milestone specs and execution chat starters
$ git status --porcelain
$ git submodule status
 8044451c74f798239a51d794ebdab85b2567234e .governance (v7.0.0)
$ cat .gitmodules
[submodule ".governance"]
	path = .governance
	url = https://github.com/panchew/ai-project-system
$ ls .ai-project/agents/
governance.agent.md
tools.json
```

`.ai-project.yml` `governance:` block pre-state:

```yaml
governance:
  source: https://github.com/panchew/ai-project-system
  version: "7.0.0"
  ref: v7.0.0
```

Working tree **clean** — nothing of the owner's to preserve here.

### Step 0 and the FM3 agent-freshness check

```
$ SUB="$(git config -f .gitmodules --get-regexp '^submodule\..*\.path$' | awk '{print $2}')"
SUB=[.governance]
guard: OK (single path, exists)

$ git submodule status .governance
 8044451c74f798239a51d794ebdab85b2567234e .governance (v7.0.0)
$ git -C .governance describe --tags
v7.0.0

$ sha256sum .ai-project/agents/governance.agent.md
66404389f29fa1ff8e829015b70ab8b33373dc3ac3eca3f2faea121d3da3441e  .ai-project/agents/governance.agent.md
expected: 66404389f29fa1ff8e829015b70ab8b33373dc3ac3eca3f2faea121d3da3441e
```

**FM3 re-run: PASS.** The installed out-of-band agent copy is byte-identical to canonical v7.0.0
(14711 bytes). This is the **only fleet project where FM3 does not bite**; recorded rather than
skipped, because it is the exception to a rule that held everywhere else.

Steps 1–3 were therefore **no-ops by verification, not by assumption**. Only the stamp was needed.

### `models:` block byte-unchanged — two independent proofs

```
=== models block sha, before and after ===
pre : 934bb8078ca2fcde829fe5de21e50fa0866249932a82cdf5500ff24a8897f3d6
post: 934bb8078ca2fcde829fe5de21e50fa0866249932a82cdf5500ff24a8897f3d6
MODELS BLOCK UNCHANGED

=== whole-file suffix check ===
tail identical to pre-state: True
bytes added: 269
```

The stamp was written by prepending 269 bytes to the file, so the **entire** pre-existing file —
`models:` block included — survives as a byte-exact suffix. `Getawayinsured2023`'s SN-2 routing was
not edited, reordered, or reformatted.

### Commit

```
$ git status --porcelain      # pre-commit — ONLY the stamped file
 M .ai-project.yml
$ git diff --cached --name-only
.ai-project.yml
$ git rev-parse HEAD
c28247532dae26e359abec1aa7c2ad9f0e93bb4d
```

Base branch: **`main`** (parent commit `e6c5ff3`).

### Confirmation method — all checks pass

```
$ grep '^framework_version:' .ai-project.yml
framework_version: v7.0.0
$ grep -E '^  (version|ref):' .ai-project.yml
  version: "7.0.0"
  ref: v7.0.0
$ git submodule status .governance
 8044451c74f798239a51d794ebdab85b2567234e .governance (v7.0.0)
$ sha256sum .ai-project/agents/governance.agent.md
66404389f29fa1ff8e829015b70ab8b33373dc3ac3eca3f2faea121d3da3441e  .ai-project/agents/governance.agent.md
$ ls .ai-project/agents/
governance.agent.md
tools.json
$ git log --oneline -1
c282475 chore(governance): stamp framework v7.0.0
$ git status -sb | head -1
## chore/framework-v7.0.0-bump
$ git status --porcelain
(end — empty, matching the clean pre-state)
```

Not pushed: no upstream configured, and `git ls-remote --heads origin chore/framework-v7.0.0-bump`
finds nothing.

---

## `footboard` — agent install + 2-major bump + stamp

### Pre-state (verbatim)

```
$ git rev-parse --abbrev-ref HEAD
milestone/M1
$ git log --oneline -1
51b5cc6 P1: Create milestone specs and execution chat starters
$ git status --porcelain
 M docs/phases/P1__Online_1v1_MVP/P1__phase-spec.md
 M genesis.md
?? .ai-project/artifacts/hq-openers/
?? .ai-project/artifacts/phase-starters/
?? .ai-project/artifacts/system-requests/
?? .ai-project/artifacts/system-responses/
?? .ai-project/seed.md
?? docs/phase-execution-chat-starter.md
?? docs/phases/P1__Online_1v1_MVP/P1-M1-E1.1-epic-execution-chat-starter.md
?? docs/phases/P1__Online_1v1_MVP/P1-M1-E1.1__spec__WebSocket_Service_Implementation.md
?? docs/phases/P1__Online_1v1_MVP/P1-M1-E1.2-epic-execution-chat-starter.md
?? docs/phases/P1__Online_1v1_MVP/P1-M1-E1.2__spec__Rooms_Manager.md
?? docs/phases/P1__Online_1v1_MVP/P1-M1-E1.3-epic-execution-chat-starter.md
?? docs/phases/P1__Online_1v1_MVP/P1-M1-E1.3__spec__Engine_Integration.md
?? docs/phases/P1__Online_1v1_MVP/P1-M1-E1.4-epic-execution-chat-starter.md
?? docs/phases/P1__Online_1v1_MVP/P1-M1-E1.4__spec__Clock_Management_System.md
?? docs/phases/P1__Online_1v1_MVP/P1-M1-E1.5-epic-execution-chat-starter.md
?? docs/phases/P1__Online_1v1_MVP/P1-M1-E1.5__spec__Match_Persistence.md
?? docs/phases/P1__Online_1v1_MVP/P1-M1-E1.6-epic-execution-chat-starter.md
?? docs/phases/P1__Online_1v1_MVP/P1-M1-E1.6__spec__Connection_Handling.md
$ git submodule status
 a59509f45b410f34dbfa617ede554bcc3b15de41 .governance (v5.1.0)
$ cat .gitmodules
[submodule ".governance"]
	path = .governance
	url = https://github.com/panchew/ai-project-system
$ ls .ai-project/agents/
ls: cannot access '.ai-project/agents/': No such file or directory
```

### FM11 — the recommended branch base is unreachable (demonstrated, not asserted)

```
$ git checkout main
error: Your local changes to the following files would be overwritten by checkout:
	docs/phases/P1__Online_1v1_MVP/P1__phase-spec.md
Please commit your changes or stash them before you switch branches.
Aborting
(exit code: 1)
```

The file is modified-tracked and **absent on `main`**:

```
docs/phases/P1__Online_1v1_MVP/P1__phase-spec.md -> ABSENT on main
genesis.md -> EXISTS on main
```

Git aborted without changing anything — the porcelain immediately afterwards was identical to the
pre-state above, and HEAD was still on the milestone branch. Reaching `main` would require committing
or stashing the owner's work, both forbidden by this Epic, so the bump was based on the **in-flight
milestone branch**. Recorded as **FM11**.

### Step 0, FM2, re-pin, agent install

```
$ SUB=[.governance]   (guard OK)

$ git checkout -b chore/framework-v7.0.0-bump
Switched to a new branch 'chore/framework-v7.0.0-bump'

$ git -C .governance rev-parse -q --verify 'v7.0.0^{commit}'
ABSENT (FM2 confirmed)
$ git -C .governance fetch --tags origin
 * [new tag]         v6.0.0                -> v6.0.0
 * [new tag]         v6.0.1                -> v6.0.1
 * [new tag]         v7.0.0                -> v7.0.0
$ git -C .governance rev-parse -q --verify 'v7.0.0^{commit}'
8044451c74f798239a51d794ebdab85b2567234e

$ git -C .governance checkout --quiet v7.0.0
$ git -C .governance describe --tags
v7.0.0

$ ls .ai-project/agents/
(confirmed absent before creation)
$ mkdir -p .ai-project/agents
$ cp -f .governance/governance/agents/governance.agent.md .ai-project/agents/governance.agent.md
$ sha256sum .ai-project/agents/governance.agent.md
66404389f29fa1ff8e829015b70ab8b33373dc3ac3eca3f2faea121d3da3441e  .ai-project/agents/governance.agent.md
$ wc -c .ai-project/agents/governance.agent.md
14711 .ai-project/agents/governance.agent.md

FM5 (superseded hq.agent.md): absent — does not apply
FM9 (second .gitmodules pin):  no branch line — does not apply
```

### Config edit — field-targeted, exactly 3 changes (FM7)

```diff
--- fb-pre.yml
+++ .ai-project.yml
@@ -1,7 +1,13 @@
+# framework_version: adoption stamp — the AI Project System release this project has
+# adopted. Set by the v7.0.0 bump procedure (P10-M33-E33.1), applied here by P10-M34-E34.2.
+# Confirm with:
+#   grep '^framework_version:' .ai-project.yml
+framework_version: v7.0.0
+
 governance:
   source: https://github.com/panchew/ai-project-system
-  version: "5.1.0"
-  ref: v5.1.0
+  version: "7.0.0"
+  ref: v7.0.0
 project:
   name: footboard
   description: "Turn-based tactical soccer board game — online 1v1 platform"
```

`project.description` untouched. No template replacement.

### Commit — exactly 3 paths staged

```
$ git diff --cached --name-only
.ai-project.yml
.ai-project/agents/governance.agent.md
.governance
$ git rev-parse HEAD
b00bb1654d39e8a5384057cac1641dbc2ddc72a0
```

Parent commit `51b5cc6` (the milestone branch's HEAD).

### Confirmation method — all checks pass

```
$ grep '^framework_version:' .ai-project.yml
framework_version: v7.0.0
$ grep -E '^  (version|ref):' .ai-project.yml
  version: "7.0.0"
  ref: v7.0.0
$ git submodule status .governance
 8044451c74f798239a51d794ebdab85b2567234e .governance (v7.0.0)
$ sha256sum .ai-project/agents/governance.agent.md
66404389f29fa1ff8e829015b70ab8b33373dc3ac3eca3f2faea121d3da3441e  .ai-project/agents/governance.agent.md
$ ls .ai-project/agents/
governance.agent.md
$ git log --oneline -1
b00bb16 chore(governance): bump to framework v7.0.0
$ git status -sb | head -1
## chore/framework-v7.0.0-bump
```

### Owner-work-intact proof

`git status --porcelain` **after** the bump:

```
 M docs/phases/P1__Online_1v1_MVP/P1__phase-spec.md
 M genesis.md
?? .ai-project/artifacts/hq-openers/
?? .ai-project/artifacts/phase-starters/
?? .ai-project/artifacts/system-requests/
?? .ai-project/artifacts/system-responses/
?? .ai-project/seed.md
?? docs/phase-execution-chat-starter.md
?? docs/phases/P1__Online_1v1_MVP/P1-M1-E1.1-epic-execution-chat-starter.md
?? docs/phases/P1__Online_1v1_MVP/P1-M1-E1.1__spec__WebSocket_Service_Implementation.md
?? docs/phases/P1__Online_1v1_MVP/P1-M1-E1.2-epic-execution-chat-starter.md
?? docs/phases/P1__Online_1v1_MVP/P1-M1-E1.2__spec__Rooms_Manager.md
?? docs/phases/P1__Online_1v1_MVP/P1-M1-E1.3-epic-execution-chat-starter.md
?? docs/phases/P1__Online_1v1_MVP/P1-M1-E1.3__spec__Engine_Integration.md
?? docs/phases/P1__Online_1v1_MVP/P1-M1-E1.4-epic-execution-chat-starter.md
?? docs/phases/P1__Online_1v1_MVP/P1-M1-E1.4__spec__Clock_Management_System.md
?? docs/phases/P1__Online_1v1_MVP/P1-M1-E1.5-epic-execution-chat-starter.md
?? docs/phases/P1__Online_1v1_MVP/P1-M1-E1.5__spec__Match_Persistence.md
?? docs/phases/P1__Online_1v1_MVP/P1-M1-E1.6-epic-execution-chat-starter.md
?? docs/phases/P1__Online_1v1_MVP/P1-M1-E1.6__spec__Connection_Handling.md
```

**Identical to the pre-state, line for line.** The tree is still dirty, which is the success
condition — a clean tree here would have meant the owner's work was committed. Corroborated by the
commit's own contents:

```
$ git show --stat --oneline HEAD
b00bb16 chore(governance): bump to framework v7.0.0
 .ai-project.yml                        |  10 +-
 .ai-project/agents/governance.agent.md | 261 +++++++++++++++++++++++++++++++++
 .governance                            |   2 +-
 3 files changed, 270 insertions(+), 3 deletions(-)
```

Not pushed: no upstream, no remote branch.

---

## `courtis` — the hard one: FM10, 3-major bump, config repair

### Pre-state (verbatim)

```
$ git rev-parse --abbrev-ref HEAD
main
$ git log --oneline -1
8bcd655 chore: update governance submodule to v4.0.1 (P5-GH-7 fix)
$ git status --porcelain
 M README.md
 M docs/context/project-tracker.md
 M docs/context/tech-stack.md
 M docs/roadmap/overview.md
?? .ai-project/
?? docs/creation/steering-notes/
?? docs/phases/P1__Court_Utilization_Core_and_Tournaments/
$ git submodule status
 ab164d88e9ecd34e6ae1ed9f64b7c2b0d7fcafc9 .governance (v4.0.1)
$ cat .gitmodules
[submodule "governance"]
	path = governance
	url = https://github.com/panchew/ai-project-system
	branch = v2.0.0
[submodule ".governance"]
	path = .governance
	url = https://github.com/panchew/ai-project-system
$ ls .ai-project/agents/
ls: cannot access '.ai-project/agents/': No such file or directory
$ cat .ai-project.yml
project:
  name: courtis
  created_at: 2026-06-07T15:01:11Z

governance:
  source: https://github.com/panchew/ai-project-system
  version: "v4.0.1"
  ref: 4.0.1
  submodule_path: .governance/
```

Non-collapsed untracked inventory, captured before committing so the porcelain rendering change
below is verifiable:

```
$ git status --porcelain -uall | grep '^??' | sort
?? .ai-project/agents/governance.agent.md      <- the file this Epic installed
?? .ai-project/artifacts/system-requests/.gitkeep
?? .ai-project/artifacts/system-responses/.gitkeep
?? docs/creation/steering-notes/2026-06-23__creation-chat__steering-note.md
?? docs/phases/P1__Court_Utilization_Core_and_Tournaments/P1__phase__court-utilization-core-and-tournaments.md
?? docs/phases/P1__Court_Utilization_Core_and_Tournaments/P1__phase-execution-chat-starter.md
```

### FM10 — Step 0 as written halts, correctly

```
$ SUB="$(git config -f .gitmodules --get-regexp '^submodule\..*\.path$' | awk '{print $2}')"
raw $SUB value:
    |governance
    |.governance
line count: 2
guard: >>> submodule path not found — STOP <<<   (correct halt)
```

Disambiguation against `git submodule status`, which lists **live submodules only**:

```
$ git submodule status
 ab164d88e9ecd34e6ae1ed9f64b7c2b0d7fcafc9 .governance (v4.0.1)

$ git config -f .gitmodules --get-regexp '^submodule\..*\.path$'
submodule.governance.path governance
submodule..governance.path .governance

  governance     exists=NO   in-submodule-status=NO     <- orphan declaration
  .governance    exists=yes  in-submodule-status=yes    <- the live submodule

$ grep -n -A3 'submodule "governance"' .gitmodules
1:[submodule "governance"]
2-	path = governance
3-	url = https://github.com/panchew/ai-project-system
4-	branch = v2.0.0                                     <- stale pin, FM9-class
```

`$SUB` resolved to `.governance`. **The guard was not hand-edited around** — the ambiguity was
resolved with evidence, and the resolution was then folded back into the procedure as FM10 with a
rewritten Step 0.

### FM2, re-pin, agent install

```
$ git checkout -b chore/framework-v7.0.0-bump      # from main, already checked out — no switch
$ git -C .governance rev-parse -q --verify 'v7.0.0^{commit}'
ABSENT (FM2 confirmed)
$ git -C .governance fetch --tags origin
 * [new tag]         v5.1.0                -> v5.1.0
 * [new tag]         v6.0.0                -> v6.0.0
 * [new tag]         v6.0.1                -> v6.0.1
 * [new tag]         v7.0.0                -> v7.0.0
$ git -C .governance checkout --quiet v7.0.0
$ git -C .governance describe --tags
v7.0.0
$ sha256sum .ai-project/agents/governance.agent.md
66404389f29fa1ff8e829015b70ab8b33373dc3ac3eca3f2faea121d3da3441e  .ai-project/agents/governance.agent.md
FM5: no hq.agent.md — does not apply
```

### Config repair — the inverted pair, and the orphan stanza

```diff
--- co-pre.yml
+++ .ai-project.yml
@@ -1,9 +1,15 @@
+# framework_version: adoption stamp — the AI Project System release this project has
+# adopted. Set by the v7.0.0 bump procedure (P10-M33-E33.1), applied here by P10-M34-E34.2.
+# Confirm with:
+#   grep '^framework_version:' .ai-project.yml
+framework_version: v7.0.0
+
 project:
   name: courtis
   created_at: 2026-06-07T15:01:11Z
 
 governance:
   source: https://github.com/panchew/ai-project-system
-  version: "v4.0.1"
-  ref: 4.0.1
+  version: "7.0.0"
+  ref: v7.0.0
   submodule_path: .governance/
```

```diff
--- co-pre.gitmodules
+++ .gitmodules
@@ -1,7 +1,3 @@
-[submodule "governance"]
-	path = governance
-	url = https://github.com/panchew/ai-project-system
-	branch = v2.0.0
 [submodule ".governance"]
 	path = .governance
 	url = https://github.com/panchew/ai-project-system
```

The **whole orphan stanza** was removed, not just its `branch` line: the path does not exist, so
FM9's narrower remedy would have left Step 0 still resolving two declarations. Step 0 afterwards:

```
$ SUB=[.governance]
guard OK
```

`project.description` remains absent — **deliberately not fixed** (recorded, per the E34.1 precedent).
`governance.submodule_path` left as-is; `.gitmodules` is the authority and the two agree.

### Commit — exactly 4 paths staged

```
$ git diff --cached --name-only
.ai-project.yml
.ai-project/agents/governance.agent.md
.gitmodules
.governance
$ git rev-parse HEAD
a2e95a9cc46b57c0abdcd60cab2be91572bf894a
$ git show --stat --oneline HEAD
a2e95a9 chore(governance): bump to framework v7.0.0
 .ai-project.yml                        |  10 +-
 .ai-project/agents/governance.agent.md | 261 +++++++++++++++++++++++++++++++++
 .gitmodules                            |   4 -
 .governance                            |   2 +-
 4 files changed, 270 insertions(+), 7 deletions(-)
```

Parent commit `8bcd655` (`main`'s HEAD).

### Confirmation method — all checks pass

```
$ grep '^framework_version:' .ai-project.yml
framework_version: v7.0.0
$ grep -E '^  (version|ref):' .ai-project.yml
  version: "7.0.0"
  ref: v7.0.0
$ git submodule status .governance
 8044451c74f798239a51d794ebdab85b2567234e .governance (v7.0.0)
$ sha256sum .ai-project/agents/governance.agent.md
66404389f29fa1ff8e829015b70ab8b33373dc3ac3eca3f2faea121d3da3441e  .ai-project/agents/governance.agent.md
$ ls .ai-project/agents/
governance.agent.md
$ git log --oneline -1
a2e95a9 chore(governance): bump to framework v7.0.0
$ git status -sb | head -1
## chore/framework-v7.0.0-bump
```

### Owner-work-intact proof

Modified-tracked files, before → after (diff of the two sorted lists):

```
1d0
<  M .ai-project.yml
5,6d3
<  M .gitmodules
<  M .governance
```

The only lines that disappeared are the **three bump files** (now committed). All four of the owner's
modified files remain modified:

```
$ git status --porcelain | grep '^ M'
 M README.md
 M docs/context/project-tracker.md
 M docs/context/tech-stack.md
 M docs/roadmap/overview.md
```

Untracked inventory, after → before (non-collapsed):

```
0a1
> ?? .ai-project/agents/governance.agent.md
```

The only difference is the agent file this Epic installed moving untracked → tracked, which is the
intended effect. All five of the owner's untracked files remain untracked.

**One honest note on rendering.** The collapsed porcelain line changed form:

```
before:  ?? .ai-project/
after:   ?? .ai-project/artifacts/
```

Nothing was lost. Before the bump `.ai-project/` was entirely untracked so git collapsed it to one
line; now that `.ai-project/agents/` is tracked, git descends and reports the still-untracked
`artifacts/` subtree instead. The non-collapsed `-uall` inventories above are what prove this, which
is exactly why they were captured before the commit rather than reconstructed after.

Not pushed: no upstream, no remote branch.

---

## Framework repo (this repo)

```
$ python3 -m pytest -q
366 passed in 23.91s
```

366 passed, 0 failed, **0 skipped** — no new skips. (Note the interpreter: `python` is not on PATH in
this environment; `python3 -m pytest` is required. The first baseline attempt failed with
`/bin/bash: line 1: python: command not found`, which is an environment detail, not a suite result.)

---

## Claim → evidence index

| Claim | Evidence |
|---|---|
| `Getawayinsured2023` stamped v7.0.0 | commit `c282475`; `grep '^framework_version:'` → `v7.0.0` |
| Its FM3 check re-run and passed | sha256 `66404389…` == canonical, recomputed at execution |
| Its `models:` block byte-unchanged | block sha identical `934bb807…`; whole pre-file is a byte-exact suffix |
| `footboard` at v7.0.0 | commit `b00bb16`; submodule `8044451c` (v7.0.0); agent sha `66404389…` |
| `footboard` agent dir created | `ls` before: "No such file or directory"; after: `governance.agent.md` |
| `courtis` at v7.0.0 | commit `a2e95a9`; submodule `8044451c` (v7.0.0); agent sha `66404389…` |
| `courtis` FM10 resolved | Step 0 halt transcript; live-vs-declared disambiguation; orphan stanza diff |
| `courtis` inverted pair fixed | `.ai-project.yml` diff: `version` → `"7.0.0"`, `ref` → `v7.0.0` |
| Both dirty trees preserved | before/after porcelain per project + `git show --stat` per commit |
| Nothing pushed | no upstream on any branch; `git ls-remote --heads origin chore/…` finds nothing |
| FM10 + FM11 recorded | `.ai-project/artifacts/reference/v7-bump-procedure/README.md` |
| Suite green | `366 passed`, 0 failed, 0 skipped |
| Baseline was actually 365/1 | pytest output above; `git diff --stat milestone/M34` empty at the time |
