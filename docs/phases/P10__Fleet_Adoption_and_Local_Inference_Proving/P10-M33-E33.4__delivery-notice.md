---
artifact_type: delivery_notice
artifact_version: 1.0
timestamp: 2026-07-21T01:20:00Z
issuer_chat: Epic Agent (P10-M33-E33.4)
issuer_role: Epic Agent
status: delivered
epic_id: P10-M33-E33.4
milestone_id: P10-M33
phase_id: P10
project_name: ai-project-system
merge_details:
  pr_number: 153
  pr_url: https://github.com/panchew/ai-project-system/pull/153
  merge_commit: <pending — human-authorized merge, not yet performed>
  merge_timestamp: <pending>
  merge_strategy: merge
  target_branch: milestone/M33
duration:
  start_date: 2026-07-20
  end_date: 2026-07-20
  elapsed_days: 0
final_artifacts:
  - name: E33.4 run record
    path: .ai-project/artifacts/agentic-runs/P10-M33-E33.4/run-record.md
    type: report
  - name: Run transcript + context + adapter metadata (evidence)
    path: .ai-project/artifacts/agentic-runs/P10-M33-E33.4/
    type: report
  - name: E33.4 Delivery Notice
    path: docs/phases/P10__Fleet_Adoption_and_Local_Inference_Proving/P10-M33-E33.4__delivery-notice.md
    type: report
completion_notice_timestamp: 2026-07-21T01:20:00Z
review_decision_timestamp: <pending — parent Milestone Chat Stage-2>
---

# Delivery Notice: P10-M33-E33.4 — home_finance real Agentic/Local epic (proving-pair completion)

## Summary

`home_finance` — the proving pair's remaining un-run half — has now carried a **real Agentic/Local
epic end-to-end** under the fixed posture, with a committed run record. The run advanced the project
(target commit `8dfb2bd`, suite 275/0 green) by closing a correction `home_finance`'s own P2/M1
documents had been carrying since 2026-06-24. Together with E33.2's `local-agent-runner` run, the
**"each proving-pair project has a committed run record"** bar is met (Milestone DoD / AC 1; Phase
Success Criterion 1 / AC). No runtime decision and no measurement were produced — those legs closed
with E33.2/E33.3. Framework suite 366 green. Awaiting Milestone-Chat Stage-2 review and merge
authorization.

## Deliverables vs Spec (DoD verification)

- [x] **At least one real Agentic/Local epic ran on `home_finance`, run record committed** —
      dispatched via `bin/run-dev-agent` on Ollama 0.30.0 with `qwen3-coder:30b`; record at
      `.ai-project/artifacts/agentic-runs/P10-M33-E33.4/run-record.md` with transcript, context, and
      adapter metadata alongside.
- [x] **The run advanced `home_finance` (real work, not a demo) — cited target commit** —
      `home_finance` `epic/P2-M1-E1.1-mxn-currency-default` @ **`8dfb2bd`** (base `0ea6924`, the
      E33.1 v7.0.0 bump). Migration `change_column_default :financial_events, :currency, from: "USD",
      to: "MXN"` + regenerated `db/schema.rb` + factory de-hardcoded + new spec example.
      **Suite 275 examples / 0 failures** (274 pre-run baseline + 1 new), independently re-run by
      this Epic Chat after the run.
- [x] **No run failed to complete → no blocker record required** — the substrate came up, the E31.2
      guard passed, and the run produced complete, green work. Nothing was substituted for an un-run
      claim (Hard Constraint honored; see the honesty note below on the exit code).
- [x] **Framework-repo suite green** — **366 passed**, no new skips (only artifacts/docs added here;
      no framework code touched). The run's code lands in the target repo, governed by its own suite.
- [x] **Delivery Notice committed; all changes on `epic/P10-M33-E33.4`; PR opened to
      `milestone/M33`** — this file; PR #153.

## Claims-vs-evidence check

- The **"advanced real work"** claim cites target commit `8dfb2bd` and the 275/0 suite, both
  verifiable from this repo's run record; the transcript showing the model's actual tool calls is
  committed alongside.
- The claim that the unit is **genuine and not a demo** is evidenced by quotation, not assertion: the
  correction is named in `home_finance`'s P2 phase §Notes and P2-M1 §Problem Statement / Goal 4 /
  §In Scope — all authored 2026-06-24, before this Epic existed.
- The **274 → 275** example counts are stated from runs performed in this session (pre-run baseline
  and post-run verification), not carried over from the spec's grounding (GH-2 discipline). The
  framework 366 baseline was likewise re-confirmed, not assumed.
- **Honesty note on the exit code:** the run exited **2 (`max_iterations_exceeded`)**, not 0. The
  work was nonetheless complete and proven green at iteration 6; iterations 7–9 were redundant
  self-verification, so no final-answer turn was emitted. This is reported as a **false-negative exit
  code** rather than smoothed over — it is corroborating evidence for E33.2's finding that on this
  stack the exit status is not a reliable completion signal (E33.2: exit 0 with zero work; here:
  exit 2 with complete work). Verification read the transcript and the target repo, not the exit
  status.

## Extent-vs-spec

Within scope: the run record + evidence under `.ai-project/artifacts/agentic-runs/P10-M33-E33.4/`,
and this Delivery Notice. **Nothing else in this repo was touched.**

**Not touched** (per Non-Goals / Technical Constraints / starter do-not-touch list): `.ai-project.yml`
`models:` routing; any `governance/` document; `measure-token-burn` or E33.3's record; E33.1/E33.2
surfaces (read as inputs only); the M31 dual-mode switch / guardrail (applied, not edited). **No
runtime decision was recorded and no comparative model trial was run** — the runtime is settled and
was applied, not re-opened. **No target-repo code was merged onto `milestone/M33` / `phase/P10`**
(Cross-Repo split honored — it lives on the `home_finance` branch).

## Design decision resolved (spec §Design Decision — mine to decide, documented)

**The genuine unit of `home_finance` work to run** = the **MXN currency-default correction**, scoped
as target epic `home_finance` P2-M1-E1.1. Chosen because it is pre-articulated and founder-deferred
(named three times in `home_finance`'s own P2/M1 documents), foundational by M1's own sequencing
rationale ("resolved at the foundation, not retrofitted later"), and small, self-contained and
reviewable — mirroring E33.2's slice-selection. Decided and proceeded without escalation, per the
spec. Recorded in run record §1.

## Documented deviations

1. **Raised model tier.** Dispatched on `qwen3-coder:30b` rather than the configured
   `models.epic_dev`/`epic_qa` = `local:qwen2.5-coder:14b` (proven unusable in E33.2), applying the
   Phase Chat / CFO disposition "Ollama stands; raise the model tier." Recorded as a **dispatch
   deviation**; the `models:` routing edit remains a separate authorized act and was **not**
   performed. Run record §2.
2. **Dispatch path.** `bin/run-dev-agent` **direct on host** rather than the orchestrator's Docker
   sandbox wrapper — the E33.2 / P7-M26-E26.3-PROVE precedent, for the same reason (keeps container
   and networking failure surface out of the run's signal). M31 routing and the E31.2 guard honored.
3. **Code committed as-produced by the model**, preserving evidentiary value per E33.2's precedent.
   One cosmetic wart is noted in the run record rather than silently fixed: the generated migration
   file has no trailing newline.
4. **Run-record layout.** Placed under `.ai-project/artifacts/agentic-runs/P10-M33-E33.4/` with no
   separate phase-folder pointer file, mirroring E33.2's layout; this Delivery Notice's
   `final_artifacts` is the phase-folder entry point. (Spec left the final layout to the Epic Chat's
   call, documented.)

## Note for the parent chat (not an escalation)

The `home_finance` target branch is committed **locally and has not been pushed** to its remote.
`home_finance` is CFO-controlled, so publishing the branch is the CFO's call rather than this Epic's.
Verification from this repo does not depend on it — the transcript and the exact context handed to
the runner are copied here, and the commit is reproducible from the cited base `0ea6924`.

## Chat Closure

This Epic Chat (P10-M33-E33.4) has completed execution (happy-path steps 1–2). PR #153 opened to
`milestone/M33`. **Stopping for parent Milestone-Chat Stage-2 review** and human merge
authorization — not merging.

**Next Step:** Milestone Chat reviews the run record, accepts (default-accept by silence, PSG §11.6,
or a Review Decision), and issues merge authorization. On acceptance the "each project" bar is met
and M33 proceeds to consolidation + closure.
