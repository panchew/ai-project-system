---
artifact_type: delivery_notice
artifact_version: 1.0
timestamp: 2026-07-20T23:25:46Z
issuer_chat: Epic Agent (P10-M33-E33.3)
issuer_role: Epic Agent
status: delivered
epic_id: P10-M33-E33.3
milestone_id: P10-M33
phase_id: P10
project_name: ai-project-system
merge_details:
  pr_number: 152
  pr_url: https://github.com/panchew/ai-project-system/pull/152
  merge_commit: <pending — human-authorized merge, not yet performed>
  merge_timestamp: <pending>
  merge_strategy: merge
  target_branch: milestone/M33
duration:
  start_date: 2026-07-20
  end_date: 2026-07-20
  elapsed_days: 0
final_artifacts:
  - name: E33.2 run measurement-trust record (captured data + validation + sizing + honesty judgment)
    path: .ai-project/artifacts/reference/token-measurement/e33-2-run-measurement-trust.md
    type: report
  - name: measure-token-burn proportionate fix (1.0.0 -> 1.1.0)
    path: bin/measure-token-burn
    type: implementation
  - name: Direction C-lite local-run extraction tests
    path: tests/test_measure_token_burn_local.py
    type: implementation
completion_notice_timestamp: 2026-07-20T23:25:46Z
review_decision_timestamp: <pending — parent Milestone Chat Stage-2>
---

# Delivery Notice: P10-M33-E33.3 — Trustworthy measurement out of the run (P9-GH-2)

## Summary

E33.2's real run numbers are now captured and trustworthy. Pointing `measure-token-burn` at E33.2's
committed artifacts showed the tool **silently dropped the entire run** — its Direction C-lite path
required the old `transcript.json`/`run-metadata.json` filenames and skipped E33.2's per-model
layout, so none of the run's numbers (Run A 223 tok / 0 rounds; Run B 829 tok / 10 rounds) reached
the dataset and no run-status was attached to any local number. Sized as a **proportionate fix**
(the numbers had to be captured before they could be trusted): the tool now recognizes the per-model
layout and records the run-status each token count is attached to, folding in E33.2's
**exit-code-untrust** finding (Run A's `completed`/0-round false-positive is flagged; Run B's missing
metadata is a recorded exit-code gap, not a skipped run). The **explicit honesty judgment** is
committed. Suite 363 → 366 green, no new skips. Awaiting Milestone-Chat Stage-2 review + merge
authorization.

## Deliverables vs Spec (DoD verification)

- [x] **Real burn/validation data from E33.2's run committed** — captured Run A/B token/round/
      throughput/status numbers, each traced to the run record/transcripts, in
      `.ai-project/artifacts/reference/token-measurement/e33-2-run-measurement-trust.md` §1.
- [x] **Sizing decision recorded with its basis** — proportionate fix, because the tool reproduced
      *none* of the run's numbers (silent zero coverage); §3 of the record, and the "what was
      deliberately NOT done" bound (no G9 prompt-token work, no corpus regen, no runner/`models:`/
      `governance/` change).
- [x] **Proportionate fix delivered + validated** — `bin/measure-token-burn` 1.0.0 → 1.1.0:
      `collect_local_runs` generalized to the per-model layout (old PROVE layout unchanged, 404/759/
      837 preserved); run-status trust fields + `zero_work_completion`; gap record G11; §5 renderer.
      New test `tests/test_measure_token_burn_local.py` (both layouts + `eval_count` fallback).
- [x] **Explicit honesty judgment committed** — record §4: the run's numbers **can** be trusted now
      (captured, matching ground truth) **but only read with the run-status the tool now attaches**;
      they could **not** before (silently dropped). No third state — the check was performed.
- [x] **Framework-repo suite green** — `pytest -q` → **366 passed** (363 baseline + 3 new), no new
      skips, warning-clean. `measure-token-burn` behavior contract preserved (reads run artifacts
      read-only; writes only its dataset files).
- [x] **Delivery Notice committed** — this file.

## Claims-vs-evidence check (load-bearing for E33.3)

- Every captured number traces to E33.2's run: Run A 223/0/completed/exit 0 → transcript-A (+ its
  `__run-metadata.json`); Run B 829/10/`max_iterations_exceeded` → transcript-B; Run B exit 2 is
  prose-only in the run record and is therefore recorded as an artifact **gap**, not asserted by the
  tool. No number is backfilled from the banned 24K/157K estimates.
- The "silently dropped before / captured after" claim is reproducible: `collect_local_runs` returned
  3 runs (old PROVE only) before the fix and 5 after (E33.2 A/B added), verified on this branch and
  locked by the new test.
- The false-negative half of exit-code-untrust (Run B's failure-status-but-good-work) is explicitly
  left to human review (gap G11), not machine-guessed.

## Extent-vs-spec

Within scope: `bin/measure-token-burn` + its new test; the captured-data/sizing/judgment record under
`.ai-project/artifacts/reference/token-measurement/`; this Delivery Notice. **Not touched** (per
Non-Goals / Technical Constraints): `.ai-project.yml` `models:`; any `governance/` doc; E33.1's
procedure or E33.2's run record/decision as *edit* surfaces (read-only inputs); the dual-mode switch/
guardrail. The committed `token-burn-dataset.json/.md` was **not regenerated** (a full regen would
rewrite the E30.1 paid/corpus numbers — forbidden by Non-Goals); the fix ensures the next authorized
full run folds E33.2 in, and E33.2's numbers are captured in the record instead (deliberate boundary,
recorded in §3).

## Honesty judgment (verbatim — Acceptance Criterion, no skipped-check third state)

> **`measure-token-burn`'s numbers for E33.2's run CAN now be trusted — the token magnitudes (Run A
> 223, Run B 829) and their tool-round counts (0 / 10), each captured directly from the run's own
> transcripts and matching the run record's ground truth — but ONLY when read together with the
> run-status each is attached to, which the tool now records and, for Run A, flags. They could NOT
> be trusted before this epic: the tool silently dropped the entire run.**

## Chat Closure

This Epic Chat (P10-M33-E33.3) has completed execution (happy-path steps 1–2). PR #152 opened to
`milestone/M33`. **Stopping for parent Milestone-Chat Stage-2 review** and human merge authorization
— not merging. If merge authorization is given directly in this chat, I will confirm it is not
bypassing the parent chat's Stage-2 review first (P9-M31 precedent).

**Next Step:** Milestone Chat reviews the measurement-trust record + fix, accepts (default-accept by
silence, PSG §11.6, or a Review Decision), and issues merge authorization. On acceptance, all three
M33 epics are delivered and the Milestone Chat moves to consolidation + closure (`is_final: false`).
