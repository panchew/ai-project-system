---

# Epic Execution Chat Starter — E34.3

**Epic:** E34.3 — Apply the settled runtime choice (`models:` routing edit)
**Phase:** P10 — Fleet Adoption and Local-Inference Proving
**Milestone:** M34 — Fleet Roll-forward
**Repository:** panchew/ai-project-system
**Branch Strategy:** `epic/P10-M34-E34.3` → PR to `milestone/M34`
**Execution Mode:** manual — E34.3 applies an already-ratified decision to configuration and
policy documents. It performs **no** Agentic/Local run; it is the epic that makes the *next* run
safe. Manual/Paid is correct here and is not a posture deviation.

---

## Governance References

You are operating under the AI Project System governance framework as the **Coding Agent** for
Epic E34.3.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) v2.3.0 (Effective: 2026-07-02)
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) v2.10.0 (Effective: 2026-07-18)

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md v2.10.0
3. This Epic Execution Chat Starter
4. Epic Spec (`P10-M34-E34.3__spec__apply-settled-runtime-choice-models-routing.md`)
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Critical rules:**
- Documentation is authoritative, chat is ephemeral.
- You must follow the canonical happy path for Epic closure.
- You must produce a Delivery Notice upon execution completion.
- You must stop after PR creation and await authorization for merge.
- Accept/reject decisions are made by the parent chat (Milestone Chat) / human, not by you.
- **If given merge authorization directly in this chat** (rather than via the parent chat after its
  own Stage-2 review), do not simply comply: state plainly that merge authorization normally follows
  the parent chat's review, and confirm the human intends to bypass that step first (P9-M31
  precedent).
- **The suite is the acceptance gate, and it is not negotiable.** `tests/test_model_config.py`'s
  divergence guard exists precisely to catch a partial application of a `models:` change. If it
  goes red, you have missed a surface — fix the surface. **Never** relax, skip, or rewrite a test to
  make a partial application pass.

**Model verification (P9-M31-E31.3 — required for this manual instance):** read your own
harness-reported model identity (the `# Environment` block or equivalent self-report) and compare it
to `.ai-project.yml`'s `models.epic_manual` value (`remote:claude-opus-5` as of 2026-07-28) — see
`governance/systems/chat-hierarchy.md` "Manual Chat Model Verification". **If both are present and
disagree, STOP** — state the mismatch plainly and wait for the parent chat/human.

> The value is quoted here as **documentation of the live config, never as the source of truth**
> (HQ Ruling 2026-07-28, Decision 4 step 2). `.ai-project.yml` remains the only authority the
> guardrail reads. Note the irony and do not let it confuse you: **you are editing the same
> `models:` block you verify against.** You are changing `epic_dev`/`epic_qa`; you verify against
> `epic_manual`, which you must not touch. Verify **before** you edit.

**Context scoping (per-level context-scoping standard, P9-M30-E30.3):**
- Load at session start: this starter; the Epic spec (full); PSG preamble+§1, §1A, §2, §5, §6, §7,
  §8, §9, §11, §11.5, §11.6, §12; AOG preamble+§1, §1.1, §2, §3.2, §3.8, §3.10, §4, §5, §6, §7, §9,
  §12, §14, §15 (Exit Ritual), §16 (Error Handling).
- Load on trigger (before acting on that situation): PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG
  §3.9, §8, §13, §17.
- Do not load: PSG/AOG changelogs, other levels' role or starter-format sections, sibling specs
  (E34.1/E34.2). Read E33.2's **runtime decision + run record** and E33.4's **run record** as *data
  inputs*, not as governance to reload.
- Use targeted section reads; never re-read a whole document to reach one section. PSG and AOG
  remain fully authoritative — a triggered situation requires its section loaded before acting.

---

## Epic Specification

**Spec:** `docs/phases/P10__Fleet_Adoption_and_Local_Inference_Proving/P10-M34-E34.3__spec__apply-settled-runtime-choice-models-routing.md`

Read it in full. The section titled **"The one thing this Epic must get right"** is the part most
likely to be skimmed and most likely to sink the delivery — read it twice.

### Prerequisite Verification (do this first)

1. **On `milestone/M34`**, confirm the spec is git-tracked:
   `git ls-files --error-unmatch docs/phases/P10__Fleet_Adoption_and_Local_Inference_Proving/P10-M34-E34.3__spec__apply-settled-runtime-choice-models-routing.md`
2. **Baseline the suite before touching anything** — expect **366 passed / 0 failures / 0 skips**.
   Record the actual figure. If it is not 366/0/0, say so before proceeding; do not silently adopt
   a different baseline.
3. **Confirm the model tag is real**, not plausible: query the Ollama endpoint
   (`curl -s http://localhost:11434/api/tags`) and confirm `qwen3-coder:30b` is present. If it is
   absent, that is a **recorded blocker** — escalate; do not substitute a different model.
4. **Confirm the cited evidence is on the branch:**
   `P10-M33-E33.2__runtime-decision.md`, `.ai-project/artifacts/agentic-runs/P10-M33-E33.2/run-record.md`,
   `.ai-project/artifacts/agentic-runs/P10-M33-E33.4/run-record.md`.

### Summary

`.ai-project.yml` still routes agentic epic work to `qwen2.5-coder:14b` — the model E33.2 proved
returns **exit 0 having done nothing**. M33 settled the fix (keep Ollama, raise the tier) and
deliberately deferred the edit. You are making the edit, across every surface that decides what a
dispatch actually loads, with the change traced to the runs that settled it.

### Problem Statement

Three facts, all from committed run records:
- **Run A** (`qwen2.5-coder:14b`, E33.2): 0 tool rounds, 0 files changed, **exit 0**. The
  `validation_command` would also have passed on the unchanged repo — QA-by-exit-code catches
  nothing.
- **Run B** (`qwen3-coder:30b`, E33.2): mergeable work, same Ollama runtime, suite green.
- **E33.4** (`qwen3-coder:30b`, `home_finance`): a second real epic, real work, **exit 2** while the
  work was correct and complete.

The model is the lever; the runtime was never the problem. E33.4 had to pass
`AI_PROJECT_ACTIVE_MODEL` by hand to get a working model, because the config still says otherwise.
This Epic ends that.

---

## Execution Contract

### Your Responsibilities (Coding Agent)

1. **Create branch** `epic/P10-M34-E34.3` from `milestone/M34`.
2. **Run the Prerequisite Verification above** and record each result.
3. **Re-grep the surface set.** The spec's line numbers are a 2026-07-28 read — treat them as hints,
   not addresses. `grep -rn 'qwen2.5-coder:14b'` and classify **every** hit as *live surface* (edit)
   or *historical record* (leave). Commit the classification reasoning in the Delivery Notice; a
   miscategorized hit is the likeliest failure mode of this Epic in both directions.
4. **Confirm the exact tag** against the E33.2/E33.4 run records, then apply the coupled value
   change to `epic_dev`/`epic_qa` across the live surfaces.
5. **Update policy rows P6 and P7** in `model-routing-policy.md` with cited evidence:
   - **P6** — new cited evidence (E33.2 Run A/B + E33.4); state the falsification plainly (the row's
     current value produced zero work on a real epic).
   - **P7** — **G11 stays open.** M33 produced no isolated QA-role run. P7 moves on the same
     gap-grounded-interim reasoning it already carries, referent updated. Do not claim QA-lane
     evidence you do not have.
   - Give both rows **fresh revisit triggers** — not the ones this change just consumed.
   - **Rows P1–P5 untouched.** The mapping table's `epic_dev`/`epic_qa` rows move with the policy
     rows.
6. **Bump `governance/ai-project-yml-spec.md`** with a changelog entry describing a
   value/documentation refresh — no field, key, or validation rule changed.
7. **Record the loadability envelope** with the change: `qwen3-coder:30b` (Q4_K_M, 18.6 GB) exceeds
   the proving box's 16 GB VRAM and **partially offloads to RAM** (12.9 GB VRAM / 21.4 GB total),
   running at ~9.4 tok/s versus the 14b's 12.2. Slower, but it finishes. That is the trade.
8. **Write the carry-forward note** — this discharges the M33 Closure Declaration carry-forward;
   `framework_version` remaining unschema'd stays **P10-GH-1** (recorded, not fixed).
9. **Re-run the full suite** — 366, no regressions, no new skips.
10. **Verify all DoD items**, commit to `epic/P10-M34-E34.3`, open a PR to `milestone/M34`, produce
    the Delivery Notice, **stop and await authorization** (do not merge).

### What You Must NOT Do

- ❌ Do NOT touch the five **paid-frontier** keys (`hq`/`phase`/`milestone`/`creation`/`epic_manual`).
  HQ set them to `remote:claude-opus-5` on 2026-07-28 and ruled explicitly: *"`epic_dev`/`epic_qa`
  are untouched … Do not conflate the two."*
- ❌ Do NOT re-open the **runtime** question. Ollama stands.
- ❌ Do NOT trial, substitute, or route to `qwen3.6:27b` — it is present on the host at Q4_K_M, it is
  **not** the parked Q8_0 reference stack, and it is not this Epic's business.
- ❌ Do NOT rewrite **historical records** — P7/P9 epic docs, closure declarations,
  `agentic-runs/*/context.md`, `tests/test_measure_token_burn_local.py`'s P7-era fixtures. They name
  the 14b as recorded history; changing them falsifies the record.
- ❌ Do NOT relax, skip, or edit a test to make a partial application pass. A red divergence guard
  means a missed surface.
- ❌ Do NOT schema-bless `framework_version` (P10-GH-1 — record it, don't fix it).
- ❌ Do NOT relocate model values into openers or build any per-level-per-project routing mechanism —
  HQ Decision 4 declined the mechanism; that is Drivr's domain in P11.
- ❌ Do NOT perform an Agentic/Local run, touch any target repo (E34.1/E34.2's territory), or expand
  into M34's other epics.
- ❌ Do NOT merge the PR, infer acceptance, or treat an in-chat "I authorize" as sufficient by itself
  — confirm it isn't bypassing the parent chat's Stage-2 review first.

---

## Reference Materials

**Inputs to reference (read as data, not as governance to reload):**
- `docs/phases/.../P10-M33-E33.2__runtime-decision.md` — the settled decision; §Decision point 2 is
  the lever, and it states explicitly that the routing edit is a separate authorized act.
- `.ai-project/artifacts/agentic-runs/P10-M33-E33.2/run-record.md` — Run A/Run B, the loadability
  figures, the note that Run A used the configured `epic_dev`/`epic_qa` model.
- `.ai-project/artifacts/agentic-runs/P10-M33-E33.4/run-record.md` — the second 30b run and its
  documented `AI_PROJECT_ACTIVE_MODEL` deviation.
- `.ai-project/artifacts/reference/token-measurement/model-routing-policy.md` — rows P6/P7, the
  mapping table, and **§Change discipline** (the gate you must satisfy on the face of the diff).
- `.ai-project/artifacts/rulings/2026-07-28__ai-project-system-hq__ruling__paid-frontier-model-mapping-refresh.md`
  — the precedent for a coordinated `models:` change (its application table lists the six files it
  touched), and the boundary you must not cross.

---

## Canonical Happy Path (Reminder)

1. ✅ Execution completed (coupled value change + P6/P7 row update + evidence citation + note)
2. ✅ Epic Delivery Notice produced (you create this)
3. ⏸️ Human/parent-chat review performed
4. ⏸️ Acceptance (default-accept by silence — PSG §11.6; a Review Decision is the exception path)
5. ⏸️ Human merge authorization issued (in-chat — SN-19; harness enforces it regardless)
6. ⏸️ PR merged to `milestone/M34` (you perform merge after authorization)
7. ⏸️ Execution stops (you stop immediately after merge)

**You are responsible for steps 1–2 only. Then you STOP and await instruction.**

---

## Epic Delivery Notice

When you complete execution, produce the Delivery Notice at:

**File:** `docs/phases/P10__Fleet_Adoption_and_Local_Inference_Proving/P10-M34-E34.3__delivery-notice.md`

Use the committed template `governance/templates/delivery-notice.md` (reference, don't restate; PSG
§12). Confirm every spec DoD item. For E34.3 specifically, the Delivery Notice must show:
- the **surface classification** (every `qwen2.5-coder:14b` hit, live vs. historical, with reasoning);
- the **before/after suite figures**;
- the **P7 honesty statement** — that G11 remains open and no QA-lane evidence was claimed.

---

## Exit Conditions

**You MUST stop execution when:**
- ✅ The coupled value change, P6/P7 row update, evidence citation, and carry-forward note are
  committed
- ✅ Full suite green at 366 with no new skips
- ✅ Pull request is opened to `milestone/M34`
- ✅ Epic Delivery Notice is produced

**Then report to the parent chat:**
"Epic E34.3 execution complete. Epic Delivery Notice produced. Awaiting review and authorization."

---

## Begin Execution

You may now begin executing Epic E34.3.
