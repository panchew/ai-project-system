---
artifact_type: delivery_notice
artifact_version: 1.0
timestamp: 2026-07-20T23:15:00Z
issuer_chat: Epic Agent (P10-M33-E33.2)
issuer_role: Epic Agent
status: delivered
epic_id: P10-M33-E33.2
milestone_id: P10-M33
phase_id: P10
project_name: ai-project-system
merge_details:
  pr_number: 151
  pr_url: https://github.com/panchew/ai-project-system/pull/151
  merge_commit: <pending — human-authorized merge, not yet performed>
  merge_timestamp: <pending>
  merge_strategy: merge
  target_branch: milestone/M33
duration:
  start_date: 2026-07-20
  end_date: 2026-07-20
  elapsed_days: 0
final_artifacts:
  - name: E33.2 run record
    path: .ai-project/artifacts/agentic-runs/P10-M33-E33.2/run-record.md
    type: report
  - name: E33.2 runtime decision
    path: docs/phases/P10__Fleet_Adoption_and_Local_Inference_Proving/P10-M33-E33.2__runtime-decision.md
    type: report
  - name: Run transcripts + context (evidence)
    path: .ai-project/artifacts/agentic-runs/P10-M33-E33.2/
    type: report
completion_notice_timestamp: 2026-07-20T23:15:00Z
review_decision_timestamp: <pending — parent Milestone Chat Stage-2>
---

# Delivery Notice: P10-M33-E33.2 — First real Agentic/Local epic on the pair + runtime decision

## Summary

A real Agentic/Local epic was run on the proving pair. `local-agent-runner` carried a genuine unit
of its own work (CF-2 slice: public `run`/`Result` library API) through the local execution path; the
run advanced the project (target commit `4ec1e8f`, suite 210/1 green). From the run's own observations
the **Ollama-vs-llama.cpp+Qwen3.6 runtime decision is recorded** across quality, throughput,
loadability, and review burden — including an honest **partial blocker + escalation**: the specific
llama.cpp+Qwen3.6-27B-Q8_0 reference stack was not trialable on the proving hardware. Framework suite
363/363 green. Awaiting Milestone-Chat Stage-2 review and merge authorization.

## Deliverables vs Spec (DoD verification)

- [x] **Real Agentic/Local run on a proving-pair project, run record committed** — `local-agent-runner`,
      dispatched via `bin/run-dev-agent` on Ollama; record at
      `.ai-project/artifacts/agentic-runs/P10-M33-E33.2/run-record.md` with both transcripts.
- [x] **Run advanced the target project (real work, cited commit)** — `local-agent-runner`
      `epic/cf-2-public-run-api` @ `4ec1e8f` (base `231a2cf`, v7.0.0); public API exported + test;
      **suite 210 passed / 1 skipped**. Not a demo.
- [x] **Runtime decision recorded with the run's own reasons across all four dimensions** —
      `…/P10-M33-E33.2__runtime-decision.md`. Quality (14b unusable / 30b mergeable), throughput
      (12.2 / 9.4 tok/s end-to-end), loadability (Q8_0 ~42 GB **not** loadable on 16 GB-VRAM/30 GB-RAM
      box; 30B/Q4 spills to RAM but runs), review burden (exit code untrustworthy **both** ways).
- [x] **Blocker + escalation where a run could not complete** — the llama.cpp+Qwen3.6-27B-Q8_0
      comparison was **not runnable** on the proving host (llama.cpp absent; ~42 GB Q8_0 unloadable).
      Recorded and escalated to the Milestone → Phase Chat in the runtime decision (§Partial blocker),
      not substituted with an un-run judgment (Hard Constraint honored).
- [x] **Framework-repo suite green** — 363 passed, no new skips (only docs/artifacts added here; no
      framework code touched). The run's code lands in the target repo (its own suite 210/1 green).
- [x] **Delivery Notice committed** — this file.

## Claims-vs-evidence check (load-bearing for E33.2)

- Every runtime reason in the decision traces to a specific run observation (Runs A/B, transcripts,
  `/api/ps` footprint, hardware readout) — no reason asserted that the run did not show.
- The "advanced real work" claim cites target commit `4ec1e8f` and the 210/1 suite, verifiable from
  this repo's run record.
- What the run could **not** observe (the llama.cpp+Qwen3.6-Q8_0 stack) is explicitly *not* decided —
  it is escalated as a partial blocker rather than presented as a decision.

## Extent-vs-spec

Within scope: run record + runtime decision + transcripts + Delivery Notice. **Not touched** (per
Non-Goals / Technical Constraints): `.ai-project.yml` `models:` (decision recorded, routing NOT
changed), any `governance/` doc, `measure-token-burn` (E33.3's object), the dual-mode switch/guardrail
(applied, not edited). No target code merged onto `phase/P10` (Cross-Repo split honored — it lives on
the `local-agent-runner` branch).

## Design decisions resolved (spec §Design Decisions — mine to decide, documented)

1. **Target = `local-agent-runner`; unit = CF-2 slice** (public library API) — genuine, small,
   self-contained, most directly exercises the substrate. Recorded in the run record §1.
2. **Runtime criteria** — four dimensions weighted with **loadability + quality as decisive** (they
   are what the substrate fork turns on), throughput secondary, review burden flagged forward to
   E33.3. Recorded in the runtime decision.

## Dispatch-path note (documented deviation)

Used `bin/run-dev-agent` **directly on host** (PROVE precedent), not the orchestrator's Docker
sandbox wrapper — the runtime evidence is identical and this keeps container/networking failure
surface out of a first run's signal. M31 routing + E31.2 guard honored. See run record §2.

## Chat Closure

This Epic Chat (P10-M33-E33.2) has completed execution (happy-path steps 1–2). PR opened to
`milestone/M33`. **Stopping for parent Milestone-Chat Stage-2 review** (the runtime decision is
confirmed there) and human merge authorization — not merging.

**Next Step:** Milestone Chat reviews the runtime decision + run record, accepts (default-accept by
silence, PSG §11.6, or a Review Decision), and issues merge authorization.
