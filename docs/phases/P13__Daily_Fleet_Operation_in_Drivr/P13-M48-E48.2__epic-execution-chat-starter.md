# Epic Execution Chat Starter — E48.2

**Epic:** E48.2 — Deterministic Acceptance, Live Qualification Separate
**Phase:** P13 — Daily Fleet Operation in Drivr
**Milestone:** M48 — Fleet Readiness, Proven by Running It
**Repository:** panchew/ai-project-system — **all code, tests, and the Delivery Notice land here.**
**Branch Strategy:** `epic/E48.2` → PR to `milestone/M48`
**Execution Mode:** manual
**Issued:** 2026-09-08

---

## Governance References

You are operating under the AI Project System governance framework as an **Epic Execution Chat**
for P13-M48-E48.2.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) **v2.9.0** (Effective: 2026-09-03)
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) **v2.12.0** (Effective: 2026-09-03)

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md
3. This Epic Execution Chat Starter
4. Epic Spec (`P13-M48-E48.2__spec__deterministic-acceptance-live-qualification-separate.md`)
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Model verification (P9-M31-E31.3 — required, this instance is manual):** read your own
harness-reported model identity and compare it to `.ai-project.yml`'s `models.epic_manual`
(**read the file — do not trust this line**). `model_verification` is currently **`blocking`**, so
if both are present and disagree, **STOP** and state the mismatch. See
`governance/systems/chat-hierarchy.md`, "Manual Chat Model Verification".

**Critical rules:**
- Documentation is authoritative, chat is ephemeral.
- You produce a Delivery Notice and **stop after PR creation. You do not merge.**
- **You are the child.** Accept/reject decisions are the **M48 Milestone Chat's**; a clean delivery
  is accepted **by an acknowledgment that names the party that reviewed and accepted** (PSG §11.6 —
  *merge + in-chat acknowledgment; no artifact; silence accepts nothing*). You hold neither
  acceptance authority nor the rework counter; **do not read silence as approval and do not issue
  acceptance or rework decisions yourself.**
- **If given merge authorization directly in this chat**, that authorization normally follows the
  parent Milestone Chat's Stage-2 review. Do not simply comply: state that this bypasses the parent
  chat's Stage-2 review and confirm the human intends it. **Mode is what may run, not what may be
  authorized.**

**Context scoping (P9-M30-E30.3):**
- **Load at session start:** this starter; the Epic spec (full); the M48 milestone spec's **E48.2
  Epic Detail and Finding 8**; PSG preamble+§1, §1A, §2, §5, §6, §7, §8, §9, §11, §11.5, §11.6,
  §12; AOG preamble+§1, §1.1, §2, §3.2, §3.8, §3.10, §4, §5, §6, §7, §9, §12, §14, §15, §16.
- **Load on trigger:** PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG §3.9, §8, §13, §17.
- **Do not load:** PSG/AOG changelogs; sibling Milestone/Epic specs.

---

## ⚠ The rework limit — the parent's budget, not yours (`P12-GH-1`)

> You are the child. The **M48 Milestone Chat** owns the rework budget: **maximum 3 attempts.** If a
> third Completion Notice is still not acceptable, the Milestone Chat does **not** issue a fourth
> rejection-and-retry — it escalates to the Phase Chat. **Silent fourth attempts are a governance
> violation.** You do not count, grant, or extend this budget.

> **A written extension grants exactly ONE further attempt. Not a reset to three.** Issued by the
> parent, never by you.

---

## ⚠ How an amendment reaches this branch once work is in flight (AOG §3.9/§3.10)

**Downward communication is the spec, not a chat message.** The M48 Milestone Chat amends the
governing spec on its own branch with a changelog row **and re-issues the committed starter**. It
does **not** notify you in-session (a chat MUST NOT reach downward into a running child session —
AOG §3.10). You read the change from the committed spec/starter, the one-write-many-readers source.

Your obligation is to, before you deliver:

1. **Re-read** the governing spec (and this starter, if re-issued) from the committed branch.
2. **State in your next delivery that you did**, naming the file and section.
3. Escalate to the **M48 Milestone Chat** (never past it) if the amendment is blocking.
4. Re-confirm the governing spec is present on the canonical `milestone/M48` branch
   (`git cat-file -e "milestone/M48:<spec>"`) and `git log milestone/M48 -- <spec>` against your
   branch point — pin the ref before `--`; a ref after `--` is read as a pathspec.

---

## Prerequisite Verification (do this first)

Before relying on the spec, confirm it is **git-tracked on the canonical `milestone/M48` branch** —
and likewise every prerequisite artifact it declares — using `git cat-file -e milestone/M48:<path>`
which examines the canonical branch ref **itself**, not the current index/worktree (a stale copy on
an Epic branch must not pass):

```bash
git cat-file -e "milestone/M48:docs/phases/P13__Daily_Fleet_Operation_in_Drivr/P13-M48-E48.2__spec__deterministic-acceptance-live-qualification-separate.md"
```

A `git ls-files --error-unmatch <path>` check reads the index/worktree and a stale Epic-branch copy
can pass; it is **not** the canonical-branch evidence here. Disk presence and index presence are
both non-proof. If the canonical `milestone/M48` ref lacks any prerequisite path, STOP and report to
the M48 Milestone Chat before proceeding.

---

## Summary

**Separate deterministic acceptance from live ComfyUI qualification so identical source and an
unchanged declared test mode produce the same acceptance result — without deleting, mocking, or
silently skipping the live check. Deliver two explicit commands (`pytest -m "not live_endpoint"`
deterministic; `pytest -m live_endpoint` qualification with pass/fail/skip + environment + reason),
regression coverage for available/unavailable/skipped, and a delivery-report integration point
(a committed summary command) that emits deterministic totals and live qualification as two
independent signals.**

---

## Spec References (load-one-reference-the-other, AOG §3.1.1)

**Full spec:** `docs/phases/P13__Daily_Fleet_Operation_in_Drivr/P13-M48-E48.2__spec__deterministic-acceptance-live-qualification-separate.md`

The Epic spec is the authoritative copy of **Deliverables (D1–D4), Definition of Done, Acceptance
Criteria, Binding Constraints, Non-Goals, and Notes.** **This starter does not duplicate them.**
Cite the spec by path and branch — **not** by a version number or sha.

**The four things easiest to get wrong, flagged here and specified there:**

1. **Converting the live check into a hidden skip or mock.** Skip must be recorded and reasoned,
   never silent; a mock never substitutes for the real check.
2. **Folding live qualification into "suite green."** Report two signals: deterministic totals and
   live qualification, never one aggregate.
3. **Leaving the split descriptive instead of executable.** The two commands must be real, committed,
   and tested — not prose.
4. **Changing the helper's exit-code contract.** Exits 2/3/4/5 stay; they are the qualification
   signal, mapped to named failures.

---

## Method Obligations

1. **Bounded claims (M48 Binding Constraint 9)** — repository, ref, date, command/check, and result on every validation claim; no claim covers another repository's suite.
2. **G2 — re-measure.** Re-confirm the live test's current shape, the `skipif` conditions, and the
   helper exit codes before changing anything.
3. **G1 — quote the non-uniform element verbatim.** Quote the marker string, the two commands, and
   the exit codes as produced.
4. **Commit every artifact you author. Untracked is undelivered.**
5. **No skips introduced to route around the change.**
6. **Work in your own worktree.**

---

## Deliverable Artifacts

1. **D1** — Deterministic acceptance boundary (live test marked; `pytest -m "not live_endpoint"`; reproducible-result invariant)
2. **D2** — Live-qualification channel (`pytest -m live_endpoint`; pass/fail/skip + environment + reason)
3. **D3** — Regression coverage for endpoint available, unavailable, and explicitly skipped
4. **D4** — Delivery-report integration point (committed summary command emitting the two independent signals)
5. **Epic Delivery Notice**

---

## Definition of Done

**See the Epic spec §Definition of Done — the authoritative copy.**

Before opening the PR, confirm every item, **including the four easiest to skip**: `pytest -m "not
live_endpoint"` is green and no longer depends on endpoint availability; the reproducible-result
invariant is tested; pass/fail/skip are explicit with environment + reason; and live qualification
is reported separately, never folded into the deterministic totals.

---

## Repository Boundary

All E48.2 work and the Delivery Notice land in **`ai-project-system`** on `epic/E48.2`. The only
cross-repository touchpoint is the local ComfyUI endpoint (`localhost:8188`) as the *external*
live-qualification subject — never as part of the deterministic gate. **Do not claim another
repository's suite is covered by this one.**

---

## Delivery Notice

When you complete execution, produce the Delivery Notice at:

**File:** `docs/phases/P13__Daily_Fleet_Operation_in_Drivr/P13-M48-E48.2__delivery-notice.md`

Use the committed template `governance/templates/delivery-notice.md` (reference, don't restate).
Confirm every spec DoD item in it, including the extent-vs-spec and claims-vs-evidence checks.

---

## Completion

1. Commit code + tests + summary command to `ai-project-system`; **verify that push at `origin`**
2. Open the PR to `milestone/M48`
3. ⏸️ **Stop and await authorization — do not merge**

Declare: *"Epic E48.2 execution complete. Epic Delivery Notice produced. Awaiting Milestone Chat
review and authorization."*

---

## Question Policy

- **Ask only blocking questions.** The deterministic/live boundary's exact mechanism (marker name,
  command form, fixture shape) is **yours**.
- **Escalate to the M48 Milestone Chat** (do not decide) if: the required separation appears to need
  Drivr machinery (no-Drivr-code escalation trigger); or a clean separation conflicts with an
  unchanged `visual_artifacts` contract in a way this spec has not anticipated.
- **CLOSED — do not reopen:** the live check is not deleted/mocked/silently skipped; deterministic
  acceptance must be stable on identical source; `undetermined`/unavailable/opt-out stay distinct.
- **Do not scope in:** changing `visual_artifacts.enabled` semantics; new visual tooling; Drivr code.
- **Do not ask for information already in the Epic spec.**

Escalate to the M48 Milestone Chat for any gap not covered here.
