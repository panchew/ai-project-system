---
project: ai-project-system
phase: P13
milestone: M48
epic: E48.2
type: spec
status: planned
last_updated: 2026-09-08
spec_version: 1.0.0
---

# Epic E48.2 — Deterministic Acceptance, Live Qualification Separate

## Context

**"Suite green" is the gate every acceptance in this phase reports against** — the P13 phase spec
says so, and the M48 milestone spec assigns the non-deterministic part of that gate here (phase-spec
risk "a flaky suite makes every acceptance unfalsifiable"). The source suite mixes deterministic
acceptance with a live endpoint: with `visual_artifacts.enabled: true`, one integration test —
`tests/integration/test_visual_artifacts_helper.py::test_helper_generates_against_endpoint` — runs
for real against local ComfyUI at `http://localhost:8188` by default, and is skipped only when
`visual_artifacts.enabled` is false or `AI_PROJECT_SKIP_LIVE_ENDPOINT_TESTS=1` is set. On a machine
without ComfyUI, or with it stopped or rate-limited, that single test's pass/fail depends on
machine-local endpoint availability rather than on the source.

That is M48 Finding 8, carried here **as the scope, not re-derived**: make deterministic suite status
stable, report live qualification separately, and never convert an expected live check into an
unreported skip or mock.

**Re-measured at planning (2026-09-08, this chat):** the live test exists exactly as Finding 8
describes — a `skipif` on `enabled`/`_SKIP_LIVE_ENDPOINT`, then a real `subprocess.run` against
`bin/ai-project-visual` whose pass requires the endpoint to produce a non-empty artifact. The
helper already returns distinct exits — disabled ⇒ 2, unconfigured type ⇒ 3, unreachable endpoint
⇒ 4, live execution-lock ⇒ 5. What is missing is the **separation layer**: a deterministic
acceptance command that excludes the live call, an explicit live-qualification command that reports
pass/fail/skip with its environment and reason, and regression coverage for all three endpoint
states.

## Problem Statement

Every P13 acceptance reports against "suite green," but that suite today contains one test whose
result depends on whether a local ComfyUI endpoint happens to be reachable. That makes the phase's
primary gate **unfalsifiable** — a red suite proves nothing (was the change wrong, or was ComfyUI
down?) and a green suite proves nothing either (was the endpoint skipped silently?). The phase spec
flags this as a named risk; E48.2 is its assignment: make deterministic acceptance reproducible,
keep live qualification honest and separate, and report the two independently.

## Goals

By the end of this Epic, the system must:

1. Provide an **executable deterministic-acceptance command** whose result is reproducible from
   identical source and an unchanged declared test mode — no machine-local endpoint availability
   entering the pass result.
2. Provide an **executable live-qualification command**, separate and runnable, with explicit
   **pass / fail / skip** semantics and a recorded environment + reason for any skip.
3. Report **deterministic suite totals and live qualification status as two independent signals** in
   every P13 delivery report, never folded into one aggregate.
4. Add **regression coverage for endpoint available, unavailable, and explicitly skipped** states.

## Non-Goals

This Epic explicitly does **not** aim to:

- **Delete, mock, or silently skip the live check.** It stays runnable; it stops being what the
  deterministic gate depends on.
- **Change `visual_artifacts.enabled`'s default or semantics.** The config contract stands.
- **Change `bin/ai-project-visual`'s exit codes** (disabled=2, unconfigured-type=3, unreachable=4,
  live-lock=5). They are the qualification signal, not the acceptance signal.
- **Change Drivr code.** No Drivr implementation is authorized by M48.

## Scope of Work

### 1. The deterministic acceptance boundary (D1)

Introduce an explicit marker/label so the deterministic acceptance command excludes the live
endpoint invocation.

**Must include:**
- A pytest marker on the live test (e.g. `@pytest.mark.live_endpoint`) so the deterministic
  acceptance is a **specific command** — e.g. `pytest -m "not live_endpoint"` — that never reaches
  ComfyUI.
- An **explicit, tested invariant**: identical source + unchanged declared test mode ⇒ identical
  deterministic result, regardless of whether a local ComfyUI happens to be reachable.
- The deterministic command, its exact marker string, and its reported **totals** (passed/failed
  counts) stated in the committed record — not described in prose.

### 2. The live-qualification channel (D2)

Keep live qualification runnable and honest, with its own executable command and result semantics.

**Must include:**
- A **specific command** for live qualification (e.g. `pytest -m live_endpoint`, or a dedicated
  helper invocation) that reports a single **status: pass | fail | skip**, plus the **environment**
  (`visual_artifacts.enabled` value, `comfyui_url`) and the **reason** for any `skip` (opt-out via
  `AI_PROJECT_SKIP_LIVE_ENDPOINT_TESTS=1`, or `enabled: false`).
- **Semantics, explicit**: `pass` = the endpoint produced a non-empty artifact through the real
  path; `fail` = the real call ran and failed (endpoint unreachable or returned no artifact — the
  helper's exit 2/3/4/5 mapped to a named failure); `skip` = a recorded, opted-out or disabled
  state, never a silent default, never a mock.
- **No hidden skip, no mock.** A skip is a recorded, reasoned state; a mock standing in for the live
  endpoint is not acceptable.

### 3. Regression coverage for the three endpoint states (D3)

**Must include** tests covering:
- **available** — deterministic plumbing proof (a controlled fixture / labeled stub that is never
  substituted for the real check in a qualification report);
- **unavailable** — closed port ⇒ the existing exit-4 path;
- **explicitly skipped** — opt-out flag ⇒ not run, reason recorded.

### 4. Delivery-report separator (D4)

Define how a P13 delivery report states the **two independent signals**: deterministic suite totals
(e.g. "deterministic: 773 passed, 0 failed") and live qualification status ("live: pass | fail |
skip — env …, reason …"). One unambiguous source-suite gate (the deterministic totals), with live
qualification reported alongside, never folded in.

## Deliverables

This Epic must produce:

- [ ] **D1** — Deterministic acceptance boundary: the live test marked, the `pytest -m "not live_endpoint"` command, and the tested reproducible-result invariant, committed
- [ ] **D2** — Live-qualification channel: the `pytest -m live_endpoint` command with explicit pass/fail/skip semantics + environment + reason
- [ ] **D3** — Regression coverage for endpoint available, unavailable, and explicitly skipped
- [ ] **D4** — The delivery-report separator: deterministic totals and live qualification stated independently
- [ ] **Epic Delivery Notice**

## Binding Constraints

Settled above this epic — **not re-decidable here** (M48 milestone spec):

1. **The non-deterministic suite is M48's assignment** (phase-spec risk). E48.2 is that assignment.
2. **Static validation is not operational proof**, but the deterministic suite must be honestly
   deterministic; separating live qualification is not the same as deleting it.
3. **An unreported skip or a mock is forbidden.** The live check either runs honestly or is a
   recorded skip.
4. **M48 authorizes no Drivr implementation.**
5. **`undetermined`, unavailable endpoint, and an explicit opt-out remain distinct.** A live check
   that cannot run is reported as such, never relabeled.

## Definition of Done

Epic E48.2 is complete when:

- [ ] `pytest -m "not live_endpoint"` is the committed deterministic-acceptance command and its result no
      longer depends on machine-local endpoint availability
- [ ] The reproducible-result invariant is tested (identical source + unchanged mode ⇒ identical
      deterministic result)
- [ ] `pytest -m live_endpoint` is the committed live-qualification command with explicit
      pass/fail/skip semantics, environment, and reason
- [ ] Regression tests cover endpoint available, unavailable, and explicitly skipped
- [ ] A delivery report states one unambiguous deterministic-suite gate plus a separate live
      qualification status (D4)
- [ ] `pytest -m "not live_endpoint"` is green (deterministic totals); live qualification reported
      separately
- [ ] Every claim states repository, ref, date, and verification command (`P11-GH-2`)
- [ ] Delivery Notice committed; PR opened from `epic/E48.2` to `milestone/M48`

## Acceptance Criteria

- [ ] Deterministic acceptance no longer depends on machine-local endpoint availability
- [ ] Live qualification remains runnable and is never represented as deterministic coverage
- [ ] Pass, fail, and skip semantics are explicit and tested
- [ ] P13 delivery reports can state one unambiguous source-suite gate

## Technical Constraints

**Repository:** all E48.2 code, tests, and the Delivery Notice land in `ai-project-system` on
`epic/E48.2`. The subject source is `tests/integration/test_visual_artifacts_helper.py` and its
supporting `bin/ai-project-visual` / `bin/ai-project-orchestrator` (this repository only). No
write to Drivr or any fleet project.

**Invocation:** `PYTHONPATH=. pytest -m "not live_endpoint"` for deterministic acceptance;
`PYTHONPATH=. pytest -m live_endpoint` for live qualification. The unchanged controls are
`visual_artifacts.enabled` and `AI_PROJECT_SKIP_LIVE_ENDPOINT_TESTS`; no credential value is
involved or read.

**Constraint — no hidden skip, no mock:** a skip is a recorded, reasoned state. A mock that claims
to be the live endpoint is not acceptable; a deterministic stub used *only* to prove the
available-path plumbing is permissible only when clearly labeled and never substituted for the real
live check in a qualification report.

## Dependencies

**Internal**
- **M48 spec** — Finding 8, Binding Constraints, E48.2 Epic Detail.
- **E48.1** — the delivery-report field shape (if the deterministic/live split is recorded in the
  run-record contract); otherwise independent.

**External**
- **local ComfyUI** (`ai-stack/comfyui`, `http://localhost:8188`) — the live qualification subject;
  its availability is external and machine-local by definition.

**Blockers**
- None at planning. If the live endpoint is unreachable at execution time, that is the *expected*
  input to the "unavailable" regression path, not a blocker.

## Timeline

**Estimated Effort:** 1 session.
**Target Completion:** may run in parallel with E48.1 and E48.3.
**Actual Completion:** In progress.

## Execution Notes

**Order:**
1. Re-measure (G2): confirm the live test's current skip/enable shape and the helper exit codes
   before changing anything.
2. Add the `live_endpoint` marker and define the two explicit commands.
3. Implement the pass/fail/skip semantics with environment + reason.
4. Add regression coverage for available / unavailable / skipped; prove the reproducible-result
   invariant.
5. Confirm `pytest -m "not live_endpoint"` is green; run live qualification and report it separately.

**Traps already paid for by this project:**
- **An unreported skip or a mock.** Converting an expected live check into a hidden skip or mock is
  forbidden (Binding Constraint 3).
- **Folding live qualification into "suite green."** They are two lines, never one aggregate.
- **Changing the helper's exit-code contract.** The exits (2/3/4/5) are the qualification signal;
  they stay.

## Related Documents

- [M48 Milestone Spec](P13-M48__milestone-spec.md) — Finding 8, Binding Constraints, E48.2 Epic Detail
- [M48 Milestone Execution Chat Starter](P13-M48__milestone-execution-chat-starter.md)
- [P13 Phase Spec](P13__phase-spec.md) — §Milestones (M48), §Risks
- `tests/integration/test_visual_artifacts_helper.py` — the live endpoint test (subject)
- `bin/ai-project-visual`, `bin/ai-project-orchestrator` — helper + config source
- [PROJECT-SYSTEM-GUIDELINES.md](../../../governance/PROJECT-SYSTEM-GUIDELINES.md)
- [AI-OPERATING-GUIDELINES.md](../../../governance/AI-OPERATING-GUIDELINES.md)

## Notes

- **Separation is not deletion.** The live check stays runnable; it stops being what "suite green"
  depends on.
- **Every P13 acceptance reports against the gate this Epic stabilizes** — that is why the phase
  spec flagged it as a risk worth assigning rather than carrying unowned.

## Visual Bindings

**Visual binding**
- **Link:** https://github.com/panchew/ai-project-system/blob/48b1f9321685a0092878486303a0f48cbe31f999/docs/phases/P13__Daily_Fleet_Operation_in_Drivr/P13-M48-E48.2__spec__deterministic-acceptance-live-qualification-separate.mmd
- **What:** diagram
- **Level:** Epic
- **State:** proposed

- **Description:** E48.2 separates deterministic acceptance from live ComfyUI qualification: a
  `pytest -m "not live_endpoint"` deterministic command whose result is reproducible from identical
  source, and a separate `pytest -m live_endpoint` qualification command with explicit
  pass/fail/skip semantics + environment + reason, reported as two independent signals. Proposed
  Structural diagram; `Link` is an immutable commit permalink.

## Changelog

| Version | Date | Change |
|---|---|---|
| 1.0.0 | 2026-09-08 | Initial E48.2 spec, from the M48 milestone spec v1.2.0 (Finding 8, Binding Constraints, E48.2 Epic Detail). **Re-measured at planning (G2):** `test_helper_generates_against_endpoint` is the single live test — `skipif` on `visual_artifacts.enabled`/`AI_PROJECT_SKIP_LIVE_ENDPOINT_TESTS`, else a real call against `localhost:8188`; the helper returns distinct exits 2/3/4/5. Defines the **executable split** (responding to the deterministic/live-command requirement): a `live_endpoint` pytest marker, two explicit commands (`pytest -m "not live_endpoint"` deterministic; `pytest -m live_endpoint` qualification), explicit pass/fail/skip semantics with environment + reason, regression coverage for available/unavailable/skipped, and a delivery-report separator. No scope, ordering, gate, or acceptance-criterion change from the milestone spec. |
