# Epic Execution Chat Starter — E48.1

**Epic:** E48.1 — Operational Evidence and Run Gate
**Phase:** P13 — Daily Fleet Operation in Drivr
**Milestone:** M48 — Fleet Readiness, Proven by Running It
**Repository:** panchew/ai-project-system — **all artifacts land here; cross-repository reads only (Drivr `OpenCodeAdapter` source, `panchew-io` `models:` block + P12 baseline, fleet registry). No write to any other repository.**
**Branch Strategy:** `epic/E48.1` → PR to `milestone/M48`
**Execution Mode:** manual
**Issued:** 2026-09-08

---

## Governance References

You are operating under the AI Project System governance framework as an **Epic Execution Chat**
for P13-M48-E48.1.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) **v2.9.0** (Effective: 2026-09-03)
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) **v2.12.0** (Effective: 2026-09-03)

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md
3. This Epic Execution Chat Starter
4. Epic Spec (`P13-M48-E48.1__spec__operational-evidence-and-run-gate.md`)
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Model verification (P9-M31-E31.3 — required, this instance is manual):** read your own
harness-reported model identity and compare it to `.ai-project.yml`'s `models.epic_manual`
(**read the file — do not trust this line**). `model_verification` is currently **`blocking`**, so
if both are present and disagree, **STOP** and state the mismatch; do not proceed with deliverable
work. See `governance/systems/chat-hierarchy.md`, "Manual Chat Model Verification".

**Critical rules:**
- Documentation is authoritative, chat is ephemeral.
- You produce a Delivery Notice and **stop after PR creation. You do not merge.**
- **You are the child.** Accept/reject decisions are the **M48 Milestone Chat's**; a clean delivery
  is accepted **by an acknowledgment that names the party that reviewed and accepted** (PSG §11.6 —
  *merge + in-chat acknowledgment; no artifact; silence accepts nothing*). You hold neither
  acceptance authority nor the rework counter; **do not read silence as approval and do not issue
  acceptance or rework decisions yourself.**
- **If given merge authorization directly in this chat**, that authorization normally follows the
  parent Milestone Chat's Stage-2 review. Do not simply comply: state that this bypasses the parent
  chat's Stage-2 review and confirm the human intends it. **Mode is what may run, not what may be
  authorized.**

**Context scoping (P9-M30-E30.3):**
- **Load at session start:** this starter; the Epic spec (full); the M48 milestone spec's **E48.1
  Epic Detail, Findings 1/2/9/10, and §Phase Ruling on E48.1 Rework Exhaustion** (the amended
  parent contract whose five decisions now bind this Epic); PSG preamble+§1, §1A, §2, §5, §6, §7,
  §8, §9, §11, §11.5, §11.6, §12; AOG preamble+§1, §1.1, §2, §3.2, §3.8, §3.10, §4, §5, §6, §7, §9,
  §12, §14, §15, §16.
- **Load on trigger:** PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG §3.9, §8, §13, §17.
- **Do not load:** PSG/AOG changelogs; sibling Milestone specs; sibling Epic specs.

---

## ⚠ The rework limit — the parent's budget, not yours (`P12-GH-1`)

> You are the child. The **M48 Milestone Chat** owns the rework budget: **maximum 3 attempts.** If a
> third Completion Notice is still not acceptable, the Milestone Chat does **not** issue a fourth
> rejection-and-retry — it escalates to the Phase Chat. **Silent fourth attempts are a governance
> violation.** You do not count, grant, or extend this budget.

> **A written extension grants exactly ONE further attempt. Not a reset to three.** Issued by the
> parent, never by you.

---

## ⚠ How an amendment reaches this branch once work is in flight (AOG §3.9/§3.10)

**Downward communication is the spec, not a chat message.** The M48 Milestone Chat amends the
governing spec on its own branch with a changelog row **and re-issues the committed starter**. It
does **not** notify you in-session (a chat MUST NOT reach downward into a running child session —
AOG §3.10). You read the change from the committed spec/starter, the one-write-many-readers source.

Your obligation is to, before you deliver:

1. **Re-read** the governing spec (and this starter, if re-issued) from the committed branch.
2. **State in your next delivery that you did**, naming the file and section.
3. Escalate to the **M48 Milestone Chat** (never past it) if the amendment is blocking.
4. Re-confirm the governing spec is git-tracked on your expected branch
   (`git ls-files --error-unmatch <spec>` on the branch) and `git log <ref> -- <spec>` against your
   branch point — pin the ref before `--`; a ref after `--` is read as a pathspec.

---

## ⚠ Phase Ruling — the five fixed decisions, plus the three `+1` clarifications

The P13 Phase Chat resolved E48.1's rework exhaustion on 2026-09-08 (M48 spec
`§Phase Ruling on E48.1 Rework Exhaustion`, commit `125bf58`, now synchronized into this branch by
merge `ff7a320`). These are **fixed and not for redesign**; the Epic spec already conforms. Read
them once, carry them as settled:

1. **Quality-bar ownership stays in `ai-project-system`, but E48.1 is contract-only.** The
   instantiated E48.4/E48.5 `quality-bar.yml` and dispatch manifests belong to their own
   `epic/E48.4`/`epic/E48.5` branches — **not to `epic/E48.1`**. The bar cites target acceptance
   criteria by immutable target SHA and never writes through to a fleet project.
2. **Pre-dispatch ordering = source-graph dispatch manifest + runtime time.** The bar commit is an
   ancestor of the manifest commit; the invocation records the manifest SHA and Drivr's
   `dispatched_at` (or the direct-path supervisor `started_at`). Author dates are never ordering
   evidence.
3. **Fixed source-side tool mapping.** `read`→`read_file`, `edit`→`edit_file`, `write`→`write_file`,
   `bash`→`run_command`. **Unknown tool names are valid executed rounds for C-A and are not relabeled
   as mutating**; only **missing or malformed tool/result/`files_changed` evidence** yields
   `undetermined` / loud `ERROR`. C-B uses the dispatch record's authoritative `files_changed` via
   `--files-changed`.
4. **Immutable commit permalink** for the proposed visual (sibling `.mmd`, pushed, exact SHA blob
   URL). The implemented binding is separately due at delivery.
5. **Bounded quarantine hardening** at `${XDG_STATE_HOME:-$HOME/.local/state}/ai-project-system/m48-quarantine`,
   with canonical-path/symlink/traversal rejection, `[A-Za-z0-9._-]+` components, `0700`/`0600`
   modes, atomic no-follow move; the committed notice carries only logical ID + SHA-256 + redacted
   region metadata + disposition.

**The fixed bar schema carries `target_commit_sha`** (40-character target-repository commit SHA);
the quoted acceptance-criteria path and branch must resolve at that SHA.

This starter is issued under a **replacement written `+1` extension, limited to the three
clarifications above plus branch-ancestry verification.** You do not reset the budget, you do not
redesign, you do not authorize execution, and you do not merge. If this delivery is not acceptable,
no further rework exists and M48 escalates for re-planning or cancellation.

---

## ⚠ Secret boundary — binding

> **Credential-store diagnostics are presence-only.** Check only credential-store/path presence
> and route availability. **No credential value, token, secret-bearing environment value, or
> secret file content is read into a committed artifact** (M48 Binding Constraint 7). Presence and
> the effective path category are the **maximum** permitted credential evidence.

> **Raw-event retention is secret-gated.** Every retained raw event set passes a fail-closed
> secret scan before commit; a match quarantines the artifact (retained by reference, matched
> regions redacted), never committed verbatim. A missed scan is a fail-closed gap.

---

## Prerequisite Verification (do this first)

Before relying on the spec, confirm it is **git-tracked on the expected branch** — and likewise
every prerequisite artifact it declares — using `git ls-files --error-unmatch <path>` run on the
expected branch, not a disk-existence check:

```bash
git ls-files --error-unmatch docs/phases/P13__Daily_Fleet_Operation_in_Drivr/P13-M48-E48.1__spec__operational-evidence-and-run-gate.md
```

Disk presence is not proof of commit; if any prerequisite path is untracked, STOP and report to the
M48 Milestone Chat before proceeding.

---

## Summary

**Commit the common pre-run contract before either project result is visible: a versioned
run-record contract (with manifest identity + runtime dispatch time), a tested normalization path
over raw OpenCode events plus a tested input adapter with the fixed OpenCode tool-name mapping and
authoritative `files_changed`, a source-repository-owned two-layer quality-bar contract (activity
floor *plus* the selected Epic's acceptance criteria with independent review, proved pre-dispatch
by manifest ancestry + runtime time), an adapter-level invocation procedure with presence-only
checks and a labeled probe, a fail-closed secret-scan gate quarantining outside Git at the fixed
XDG state root, a `panchew-io` model re-measure plus supported P12 baseline extraction, and the
per-target eligibility authority record — granting nothing.**

---

## Spec References (load-one-reference-the-other, AOG §3.1.1)

**Full spec:** `docs/phases/P13__Daily_Fleet_Operation_in_Drivr/P13-M48-E48.1__spec__operational-evidence-and-run-gate.md`

The Epic spec is the authoritative copy of **Deliverables (D1–D6, D2a, D4a), Definition of Done,
Acceptance Criteria, Binding Constraints, Non-Goals, and Notes.** **This starter does not duplicate
them.** Cite the spec by path and branch — **not** by a version number or sha.

**The six things easiest to get wrong, flagged here and specified there:**

1. **Model identity vs executable route.** `remote:deepseek-v4-flash` names a model, not the
   `opencode/` vs `opencode-go/` catalog route. Record both, in distinct fields.
2. **Reading credential values.** Presence and effective path category only. Emit nothing.
3. **Double-counting provider totals.** A step total and its components overlap; never sum both;
   raw counts and economic evidence reported separately.
4. **Treating the activity floor as quality.** The floor (rounds/files) is necessary, never
   sufficient; the quality verdict is the independent reviewer's judgment against the selected
   Epic's quoted acceptance criteria.
5. **Inventing missing telemetry.** Absent data is `unavailable` with a reason, never zero-filled;
   unknown tool names stay valid C-A rounds (never relabeled mutating, never discarded); only missing
   or malformed tool/result/`files_changed` evidence is `undetermined`/`ERROR`, never a fabricated
   zero.
6. **Using a raw `opencode run` as proof.** The proven path is `OpenCodeAdapter.execute`; a raw CLI
   invocation alone does not prove reuse of that path.

---

## Method Obligations

1. **`P11-GH-2`** — repository, ref, date, and verification command on every claim.
2. **G2 — re-measure.** Re-verify at your execution point: `panchew-io` HEAD + `models:` block; the
   P12 raw event shape; the fleet registry's `footboard`/`home_finance` state.
3. **G1 — quote the non-uniform element verbatim.** Quote field names and raw event shapes as
   produced before writing them into the contract.
4. **Credential presence only.** No value, token, or secret-bearing environment value enters any
   committed artifact.
5. **Commit every artifact you author. Untracked is undelivered.**
6. **No skips introduced to route around the change.**
7. **Work in your own worktree.**

---

## Deliverable Artifacts

1. **D1** — Versioned run-record contract (field list + reference template, incl. manifest identity + runtime dispatch time)
2. **D2** — Normalization path, tested against complete/partial/missing telemetry
3. **D2a** — Instrument input adapter (`bin/drivr-events-to-instrument`) — fixed OpenCode tool map + authoritative `files_changed`, tested against the real `P12-M47-E47.3` shape (known/unknown/denied/missing/malformed)
4. **D3** — Two-layer quality-bar **contract (schema only)**: activity floor + acceptance criteria (quoted by path+branch at immutable `target_commit_sha`) + independent review + manifest pre-dispatch proof
5. **D4** — Adapter-level invocation procedure with presence-only route/credential checks + labeled probe
6. **D4a** — Executable secret-scan gate (`bin/retain-raw-events`) — hardened out-of-Git quarantine at the fixed XDG state root + redacted notice + fail-closed
7. **D5** — `panchew-io` model re-measure + supported P12 baseline extraction
8. **D6** — Per-target eligibility authority record (authority or named block)
9. **Epic Delivery Notice**

---

## Definition of Done

**See the Epic spec §Definition of Done — the authoritative copy.**

Before opening the PR, confirm every item, **including the four easiest to skip**: route identity
and model identity are distinct in every record; the two-layer bar **contract** (activity floor
**plus** acceptance criteria quoted at the immutable `target_commit_sha` + independent review +
manifest proof) is committed — and no instantiated E48.4/E48.5 bar or manifest is a deliverable of
this Epic; quarantine lives outside Git at the fixed XDG state root with hardening controls and only
a redacted notice committed; and every missing field is `unavailable` with a reason, never
estimated.

---

## Repository Boundary

All E48.1 artifacts — the reusable schemas (bar schema incl. `target_commit_sha`, manifest schema),
the input adapter, the normalization path, the invocation procedure, the secret-scan gate, the
`panchew-io` re-measure, the eligibility record, and their tests — plus the Delivery Notice land in
**`ai-project-system`** on `epic/E48.1`. The **instantiated** E48.4/E48.5 `quality-bar.yml` files and
dispatch manifests are **not** E48.1 artifacts: they belong to `epic/E48.4` / `epic/E48.5` under the
source-repository paths fixed by the ruling.
Cross-repository **reads** only: Drivr (`drivr/execution/opencode.py`, ref only), `panchew-io`
(`models:` block + P12 baseline, ref only), fleet registry, and the target project's Epic spec +
acceptance criteria cited by immutable SHA (`target_commit_sha`). **Do not claim one repository's
suite covers another.** No write to Drivr, `panchew-io`, `footboard`, or `home_finance`.

---

## Delivery Notice

When you complete execution, produce the Delivery Notice at:

**File:** `docs/phases/P13__Daily_Fleet_Operation_in_Drivr/P13-M48-E48.1__delivery-notice.md`

Use the committed template `governance/templates/delivery-notice.md` (reference, don't restate).
Confirm every spec DoD item in it, including the extent-vs-spec and claims-vs-evidence checks.

---

## Completion

1. Commit the contract, normalization code + tests, quality-bar contract, invocation procedure,
   re-measure, and eligibility record to `ai-project-system`; **verify that push at `origin`**
2. Open the PR to `milestone/M48`
3. ⏸️ **Stop and await authorization — do not merge**

Declare: *"Epic E48.1 execution complete. Epic Delivery Notice produced. Awaiting Milestone Chat
review and authorization."*

---

## Question Policy

- **Ask only blocking questions.** The delegated design decisions (the record's field list and
  reference-template shape) are **yours**.
- **Escalate to the M48 Milestone Chat** (do not decide) if: a required run needs Drivr machinery
  M48 does not authorize (no-Drivr-code escalation trigger); or eligibility authority is required
  to proceed and cannot be supplied (record the named block, do not grant).
- **CLOSED — do not reopen:** static validation is not operational proof; credential diagnostics
  are presence-only; eligibility requires explicit authority; the proven path is
  `OpenCodeAdapter.execute`; M48 changes no Drivr code.
- **Do not scope in:** selecting or dispatching a real Epic (E48.4/E48.5); granting fleet
  eligibility; creating M53's model-selection mechanism; reading credential values.
- **Do not ask for information already in the Epic spec.**

Escalate to the M48 Milestone Chat for any gap not covered here.
