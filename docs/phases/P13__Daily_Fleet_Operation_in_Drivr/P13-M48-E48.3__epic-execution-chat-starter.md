# Epic Execution Chat Starter — E48.3

**Epic:** E48.3 — Enrollment Completes the Delivery Substrate
**Phase:** P13 — Daily Fleet Operation in Drivr
**Milestone:** M48 — Fleet Readiness, Proven by Running It
**Repository:** panchew/ai-project-system — **all code, tests, and the Delivery Notice land here.**
**Branch Strategy:** `epic/E48.3` → PR to `milestone/M48`
**Execution Mode:** manual
**Issued:** 2026-09-09

---

## Governance References

You are operating under the AI Project System governance framework as an **Epic Execution Chat**
for P13-M48-E48.3.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) **v2.9.0** (Effective: 2026-09-03)
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) **v2.12.0** (Effective: 2026-09-03)

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md
3. This Epic Execution Chat Starter
4. Epic Spec (`P13-M48-E48.3__spec__enrollment-completes-the-delivery-substrate.md`)
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Model verification (P9-M31-E31.3 — required, this instance is manual):** read your own
harness-reported model identity and compare it to `.ai-project.yml`'s `models.epic_manual`
(**read the file — do not trust this line**). `model_verification` is currently **`blocking`**, so
if both are present and disagree, **STOP** and state the mismatch. See
`governance/systems/chat-hierarchy.md`, "Manual Chat Model Verification".

**Critical rules:**
- Documentation is authoritative, chat is ephemeral.
- You produce a Delivery Notice and **stop after PR creation. You do not merge.**
- **You are the child.** Accept/reject decisions are the **M48 Milestone Chat's**; a clean delivery
  is accepted **by an acknowledgment that names the party that reviewed and accepted** (PSG §11.6 —
  *merge + in-chat acknowledgment; no artifact; silence accepts nothing*). You hold neither
  acceptance authority nor the rework counter; **do not read silence as approval and do not issue
  acceptance or rework decisions yourself.**
- **If given merge authorization directly in this chat**, that authorization normally follows the
  parent Milestone Chat's Stage-2 review. Do not simply comply: state that this bypasses the parent
  chat's Stage-2 review and confirm the human intends it. **Mode is what may run, not what may be
  authorized.**

**Context scoping (P9-M30-E30.3):**
- **Load at session start:** this starter; the Epic spec (full); the M48 milestone spec's **E48.3
  Epic Detail and Binding Constraints**; PSG preamble+§1, §1A, §2, §5, §6, §7, §8, §9, §11, §11.5,
  §11.6, §12; AOG preamble+§1, §1.1, §2, §3.2, §3.8, §3.10, §4, §5, §6, §7, §9, §12, §14, §15, §16.
- **Load on trigger:** PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG §3.9, §8, §13, §17.
- **Do not load:** PSG/AOG changelogs; sibling Milestone/Epic specs.

---

## ⚠ The rework limit — the parent's budget, not yours (`P12-GH-1`)

> You are the child. The **M48 Milestone Chat** owns the rework budget: **maximum 3 attempts.** If a
> third Completion Notice is still not acceptable, the Milestone Chat does **not** issue a fourth
> rejection-and-retry — it escalates to the Phase Chat. **Silent fourth attempts are a governance
> violation.** You do not count, grant, or extend this budget.

> **A written extension grants exactly ONE further attempt. Not a reset to three.** Issued by the
> parent, never by you.

---

## ⚠ How an amendment reaches this branch once work is in flight (AOG §3.9/§3.10)

**Downward communication is the spec, not a chat message.** The M48 Milestone Chat amends the
governing spec on its own branch with a changelog row **and re-issues the committed starter**. It
does **not** notify you in-session (a chat MUST NOT reach downward into a running child session —
AOG §3.10). You read the change from the committed spec/starter, the one-write-many-readers source.

Your obligation is to, before you deliver:

1. **Re-read** the governing spec (and this starter, if re-issued) from the committed branch.
2. **State in your next delivery that you did**, naming the file and section.
3. Escalate to the **M48 Milestone Chat** (never past it) if the amendment is blocking.
4. Re-confirm the governing spec is present on the refreshed canonical branch:
   `git fetch origin milestone/M48`, then `git cat-file -e "origin/milestone/M48:<spec>"`, and
   `git log origin/milestone/M48 -- <spec>` against your branch point — pin the ref before `--`; a
   ref after `--` is read as a pathspec.

---

## ⚠ Secret boundary — binding

> **Never create, store, or print credentials.** The remote URL and tracking branch are
> configuration; no token, PAT, SSH passphrase, or secret-bearing value is accepted into, stored
> in, or printed by the enrollment path. Diagnostics contain no credential values.

---

## Prerequisite Verification (do this first)

Confirm **every** prerequisite artifact is git-tracked on the refreshed canonical
`origin/milestone/M48` — not just the Epic spec. A stale local `milestone/M48` must not pass, so
**fetch the remote-tracking ref first**, then inspect each path, and record the branch-point
comparison:

```bash
git fetch origin milestone/M48
for p in \
  "docs/phases/P13__Daily_Fleet_Operation_in_Drivr/P13-M48-E48.3__spec__enrollment-completes-the-delivery-substrate.md" \
  "docs/phases/P13__Daily_Fleet_Operation_in_Drivr/P13-M48-E48.3__epic-execution-chat-starter.md" \
  "docs/phases/P13__Daily_Fleet_Operation_in_Drivr/P13-M48__milestone-spec.md" \
  ".ai-project/registry/fleet-registry.yml"; do
  git cat-file -e "origin/milestone/M48:$p" || { echo "MISSING on origin/milestone/M48: $p"; exit 1; }
done
# Branch-point comparison: your branch must descend from the canonical tip.
git merge-base --is-ancestor origin/milestone/M48 HEAD \
  || { echo "HEAD does not descend from origin/milestone/M48"; exit 1; }
git log origin/milestone/M48 -1 --oneline
```

Disk presence, index presence, an un-refreshed local ref, and a spec-only check are all non-proof. If
any prerequisite path is missing on `origin/milestone/M48`, or `HEAD` does not descend from it, STOP
and report to the M48 Milestone Chat before proceeding.

---

## Summary

**Close SN-45 with one executable enrollment command: mutually exclusive `--remote`/`--local-only`
inputs, an output schema with a six-state reason taxonomy, three-part verification (tracking
recorded, branch pushed, ref equality at final `HEAD`) as the exclusive `delivery-ready` predicate,
a durable non-delivery-ready local-only disposition, mutation ordering/recovery (post-amend, final
HEAD, existing origin, partial push, idempotent retry), and secret safety (reject credential-bearing
URLs, redact Git diagnostics) — keeping enrollment distinct from fleet eligibility and dispatch, and
never creating, storing, or printing credentials.**

---

## Spec References (load-one-reference-the-other, AOG §3.1.1)

**Full spec:** `docs/phases/P13__Daily_Fleet_Operation_in_Drivr/P13-M48-E48.3__spec__enrollment-completes-the-delivery-substrate.md`

The Epic spec is the authoritative copy of **Deliverables (D1–D6, D2a), Definition of Done,
Acceptance Criteria, Binding Constraints, Non-Goals, and Notes.** **This starter does not duplicate
them.** Cite the spec by path and branch — **not** by a version number or sha.

**The six things easiest to get wrong, flagged here and specified there:**

1. **Reachability as verification.** `git ls-remote` answering is not a relationship; enrollment
   proves tracking + pushed + ref equality at final `HEAD`.
2. **Local-only satisfying delivery-ready.** It is a valid, durable disposition but explicitly
   non-delivery-ready; only the three-part verification emits `delivery-ready`.
3. **No executable contract.** One committed command, mutually exclusive inputs, output schema, reason
   taxonomy, exit semantics — not prose.
4. **Pre-amend SHA.** Enroll after the initializer's final commit amendment and verify against final
   `HEAD`.
5. **Emitting credentials.** Reject credential-bearing URLs before `.git/config`; redact Git
   diagnostics; the path never reads or prints authentication material.
6. **Letting enrollment change fleet state.** It configures a remote and records a disposition; it
   never activates, never dispatches, never writes fleet registry state.

---

## Method Obligations

1. **Bounded claims (M48 Binding Constraint 9)** — repository, ref, date, command/check, and result on every validation claim; no claim covers another repository's suite.
2. **G2 — re-measure.** Re-confirm `bin/ai-project-init`'s current git-related flags and the absence
   of any project-remote path before changing it.
3. **G1 — quote the non-uniform element verbatim.** Quote the exact flag/exit/message strings you
   add or change.
4. **No credentials.** Local bare-repo fixtures; no network, no token.
5. **Commit every artifact you author. Untracked is undelivered.**
6. **No skips introduced to route around the change.**
7. **Work in your own worktree.**

---

## Deliverable Artifacts

1. **D1** — `bin/ai-project-enroll`: mutually exclusive inputs, output schema, reason taxonomy, exit semantics
2. **D2** — Three-part verification (tracking + pushed + ref equality at final `HEAD`) as the exclusive `delivery-ready` predicate
3. **D2a** — The durable local-only disposition record, explicitly non-delivery-ready
4. **D3** — Documented eligibility boundary (enrollment ≠ fleet activation ≠ dispatch)
5. **D4** — Mutation ordering + recovery (post-amend, final HEAD, existing origin, partial push, retry/idempotency)
6. **D5** — Secret safety: credential-URL rejection + Git-diagnostic redaction + absence tests
7. **D6** — Regression coverage for all states + ordering/recovery + secret safety
8. **Epic Delivery Notice**

---

## Definition of Done

**See the Epic spec §Definition of Done — the authoritative copy.**

Before opening the PR, confirm every item, **including the four easiest to skip**: `delivery-ready`
is emitted **only** by the three-part verification and a durable local-only disposition is
**non-delivery-ready**; the six reason states (absent / credential-absent / authenticated-refusal /
verification-failure / local-only / undetermined) are distinct and tested; enrollment runs after the
final amend and verifies final `HEAD`; and credential-bearing URLs are rejected with no secret in
config or output.

---

## Repository Boundary

All E48.3 work and the Delivery Notice land in **`ai-project-system`** on `epic/E48.3`. The subject
is `bin/ai-project-init` and the new `bin/ai-project-enroll`, plus their tests — this repository
only. Enrollment verification uses local bare-repo fixtures; secret-safety tests use a synthetic
credential-bearing URL fixture and never contact a real remote or read a real credential.

---

## Delivery Notice

When you complete execution, produce the Delivery Notice at:

**File:** `docs/phases/P13__Daily_Fleet_Operation_in_Drivr/P13-M48-E48.3__delivery-notice.md`

Use the committed template `governance/templates/delivery-notice.md` (reference, don't restate).
Confirm every spec DoD item in it, including the extent-vs-spec and claims-vs-evidence checks.

---

## Completion — follow PSG §1A, in order (do not open the PR early)

1. Commit all deliverables to `ai-project-system`; **verify that push at `origin`**
2. Produce the **Epic Delivery Notice**; declare execution complete
3. ⏸️ **Stop and await human review and the M48 Milestone Chat's explicit delivery authorization** —
   a PR is created **only after** explicit authorization (PSG §1A step 6), and the parent performs
   the merge (PSG §11.6). Do not open the PR before authorization; do not merge.

Declare: *"Epic E48.3 execution complete. Epic Delivery Notice produced. Awaiting Milestone Chat
review and delivery authorization."*

---

## Question Policy

- **Ask only blocking questions.** The exact option name/form (`--remote` vs a post-init step) and
  the disposition artifact's shape are **yours**.
- **Escalate to the M48 Milestone Chat** (do not decide) if: the enrollment work appears to need
  Drivr machinery (no-Drivr-code escalation trigger); or a remote requires credentials that the
  fail-closed path cannot represent without creating/storing a secret.
- **CLOSED — do not reopen:** enrollment is config not eligibility; enrollment never activates or
  dispatches; never create/store/print credentials; an authenticated refusal is distinct from "no
  remote."
- **Do not scope in:** fleet activation/dispatch (M50+); fleet-registry state changes; Drivr code.
- **Do not ask for information already in the Epic spec.**

Escalate to the M48 Milestone Chat for any gap not covered here.
