---
type: delivery-notice
epic: E25.3
status: delivered
delivery_date: 2026-07-07
---

# Epic Delivery Notice — E25.3

**Epic:** E25.3 — Align `ai-project-init` agent path (P6-GH-11)
**Status:** Execution Complete, Delivered to HQ for Review
**Delivery Date:** 2026-07-07

## Execution Summary

Changed `bin/ai-project-init` to write the canonical `.ai-project/agents/` path in all three
occurrences (L133 scaffolding `mkdir`, L327 `agents_dir` in `install_hq_agent()`, L408 `git add`).
Took **Option A**: the initializer now writes **only** `.ai-project/agents/hq.agent.md`; it no
longer writes a `.github/agents/` copy at all. Added `tests/test_init_agent_path.py`, which runs the
initializer end-to-end against a temp directory (`--skip-git --skip-submodule`) and asserts the
canonical file exists, is non-empty, and that `.github/agents/` is not created. Reconciled
`governance/guides/QUICK-START.md` (the "CLI also writes a copy to `.github/agents/`" claim at the
old L92, the verify-structure tree's `.github/agents/` block, and the File Locations table row) and
the sibling integration guide `governance/guides/integrations/github-copilot.md` (the claim that
"the CLI already creates `.github/agents/governance.agent.md`"), so the docs no longer claim the CLI
writes a path it doesn't.

## Deliverables Completed
- [x] `bin/ai-project-init` writes `.ai-project/agents/` (L133/L327/L408)
- [x] Test asserting `.ai-project/agents/hq.agent.md` (`tests/test_init_agent_path.py`)
- [x] QUICK-START.md (+ `integrations/github-copilot.md`) reconciled
- [x] Delivery Notice

## Definition of Done Status
- [x] `bin/ai-project-init` writes `.ai-project/agents/hq.agent.md` (L133/L327/L408 aligned)
- [x] A test asserts the `.ai-project/agents/` path (added; none existed before)
- [x] QUICK-START (+ `github-copilot.md`) agrees with the script — no claim the CLI writes a path it
      doesn't
- [x] The `.github/agents/` copy is dropped (Option A); docs reflect actual behavior
- [x] Full test suite passes: `260 passed, 1 skipped` (259 passed/1 skipped baseline + the new test)
- [x] Delivery Notice produced and committed
- [x] All changes committed to `epic/P6-M25-E25.3`
- [x] Pull request opened to `milestone/M25`

## Branch and PR Information
- **Branch:** `epic/P6-M25-E25.3`
- **PR:** https://github.com/panchew/ai-project-system/pull/108
- **Target:** `milestone/M25`

## Notes
- **Option A taken** (recommended by both the phase spec and Epic spec): write only
  `.ai-project/agents/`; no `.github/agents/` copy. This was the simplest option and matches the
  framework's "tool-neutral canonical path" intent — a Copilot-specific copy remains available as a
  manual/tooling step per `governance/guides/integrations/github-copilot.md`, not an automatic CLI
  side effect.
- **Filename mismatch flagged, not fixed:** the script installs `hq.agent.md`; several docs
  (QUICK-START.md, ADOPTION-GUIDE.md, the integration guides) refer to `governance.agent.md`. This is
  a separate, pre-existing discrepancy per the Epic spec's Non-Goals — noted here as a candidate
  follow-up, out of scope for E25.3.
- **Scope note:** `governance/guides/ADOPTION-GUIDE.md` and `docs/systems/cli-usage-guide.md` also
  contain claims about the CLI writing `.github/agents/` (e.g., ADOPTION-GUIDE.md's sample CLI output
  and "Note" at Step 1). These were left untouched — they are adoption/CLI-usage guides, not
  "Tool Integration Guides," and are outside this Epic's scoped file list
  (`bin/ai-project-init`, one test file, `QUICK-START.md` + sibling *integration* guides). Flagged as
  a candidate follow-up alongside the filename mismatch.
- E25.3 is the last M25 epic; on merge, M25 is ready for its Closure Declaration.

## Awaiting HQ Review
1. Human Review
2. (Epic Review Seal — exception path)
3. HQ acceptance decision
4. HQ merge authorization

---
**Coding Agent stopped per AI-OPERATING-GUIDELINES.md canonical happy path.**
