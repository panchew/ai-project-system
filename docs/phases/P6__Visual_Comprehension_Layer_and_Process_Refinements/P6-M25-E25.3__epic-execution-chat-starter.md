# Epic Execution Chat Starter — E25.3

**Epic:** E25.3 — Align `ai-project-init` agent path (P6-GH-11)
**Phase:** P6 — Visual Comprehension Layer and Process Refinements
**Milestone:** M25 — Process Refinements
**Repository:** panchew/ai-project-system
**Branch Strategy:** `epic/P6-M25-E25.3` → PR to `milestone/M25`

> **Provenance:** Authored by the Milestone Chat (P6-M25) on 2026-07-03 under GH-8 adjacency. E25.3 is
> the last M25 epic. The Coding Agent performs the edits.

---

## Governance References

You are operating under the AI Project System governance framework (Epic mode).

**Authoritative governance documents:**
- PROJECT-SYSTEM-GUIDELINES.md v2.3.0
- AI-OPERATING-GUIDELINES.md v2.6.0

**Governance hierarchy:** PSG > AOG > this Starter > Epic Spec > execution decisions > system references > chat.

**Critical rules:**
- Documentation is authoritative; chat is ephemeral
- Follow the §1A canonical happy path for Epic closure
- Produce a Delivery Notice upon completion
- Stop after PR creation and await HQ authorization for merge
- Accept/reject decisions are made by HQ Chat (human), not by you

---

## Epic Specification

**Full spec:** `docs/phases/P6__Visual_Comprehension_Layer_and_Process_Refinements/P6-M25-E25.3__spec__align-ai-project-init-agent-path.md`
**Commit:** `8cf3962` (on `milestone/M25`)

### Prerequisite Verification (do this first)
- `git ls-files --error-unmatch <spec path>` — confirm the spec is git-tracked (GH-1).
- Confirm the surfaces exist on `milestone/M25`: `bin/ai-project-init` (writes `.github/agents/` at L133/L327/L408), `governance/guides/QUICK-START.md`, `tests/` (no test references either agent path yet).

### Summary

Change `bin/ai-project-init` to write the **canonical `.ai-project/agents/`** path (not
`.github/agents/`), **add a test** asserting it (none exists), and **reconcile QUICK-START** so docs
and script agree. Small, self-contained (script + test + doc); fully independent of the other M25
epics.

### Grounding (verified on `milestone/M25`)
- `bin/ai-project-init`: `mkdir -p "$project_dir/.github/agents"` (L133); `agents_dir="$project_dir/.github/agents"` (L327, drives `dest_file` L329 + `mkdir` L331); `git add .github/agents/hq.agent.md` (L408). Installed file: **`hq.agent.md`** (source `governance/agents/hq.agent.md`).
- **No test** references `.github/agents` or `.ai-project/agents` — add one.
- **QUICK-START.md**: `.ai-project/agents/…` canonical (L92/L166/L735) + `.github/agents/…` Copilot copy (L736); L92 parenthetical claims "the CLI also writes a copy to `.github/agents/`".
- **Filename mismatch (flag, do not fix):** script installs `hq.agent.md`; QUICK-START says `governance.agent.md`. Separate pre-existing discrepancy — note in the Delivery Notice, do not resolve here.

---

## Required Content

**1. `bin/ai-project-init` → canonical path** (change all three):
- L327 `agents_dir="$project_dir/.ai-project/agents"` (fixes L329 `dest_file` + L331 `mkdir`, which derive from it)
- L133 `mkdir -p "$project_dir/.ai-project/agents"`
- L408 `git add .ai-project/agents/hq.agent.md`

**Design call — the `.github/agents/` copy** (phase spec: canonical write to `.ai-project/agents/`, "not `.github/agents/`"):
- **Option A (recommended):** write **only** `.ai-project/agents/`; reconcile QUICK-START (L92 parenthetical + L736) so it no longer claims a `.github/agents/` copy the CLI doesn't write.
- **Option B:** write `.ai-project/agents/` **and** a `.github/agents/` Copilot copy; QUICK-START L92/L736 already agree — ensure the script actually writes both.
- Either way: **canonical write MUST be `.ai-project/agents/`**, and **docs must match the final script behavior**.

**2. Add a test** (in `tests/`, matching the existing style/fixtures): run the initializer against a temp project and **assert `.ai-project/agents/hq.agent.md` exists**. (Option B may also assert the `.github/agents/` copy; the required assertion is the canonical path.)

**3. Reconcile docs:** QUICK-START.md so the CLI's documented behavior matches the script (path = `.ai-project/agents/`; the `.github/agents/` mention accurate for the Option taken). Align any sibling integration guide that names the init agent path.

---

## Deliverables
1. [ ] `bin/ai-project-init` writing `.ai-project/agents/` (L133/L327/L408)
2. [ ] A test asserting `.ai-project/agents/hq.agent.md` is written
3. [ ] QUICK-START.md (+ any sibling) reconciled so docs and script agree
4. [ ] **Epic Delivery Notice** (noting the Option A/B taken + the `hq`-vs-`governance` filename flag)

---

## Definition of Done
- [ ] `bin/ai-project-init` writes `.ai-project/agents/hq.agent.md` (L133/L327/L408 aligned)
- [ ] A test asserts the `.ai-project/agents/` path (added; none existed)
- [ ] QUICK-START (and any sibling) agrees with the script (no claim the CLI writes a path it doesn't)
- [ ] The `.github/agents/` copy is dropped (A) or retained consistently (B); docs reflect actual behavior
- [ ] Full test suite passes (`pytest`), including the new test
- [ ] Delivery Notice produced and committed (Option taken + filename-mismatch flag); PR opened to `milestone/M25`

---

## Acceptance Criteria
- [ ] `bin/ai-project-init` writes `.ai-project/agents/`
- [ ] Test coverage asserts it; docs and script agree
- [ ] Path alignment (+ test + doc), not a refactor; suite green

---

## Technical Constraints
- **Languages:** Bash (`bin/ai-project-init`), pytest (`tests/`), Markdown (QUICK-START + siblings).
- **Surgical:** three path edits (centralize on `agents_dir`), one test, doc reconcile. No refactor.
- **Docs must match the script** (agreement is the bar; Option A or B fine).
- **Scope of files:** `bin/ai-project-init`, one `tests/` file, `governance/guides/QUICK-START.md` (+ any sibling init-path guide). Do **NOT** touch `governance/agents/`, the agent content, PSG/AOG/templates, or any M23/M24 visual surface.
- **Do not resolve** the `hq.agent.md`-vs-`governance.agent.md` filename mismatch (flag only).

---

## Execution Contract

### Your Responsibilities (Coding Agent)
1. Create branch `epic/P6-M25-E25.3` from `milestone/M25`
2. Verify the spec is git-tracked; read `bin/ai-project-init` (L133/L327/L408) + QUICK-START
3. Change the three occurrences to `.ai-project/agents/`; pick Option A/B for the `.github/agents/` copy
4. Add the test asserting `.ai-project/agents/hq.agent.md`
5. Reconcile QUICK-START (+ siblings) so docs match the script
6. Run `pytest` (green, incl. new test); commit to `epic/P6-M25-E25.3`; open a PR to `milestone/M25`
7. Produce the Epic Delivery Notice (Option taken + filename-mismatch flag); stop and await HQ authorization (do not merge)

### What You Must NOT Do
- ❌ Merge the PR / infer acceptance
- ❌ Refactor the initializer
- ❌ Resolve the `hq.agent.md` vs `governance.agent.md` filename mismatch (flag only)
- ❌ Change `governance/agents/` or the agent file content
- ❌ Touch PSG/AOG/templates (E25.1/2/4/5) or any M23/M24 visual surface
- ❌ Iterate without explicit HQ instruction

---

## Reference Materials
- `docs/phases/P6__.../P6-M25-E25.3__spec__align-ai-project-init-agent-path.md` (your Epic spec — authoritative)
- `bin/ai-project-init` (L133/L327/L408)
- `governance/guides/QUICK-START.md` (L92/L166/L735/L736)
- `tests/` (add the initializer agent-path test, matching existing style)

---

## Canonical Happy Path (reminder)
Execution → Delivery Notice → **[STOP]** → Human Review → (Epic Review Seal, exception path) → HQ decision → HQ merge authorization → merge → stop. **You own steps 1–2 only.**

## Epic Delivery Notice Template

**File:** `docs/phases/P6__Visual_Comprehension_Layer_and_Process_Refinements/P6-M25-E25.3__delivery-notice.md`

```markdown
---
type: delivery-notice
epic: E25.3
status: delivered
delivery_date: <YYYY-MM-DD>
---

# Epic Delivery Notice — E25.3

**Epic:** E25.3 — Align `ai-project-init` agent path (P6-GH-11)
**Status:** Execution Complete, Delivered to HQ for Review
**Delivery Date:** <YYYY-MM-DD>

## Execution Summary
[Path change; Option A/B taken; the test added; the QUICK-START reconcile]

## Deliverables Completed
- [ ] bin/ai-project-init writes .ai-project/agents/ (L133/L327/L408)
- [ ] Test asserting .ai-project/agents/hq.agent.md
- [ ] QUICK-START (+ siblings) reconciled
- [ ] Delivery Notice

## Definition of Done Status
[Confirm each — incl. "docs and script agree", "new test passes", "suite green", Option A/B, filename-mismatch flag]

## Branch and PR Information
- **Branch:** `epic/P6-M25-E25.3`
- **PR:** [Link]
- **Target:** `milestone/M25`

## Notes
[Option A/B rationale; the hq.agent.md-vs-governance.agent.md filename mismatch flagged as a follow-up]

## Awaiting HQ Review
1. Human Review
2. (Epic Review Seal — exception path)
3. HQ acceptance decision
4. HQ merge authorization

---
**Coding Agent stopped per AI-OPERATING-GUIDELINES.md canonical happy path.**
```

---

## Exit Conditions
Stop when all deliverables committed, PR opened to `milestone/M25`, Delivery Notice produced. Then report: "Epic E25.3 execution complete. Epic Delivery Notice produced. Awaiting HQ review and authorization."

## Begin Execution
1. Create branch `epic/P6-M25-E25.3` from `milestone/M25`
2. Verify the spec is git-tracked; read `bin/ai-project-init` + QUICK-START
3. Implement deliverables per Definition of Done — align the path, add the test, reconcile the docs

Copy the entire chat starter above and paste into the Governance Agent (Epic mode) to begin execution.
