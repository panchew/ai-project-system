---
type: delivery-notice
epic: E24.1
status: delivered
delivery_date: 2026-06-29
---

# Epic Delivery Notice — E24.1

**Epic:** E24.1 — Proposed-vs-implemented comprehension behavior (P6-VC-3)
**Status:** Execution Complete, Delivered to HQ for Review
**Delivery Date:** 2026-06-29

## Execution Summary

Turned M23's binding *mechanism* into routine *behavior*. Added a new AOG subsection (**§16.6
"Proposed vs. implemented"**) establishing the two-track expectation as the routine default when
`visual_artifacts.enabled: true` — every level produces **both** a *proposed* (intent, before build)
and an *implemented* (after) visual — with the **"nothing is too much"** coverage bar and a
**Structural-first** preference that keeps generous coverage cheap. The behavior is recorded through
the existing **§7 `State` field**, referenced (not restated). Extended guide §5 so every level
(Creation / HQ / Phase / Milestone / Epic) shows a *proposed* and an *implemented* worked example,
each recorded as a §7 binding distinguished by `State`. Documentation only — no code, helper, schema,
or config change.

## Deliverables Completed

- [x] AOG §16 subsection (two-track default, "nothing is too much", Structural-first) + §16.2/§16.3 touch + Changelog row (v2.4.0)
- [x] Guide §5 per-level proposed/implemented examples (Creation / HQ / Phase / Milestone / Epic), each bound via §7 (State) + Changelog row
- [x] Delivery Notice

## Definition of Done Status

- [x] AOG §16 has a subsection (§16.6) directing **both** a proposed and an implemented visual per level when `enabled: true`, with the "nothing is too much" bar and Structural-first preference stated
- [x] Guide §5 shows a per-level proposed/implemented example for **every** level (Creation / HQ / Phase / Milestone / Epic), each bound via the §7 `State` field
- [x] The §7 schema is **referenced, not redefined or restated** — the five-element block and `State` field are unchanged; §16.6 and §5 point at §7 ("schema in §7", "documented once in §7") and the schema-template/table in §7 are byte-for-byte unchanged
- [x] By-link unchanged — every example binds a **link** (hosted render URL for generative; hosted permalink for inline structural diagrams); no new `.ai-project/visuals/...` committed path (grep finds only the pre-existing §7 enforcement prose)
- [x] Behavior recorded in the **AOG Changelog** (minor bump → v2.4.0) and the **guide Changelog**
- [x] Full test suite passes — `259 passed, 1 skipped`; no plumbing touched; the visual integration test still skips at `enabled: false`
- [x] Delivery Notice produced and committed
- [x] All changes committed to `epic/P6-M24-E24.1`
- [x] Pull request opened to `milestone/M24`

## Branch and PR Information

- **Branch:** `epic/P6-M24-E24.1`
- **PR:** <!-- PR_URL -->
- **Target:** `milestone/M24`

## Notes

- **Chosen §16 subsection number:** §16.6 (following §16.1–16.5), inserted after §16.5 and before the
  section-closing "See guide" pointer; §16's existing subsections were not restructured.
- **Per-level example choices:** Structural pairs (two cheap text diagrams) for HQ / Phase / Milestone
  — each implemented diagram shows a realistic divergence from its proposed counterpart (HQ: a cache
  added; Phase: reporting pulled in, validation deferred; Milestone: a validation step added).
  Generative pairs for Creation (concept → shipped hero) and Epic (mockup → shipped screen), where a
  render communicates better — this demonstrates the Structural-first economy (3 of 5 levels free).
- **`What` across a pair:** a pair differs by `State`; for Epic it also shifts `What` (`mockup` →
  `image`) because the medium changes from a design mockup to a screenshot. The §5 intro states this
  explicitly so the "same block, different State" point stays clear.
- **Structural binding `Link`:** structural diagrams live inline (free); their binding `Link` is a
  hosted permalink to the committed diagram — a hosted URL, never a local `.ai-project/visuals/` path —
  so by-link is honored without implying every diagram must be re-hosted as a binary.
- **No scope creep:** clips (E24.2), templates, the integration test, `ai-project-yml-spec.md` §3.5,
  the helper/producer, and §16.5 / guide §4 storage prose were not touched.

## Awaiting HQ Review

1. Human Review
2. Epic Review Seal
3. HQ acceptance decision
4. HQ merge authorization

---
**Coding Agent stopped per AI-OPERATING-GUIDELINES.md canonical happy path.**
