---
type: delivery-notice
epic: E23.1
status: delivered
delivery_date: 2026-06-29
---

# Epic Delivery Notice — E23.1

**Epic:** E23.1 — By-link storage reconciliation (P6-VC-1)
**Status:** Execution Complete, Delivered to HQ for Review
**Delivery Date:** 2026-06-29

## Execution Summary

Reversed the v5.0.0 **commit-the-binary** storage guidance to **by-link** across the named surfaces,
leaving **no** instruction to commit a generated binary anywhere, and recorded the change **as a
reversal of v5.0.0** in both the AOG changelog and a new guide changelog. This is
governance/documentation only — the helper's runtime behaviour is unchanged; only its output
*guidance* (docstring example path and `--output` help text) was clarified. Structural-diagram
(Mermaid/PlantUML) guidance was left untouched, and the integration test (already by-link-consistent)
was verified and recorded with no make-work edit.

Per the SN-16 binding decision (storage by link; adopter owns the storage backend), the residual
`.ai-project/visuals/...` commit paths were swept from every edited surface. A post-edit grep
confirms **zero** `.ai-project/visuals/` references remain in the four surfaces, and the only
remaining "commit" phrasing in the guide is the structural-diagram guidance (correctly preserved).

## Deliverables Completed

- [x] Guide by-link (§4 prose, §1 note, §3 + §5 example paths, related-docs phrase) + guide changelog
- [x] AOG §16.5 by-link (generated + source-repo bullets), version 2.3.0 + Changelog row
- [x] Helper output guidance reframed (docstring + `--output` help), no behaviour change
- [x] Integration test verified already by-link-consistent (recorded, no edit)
- [x] Delivery Notice

## Definition of Done Status

- [x] Guide §4 prose, §1 note, and all `--output` example paths (§3 + §5) describe by-link; **no
      commit-the-binary instruction remains anywhere** in the guide (grep-verified — residual
      `.ai-project/visuals/` paths removed; the related-docs "commit guidance" phrase reconciled to
      "by-link storage guidance")
- [x] AOG §16.5 describes by-link; no commit-the-binary instruction remains; **version 2.2.0 → 2.3.0**
      with a Changelog row
- [x] `bin/ai-project-visual` output guidance frames `--output` as a local working file; **runtime
      behaviour unchanged** — same args, dest, `required`, exit codes, and generation path (diff is
      docstring + help string only)
- [x] Integration-test surface verified by-link-consistent (`out = tmp_path / "out.png"`,
      `out.stat().st_size > 0`, no committed-binary assertion under `.ai-project/visuals/`) and
      **recorded here with no make-work edit**
- [x] The reversal is recorded **as a reversal of v5.0.0** in the AOG changelog **and** the new guide
      changelog
- [x] Structural-diagram (Mermaid/PlantUML) guidance is unchanged (AOG §16.5 structural bullet, guide
      §2, §5 mermaid blocks, and §4 format table all untouched)
- [x] **Full test suite passes** (`pytest`: 259 passed, 1 skipped); the integration test still
      **skips** at `enabled: false` ("visual_artifacts.enabled is false (repo default); no live
      ComfyUI endpoint to exercise")
- [x] Delivery Notice produced and committed
- [x] All changes committed to `epic/P6-M23-E23.1`
- [x] Pull request opened to `milestone/M23`

## Branch and PR Information

- **Branch:** `epic/P6-M23-E23.1`
- **PR:** [#97](https://github.com/panchew/ai-project-system/pull/97)
- **Target:** `milestone/M23`

## Notes

- **Guide changelog placement:** the guide had no changelog today; a new `## Changelog` section
  (date-keyed, since the guide carries no version header) was added at the end of the file, after
  "Related documents."
- **Integration test needed no change:** it already writes to `tmp_path` and asserts non-empty bytes
  with no committed-binary assertion. Verified and recorded — no make-work edit, per the spec.
- **`--output` example convention:** reconciled example paths to local working files in the current
  directory (e.g. `./architecture.png`, `./vision.png`, `./E12.3-settings-mockup.png`), with prose
  and an inline comment making clear the file is hosted and linked, not committed.
- **No upload/link-emitting step added** (Open Design Question B resolved — not reopened). Hosting +
  linking is the agent's job, to be bound by **E23.2**. No forward reference to the unmerged E23.2
  epic was added to the shipped guide to avoid a dangling reference.
- **E23.2 scope untouched:** the binding schema, templates, and `seed.md` were not modified.

## Awaiting HQ Review
1. Human Review
2. Epic Review Seal
3. HQ acceptance decision
4. HQ merge authorization

---
**Coding Agent stopped per AI-OPERATING-GUIDELINES.md canonical happy path.**
