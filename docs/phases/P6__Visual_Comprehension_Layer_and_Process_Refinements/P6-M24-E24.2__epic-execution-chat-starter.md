# Epic Execution Chat Starter — E24.2

**Epic:** E24.2 — Clips as documentation + publishable media (P6-VC-4)
**Phase:** P6 — Visual Comprehension Layer and Process Refinements
**Milestone:** M24 — Comprehension Behavior and Clips
**Repository:** panchew/ai-project-system
**Branch Strategy:** `epic/P6-M24-E24.2` → PR to `milestone/M24`

> **Provenance:** Authored by the Milestone Chat (P6-M24) under its own authority on 2026-07-02,
> after E24.1 was accepted and merged (`0dcd14a`). Per the GH-8 adjacency rule, the Phase Chat
> produced only the Milestone spec and Milestone Execution Chat Starter; the Milestone Chat authors
> this starter.

---

## Governance References

You are operating under the AI Project System governance framework (Epic mode).

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) v2.1.0 (Effective: 2026-06-23)
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) v2.4.0 (Effective: 2026-06-29, two-track §16.6 after E24.1; this Epic bumps it to v2.5.0)

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md
3. This Epic Execution Chat Starter
4. Epic Spec (`P6-M24-E24.2__spec__clips-documentation-publishable.md`)
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Critical rules:**
- Documentation is authoritative, chat is ephemeral
- Follow the canonical happy path for Epic closure
- Produce a Delivery Notice upon completion
- Stop after PR creation and await HQ authorization for merge
- Accept/reject decisions are made by HQ Chat (human), not by you

---

## Epic Specification

**Full spec:** [P6-M24-E24.2__spec__clips-documentation-publishable.md](https://github.com/panchew/ai-project-system/blob/milestone/M24/docs/phases/P6__Visual_Comprehension_Layer_and_Process_Refinements/P6-M24-E24.2__spec__clips-documentation-publishable.md)

**Commit:** `f3ac45e` (on `milestone/M24`)

### Prerequisite Verification (do this first)

Before relying on the spec, confirm it is **git-tracked on the expected branch**, not merely present
on disk:

```
git ls-files --error-unmatch docs/phases/P6__Visual_Comprehension_Layer_and_Process_Refinements/P6-M24-E24.2__spec__clips-documentation-publishable.md
```

Disk presence is not proof of commit (the GH-1 convention). Then confirm the surfaces you rely on are
git-tracked on `milestone/M24`:
- Edit targets: `governance/AI-OPERATING-GUIDELINES.md`, `governance/guides/visual-artifacts.md`.
- **E24.1 merged** — AOG §16.6 "Proposed vs. implemented" and the guide §5 per-level
  proposed/implemented examples must already be present (`grep -n "16.6" governance/AI-OPERATING-GUIDELINES.md`;
  AOG version must read **2.4.0**).
- **Reference bundle present** — `git ls-files .ai-project/artifacts/reference/comfyui-endpoint/` must
  list `ltxv-video.json` and `VISUAL-ARTIFACTS.md`.
- **§7 has `clip`** — guide §7 `What` enumeration must already include `clip`
  (`image | infographic | mockup | diagram | clip`), and by-link must hold (grep for
  `.ai-project/visuals/` across the guide and AOG should return only the pre-existing §7 prohibition).

If any path is untracked or E24.1 / the reference bundle is absent, STOP and report to HQ.

### Summary

Document the **clip convention**: a clip binds to exactly **one** governance node (epic / milestone /
phase) via §7 (`What: clip`) and tells that node's proposed→implemented story (the §16.6 two tracks
rendered as video); it is **hosted-and-linked, never committed** (by-link). Document **production**
from the arc on the **verified LTX-Video path** (`ltxv-video.json` → `.webm` via `bin/ai-project-visual
--type video --workflow …`, citing the reference bundle — **no new plumbing**) and the **publish
path** (YT / TikTok / IG / FB) as the **same hosted asset reused**. Record the resolution of Open
Design Question A (**reference, not vendor**). State the **no-cross-cutting-reel** boundary.
**Documentation only** — no code, helper, schema, or config change.

### Problem Statement

The clip — the most CFO-facing artifact — has a verified production path (LTX-Video) and a `What:
clip` slot in §7, but no documented convention: nothing states single-parent binding, production from
the arc, or publish-as-reuse, and ODQ A is resolved in the milestone spec but not yet recorded in a
governance surface. This Epic supplies the convention and records the resolution.

### Goals

1. **Clip convention as single-parent** — a clip binds to exactly one node via §7 (`What: clip`),
   tells that node's proposed→implemented story, hosted-and-linked (never committed); the
   no-cross-cutting-reel boundary is stated.
2. **Production from the arc** — on the verified LTX-Video path, citing the reference bundle; no new
   helper capability.
3. **Publish path as reuse** — the same hosted asset reused (YT / TikTok / IG / FB); no pipeline, no
   hosting.
4. **Record ODQ A** (reference, not vendor) and **keep the suite green**.

---

## Required Content — what the convention must encode

**AOG §16 new subsection (e.g. §16.7 "Clips" — exact number is your design call within M24's scope):**

- A clip binds to exactly **one** governance node (epic / milestone / phase) — **single-parent** — via
  the §7 binding with `What: clip` and `Level` = that one node; it tells **that node's**
  proposed→implemented story (the §16.6 two tracks as video).
- A clip is **hosted and linked, never committed** (by-link, §16.5 / §7) — holds for `.webm` as for
  images.
- The **no-cross-cutting-reel boundary**: a clip is single-parent; a project-spanning montage is
  **deferred in P6** (SN-16 decision 3).
- A pointer to the guide (§8) for production + publish; **reference §7 for the schema — do not restate
  it.**

**Guide new section (e.g. §8 "Clips" — after §7, before "Related documents"):**

- **Production from the arc:** `ltxv-video.json` → `.webm` via `bin/ai-project-visual --type video
  --workflow <ltxv-video graph>`. **Cite the reference bundle**
  (`.ai-project/artifacts/reference/comfyui-endpoint/VISUAL-ARTIFACTS.md` + `ltxv-video.json`) for the
  exact command and verified parameters (768×512, 97 frames @ 25 fps) — **do not re-transcribe or ship
  the workflow**; no new plumbing, no helper change.
- A **worked §7 clip binding example** (`What: clip`, `Level:` one node), binding a **hosted link** to
  the `.webm`, never a committed path. Reuse the §7 block; point to §7 — do not restate the schema.
  (On `State`: a clip narrates the arc and most naturally reads as an `implemented` binding — keep it
  simple and consistent with §16.6/§7; do not invent a new `State` value.)
- **Publish path:** publishing to YT / TikTok / IG / FB is the **same hosted asset reused** — the
  already-hosted clip link is what is published; document the path, **do not** build a publisher,
  pipeline, or host the asset (infrastructure-agnostic).
- **Open Design Question A — record the resolution:** **reference, not vendor.** Workflow JSONs +
  models stay CFO-side; the verified reference bundle is the documented reference E24.2 points at — do
  **not** ship a runnable `workflows/` directory. Record this in a governance surface (guide and/or AOG
  §16.7).

**Changelogs:**

- AOG Changelog row + **minor bump to v2.5.0** (following §16's history: v2.4.0 two-track → v2.5.0
  clips), citing E24.2 (P6-M24).
- Guide Changelog dated row, citing E24.2 (P6-M24).

---

## Deliverables

1. [ ] `governance/AI-OPERATING-GUIDELINES.md` — new §16 subsection (clip = single-parent via §7
   `What: clip`, hosted-and-linked, no-cross-cutting-reel boundary, pointer to the guide) + Changelog
   row (minor bump → v2.5.0)
2. [ ] `governance/guides/visual-artifacts.md` — new §8 "Clips" (production on the verified LTX-Video
   path citing the reference bundle; a worked §7 `clip` binding example; publish-as-reuse; ODQ A
   resolution) + Changelog row
3. [ ] **Epic Delivery Notice**
   (`docs/phases/P6__Visual_Comprehension_Layer_and_Process_Refinements/P6-M24-E24.2__delivery-notice.md`)

---

## Definition of Done

- [ ] The clip convention is documented as **single-parent**, bound via §7 (`What: clip`), **hosted-and-linked** (never committed)
- [ ] Clip production is documented on the **verified LTX-Video path**, citing the reference bundle, with **no new plumbing** and no helper change
- [ ] The **publish path** is documented as the **same hosted asset reused** (no pipeline/hosting built)
- [ ] The **no-cross-cutting-reel** boundary is stated
- [ ] **Open Design Question A is resolved and recorded** (reference, not vendor)
- [ ] The clip convention is recorded in the **AOG Changelog** (minor bump → v2.5.0) and the **guide Changelog**
- [ ] The §7 schema is **referenced, not restated**; `clip` is used as the existing `What` value (not re-added); by-link unchanged (no `.ai-project/visuals/…` committed path)
- [ ] Full test suite passes (`pytest`); no plumbing touched; integration test still skips at `enabled: false`
- [ ] Delivery Notice produced and committed
- [ ] All changes committed to `epic/P6-M24-E24.2` branch
- [ ] Pull request opened to `milestone/M24`

---

## Acceptance Criteria

- [ ] The clip convention is documented as **single-parent** with a **defined publish path**
- [ ] Clip production **references the verified LTX-Video path**; **no helper change** is introduced
- [ ] **By-link holds for clips** (hosted + linked, never committed)
- [ ] Open Design Question A is resolved (reference, not vendor) and recorded in a governance surface
- [ ] The §7 schema and by-link are unchanged; the cross-cutting reel is stated as deferred

---

## Technical Constraints

- **Format:** Markdown (AOG + guide). No code, no helper change, no config key, no upload step, no
  `workflows/` directory shipped in-repo.
- **Convention, not schema:** reference the §7 binding schema (documented once in §7); do **not**
  restate or redefine the block or its `What`/`State` fields. `clip` is an **existing** `What` value —
  do not re-add it.
- **No new plumbing:** production references the **existing** verified LTX-Video path
  (`--type video --workflow`) and cites the reference bundle; do not modify `bin/ai-project-visual` or
  add a flag/upload/publish step.
- **Bind a link, never a path:** the clip's binding `Link` is a hosted URL to the `.webm`; do not
  reintroduce `.ai-project/visuals/<level>/…` (that would regress by-link).
- **Reference, not vendor (ODQ A):** point at the CFO-side contract via the preserved reference
  bundle; do not ship a runnable `workflows/` directory.
- **Surgical, additive edits:** add the AOG subsection and the guide section; do not restructure §16's
  existing subsections, §5/§7, or the guide's existing sections, and do not edit §16.5 / guide §4
  storage prose.
- **Scope of files:** `governance/AI-OPERATING-GUIDELINES.md` and `governance/guides/visual-artifacts.md`
  only. Do **NOT** touch `bin/ai-project-visual`, the reference bundle (read-only), the integration
  test, `ai-project-yml-spec.md` §3.5, or any template.

---

## Execution Contract

### Your Responsibilities (Coding Agent)

1. Create branch `epic/P6-M24-E24.2` from `milestone/M24` (which carries the merged E24.1 two-track behavior + M23 mechanism + reference bundle)
2. Read the merged E24.1 result (AOG §16.6; guide §5 examples) and the reference bundle (`.ai-project/artifacts/reference/comfyui-endpoint/VISUAL-ARTIFACTS.md` + `ltxv-video.json`) so the clip convention is written on top of the two-track behavior and cites the verified path
3. Add the AOG §16 clip subsection (single-parent via §7 `What: clip`, hosted-and-linked, no-cross-cutting-reel boundary, pointer to the guide)
4. Add the guide §8 "Clips" section: production from the arc on the verified LTX-Video path (cite the bundle), a worked §7 `clip` binding example, the publish-as-reuse path, and the ODQ A resolution
5. Add the AOG Changelog row (bump to v2.5.0) and the guide Changelog row
6. Run the full test suite (`pytest`) — must be green
7. Commit all changes to `epic/P6-M24-E24.2`
8. Open a pull request to `milestone/M24`
9. Produce the Epic Delivery Notice; stop and await HQ authorization (do not merge)

### What You Must NOT Do

- ❌ Do NOT merge the PR (HQ authorizes merge)
- ❌ Do NOT infer acceptance
- ❌ Do NOT re-add the `clip` `What` value or restate/redefine the §7 binding schema (reference §7; documented once)
- ❌ Do NOT build a cross-cutting / project-spanning reel (deferred in P6) — a clip is single-parent
- ❌ Do NOT add plumbing: no helper flag, no upload/publish step, no `workflows/` directory in-repo (ODQ A = reference, not vendor; ODQ B stays resolved)
- ❌ Do NOT reintroduce a `.ai-project/visuals/` committed path, or edit §16.5 / guide §4 storage prose (that would regress by-link)
- ❌ Do NOT touch `bin/ai-project-visual`, the reference bundle, templates, the integration test, or `ai-project-yml-spec.md`
- ❌ Do NOT iterate without explicit HQ instruction

---

## Reference Materials

For structural reference, examine:
- `docs/phases/P6__Visual_Comprehension_Layer_and_Process_Refinements/P6-M24-E24.2__spec__clips-documentation-publishable.md` (your Epic spec — authoritative)
- `governance/AI-OPERATING-GUIDELINES.md` §16.1–16.6 (home for the new clip subsection; v2.4.0 → v2.5.0)
- `governance/guides/visual-artifacts.md` §5 (the two-track examples E24.1 added), §7 (the binding schema + `clip` `What` value — **reference, do not restate**), and the new §8 "Clips"
- `.ai-project/artifacts/reference/comfyui-endpoint/VISUAL-ARTIFACTS.md` + `ltxv-video.json` (the verified LTX-Video contract — **cite, do not vendor**)
- `docs/phases/P6__Visual_Comprehension_Layer_and_Process_Refinements/P6-M24__milestone-spec.md` (§ "Epic Detail → E24.2" and "Open Design Question A — Resolved")
- `docs/phases/P6__Visual_Comprehension_Layer_and_Process_Refinements/P6-M24-E24.1__spec__proposed-vs-implemented-comprehension.md` (the merged two-track behavior this builds on)

---

## Canonical Happy Path (Reminder)

1. ✅ Execution completed
2. ✅ Epic Delivery Notice produced
3. ⏸️ Human Review
4. ⏸️ Epic Review Seal
5. ⏸️ HQ decision recorded
6. ⏸️ HQ delivery authorization issued
7. ⏸️ PR merged (after authorization)
8. ⏸️ Execution stops

**You are responsible for steps 1–2 only. Then STOP and await HQ instruction.**

---

## Epic Delivery Notice Template

**File:** `docs/phases/P6__Visual_Comprehension_Layer_and_Process_Refinements/P6-M24-E24.2__delivery-notice.md`

```markdown
---
type: delivery-notice
epic: E24.2
status: delivered
delivery_date: <YYYY-MM-DD>
---

# Epic Delivery Notice — E24.2

**Epic:** E24.2 — Clips as documentation + publishable media (P6-VC-4)
**Status:** Execution Complete, Delivered to HQ for Review
**Delivery Date:** <YYYY-MM-DD>

## Execution Summary
[Brief summary]

## Deliverables Completed
- [ ] AOG §16 clip subsection (single-parent via §7 What: clip, hosted-and-linked, no-cross-cutting-reel) + Changelog row (v2.5.0)
- [ ] Guide §8 "Clips" (production on verified LTX-Video path citing the bundle; worked §7 clip binding; publish-as-reuse; ODQ A resolution) + Changelog row
- [ ] Delivery Notice

## Definition of Done Status
[Confirm each DoD item — including "§7 referenced not restated", "clip not re-added", "no new plumbing", "by-link unchanged", "ODQ A recorded", "suite green"]

## Branch and PR Information
- **Branch:** `epic/P6-M24-E24.2`
- **PR:** [Link]
- **Target:** `milestone/M24`

## Notes
[Deviations / follow-ups — e.g., the chosen §16 subsection / guide section numbers, the clip example's State choice, where ODQ A was recorded]

## Awaiting HQ Review
1. Human Review
2. Epic Review Seal
3. HQ acceptance decision
4. HQ merge authorization

---
**Coding Agent stopped per AI-OPERATING-GUIDELINES.md canonical happy path.**
```

---

## Exit Conditions

Stop when: all deliverables committed, PR opened to `milestone/M24`, Delivery Notice produced.

Then report: "Epic E24.2 execution complete. Epic Delivery Notice produced. Awaiting HQ review and authorization."

---

## Begin Execution

1. Create branch `epic/P6-M24-E24.2` from `milestone/M24`
2. Verify the Epic spec is git-tracked and E24.1 + the reference bundle are present (prerequisite checks above), then review the spec, AOG §16.1–16.6, guide §5/§7, and the reference bundle
3. Implement deliverables per Definition of Done

Copy the entire chat starter above and paste into the Governance Agent (Epic mode) to begin execution.
