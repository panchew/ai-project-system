# Epic Execution Chat Starter — E29.2

**Epic:** E29.2 — Enable + real endpoint test
**Phase:** P8 — Visual Artifacts Activation
**Milestone:** M29 — Visual Artifacts Activation
**Repository:** panchew/ai-project-system
**Branch Strategy:** `epic/P8-M29-E29.2` → PR to `milestone/M29`

> **Provenance:** Authored by the Milestone Chat (P8-M29) on 2026-07-15. E29.1 (Direction B, no
> rename) is merged — this Epic's test assertions need no adjustment on that account. E29.3
> depends on this Epic's completion.

---

## Governance References

You are operating under the AI Project System governance framework (Epic mode).

**Authoritative governance documents:**
- PROJECT-SYSTEM-GUIDELINES.md v2.3.0
- AI-OPERATING-GUIDELINES.md v2.9.0

**Governance hierarchy:** PSG > AOG > this Starter > Epic Spec > execution decisions > system
references > chat.

**Critical rules:**
- Documentation is authoritative; chat is ephemeral
- Follow the §1A canonical happy path for Epic closure
- Produce a Delivery Notice upon completion
- Stop after PR creation — **do not merge.** Under default-accept (PSG §11.6 / SN-19) a clean
  delivery is accepted by silence (no Review Decision on the happy path); the PR **merge**
  requires explicit human authorization, given in-chat (no Delivery Authorization artifact)
- Accept/reject decisions belong to the parent chats (human-backed), not to you

---

## Epic Specification

**Full spec:** `docs/phases/P8__Visual_Artifacts_Activation/P8-M29-E29.2__spec__enable-and-real-endpoint-test.md`
**Commit:** committed alongside this starter on `milestone/M29` — resolve the exact hash with
`git log --oneline -1 -- <spec path>` before relying on it.

### Prerequisite Verification (do this first)

- `git ls-files --error-unmatch <spec path>` — confirm the spec is git-tracked (GH-1).
- Confirm the anchors: `.ai-project.yml:29-40` (stale opt-out comment), `.ai-project.yml:42`
  (`enabled: false`); `tests/integration/test_visual_artifacts_helper.py:61-76`
  (`test_helper_generates_against_endpoint`, `skipif` gate, dynamic `vtype` resolution at line
  68); the other six tests in that file (lines 79-177) using isolated `tmp_path` fixtures.
- **Re-verify ComfyUI endpoint reachability before flipping the flag:** `curl -m 5
  http://localhost:8188/system_stats` — confirmed reachable at spec-authoring time
  (2026-07-15); if unreachable now, **STOP and escalate to the Phase Chat** rather than
  proceeding on stale grounding.
- Confirm no `.github/workflows/` directory exists (it doesn't, as of this spec) — the
  no-live-endpoint decision below is scoped to local `pytest` runs, not CI infrastructure.

### Summary

Flip this repo's own `visual_artifacts.enabled` to `true`, rewrite the stale opt-out comment,
prove `test_helper_generates_against_endpoint` passes for real against the live local ComfyUI
endpoint, decide and document how a no-live-endpoint contributor is handled, confirm the five
isolated-fixture tests remain unaffected, and confirm Structural-mode independence from this
flag on the record.

### Binding constraints (carry into every decision — verbatim from the Milestone spec)

- **Hard Constraint (non-negotiable):** the suite MUST be green at delivery with
  `test_helper_generates_against_endpoint` passing for real against the live endpoint.
  Re-disabling `enabled` or re-skipping the test to keep the suite green is **not** acceptable
  under any circumstance. If the endpoint becomes unreachable during execution, escalate — do
  not route around this constraint.
- **Do not touch E29.1's surfaces** — `governance/ai-project-yml-spec.md`,
  `bin/ai-project-visual`'s docstring, `VISUAL-ARTIFACTS.md` are merged and correct.
- **Structural mode (Mermaid/PlantUML) must remain fully independent of `visual_artifacts.enabled`**
  — it was never gated by this flag (AOG §17.3/§17.8); this Epic must not introduce any coupling,
  and must confirm on record that none exists.
- **No CI workflow to build** — the no-live-endpoint decision is about local `pytest` runs only.

### Design point (yours to resolve — decide, document, move; escalate only if truly blocked)

**No-live-endpoint contributor path.** Candidates (non-exhaustive):
- An opt-**out** environment variable (e.g., `AI_PROJECT_SKIP_LIVE_ENDPOINT_TESTS=1`) — default
  (unset) still runs and requires the endpoint.
- Document the live endpoint as a dev-environment requirement (consistent with
  `models.epic_dev`/`epic_qa` already requiring local Ollama) — no code change.
- Any other mechanism, provided the default is **not** to skip.

Whatever you choose, the default behavior (nothing set, nothing configured) must still attempt
and require the real endpoint call — an opt-in-to-skip inverted default is fine; an opt-in-to-run
default is not (that's just the old skip-by-default behavior renamed).

---

## Deliverables

1. [ ] `.ai-project.yml`: `enabled: true`; stale opt-out comment (lines 29-40) rewritten
2. [ ] `test_helper_generates_against_endpoint` passes for real against the live endpoint
3. [ ] No-live-endpoint contributor path decided, implemented (if code-level), documented
4. [ ] Five isolated-fixture tests confirmed green and unaffected
5. [ ] Structural-mode independence from `enabled` confirmed in the Delivery Notice
6. [ ] Full suite passes, no new skips introduced
7. [ ] **Epic Delivery Notice**

---

## Definition of Done

- [ ] `visual_artifacts.enabled: true`, no stale opt-out comment
- [ ] `test_helper_generates_against_endpoint` passes for real against the live endpoint
- [ ] No-live-endpoint path decided, implemented, documented
- [ ] Five isolated-fixture tests confirmed green
- [ ] Structural-mode independence confirmed on record
- [ ] Full suite passes with no new skips (Hard Constraint)
- [ ] Delivery Notice committed; all changes on `epic/P8-M29-E29.2`; PR opened to `milestone/M29`

---

## Acceptance Criteria

- [ ] `pytest` runs the endpoint test as a real pass, not a skip
- [ ] Suite is green at delivery with a live network call exercised
- [ ] Structural-mode remains available regardless of this flip, confirmed on record

---

## Technical Constraints

- **Scope of files:** `.ai-project.yml` (lines 29-48),
  `tests/integration/test_visual_artifacts_helper.py` (no-live-endpoint mechanism only; confirm,
  don't alter, the six isolated-fixture tests). No `governance/ai-project-yml-spec.md`, AOG,
  `bin/ai-project-visual`, or `bin/ai-project-orchestrator` changes expected — if execution
  reveals a genuine need (e.g., a real bug surfaces while proving the endpoint call), keep the
  fix minimal and document why.
- **No CI infrastructure** — do not create `.github/workflows/`.

---

## Execution Contract

### Your Responsibilities (Coding Agent)

1. Create branch `epic/P8-M29-E29.2` from `milestone/M29`
2. Verify prerequisites; re-confirm ComfyUI endpoint reachability
3. Flip `.ai-project.yml`'s `enabled` flag; rewrite the stale comment
4. Run `test_helper_generates_against_endpoint`; if it fails, root-cause and fix — do not
   route around it
5. Decide and implement the no-live-endpoint contributor path; document it
6. Run and confirm the five isolated-fixture tests remain green
7. Confirm Structural-mode independence from `enabled`; record it in the Delivery Notice
8. Run the full suite; commit to `epic/P8-M29-E29.2`; open a PR to `milestone/M29`
9. Produce the Epic Delivery Notice; stop — **do not merge** (human merge authorization is
   in-chat, SN-19/§11.6)

### What You Must NOT Do

- ❌ Merge the PR / infer acceptance
- ❌ Re-disable `enabled` or re-skip the endpoint test to keep the suite green under any
  circumstance
- ❌ Touch E29.1's surfaces or E29.3's precision-validation surface
- ❌ Build CI infrastructure that doesn't exist in this repo
- ❌ Leave the no-live-endpoint decision undocumented or defaulting to skip
- ❌ Iterate without explicit instruction from the parent chat

---

## Reference Materials

- `docs/phases/P8__Visual_Artifacts_Activation/P8-M29-E29.2__spec__enable-and-real-endpoint-test.md` (your Epic spec — authoritative)
- `docs/phases/P8__Visual_Artifacts_Activation/P8-M29__milestone-spec.md` (Epic Detail → E29.2; binding context; Hard Constraint)
- `docs/phases/P8__Visual_Artifacts_Activation/P8-M29-E29.1__delivery-notice.md` (merged; Direction B, no rename — confirms no naming adjustment needed here)
- `.ai-project.yml` (lines 29-48)
- `tests/integration/test_visual_artifacts_helper.py`
- `governance/AI-OPERATING-GUIDELINES.md` §17.3, §17.5, §17.8 (reference only)

---

## Canonical Happy Path (reminder — SN-19/§11.6)

Execution → Delivery Notice → **[STOP]** → default-accept by silence (Review Decision =
exception path only) → **human merge authorization in-chat** → merge → stop. **You own the
execution and the Delivery Notice; then you stop.**

## Epic Delivery Notice Template

**File:** `docs/phases/P8__Visual_Artifacts_Activation/P8-M29-E29.2__delivery-notice.md`

```markdown
---
type: delivery-notice
epic: E29.2
status: delivered
delivery_date: <YYYY-MM-DD>
---

# Epic Delivery Notice — E29.2

**Epic:** E29.2 — Enable + real endpoint test
**Status:** Execution Complete, Delivered for Review
**Delivery Date:** <YYYY-MM-DD>

## Execution Summary
[Endpoint reachability re-verified at execution time; enabled flip; comment rewrite; endpoint
test result; no-live-endpoint mechanism chosen and why; guardrail-test confirmation;
Structural-mode independence confirmation]

## Deliverables Completed
- [ ] .ai-project.yml enabled: true + comment rewritten
- [ ] test_helper_generates_against_endpoint passes for real
- [ ] No-live-endpoint path decided, implemented, documented
- [ ] Five isolated-fixture tests confirmed green
- [ ] Structural-mode independence confirmed
- [ ] Delivery Notice

## Definition of Done Status
[Confirm each — incl. "suite green with real network call, no new skips" (Hard Constraint)]

## Branch and PR Information
- **Branch:** `epic/P8-M29-E29.2`
- **PR:** [Link]
- **Target:** `milestone/M29`

## Notes
[No-live-endpoint mechanism chosen and reasoning; any deviation, failure root-caused, or
follow-up for E29.3]

## Awaiting Review
Default-accept (PSG §11.6): clean delivery accepted by silence; merge awaits explicit human
authorization in-chat (SN-19).

---
**Coding Agent stopped per AI-OPERATING-GUIDELINES.md canonical happy path.**
```

---

## Exit Conditions

Stop when all deliverables are committed, the PR is open to `milestone/M29`, and the Delivery
Notice is produced. Then report: "Epic E29.2 execution complete. Epic Delivery Notice produced.
Awaiting review (default-accept) and human merge authorization."

## Begin Execution

1. Create branch `epic/P8-M29-E29.2` from `milestone/M29`
2. Verify prerequisites; re-confirm endpoint reachability before flipping the flag
3. Implement, test, document the no-live-endpoint decision; open the PR; deliver

Copy the entire chat starter above and paste into the Governance Agent (Epic mode) to begin execution.
