# Epic Execution Chat Starter — E26.2

**Epic:** E26.2 — Real-model wiring + mock retirement
**Phase:** P7 — Agentic Execution and Default-On Visuals
**Milestone:** M26 — First Real Agentic Run (P7-AE-1)
**Repository:** panchew/ai-project-system
**Branch Strategy:** `epic/P7-M26-E26.2` → PR to `milestone/M26`

> **Provenance:** Authored by the Milestone Chat (P7-M26) on 2026-07-12. Second Epic of the
> **binding sequence E26.1 → E26.2 → E26.3** (HQ ruling 2026-07-11). E26.1 is merged
> (`e65f758`); the live run is **E26.3**, not this Epic.

---

## Governance References

You are operating under the AI Project System governance framework (Epic mode).

**Authoritative governance documents:**
- PROJECT-SYSTEM-GUIDELINES.md v2.3.0
- AI-OPERATING-GUIDELINES.md v2.6.0

**Governance hierarchy:** PSG > AOG > this Starter > Epic Spec > execution decisions > system references > chat.

**Critical rules:**
- Documentation is authoritative; chat is ephemeral
- Follow the §1A canonical happy path for Epic closure
- Produce a Delivery Notice upon completion
- Stop after PR creation — **do not merge.** Under default-accept (PSG §11.6 / SN-19) a clean
  delivery is accepted by silence (no Review Decision on the happy path); the PR **merge**
  requires explicit human authorization, given in-chat (no Delivery Authorization artifact)
- Accept/reject decisions belong to the parent chats (human-backed), not to you

---

## Epic Specification

**Full spec:** `docs/phases/P7__Agentic_Execution_and_Default_On_Visuals/P7-M26-E26.2__spec__real-model-wiring-mock-retirement.md`
**Commit:** `3f8d11c` (on `milestone/M26`)

### Prerequisite Verification (do this first)
- `git ls-files --error-unmatch <spec path>` — confirm the spec is git-tracked (GH-1).
- Confirm E26.1 is merged on `milestone/M26`: `bin/run-dev-agent` exists and is executable;
  `tests/integration/test_run_dev_agent_adapter.py` present; suite baseline 286 passed / 1 skipped.
- Confirm the anchors: `.ai-project.yml:26` (`epic_dev: local:llama3:8b`),
  `bin/ai-project-orchestrator:21` (`DEFAULT_MODELS["epic_dev"]`),
  `governance/ai-project-yml-spec.md` L58/L330/L336, `bin/verify-loop.sh` (the only mock consumer).

### Summary

Switch the dev model off the tool-call-incapable `llama3:8b` to `qwen2.5-coder:14b`, make the
documented live epic-execution flow name `./bin/run-dev-agent` (never a mock), and retire the
mock-driven path from the live flow with a decided, documented CI-retention boundary. The
`04_epic.json` trigger **mechanism stays** — it is how E26.3 launches.

### Verified surface map (from the Epic spec — trust it, it was grounded this session)

- **Two `epic_dev` definitions:** `.ai-project.yml:26` and orchestrator `DEFAULT_MODELS` (L21).
  Consistency call is yours — **recommended: both move** to `local:qwen2.5-coder:14b`.
- **One documented default:** `governance/ai-project-yml-spec.md` L58/L330 (+ L336 example) —
  "off `llama3:8b` everywhere it is defined" includes these rows.
- **One mock consumer:** `bin/verify-loop.sh` (L132/189/246/319/388). **pytest never runs the
  mocks** — `test_agentic_mode.py` is daemon-routing only. Retention is therefore cheap.
- **Mechanism references that STAY:** README.md:441, `governance/agents/governance.agent.md`
  L194/L206, `.ai-project/queue/README.md` — they describe the `04_epic.json` mechanism, not
  the mocks.
- **Frozen history (do not edit):** P3 completion docs/specs mentioning mocks.

### Boundary decision (yours — decide, document, move)

**Recommended:** retain `tests/mocks/` + `bin/verify-loop.sh` as the loop-logic **regression
harness**, explicitly labeled verification-only (header comments), and update live trigger docs
to name the adapter + a real `validation_command`. Alternative (justify if chosen): fold the
verify-loop scenarios into pytest and drop the shell harness. Either way: **grep-prove** that no
live/documented flow routes through `tests/mocks/` and nothing left *defines* `epic_dev` as
`llama3:8b`.

---

## Deliverables
1. [ ] `.ai-project.yml:26` → `epic_dev: local:qwen2.5-coder:14b`
2. [ ] `DEFAULT_MODELS` consistency call decided + applied/documented (orchestrator L21)
3. [ ] `governance/ai-project-yml-spec.md` default rows consistent (L58/L330; L336 reworded if needed)
4. [ ] Live trigger docs name `./bin/run-dev-agent` (no mock in any live/documented flow)
5. [ ] Mock CI-retention boundary decided, labeled in the retained files, documented
6. [ ] Consistency-guard test (config + script default agree; neither is `llama3:8b`)
7. [ ] **Epic Delivery Notice**

---

## Definition of Done
- [ ] `.ai-project.yml` `epic_dev` is `local:qwen2.5-coder:14b`
- [ ] The live epic-execution path invokes `bin/run-dev-agent` (no mock in the live flow)
- [ ] The `04_epic.json` mock trigger is retired from the live path; CI boundary documented
      (trigger *mechanism* untouched)
- [ ] The `DEFAULT_MODELS` call and yml-spec default rows decided/updated and documented
- [ ] **Grep-proven:** nothing defines `epic_dev` as `llama3:8b` outside frozen history; no
      live/documented flow routes epic execution through `tests/mocks/`
- [ ] Config/tests updated; full suite passes (286/1 baseline + new)
- [ ] Delivery Notice committed; all changes on `epic/P7-M26-E26.2`; PR opened to `milestone/M26`

---

## Acceptance Criteria
- [ ] `epic_dev` is off `llama3:8b` everywhere it is defined
- [ ] No live or documented flow routes epic execution through the mock scripts
- [ ] The trigger mechanism (`04_epic.json`) is intact and ready for E26.3's real trigger

---

## Technical Constraints
- **The trigger mechanism stays** — no changes to `04_epic.json` handling in the orchestrator
  or daemon.
- **E26.1's surfaces are frozen:** `bin/run-dev-agent`, `.ai-project/agents/tools.json`,
  `tests/integration/test_run_dev_agent_adapter.py` — do not modify.
- **`epic_qa` stays** at `local:qwen2.5-coder:7b` (L27 — not directed by M26).
- **Scope of files:** `.ai-project.yml`, `bin/ai-project-orchestrator` (L21 only),
  `governance/ai-project-yml-spec.md` (default rows + its version/`last_updated` conventions),
  `.ai-project/queue/README.md` / live trigger docs, `tests/mocks/*` + `bin/verify-loop.sh`
  (labeling only, per your boundary), one new test file, Delivery Notice. Nothing else.

---

## Execution Contract

### Your Responsibilities (Coding Agent)
1. Create branch `epic/P7-M26-E26.2` from `milestone/M26`
2. Verify prerequisites (spec git-tracked; E26.1 merged; anchors present)
3. Switch the model (config + consistency call + yml-spec rows); wire the live docs to the adapter
4. Decide + label + document the mock CI-retention boundary; grep-prove the live flow is mock-free
5. Add the consistency-guard test; run the full suite
6. Commit to `epic/P7-M26-E26.2`; open a PR to `milestone/M26`
7. Produce the Epic Delivery Notice; stop — **do not merge** (human merge authorization is
   in-chat, SN-19/§11.6)

### What You Must NOT Do
- ❌ Merge the PR / infer acceptance
- ❌ Touch `epic_qa`, the trigger mechanism, or any E26.1 surface
- ❌ Execute a live run or require an Ollama endpoint (E26.3's job)
- ❌ Edit frozen P3 history (E12.x completion docs/specs)
- ❌ Broad orchestrator refactors — L21 is the only orchestrator line in scope
- ❌ Iterate without explicit instruction from the parent chat

---

## Reference Materials
- `docs/phases/P7__Agentic_Execution_and_Default_On_Visuals/P7-M26-E26.2__spec__real-model-wiring-mock-retirement.md` (your Epic spec — authoritative)
- `docs/phases/P7__Agentic_Execution_and_Default_On_Visuals/P7-M26__milestone-spec.md` (Epic Detail → E26.2; the "load-bearing nuance" paragraph)
- `.ai-project.yml:26–27`; `bin/ai-project-orchestrator:17–23`; `governance/ai-project-yml-spec.md` L58/L330/L336
- `bin/verify-loop.sh` + `tests/mocks/mock_{dev,qa}.sh` (boundary subject)
- `~/soft-dev/local-agent-runner/CONTRACT.md` §1.4 (why `llama3:8b` is out — read-only)

---

## Canonical Happy Path (reminder — SN-19/§11.6)
Execution → Delivery Notice → **[STOP]** → default-accept by silence (Review Decision =
exception path only) → **human merge authorization in-chat** → merge → stop. **You own the
execution and the Delivery Notice; then you stop.**

## Epic Delivery Notice Template

**File:** `docs/phases/P7__Agentic_Execution_and_Default_On_Visuals/P7-M26-E26.2__delivery-notice.md`

```markdown
---
type: delivery-notice
epic: E26.2
status: delivered
delivery_date: <YYYY-MM-DD>
---

# Epic Delivery Notice — E26.2

**Epic:** E26.2 — Real-model wiring + mock retirement
**Status:** Execution Complete, Delivered for Review
**Delivery Date:** <YYYY-MM-DD>

## Execution Summary
[Model switch; DEFAULT_MODELS decision; yml-spec rows; live-doc wiring; the boundary chosen and why]

## Deliverables Completed
- [ ] .ai-project.yml epic_dev switch
- [ ] DEFAULT_MODELS consistency call
- [ ] yml-spec default rows
- [ ] Live trigger docs → adapter
- [ ] Mock boundary labeled + documented
- [ ] Consistency-guard test
- [ ] Delivery Notice

## Definition of Done Status
[Confirm each — incl. grep evidence for "off llama3:8b everywhere defined" and "no live flow
through tests/mocks/", trigger mechanism untouched, suite result]

## Branch and PR Information
- **Branch:** `epic/P7-M26-E26.2`
- **PR:** [Link]
- **Target:** `milestone/M26`

## Notes
[The CI-retention boundary decision and its rationale; anything E26.3 should know]

## Awaiting Review
Default-accept (PSG §11.6): clean delivery accepted by silence; merge awaits explicit human
authorization in-chat (SN-19).

---
**Coding Agent stopped per AI-OPERATING-GUIDELINES.md canonical happy path.**
```

---

## Exit Conditions
Stop when all deliverables are committed, the PR is open to `milestone/M26`, and the Delivery
Notice is produced. Then report: "Epic E26.2 execution complete. Epic Delivery Notice produced.
Awaiting review (default-accept) and human merge authorization."

## Begin Execution
1. Create branch `epic/P7-M26-E26.2` from `milestone/M26`
2. Verify prerequisites; read the Epic spec (esp. the verified surface map)
3. Switch, wire, decide the boundary, grep-prove, test; open the PR; deliver

Copy the entire chat starter above and paste into the Governance Agent (Epic mode) to begin execution.
