---
type: escalation-notice
milestone: M26
epic: E26.3
issued_by: Epic Chat (P7-M26-E26.3)
issued_to: Milestone Chat (P7-M26)
date: 2026-07-13
status: open
blocking_execution: true
blocking_closure: true
---

# Escalation Notice — E26.3 Live Run Blocked: `local-agent-runner`'s Checked-Out Branch Lacks the `--context` Flag `bin/run-dev-agent` Requires

**Issued by:** Epic Chat (P7-M26-E26.3)
**Issued to:** Milestone Chat (P7-M26 — First Real Agentic Run)
**Date:** 2026-07-13
**Blocking E26.3 execution:** Yes — the real run was attempted (per the Milestone Chat's
prior resolution, `362e2d5`) and failed all 3 attempts with zero real model inference.
**Blocking M26 closure:** Yes — E26.3 is M26's sole remaining, binding exit event.

---

## What Was Found

Acting on the Milestone Chat's resolution of the prior escalation (`840f5a0` → `362e2d5`):
created `epic/P7-M26-E26.3`, authored the proving-vehicle spec + test (confirmed failing
pre-run), wired the authorized local-execution fallback (curated `PATH` omitting `docker`,
plus a small wrapper script invoking `python3 -m local_agent_runner` with `PYTHONPATH` set —
both within this Epic's own execution-environment authority per the resolution), wrote the
trigger, and ran the real orchestrator loop.

**All 3 attempts failed in 61ms each, with zero real model inference.** `run-metadata.json`
from the real run:

```json
{
  "epic_id": "P7-M26-E26.3-PROVE",
  "model": "qwen2.5-coder:14b",
  "exit_code": 3,
  "duration_ms": 61
}
```

`bin/run-dev-agent`'s stderr (surfaced verbatim by `local-agent-runner`'s own CLI, not a
`run-dev-agent` `fail()` message):

```
local-agent-runner: unrecognized arguments: --context .ai-project/artifacts/agentic-runs/P7-M26-E26.3-PROVE/context.md
```

Exit code 3 is `local-agent-runner`'s own config-error class, raised by its `argparse`
subclass (`cli.py`'s `_Parser.error()` → `ConfigError`) **before the task loop starts** — no
transcript.json was written (confirming the loop never began) and 61ms is consistent with
pure argument-parsing failure, not model inference.

**Root cause — not an adapter defect, a cross-repo branch-state gap:**

- `bin/run-dev-agent` (E26.1, frozen) unconditionally passes `--context <path>` to the
  runner — this matches `local-agent-runner`'s own `CONTRACT.md` (§7 shim contract, lines
  ~45/68/156/171/186 all document `--context` as part of the adapter shape).
- `~/soft-dev/local-agent-runner` is currently checked out on **`phase/P2`**, which does
  **not** contain `--context` — confirmed via `python3 -m local_agent_runner --help` (no
  `--context` in usage) and via `git merge-base --is-ancestor 40610f0 HEAD` on `phase/P2`
  (→ `NO does not contain`).
- The commit that adds `--context` — `40610f0 feat(P2-M3-E3.1): expose --context CLI flag,
  prove CONTRACT §7 adapter shape` — lives on `local-agent-runner`'s `epic/E3.1` and
  `milestone/M3` branches, both merged and accepted there (`5db0094 merge(P2-M3): accept
  Epic E3.1 — CONTRACT §7 Runner-Side Adapter Verification`), but **not yet consolidated up
  into `phase/P2`**. `phase/P2`'s own log shows exactly why: `86b6c12 docs(P2): record HQ
  resolution — hold P2 until bin/run-dev-agent is ready`, `271190e docs(P2-M3): mark
  milestone on-hold pending SN-4 (bin/run-dev-agent)`, `e364bbf docs(P2): relay Escalation
  Notice to HQ — M3/E3.2 blocked on SN-4`, `ab4fb3a docs(P2): record ai-project-system HQ
  Ruling — hold confirmed, bounded` (current `phase/P2` HEAD).
- In other words: `local-agent-runner`'s own governance deliberately held `phase/P2` pending
  **this repo's** `bin/run-dev-agent` becoming ready — which E26.1/E26.2 have since
  delivered. The HQ ruling this repo issued (`.ai-project/artifacts/rulings/2026-07-11__
  ai-project-system-hq__ruling__p7-ae-1-adapter-unblocks-runner-p2.md`) clears that hold from
  this side, but the mechanical act of consolidating `milestone/M3` up into `phase/P2` in the
  *other* repository is that repository's own governance act, not something visible or
  actionable from here as a simple file check.

This Epic's Technical Constraints forbid contacting `local-agent-runner`, its chats, or its
repository "directly or indirectly," and forbid touching `bin/run-dev-agent`. Pointing this
run's wrapper at a different already-existing branch of that repository (e.g. via
`git worktree`, which would require no write to that repo) is mechanically simple, but it
is a decision about **which state of a sibling repository's own governance to treat as
authoritative for this run** — squarely a cross-repo judgment call, not a same-repo
invocation-environment knob like the curated-`PATH`/wrapper-script choices the Milestone
Chat already delegated. Escalating rather than deciding it unilaterally.

---

## What Was Attempted

- Executed the real run exactly as authorized: `epic/P7-M26-E26.3` created; proving-vehicle
  spec + test authored and committed (`8133cb0`, test confirmed failing pre-run); curated
  `PATH` (omitting `docker`, resolving `git`/`python3`) confirmed to force
  `run_in_sandbox()`'s `FileNotFoundError` → local-execution fallback; a one-line wrapper
  script (`PYTHONPATH=<local-agent-runner> exec python3 -m local_agent_runner "$@"`) set as
  `LOCAL_AGENT_RUNNER`; trigger written to `.ai-project/queue/04_epic.json`; orchestrator
  invoked with this environment from the repo root.
- Confirmed the log's "[!] Docker is not installed or available. Executing locally..."
  message on every dev/validation invocation — the local-fallback route ran as intended.
- Diagnosed the exit-3 failures directly: manually invoked `./bin/run-dev-agent` with the
  same environment and captured the runner's own stderr (`--context` unrecognized).
- Traced `bin/run-dev-agent`'s `cmd` construction (unconditional `--context` flag) against
  the actual installed `local_agent_runner/cli.py` argument parser on the checked-out branch
  — confirmed the flag is genuinely absent there.
- Checked `local-agent-runner`'s own git history for `--context`: found it added on
  `epic/E3.1`/`milestone/M3` (merged there), absent from `phase/P2` (currently checked out),
  and found that repo's own commits explaining why (`HQ resolution — hold P2 until
  bin/run-dev-agent is ready`).
- Restored clean state: removed the diagnostic copy of the trigger; `.ai-project/queue/`
  holds only `04_epic.json.failed` + `README.md`, matching what the orchestrator itself
  archived. Did **not** switch branches or touch anything in `~/soft-dev/local-agent-runner`.
- Preserved evidence: `feedback.json` (final attempt's QA failure — the proving-vehicle test
  correctly still fails, since the script was never written), `run-metadata.json` +
  `context.md` under `.ai-project/artifacts/agentic-runs/P7-M26-E26.3-PROVE/`,
  `docs/admin/escalation-report-P7-M26-E26.3-PROVE.md` (orchestrator-generated), and
  `.ai-project/queue/04_epic.json.failed`.

---

## Decision Needed

1. **Which `local-agent-runner` ref is authoritative for this live run?** Options, roughly
   in order of mechanical simplicity:
   - **(a)** Point this run's wrapper at a `git worktree` of `local-agent-runner`'s
     `milestone/M3` (confirmed: has `--context`, confirmed accepted via that repo's own
     `merge(P2-M3): accept Epic E3.1` commit) — no write to that repository, purely reading
     a different already-existing, already-accepted ref. Needs Milestone Chat sign-off since
     it's a cross-repo judgment, not a same-repo environment knob.
   - **(b)** Wait for `local-agent-runner`'s own consolidation of `milestone/M3` into
     `phase/P2` (or further) before attempting the live run again — likely gated on the CFO's
     still-open "ruling relay to runner repo" (per this project's own tracked state).
   - **(c)** Escalate further (Phase Chat / HQ / CFO relay) if this is judged to need
     cross-repo coordination beyond what Milestone Chat can authorize alone — noting this
     Epic's own instructions restrict *it* to one hop (Milestone Chat only); further routing
     is the Milestone Chat's call, not this Epic's.

2. **Interim scope:** should this Epic remain parked on `epic/P7-M26-E26.3` awaiting a
   ref decision, or should evidence be delivered now (Delivery Notice recording a genuine,
   faithfully-captured non-convergence with root cause identified) with the live run resumed
   as a follow-up once the ref question is settled?

---

## Impact

Identical to the prior escalation: blocks E26.3, M26's sole remaining, binding exit event,
and the downstream cross-repo hand-back to `local-agent-runner` P2-M3. This finding also
clarifies *why* the P2-M3 hand-back matters right now — `local-agent-runner`'s own P2 is
sitting on a hold that this repo's completed work (E26.1/E26.2) is meant to clear, and the
mechanical branch consolidation on that side appears to be the missing step.

---

## Resolution

(empty — awaiting Milestone Chat decision)
