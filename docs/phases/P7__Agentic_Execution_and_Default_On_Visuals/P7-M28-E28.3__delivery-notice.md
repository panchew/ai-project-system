---
type: delivery-notice
epic: E28.3
status: delivered
delivery_date: 2026-07-13
---

# Epic Delivery Notice — E28.3

**Epic:** E28.3 — init canonical agent file
**Status:** Execution Complete, Delivered for Review
**Delivery Date:** 2026-07-13

## Execution Summary

The three-line script fix is confirmed: `bin/ai-project-init` now sources
`governance/agents/governance.agent.md` (line 328), installs it as
`.ai-project/agents/governance.agent.md` (line 329), and stages that same canonical path
(line 408). A grep of `bin/ai-project-init` after the edit confirms zero remaining literal
`hq.agent.md` references anywhere in the file, including inside `install_hq_agent()` and its
call site — the fallback stub's "Milestone M8" prose (its heading text, not a filename) was
left untouched per the Epic's Non-Goals.

`tests/test_init_agent_path.py::test_init_writes_canonical_agent_path` was extended to assert
the canonical filename and now also asserts the installed file starts with a valid Markdown
header/frontmatter (mirroring the script's own internal validation). It was run twice: once via
`pytest tests/test_init_agent_path.py -v` (passed), and once by invoking the actual script
directly — `bash bin/ai-project-init test-proj --dir <tmp> --skip-git --skip-submodule` — and
inspecting the output directory by hand: `.ai-project/agents/governance.agent.md` was present,
230 bytes, first line `# HQ Chat Agent` (the fallback stub, since no `governance/` submodule is
present in the `--skip-submodule` test scaffold — the same code path the prior version of this
test always exercised).

`docs/systems/cli-usage-guide.md`'s two stale references (line 36 "What You Get", line 75
verification-checklist `cat` command) were updated to `governance.agent.md`, matching its own
already-correct Troubleshooting section. The file no longer contradicts itself.

## Deliverables Completed
- [x] bin/ai-project-init installs governance.agent.md
- [x] Test asserts canonical filename; legacy-path assertion preserved
- [x] cli-usage-guide.md reconciled
- [x] Delivery Notice

## Definition of Done Status
- [x] `bin/ai-project-init` writes `.ai-project/agents/governance.agent.md` — confirmed by
      direct script invocation (see Execution Summary).
- [x] A test asserts the canonical filename; the legacy `.github/agents/` absence assertion
      (Option A) is preserved unchanged.
- [x] `docs/systems/cli-usage-guide.md` no longer names `hq.agent.md` as what the initializer
      installs and no longer contradicts its own Troubleshooting section.
- [x] No other live doc asserts the superseded filename as the initializer's output — see Notes
      for the full grep-confirmed accounting.
- [x] Full test suite passes: `pytest tests/` → **306 passed, 1 skipped** (matches the
      pre-existing baseline; no regressions). No embedded shell/Python test suite outside
      `tests/` references either filename (grep-confirmed).
- [x] Delivery Notice committed.
- [x] All changes on `epic/P7-M28-E28.3`, branched from `milestone/M28`.

## Branch and PR Information
- **Branch:** `epic/P7-M28-E28.3`
- **PR:** [Link — filled in after `gh pr create`]
- **Target:** `milestone/M28`

## Notes

**Full re-grep of `hq.agent.md` across the repo** (excluding `.git/`), classified:

- **Already confirmed correct by the Epic spec, not touched:** `governance/submodule-setup.md`,
  `governance/guides/ADOPTION-FAQ.md`, `governance/guides/integrations/github-copilot.md` — all
  treat `hq.agent.md` as legacy to remove, never as what the initializer installs.
- **This Epic's own governing/planning docs**, self-referentially describing the defect being
  fixed (expected and correct to retain the old name in problem-statement prose):
  `P7-M28-E28.3__epic-execution-chat-starter.md`, `P7-M28-E28.3__spec__init-canonical-agent-file.md`,
  `P7-M28__milestone-execution-chat-starter.md`, `P7-M28__milestone-spec.md`, `P7__phase-spec.md`,
  and the `2026-07-12` P7-spine steering note (records the P6-GH-15 backlog item by name).
- **Frozen historical records**, not live guidance: all P2-era phase/milestone/epic
  completion/spec/test-report docs (original M8 HQ-agent rollout), the two P6-M25-E25.3/E25.6
  epic records and P6 milestone-spec/phase-closure-declaration (the prior *path* fix, already
  closed), the two 2026-05-21 adoption-event records (describe the pre-E25.3 `.github/agents/`
  path era), and `README.md`'s M8 milestone-completion table row.
- **Not the initializer's output, out of this Epic's scope:** `governance/diagrams/repository-structure.md:24`
  describes `governance/agents/hq.agent.md` as a file within the `ai-project-system` repo's own
  `governance/` folder (the source stub still legitimately exists there, unchanged by this
  Epic) — not what `.ai-project/agents/` receives in a bootstrapped project.
  `governance/agents/hq.agent.md` itself (the stub file) is out of this Epic's named file
  scope.
- **Roadmap backlog entry, not this Epic's file scope:** `docs/roadmap/overview.md:239` still
  lists P6-GH-15 as open/tracked. This Epic's Technical Constraints scope the touchable files to
  three; closing out the roadmap tracking row is a parent-chat (Milestone/Phase) bookkeeping
  action, flagged here rather than done silently out-of-scope.
- **No live, current-facing doc remains asserting `hq.agent.md` as the initializer's output.**

**`install_hq_agent()` naming, per Non-Goals:** the function name and the fallback stub's
"under development in Milestone M8" prose were left untouched — M8 is long complete (P2-era)
and this staleness is a known, separately-tracked candidate carry-forward (noted in the Epic
spec's own Notes), not fixed here.

## Awaiting Review
Default-accept (PSG §11.6): clean delivery accepted by silence; merge awaits explicit human
authorization in-chat (SN-19).

---
**Coding Agent stopped per AI-OPERATING-GUIDELINES.md canonical happy path.**
