# Epic Execution Chat Starter — E26.3

**Epic:** E26.3 — First real run + cross-repo acceptance
**Phase:** P7 — Agentic Execution and Default-On Visuals
**Milestone:** M26 — First Real Agentic Run (P7-AE-1)
**Repository:** panchew/ai-project-system
**Branch Strategy:** `epic/P7-M26-E26.3` → PR to `milestone/M26`

> **Provenance:** Authored by the Milestone Chat (P7-M26) on 2026-07-12. Third and **final**
> Epic of the binding sequence E26.1 → E26.2 → E26.3 (HQ ruling 2026-07-11). E26.1
> (`e65f758`) and E26.2 (`4fa5f47`) are merged. This is M26's exit event.

> **Amended 2026-07-12** (per Escalation Notice `P7-M26-E26.3__escalation-notice__
> sandbox-and-tools-json-execution-adequacy.md`, resolved): (1) the Docker-sandboxed route
> (`alpine:latest`) is confirmed unreachable on this host — use the orchestrator's
> local-execution fallback (curated `PATH` omitting `docker`), authorized below; (2) the
> proving vehicle no longer requires `bin/ai-project-version` to be executable — the coding
> tool set cannot set permission bits. It is invoked as `python3 bin/ai-project-version`
> throughout. Both changes are reflected in the governing Epic spec (same commit series).

---

## Governance References

You are operating under the AI Project System governance framework (Epic mode).

**Authoritative governance documents:**
- PROJECT-SYSTEM-GUIDELINES.md v2.3.0
- AI-OPERATING-GUIDELINES.md v2.6.0

**Governance hierarchy:** PSG > AOG > this Starter > Epic Spec > execution decisions > system references > chat.

**Critical rules:**
- Documentation is authoritative; chat is ephemeral
- Follow the §1A canonical happy path for Epic closure
- Produce a Delivery Notice upon completion
- Stop after PR creation — **do not merge.** Under default-accept (PSG §11.6 / SN-19) a clean
  delivery is accepted by silence (no Review Decision on the happy path); the PR **merge**
  requires explicit human authorization, given in-chat (no Delivery Authorization artifact)
- Accept/reject decisions belong to the parent chats (human-backed), not to you
- **Escalate upward only, one hop:** if genuinely blocked (e.g., no reachable Ollama endpoint),
  stop and escalate to the Milestone Chat. Do **not** contact the Phase Chat, HQ, or
  `local-agent-runner`/its repository directly — that is out of this Epic's authority entirely.

---

## Epic Specification

**Full spec:** `docs/phases/P7__Agentic_Execution_and_Default_On_Visuals/P7-M26-E26.3__spec__first-real-run-and-cross-repo-acceptance.md`
**Commit:** `b2e230f` (on `milestone/M26`)

### Prerequisite Verification (do this first)
- `git ls-files --error-unmatch <spec path>` — confirm the spec is git-tracked (GH-1).
- Confirm E26.1 + E26.2 are merged on `milestone/M26`: `bin/run-dev-agent` exists and is
  executable; `.ai-project.yml:26` reads `epic_dev: local:qwen2.5-coder:14b`;
  `bin/ai-project-orchestrator:21` `DEFAULT_MODELS["epic_dev"]` agrees; suite baseline
  290 passed / 1 skipped.
- **Host-side (before writing any trigger):** confirm an Ollama endpoint is reachable and
  `qwen2.5-coder:14b` is pulled; confirm `local-agent-runner` is discoverable
  (`LOCAL_AGENT_RUNNER` env or on `PATH`). If any of this is unmet and you cannot resolve it
  yourself, **stop and escalate to the Milestone Chat** rather than improvising a substitute run.

### Summary

Execute M26's exit event: one real, non-mocked Epic run through `bin/ai-project-orchestrator`
Agentic Mode, on `qwen2.5-coder:14b`, using the adapter and wiring E26.1/E26.2 delivered.
Capture the transcript as evidence, and hand it to the Milestone Chat — you do not escalate
further yourself.

### Open Design Question A — already resolved (execute this, don't re-litigate)

The live run executes a purpose-built minimal **proving-vehicle epic**, `P7-M26-E26.3-PROVE`,
that you author as part of this Epic:

1. **Proving-vehicle spec** — `P7-M26-E26.3-PROVE__spec__version-script-proving-vehicle.md`,
   with a literal `## Definition of Done` heading (exact text — the adapter's DoD-extraction
   regex requires it), instructing the creation of `bin/ai-project-version`: a script that,
   when run as `python3 bin/ai-project-version` (**not** `./bin/ai-project-version` — do not
   instruct the model to make it executable; no available tool can set permission bits), reads
   `governance.version` from `.ai-project.yml` and prints it to stdout with a trailing newline,
   exit `0`, nothing else.
2. **Proving-vehicle test** — `tests/test_ai_project_version_script.py`: asserts the script
   exists; running `python3 bin/ai-project-version` exits 0 and its stdout (stripped) equals
   the `governance.version` value read independently via PyYAML. **No executable-bit
   assertion.** **Confirm it fails before the run** (script doesn't exist yet) — this proves
   the validation gate is real, not rigged.
3. **The trigger** (write at run time — `.ai-project/queue/` is not git-tracked except
   `README.md`):
   ```json
   {
     "epic_id": "P7-M26-E26.3-PROVE",
     "epic_spec_path": "docs/phases/P7__Agentic_Execution_and_Default_On_Visuals/P7-M26-E26.3-PROVE__spec__version-script-proving-vehicle.md",
     "sandbox_env": "alpine:latest",
     "dev_command": "./bin/run-dev-agent",
     "validation_command": "python3 -m pytest tests/test_ai_project_version_script.py -q",
     "run_dev_initially": true
   }
   ```
   `sandbox_env` stays for documentation continuity but is inert on this host — the run
   executes via the **local-execution fallback**: invoke the orchestrator with a curated `PATH`
   that omits `docker`, so `run_in_sandbox()`'s own `FileNotFoundError` handler fires (no
   frozen-file changes). Document the exact invocation in the run record.
4. **Do not hand-write `bin/ai-project-version` yourself.** It must be produced by the live
   model through the real orchestrator loop. If you find yourself typing that file's contents,
   the run hasn't actually happened — stop and diagnose why (host prerequisites, adapter
   discovery, endpoint reachability) instead of substituting your own work for the model's.

### Executing the run

1. Commit the proving-vehicle spec + test to `epic/P7-M26-E26.3` first (before writing the
   trigger).
2. Write the trigger JSON above to `.ai-project/queue/04_epic.json`.
3. Invoke `bin/ai-project-orchestrator` so it discovers and processes the trigger; document the
   exact invocation (args/env) you used.
4. Let the Dev-QA loop run (`MAX_RETRIES = 3`). On success the orchestrator stages/commits and
   clears the trigger itself — don't hand-author that commit. On non-convergence, capture the
   failure state faithfully (feedback.json, `04_epic.json.failed`) and **escalate to the
   Milestone Chat** rather than retrying indefinitely or hand-fixing the proving vehicle.
5. Write the run record: `P7-M26-E26.3__run-record.md` — what ran, trigger contents verbatim,
   model, validation outcome per attempt, iterations/tokens (from the transcript's
   `iterations`/`tokens` fields), duration, links to the artifact files. **State explicitly that
   `final_answer` was not consulted** — the QA exit code decided the outcome.

### The cross-repo hand-back — where this Epic's job ends

Per the Epic spec's Non-Goals: you do **not** issue the cross-repo Escalation Notice. That
artifact (Milestone Chat (P7-M26) → Phase Chat (P7), requesting relay to HQ → CFO →
`local-agent-runner` P2-M3) is the **Milestone Chat's own act**, after this Epic's delivery is
accepted. Your job is to make the evidence (transcript + run record) complete, accurate, and
git-tracked — then stop.

---

## Deliverables
1. [ ] `P7-M26-E26.3-PROVE__spec__version-script-proving-vehicle.md`
2. [ ] `tests/test_ai_project_version_script.py` (fails before the run)
3. [ ] `bin/ai-project-version` (model-produced, via the live run)
4. [ ] Transcript + context + run-metadata under `.ai-project/artifacts/agentic-runs/P7-M26-E26.3-PROVE/`
5. [ ] `P7-M26-E26.3__run-record.md`
6. [ ] **Epic Delivery Notice**

---

## Definition of Done
- [ ] `P7-M26-E26.3-PROVE` completed through the orchestrator, non-mocked, on `qwen2.5-coder:14b`
- [ ] Success determined by the QA `validation_command` exit code + transcript — confirmed in
      the run record; `final_answer` explicitly stated as not consulted
- [ ] Transcript/context/run-metadata git-tracked under `.ai-project/artifacts/agentic-runs/P7-M26-E26.3-PROVE/`
- [ ] Run record committed, accurate
- [ ] `bin/ai-project-version` exists, model-produced, proving-vehicle test passes
- [ ] Cross-repo hand-back evidence complete and delivered to the Milestone Chat (you do not
      escalate further — see above)
- [ ] Full suite passes (290/1 baseline + new proving-vehicle test)
- [ ] Delivery Notice committed; all changes on `epic/P7-M26-E26.3`; PR opened to `milestone/M26`

---

## Acceptance Criteria
- [ ] A recorded live-run transcript shows `P7-M26-E26.3-PROVE` completing through the
      orchestrator on `qwen2.5-coder:14b`, non-mocked
- [ ] Open Design Question A's resolution matches what actually ran (or deviations are
      documented and justified in the run record)
- [ ] The cross-repo hand-back evidence is on record and ready for the Milestone Chat's escalation

---

## Technical Constraints
- **No changes to E26.1/E26.2 surfaces:** `bin/run-dev-agent`, `.ai-project/agents/tools.json`,
  `.ai-project.yml`, `bin/ai-project-orchestrator`, `tests/mocks/`, `bin/verify-loop.sh`. If any
  of these is inadequate for the real run, that's a blocker to escalate, not a workaround to
  patch here.
- **The proving vehicle stays minimal** — one script, one test, nothing else. Note anything
  else you notice; don't fix it here.
- **Escalate upward, one hop, to the Milestone Chat only** — never to Phase Chat, HQ, or the
  other repo directly.
- **Scope of files:** the proving-vehicle spec + test, `bin/ai-project-version` (model-produced),
  the run's artifact directory, the run record, this Epic's Delivery Notice. Nothing else.

---

## Execution Contract

### Your Responsibilities (Coding Agent)
1. Create branch `epic/P7-M26-E26.3` from `milestone/M26`
2. Verify prerequisites (spec git-tracked; E26.1+E26.2 merged; host-side Ollama/runner reachable)
3. Author + commit the proving-vehicle spec and test; confirm the test fails pre-run
4. Write the trigger; invoke the orchestrator; let the loop run; capture the outcome faithfully
5. Write the run record; run the full suite
6. Commit to `epic/P7-M26-E26.3`; open a PR to `milestone/M26`
7. Produce the Epic Delivery Notice; stop — **do not merge**, **do not escalate beyond the
   Milestone Chat** (human merge authorization is in-chat, SN-19/§11.6)

### What You Must NOT Do
- ❌ Merge the PR / infer acceptance
- ❌ Hand-write `bin/ai-project-version` yourself
- ❌ Rig or skip the pre-run test-failure check
- ❌ Issue the cross-repo Escalation Notice (Milestone Chat's job)
- ❌ Contact `local-agent-runner`, its chats, or its repository, directly or indirectly
- ❌ Touch E26.1/E26.2 surfaces
- ❌ Substitute a scripted/simulated run for the real orchestrator-driven one, even if blocked —
  escalate instead
- ❌ Iterate without explicit instruction from the parent chat

---

## Reference Materials
- `docs/phases/P7__Agentic_Execution_and_Default_On_Visuals/P7-M26-E26.3__spec__first-real-run-and-cross-repo-acceptance.md` (your Epic spec — authoritative)
- `docs/phases/P7__Agentic_Execution_and_Default_On_Visuals/P7-M26__milestone-spec.md` (Epic Detail → E26.3; binding context)
- `.ai-project/artifacts/rulings/2026-07-11__ai-project-system-hq__ruling__p7-ae-1-adapter-unblocks-runner-p2.md` (binding HQ ruling)
- `.ai-project/queue/README.md` (live trigger convention, E26.2)
- `bin/run-dev-agent` module docstring (E26.1 — adapter behavior, exit codes, artifact paths)
- `governance/templates/escalation-notice.md` (why this Epic stops at the Milestone Chat)

---

## Canonical Happy Path (reminder — SN-19/§11.6)
Execution → Delivery Notice → **[STOP]** → default-accept by silence (Review Decision =
exception path only) → **human merge authorization in-chat** → merge → stop. **You own the
execution and the Delivery Notice; then you stop.**

## Epic Delivery Notice Template

**File:** `docs/phases/P7__Agentic_Execution_and_Default_On_Visuals/P7-M26-E26.3__delivery-notice.md`

```markdown
---
type: delivery-notice
epic: E26.3
status: delivered
delivery_date: <YYYY-MM-DD>
---

# Epic Delivery Notice — E26.3

**Epic:** E26.3 — First real run + cross-repo acceptance
**Status:** Execution Complete, Delivered for Review
**Delivery Date:** <YYYY-MM-DD>

## Execution Summary
[Proving vehicle authored; trigger written; orchestrator invocation; loop outcome (attempts,
convergence); run record highlights]

## Deliverables Completed
- [ ] Proving-vehicle spec
- [ ] Proving-vehicle test (confirmed failing pre-run)
- [ ] bin/ai-project-version (model-produced)
- [ ] Transcript/context/run-metadata (git-tracked)
- [ ] Run record
- [ ] Delivery Notice

## Definition of Done Status
[Confirm each — incl. "success = QA exit code + transcript, final_answer not consulted",
artifact paths, suite result, hand-back evidence readiness]

## Branch and PR Information
- **Branch:** `epic/P7-M26-E26.3`
- **PR:** [Link]
- **Target:** `milestone/M26`

## Notes
[Which reachability path was used (sandbox vs. local fallback); attempt count; anything the
Milestone Chat needs for its own escalation]

## Awaiting Review
Default-accept (PSG §11.6): clean delivery accepted by silence; merge awaits explicit human
authorization in-chat (SN-19). **Cross-repo hand-back escalation is the Milestone Chat's own
next act, not part of this delivery.**

---
**Coding Agent stopped per AI-OPERATING-GUIDELINES.md canonical happy path.**
```

---

## Exit Conditions
Stop when all deliverables are committed, the PR is open to `milestone/M26`, and the Delivery
Notice is produced. Then report: "Epic E26.3 execution complete. Epic Delivery Notice produced.
Awaiting review (default-accept) and human merge authorization. Cross-repo hand-back evidence is
ready for the Milestone Chat's escalation."

## Begin Execution
1. Create branch `epic/P7-M26-E26.3` from `milestone/M26`
2. Verify prerequisites, including host-side reachability — escalate immediately if unmet
3. Author the proving vehicle; run it for real; record it; open the PR; deliver

Copy the entire chat starter above and paste into the Governance Agent (Epic mode) to begin execution.
