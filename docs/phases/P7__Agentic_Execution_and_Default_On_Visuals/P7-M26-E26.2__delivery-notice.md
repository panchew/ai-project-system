---
type: delivery-notice
epic: E26.2
status: delivered
delivery_date: 2026-07-12
---

# Epic Delivery Notice — E26.2

**Epic:** E26.2 — Real-model wiring + mock retirement
**Status:** Execution Complete, Delivered for Review
**Delivery Date:** 2026-07-12

## Execution Summary

- **Model switch:** `.ai-project.yml:26` `epic_dev` moved `local:llama3:8b` →
  `local:qwen2.5-coder:14b` (llama3:8b is verified unusable for tool-calling — empty
  tool-call responses, local-agent-runner CONTRACT §1.4).
- **DEFAULT_MODELS consistency call — decided: both move.** The orchestrator's in-script
  default (`bin/ai-project-orchestrator:21`) now also reads `local:qwen2.5-coder:14b`, so an
  absent `models:` block cannot silently resurrect an unusable model. Config and script default
  agree, enforced by test.
- **yml-spec default rows:** `governance/ai-project-yml-spec.md` updated consistently — schema
  comment (L58), §3.4 field-table default/examples (L330, with a "must be tool-calling-capable"
  role note), and the format-constraint examples (L336, now explicitly labeled format examples,
  not defaults). Doc bumped to **v2.2.0** with a Changelog entry per its own conventions;
  `.ai-project.yml`'s spec-pointer comment updated to v2.2.0.
- **Live-doc wiring:** `.ai-project/queue/README.md` now documents the live `04_epic.json`
  trigger with `dev_command: ./bin/run-dev-agent` (also the orchestrator default at L317) and a
  real `validation_command` (`python3 -m pytest -q`), notes model routing via
  `AI_PROJECT_ACTIVE_MODEL`, and states that success is decided by the QA `validation_command`
  exit code — never the dev agent's own output.
- **Mock CI-retention boundary — decided: retain as regression harness** (the spec's
  recommended boundary). `tests/mocks/mock_{dev,qa}.sh` + `bin/verify-loop.sh` are kept as the
  loop-logic verification-only regression harness, each labeled with a header comment stating
  they are never the live path; the queue README carries the same boundary note. Rationale: the
  verified consumer map shows pytest never runs the mocks (their only consumer is
  `bin/verify-loop.sh`), so retention costs nothing, preserves the P3-M12-E12.3 loop-regression
  capability, and avoids an out-of-scope pytest port.

## Deliverables Completed

- [x] `.ai-project.yml` `epic_dev` switch → `local:qwen2.5-coder:14b`
- [x] `DEFAULT_MODELS` consistency call — decided (both move, must agree) and applied
- [x] yml-spec default rows consistent (L58/L330; L336 reworded as format-example-only); doc
      v2.1.0 → v2.2.0 + Changelog entry
- [x] Live trigger docs → adapter (`.ai-project/queue/README.md` names `./bin/run-dev-agent`)
- [x] Mock boundary labeled (3 file headers) + documented (queue README + this notice)
- [x] Consistency-guard test (`tests/test_model_config.py`, 4 tests)
- [x] Delivery Notice (this document)

## Definition of Done Status

- [x] `.ai-project.yml` `epic_dev` is `local:qwen2.5-coder:14b`
- [x] Live epic-execution path invokes `bin/run-dev-agent` — orchestrator default L317 confirmed
      unchanged; no live/documented flow overrides `dev_command` to a mock
- [x] `04_epic.json` mock trigger retired from the live path; CI boundary documented; trigger
      *mechanism* untouched (zero changes to `04_epic.json` handling — orchestrator diff is
      the single `DEFAULT_MODELS` line)
- [x] `DEFAULT_MODELS` call + yml-spec default rows decided/updated/documented
- [x] **Grep-proven:**
      - `grep -rn "llama3:8b"` outside frozen phase docs/artifacts returns only: the yml-spec
        Changelog entry (history), `docs/roadmap/overview.md:235` (describes this switch — not
        a definition), and the guard test's own assertions. **Nothing defines `epic_dev` as
        `llama3:8b`.**
      - `grep -rln "tests/mocks|mock_dev|mock_qa"` outside frozen history hits only the mocks
        themselves, `bin/verify-loop.sh` (retained harness), and the queue README's
        verification-only boundary note. **No live or documented flow routes epic execution
        through `tests/mocks/`.**
- [x] Full suite passes: **290 passed / 1 skipped** (286/1 baseline + 4 new guard tests)
- [x] All changes committed to `epic/P7-M26-E26.2`; PR opened to `milestone/M26`

## Acceptance Criteria Status

- [x] `epic_dev` is off `llama3:8b` everywhere it is defined (config, script default,
      reference-doc default rows — grep evidence above)
- [x] No live or documented flow routes epic execution through the mock scripts
- [x] Trigger mechanism (`04_epic.json`) intact and ready for E26.3's real trigger

## Branch and PR Information

- **Branch:** `epic/P7-M26-E26.2`
- **PR:** [Recorded after PR creation]
- **Target:** `milestone/M26`

## Notes

- **Boundary decision for the record:** retain-and-label (recommended option). The alternative
  (fold verify-loop scenarios into pytest, drop the shell harness) was not taken — it is larger
  than M26 needs and would delete a working regression capability for no acceptance gain.
- **For E26.3:** everything is now in place — adapter merged (E26.1), `epic_dev` =
  `local:qwen2.5-coder:14b` in both definition sites, live docs name the adapter with a real
  `validation_command`, trigger mechanism untouched. E26.3 needs the host-side Ollama endpoint
  with `qwen2.5-coder:14b` pulled (CFO prerequisite) and resolves Open Design Question A
  (proving vehicle) in its own spec. Success remains QA `validation_command` exit code +
  transcript — never `final_answer`.
- E26.1 surfaces (`bin/run-dev-agent`, `.ai-project/agents/tools.json`, adapter tests) and
  `epic_qa` (`local:qwen2.5-coder:7b`) untouched, per constraints.

## Awaiting Review

Default-accept (PSG §11.6): clean delivery accepted by silence; merge awaits explicit human
authorization in-chat (SN-19).

---
**Coding Agent stopped per AI-OPERATING-GUIDELINES.md canonical happy path.**
