# Epic Execution Chat Starter — E28.3

**Epic:** E28.3 — init canonical agent file
**Phase:** P7 — Agentic Execution and Default-On Visuals
**Milestone:** M28 — Governance Reconciliations
**Repository:** panchew/ai-project-system
**Branch Strategy:** `epic/P7-M28-E28.3` → PR to `milestone/M28`

> **Provenance:** Authored by the Milestone Chat (P7-M28) on 2026-07-13. E28.3 is fully
> independent of every other M28 epic (disjoint files) and may run in parallel with any of
> them. This is the smallest of the four M28 epics.

---

## Governance References

You are operating under the AI Project System governance framework (Epic mode).

**Authoritative governance documents:**
- PROJECT-SYSTEM-GUIDELINES.md v2.3.0
- AI-OPERATING-GUIDELINES.md v2.8.0 — verify the exact version in force on `milestone/M28` at
  session start rather than trusting this number.

**Governance hierarchy:** PSG > AOG > this Starter > Epic Spec > execution decisions > system
references > chat.

**Critical rules:**
- Documentation is authoritative; chat is ephemeral
- Follow the §1A canonical happy path for Epic closure
- Produce a Delivery Notice upon completion
- Stop after PR creation — **do not merge.** Under default-accept (PSG §11.6 / SN-19) a clean
  delivery is accepted by silence (no Review Decision on the happy path); the PR **merge**
  requires explicit human authorization, given in-chat (no Delivery Authorization artifact)
- Accept/reject decisions belong to the parent chats (human-backed), not to you
- **You are a script/test/doc reconciliation Epic** — no new capability, minimal surface area

---

## Epic Specification

**Full spec:** `docs/phases/P7__Agentic_Execution_and_Default_On_Visuals/P7-M28-E28.3__spec__init-canonical-agent-file.md`
**Commit:** committed alongside this starter on `milestone/M28` — resolve the exact hash with
`git log --oneline -1 -- <spec path>` before relying on it.

### Prerequisite Verification (do this first)
- `git ls-files --error-unmatch <spec path>` — confirm the spec is git-tracked (GH-1).
- Confirm anchors: `bin/ai-project-init:328-329,408`; `tests/test_init_agent_path.py`
  (existing test asserts `hq.agent.md`); `governance/agents/hq.agent.md` (superseded stub,
  965 bytes); `governance/agents/governance.agent.md` (canonical, 14,352 bytes);
  `docs/systems/cli-usage-guide.md:36,75` (superseded filename) vs. its own
  Troubleshooting section (already canonical).
- Re-grep `hq.agent.md` and `governance.agent.md` across live (non-historical) docs before
  editing, in case anything drifted since spec authoring.

### Summary

Fix `bin/ai-project-init` to source and install `governance/agents/governance.agent.md` (not
the superseded `hq.agent.md` stub) as `.ai-project/agents/governance.agent.md`; extend the
existing filename-path test to assert the canonical filename; reconcile
`docs/systems/cli-usage-guide.md`'s two stale filename references to match its own
already-correct Troubleshooting section.

### Binding constraints (carry into every decision — verbatim from the Epic spec)

- **The path is already correct** (`.ai-project/agents/`, fixed by E25.3) — this Epic changes
  only the **filename**. Do not touch path logic.
- **Preserve the existing `.github/agents/` absence assertion** in the test (Option A,
  canonical-only) — do not weaken or remove it.
- **Do NOT edit** `governance/submodule-setup.md`, `ADOPTION-FAQ.md`, or
  `github-copilot.md` — confirmed already correct (they treat `hq.agent.md` as legacy to
  remove, never as what the initializer installs).
- **Do NOT rewrite the fallback stub's "Milestone M8" prose** or rename
  `install_hq_agent()` — out of scope; note as a candidate carry-forward if found, do not fix.

### Design points (yours to resolve — decide, document, move; escalate only if truly blocked)

1. **Whether to touch `install_hq_agent()`'s in-function stub filename references** — fix any
   filename mentions inside the function for consistency with the rename; leave the stub's
   "under development" prose untouched (Non-Goals).

---

## Deliverables
1. [ ] `bin/ai-project-init` installs `governance.agent.md` (lines 328-329, 408; in-function
       filename references reconciled)
2. [ ] `tests/test_init_agent_path.py` asserts the canonical filename; legacy-path assertion
       preserved
3. [ ] `docs/systems/cli-usage-guide.md` reconciled (lines 36, 75)
4. [ ] **Epic Delivery Notice**

---

## Definition of Done
- [ ] `bin/ai-project-init` writes `.ai-project/agents/governance.agent.md`
- [ ] A test asserts the canonical filename; legacy-path absence assertion preserved
- [ ] `docs/systems/cli-usage-guide.md` no longer contradicts itself on the installed filename
- [ ] No other live doc asserts the superseded filename as the initializer's output (confirmed
      by grep, recorded)
- [ ] Full test suite passes
- [ ] Delivery Notice committed; all changes on `epic/P7-M28-E28.3`; PR opened to
      `milestone/M28`

---

## Acceptance Criteria
- [ ] `bin/ai-project-init` installs `governance.agent.md`

---

## Technical Constraints
- **Scope of files:** `bin/ai-project-init` (lines 328-329, 408 + in-function filename
  references only), `tests/test_init_agent_path.py`, `docs/systems/cli-usage-guide.md`
  (lines 36, 75 only). Do **NOT** touch any other file.
- **No AOG/PSG version bump** — no AOG/PSG content is touched.

---

## Execution Contract

### Your Responsibilities (Coding Agent)
1. Create branch `epic/P7-M28-E28.3` from `milestone/M28`
2. Verify prerequisites; re-grep for drift since spec authoring
3. Fix `bin/ai-project-init` (source path, dest filename, the later `git add`, and any
   in-function stub filename references)
4. Extend `tests/test_init_agent_path.py` to assert the canonical filename; run it against the
   actual script (`bash bin/ai-project-init test-proj --dir <tmp> --skip-git
   --skip-submodule`), don't just reason about it
5. Reconcile `docs/systems/cli-usage-guide.md`'s two stale references
6. Run the full suite; commit to `epic/P7-M28-E28.3`; open a PR to `milestone/M28`
7. Produce the Epic Delivery Notice; stop — **do not merge** (human merge authorization is
   in-chat, SN-19/§11.6)

### What You Must NOT Do
- ❌ Merge the PR / infer acceptance
- ❌ Touch path logic (already correct) or the `.github/agents/` absence assertion
- ❌ Edit `submodule-setup.md`, `ADOPTION-FAQ.md`, or `github-copilot.md`
- ❌ Rewrite the fallback stub's prose or rename `install_hq_agent()`
- ❌ Touch any frozen historical P2-era record
- ❌ Iterate without explicit instruction from the parent chat

---

## Reference Materials
- `docs/phases/P7__Agentic_Execution_and_Default_On_Visuals/P7-M28-E28.3__spec__init-canonical-agent-file.md` (your Epic spec — authoritative)
- `docs/phases/P7__Agentic_Execution_and_Default_On_Visuals/P7-M28__milestone-spec.md` (Epic Detail → E28.3; binding context)
- `bin/ai-project-init` (lines 300-345, 405-412)
- `tests/test_init_agent_path.py`
- `governance/agents/hq.agent.md`, `governance/agents/governance.agent.md`
- `docs/systems/cli-usage-guide.md`

---

## Canonical Happy Path (reminder — SN-19/§11.6)
Execution → Delivery Notice → **[STOP]** → default-accept by silence (Review Decision =
exception path only) → **human merge authorization in-chat** → merge → stop. **You own the
execution and the Delivery Notice; then you stop.**

## Epic Delivery Notice Template

**File:** `docs/phases/P7__Agentic_Execution_and_Default_On_Visuals/P7-M28-E28.3__delivery-notice.md`

```markdown
---
type: delivery-notice
epic: E28.3
status: delivered
delivery_date: <YYYY-MM-DD>
---

# Epic Delivery Notice — E28.3

**Epic:** E28.3 — init canonical agent file
**Status:** Execution Complete, Delivered for Review
**Delivery Date:** <YYYY-MM-DD>

## Execution Summary
[Confirm the three-line script fix; confirm the test was run against the actual script, not
just reasoned about; confirm cli-usage-guide.md's two references reconciled]

## Deliverables Completed
- [ ] bin/ai-project-init installs governance.agent.md
- [ ] Test asserts canonical filename; legacy-path assertion preserved
- [ ] cli-usage-guide.md reconciled
- [ ] Delivery Notice

## Definition of Done Status
[Confirm each — incl. actual script run output, suite result]

## Branch and PR Information
- **Branch:** `epic/P7-M28-E28.3`
- **PR:** [Link]
- **Target:** `milestone/M28`

## Notes
[Final grep confirmation that no other live doc asserts the superseded filename; the
install_hq_agent() "Milestone M8" stub / function-name finding, if not touched]

## Awaiting Review
Default-accept (PSG §11.6): clean delivery accepted by silence; merge awaits explicit human
authorization in-chat (SN-19).

---
**Coding Agent stopped per AI-OPERATING-GUIDELINES.md canonical happy path.**
```

---

## Exit Conditions
Stop when all deliverables are committed, the PR is open to `milestone/M28`, and the Delivery
Notice is produced. Then report: "Epic E28.3 execution complete. Epic Delivery Notice produced.
Awaiting review (default-accept) and human merge authorization."

## Begin Execution
1. Create branch `epic/P7-M28-E28.3` from `milestone/M28`
2. Verify prerequisites; re-grep for drift
3. Fix the script, extend the test, run it for real, reconcile the doc, open the PR, deliver

Copy the entire chat starter above and paste into the Governance Agent (Epic mode) to begin execution.
