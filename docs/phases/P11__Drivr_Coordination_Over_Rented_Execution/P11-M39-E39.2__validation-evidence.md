---
project: ai-project-system
phase: P11
milestone: M39
epic: E39.2
type: validation-evidence
status: pre-registered
last_updated: 2026-08-16
---

# E39.2 Validation Evidence — E39.1's completion judgment against the six preserved runs

> **This file was created and committed in the pre-registration state below, with §1 complete and
> every later section empty, BEFORE any case was run.** The commit that added it is the
> pre-registration; the commit that fills in §3 onward is the result. **A reviewer establishes the
> order from `git log --follow` on this path, not from this sentence.**

---

## 1. Pre-registration — recorded before a single case was run

**Spec obligation:** E39.2 spec §Pre-registration, a Definition-of-Done item, not a recommendation.
The milestone requires that post-hoc tuning be disclosed; this epic makes the disclosure
**checkable by diff** rather than believable by assertion.

| Item | Value |
|---|---|
| **Pre-registered mechanism SHA** | **`715099cb94a9f7c010cde1c22e455d4b41161a14`** (`715099c`) |
| Repository | `drivr`, branch `main`, working tree clean (`git status --porcelain` empty) |
| Commit subject | `feat(E39.1): the completion judgment, above ExecutionResult` |
| Parent | `31dad51` (E38.2's adapter surface) |
| **Date of pre-registration** | **2026-08-16**, 16:05Z (host clock, `date -u`) |
| Mechanism entry point | `judge_completion(from_runner_transcript(payload, origin=…))` — `drivr.judgment` |
| **Command that will be run** | `PYTHONPATH=/home/panchew/soft-dev/drivr python3 .ai-project/artifacts/validation-runs/P11-M39-E39.2/validate_completion_judgment.py` |

**`P11-GH-2` — layer, time, scope of this verification.** *Layer:* the host filesystem and the two
local git repositories, not a container and not GitHub. *Time:* 2026-08-16T16:05:23Z. *Scope:*
`drivr@715099c` as it exists in the local working copy at `/home/panchew/soft-dev/drivr`. *Literal
vs rendered:* the SHA above is the literal 40-character object name, quoted whole; the 7-character
form is a convenience and is never the citation.

**drivr has no git remote** (`git remote -v` empty, verified 2026-08-16). This was recorded by
E39.1's Delivery Notice and is restated because it bounds what a reviewer can do: re-measurement of
drivr (G2) is performable **on this machine at `715099c`** and nowhere else.

### What the pre-registration does and does not bind

- It binds **the mechanism** — `drivr@715099c`. If any file under `drivr/judgment/` changes after
  this point, the change is **post-hoc by definition** and §7 records the new SHA, the `git diff`,
  and the reason.
- It does **not** bind the **harness** — the script named above is this epic's instrument, not the
  mechanism, and it did not exist when this section was committed. The harness reads artifacts and
  calls the public API described in `drivr/docs/completion-judgment.md` §7. It is committed with the
  results so a reader can re-run the validation (Acceptance Criterion 1).
- **No case had been run when this section was committed.** Not the binding pair, not the held-out
  four, not a probe.

### The order this epic will execute in

1. This pre-registration — **committed** ✅
2. Harness written against the public API in the E39.1 description
3. **Binding pair** — E33.2 Run A, then E33.4. Verdicts recorded.
4. **Held-out four**, after the binding verdicts are recorded, never tuned against
5. Final SHA re-read and compared to the pre-registered SHA
6. Evidence, limits statement, Delivery Notice

---

## 2. Prerequisite verification — performed before pre-registration

| Check | Result | How |
|---|---|---|
| Branch | `epic/P11-M39-E39.2`, created from `milestone/M39` | `git log --oneline epic/P11-M39-E39.2..milestone/M39` → **empty** |
| E39.1 merged to `milestone/M39` | **yes** — `a2ba2c7` *Merge pull request #201 from panchew/epic/P11-M39-E39.1* | `git log --oneline -3 milestone/M39` |
| All preserved runs tracked | **15/15 paths tracked** | `git ls-files --error-unmatch <path>` **on the branch** — not a disk check |
| Model verification (`P9-M31-E31.3`) | **match** — harness reports `claude-opus-5`; `.ai-project.yml` `models.epic_manual` = `remote:claude-opus-5` | `.ai-project.yml:57` |

**Inventory is a floor (method obligation 6), and it moved again.** The Starter lists the three
`-PROVE` directories as `transcript.json` + `run-metadata.json`. `git ls-files` shows **each also
carries a `context.md`** — 18 tracked files across the five directories, not the 15 the Starter
enumerates. Nothing turns on it here (`context.md` is the prompt, not evidence about the outcome)
and it is recorded because this epic exists partly because an inventory of two was six.

---

## 3. Results

*(empty at pre-registration time — filled in after the runs)*

---

## 4. Limits

*(empty at pre-registration time)*
