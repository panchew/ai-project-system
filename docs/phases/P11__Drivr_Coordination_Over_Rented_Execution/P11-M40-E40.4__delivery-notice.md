---
artifact_type: delivery_notice
artifact_version: 1.0
timestamp: 2026-08-17T20:05:00Z
issuer_chat: Epic Agent (P11-M40-E40.4)
issuer_role: Epic Agent
status: delivered
epic_id: P11-M40-E40.4
milestone_id: P11-M40
phase_id: P11
project_name: ai-project-system
merge_details:
  pr_number: 210
  pr_url: https://github.com/panchew/ai-project-system/pull/210
  merge_commit: "<not merged — Epic Chats do not merge; awaiting Milestone Chat authorization>"
  merge_timestamp: "<pending>"
  merge_strategy: merge
  target_branch: milestone/M40
duration:
  start_date: 2026-08-17
  end_date: 2026-08-17
  elapsed_days: 0
final_artifacts:
  - name: The review configuration and its evidence (D1, D2)
    path: docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M40-E40.4__review-configuration.md
    type: report
  - name: The findings (D3)
    path: docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M40-E40.4__review-findings.md
    type: report
  - name: Nothing was auto-applied, and the precedent note (D4, D5)
    path: docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M40-E40.4__authority-ceiling-evidence.md
    type: report
  - name: Competing-model reviewer (local, Ollama) — invoked, never triggered
    path: .ai-project/artifacts/reviews/P11-M40-E40.4/run_competing_review.py
    type: implementation
  - name: Read-only state and configuration capture
    path: .ai-project/artifacts/reviews/P11-M40-E40.4/capture_state.py
    type: implementation
  - name: Raw captures — Copilot review, Copilot comments, local model findings
    path: .ai-project/artifacts/reviews/P11-M40-E40.4/
    type: report
  - name: Before/after state snapshots (D4)
    path: .ai-project/artifacts/reviews/P11-M40-E40.4/state-before.json
    type: report
completion_notice_timestamp: 2026-08-17T20:05:00Z
review_decision_timestamp: "<pending — Milestone Chat Stage-2 review>"
---

# Delivery Notice: P11-M40-E40.4 — Competing-Model PR Review, Findings Only

## Summary

**GitHub Copilot and a local `qwen3-coder:30b` reviewed PR #173 for performance, security and
scalability. 23 findings were captured verbatim and committed in this repository. None was acted on.
None was resolved. No configuration grants any model authority, and that was verified at the layer
where it lives — GitHub — rather than inferred from a tree that does not contain it.**

The epic's own prerequisite checks produced **two live `P11-GH-2` instances**, one of which would
have raised a false blocker against the phase's final milestone.

## Merge Details

- PR Number: **#210**
- PR URL: https://github.com/panchew/ai-project-system/pull/210
- Target Branch: `milestone/M40`
- Merge Commit: **not merged** — awaiting Milestone Chat Stage-2 review and human-keyed authorization
- Merge Strategy: merge

## Duration

- Started: 2026-08-17
- Completed: 2026-08-17
- Total Time: under one day

## Deliverables

### D1 — Copilot plus a competing model, configured

**Copilot**, requested per-PR by a single API call at `2026-08-17T19:44:17Z`. **No repository file
was added**, and none is needed.

**`qwen3-coder:30b`**, local via Ollama at `localhost:11434`, driven by a committed script.

**All four delegated decisions were made, documented, and their costs stated:**

1. **Competing model: local.** Coherent with the phase spine, avoids a second external dependency in
   the phase's final milestone. **Cost, stated and demonstrated:** a much weaker reviewer than
   Copilot — every line number it produced was wrong.
2. **PR: #173**, optimised for **relevance over size**. It is the PR the CFO's §11.6.1 review applies
   to at phase closure. It was also the only open PR — every M40 epic PR had already merged.
3. **Findings captured** as verbatim markdown in `docs/phases/` plus raw JSON in
   `.ai-project/artifacts/reviews/P11-M40-E40.4/`.
4. **Invoked per PR, not triggered.** The spec permits automatic triggering; this epic **declined the
   permitted half too**, because automatic triggering would have required adding repository
   configuration and R1 records that whatever is added becomes the precedent. **Cost, stated:** a
   human must invoke the reviewers.

### D2 — The ceiling demonstrated, verified at the right layer

Read from **GitHub** at `2026-08-17T19:51:32Z`: no branch protection on `master`, `phase/P11` or
`milestone/M40` (404 each); **no rulesets**; **no webhooks**; no `CODEOWNERS`; `allow_auto_merge:
false`; no deploy keys; `panchew` the only account with write access. Copilot's review state is
**`COMMENTED`**.

**The inventory is a floor — extended from the spec's five prohibited paths to twelve**, and one is
recorded as **UNVERIFIED**: GitHub App installations cannot be enumerated with a user token
(`repos/.../installations` → 404, `user/installations` → 403). **Recorded as a gap, not assumed
clean.**

### D3 — 23 findings, verbatim

**Copilot 3** (all three verified accurate, with correct locations). **`qwen3-coder:30b` 20.**
Coverage limits stated for both: Copilot reviewed **90 of 187** changed files and says so; the local
model was scoped to the **3 shipped `bin/` scripts, 1,078 lines**. **The two reviewers overlap on
nothing** — no agreement existed to convert into a verdict, and no mechanism to convert it was built.

### D4 — Nothing auto-applied or auto-resolved, measured

Snapshots taken **before any finding existed** and **after both reviewers finished**. The three
reviewed files are **byte-identical by blob SHA**; head SHA, PR state, `merged`, `auto_merge`,
`mergeable_state`, commit and file counts, protection, rulesets and webhooks all unchanged. **All
three Copilot threads stand `isResolved: false` on delivery.** No `suggestion` blocks were produced.
Three limits are stated, including that the observation window is four minutes.

### D5 — The precedent note

**This epic added no repository-side review configuration at all**, so R1's clean start survives
intact. Eight prohibitions are recorded for a later reader — including that a `success` check
conclusion is **not** an approval, that `requested_reviewers` is **not** a reliable answer, and that
Copilot's own review body offers a **one-click link to create this repository's first `.github/`
file**.

## The two `P11-GH-2` instances

1. **`requested_reviewers` read `[]` while Copilot was actively reviewing.** The timeline and GraphQL
   layers both said otherwise. A negative control proved the endpoint returns **HTTP 200 for a
   reviewer that does not exist**, so the status code is not evidence either. Trusting the obvious
   field would have escalated *"Copilot review cannot be enabled"* — **a false blocker against M40**.
2. **`.github/` is absent from the tree while an `active` workflow exists on GitHub** —
   `dynamic/agents/copilot-pull-request-reviewer`, created nine seconds after the review request,
   producing a check run on the head commit. A tree read reports "no workflows, no checks."

## Surfaced to the Milestone Chat, not acted on

**Copilot Finding A2 is correct:** PR #173's description lists two M36 planning documents while the
PR changes **187 files**. #173 is the PR the CFO's §11.6.1 diff review applies to at phase closure,
so its description is a document a CFO reads before reviewing. **Acting on it is out of scope for
this epic** (spec §Out of Scope: *report; do not fix*), and it does not meet the "serious enough to
demand action" escalation bar — nothing is unsafe. It is pointed at here for the levels that own
that PR.

## Verification

- **Model verification (P9-M31-E31.3):** harness `claude-opus-5` matches `.ai-project.yml`
  `models.epic_manual: remote:claude-opus-5` ✓
- **Branch:** verified before every commit; `epic/P11-M40-E40.4` throughout. Push verified at
  `origin` — `243c6e5` at remote matches local HEAD.
- **Suite:** **549 passed**, `PYTHONPATH=. pytest -q`. Baseline **549**, **delta 0** — this epic adds
  no tests. `P10-GH-10` did not fire. **Drivr not touched**, so its 357 baseline is not restated as a
  measurement here.
- **Constraint 8:** no normative document amended → **no Structural diagram owed**.
- **`P11-GH-3` not allocated**; no new `GH-` ID filed.
- **G2 (the reviewer re-measures):** R1, R2 and R3 were all re-measured at the epic's start rather
  than taken from the spec. R1 and R3 confirmed; R2 resolved to **enabled**.

## Self-corrections during execution

1. **Local finding count.** Stated as 15 mid-execution and written as 15 into the configuration
   record; the actual count is **20** (8 + 9 + 3). Corrected in the record before commit and noted
   here rather than silently fixed, since this phase tracks count errors.
2. **Characterisation of the local model's findings.** After checking line numbers alone, they looked
   fabricated. Checking the **names** showed every construct is real and only the **locations** are
   wrong — a materially different and more useful finding, and the one D5 clause 4 rests on.

## Question Policy compliance

No blocking questions were raised. The external dependency was resolved on day one as required. No
finding met the escalation bar. Nothing on the CLOSED list was reopened.

## Completion

Epic E40.4 execution complete. Epic Delivery Notice produced. **Awaiting Milestone Chat review and
authorization.**

⏸️ **This chat does not merge.**
