---
artifact_type: delivery_notice
artifact_version: 4.0
timestamp: 2026-08-14T22:30:00Z
issuer_chat: Epic Agent (P11-M38-E38.6)
issuer_role: Epic Agent
status: resubmission (re-review 03 request — record corrections)
epic_id: P11-M38-E38.6
milestone_id: P11-M38
phase_id: P11
project_name: ai-project-system
resubmission_of: P11-M38-E38.6 delivery notice v3.0 (2026-08-14T20:50:00Z)
review_decision_cited: 2026-08-14T22:13:05Z (REJECT, action: rework) — re-review 02, record-only
merge_details:
  pr_number: 199
  pr_url: https://github.com/panchew/ai-project-system/pull/199
  merge_commit: pending — not merged at Delivery Notice time (P10-GH-4)
  merge_timestamp: pending
  merge_strategy: merge
  target_branch: milestone/M38
duration:
  start_date: 2026-08-12
  end_date: 2026-08-14
  elapsed_days: 3
final_artifacts:
  - name: Comparison material, pre-registered rubric, packet, protocol-correction addenda, erratum
    path: .ai-project/artifacts/reference/local-paid-comparison/
    type: report
  - name: E38.6 delivery notice (re-review 03 request — metadata cleanup)
    path: docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M38-E38.6__delivery-notice.md
    type: report
completion_notice_timestamp: 2026-08-14T22:30:00Z
review_decision_timestamp: 2026-08-14T22:13:05Z — this resubmission is in response
---

# Delivery Notice (Re-review 03 Request — record corrections): P11-M38-E38.6 — Local/Paid Controlled Comparison

## Summary

**A compliant valid local/paid comparison is accepted on substance.** Re-review 01 rejected
paid run 2 (programmatic subagent; isolation instructed, not sandboxed). Per the second
protocol-correction addendum (committed before run 3), **paid run 3** was run as a genuinely
fresh, human-operated manual session via Claude Code CLI: model `claude-opus-5`, empty
non-repo directory, all filesystem/shell/search tools disallowed, receiving only the sealed
packet. The **valid pair** was scored against the original pre-registered rubric:

- **Local run 2** (Drivr `OpenCodeAdapter`, `ContainerEnvironment`, qwen3-coder:30b) — **MISS**.
- **Paid run 3** (Claude Code CLI, packet-only, claude-opus-5) — **CATCH**.

**Re-review 02 (record-only) found three documentary defects, all corrected here:**
1. **Current commit IDs** — the README and Delivery Notice chronology now use the resolvable
   IDs at the reviewed head (`0a46e34`, `3b0c509`, `d297a1b`, `679f796`, `ea6ddcf`,
   `3a63b3d`, `a68abc1`); stale post-rebase IDs removed.
2. **Paid run 2 conforming statement** — removed; the final history is that runs 1 and 2 were
   invalid for their recorded reasons and **paid run 3** is the one manual, packet-only
   conforming arm.
3. **Eight vs seven new tests** — paid run 3 supplied **seven** new test functions; the
   editable narrative now says seven. Historical commit messages are preserved unedited.

**No model arm was rerun; no rubric, packet, sealed input, run artifact, protocol addendum,
or erratum was changed. No routing policy changes.**

## Merge Details

- PR Number: #199 — `epic/P11-M38-E38.6` → `milestone/M38`
- Target Branch: `milestone/M38`
- Merge Strategy: merge
- Merge Commit: pending (`P10-GH-4` — this Epic does not merge its own PR)

## Duration

- Started: 2026-08-12
- Completed: 2026-08-14
- Total Time: 3 days (includes the rejected submission and the rework)

## The comparison — what was compared and why its ground truth is knowable

**Material:** E38.3's fleet-registry validation fix (commit `927b7fa`). The task: replace the
single `total_warnings: 14` with distinct fleet-only and raw totals, add derived fields, and
add a test verifying the derived invariants.

**Ground truth is knowable** because E38.3's Stage-2 rework committed the fix, and the
correct numbers are independently derivable from the pre-fix registry (sum each enrolled
project's `section_4_warnings`). Verified: `fleet_warnings = 12`, `fleet_errors = 8`,
`invalid = 4`, `enrolled = 13`.

## The blinding method, defended

**Method:** E35.5's discipline — a blinded packet with an audit header and a
`<!-- PROMPT-BEGIN -->` marker (the prompt is exactly the bytes after the marker), a
pre-registered rubric, and the excised materials named in the packet header.

**Local arm:** run in an **isolated git-free workspace** containing only the two pre-fix
files. Run 2 additionally ran through Drivr's `OpenCodeAdapter` in `ContainerEnvironment`
(Finding 2).

**Paid arm (intended):** **packet content only** — no repository, no git, no search. The
**one manual, packet-only conforming arm is paid run 3** (Claude Code CLI, model
claude-opus-5, no filesystem/shell/search tools, zero tool use). Paid run 1 and paid run 2
both violated the frozen conditions — run 1 had repository access and read the committed
answer; run 2 was a programmatic subagent with instructed-not-sandboxed isolation. See
§Protocol history.

## The rubric was committed BEFORE any run

- **`0a46e34`**: packet + rubric + README — **before any run**.
- **`d297a1b`**: protocol-correction addendum v1 — **before any replacement run**.
- **`ea6ddcf`**: protocol-correction addendum v2 + sealed input — **before paid run 3**.

The original rubric is unchanged throughout. Git history proves the ordering.

## Finding 2 — local environment proof (addressed)

**Local run 1** ran directly on the host via a bare `opencode run`; it did **not** go through
the registered Drivr `OpenCodeAdapter` in `ContainerEnvironment`. It is therefore a
**protocol-invalid trial**, preserved at `runs/local/`.

**Local run 2** is the replacement, run under the registered conditions. Its metadata
(`runs/local/local-arm-run-2-meta.json`) records:

- **adapter:** `drivr.execution.opencode.OpenCodeAdapter`
- **environment:** `container` (`debian:12-slim`, drivr's default)
- **exact command:** the Docker invocation, verbatim (host `opencode` mounted read-only,
  workspace at `/work`, `XDG_CONFIG_HOME` set, `--auto`, model `ollama/qwen3-coder:30b`)
- **context limit:** 32,768, source `observed-after-load` (derived from `/api/ps`, not the
  trained max — Constraint 3)
- **files_changed:** `.ai-project/registry/fleet-registry.yml`, `tests/test_fleet_registry.py`

## The valid local/paid pair — results

### Local run 2 — MISS (numbers wrong)

- **Conditions:** Drivr `OpenCodeAdapter`, `ContainerEnvironment` (`debian:12-slim`), model
  `ollama/qwen3-coder:30b`, isolated git-free workspace. Exit code **0**.
- **What it did (re-measured from files):** separated `total_warnings: 14` into
  `fleet_warnings: 10` and `raw_total_warnings: 14`; added `excluded_configs` to the
  `enumeration` section; added 4 duplicated test functions.
- **Why MISS:** `fleet_warnings: 10` is **wrong** — the ground truth is **12**. The arm
  mis-attributed the 2-warning gap to the 2 unenrolled projects (which carry no warnings);
  the gap is actually the 2 worktree checkouts of `ai-project-system`. It also omitted the
  structural invariant (`raw == fleet + excluded`) and duplicated tests.
- **G1:** PASS — `total_warnings: 14` quoted verbatim. **G2:** PASS — scored from file diffs
  and a test re-run (18 passed), not from prose or exit code.

### Paid run 3 — CATCH (correct, genuinely manual, packet-only, non-contaminated)

- **Conditions:** Claude Code CLI v2.1.197 print mode, **fresh session**, model
  `claude-opus-5` (verified — matches `models.epic_manual`), run in an **empty non-repo
  directory** (`/tmp/opencode/e38.6-paid-run3`) with **all filesystem/shell/search tools
  disallowed**. Only the sealed packet (42,875 bytes, MD5
  `450dcfb78800f13ff39cabf4bcf1907f`) was passed as the prompt.
- **Isolation verified (G2, from the run record):** available tool set contained **no**
  `Bash`, `Read`, `Write`, `Edit`, `Grep`, `Glob`, `WebSearch`, or `WebFetch`; the run made
  **zero tool_use events**; it answered purely from the packet. 1 turn, no permission
  denials. Model identity `claude-opus-5` confirmed by both the init event and result usage.
  This satisfies re-review 01's Finding 1 (genuinely fresh, manual, packet-only session with
  no repo/shell/filesystem/search access).
- **What it did (re-measured/verified):** proposed registry rewrite splitting
  `total_warnings: 14` into `fleet_warnings: 12`, `raw_warnings: 14`, `excluded_warnings: 2`,
  plus scope-named error/invalid counts and checked/excluded config counts, and 7 tests
  including the structural invariant `raw == fleet + excluded` for all three measures.
- **Why CATCH:** all three rubric criteria met — fleet total distinct from raw (12/14);
  derived fields making the relationship explicit; a test verifying the derived invariants.
- **Values verified (G2):** `fleet_warnings=12`, `fleet_errors=8`, `invalid=4`, `enrolled=13`
  and the independent `schema_drift_class` cross-check (12) all re-derived correctly from the
  pre-fix registry. **The arm's proposed changes, applied verbatim, pass the full test file
  (23/23 = 16 original + 7 new) against the pre-fix registry.**
- **Correction note (re-review 02, Finding 3):** paid run 3 supplied **seven** new test
  functions, not eight. The seven provide the invariant-test coverage the rubric requires,
  so the CATCH verdict is unchanged. Historical commit messages that say "eight" are
  preserved unedited; this note is the correction record.
- **Non-contamination:** zero tool_use and no file-access capability make contamination
  structurally impossible in this run.
- **G1:** PASS. **G2:** PASS.

## Finding 3 — wall-clock timing (addressed)

| | Local run 2 | Paid run 3 |
|---|---|---|
| Wall-clock start | 2026-08-14T19:26:41Z (approx) | 2026-08-14T20:40:00Z (approx) |
| Wall-clock end | adapter return | 2026-08-14T20:41:47Z |
| Wall-clock duration | **276.9 s** (host process) | **107 s** (single CLI invocation) |
| Comparable duration | **237,200 ms** (adapter `duration_ms`) | **105,244 ms** (result `duration_ms`, single turn) |

**Comparable basis:** the local arm's `adapter_duration_ms` (237,200) and the paid arm's
`duration_ms` (105,244) both measure engine runtime from dispatch to final message. The paid
arm's wall-clock (107 s) is a clean single CLI invocation — no operator-inflated activity.

**GPU lane:** `qwen3-coder:30b` loaded for local run 2 and released. No contention with E38.5
(already delivered).

## Finding 4 — actual suite result (addressed)

**Command:** `PYTHONPATH=. pytest -q`

**Actual result on the final re-review-03 state** (measured 2026-08-14, on the re-review-03
metadata-cleanup commit):

```
$ PYTHONPATH=. pytest -q
489 passed in 25.58s
```

**489 passed / 0 failed / 0 skipped / 0 xfailed.** **`P10-GH-10` did not occur in this run**
(`test_daemon_extensions_error_branches` passed). The independent review baseline was 489
passed; this resubmission reports its own final-state measurement.

## §What this does NOT establish

- **It does NOT establish that the paid tier is generally better than the local tier.** This
  is **one task, n=1 per arm**. A single data point cannot carry a general claim.
- **It does NOT establish that the local tier is unsuitable for code-shaped work.** Local run
  2 got the concept right and only the fleet number wrong; local run 1 got both headline
  numbers right. A partial single result is evidence, not a verdict.
- **It does NOT move any routing policy.** `model-routing-policy.md` is unmodified. Row P4,
  P6, P7 untouched.
- **It does NOT close G11** (Constraint 8). Not an `epic_qa` run.
- **It does NOT re-run B3.1's engine comparison.** This is a tier comparison on one task.
- **It does NOT decide row P4.** That is a further HQ call.

## Confirmations the Definition of Done requires

- **`model-routing-policy.md` unmodified:** ✅ confirmed — empty diff.
- **No dispatch path was scaffolded:** ✅ the local arm ran through E38.2's existing adapter;
  nothing was built. The isolated workspace is the adapter's normal function, not scaffolding.
- **G11 not claimed:** ✅ (Constraint 8).
- **No completion judgment, scheduler, gate queue, or thin surface built:** ✅ none.
- **No delivered epic's artifacts modified to make them comparable material:** ✅ E38.3's
  artifacts were reconstructed at their pre-fix commit (`9e2feaf`) for the blinded packet.
- **Constraint 9:** ✅ **did not fire** — nothing under `governance/` was amended (empty
  diff); no Structural diagram required.

## The citation rule applied (Finding 1)

- No count-error instance is cited by ordinal. The count-error class's tally is a **floor of
  ten as of 2026-08-10** (base eight from M37), carried from the milestone spec; this Epic
  does not increment, renumber, or re-adjudicate the collision.
- The warning totals in this Epic's material (12 fleet / 14 raw / 2 excluded) are
  **measured/derived** values with their basis stated, not a count-error tally.

## Protocol history — the invalid trials, preserved (Finding 1)

- **Paid run 1:** repository access, read the committed answer (`927b7fa`). **INVALID —
  retrieval, not capability.** Preserved at `runs/paid/paid-arm-run-1.md`.
- **Local run 1:** ran on the host, not the registered ContainerEnvironment. **INVALID
  (environment mismatch).** Preserved at `runs/local/`.
- **Paid run 2:** programmatically dispatched subagent; isolation instructed, not sandboxed.
  **INVALID (session-type + access-boundary mismatch)** — rejected by re-review 01.
  Preserved at `runs/paid/paid-arm-run-2-output.md`.

**Addendum v1** (before run 2) authorized local run 2 and paid run 2. **Addendum v2**
(before paid run 3, `ea6ddcf`) classified paid run 2 INVALID and authorized exactly one
**paid run 3** as a genuinely fresh, manual, packet-only session.

## Re-review 01 findings addressed

- **Finding 1 (paid-arm protocol):** paid run 3 ran as a genuinely fresh, human-operated
  manual session via Claude Code CLI — model `claude-opus-5`, empty non-repo directory, all
  filesystem/shell/search tools disallowed, only the sealed packet as prompt. Zero tool_use.
  Isolation verified from the run record (`paid-arm-run-3.json`).
- **Finding 2 (record corrections):** the sealed input is committed
  (`runs/paid/sealed-input-run-3.txt`, 42,875 bytes, MD5
  `450dcfb78800f13ff39cabf4bcf1907f`); the exact run-3 output and stderr are committed; the
  addendum v1 timestamp erratum is recorded (`erratum-addendum-v1-timestamp.md`); the README
  and this Delivery Notice show current branch commit IDs with pre-rebase labels.

## Things noticed and deliberately left — named individually

1. **Paid run 1 was not re-run to produce a "clean" number at first** — the first submission
   recorded it as a finding; the reviews correctly required valid replacements.
2. **E38.3's committed fix asserts its warning invariant as literals** (12, 14), not
   structurally. Identified by paid run 1; left unedited (it is E38.3's artifact).
3. **The paid run-2 raw wall-clock (538 s) was inflated by operator activity** — recorded but
   superseded by run 3's clean timing.
4. **Local run 2 duplicated test functions** (4 copies of the new test) — a quality defect in
   the local output, recorded as part of the MISS rather than corrected.
5. **The material's ground truth being in git** forced the strict packet-only protocol for
   the paid arm. A structurally-blinded comparison would use material whose answer is not
   committed at all.
6. **The reviewer's own verified run-2 sealed input hash** (`450dcfb78800f13ff39cabf4bcf1907f`)
   equals the committed sealed input — the prompt boundary and absence of the answer are
   verifiable in the repository.

## Which arm, environment and repository every reported command ran in (`P11-GH-2`)

| Command | Arm | Environment | Repository |
|---|---|---|---|
| Drivr `OpenCodeAdapter`/`ContainerEnvironment` | local run 2 | container (`debian:12-slim`) | `/tmp/opencode/e38.6-comparison-run2` (no git) |
| Claude Code CLI `-p`, packet-only, no fs/shell/search tools | paid run 3 | host, empty non-repo dir | `/tmp/opencode/e38.6-paid-run3` (sealed packet only) |
| `PYTHONPATH=. pytest -q` | reviewer | this host | `ai-project-system` @ `epic/P11-M38-E38.6` |

## Closing a loop the CFO opened

The split-posture decision survived a relocation, a ruling and a restructure to arrive at the
one place it could be run natively. It was run, the invalid trials were caught and replaced,
and a **valid, comparable local/paid result** now exists on this task. **The "therefore"
belongs to the CFO and M39.**

## Chat Closure

Epic E38.6 re-review 03 request complete. Delivery Notice (v4, re-review 03 record corrections) produced. Awaiting Milestone Chat Stage-2 re-review and merge authorization.

**This Epic does not merge its own PR.** Merge authorization normally follows the parent chat's Stage-2 review (P9-M31 precedent).
