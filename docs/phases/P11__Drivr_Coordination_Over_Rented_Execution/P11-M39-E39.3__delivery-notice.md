---
artifact_type: delivery_notice
artifact_version: 1.2
timestamp: 2026-08-16T21:23:55Z
issuer_chat: Epic Agent (P11-M39-E39.3)
issuer_role: Epic Agent
status: delivered
epic_id: P11-M39-E39.3
milestone_id: P11-M39
phase_id: P11
project_name: ai-project-system
merge_details:
  pr_number: pending — PR opened after this notice is committed (P10-GH-4)
  pr_url: pending — PR opened after this notice is committed (P10-GH-4)
  merge_commit: pending — not merged at Delivery Notice time (P10-GH-4)
  merge_timestamp: pending — not merged at Delivery Notice time (P10-GH-4)
  merge_strategy: merge
  target_branch: milestone/M39
duration:
  start_date: 2026-08-16
  end_date: 2026-08-16
  elapsed_days: 1
final_artifacts:
  - name: QA-role dispatch for the epic_qa lane
    path: bin/run-qa-agent
    type: implementation
  - name: read-only QA tool set (criterion 2's committed evidence)
    path: .ai-project/agents/qa-tools.json
    type: implementation
  - name: reproducible runner availability shim (F4)
    path: bin/local-agent-runner-shim
    type: implementation
  - name: contract tests for the QA-role dispatch (21 tests)
    path: tests/integration/test_run_qa_agent_adapter.py
    type: implementation
  - name: captured epic_qa run — the run G11 closes on
    path: .ai-project/artifacts/agentic-runs/P11-M39-E39.3/
    type: report
  - name: captured epic_qa run — reproducibility second dispatch
    path: .ai-project/artifacts/agentic-runs/P11-M39-E39.3-RUN2/
    type: report
  - name: E39.3 feasibility record — the five criteria, measured
    path: docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M39-E39.3__feasibility-record.md
    type: report
  - name: E39.3 delivery notice
    path: docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M39-E39.3__delivery-notice.md
    type: report
completion_notice_timestamp: 2026-08-16T21:23:55Z
review_decision_timestamp: pending — Stage-2 review not yet performed
governing_spec_version: 1.0.1
---

# Delivery Notice: P11-M39-E39.3 — Exercise the `epic_qa` Lane and Close G11

## Summary

**G11 is closed on a named run, and the run proves the lane's output cannot be trusted.** Both
halves are the delivery; the second is the more useful one.

The gate was passed: all five genuineness criteria were assessed as achievable in scope, a QA-role
dispatch was built to the minimum shape, and a real run was performed on the host under
`.ai-project.yml`'s `models.epic_qa` and captured in this repository. The three-phase-old gap — *the
lane routes a QA model to a developer task* — is closed: `bin/run-qa-agent` routes it to a QA task,
with a read-only tool set, at artifact paths that overwrite nothing.

**Then the run returned `VERDICT: PASS` on all 26 rules of its standard having made zero tool
calls**, citing `read_file('.ai-project.yml')` on every line — including a line about a
`framework_version` field **that file does not contain**. Reproduced on a second identical dispatch.
The lane exists and works; the model in it fabricated its evidence. **The first real QA run ever
captured corroborates E39.1's binding decision that no model-generated judgment may be load-bearing.**

## The five genuineness criteria

Full measurements, with the environment and time of each, in the **feasibility record**
(`…E39.3__feasibility-record.md`). Every measurement was taken **on the host** (PID 1 = `systemd`,
no `/.dockerenv` — not the Docker sandbox) on **2026-08-16**.

| # | Criterion | Verdict | The measurement |
|---|---|---|---|
| 1 | **Role** — assesses, does not produce | **MET** | Task text instructs a per-claim judgment + `VERDICT:` line; it is **not** `extract_dod(...)`, which is what `bin/run-dev-agent:241` passes. Pinned by a test that fails if the DoD reaches `--task-text`. |
| 2 | **Tools** — cannot mutate the tree | **MET** | Committed `qa-tools.json`: `enabled = ["read_file","list_dir"]`, `allow_commands = []`, `allow_subcommands = []`, `deny_commands = ["*"]`. Bound to the run by `tools_sha256` `83ff08e7…`. Enforcement **measured**: `parse_tool_call` returns `unknown_tool` for `write_file`/`edit_file`/`git`. |
| 3 | **Routing** — provably from `models.epic_qa` | **MET** | No `--model` flag and no model env override exists; `resolve_qa_model` reads `.ai-project.yml` and records `config_sha256` `cd22d24c…`. A disagreeing `AI_PROJECT_ACTIVE_MODEL` is refused as a config error (measured). |
| 4 | **Capture** — committed, distinct paths | **MET** | `qa-transcript.json` / `qa-context.md` / `qa-run-metadata.json`; the four §F3 `epic_id`s refused unconditionally; any pre-existing artifact aborts the run. `git status` over the corpus shows **no tracked file modified or deleted**. |
| 5 | **Non-authority** — advisory only | **MET** | `"authority": "advisory"` in the metadata; nothing reads these artifacts; nothing was scheduled, queued, notified or escalated. |

**One boundary stated honestly:** criterion 1 is a property of the **dispatched task**, not of the
quality of the assessment returned. The bar as written does not require the verdict to be grounded —
and this run's was not. That is a limitation of the bar, surfaced for the Milestone Chat, not a
defect in the measurement.

## The G11 statement

> **G11 is CLOSED on the run captured at
> `.ai-project/artifacts/agentic-runs/P11-M39-E39.3/`, dispatched on the host on 2026-08-16 under
> `.ai-project.yml`'s `models.epic_qa` (`local:qwen3-coder:30b`) with the read-only tool set
> `.ai-project/agents/qa-tools.json`, via `bin/run-qa-agent` — a QA-role dispatch built in this
> epic, because E39.1 built none.**

It is **not** claimed by inference, by a relabelled dev-lane run, or from E39.2's validation — whose
evidence and verdicts were **not loaded or consulted**, and whose subject matter has no connection
to the QA task chosen here.

## The dispatch path and its origin

**`bin/run-qa-agent`, built in this epic.** E39.1 built none, by its binding QA-pass decision.

It is a **sibling** of `bin/run-dev-agent`, not a fork: the shared machinery (`resolve_model`'s
local-tag mapping, `check_local_availability`'s Hard-Constraint-4 preflight, `discover_runner`,
`extract_dod`) is **imported at runtime** rather than copied, so the two cannot drift. Exactly three
things differ, and they are exactly the three the role requires — **the task text, the tool set, and
the artifact paths.** `bin/ai-project-orchestrator` is **not modified**; its retry, feedback and
escalation behaviour is untouched.

The orchestrator's own source already said this was the missing piece, at
`handle_epic_execution` (lines 419–426), quoted verbatim:

> *Honesty note: qa_model is resolved and logged here, but no code path in this dispatch surface
> actually invokes it as an LLM today [...] Building a QA-role dispatch mechanism is out of scope
> (Non-Goals).*

## The environment

**Host**, not the Docker sandbox — the route with precedent (`P7-M26-E26.3-PROVE` and
`P10-M33-E33.2` both dispatched on the host). Verified at the layer, not assumed: PID 1 = `systemd`,
no `/.dockerenv`, cgroup `/init.scope`, `hostname dtx`. Date **2026-08-16**; the closing run at
21:17:00Z, the reproducibility run at 21:19:41Z.

**What the host route cost (F5):** it bypasses the orchestrator's `AI_PROJECT_ACTIVE_MODEL`
injection, so Docker did not perform the routing. F5's prescribed resolution is applied — the value
is re-derived from the same config file the orchestrator would have read, and bound to it by hash.

## Runner availability (F4)

A durable install is **blocked on this host**, measured: `pip3 install --user -e … --dry-run` is
refused under **PEP 668** (externally-managed environment, python3.14). `--break-system-packages`
was not used — a system-interpreter change is outside this epic's scope.

Made available instead by **`bin/local-agent-runner-shim`, committed in this repository**, which
execs `python3 -m local_agent_runner` with `PYTHONPATH` from a required `LOCAL_AGENT_RUNNER_HOME`.
**Runner anchor: `local-agent-runner` at commit `4ec1e8f`, branch `epic/cf-2-public-run-api`, as of
2026-08-16.** All five preserved runs named a scratchpad wrapper that no longer exists; this run
names a tracked file a reader can still open. **Limitation stated:** that checkout is on a feature
branch, and the shim pins the invocation, not the runner's version.

## The finding — reported, not filed

**Both dispatches: `iterations: 0`, zero tool rounds, `VERDICT: PASS` on all 26 rules.**

| Run | tool rounds | tokens | duration | verdict |
|---|---|---|---|---|
| `P11-M39-E39.3` | **0** | 716 | 51.3 s | `VERDICT: PASS` |
| `P11-M39-E39.3-RUN2` | **0** | 678 | 12.7 s | `VERDICT: PASS` |

The read-only tools **were** advertised to the model. The fabrication is **falsifiable from the
committed artifacts**: line 26 claims `read_file('.ai-project.yml') shows framework_version …`, and
that file's top-level keys are `['cfo_review_gate','governance','models','project',
'visual_artifacts']` — **no `framework_version`**.

This is the failure mode `governance/ai-project-yml-spec.md` §3.4's sizing note predicts —
*"never accept a green exit code as proof of work"* — appearing at the model tier that note names as
the fix, in the QA role. **Narrow evidence: one QA task shape, two runs; not a claim about the 30b
in `epic_dev`, where E33.2 Run B and E33.4 recorded real tool use.**

**No `P11-GH-<n>` ID was allocated by this chat, deliberately.** `P11-GH-3` is the next ordinal but
is **contested** — `P11__carry-forward-note__P11-GH-2-….md` declines to file a different pattern
under it and refers that call to the CFO. Claiming it here would collide with a pending decision.
Recorded **by artifact + defect** and **escalated to the Milestone Chat** for ID allocation.

## No preserved run artifact was altered

```
$ git status --porcelain .ai-project/artifacts/agentic-runs/
?? .ai-project/artifacts/agentic-runs/P11-M39-E39.3-RUN2/
?? .ai-project/artifacts/agentic-runs/P11-M39-E39.3/
```

**Only untracked (`??`) entries, both of them this epic's own new runs. No tracked file under that
path is modified or deleted**, and `git diff --stat HEAD` over it is empty.

**Inventory correction (every inventory is a floor):** spec §F3 inventories five directories; there
are **seven** — `P11-M38-E38.4/` and `P11-M38-E38.5/` also exist. Neither holds a `transcript.json`
or `run-metadata.json`, so **§F3's exposure table is unchanged**; the count is recorded because this
corpus has now been re-inventoried by three different chats.

## Suite

| Repo | Baseline | After | Invocation |
|---|---|---|---|
| `ai-project-system` | **489 passed / 0 failed**, re-measured here on `epic/P11-M39-E39.3` at `4f09ee5` **before any change** | **510 passed / 0 failed** (+21) | `PYTHONPATH=. pytest -q` — **bare `pytest` fails collection** |
| `drivr` | not touched | not touched | — |

**`P10-GH-10` did not fire** in either full-suite run — nothing to record under its both-results
obligation.

## Scope statements

- **Nothing from M40 was built** — no scheduler, no gate queue, no thin surface, no approval link,
  no competing-model review.
- **Drivr was not touched.** No file under it was read, written or executed; no `role` field was
  added to `ExecutionRequest`; no role framework exists. **Drivr still rents.**
- **The Hard Constraint held.** M39 judged and did not act: the verdict triggered nothing.
- **`model-routing-policy.md` row P4, the four §4-invalid enrolled configs and the two open
  `bin/ai-project-init` defects are untouched.**

## Visual Bindings

**None.** Milestone constraint 7 requires a Structural diagram on any delivery amending a
**normative document in this repository**; this epic amends none — a QA tool set, a dispatch script,
a shim and a test module are not normative documents. Stated explicitly, as that constraint directs.

## Definition of Done

- [x] Five genuineness criteria each **assessed with a recorded measurement**
- [x] **A real captured `epic_qa` run exists as a committed artifact** in this repo
- [x] **Dispatch path named and its origin stated** — `bin/run-qa-agent`, built here
- [x] **Environment recorded** (host, not sandbox), per F5, with what the route cost
- [x] **Tool set committed and contains no mutating tool**; **artifact paths overwrote nothing**
- [x] **No QA run dispatched against the four §F3 `epic_id` values** — refused unconditionally, tested
- [x] **Model shown to come from `.ai-project.yml`'s `models.epic_qa`**, bound by hash
- [x] **G11 not claimed by inference, by a relabelled dev-lane run, or by E39.2's validation**
- [x] **QA verdict recorded as advisory only**
- [x] **No preserved run artifact overwritten, edited or regenerated** — `git status` evidence above
- [x] **Nothing from M40 built; Drivr not touched**
- [x] **Suite green**, baseline named per repo; `P10-GH-10` did not fire
- [x] Delivery Notice **committed**; all changes on `epic/P11-M39-E39.3`; PR opened to `milestone/M39`

## Escalations to the Milestone Chat

1. **ID allocation for the fabricated-verdict finding** — deliberately not self-assigned, because
   `P11-GH-3` is contested.
2. **The bar's boundary.** The five criteria make a run's *genuineness* checkable but say nothing
   about whether its verdict is *grounded*. This run met all five and returned a fabricated PASS.
   If G11's purpose was a trustworthy independent signal, **closing it does not deliver that**, and
   the milestone should say so rather than let the closure imply otherwise.

## Status

**Epic E39.3 execution complete. Awaiting Milestone Chat Stage-2 review and merge authorization.**
