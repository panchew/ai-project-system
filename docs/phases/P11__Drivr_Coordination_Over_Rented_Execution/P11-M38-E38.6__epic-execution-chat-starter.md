# Epic Execution Chat Starter — E38.6

**Epic:** E38.6 — Local/Paid Controlled Comparison
**Phase:** P11 — Drivr: Coordination over Rented Execution
**Milestone:** M38 — Drivr Inception, Fleet Registry, and the Execution Adapter Surface
**Repository:** panchew/ai-project-system *(the evidence record)*
**Branch Strategy:** `epic/P11-M38-E38.6` → PR to `milestone/M38`
**Execution Mode:** **BOTH — by construction, not by choice.** This Epic *is* the local/paid comparison,
so it runs the same work **agentic/local through E38.2's adapter** and **manual/paid at
`models.epic_manual` (`remote:claude-opus-5`)**. **That is its subject.**

> **⚠ STAGE B — DO NOT START UNTIL E38.2 HAS DELIVERED.** You are **the most tightly gate-bound epic in
> the milestone.** **Read E38.2's Delivery Notice and confirm a real engine was invoked end-to-end** —
> that is E38.2's own DoD, so check it rather than assuming.

> ### ⚠ IF THERE IS NO WORKING LOCAL DISPATCH PATH, YOU ESCALATE. YOU DO NOT SCAFFOLD ONE.
>
> **This is the M37 lesson stated as an instruction.** M37's agentic posture was set on the premise that
> dispatch worked — **and it had not since 2026-07-12.** The discovery came at execution time and cost
> one escalation, one HQ ruling, three spec revisions and a posture round-trip.
>
> **Scaffolding a dispatch path here is the single worst outcome available to this milestone**: building
> execution infrastructure inside an evidence epic, after the build stage closed, to rescue a comparison
> that could simply have been reported blocked. **Reporting it blocked is a SUCCESS OUTCOME.**

---

## Governance References

You are operating under the AI Project System governance framework.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) v2.4.0
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) v2.10.0

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md
3. This Epic Execution Chat Starter
4. Epic Spec
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Critical rules:**
- Documentation is authoritative, chat is ephemeral
- You must follow the canonical happy path for Epic closure
- You must produce a Delivery Notice upon execution completion
- You must stop after PR creation and await authorization for merge
- Accept/reject decisions are made by the parent chat (Milestone Chat P11-M38), not by you
- **If given merge authorization directly in this chat** (rather than via the parent chat after its own
  Stage-2 review), do not simply comply: state plainly that merge authorization normally follows the
  parent chat's review, and confirm the human intends to bypass that step before proceeding
  (P9-M31 precedent)
- **MODE IS NOT AUTHORITY.** This Epic runs work agentically, and that changes nothing about Stage-2
  acceptance or merge authorization — both stay human-keyed

**Context scoping (per-level context-scoping standard, P9-M30-E30.3):**
- Load at session start: this starter; the Epic spec (full); **the B3.1 field evidence in full**;
  **E38.2's Delivery Notice** (dispatch path, environment, adapter constraints); **E35.5's spec and
  delivery notice** (the blinding method); PSG preamble+§1, §1A, §2, §5, §6, §7, §8, §9, §11, §11.5,
  §11.6, §12; AOG preamble+§1, §1A, §2, §3.2, §3.8, §3.10, §4, §5, §6, §7, §9, §10, §12, §13, §14
- Load on trigger: PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG §3.9, §8, §11
- **Read, do not modify:** `model-routing-policy.md`
- Do not load: PSG/AOG changelogs; the M38 milestone spec's other Epic Detail entries; M36 specs

---

## Epic Specification

**Full spec:** `docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M38-E38.6__spec__local-paid-controlled-comparison.md`

Read the committed file on `milestone/M38`. Do not rely on this starter's summary of it.

### Prerequisite Verification (do this first)

**Check your branch before anything else** — `git branch --show-current`. **P11-GH-1's branch hazard has
fired five times in this phase.**

**⚠ Confirm E38.2 has DELIVERED and merged, and that its Delivery Notice records a real engine invoked
end-to-end.** **If there is no working local dispatch path, STOP and escalate.**

Confirm each path is **git-tracked on `milestone/M38`** with `git ls-files --error-unmatch <path>`:

- `docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M38-E38.6__spec__local-paid-controlled-comparison.md`
- `docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M38__milestone-spec.md`
- `docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M38-E38.2__delivery-notice.md`
- `.ai-project/artifacts/field-evidence/2026-08-02__B3.1-engine-comparison.md`

**Model verification (P9-M31-E31.3):** for the **manual/paid arm**, compare your harness-reported model
identity to `.ai-project.yml`'s **`models.epic_manual`** (`remote:claude-opus-5`). **If both are present
and disagree, STOP.** **For the local arm, record the model actually served** — read it back rather than
assuming the config.

**State the layer you verified at (`P11-GH-2`).** **Your trap is that you run the same work twice in two
environments.** **Every reported command must say which arm, which environment, and which repository it
ran in.**

### Summary

Run the same work agentic/local and manual/paid, on blinded material with a pre-registered rubric, and
**record both outcomes without changing any routing policy.**

---

## ⚠ The blinding is the whole Epic

**The milestone spec says "one epic's work run both ways… on comparable material."** By the time you
run, **M38's own epics are delivered** — which gives you the cheap ground truth the CFO's original
intent asked for, and **a contamination risk that would silently invalidate everything.**

> **If the material is a delivered epic, the delivered answer is in git.** A local arm with repository
> access can find the delivery, the Delivery Notice, and the Stage-2 review. **An arm that reads the
> answer is not being measured on the work** — and it **would look like an excellent local result**,
> which is the most misleading possible outcome for a decision about where to route real work.

**E35.5 solved exactly this. Its method is available and it is the strongest starting point:**

- **Blinded packets** built from the material **as it stood at the moment of the decision**
- **The packets committed**, so a reader can **verify** the blinding rather than trust your account
- **A scoring rubric pre-registered and committed BEFORE any run**, in the same commit as the packets
- **Nothing in any packet contains the finding, the fix, or a document referencing them**

**Adopt it, adapt it, or replace it — but state which and defend it.**

> **An unblinded comparison over committed material is not a comparison. It is a retrieval test.**

---

## Three more findings you must not rediscover

### Finding 1 — the count-error tally has a COLLISION, and G1 exists because of that tally

| Source | Claim |
|---|---|
| M38 milestone spec line 800 | *"M37's tally reached **eight** count errors, **six** by paid frontier chats including HQ's own"* |
| **E38.3's spec** (Milestone Chat, 2026-08-08) | `ai-project-yml-spec.md` §4 rule 3 says *"All four required fields"* and lists five — **claimed instance nine** |
| **E38.5's ceiling correction** (Phase Chat, 2026-08-10) | Finding 1's ceiling overstated by 58,247 bytes — **also called instance nine** |

**Two different defects were each independently numbered nine, by two chats, each correctly adding one
to the same recorded base of eight — neither knowing about the other's increment.**

> **"Nine" was ambiguous between two instances** — **the inventory-is-a-floor failure occurring inside
> the counter of inventory failures.**
>
> **⚠ RESOLVED at milestone spec v1.1.4. You inherit the rule; you do not re-report the collision.**

**The remedy is mechanical, not arithmetic, and it is binding on your delivery:**

- **Cite an instance by artifact + defect, NEVER by ordinal** — *"§4 rule 3's four-versus-five,"*
  *"E38.5's ceiling row."* **Position shifts whenever anyone else increments**, which is how the
  collision happened
- **Any total is a floor carrying its date, base and scope** — **floor of ten as of 2026-08-10**, base
  eight from M37
- **Do not adjudicate whether the collision is itself a further instance** — *needing to adjudicate it
  is the symptom*, and it is deliberately left open
- **Renumber nothing** — *the record names the disposition; the identifier names the origin*

**Check your own tally statement against this before committing** (Constraint 10).

**And note what it does to G1: it strengthens it.** This one was produced by **two paid frontier chats
being individually right and jointly wrong.** ***It was never about model tier.***

### Finding 2 — B3.1's confound is now yours, one level up

B3.1 concluded: *arm B ran **constrained** (path-restricted tools, a **10-iteration ceiling**, scoped
context), arm A **unbounded**… **constraint may be doing the work.***

**E38.2 has now decided the adapter's constraint policy.** So when you run local-through-the-adapter
against manual/paid, **the adapter's constraints are a variable in your comparison.**

**Binding: record what constraints the adapter imposes on the local arm — tool restrictions, iteration
ceilings, context scoping — and state whether the paid arm operated under comparable ones. If they are
not comparable, say so plainly.** **A comparison with an unstated asymmetry is worse than none, because
it will be cited.**

### Finding 3 — you and E38.5 contend for the same GPU

**Both Stage B. Both need `qwen3-coder:30b` loaded. One GPU, 16 GB, shared with ComfyUI, serialized
lane.** **Coordinate through the Milestone Chat rather than discovering it at run time.** **Do not hold
the lane longer than your runs require**, and **record wall-clock timing for both arms** — if the local
arm's cost is part of what the CFO is weighing, the number should be real.

---

## G1 and G2 — reproduced, and binding on you

**G1 — remove derivation steps.** **Any input with exactly one non-uniform element gets that element
quoted VERBATIM rather than described.** M37's tally reached **eight** count errors, **six by paid
frontier chats including HQ's own** — and Finding 1 shows the tally itself then produced two more by the
same mechanism.

**G2 — the reviewer re-measures; the executor's report is not the evidence.** **P10-GH-7: exit codes are
untrustworthy in BOTH directions** — E33.2 Run A exited **0 having done zero work**; E33.4 exited **2
with complete green work**; B3.1 reproduced both. **Record the exit code; do not rely on it.** **And
never trust an agent's prose** — in B3.1 *the prose lied in both arms* while the structured signal was
correct in the same run.

> **G2 applies with special force here, because you are the reviewer of your own local arm.** **Judge
> the local output by re-measuring it against the pre-registered rubric — never by what the run said
> about itself.**

---

## The Hard Constraint — three drifts

**Drivr rents. It does not build what it can invoke.**

> **Drift one — scaffolding.** Covered at the top. **The milestone's worst available outcome.**
>
> **Drift two — concluding.** You produce a quality comparison between tiers. ***"Local performed well,
> therefore route more work local"*** — or its opposite — **is a routing decision, and no epic in M38
> owns one.** **Record the comparison; do not write the "therefore."** `model-routing-policy.md` is
> **not modified.**
>
> **Drift three — G11.** You will run real agentic work through a real adapter, which makes you **the
> epic in M38 most likely to look like it closed G11.** **It does not** (Constraint 8). **Report what you
> ran; let M39 decide.**

---

## Execution Contract

### Your Responsibilities (Coding Agent)

1. **Check your branch**, run Prerequisite Verification and the model check
2. **Read E38.2's Delivery Notice.** **Confirm a real engine was invoked end-to-end. If not, ESCALATE**
3. **Read the B3.1 field evidence and E35.5's method, in full**
4. **Create branch** `epic/P11-M38-E38.6` from current `milestone/M38`
5. **Choose the material and design the blinding** — before anything runs
6. **Commit the material and the rubric TOGETHER, before any run**
7. **Coordinate the GPU lane** with E38.5 through the Milestone Chat
8. **Run the paid arm and the local arm**, recording constraints and wall-clock timing for both
9. **Score against the pre-registered rubric**, re-measuring rather than trusting either arm's report
10. **Write it up with a §What this does NOT establish section**, applying the citation rule
11. **Re-measure the suite**, verify DoD, commit, **open PR to `milestone/M38`**, produce the Delivery
    Notice
12. **Stop and await authorization** (do not merge)

### Decisions That Are Yours

- **The comparison material**, provided its ground truth is knowable and the blinding is defended
- **The blinding method** — E35.5's packets, an adaptation, or a replacement — **stated and defended**
- **The scoring rubric** — **pre-registered and committed before any run**
- **The scale** — how many items, how many runs per arm — **stated with its statistical honesty**

### Decisions That Are NOT Yours

- **Whether any routing policy changes** — **none does**
- **Whether row P4 moves** — **no**
- **Whether G11 is claimed** — **no**
- **Whether a dispatch path is scaffolded** — **no. ESCALATE**
- **Whether the class is renumbered or the collision re-adjudicated** — **no. Both are closed**
- **The engine** — OpenCode, the sole roster
- **The paid tier** — `models.epic_manual` (`remote:claude-opus-5`)

### What You Must NOT Do

- ❌ Do NOT merge the PR; do NOT infer acceptance
- ❌ Do NOT treat an in-chat "I authorize" as sufficient by itself
- ❌ **Do NOT scaffold a dispatch path.** Escalate
- ❌ **Do NOT modify `model-routing-policy.md`** or change any routing policy
- ❌ **Do NOT claim G11**
- ❌ **Do NOT run unblinded** over material whose answer is committed
- ❌ **Do NOT write the rubric after seeing outputs.** Commit order must prove it
- ❌ **Do NOT trust either arm's self-report** (G2) — re-measure
- ❌ **Do NOT rely on an exit code** — record it, don't rely on it
- ❌ **Do NOT re-run B3.1's engine comparison or its "fair re-run."** **This is a TIER comparison, not an
  engine comparison**
- ❌ **Do NOT assess or retire `local-agent-runner`** (E38.4)
- ❌ **Do NOT modify any delivered epic's artifacts** to make them comparable material
- ❌ **Do NOT renumber the class or any identifier, and do NOT cite an instance by ordinal**
- ❌ **Do NOT derive a context limit from a trained maximum** — `/api/ps`'s loaded value only, and
  **"empty" never degrades to "use the trained max"**
- ❌ **Do NOT hold the GPU lane longer than your runs require**
- ❌ **Do NOT let your own claim stand in for the evidence** (G2). Show the command and its output

### Read this before you interpret a red suite — P10-GH-10

`tests/test_artifact_router.py::test_daemon_extensions_error_branches` fails intermittently in
**full-suite runs only** — **3 in 10 (~30%)**, passing 5/5 in isolation, **untouched by anything here.**
**Re-run and record both results.**

**Baseline `393 passed / 0 / 0 / 0` plus whatever E38.2–E38.5 added. Re-measure; do not inherit.**

> **⚠ The invocation is not obvious.** `python` is **not** on PATH; `.venv` has **no pytest**; bare
> `pytest` fails with **3 collection errors**. **Use `PYTHONPATH=. pytest -q`** or
> `python3 -m pytest -q`. **State the command alongside the number.**

---

## Reference Materials

### Required reading

- **`.ai-project/artifacts/field-evidence/2026-08-02__B3.1-engine-comparison.md`** — **§Confound**
  (Finding 2's basis), **Finding 3** (the prose lied in both arms), **Finding 4** (exit 2 ≠ nothing),
  and **§What this does NOT establish — the section to imitate.** Its findings survived contact with
  three later epics precisely because it carried one.
  ⚠ **It predates the 2026-08-05 restructure — where it says "M38" it means today's M39.**
- **E35.5's spec and delivery notice** (P10-M35) — **the blinding discipline, the pre-registered rubric,
  and the six recorded arguments against over-reading a PASS.** **Read the six.**
- **P11-M37 milestone spec §Guardrails, lines 162–181** — G1 and G2 in their original form, plus the
  note that **neither risk they address is agentic-specific**
- **E38.2's Delivery Notice** — the dispatch path, the environment, and **the adapter's constraints**

### The framing to carry

> **The comparison's value is entirely in its method.**

A result from an unblinded, un-pre-registered, asymmetrically-constrained comparison is **not a weak
result — it is not a result.**

---

## Canonical Happy Path (Reminder)

1. ✅ Execution completed (all deliverables created)
2. ✅ Epic Delivery Notice produced (you create this)
3. ⏸️ Stage-2 review by the Milestone Chat — **clean deliveries accepted by silence** (SN-13 /
   PSG §11.6); a Review Decision is the exception path only
4. ⏸️ Merge authorization — an **in-chat act, no ceremonial artifact** (SN-19 / PSG §1A under §11.6);
   the harness enforces explicit human authorization regardless. **MODE IS NOT AUTHORITY** — running
   agentically changes nothing here
5. ⏸️ PR merged to `milestone/M38`
6. ⏸️ Execution stops

**You are responsible for steps 1–2 only. Then you STOP and await instruction.**

---

## Epic Delivery Notice

**File:** `docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M38-E38.6__delivery-notice.md`

Use the committed template `governance/templates/delivery-notice.md` (reference, don't restate;
PSG §12 defines the content requirements).

**This Epic's Delivery Notice must additionally record:**

- **What was compared and why its ground truth is knowable**
- **The blinding method, defended** — and **where the committed material is**, so a reader can verify it
- **That the rubric was committed BEFORE any run**, with the commits that prove it
- **Both arms' results**, whichever way they fell — **a poor local result is evidence, not failure**
- **The dispatch path and environment** — which engine, which adapter, what environment
- **The adapter's constraints on the local arm**, and **whether the paid arm's were comparable** — stated
  plainly if they were not
- **Wall-clock timing for both arms**
- **G1 honoured** — the non-uniform inputs quoted verbatim, shown
- **G2 honoured** — how completion was re-measured, **the exit code recorded and explicitly not relied
  on**, and confirmation that **no agent's prose was treated as evidence**
- **A §What this does NOT establish section** — sample size, confounds, and the conclusions a reader must
  not draw, **chiefly that no routing policy changed**
- **Explicit confirmation that `model-routing-policy.md` is unmodified** and no policy changed
- **Explicit confirmation that no dispatch path was scaffolded** — or, if none existed, **that the Epic
  escalated**
- **Explicit confirmation that G11 was not claimed**
- **The citation rule applied** — instances by artifact + defect, any total a floor with its date, base
  and scope, **nothing renumbered, the collision not re-adjudicated**
- **Constraint 9 addressed explicitly**
- **Things noticed and deliberately left, named individually**
- **The suite figure with the exact command**, and any P10-GH-10 occurrence **with both results**
- **Which arm, environment and repository every reported command ran in** (`P11-GH-2`)

---

## Exit Conditions

**You MUST stop execution when:**
- ✅ All deliverables are completed and committed
- ✅ Pull request is opened to `milestone/M38`
- ✅ Epic Delivery Notice is produced

**Then report:**
"Epic E38.6 execution complete. Epic Delivery Notice produced. Awaiting Milestone Chat review and merge
authorization."

**Also report if you STOPPED without completing** — no working local dispatch path, material you could
not blind honestly, a GPU lane you could not get. **Say what happened and stop.** **Reporting blocked is
a success outcome; scaffolding is not.**

---

## Begin Execution

You may now begin executing Epic E38.6 — **once E38.2 has delivered.**

Start by:
1. Checking your branch, then Prerequisite Verification and the model check
2. **Reading E38.2's Delivery Notice and confirming a real engine was invoked end-to-end** — **if not,
   escalate before doing anything else**
3. Creating branch `epic/P11-M38-E38.6` from `milestone/M38`
4. **Designing the blinding, and committing the material and rubric together before any run**

**Three framings to carry throughout.**

**The method is the deliverable.** An unblinded comparison over material whose answer is in git measures
retrieval, not capability — and it would produce **a flattering local result**, the most misleading
possible outcome for a decision about where to route real work. **Blind it, pre-register it, and commit
the proof.**

**Escalating beats scaffolding, and it is not close.** M37 paid for this lesson in full. This is the
place where the same mistake would be cheapest to make and most expensive to have made.

**And you are closing a loop the CFO opened.** The split-posture decision survived a relocation, a
ruling and a restructure rather than being discarded, **and it arrives at the one place it can be run
natively.** Whatever the result, **record it honestly and let someone else write the "therefore."**
