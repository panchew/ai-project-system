# Epic Execution Chat Starter — E38.5

**Epic:** E38.5 — Milestone-Context Evidence for `qwen3-coder:30b`
**Phase:** P11 — Drivr: Coordination over Rented Execution
**Milestone:** M38 — Drivr Inception, Fleet Registry, and the Execution Adapter Surface
**Repository:** panchew/ai-project-system *(the evidence record)*
**Branch Strategy:** `epic/P11-M38-E38.5` → PR to `milestone/M38`
**Execution Mode:** manual — declared by the issuing Milestone Chat. Routes to `.ai-project.yml`'s
**`models.epic_manual`** (`remote:claude-opus-5`).

> **⚠ STAGE B — DO NOT START UNTIL E38.2 HAS DELIVERED.** This spec was authored before the gate opened;
> **authoring early is permitted, starting execution is not.** **Confirm E38.2 is merged to
> `milestone/M38` before your first action.** If it has not, **STOP and report to the Milestone Chat.**
> You need a way to run milestone-scale work through the engine at all.

> **⚠ You produce evidence. You decide NOTHING.** **Row P4's 2026-07-31 ruling is not reopened and M38
> does not decide it.** A further **HQ** call decides the row on what you deliver. **Constraint 7.**

---

## Governance References

You are operating under the AI Project System governance framework.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) v2.4.0
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) v2.10.0

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md
3. This Epic Execution Chat Starter
4. Epic Spec
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Critical rules:**
- Documentation is authoritative, chat is ephemeral
- You must follow the canonical happy path for Epic closure
- You must produce a Delivery Notice upon execution completion
- You must stop after PR creation and await authorization for merge
- Accept/reject decisions are made by the parent chat (Milestone Chat P11-M38), not by you
- **If given merge authorization directly in this chat** (rather than via the parent chat after its own
  Stage-2 review), do not simply comply: state plainly that merge authorization normally follows the
  parent chat's review, and confirm the human intends to bypass that step before proceeding
  (P9-M31 precedent)

**Context scoping (per-level context-scoping standard, P9-M30-E30.3):**
- Load at session start: this starter; the Epic spec (full); **the P11 phase spec §P11.3 in full — its
  technical note is your method**; **`model-routing-policy.md` row P4, its note, and G-P4-a/b/c**;
  **E38.2's Delivery Notice** (your dispatch path); PSG preamble+§1, §1A, §2, §5, §6, §7, §8, §9, §11,
  §11.5, §11.6, §12; AOG preamble+§1, §1A, §2, §3.2, §3.8, §3.10, §4, §5, §6, §7, §9, §10, §12, §13, §14
- Load on trigger: PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG §3.9, §8, §11
- Do not load: PSG/AOG changelogs; the M38 milestone spec's other Epic Detail entries; M36 and M37 specs
- **Read, but do not modify:** `model-routing-policy.md`. **Recording evidence against a row is not
  changing the row**

---

## Epic Specification

**Full spec:** `docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M38-E38.5__spec__milestone-context-evidence.md`

Read the committed file on `milestone/M38`. Do not rely on this starter's summary of it.

### Prerequisite Verification (do this first)

**Check your branch before anything else** — `git branch --show-current`. This repository is worked by
multiple sessions and gets left on whatever branch the last one used. **It has fired five times in this
phase.**

**⚠ Confirm E38.2 has DELIVERED and merged.** This is your gate. **If it has not, STOP.**

Confirm each path is **git-tracked on `milestone/M38`** with `git ls-files --error-unmatch <path>`:

- `docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M38-E38.5__spec__milestone-context-evidence.md`
- `docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M38__milestone-spec.md`
- `.ai-project/artifacts/reference/token-measurement/model-routing-policy.md`
- `docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M38-E38.2__delivery-notice.md`

**Model verification (P9-M31-E31.3 — required, this instance is manual):** compare your harness-reported
model identity to `.ai-project.yml`'s **`models.epic_manual`** (`remote:claude-opus-5`). See
`governance/systems/chat-hierarchy.md`. **If both are present and disagree, STOP.**

**State the layer you verified at (`P11-GH-2`).** **Your trap is the estimate/measurement boundary.**
This Epic traffics in numbers, and **a number derived from a ratio is not a measurement.** **Label every
figure as measured — with the command — or as an estimate.**

### Summary

Measure whether `qwen3-coder:30b` can handle a milestone's context, as a **fourth axis** beside row P4's
three gates — **and hand the number to HQ without drawing the conclusion.**

---

## ⚠ The arithmetic already answers "does it fit." Read this before planning.

**Measured 2026-08-08 on `milestone/M38`, using the project's own ratio from B3.1 (7,819 bytes ≈ 2,000
tokens → ~3.9 bytes/token):**

| Scope | Bytes | ≈ Tokens | **% of the 32,768 loaded window** |
|---|---|---|---|
| M38's milestone spec alone | 75,252 | ~19,300 | **~59%** |
| **FLOOR** — starter + spec + phase-spec §P11.3 only | **106,510** | **~27,300** | **~83%** |
| **CEILING** — the floor plus PSG + AOG in full | **215,043** | **~55,100** | **~168%** |

> **Even the FLOOR — excluding `PROJECT-SYSTEM-GUIDELINES.md` and `AI-OPERATING-GUIDELINES.md` entirely
> — consumes ~83% of the loaded window before the model emits a single token**, and before any Epic
> spec, Delivery Notice, field evidence, or conversation.

> ### ⚠ That ceiling row was wrong once already — and its cause is your Epic's own subject
>
> **At spec v1.0.0 the ceiling read 273,290 bytes / ~213%. It was overstated by 58,247 bytes (~27%),
> caught on the Phase Chat's Stage-1 review.**
>
> **The cause: `58,247 = 64,345 − 6,098`** — the phase spec's **full** size minus **§P11.3**. The
> ceiling was computed by summing five documents **in full** and then labelled *"the floor plus PSG and
> AOG."* **One table counted the same document at two scopes in adjacent rows, and the label described
> one while the number carried the other.**
>
> **The floor was exact and the conclusion survived** — 168% still does not fit. **But take the lesson
> into your own method: label the scope in the row, not only in the prose, and recompute every row from
> its own definition rather than from an earlier total.**

> ### ⚠ The floor is moving, and this Epic's own review is moving it
>
> **The milestone spec grew 72,344 → 75,252 bytes across M38's Stage-1 reviews.** **The context pressure
> you are measuring is partly produced by the process measuring it.**
>
> **So: date and commit-anchor every byte count you report, and state which commit of the milestone spec
> you measured.** A bare number is stale on arrival.

**What this changes about your Epic — three things:**

1. **It is an ESTIMATE, not a result.** One ratio, from one document set, applied to another.
   **Re-measure. Do not inherit it.** A tokenizer disagreeing by 20% moves the floor between 66% and
   100% — qualitatively the same, quantitatively very different.
2. **It reframes the valuable measurement.** *"Does it fit"* has an arithmetic answer already. **So the
   measurement worth making is what the usable ceiling actually is and WHAT HAPPENS as it is
   approached** — see §Distinguish the failure modes.
3. **It does NOT decide row P4 and does NOT condemn the model.** Capacity at a **given loaded window** is
   a property of the **deployment**, not the weights. **"The model is fine and the window is too small"**
   is a different sentence with different consequences. **Say which one you measured.**

---

## Two more findings you must not rediscover

### Finding 2 — E35.5's PASS belongs to a DIFFERENT MODEL

**Measured in E35.5's own spec and delivery notice:**

- **Its primary candidate was `qwen3.6:27b`, not `qwen3-coder:30b`** — chosen deliberately: *"it is a
  general reasoning model, which is what Stage-2 review is — `qwen3-coder:30b` is coder-tuned."*
- **`qwen3-coder:30b` was only an "optional comparator, if running it is cheap."**
- **The 4-of-5 PASS with zero false alarms is `qwen3.6:27b`'s.**

> **So the model with a milestone-level *judgment* result is not the model you measure for *capacity*.**
> **A1.4 and the milestone spec both name `qwen3-coder:30b` — measure what you were told to measure** —
> **but RECORD the mismatch.** A fourth axis is only useful if the axes compose, and these two were
> measured on different models.

**Two facts that sharpen it:** **`qwen3.6:27b` is in Ollama and declared NOWHERE in `opencode.json`** —
so the model that passed the judgment back-test **is not currently routable through the adapter**
(record it; **do not fix it**). And **E35.5's delivery notice records six arguments against over-reading
its PASS** — **read them, and do not lean on that PASS.**

### Finding 3 — the loaded window is not adjustable from where you sit

- **The `/v1` endpoint silently ignores `options` entirely** — forcing `num_ctx` changed nothing. **The
  transport OpenCode uses cannot set the loaded context.**
- **`local-agent-runner`'s native `/api/chat` DOES honour `options`** — a real transport asymmetry, and
  the phase spec explicitly says it is **not** a retention argument for E38.4.

> **So through the adapter path, 32,768 is a fixed denominator.** Your question is not *"what could this
> model do with a bigger window"* but *"what can it do with the window the deployment provides."*
> **If you find a way to raise it, that is a finding — changing the deployment is not your Epic.**

**And read the loaded value from `/api/ps` AFTER loading** — it returns **`{"models":[]}`** when nothing
is loaded, and **`/api/show` reports the TRAINED MAXIMUM (262,144)**, which is exactly the number
`opencode.json` wrongly declares. **"Empty" must never degrade to "use the trained maximum."**

---

## Distinguish the failure modes — this is where your value is

Finding 1 makes *"it doesn't fit"* nearly arithmetic. **So measure what "doesn't fit" actually DOES:**

| Failure mode | Why it matters to row P4 |
|---|---|
| **Silent truncation** | **The worst outcome — nothing reports it.** The phase spec names this as the live risk |
| **Degraded recall before the ceiling** | Comprehension falls off before the hard limit — the usable window is smaller than the loaded one |
| **Clean failure** | The stack refuses or errors — recoverable, detectable, schedulable |

**These have very different consequences, and only a run distinguishes them. Report which occurred, with
evidence.**

**The marker method is recorded and available** — *plant a marker at position 0 of a prompt that exceeds
the suspected limit, ask for it back, and read `/api/ps` for `context_length`.* The phase spec records
it precisely so *"the next correction is cheaper."* **Use it or better it; say which.**

---

## The Hard Constraint — and the drift, which is NOT building

**Drivr rents. It does not build what it can invoke.**

> **Your drift is not building something. It is CONCLUDING something.**
>
> You will produce a number that bears directly on a routing decision, and **the pull to finish the
> sentence will be strong.** *"The floor alone consumes 83% of the window, therefore milestone × local
> is not viable"* is one short step from the measurement — **and it is row P4's sentence, not yours.**
>
> **Constraint 7 exists because the evidence and the decision are owned by different levels.** HQ ruled
> on row P4 on 2026-07-31 with three gates named. **You add a fourth axis to that record. You do not
> close the record.**
>
> **Report what you measured and what it implies. Do not write the "therefore."**

**Second drift: scope creep into the deployment.** You will want to raise the loaded context and
re-measure — a more interesting experiment. **It is also a change to the deployment and it is not
yours.** **Note the possibility; do not act on it.**

---

## Execution Contract

### Your Responsibilities (Coding Agent)

1. **Check your branch**, run Prerequisite Verification and the model check
2. **CONFIRM E38.2 HAS DELIVERED** — your gate. If not, **STOP**
3. **Read phase spec §P11.3's technical note in full** — it is your method
4. **Create branch** `epic/P11-M38-E38.5` from current `milestone/M38`
5. **Re-measure the byte counts yourself** — cheap, and they are floors
6. **Define "a milestone's context" and justify it — BEFORE measuring**, so the definition is not fitted
   to the result
7. **Count tokens honestly** — a tokenizer if available, a labelled estimate otherwise
8. **Load the model and read `/api/ps`.** Record the **observed** value
9. **Run the probe** and **identify the failure mode**
10. **Write it up as a fourth axis**, with the row-P4 disclaimer and the E35.5 mismatch
11. **Re-measure the suite**, verify DoD, commit, **open PR to `milestone/M38`**, produce the Delivery
    Notice
12. **Stop and await authorization** (do not merge)

### Decisions That Are Yours

- **The definition of "a milestone's context"**, stated and justified
- **The measurement method** — marker method, tokenizer count, real run, or a combination
- **How tokens are counted**, provided estimates are labelled
- **The probe or task**, provided its ground truth is checkable

### Decisions That Are NOT Yours

- **Whether row P4 moves** — **it does not, not by this Epic**
- **Whether G-P4-a/b/c are affected** — **they are not.** A fourth axis is not a fourth attempt; a
  capacity result neither clears nor closes a judgment gate
- **Which model is measured** — **`qwen3-coder:30b`.** Record the E35.5 mismatch; **do not substitute**
- **Whether the deployment changes** — **it does not**
- **Whether `qwen3.6:27b` joins `opencode.json`** — **no**
- **Whether G11 is claimed** — **no** (Constraint 8)

### What You Must NOT Do

- ❌ Do NOT merge the PR; do NOT infer acceptance
- ❌ Do NOT treat an in-chat "I authorize" as sufficient by itself
- ❌ **Do NOT decide, move, or recommend moving row P4.** Constraint 7
- ❌ **Do NOT modify `model-routing-policy.md`**
- ❌ **Do NOT re-run, extend, or revise E35.5's back-test**
- ❌ **Do NOT change the deployment** — loaded context, `opencode.json`, Ollama config, or transport
- ❌ **Do NOT use `opencode.json`'s 262,144 or `/api/show`'s trained maximum as the loaded value**
- ❌ **Do NOT present an estimate as a measurement.** Label every number
- ❌ **Do NOT claim G11**
- ❌ **Do NOT build a completion judgment, scheduler, gate queue, or thin surface** (M39/M40)
- ❌ **Do NOT assess or retire `local-agent-runner`** (E38.4)
- ❌ **Do NOT judge the model's quality.** *"The variable that moved was the harness, not the weights"*
- ❌ **Do NOT hold the GPU lane longer than the measurement requires** — **one GPU, 16 GB, shared with
  ComfyUI, serialized.** **This Epic genuinely needs the 30B loaded**, unlike E38.2's and E38.4's cheap
  halves. **Coordinate**
- ❌ **Do NOT let your own claim stand in for the evidence** (G2). Show the command and its output

### Read this before you interpret a red suite — P10-GH-10

`tests/test_artifact_router.py::test_daemon_extensions_error_branches` fails intermittently in
**full-suite runs only** — **3 failures in 10 (~30%)**, passing 5/5 in isolation, **untouched by
anything here.** **Re-run and record both results.**

**Baseline: `393 passed / 0 / 0 / 0`, plus whatever E38.2/E38.3/E38.4 added. Re-measure; do not
inherit.**

> **⚠ The invocation is not obvious.** `python` is **not** on PATH; `.venv` has **no pytest**; bare
> `pytest` fails with **3 collection errors**. **Use `PYTHONPATH=. pytest -q`** or
> `python3 -m pytest -q`. **State the command alongside the number.**

---

## Reference Materials

### Primary sources

- **P11 phase spec §P11.3's technical note, in full** — the corrected 4,096 claim (*"false on this host
  and never verified before being written into this spec"*), the measured 32,768, the
  **`/v1`-ignores-`options`** finding, the **8× overpack**, and **the marker method**. **This is your
  method, and it was itself a correction — treat every number in it as re-measurable**
- **`model-routing-policy.md`** — **row P4 (line 41), its note (line 46), G-P4-a/b/c (lines 59–60).**
  **Read it; do not modify it**
- **E35.5's spec** (model-choice reasoning, lines 114–119) and **delivery notice** (the PASS, and **the
  six arguments against over-reading it**)
- **B3.1 field evidence** — the ~3.9 bytes/token ratio, and **§What this does NOT establish**, which
  states in terms that it **does not decide row P4**: *"A bugfix spec is not a milestone's context, and
  A1.4's question is untouched by it"*

### The framing to carry

> **You are producing a number for someone else to rule on.**

The most useful thing you can deliver is a measurement so clearly scoped that **HQ can act on it without
re-deriving anything.** The least useful is **a conclusion you were not authorized to draw.**

---

## Canonical Happy Path (Reminder)

1. ✅ Execution completed (all deliverables created)
2. ✅ Epic Delivery Notice produced (you create this)
3. ⏸️ Stage-2 review by the Milestone Chat — **clean deliveries accepted by silence** (SN-13 /
   PSG §11.6); a Review Decision is the exception path only
4. ⏸️ Merge authorization — an **in-chat act, no ceremonial artifact** (SN-19 / PSG §1A under §11.6);
   the harness enforces explicit human authorization regardless
5. ⏸️ PR merged to `milestone/M38`
6. ⏸️ Execution stops

**You are responsible for steps 1–2 only. Then you STOP and await instruction.**

---

## Epic Delivery Notice

**File:** `docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M38-E38.5__delivery-notice.md`

Use the committed template `governance/templates/delivery-notice.md` (reference, don't restate;
PSG §12 defines the content requirements).

**This Epic's Delivery Notice must additionally record:**

- **The definition of "a milestone's context"**, with its justification, byte count, token count, and
  **the counting method named**
- **Which figures are measured and which are estimates** — every one labelled
- **`/api/ps`'s OBSERVED loaded value**, read after loading, with the command shown, alongside any
  declared limit — and **explicit confirmation that neither `opencode.json`'s 262,144 nor `/api/show`'s
  trained maximum was used as the loaded value**
- **The method, reproducibly** — enough that a reader can re-run it
- **The failure mode identified** — silent truncation, degraded recall, or clean failure — **with
  evidence**
- **Whether the honest finding is about the model or about the deployment** — *"the model is fine and
  the window is too small"* is a distinct result, and it must be stated as such if that is what you
  found
- **An explicit statement that row P4 is NOT decided by this**, and that a further HQ call decides it
- **An explicit statement that G-P4-a/b/c are unaffected**
- **The E35.5 model mismatch** — its PASS is `qwen3.6:27b`'s, chosen over `qwen3-coder:30b` because
  milestone work is general reasoning — **and `qwen3.6:27b`'s absence from `opencode.json`**
- **Explicit confirmation that the deployment is unchanged** and `model-routing-policy.md` is unmodified
- **Explicit confirmation that G11 was not claimed**
- **Constraint 9 addressed explicitly** — diagram if `governance/` was amended, or a plain statement
  that it did not fire
- **Things noticed and deliberately left, named individually**
- **The suite figure with the exact command** for any in-repo change, and any P10-GH-10 occurrence
  **with both results**
- **Which layer and environment every reported command ran in** (`P11-GH-2`)

---

## Exit Conditions

**You MUST stop execution when:**
- ✅ All deliverables are completed and committed
- ✅ Pull request is opened to `milestone/M38`
- ✅ Epic Delivery Notice is produced

**Then report:**
"Epic E38.5 execution complete. Epic Delivery Notice produced. Awaiting Milestone Chat review and merge
authorization."

**Also report if you STOPPED without completing** — E38.2 not delivered, no working dispatch path, a
definition of "milestone context" you could not defend, or a GPU lane you could not get. **Say what
happened and stop.**

---

## Begin Execution

You may now begin executing Epic E38.5 — **once E38.2 has delivered.**

Start by:
1. Checking your branch, then Prerequisite Verification and the model check
2. **Confirming E38.2 has delivered and merged** — the gate
3. **Reading phase spec §P11.3's technical note in full**
4. Creating branch `epic/P11-M38-E38.5` from `milestone/M38`
5. **Defining "a milestone's context" and justifying it — before you measure anything**

**Three framings to carry throughout.**

**Define before you measure.** A definition chosen after seeing the result is fitted to the result. **The
one decision that most affects your number is the one you make first**, so make it in writing and defend
it on its merits.

**Every number is measured or estimated, and you say which.** This Epic is about a limit that was
overstated by 8× because someone read the endpoint that answers without being asked twice. **A delivery
about silent truncation and overstated limits must not itself state a limit it did not observe.**

**And do not write the "therefore."** The measurement is yours; the ruling is HQ's; **the gap between
them is deliberate.** Row P4 was ruled on with three gates named, and you are adding a fourth axis to
that record — **not closing it.**
