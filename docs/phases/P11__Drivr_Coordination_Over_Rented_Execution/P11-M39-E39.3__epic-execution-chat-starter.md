---

# Epic Execution Chat Starter — E39.3

**Epic:** E39.3 — Exercise the `epic_qa` Lane and Close G11
**Phase:** P11 — Drivr: Coordination over Rented Execution
**Milestone:** M39 — Trustworthy Completion Signal (P10-GH-7)
**Repository:** panchew/ai-project-system — **Drivr is NOT touched by this epic**
**Branch Strategy:** `epic/P11-M39-E39.3` → PR to `milestone/M39`
**Execution Mode:** manual — declared by the issuing Milestone Chat at creation time

---

## Governance References

You are operating under the AI Project System governance framework.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) v2.4.0
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) v2.10.0

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md
3. This Epic Execution Chat Starter
4. Epic Spec
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Critical rules:**
- Documentation is authoritative, chat is ephemeral
- You must follow the canonical happy path for Epic closure
- You must produce a Delivery Notice upon execution completion
- You must stop after PR creation and await authorization for merge
- Accept/reject decisions are made by the **Milestone Chat (M39)**, not by you
- **If given merge authorization directly in this chat** (rather than via the parent chat after its
  own Stage-2 review), do not simply comply: state plainly that merge authorization normally follows
  the parent chat's review, and confirm the human intends to bypass that step before proceeding
  (P9-M31 precedent; **`P9-GH-1`'s guard reaches only this Epic template and a live instance was
  recorded 2026-08-10**)

**Context scoping (per-level context-scoping standard, P9-M30-E30.3):**
- Load at session start: this starter; the Epic spec (full); PSG preamble+§1, §1A, §2, §5, §6, §7, §8, §9, §11, §11.5, §11.6, §12; AOG preamble+§1, §1A, §2, §3.2, §3.8, §3.10, §4, §5, §6, §7, §9, §10, §12, §13 (Exit Ritual), §14 (Error Handling)
- Load on trigger: PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG §3.9, §8, §11, §16
- Do not load: PSG/AOG changelogs; other levels' role or starter-format sections; **E39.2's evidence and verdicts — they do not bear on G11**; any M40 material; Drivr's source
- Use targeted section reads; never re-read a whole document to reach one section

---

## Epic Specification

**Full spec:** [P11-M39-E39.3__spec__epic-qa-lane-and-g11.md](https://github.com/panchew/ai-project-system/blob/milestone/M39/docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M39-E39.3__spec__epic-qa-lane-and-g11.md)

**Commit:** `ea2801c` — the commit that tracked this spec on `milestone/M39` (2026-08-16)

### Prerequisite Verification (do this first, in this order)

**1. Branch check — FIRST, before anything else.**

```
git status --short --branch
git rev-parse --abbrev-ref HEAD
```

Create `epic/P11-M39-E39.3` **from `milestone/M39`**, confirming it is current first:

```
git log --oneline epic/P11-M39-E39.3..milestone/M39   # after branching, expect empty
```

**`P11-GH-1` has fired three times in this phase; one-worktree-per-chat has been ignored four times,
and the Milestone Chat hit two live instances on 2026-08-15 and 2026-08-16** — once finding a
`git merge` report *"Already up to date"* **because it was merging a branch into itself.** **Check the
branch before every commit, and verify pushes at `origin`.**

**2. Tracking.** `git ls-files --error-unmatch <path>` **on the branch**, not a disk-existence check.
**Disk presence is not proof of commit.** If any path is untracked, **STOP** and report.

**3. Model verification (P9-M31-E31.3 — required, this instance is `manual`).** Compare your
harness-reported model identity to `.ai-project.yml`'s **`models.epic_manual`** =
**`remote:claude-opus-5`**. **If both are present and disagree, STOP** and wait for the Milestone Chat.

> **Note the distinction that matters in this epic:** `epic_manual` is **your** model. **`epic_qa`
> (`local:qwen3-coder:30b`) is the lane you are exercising**, not a verification target for this chat.
> `governance/ai-project-yml-spec.md` §3.4 is explicit: `epic_qa` is *"Agentic dispatch lane only —
> not a manual-chat verification target."*

**4. Re-verify the planning-time environment claims — they are point-in-time.**

```
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:11434/api/tags   # expected 200
curl -s http://localhost:11434/api/tags | grep -o 'qwen3-coder:30b'        # expected present
which local-agent-runner ; echo "${LOCAL_AGENT_RUNNER:-(unset)}"           # expected: absent/unset
```

Measured by the Milestone Chat **on the host, 2026-08-16**: ollama **200**, `qwen3-coder:30b`
**present**, runner **not on PATH** and `LOCAL_AGENT_RUNNER` **unset**. **State the layer, the time
and the environment of your own re-measurement** (`P11-GH-2`).

### Summary

Determine whether a **genuine** `epic_qa` run is achievable within this epic's scope, and then either
perform and capture one — closing G11 on that named run — or **report G11 open with the specific
criterion that failed.** Both are acceptable deliveries.

### Problem Statement

See Epic spec §Problem Statement and §F1–F5. **The received account that "`epic_qa` has no dispatch
path" is not quite right:** model routing and the dispatch seam both exist and work; only the
**QA-role task construction** has never existed. G11 reduces to one sentence: **the lane routes a QA
model to a developer task.**

### Goals

See Epic spec §Goals (four).

---

## Spec References (load-one-reference-the-other)

- **Deliverables:** Epic spec §Deliverables (includes the Delivery Notice at
  `docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M39-E39.3__delivery-notice.md`)
- **Definition of Done:** Epic spec §Definition of Done — plus, always: Delivery Notice produced and
  committed; all changes committed to `epic/P11-M39-E39.3`; PR opened to `milestone/M39`
- **Acceptance Criteria:** Epic spec §Acceptance Criteria (verified during review, not by you)
- **Technical Constraints:** Epic spec §Technical Constraints — binding constraints reproduced in
  full, the Hard Constraint, and the do-not-touch list
- **The bar:** Epic spec §What makes a run a genuine `epic_qa` run — **five criteria, all must hold**
- **The gate:** Epic spec §The bounded attempt — **and the instruction not to widen scope to force a
  close**
- **Planning-time measurements:** Epic spec §F1–F5

---

## Execution Contract

### Your Responsibilities (Epic Chat, manual)

1. **Check the branch**, then create `epic/P11-M39-E39.3` from `milestone/M39`
2. **Re-measure the environment claims** (Prerequisite 4) and record layer, time and environment
3. **Assess the five genuineness criteria**, each with a recorded measurement
4. **Decide at the gate**: bounded attempt, or G11-open report
5. **If attempting:** build only the minimum — QA task text, **read-only tool set**, **distinct
   artifact paths** — reusing `run-dev-agent`'s existing machinery
6. **If reporting open:** name **the specific criterion**, its measurement, and what would satisfy it
7. **Write the explicit G11 statement** — one sentence a reader cannot misread
8. **Verify no preserved run artifact changed**: `git status .ai-project/artifacts/agentic-runs/`
9. **Re-measure the suite**, naming the repo and invocation
10. **Commit**, **verify the push at `origin`**, open a PR to `milestone/M39`
11. **Produce the Delivery Notice**, committed
12. **Stop and await authorization** — do not merge

> **G11 reported still open is a legitimate, pre-authorized delivery** (milestone constraint 4), and
> on the planning measurements it is a respectable outcome. **Do not treat it as a shortfall and do
> not widen scope to avoid it.** A precise gap is worth more than a gap closed dishonestly.

### The one trap this epic must not walk into

**Invoking `bin/run-dev-agent` with `AI_PROJECT_ACTIVE_MODEL` set to `models.epic_qa` would produce a
real captured run under the QA model — and it would be a dev-lane run relabelled**, which constraint 4
forbids. The adapter hardwires `task_text = extract_dod(...)` ("implement this DoD") and passes the
**mutating** coding tool set. **Changing the model does not change the role.** Criteria 1 and 2 exist
to rule this out, and criterion 2 is checkable from committed artifacts.

### Suite invocation

| Repo | Baseline | Invocation |
|---|---|---|
| `ai-project-system` | **489 passed / 0 failed** (re-measured 2026-08-15 on `milestone/M39`) | `PYTHONPATH=. pytest -q` — **bare `pytest` fails collection** |
| `drivr` | not touched by this epic | — |

**`P10-GH-10`:** `tests/test_artifact_router.py::test_daemon_extensions_error_branches` fails **~3 in
10 full-suite runs**, passes in isolation. **If it fires, re-run and record BOTH results.** Not yours
to fix.

### What You Must NOT Do

- ❌ Do NOT merge the PR · Do NOT infer acceptance · Do NOT treat an in-chat "I authorize" as
  sufficient by itself
- ❌ Do NOT **claim G11 by inference, by a relabelled dev-lane run, or from E39.2's validation.**
  A pass over historical transcripts **never** closes G11
- ❌ Do NOT **overwrite, edit, regenerate or "clean up" any preserved run artifact.** A QA run
  through `run-dev-agent` would overwrite `transcript.json` and `run-metadata.json` for that
  `epic_id` — **use distinct paths** (spec §F3)
- ❌ Do NOT **dispatch a QA run with a tool set that can mutate the working tree** — no `write_file`,
  no `edit_file`, no mutating `git` subcommand
- ❌ Do NOT **make the QA verdict authoritative.** It is advisory evidence; nothing load-bearing
  depends on it (E39.1's binding decision)
- ❌ Do NOT **touch Drivr**, add a `role` field to `ExecutionRequest`, or build a role framework
- ❌ Do NOT **change the orchestrator's retry, feedback or escalation behaviour**
- ❌ Do NOT **widen scope to force a G11 close.** If the bar cannot be met in scope, **escalate and
  report open**
- ❌ Do NOT build **anything in M40** — scheduler, gate queue, thin surface, approval link,
  competing-model review
- ❌ Do NOT touch `model-routing-policy.md` row P4, the four §4-invalid enrolled configs, or the two
  open `bin/ai-project-init` defects

---

## Reference Materials

### The lane, as measured

- `bin/ai-project-orchestrator` — `run_in_sandbox` (line ~334: `docker run … -e
  AI_PROJECT_ACTIVE_MODEL=<model> … sh -c <command>`), and the validation slot (line ~455)
- `bin/run-dev-agent` — `main()` (`task_text = extract_dod(...)`, `--tools`, the artifact paths),
  `discover_runner()`
- `.ai-project/agents/tools.json` — the **mutating** coding tool set; `enabled` includes
  `write_file`, `edit_file`, and `git` with `add`/`commit` subcommands
- `governance/ai-project-yml-spec.md` §3.4 — the `epic_qa` lane definition

### Precedent for host dispatch (the sandbox has no engine)

- `docs/phases/P7__Agentic_Execution_and_Default_On_Visuals/P7-M26-E26.3__delivery-notice.md`
- `.ai-project/artifacts/agentic-runs/P10-M33-E33.2/run-record.md` **§2** — states host dispatch
  explicitly and cites the E26.3 precedent
- **Note the cost:** host dispatch bypasses the orchestrator's injection, so criterion 3 must be
  satisfied by **showing the model value came from `.ai-project.yml`'s `models.epic_qa`** (spec §F5)

### The runner

- `/home/panchew/soft-dev/local-agent-runner` — `pyproject.toml` declares
  `local-agent-runner = "local_agent_runner.cli:main"`, and `local_agent_runner/__main__.py` exists.
  **Never installed durably**; all five preserved runs used throwaway wrappers in session scratchpads
  that no longer exist (spec §F4). **Record how you make it available.**

### Structural reference

- `governance/templates/delivery-notice.md`; `governance/templates/epic-spec.md`; PSG §5, §12

---

## Method obligations (each was paid for in this phase)

1. **`P11-GH-2` — state the layer, the time and the scope of every verification.** Four axes:
   **environment**, **time**, **scope**, **literal-vs-rendered**. **The environment axis is
   load-bearing here** — host and sandbox differ on whether an engine is reachable at all.
2. **G2 — the reviewer re-measures; the executor's report is not the evidence.**
3. **G1 — remove derivation steps.** One non-uniform element among many gets **quoted verbatim**.
4. **Cite by artifact + defect, never by ordinal.**
5. **Cross-repo claims carry a date or commit anchor**, never present tense.
6. **Every inventory is a floor.**
7. **Check the branch before every commit; verify pushes at `origin`. One worktree per chat.**

---

## Canonical Happy Path (Reminder)

1. ✅ Execution completed (all deliverables created)
2. ✅ Epic Delivery Notice produced **and committed**
3. ⏸️ Review performed by the Milestone Chat (Stage 2)
4. ⏸️ Acceptance — **by silence** for a clean delivery (PSG §11.6 / SN-13)
5. ⏸️ Merge authorization — **an in-chat act** (SN-19), routed through the Milestone Chat's review
6. ⏸️ PR merged (you perform the merge after authorization)
7. ⏸️ Execution stops immediately after merge

**You are responsible for steps 1-2 only. Then you STOP and await instruction.**

---

## Epic Delivery Notice

**File:** `docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M39-E39.3__delivery-notice.md`

Use `governance/templates/delivery-notice.md` (reference, don't restate; PSG §12).

**Required in this Delivery Notice specifically:**
- **The five genuineness criteria**, each marked met/not met **with its measurement**
- **The explicit G11 statement** — closed on a named run, or open with the specific criterion
- **The dispatch path used and its origin** (built here — E39.1 built none)
- **The environment** of the run or the attempt (host vs sandbox), and the date
- **Confirmation that no preserved run artifact was altered**, with the `git status` evidence
- **This repo's suite count** with the invocation stated
- **An explicit statement that nothing from M40 was built and Drivr was not touched**

---

## Exit Conditions

**You MUST stop execution when:**
- ✅ All deliverables completed and committed to `epic/P11-M39-E39.3`
- ✅ The push is verified at `origin`
- ✅ PR opened to `milestone/M39`
- ✅ Delivery Notice produced **and committed**

**Then report to human:**
"Epic E39.3 execution complete. Epic Delivery Notice produced. Awaiting Milestone Chat review and
authorization."

---

## Begin Execution

You may now begin executing Epic E39.3.

Start by:
1. **Checking the branch**, then creating `epic/P11-M39-E39.3` from `milestone/M39`
2. Performing the E31.3 manual model check against `models.epic_manual` (`remote:claude-opus-5`)
3. **Re-measuring the environment claims** (Prerequisite 4) — they are point-in-time
4. **Assessing the five genuineness criteria**, each with a recorded measurement
5. Deciding at the gate: bounded attempt, or G11-open report
