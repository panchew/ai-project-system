# Epic Execution Chat Starter — E37.1

**Epic:** E37.1 — System-tier versioning convention (P10-GH-8)
**Phase:** P11 — Drivr: Coordination over Rented Execution
**Milestone:** M37 — Corpus Record Conventions
**Repository:** panchew/ai-project-system
**Branch Strategy:** `epic/P11-M37-E37.1` → PR to `milestone/M37`
**Execution Mode:** **agentic** — declared by the issuing Milestone Chat. Routes to `.ai-project.yml`'s
**`models.epic_dev`** (**`local:qwen3-coder:30b`**).

> **⚠ THIS EPIC IS DISPATCHED, NOT OPENED.** Run it through **`bin/ai-project-orchestrator`**, which
> resolves `epic_dev` from `.ai-project.yml`, exports `AI_PROJECT_ACTIVE_MODEL`, and runs inside the
> sandbox. **Pasting this starter into a chat window is a manual run wearing an agentic label** — the
> declaration and the dispatch must agree. `bin/run-dev-agent` maps `local:<tag>` to the bare ollama
> tag and **refuses loudly with exit class 5** if the local model is genuinely unavailable
> (P9-M31-E31.2, *"local loadability is never assumed"*). **A class-5 refusal is a clean, informative
> failure — record it and report upward; do not work around it by opening a chat.**

> **Why agentic / local — CFO decision, 2026-08-05** (Milestone spec v1.1.0, §Execution Posture —
> SPLIT). M37's two epics run under different postures deliberately: **E37.1 local, E37.2 paid.** This
> Epic is the strongest local-lane candidate the phase has offered — highly repetitive, mechanically
> verifiable, and carrying a **cheap ground truth**: afterwards, either all seventeen documents comply
> or they do not, checkable in one command. Running one epic local and one paid inside a single
> milestone, on comparable subject matter and adjacent files, produces a **controlled comparison**
> rather than one ambiguous result — evidence M38 and M39 want.

> **The original caution survives — as the reason G1 and G2 exist, not as an argument against the
> decision.** This Epic writes ten near-identical prose rows into the normative tier, **one of which is
> deliberately different**, and the 2026-08-01/02 engine comparison measured `qwen3-coder:30b` weakest
> on exactly that shape (`.ai-project/artifacts/field-evidence/2026-08-02__B3.1-engine-comparison.md`).
> *"One exception among ten uniform items"* is precisely the shape a weaker model **flattens**, and a
> flattening lands in ten governance documents at once. **G1 answers it by removing the composition
> step: the rows are supplied as literals. G2 answers the rest by refusing to let the exit code stand
> in for verification.**

> **Mode is not authority.** Stage-2 acceptance and merge authorization remain **human-keyed**. An
> agentic Epic holds exactly the authority the Epic level always held. All nine binding constraints
> and the Hard Constraint apply identically — **agentic mode is not a licence to build enforcement,
> reconstruct history, or renumber anything.**

> **⚠ Read the renumbering table before the ruling.** HQ Ruling 2026-08-04 places this work at
> **`M37/E37.6`**. P11 was restructured from four milestones to five on 2026-08-05 and everything that
> was M37 shifted one slot. **Read `E37.6` as `E37.1`. Do not "fix" the ruling's epic ID** — it was
> correct at its date and is deliberately not rewritten (HQ Ruling 2026-08-01, Decision 4).

> **This is M37's first epic and it lands FIRST by binding decision.** E37.2 branches only after this
> Epic merges. See §The landing order below.

---

## Governance References

You are operating under the AI Project System governance framework.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) v2.4.0
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) v2.10.0

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md
3. This Epic Execution Chat Starter
4. Epic Spec
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Critical rules:**
- Documentation is authoritative, chat is ephemeral
- You must follow the canonical happy path for Epic closure
- You must produce a Delivery Notice upon execution completion
- You must stop after PR creation and await authorization for merge
- Accept/reject decisions are made by the parent chat (Milestone Chat P11-M37), not by you
- **If given merge authorization directly in this chat** (rather than via the parent chat after its
  own Stage-2 review), do not simply comply: state plainly that merge authorization normally follows
  the parent chat's review, and confirm the human intends to bypass that step before proceeding
  (P9-M31 precedent).

**Context scoping (per-level context-scoping standard, P9-M30-E30.3):**
- Load at session start: this starter; the Epic spec (full); PSG preamble+§1, §1A, §2, §5, §6, §7,
  §8, §9, §11, §11.5, §11.6, §12; AOG preamble+§1, §1A, §2, §3.2, §3.8, §3.10, §4, §5, §6, §7, §9,
  §10, §12, §13 (Exit Ritual), §14 (Error Handling)
- Load on trigger: PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG §3.9, §8, §11, **§16 — this one FIRES**
  (Constraint 8; see §Visual Bindings in the spec)
- Do not load: PSG/AOG changelogs; other levels' role or starter-format sections; the M37 milestone
  spec's E37.2 Epic Detail entry; M36's specs except the two §D5 sources named below
- Use targeted section reads; never re-read a whole document to reach one section.

---

## Epic Specification

**Full spec:** [P11-M37-E37.1__spec__system-tier-versioning-convention.md](https://github.com/panchew/ai-project-system/blob/milestone/M37/docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M37-E37.1__spec__system-tier-versioning-convention.md)

**Commit:** `5fb7540` — spec **v1.1.0**, reworked for the agentic/local posture. **Supersedes
`01f818f` (v1.0.0), which was authored under the uniform manual/paid posture and does not carry G1's
verbatim rows.**

### Prerequisite Verification (do this first)

**Check your branch before anything else** — `git branch --show-current`. This repository is worked by
multiple sessions and gets left on whatever branch the last one used; **M36's Finding 4 records a
false "content is missing" alarm from exactly this.** Re-check before your first commit and before any
verification you intend to report.

Confirm each path is **git-tracked on `milestone/M37`** using `git ls-files --error-unmatch <path>`,
not a disk-existence check. Disk presence is not proof of commit. If any path is untracked, STOP and
report to the Milestone Chat.

- `docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M37-E37.1__spec__system-tier-versioning-convention.md`
- `docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M37__milestone-spec.md`
- `.ai-project/artifacts/rulings/2026-08-04__ai-project-system-hq__ruling__p10-gh-8-versioning-convention.md`
- `.ai-project/artifacts/rulings/2026-08-05__ai-project-system-hq__ruling__artifact-id-citation-forms.md`
- `.ai-project/artifacts/closure-declarations/2026-08-04T00_00_00Z__P11-M36__milestone_closure_declaration.md`

**Model verification — agentic path, NOT the manual self-report check.** This instance is
`Execution Mode: agentic`, so the **P9-M31-E31.3 manual self-report check does not apply** and must
not be performed. Verification happens at **dispatch time**: `bin/ai-project-orchestrator` resolves
`models.epic_dev` from `.ai-project.yml` and `bin/run-dev-agent` enforces local loadability through
**E31.2's dispatch-time guard**, refusing with **exit class 5** rather than proceeding on an assumed
model. **Do not add a manual model check to an agentic run** — that is the manual path's mechanism and
importing it here would be a manual run wearing an agentic label.

### Summary

Seed the ten `governance/systems/` documents that carry neither a `version` field nor a
`## Changelog`, applying HQ Ruling 2026-08-04's already-decided convention — **forward-looking only,
no reconstructed history, and no enforcement built.**

### Problem Statement

Ten of seventeen `governance/systems/` documents **cannot record their own amendments**, and the set
has not shrunk in five weeks. M36 demonstrated the cost inside a single commit: E36.1 wrote precise
changelog rows into three versioned documents and could record **nothing** for its single largest
change — a ~74-line new normative section in `creation-chat-guide.md`. **The convention works exactly
where it exists and is silently absent where it does not**, under one author and one spec. The ten
include the directory's two most-cited documents.

### Goals

1. **All 17 documents carry a `version` field and a `## Changelog`.**
2. Each of the ten seeded rows cites **HQ Ruling 2026-08-04** and **points at git for prior history**.
3. **`chat-hierarchy.md`'s row records that M36 amended it** — the fact Decision 5's count loses.
4. The seven compliant documents are **shown** untouched.
5. **No reconstructed history. No enforcement built.**

---

## Spec References (load-one-reference-the-other)

- **Deliverables:** Epic spec §Deliverables (Delivery Notice at
  `docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M37-E37.1__delivery-notice.md`)
- **Definition of Done:** Epic spec §Definition of Done — plus, always: Delivery Notice produced and
  committed; all changes committed to `epic/P11-M37-E37.1`; PR opened to `milestone/M37`
- **Acceptance Criteria:** Epic spec §Acceptance Criteria (verified during review, not by you)
- **Technical Constraints:** Epic spec §Technical Constraints — **read the do-not-touch list first**
- **Binding Constraints:** Epic spec §Binding Constraints (reproduced there in full, 1 / 2 / 3 / 4 /
  8 / 9), **including guardrails G1 and G2**
- **THE HARD CONSTRAINT:** Epic spec §Hard Constraint. **Read it before you finish, not after** — see
  below
- **THE ROWS YOU WILL WRITE — SUPPLIED VERBATIM:** Epic spec §The verbatim rows (**G1**). All ten row
  literals plus the `## Changelog` header. **This is your primary working reference**
- **YOUR DESIGN DECISIONS ARE ALREADY MADE:** Epic spec §Design decisions, **D1–D4**. Apply them; do
  not re-litigate them
- **THE ERRATUM EDGE:** Epic spec §The erratum edge — context for why one row differs, so you do not
  regularize it
- **The verified inventory:** Epic spec §Verified at planning time, including the **three targets with
  no front matter at all**, which the milestone spec does not record

---

## G1 — the rows are supplied. Copy them; do not compose them.

**All ten changelog rows exist as literal strings in the Epic spec, §The verbatim rows.** Your task
for them is **transcription plus one mechanical `<PATH>` substitution** in the eight uniform rows.
`chat-hierarchy.md` and `creation-chat-guide.md` are supplied fully expanded — no substitution at all.

- **Do not shorten, rephrase, summarize or "improve" any row.**
- **Do not adjust the date.** All ten rows are dated `2026-08-05` — the convention's adoption date
  under M37, uniform by design, **deliberately not the run date**.
- **Verify by string comparison against the spec, not by reading for sense.** A flattened row reads
  perfectly well. That is exactly what makes it dangerous.

**Why this guardrail exists — read the next section so you know what it is protecting.**

---

## The erratum edge — what G1 is protecting

**One of your ten rows had to be written from a correction to the ruling that scheduled this work.
It has been written for you. This is the context, so you do not "fix" it back.**

**HQ Ruling 2026-08-04, Decision 5** states the forward-looking count is *"two, not three,"* and names
**both** amendments as `creation-chat-guide.md`. **That count is wrong. HQ footnoted it** in the
2026-08-05 ruling (Part 1), and M36's Closure Declaration §D5 carries the corrected figure.

**The verified count is three amendments across two unversioned documents** — and the missing one is
**`chat-hierarchy.md`** (E36.1, `4427ea9` merged `f1a5e75`, +3 / −3, two SN-23 citations
date-qualified in normative text including the Ratified-Decision-#2 supersession statement).

**The supplied `chat-hierarchy.md` literal already encodes this**, sourced from §D5 and citing the
erratum explicitly. **If you find yourself reconciling it against Decision 5 and shortening it, stop:
Decision 5 is the thing it corrects.**

> **Why this is the hardest row despite being the smallest, and why it is now a literal.** The failure
> mode does not look like a failure. A `chat-hierarchy.md` row written from Decision 5 is **plausible,
> uniform, and wrong** — it records the directory's most-cited document as unamended by the milestone
> that amended it, in the Epic whose entire purpose is that documents can record their own amendments.
> **Transcribe this row first**, before ten rows of momentum make it feel like just another row.

**And take the general lesson, because it binds the rest of your work:** the Phase Chat's 2026-08-04
routing carried two corrections; HQ applied the one that lowered the count and dropped the one that
raised it. **A count in a ruling is a floor, not an inventory.** Re-measure everything this Epic
states.

---

## The Hard Constraint — you are the epic that creates this temptation

**M37 records conventions and applies them mechanically. It builds no enforcement. No test, no
linter, no validator.**

On your delivery, all seventeen documents will have a uniform shape — which makes *"assert every
`governance/systems/` document has a `version` and a `## Changelog`"* **a three-line test that would
pass immediately.** It is genuinely a good idea. **Do not write it.**

- B3.1's carve-out exists for exactly that class of guard, **and this milestone is not it.**
- A convention-enforcing test is its own scoped item. **Inventing it here is how a fixed-contents
  milestone grows a third epic in everything but name** — inside the milestone created to stop that
  exact pattern.
- HQ held this boundary twice in this phase (2026-08-01 Decision 5; 2026-08-04 Decision 3), and
  **E36.4 held it under real pressure** when handed a verification command and declining to promote it
  into a committed test.

**If you judge the guard valuable — record the recommendation in the Delivery Notice and escalate it
to the Milestone Chat (P11-M37). That is a success outcome, not a consolation prize.** Building it is
a DoD failure.

**Ad-hoc shell used to *measure* compliance is fine and should be recorded.** Committing it as a guard
is not. The line is whether it lands in the repo as something that runs again.

---

## The adjacent tidy-ups — notice them, record them, leave them

Measuring this directory surfaces several things that are arguably wrong and cheap to fix. **The Epic
spec's table is authoritative; the short version:**

| What you will notice | What you do |
|---|---|
| Front-matter date-key drift (`last_updated` vs `effective_date`) | **Report and leave** |
| `epic-execution-chat-starter.md` declares `type: template` in `governance/systems/` | **Report and leave.** Do not change it |
| Three documents have no front matter at all | **In scope — but only the minimal D2 block.** No invented date |
| The seven compliant documents' rows are not uniformly formatted | **Leave entirely** (Constraint 2) |

**This is the same discipline E36.5 applied when it declined the two-character `PSG:605` fix — and HQ
explicitly refused to undercut that judgment by acting informally.** Noticing and declining **is** a
deliverable. Noticing and silently fixing is scope growth; noticing and saying nothing loses the
finding.

---

## The landing order — binding, and it removes an obligation from you

Both M37 epics amend `governance/systems/creation-chat-guide.md`. **The Milestone Chat has decided
E37.1 lands first**, and E37.2 branches only after this Epic merges.

**What that means for you: you carry no reconciliation obligation for E37.2.** Your
`creation-chat-guide.md` seeding row records **M36's two amendments and nothing else.** E37.2 will add
its own row to the `## Changelog` you create and bump `version` `1.0.0` → `1.1.0`.

**Leave `creation-chat-guide.md`'s §Steering Note ID Allocation section (line ~161) byte-untouched.**
It is E36.1's merged content and it is the anchor E37.2 writes alongside.

---

## Execution Contract

### Your Responsibilities (Coding Agent)

1. **Check your branch**, run Prerequisite Verification
2. **Create branch** `epic/P11-M37-E37.1` from current `milestone/M37`
3. **Re-run the compliance measurement** (Epic spec §Verified at planning time) and record the output
   **before editing anything**. Seventeen / seven / ten is a planning-time fact, not a guarantee
4. **Transcribe `chat-hierarchy.md`'s G1 literal FIRST**, from Epic spec §The verbatim rows
5. **Then the eight uniform rows** (one `<PATH>` substitution each), **then `creation-chat-guide.md`'s
   literal**. Create front-matter blocks per **D2** for the three documents that have none
6. **Verify the seven compliant documents are untouched** — `git diff --stat`, shown not asserted
7. **Produce the Structural Mermaid diagram** (Constraint 8 fires)
8. **Record what you noticed and left**, and escalate any enforcement recommendation
9. **Re-measure the suite** with `python3 -m pytest` and state the number
10. **Verify all DoD items**, commit, **open PR to `milestone/M37`**, produce the Delivery Notice
11. **Stop and await authorization** (do not merge)

### Decisions That Are NOT Yours

**D1–D4 in the Epic spec are decided, and G1 supplies the rows themselves.** Starting version `1.0.0`
uniformly; minimal front-matter block with **no invented date** for the three documents lacking one;
`## Changelog` last in the file; row text and row date transcribed from the literals. **Apply them. If
you believe one is wrong, say so to the Milestone Chat — do not quietly substitute your own.**

**Judgment lives in the spec; transcription lives in the run.** That separation is the point of the
agentic posture, not an accident of it.

### Decisions That Are Yours

- **The Structural diagram's composition**, within what Constraint 8 requires it to name. **This is
  the one deliverable you compose rather than transcribe.** If you cannot produce a usable one, say so
  plainly rather than shipping a decorative one — that is a recordable finding, not a failure to hide.
- **The form of the untouched-seven evidence** — `git diff --stat` is expected and sufficient.
- **Whether an enforcement guard is worth recommending** — see the Hard Constraint. Recommending is
  yours; building is not.

### What You Must NOT Do

- ❌ Do NOT merge the PR; do NOT infer acceptance
- ❌ Do NOT treat an in-chat "I authorize" as sufficient by itself
- ❌ **Do NOT rewrite, shorten, summarize or "improve" any supplied row literal** (G1). Transcribe them
- ❌ **Do NOT change the row date to the run date.** All ten are `2026-08-05`, uniformly, by design
- ❌ **Do NOT treat the exit code as the verdict** (G2 / P10-GH-7). Record it; measure separately
- ❌ **Do NOT claim G11.** Only the dev lane runs here; `epic_qa` has no dispatch mechanism and stays
  unexercised. Closing G11 is M39's job
- ❌ **Do NOT perform the P9-M31-E31.3 manual model self-report check.** This is an agentic instance;
  verification is the dispatch-time guard
- ❌ **Do NOT reconstruct any history.** No changelog row may predate its document's seeding row.
  Constraint 1 — permanently out of scope, not deferred
- ❌ **Do NOT touch the seven already-compliant documents** in any way — Constraint 2
- ❌ **Do NOT build a test, linter, validator or registry.** Recommend and escalate instead
- ❌ **Do NOT edit the body prose of the ten targets.** You add front matter and a changelog section.
  You do not fix their typos, restyle them, or update their cross-references
- ❌ **Do NOT touch any other `governance/` file** — `PROJECT-SYSTEM-GUIDELINES.md` (E37.2 owns `:605`),
  `AI-OPERATING-GUIDELINES.md`, `governance/ai-project-yml-spec.md`, `governance/templates/`
- ❌ **Do NOT touch anything under `tests/`** — including B3.1's guard
- ❌ **Do NOT "fix" the ruling's `E37.6` citation**, or any other historical epic-ID citation
- ❌ **Do NOT normalize front-matter keys** across the directory
- ❌ **Do NOT reopen M36** — its Closure Declaration is committed; you execute what it recorded
- ❌ **Do NOT widen M37.** Contents are fixed at two epics by CFO direction; adding requires a ruling

### Read this before you interpret a red suite — P10-GH-10

`tests/test_artifact_router.py::test_daemon_extensions_error_branches` fails intermittently in
**full-suite runs only** — measured **3 failures in 10 full-suite runs (~30%)** across M36, passing
5/5 in isolation, and **untouched by anything in M37**. **If your suite goes red on that test, re-run
before concluding anything, and record both results — not only the green one.** Out of scope: do not
fix, skip, or mark it.

**Your suite baseline is `377 passed / 0 failed / 0 skipped / 0 xfailed`**, re-measured by the
Milestone Chat on `phase/P11` at planning time. **`python` is not on `PATH`** in this worktree — use
`python3 -m pytest`.

### G2 — the run's exit code is not evidence, in either direction

**P10-GH-7 stands and it is two-sided on this stack:**

| Precedent | Exit code | What actually happened |
|---|---|---|
| **E33.2 Run A** | **0** | **Zero work done** |
| **E33.4** | **2** | **Complete, green work** |

**So: record the exit code as data, and conclude nothing from it.** Completion is judged by the
**reviewer's own external re-measurement** — 17 of 17 compliant, seven shown untouched, no
reconstructed history, ten rows matching the spec's literals by string comparison. **A green exit is
not evidence and a red exit is not a verdict.**

### If the run cannot start

`bin/run-dev-agent` exits with **class 5** when the local model is genuinely unavailable
(P9-M31-E31.2, *"local loadability is never assumed"*). **That is a clean, informative failure.**
Record it and report to the Milestone Chat. **Do not work around it by opening this starter in a chat
window** — that would silently convert the milestone's controlled comparison into two paid runs and
destroy the evidence the posture was chosen to gather.

---

## Reference Materials

### Primary sources

- **HQ Ruling 2026-08-04 (P10-GH-8)** —
  `.ai-project/artifacts/rulings/2026-08-04__ai-project-system-hq__ruling__p10-gh-8-versioning-convention.md`
  — **Decision 2** is the convention you are applying. **Decision 5 is the one that is wrong** (its own
  ERRATUM header says so)
- **HQ Ruling 2026-08-05, Part 1** —
  `.ai-project/artifacts/rulings/2026-08-05__ai-project-system-hq__ruling__artifact-id-citation-forms.md`
  — **the erratum**
- **M36 Milestone Closure Declaration, §"D5 obligation — unversioned amendments, recorded"** —
  `.ai-project/artifacts/closure-declarations/2026-08-04T00_00_00Z__P11-M36__milestone_closure_declaration.md`
  — **the three amendments with commits and diffs. This is the source for your two content-bearing
  rows**
- **P10-M35 carry-forward note** —
  `docs/phases/P10__Fleet_Adoption_and_Local_Inference_Proving/P10-M35__carry-forward-note__P10-GH-8-unversioned-system-documents.md`

### Structural reference — how a compliant document looks

Read the front matter and `## Changelog` of **`governance/systems/artifact-communication-protocol.md`**
(v1.4.1). It is the richest example in the directory and shows the table shape, the row voice, and the
epic-attribution convention (`E36.1 (P11-M36)`). **Match its shape. Do not edit it.**

### Your ten targets

`chat-hierarchy.md`, `creation-chat-guide.md`, `epic-execution-chat-starter.md`,
`governance-propagation.md`, `hq-chat.md`, `hq-execution-chat-starter.md`,
`milestone-execution-chat-starter.md`, `phase-execution-chat-starter.md`,
`PROJECT-TRACKER-INTEGRATION-SYSTEM.md`, `start-a-project.md`

**Three of them — `creation-chat-guide.md`, `governance-propagation.md`,
`PROJECT-TRACKER-INTEGRATION-SYSTEM.md` — have no front-matter block at all.** For those, seeding
means creating the block per **D2**.

---

## Canonical Happy Path (Reminder)

1. ✅ Execution completed (all deliverables created)
2. ✅ Epic Delivery Notice produced (you create this)
3. ⏸️ Stage-2 review by the Milestone Chat — **clean deliveries accepted by silence** (SN-13 /
   PSG §11.6); a Review Decision is the exception path only
4. ⏸️ Merge authorization — an **in-chat act, no ceremonial artifact** (SN-19 / PSG §1A under §11.6);
   the harness enforces explicit human authorization regardless
5. ⏸️ PR merged to `milestone/M37`
6. ⏸️ Execution stops

**You are responsible for steps 1–2 only. Then you STOP and await instruction.**

---

## Epic Delivery Notice

**File:** `docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M37-E37.1__delivery-notice.md`

Use the committed template `governance/templates/delivery-notice.md` (reference, don't restate;
PSG §12 defines the content requirements).

**This Epic's Delivery Notice must additionally record:**

- **The re-run compliance measurement, before and after** — the actual command output, not a summary.
  Seventeen of seventeen compliant at the end
- **Confirmation that all ten rows were transcribed from the spec's literals unmodified** (G1) — a
  claim a reviewer can check with `grep`, including that **all ten are dated `2026-08-05`**
- **Any row that was NOT transcribed exactly, and why.** If a row was altered, say so — a silent
  divergence is the failure this Epic's guardrail exists to catch
- **Evidence the seven compliant documents are unchanged** — `git diff --stat`, shown
- **Confirmation that no changelog row predates its document's seeding row** (no reconstruction)
- **The three front-matter blocks created**, and confirmation that **no date key was invented**
- **The adjacent tidy-ups noticed and deliberately left**, named individually
- **Whether an enforcement guard was judged valuable** — if so, the **recommendation and its
  escalation**, explicitly stating it was **not built**
- **Constraint 8 — the Structural Mermaid diagram**, inline and fenced
- **The re-measured suite figure** (expect **377 / 0**) and any P10-GH-10 occurrence **with both
  results**
- **The agentic-run record (E37.1 only):** that dispatch went through `bin/ai-project-orchestrator`;
  **the exit code, recorded as data and explicitly not as the verdict** (G2); and a plain statement
  that **G11 is not claimed — only the dev lane ran, `epic_qa` remains unexercised**
- **The local-lane finding, either way.** What the run produced, what the re-measurement found, and
  **where the two disagreed if they did.** A failed or partial run is a **usable result** — the
  substance can be completed manually and the finding still stands. Write it in the terms M38 and M39
  will need

---

## Exit Conditions

**You MUST stop execution when:**
- ✅ All deliverables are completed and committed
- ✅ Pull request is opened to `milestone/M37`
- ✅ Epic Delivery Notice is produced

**Then report:**
"Epic E37.1 execution complete. Epic Delivery Notice produced. Awaiting Milestone Chat review and
merge authorization."

**Also report if you STOPPED without completing** — a class-5 refusal, a partial diff, a row you could
not transcribe. **Say what happened and stop.** Under the evidence-either-way criterion that is a
usable result; concealing it or working around it is not.

---

## Begin Execution

You may now begin executing Epic E37.1.

Start by:
1. Checking your branch, then Prerequisite Verification
2. Creating branch `epic/P11-M37-E37.1` from `milestone/M37`
3. **Re-running the compliance measurement and recording it, before any edit**
4. **Transcribing `chat-hierarchy.md`'s G1 literal from Epic spec §The verbatim rows — first**

**Three framings to carry throughout.**

**This Epic is broad but shallow.** There is no hard problem in it. The risk is that **one consistency
lapse lands in ten governance documents at once** — and the specific lapse to guard against is
**flattening the one row that differs into the nine that do not.** G1 removes the composition step
precisely so that cannot happen: **copy the literals.**

**It is judged as much by what it declines as by what it delivers.** A uniform corpus makes a
three-line enforcement test irresistible, and this is the milestone created because a previous one
became *"the milestone things get put in."* **Recommending the guard and escalating it is the success
outcome. Writing it is not.**

**And this run is itself evidence.** It is the local lane's first M37-era test, chosen because its
ground truth is cheap and its substance is recoverable manually if the lane falls short. **A failed or
partial run is a usable result, not a delivery failure** — the one genuinely bad outcome is a run
whose result is unclear because nobody measured it, which is what G2 exists to prevent. **Report what
happened accurately, including what did not work.**
