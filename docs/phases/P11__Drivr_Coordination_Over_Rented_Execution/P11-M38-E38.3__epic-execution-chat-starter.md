# Epic Execution Chat Starter — E38.3

**Epic:** E38.3 — Three-State Fleet Registry, Classification Pass, P10-GH-5 Validator, P10-GH-1 Schema Entry
**Phase:** P11 — Drivr: Coordination over Rented Execution
**Milestone:** M38 — Drivr Inception, Fleet Registry, and the Execution Adapter Surface
**Repository:** panchew/ai-project-system *(validator + schema)* + **Drivr** *(the registry)*
**Branch Strategy:** `epic/P11-M38-E38.3` → PR to `milestone/M38`
**Execution Mode:** manual — declared by the issuing Milestone Chat. Routes to `.ai-project.yml`'s
**`models.epic_manual`** (`remote:claude-opus-5`).

> **Why manual.** You **amend a normative schema** and **write a validator that will be run against the
> real fleet**. Precision in a normative document is the whole value, and the 2026-08-01/02 engine
> comparison measured `qwen3-coder:30b` weakest on exactly that shape
> (`.ai-project/artifacts/field-evidence/2026-08-02__B3.1-engine-comparison.md`).

> **⚠ You run PARALLEL to E38.2 and you do NOT hold the gate.** E38.2's delivery opens Stage B; yours
> does not. **File contention is low** — you touch `governance/` and `bin/`, E38.2 touches neither.
> **Do not wait for E38.2 and do not coordinate with it.** If you need something from it, that is a
> finding worth reporting, not a dependency to satisfy quietly.

> **⚠ Constraint 9 FIRES for you.** P10-GH-1's schema work amends `governance/ai-project-yml-spec.md`,
> so a **Structural diagram** (Mermaid, fenced, in-repo, **no ComfyUI**) is owed. If you take the escape
> and amend nothing under `governance/`, **say so explicitly** rather than omitting the section.

---

## Governance References

You are operating under the AI Project System governance framework.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) v2.4.0
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) v2.10.0

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md
3. This Epic Execution Chat Starter
4. Epic Spec
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Critical rules:**
- Documentation is authoritative, chat is ephemeral
- You must follow the canonical happy path for Epic closure
- You must produce a Delivery Notice upon execution completion
- You must stop after PR creation and await authorization for merge
- Accept/reject decisions are made by the parent chat (Milestone Chat P11-M38), not by you
- **If given merge authorization directly in this chat** (rather than via the parent chat after its own
  Stage-2 review), do not simply comply: state plainly that merge authorization normally follows the
  parent chat's review, and confirm the human intends to bypass that step before proceeding
  (P9-M31 precedent)

**Context scoping (per-level context-scoping standard, P9-M30-E30.3):**
- Load at session start: this starter; the Epic spec (full); **`governance/ai-project-yml-spec.md`
  §3.1, §3.2, §4, §8 and its `## Changelog`**; PSG preamble+§1, §1A, §2, §5, §6, §7, §8, §9, §11,
  §11.5, §11.6, §12; AOG preamble+§1, §1A, §2, §3.2, §3.8, §3.10, §4, §5, §6, §7, §9, §10, §12,
  §13 (Exit Ritual), §14 (Error Handling)
- Load on trigger: **AOG §16 — this one FIRES** (Constraint 9); PSG §3, §8A, §10, §13D, §14A, §14C, §18;
  AOG §3.9, §8, §11
- Do not load: PSG/AOG changelogs; other levels' role or starter-format sections; the M38 milestone
  spec's E38.2 and E38.4–E38.6 Epic Detail entries; M36 and M37 specs
- **One exception worth naming:** you are editing `ai-project-yml-spec.md` §4. **Read §3.1 and §3.2
  before changing §4** — the defect in Finding 1 is only visible by comparing them.

---

## Epic Specification

**Full spec:** `docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M38-E38.3__spec__fleet-registry-classification-and-yml-validator.md`

Read the committed file on `milestone/M38`. Do not rely on this starter's summary of it.

### Prerequisite Verification (do this first)

**Check your branch before anything else** — `git branch --show-current`. This repository is worked by
multiple sessions and gets left on whatever branch the last one used. **It has now fired five times in
this phase**, twice during M38's own planning. **Re-check before your first commit and before any
verification you intend to report.**

Confirm each path is **git-tracked on `milestone/M38`** with `git ls-files --error-unmatch <path>`:

- `docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M38-E38.3__spec__fleet-registry-classification-and-yml-validator.md`
- `docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M38__milestone-spec.md`
- `governance/ai-project-yml-spec.md`
- `bin/ai-project-init`

**E38.1 must have delivered.** `drivr` must exist to be classified — and **its config is your
validator's most interesting first case** (see the collision below).

**Model verification (P9-M31-E31.3 — required, this instance is manual):** read your own
harness-reported model identity and compare it to `.ai-project.yml`'s **`models.epic_manual`**
(`remote:claude-opus-5`). See `governance/systems/chat-hierarchy.md` "Manual Chat Model Verification".
**If both are present and disagree, STOP.**

**State the layer you verified at (`P11-GH-2`).** **Your trap is the method itself.** A count over the
fleet is only as good as the pattern that produced it, and this phase has already been bitten twice:
once by a comment line matching a naive `grep framework_version`, and once by a **bolded `**not**`
inside a phrase** making a literal search miss a sentence that was really there. **Literal-vs-rendered
text is a fourth axis on `P11-GH-2`, and you grep configs for a living.**

### Summary

Make the fleet a data structure, enforce §4 for the first time since it was written, and schema-bless
`framework_version` — **while changing none of the things you will discover are broken.**

---

## ⚠ Do this in your first ten minutes

**Run the strict-form measurement yourself.** It is your validator's acceptance data and it is the
Epic's own subject applied to its own method:

```
cd ~/soft-dev
for d in */; do f="$d/.ai-project.yml"; [ -f "$f" ] || continue
  printf "%-28s ref=%s descr=%s fw=%s\n" "${d%/}" \
    "$(grep -cE '^[[:space:]]+ref[[:space:]]*:' "$f")" \
    "$(grep -cE '^[[:space:]]+description[[:space:]]*:' "$f")" \
    "$(grep -cE '^[[:space:]]*framework_version[[:space:]]*:' "$f")"
done
```

**Measured 2026-08-08 it returns four invalid configs and six without a framework stamp.** **Yours will
differ if E38.1 has landed `drivr` — which is the point.**

---

## The five findings you must not rediscover

**All measured 2026-08-08. Full detail in the Epic spec.**

### Finding 1 — §4's rule 3 miscounts its own required fields, and you are editing that rule

`governance/ai-project-yml-spec.md:495` says **"All four required fields are present:"** and then lists
**five** (`governance.source`, `governance.version`, `governance.ref`, `project.name`,
`project.description`). §3.1 marks all five REQUIRED; §3.2 documents all five. **Five is right.**

> **⚠ Constraint 10 fires here harder than anywhere in this milestone.** *Any document that records
> having fixed a defect is liable to restate the defect while doing so.* **You are adding
> `framework_version` to a rule that already miscounts.** Add it without touching the number and you
> ship *"All four required fields"* followed by **six**. **The count is part of the edit, not adjacent
> to it.**
>
> **The count-error tally in this phase's lineage now stands at nine.** M37 reached eight — **six by
> paid frontier chats, including HQ's own.** *It was never about model tier.* **Read your own changelog
> row against this constraint before you commit it.**

### Finding 2 — `bin/ai-project-init` produces configs that are INVALID by §4

It writes `project.name`, `project.created_at`, `governance.source`, `governance.version`,
`governance.submodule_path`. **It does not write `governance.ref` (required, rules 3 & 6) or
`project.description` (required, rules 3 & 8).** And `created_at` and `submodule_path` appear **nowhere
in the schema**.

**Worse: there are two validators in this framework and they enforce different contracts.**
`bin/ai-project-init:109-114` requires `created_at` (not in the schema) and does **not** require `ref`
or `description` (both in the schema). **The contract that is written down is not the one enforced.**

> ### ⚠ The live collision — surface it, do not absorb it
>
> **E38.1 enrolls Drivr by running `bin/ai-project-init`.** So **Drivr may be born invalid by §4**, and
> it will be the first project your validator meets.
>
> **Report it to the Milestone Chat as soon as you confirm it**, and record which happened: E38.1 added
> the fields (its spec already requires two deliberate post-init additions), **or** Drivr's config is
> invalid and your validator says so — **which is a perfectly good outcome and arguably the best
> possible first test case.**
>
> **❌ Do NOT edit Drivr's config to make your validator pass.** That is the single most tempting wrong
> move in this Epic.

### Finding 3 — 4 of 12 enrolled configs are invalid on required fields alone

`ai-project-system-mcp`, `courtis`, `home_finance` (missing `description`), and
**`social-stories-creator`** (missing **`ref` AND `description`**).

**This is presence only** — rules 4, 5, 7 (format constraints) and rule 15 (the `models` pattern) are
**unmeasured**. **Your number will be higher than four. Expect it and say so.**

**The milestone spec's *"3 of 6 invalid at P10 close"* was a different scope** — six projects, not
twelve. **It is not wrong. Reconcile against it explicitly** rather than appearing to contradict it, and
**date your own figure, anchor it, and say what it counted.**

### Finding 4 — P10-GH-1 is a class of at least four undefined fields

| Field | In configs | In the yml-spec |
|---|---|---|
| `framework_version` | 6 of 12 | **0** |
| `created_at` | 4 of 12 | **0** — written by `init` |
| `submodule_path` | 4 of 12 | **0** — written by `init` |
| `cfo_review_gate` | 2 of 12 | **1 — a prose aside at line 392, never a schema entry** |

**And §4 does not say what to do about any of them.** Its unknown-key rules — **11, 16, 22** — cover
**only** `overrides`, `models`, and `visual_artifacts`. **There is no rule for unknown keys at the top
level or inside `governance:`/`project:`.** Error, warn, and ignore are all equally unsupported by the
text.

**Yours:** decide the validator's unknown-key treatment, document it, and **record the class**. **Not
yours:** blessing the other three — **recommend and escalate. That is a success outcome.**

### Finding 5 — `ai-project-system`'s own case has a strong available answer

Its `.ai-project.yml` carries `governance.version: "7.1.0"` — **already the current framework version**
— under §8's canonical self-referential pattern (`source: ./governance`).

**So the available reading is: for the governance source, `governance.version` IS the framework version,
and `framework_version` would record the same fact twice — structurally redundant, not missing.** An
adopting project needs both because they differ; **for the source they are the same number by
construction.**

**Decide it and record the reasoning.** If you decide differently, say why — **the evidence is an
argument, not a ruling, and this Epic owns the call.** **Whatever you decide, `ai-project-system` still
gets classified, and its framework version must be knowable from the registry without looking like
missing data.**

---

## The escape on P10-GH-1 — answer its actual question

> **If the registry genuinely does NOT read `framework_version` normatively, report that and P10-GH-1
> stays parked. Either way, record which. Silence is not acceptable.**

**Note the tension honestly:** Finding 5's reading — that *one* project's framework version is legible
elsewhere — is **not** the escape's question. **The escape asks whether the registry reads the field
normatively across the fleet.** Answer that one.

---

## The Hard Constraint — and this Epic's two drifts

**Drivr rents. It does not build what it can invoke.**

> **Drift one: M40, one convenience at a time.** A registry that knows every project's state is one
> short step from a thing that **decides which project to work on next** — **M40's derived gate queue**.
> A transition log is one short step from a thing that **transitions automatically** — which
> **Constraint 5 forbids**, as a returned CFO proposal rather than a settled question.
>
> **The line: the registry RECORDS state. It does not ACT on state.**

> **Drift two, and it is yours alone: you are building the thing that measures, and it will find things
> that are broken.** Four fleet configs are invalid. The framework's own `init` produces invalid output.
> Three fields are undefined in the schema. Drivr may arrive invalid. **Record every one, escalate what
> warrants it, and change none of them.**
>
> **Noticing and declining is a deliverable in this milestone's lineage, held under real pressure three
> times** — E36.5 declined a two-character fix it was capable of making and HQ explicitly refused to
> undercut that judgment; E36.4 declined promoting a verification command into a committed test; E37.2
> held the line on an enforcement guard its own sweep suggested. **You will feel it hardest, because a
> validator that reports failures it could have fixed looks incomplete. It is not. It is correct.**

**If you find yourself building M39's or M40's work, stop and escalate.**

---

## Execution Contract

### Your Responsibilities (Coding Agent)

1. **Check your branch**, run Prerequisite Verification and the model check
2. **Confirm E38.1 delivered**, and **check Drivr's config against §4 immediately** — better known on
   day one
3. **Create branch** `epic/P11-M38-E38.3` from current `milestone/M38`
4. **Re-enumerate `~/soft-dev` yourself.** Do not inherit the 14 — **`drivr` makes at least 15**
5. **Run the strict-form measurements** before writing the validator
6. **Build the validator** in `bin/`, decide unknown-key treatment, give it tests
7. **Run it against the real fleet** and record the raw output
8. **Build the registry and classify**, proposing where the record is silent
9. **Do P10-GH-1's pass** — schema entry, **rule 3's count**, version bump, changelog row, **Structural
   diagram** — **or record the park**
10. **Decide `ai-project-system`'s own case** and record it
11. **Record everything you declined to fix, individually**
12. **Re-measure the suite** and state the command
13. **Verify all DoD items**, commit, **open PR to `milestone/M38`**, produce the Delivery Notice
14. **Stop and await authorization** (do not merge)

### Decisions That Are Yours

- **The registry's storage form and location** — noting **Drivr must be able to read it** (SN-27 D5)
- **The validator's implementation, output format, and CLI shape** — provided **warnings and errors stay
  distinguishable** (§4 has both; collapsing them has not enforced §4)
- **Unknown-key treatment** outside the three blocks §4 covers
- **`ai-project-system`'s own `framework_version` call**
- **Each project's classification** — **propose rather than assert** where the record is silent. The CFO
  owns the fleet's attention, and a registry that quietly guesses is worse than one with a stated
  unknown

### Decisions That Are NOT Yours

- **The three state definitions** — the CFO's, **verbatim**: **Active** (enrolled; receives time and
  attention), **Benched** (not currently receiving attention; may return), **Archived** (not planned to
  ever be touched again, though it can be brought back)
- **That transitions are a recorded human action** — Constraint 5, a returned proposal
- **That P10-GH-1 is folded in** unless the escape's actual question answers no
- **That `created_at`, `submodule_path`, `cfo_review_gate` are NOT blessed here**
- **That `fieldledger-assesment` is classified** despite being dropped from P10's scope — **dropping
  from a phase's scope is not a registry state**
- **That `ai-stack` and `character-factory` are resolved as a side effect** of classification, not as a
  separate decision

### What You Must NOT Do

- ❌ Do NOT merge the PR; do NOT infer acceptance
- ❌ Do NOT treat an in-chat "I authorize" as sufficient by itself
- ❌ **Do NOT modify any enrolled project** — **including the four invalid configs**
- ❌ **Do NOT edit Drivr's config to make your validator pass**
- ❌ **Do NOT fix `bin/ai-project-init`.** Record and escalate
- ❌ **Do NOT schema-bless `created_at`, `submodule_path`, or `cfo_review_gate`**
- ❌ **Do NOT automate any fleet-state transition** — not on a timer, a git event, or inactivity
- ❌ **Do NOT build the adapter surface** (E38.2, in parallel), a scheduler, a gate queue, a thin
  surface, or any completion judgment (M39/M40)
- ❌ **Do NOT assess `local-agent-runner`, measure milestone-context, or run the local/paid
  comparison** — E38.4/E38.5/E38.6, all behind E38.2's gate
- ❌ **Do NOT renumber, rename, or move any artifact.** *The record names the disposition; the identifier
  names the origin*
- ❌ **Do NOT reconcile the fleet's `framework_version` values.** Drivr at `v7.1.0` against five at
  `v7.0.0` and six absent **is the true state of the fleet** — record it, do not smooth it
- ❌ **Do NOT use a naive substring grep** anywhere a count matters
- ❌ **Do NOT state a bare inventory number.** Date it, anchor it, say what it counted
- ❌ **Do NOT let your own claim stand in for the evidence** (G2). Show the command and its output

### Read this before you interpret a red suite — P10-GH-10

`tests/test_artifact_router.py::test_daemon_extensions_error_branches` fails intermittently in
**full-suite runs only** — **3 failures in 10 full-suite runs (~30%)**, passing 5/5 in isolation,
**untouched by anything here.** **Re-run before concluding anything, and record both results.** Do not
fix, skip, or mark it.

**Your suite baseline is `393 passed / 0 failed / 0 skipped / 0 xfailed`**, plus whatever your
validator's own tests add.

> **⚠ The invocation is not obvious.** `python` is **not** on PATH; `.venv` has **no pytest**; bare
> `pytest` fails with **3 collection errors** (`ModuleNotFoundError: No module named 'lib'`). **Use
> `PYTHONPATH=. pytest -q`** or `python3 -m pytest -q`. **State the command alongside the number.**

---

## Reference Materials

### Primary sources

- **`governance/ai-project-yml-spec.md`** — **§3.1** (the schema block; five REQUIRED), **§3.2** (all
  five documented), **§4** (the 25 validation rules; **rule 3 is Finding 1**; unknown-key rules are 11,
  16, 22), **§8** (self-referential usage — **Finding 5's basis**), and the **`## Changelog` at line
  655**
- **`bin/ai-project-init`** — **`:109-114`** its internal validator, **`:255-268`**
  `create_ai_project_yml()`. **Finding 2 came from reading both. Read them yourself**
- **P10's phase closure declaration** — P10-GH-1 and P10-GH-5 as originally filed

### On versioning the document you amend

M37 made system-tier documents carry a version and a changelog. **`ai-project-yml-spec.md` already has a
`## Changelog`.** **Check the convention's actual scope before assuming it binds this file** — it was
written for `governance/systems/` — **and say what you found.** Getting this wrong in either direction
is a citation defect in a milestone whose lineage spent two milestones on citation integrity.

### The framings to carry

> **The fleet is a data structure, not a memory.**

> **The record names the disposition; the identifier names the origin.**

---

## Canonical Happy Path (Reminder)

1. ✅ Execution completed (all deliverables created)
2. ✅ Epic Delivery Notice produced (you create this)
3. ⏸️ Stage-2 review by the Milestone Chat — **clean deliveries accepted by silence** (SN-13 /
   PSG §11.6); a Review Decision is the exception path only
4. ⏸️ Merge authorization — an **in-chat act, no ceremonial artifact** (SN-19 / PSG §1A under §11.6);
   the harness enforces explicit human authorization regardless
5. ⏸️ PR merged to `milestone/M38`
6. ⏸️ Execution stops

**You are responsible for steps 1–2 only. Then you STOP and await instruction.**

---

## Epic Delivery Notice

**File:** `docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M38-E38.3__delivery-notice.md`

Use the committed template `governance/templates/delivery-notice.md` (reference, don't restate;
PSG §12 defines the content requirements).

**This Epic's Delivery Notice must additionally record:**

- **Every project directory classified**, with the enumeration method stated — **`panchew-io`,
  `fieldledger-assesment`, and `drivr` named explicitly**
- **The three state definitions, verbatim**, and confirmation that **nothing automatic** exists
- **The validator's full run against the real fleet — raw output, not a summary** — with **warnings and
  errors distinguished**
- **The demonstration that it fails on a genuinely invalid config**
- **Your invalid-config count, dated, anchored, scoped**, and **reconciled against the 3-of-6 figure**
- **P10-GH-1: folded in or parked**, with the reason — **never silence.** If folded in: the schema
  entry, **rule 3's count corrected**, the version bump, the changelog row, and the **Structural
  diagram**
- **`ai-project-system`'s own case, decided, with the reasoning**
- **The schema-drift class** — all four fields, with counts and spec-occurrence figures, and any
  recommendation **escalated rather than acted on**
- **Finding 2 reported** — that `init` produces §4-invalid configs, that the two validators enforce
  different contracts, **and Drivr's resulting config state whichever way it fell**
- **Explicit confirmation that NO enrolled project was modified** — including the four invalid configs,
  `bin/ai-project-init`, and Drivr's config
- **Explicit confirmation that nothing was renumbered, renamed, or moved**
- **Explicit confirmation that no scheduler, gate queue, thin surface, or completion judgment was built**
- **Things noticed and deliberately left, named individually**
- **The re-measured suite figure with the exact command**, and any P10-GH-10 occurrence **with both
  results**
- **Which repository and which method every reported measurement used** (`P11-GH-2`) — **and confirm the
  strict form was used wherever a count mattered**

---

## Exit Conditions

**You MUST stop execution when:**
- ✅ All deliverables are completed and committed
- ✅ Pull request is opened to `milestone/M38`
- ✅ Epic Delivery Notice is produced

**Then report:**
"Epic E38.3 execution complete. Epic Delivery Notice produced. Awaiting Milestone Chat review and merge
authorization."

**Also report if you STOPPED without completing** — a classification you could not make honestly, a §4
rule you could not enforce without deciding something above your scope, a schema amendment that turned
out to require blessing more than one field. **Say what happened and stop.**

---

## Begin Execution

You may now begin executing Epic E38.3.

Start by:
1. Checking your branch, then Prerequisite Verification and the model check
2. **Checking Drivr's config against §4** — the collision, known on day one
3. Creating branch `epic/P11-M38-E38.3` from `milestone/M38`
4. **Re-enumerating `~/soft-dev` and running the strict-form measurement yourself**

**Three framings to carry throughout.**

**You are building the thing that measures, and you will measure things that are broken.** Reporting
them accurately is the delivery. **Fixing them is a different epic that nobody has authorized** — and
the count of things you will want to fix is at least eight: four invalid configs, `init`'s two missing
fields, three undefined schema fields, and possibly Drivr itself.

**Your method is your subject.** A count is only as good as the pattern behind it, and this phase has
been bitten twice already — once by a comment line, once by bold markup inside a phrase. **Use the
strict form, and say that you did.**

**And rule 3 miscounts its own fields in the exact rule you are about to add a field to.** That is not a
coincidence; it is the metavariable constraint choosing the most expensive possible place to fire.
**Fix the count as part of the edit, and check your own changelog row for the same defect before you
commit it.**
</content>
