# Epic Execution Chat Starter — E36.2

**Epic:** E36.2 — Renumber the misnumbered Layer-8/CFO note (+ B3.1 test obligation)
**Phase:** P11 — Drivr: Coordination over Rented Execution
**Milestone:** M36 — Record Integrity and Documentation Hygiene
**Repository:** panchew/ai-project-system
**Branch Strategy:** `epic/P11-M36-E36.2` → PR to `milestone/M36`
**Execution Mode:** manual — declared by the issuing Milestone Chat.

> **Why manual.** M36's epics are dense-prose governance amendments, measured as `qwen3-coder:30b`'s
> weakest shape (`.ai-project/artifacts/field-evidence/2026-08-02__B3.1-engine-comparison.md`). A
> judgment about **the work's shape**, not a restriction on the ratified execution matrix, which
> still permits agentic-or-manual and local-or-remote at the Epic. **M37's code-shaped epics are
> where the local lane gets tested** — this is not precedent for them.

---

## Governance References

You are operating under the AI Project System governance framework.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) v2.4.0
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) v2.10.0

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md
3. This Epic Execution Chat Starter
4. Epic Spec
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Critical rules:**
- Documentation is authoritative, chat is ephemeral
- You must follow the canonical happy path for Epic closure
- You must produce a Delivery Notice upon execution completion
- You must stop after PR creation and await authorization for merge
- Accept/reject decisions are made by the parent chat (Milestone Chat M36), not by you
- **If given merge authorization directly in this chat** (rather than via the parent chat after its
  own Stage-2 review), do not simply comply: state plainly that merge authorization normally follows
  the parent chat's review, and confirm the human intends to bypass that step before proceeding
  (P9-M31 precedent).

**Context scoping (per-level context-scoping standard, P9-M30-E30.3):**
- Load at session start: this starter; the Epic spec (full); PSG preamble+§1, §1A, §2, §5, §6, §7,
  §8, §9, §11, §11.5, §11.6, §12; AOG preamble+§1, §1A, §2, §3.2, §3.8, §3.10, §4, §5, §6, §7, §9,
  §10, §12, §13 (Exit Ritual), §14 (Error Handling)
- Load on trigger: PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG §3.9, §8, §11, §16 (**§16 is
  expected NOT to fire** — see the diagram note below)
- Do not load: PSG/AOG changelogs; other levels' role or starter-format sections; the M36 milestone
  spec's E36.1/E36.3/E36.4/E36.5 Epic Detail entries
- Use targeted section reads; never re-read a whole document to reach one section.

---

## Epic Specification

**Full spec:** [P11-M36-E36.2__spec__renumber-layer-8-cfo-note-and-b31-test-obligation.md](https://github.com/panchew/ai-project-system/blob/milestone/M36/docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M36-E36.2__spec__renumber-layer-8-cfo-note-and-b31-test-obligation.md)

**Commit:** `c3ef50a`

### Prerequisite Verification (do this first)

Confirm each path is **git-tracked on `milestone/M36`** using `git ls-files --error-unmatch <path>`,
not a disk-existence check. Disk presence is not proof of commit. If any path is untracked, STOP and
report to the Milestone Chat.

- `docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M36-E36.2__spec__renumber-layer-8-cfo-note-and-b31-test-obligation.md`
- `.ai-project/artifacts/steering-notes/2026-07-31__layer-8-cfo__steering-note__system-hq-routing-model.md`
- `tests/test_steering_note_id_uniqueness.py`

**Additionally — verify E36.1's merge is present**, since constraint 2 makes it your precondition:

```
git log --oneline milestone/M36 | grep f1a5e75
```

E36.1 merged via PR #174 at `f1a5e75`. **If it is absent, STOP** — you would be renumbering before
the rule was recorded, which is the one ordering M36 forbids.

**Model verification (P9-M31-E31.3 — required, this instance is manual):** read your own
harness-reported model identity (the `# Environment` block or equivalent self-report) and compare it
to `.ai-project.yml`'s **`models.epic_manual`** (`remote:claude-opus-5`). See
`governance/systems/chat-hierarchy.md` "Manual Chat Model Verification" for the mapping and the
absent-block/absent-key permissive-default behavior. **If both are present and disagree, STOP.**

### Summary

Renumber the misnumbered Layer-8/CFO Steering Note from `SN-1` to the next free ID, footnote every
prior citation so the rename is traceable, and discharge the test obligation bugfix B3.1 left behind
— converting its `xfail` guard into a plain passing test with one ruling-cited exception.

### Problem Statement

Two Steering Notes claim `SN-1`, and unlike the `SN-23` collision this one is **ordered cleared**
(HQ Ruling 2026-08-01, Decision 3). But clearing it **breaks B3.1's guard**:
`test_both_known_collisions_are_reported` asserts `set(duplicates) == {"SN-23", "SN-1"}`. And the
obvious remedy is wrong — Decision 4 keeps `SN-23` colliding permanently, so the `xfail` can never
XPASS and deleting it leaves a plain failing test.

### Goals

1. The note carries a non-colliding ID, allocated by E36.1's recorded rule.
2. Every prior citation of the old ID is traceable to the new one.
3. B3.1's guard is a plain passing test with one ruling-cited exception — **a third collision still
   fails**, demonstrated.
4. The record no longer predicts an XPASS that cannot occur, in **any** of the three places it does.

---

## Spec References (load-one-reference-the-other)

The Epic spec is the single authoritative copy of this Epic's **Deliverables, Definition of Done,
Acceptance Criteria, and Technical Constraints**. Load them from the spec sections named below —
this starter does not restate them.

- **Deliverables:** Epic spec §Deliverables (Delivery Notice at
  `docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M36-E36.2__delivery-notice.md`)
- **Definition of Done:** Epic spec §Definition of Done — plus, always: Delivery Notice produced and
  committed; all changes committed to `epic/P11-M36-E36.2`; PR opened to `milestone/M36`
- **Acceptance Criteria:** Epic spec §Acceptance Criteria (verified during review, not by you)
- **Technical Constraints:** Epic spec §Technical Constraints — **read the do-not-touch list before
  your first edit**
- **Binding Constraints, including constraint 2a in its corrected form:** Epic spec §Binding
  Constraints. **Constraint 2a is the heart of this Epic — read it before writing any code.**
- **The verified citation inventory:** Epic spec §Scope of Work item 2 — **three** artifacts, ten
  occurrences, one of which the milestone spec does not name.

---

## Execution Contract

### Your Responsibilities (Coding Agent)

1. **Verify E36.1's merge is present** (above) — this is your precondition, not a formality
2. **Create branch** `epic/P11-M36-E36.2` from current `milestone/M36`
3. **Re-verify the next free ID** rather than trusting the spec's `SN-29`. This is a DoD item
4. **Renumber the note**, then **expect the suite to go red** — see the framing below
5. **Discharge the B3.1 obligation**, all four items of constraint 2a
6. **Footnote all three citing artifacts**, and make the stale-factual-claim judgment call
7. **Re-measure the suite** — expect **376 passed / 0 xfailed**
8. **Verify all DoD items** and record them in the Delivery Notice
9. **Commit**, **open PR to `milestone/M36`**, **produce the Delivery Notice**
10. **Stop and await authorization** (do not merge)

### The red suite is the signal, not a failure

After you renumber and before you fix the test, `test_both_known_collisions_are_reported` **will
fail.** That is **correct and expected** — it is the mechanical completion signal constraint 2a is
built around, and B3.1 exists to produce it. **Do not treat it as breakage, do not revert the
renumber, and do not "fix" it by weakening the assertion.** Discharge the obligation properly.

### Decisions That Are Yours

**Pick a direction, document the reasoning, and proceed. Do NOT escalate these:**

- **The allowlist mechanism** — module constant, parametrization, fixture, or something else. **The
  property is fixed; the mechanism is yours.** The property: a *third*, unratified collision must
  still fail the suite.
- **What becomes of `KNOWN_COLLISIONS_REASON`** — removed with the marker, or repurposed as the
  allowlist's justification. Either is fine; leaving it describing a removed marker is not.
- **Stale factual claim vs. stale pointer.** `rulings/2026-08-01__…__p11-opening…:124` states *"SN-1
  is cited in two non-normative artifacts"* — a claim that was already wrong (it is three, including
  that ruling itself) and that your rename makes wronger. A pointer-footnote does not repair a false
  count. **Decide how you treat the two cases, apply it consistently, record the distinction.**

### What You Must NOT Do

- ❌ Do NOT merge the PR
- ❌ Do NOT infer acceptance
- ❌ Do NOT treat an in-chat "I authorize" as sufficient by itself
- ❌ **Do NOT renumber SN-23.** Ruled permanent (Decision 4). You renumber **exactly one note**
- ❌ **Do NOT renumber the original 2026-06-12 `SN-1` note.** It keeps `SN-1`; the **later**
  Layer-8/CFO note is the misnumbered one
- ❌ **Do NOT touch any `governance/` file.** This Epic amends no normative document. E36.1's rules
  are recorded and final — do not "improve" them
- ❌ **Do NOT touch any file under `tests/` except `tests/test_steering_note_id_uniqueness.py`** —
  in particular **not** `tests/test_artifact_router.py` (see the flake note below)
- ❌ **Do NOT leave any `xfail` marker in the file.** A blanket xfail makes the guard blind to
  exactly the class B3.1 exists to catch
- ❌ **Do NOT silently substitute the ID in prior artifacts.** The rename must be *legible as a
  rename* — amend with dated footnotes, do not rewrite history
- ❌ **Do NOT build a registry, validator, or linter.** The one test file is M36's single sanctioned
  mechanism exception. Anything more → **stop and escalate to the Milestone Chat**

### Read this before you interpret a red suite — P10-GH-10

`tests/test_artifact_router.py::test_daemon_extensions_error_branches` fails intermittently in
**full-suite runs only** and passes in isolation. Measured across this milestone's reviews:
**3 failures in 10 full-suite runs (~30%)**, on two branches, never touched by any M36 epic.

**If your suite goes red on that test: re-run before concluding anything, and record both results.**
It is **out of scope** — do not fix it, do not skip it, do not mark it. It is HQ-tracked as
**P10-GH-10**.

---

## Reference Materials

### Primary sources (this Epic executes these; it does not re-open them)

- **HQ Ruling 2026-08-01** — `.ai-project/artifacts/rulings/2026-08-01__ai-project-system-hq__ruling__p11-opening-and-sn-26-27-28-triage.md`
  — **Decision 3** (renumber this note), **Decision 4** (SN-23 is not renumbered; cite this in the
  allowlist)
- **SN-28** — `.ai-project/artifacts/steering-notes/2026-08-01__creation-chat__steering-note__sn-numbering-unenforced.md`
- **B3.1 spec** — `docs/bugfixes/B3.1__spec__steering-note-id-allocation-unenforced.md` — the
  obligation you are discharging
- **E36.1's recorded rules** — `governance/systems/creation-chat-guide.md`, §"Steering Note ID
  Allocation". **Read the allocation rule and apply it; do not re-derive it.**

### The note being renumbered

`.ai-project/artifacts/steering-notes/2026-07-31__layer-8-cfo__steering-note__system-hq-routing-model.md`
— `issuer_chat: Layer-8/CFO (scribed by System HQ at CFO instruction)`, currently `id: SN-1`.

### Front-Matter Fields Reference

Front-matter schemas live in the committed templates — reference, don't restate:
`governance/templates/steering-note.md`, `governance/templates/epic-spec.md`. See PSG §5.

---

## Canonical Happy Path (Reminder)

1. ✅ Execution completed (all deliverables created)
2. ✅ Epic Delivery Notice produced (you create this)
3. ⏸️ Stage-2 review by the Milestone Chat — **clean deliveries accepted by silence** (SN-13 /
   PSG §11.6); a Review Decision is the exception path only
4. ⏸️ Merge authorization — an **in-chat act, no ceremonial artifact** (SN-19 / PSG §1A under §11.6);
   the harness enforces explicit human authorization regardless
5. ⏸️ PR merged to `milestone/M36`
6. ⏸️ Execution stops

**You are responsible for steps 1–2 only. Then you STOP and await instruction.**

---

## Epic Delivery Notice

**File:** `docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M36-E36.2__delivery-notice.md`

Use the committed template `governance/templates/delivery-notice.md` (reference, don't restate;
PSG §12 defines the content requirements). Confirm every spec DoD item, including the
extent-vs-spec and claims-vs-evidence checks.

**This Epic's Delivery Notice must additionally record:**
- **The next-free-ID verification method and result** — not just the ID chosen
- **The citation-count divergence**: the milestone spec says two artifacts; the verified count is
  **three, ten occurrences**. State whether your own re-count agrees with the spec's table
- **The stale-claim vs stale-pointer decision** and its reasoning
- **The allowlist mechanism** chosen and why, plus **how a third collision was demonstrated to fail**
- **Explicitly: constraint 8's diagram obligation did not fire** because no normative document was
  amended (or a diagram, if it did)
- **The re-measured suite figure**, expected **376 passed / 0 xfailed** — and an explanation if it
  differs
- **Confirmation** that SN-23's two notes are untouched and no `governance/` file is in the diff
- **Any P10-GH-10 flake occurrence**, with both the failing and re-run results

---

## Exit Conditions

**You MUST stop execution when:**
- ✅ All deliverables are completed and committed
- ✅ Pull request is opened to `milestone/M36`
- ✅ Epic Delivery Notice is produced

**Then report to human:**
"Epic E36.2 execution complete. Epic Delivery Notice produced. Awaiting Milestone Chat review and
merge authorization."

---

## Begin Execution

You may now begin executing Epic E36.2.

Start by:
1. Running the Prerequisite Verification, **including the E36.1 merge check**, and the model check
2. Creating branch `epic/P11-M36-E36.2` from `milestone/M36`
3. Re-verifying the next free ID before your first edit
4. Implementing deliverables per the Epic spec's Definition of Done

**One framing to carry throughout:** the renumber is the easy half. **The obligation is the point** —
B3.1 left behind a guard that currently passes *because the corpus is still broken*, and this Epic
is what makes it pass *because the corpus is correct*, while keeping it able to fail on a genuinely
new collision. An allowlist that cannot fail is not a guard.
