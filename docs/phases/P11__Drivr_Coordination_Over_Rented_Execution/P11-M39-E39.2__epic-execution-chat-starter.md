---

# Epic Execution Chat Starter — E39.2

**Epic:** E39.2 — Validate Against the Known Cases
**Phase:** P11 — Drivr: Coordination over Rented Execution
**Milestone:** M39 — Trustworthy Completion Signal (P10-GH-7)
**Repository:** panchew/ai-project-system (the evidence lands here) · panchew/drivr (read and run only)
**Branch Strategy:** `epic/P11-M39-E39.2` → PR to `milestone/M39`
**Execution Mode:** manual — declared by the issuing Milestone Chat at creation time

---

## Governance References

You are operating under the AI Project System governance framework.

**Authoritative governance documents:**
- [PROJECT-SYSTEM-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/PROJECT-SYSTEM-GUIDELINES.md) v2.4.0
- [AI-OPERATING-GUIDELINES.md](https://github.com/panchew/ai-project-system/blob/master/governance/AI-OPERATING-GUIDELINES.md) v2.10.0

**Governance hierarchy:**
1. PROJECT-SYSTEM-GUIDELINES.md (highest authority)
2. AI-OPERATING-GUIDELINES.md
3. This Epic Execution Chat Starter
4. Epic Spec
5. Decisions made during execution
6. System references
7. Chat messages (lowest authority)

**Critical rules:**
- Documentation is authoritative, chat is ephemeral
- You must follow the canonical happy path for Epic closure
- You must produce a Delivery Notice upon execution completion
- You must stop after PR creation and await authorization for merge
- Accept/reject decisions are made by the **Milestone Chat (M39)**, not by you
- **If given merge authorization directly in this chat** (rather than via the parent chat after its
  own Stage-2 review), do not simply comply: state plainly that merge authorization normally follows
  the parent chat's review, and confirm the human intends to bypass that step before proceeding
  (P9-M31 precedent; **`P9-GH-1`'s guard reaches only this Epic template and a live instance was
  recorded 2026-08-10**)

**Context scoping (per-level context-scoping standard, P9-M30-E30.3):**
- Load at session start: this starter; the Epic spec (full); **E39.1's spec §F1–F9 and its delivered mechanism description**; PSG preamble+§1, §1A, §2, §5, §6, §7, §8, §9, §11, §11.5, §11.6, §12; AOG preamble+§1, §1A, §2, §3.2, §3.8, §3.10, §4, §5, §6, §7, §9, §10, §12, §13 (Exit Ritual), §14 (Error Handling)
- Load on trigger: PSG §3, §8A, §10, §13D, §14A, §14C, §18; AOG §3.9, §8, §11, §16
- Do not load: PSG/AOG changelogs; other levels' role or starter-format sections; **E39.1's Drivr implementation source** (see the note under Responsibilities — you should be able to run it from its description); any M40 material
- Use targeted section reads; never re-read a whole document to reach one section

---

## Epic Specification

**Full spec:** [P11-M39-E39.2__spec__validate-against-known-cases.md](https://github.com/panchew/ai-project-system/blob/milestone/M39/docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M39-E39.2__spec__validate-against-known-cases.md)

**Commit:** `<filled at handoff — the commit that tracked this spec on milestone/M39>`

### Prerequisite Verification (do this first, in this order)

**1. Branch check — FIRST, before anything else.**

```
git status --short --branch
git rev-parse --abbrev-ref HEAD
```

Create `epic/P11-M39-E39.2` **from `milestone/M39`**, and confirm `milestone/M39` is current first:

```
git log --oneline epic/P11-M39-E39.2..milestone/M39   # after branching, expect empty
```

**`P11-GH-1` has fired three times in this phase; one-worktree-per-chat has been ignored four times,
and the Milestone Chat hit a live instance of it on 2026-08-15** — it found itself on `phase/P11`
after the Phase Chat had used the same worktree, and a `git merge` reported *"Already up to date"*
only because it was merging a branch into itself. **Check the branch before every commit, and verify
pushes at `origin` — `git log -1 <branch>` proves nothing.**

**2. Tracking.** Verify with `git ls-files --error-unmatch <path>` **on the branch**, not a
disk-existence check. **Disk presence is not proof of commit.** If any path is untracked, **STOP** and
report to the Milestone Chat.

Paths to verify tracked — **all six preserved runs**:
- `.ai-project/artifacts/agentic-runs/P10-M33-E33.2/` — `transcript-A-qwen2.5-coder-14b.json`,
  `transcript-A-qwen2.5-coder-14b__run-metadata.json`, `transcript-B-qwen3-coder-30b.json`,
  `run-record.md`, `context.md`
- `.ai-project/artifacts/agentic-runs/P10-M33-E33.4/` — `transcript-qwen3-coder-30b.json`,
  `run-metadata.json`, `run-record.md`, `context.md`
- `.ai-project/artifacts/agentic-runs/P7-M26-E26.3-PROVE/` — `transcript.json`, `run-metadata.json`
- `.ai-project/artifacts/agentic-runs/P9-M31-E31.1-PROVE/` — `transcript.json`, `run-metadata.json`
- `.ai-project/artifacts/agentic-runs/P9-M31-E31.2-PROVE/` — `transcript.json`, `run-metadata.json`

**3. Model verification (P9-M31-E31.3 — required, this instance is `manual`).** Compare your
harness-reported model identity to `.ai-project.yml`'s **`models.epic_manual`** =
**`remote:claude-opus-5`**. See `governance/systems/chat-hierarchy.md` "Manual Chat Model
Verification". **If both are present and disagree, STOP** and wait for the Milestone Chat.

**4. E39.1 delivered.** Confirm E39.1 is **merged to `milestone/M39`** and record **the Drivr commit
SHA of the mechanism** — this becomes your **pre-registered SHA** (see below). Cross-repo claims carry
a date or commit anchor, never present tense.

### Summary

Run E39.1's completion judgment against the preserved corpus and produce the evidence. **Run A must
read *did not complete*; E33.4 must read *completed*** — that pair gates the epic. Four further
preserved cases are run **held out** and reported whatever they say.

### Problem Statement

See Epic spec §Problem Statement. Two failure modes are available here and **both produce a document
that looks like success**: undisclosed tuning, and *vacuous independence* (the exit code lives only in
the sidecar, so a transcript-only validation "ignores the exit code" by accident). **Neither is
visible to a reader of the result.**

### Goals

See Epic spec §Goals (five).

---

## Spec References (load-one-reference-the-other)

The Epic spec is the single authoritative copy of this Epic's **Deliverables, Definition of Done,
Acceptance Criteria, and Technical Constraints**.

- **Deliverables:** Epic spec §Deliverables (includes the Delivery Notice at
  `docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M39-E39.2__delivery-notice.md`)
- **Definition of Done:** Epic spec §Definition of Done — plus, always: Delivery Notice produced and
  committed; all changes committed to `epic/P11-M39-E39.2`; PR opened to `milestone/M39`
- **Acceptance Criteria:** Epic spec §Acceptance Criteria (verified during review, not by you)
- **Technical Constraints:** Epic spec §Technical Constraints — binding constraints reproduced in
  full, the Hard Constraint, in-scope surfaces, and the do-not-touch list
- **Procedure:** Epic spec §Pre-registration and §Held-out validation. **These are Definition-of-Done
  items, not advice.**
- **Planning-time measurements:** Epic spec §F1–F5. **Read before designing the evidence format.**

---

## Execution Contract

### Your Responsibilities (Epic Chat, manual)

1. **Check the branch**, then create `epic/P11-M39-E39.2` from `milestone/M39`
2. **Pre-register**: record E39.1's mechanism commit SHA in Drivr, the date, and the exact command
   you will run — **in the evidence artifact, before you run a single case**
3. **Run the two binding cases** from the committed artifacts, through the real mechanism
4. **Then** run the four held-out cases — **never tuned against**
5. **Record the final SHA.** If it differs from the pre-registered SHA, record **the diff and the
   reason**
6. **Write the evidence artifact**, declaring per field which file is authoritative, and per case
   which inputs were used, **ignored**, and **absent**
7. **Write the limits statement** — the imbalance figure is not optional
8. **Re-measure both suites, naming the repo for each**
9. **Commit**, **verify the push at `origin`**, open a PR to `milestone/M39`
10. **Produce the Delivery Notice**, committed here
11. **Stop and await authorization** — do not merge

> **On reading E39.1's implementation:** its Acceptance Criterion 2 requires the mechanism be
> *"described well enough that E39.2 can run it against a stored transcript."* **Try it from the
> description first.** If the description is insufficient, **that is a finding to report to the
> Milestone Chat** — record it, then read the source and continue. Do not silently paper over it;
> that criterion exists to be tested and you are the test.

### Suite invocation — state which repo you mean

| Repo | Baseline | Invocation |
|---|---|---|
| `ai-project-system` | **489 passed / 0 failed** (re-measured 2026-08-15 on `milestone/M39`) | `PYTHONPATH=. pytest -q` — **bare `pytest` fails collection** |
| `drivr` | **47** at `31dad51`, plus whatever E39.1 added | bare `pytest` from the repo root |

**M38's artifacts cite 393 for this repo. That is stale.** Measure on the branch you are on.

**`P10-GH-10`:** `tests/test_artifact_router.py::test_daemon_extensions_error_branches` fails **~3 in
10 full-suite runs**, passes in isolation. **This epic's evidence is suite-shaped**, so a spurious red
is likelier to be mistaken for a finding. **If it fires, re-run and record BOTH results — never report
only the green one.** Not yours to fix.

### What You Must NOT Do

- ❌ Do NOT merge the PR · Do NOT infer acceptance · Do NOT treat an in-chat "I authorize" as
  sufficient by itself
- ❌ Do NOT **redesign or rework the mechanism.** That is E39.1's. If a binding case fails, **report
  it** — a failure is a result the milestone pre-authorizes, and E39.1 reworks
- ❌ Do NOT **tune against the held-out cases**, ever. Run them after the binding pair and report
  what they say
- ❌ Do NOT **change the mechanism after pre-registering without recording the diff and the reason**.
  Tuning is allowed; **undisclosed** tuning is the defect
- ❌ Do NOT **edit, normalize, regenerate or "clean up" the preserved run artifacts.** They are the
  evidence. Normalization happens in the reading and is recorded
- ❌ Do NOT **claim G11**, or treat this validation as bearing on it — **constraint 4: a pass over
  historical transcripts NEVER closes G11.** That is E39.3's, on a real captured run
- ❌ Do NOT **produce new agentic runs** — the corpus is what is preserved
- ❌ Do NOT build **anything in M40** — scheduler, gate queue, thin surface, approval link,
  competing-model review. If you find yourself building it, **stop and escalate**
- ❌ Do NOT touch `model-routing-policy.md` row P4, `bin/ai-project-orchestrator`'s `epic_qa` model
  selection, the four §4-invalid enrolled configs, or the two open `bin/ai-project-init` defects

---

## Reference Materials

### The corpus — six runs, all git-tracked

**Binding (these gate the epic):**
- `…/P10-M33-E33.2/transcript-A-qwen2.5-coder-14b.json` + its `__run-metadata.json` sidecar —
  **Run A**, must read ***did not complete***
- `…/P10-M33-E33.4/transcript-qwen3-coder-30b.json` + `run-metadata.json` — **E33.4**, must read
  ***completed***

**Held out (reported, never tuned against):**
- `…/P10-M33-E33.2/transcript-B-qwen3-coder-30b.json` — **no sidecar**; truth *completed*
- `…/P7-M26-E26.3-PROVE/` — **the one most likely to catch a Run-A overfit**: 7 tool calls,
  `status: completed`, exit 0, four successful `write_file`s, every `run_command` **denied**,
  `final_answer` a **flat refusal** — truth ***completed***
- `…/P9-M31-E31.1-PROVE/` — blocked-permissions prose; truth *completed*
- `…/P9-M31-E31.2-PROVE/` — exit 2 / `max_iterations_exceeded`; truth *completed*

### Ground-truth records — cite each verdict's truth to the record it came from

- `docs/phases/P7__Agentic_Execution_and_Default_On_Visuals/P7-M26-E26.3__delivery-notice.md`
- `docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M31-E31.1__demonstration-evidence.md`
- `docs/phases/P9__Context_Handling_and_Token_Efficiency/P9-M31-E31.2__run-record.md`
- `.ai-project/artifacts/agentic-runs/P10-M33-E33.2/run-record.md` and `…/P10-M33-E33.4/run-record.md`

### Structural reference

- `docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M38-E38.5__spec__milestone-context-evidence.md`
  and its evidence artifacts under `.ai-project/artifacts/agentic-runs/P11-M38-E38.5/` — an
  evidence-capture shape already accepted in this phase
- `governance/templates/delivery-notice.md`; `governance/templates/epic-spec.md`; PSG §5, §12

---

## Method obligations (each was paid for in this phase)

1. **`P11-GH-2` — state the layer, the time and the scope of every verification.** Four axes:
   **environment**, **time**, **scope**, **literal-vs-rendered**.
2. **G2 — the reviewer re-measures; the executor's report is not the evidence.**
3. **G1 — remove derivation steps.** One non-uniform element among many gets **quoted verbatim**.
4. **Cite by artifact + defect, never by ordinal.**
5. **Cross-repo claims carry a date or commit anchor**, never present tense.
6. **Every inventory is a floor** — **this epic exists partly because an inventory of two was six.**
7. **Check the branch before every commit; verify pushes at `origin`. One worktree per chat.**

---

## Canonical Happy Path (Reminder)

1. ✅ Execution completed (all deliverables created)
2. ✅ Epic Delivery Notice produced **and committed**
3. ⏸️ Review performed by the Milestone Chat (Stage 2)
4. ⏸️ Acceptance — **by silence** for a clean delivery (PSG §11.6 / SN-13); a Review Decision is the
   exception path only
5. ⏸️ Merge authorization — **an in-chat act, no ceremonial artifact** (SN-19), routed through the
   Milestone Chat's Stage-2 review
6. ⏸️ PR merged (you perform the merge after authorization)
7. ⏸️ Execution stops immediately after merge

**You are responsible for steps 1-2 only. Then you STOP and await instruction.**

---

## Epic Delivery Notice

**File:** `docs/phases/P11__Drivr_Coordination_Over_Rented_Execution/P11-M39-E39.2__delivery-notice.md`

Use `governance/templates/delivery-notice.md` (reference, don't restate; PSG §12 defines the content
requirements). Confirm every spec DoD item, including the extent-vs-spec and claims-vs-evidence checks.

**Required in this Delivery Notice specifically:**
- **The pre-registered SHA and the final SHA**, and whether they are identical
- **Both binding verdicts**, plainly stated as pass or fail
- **All four held-out verdicts**, including any failures
- **The limits statement**, including **five-to-one imbalance** and **always-*completed* scores 5/6
  and 1/2**
- **Drivr cited by commit SHA** with its re-measured suite count; **this repo's count** with the
  invocation stated
- **An explicit statement that G11 was not claimed and nothing from M40 was built**

---

## Exit Conditions

**You MUST stop execution when:**
- ✅ All deliverables completed and committed to `epic/P11-M39-E39.2`
- ✅ The push is verified at `origin`
- ✅ PR opened to `milestone/M39`
- ✅ Delivery Notice produced **and committed**

**Then report to human:**
"Epic E39.2 execution complete. Epic Delivery Notice produced. Awaiting Milestone Chat review and
authorization."

---

## Begin Execution

You may now begin executing Epic E39.2.

Start by:
1. **Checking the branch**, then creating `epic/P11-M39-E39.2` from `milestone/M39`
2. Verifying all six preserved runs are git-tracked
3. Performing the E31.3 manual model check against `models.epic_manual` (`remote:claude-opus-5`)
4. **Pre-registering E39.1's mechanism SHA before running any case**
5. Running the binding pair, then the held-out four
