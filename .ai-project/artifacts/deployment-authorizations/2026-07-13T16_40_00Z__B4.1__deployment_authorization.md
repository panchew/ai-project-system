---
type: deployment-authorization
epic: B4.1
project: ai-project-system
issued_by: <AWAITING CFO SIGNATURE>
date: 2026-07-13
environment: staging
decision: <AWAITING CFO DECISION>
---

# Deployment Authorization: B4.1 — Orchestrator embedded-test-suite stale default

## What Is Being Deployed

Commit `047ebfc` on `bugfix/B4.1` (branched from `milestone/M27`): rewrites 2 stale test
methods in `bin/ai-project-orchestrator`'s embedded `--test` suite that asserted the
pre-E27.1 opt-in default for `visual_artifacts.enabled`; adds 1 new test covering the
opt-out path. Follows the accepting Review Decision dated 2026-07-13
(`.ai-project/artifacts/review-decisions/2026-07-13T16_35_00Z__B4.1__review_decision.md`).
Both test suites verified green: `python3 bin/ai-project-orchestrator --test` (18/18, `OK`)
and `pytest tests/` (306 passed, 1 skipped).

**Target environment note:** this repo has no live production service — "deployment" here
means merging `bugfix/B4.1` into `milestone/M27`, the shared branch M27's Milestone Closure
Declaration depends on. Recorded as `staging` in the schema's `environment` field for that
reason; there is no `production` environment to distinguish it from in this project.

## Risk Assessment

- **What could go wrong:** essentially nothing — the change only rewrites 2 test assertions
  and adds 1 new test; it does not touch `resolve_visual_artifacts()` or any other
  production-path logic (already correct and covered by the passing `pytest` suite).
- **Blast radius:** `bin/ai-project-orchestrator`'s embedded self-test suite only, invoked
  manually via `--test` — not part of `pytest` or any CI workflow (none exists for this repo).
- **Mitigation:** the fix is a direct mirror of an already-correct, already-merged pattern
  (`tests/test_visual_artifacts_config.py`, from E27.1); both suites re-verified independently
  before this authorization was prepared.
- **Rollback:** revert commit `047ebfc` on `bugfix/B4.1` (or simply do not merge) — no state,
  data, or schema to unwind; a single-file, test-only change.

## Authorization Statement

**Awaiting the CFO's explicit decision.** As HQ Agent, I prepared this authorization per
`governance/templates/deployment-authorization.md` — I cannot self-authorize the merge.
Requesting the CFO record `authorized` or `rejected` above (and `issued_by: CFO`) to close
out B4.1's one remaining gate before the M27 Milestone Closure Declaration.

## Conditions

None proposed. Low severity; no post-mortem required (`docs/bugfixes/README.md` severity
table). If authorized, merge `bugfix/B4.1` → `milestone/M27`, issue the Delivery Notice with
the merge SHA, and close B4.1.
