---
artifact_type: delivery_notice
artifact_version: 1.0
timestamp: 2026-08-22T19:30:00Z
issuer_chat: Epic Execution Chat (P12-M41-E41.2)
issuer_role: Epic Agent
status: delivered
epic_id: P12-M41-E41.2
milestone_id: P12-M41
phase_id: P12
project_name: ai-project-system
merge_details:
  pr_number: 231
  pr_url: https://github.com/panchew/ai-project-system/pull/231
  merge_commit: <pending — this Epic Chat does not merge>
  merge_timestamp: <pending>
  merge_strategy: <pending — parent's call>
  target_branch: milestone/M41
duration:
  start_date: 2026-08-22
  end_date: 2026-08-22
  elapsed_days: 0
final_artifacts:
  - name: E41.2 Execution Record (D1–D5 + Structural diagram)
    path: docs/phases/P12__Completion_Fail_Closed_Defaults_and_the_Drivr_MVP/P12-M41-E41.2__record__successful-nothing-instrument-and-incumbent-baseline.md
    type: report
  - name: The objective check list and the ruled per-lane floor (D3)
    path: docs/phases/P12__Completion_Fail_Closed_Defaults_and_the_Drivr_MVP/P12-M41-E41.2__check-list-and-floor.md
    type: report
  - name: The instrument (D1)
    path: bin/successful-nothing-instrument
    type: code
  - name: The declared replay set (D2)
    path: docs/phases/P12__Completion_Fail_Closed_Defaults_and_the_Drivr_MVP/P12-M41-E41.2__replay-set.json
    type: data
  - name: Replay validation, five cases, full machine record (D2)
    path: .ai-project/artifacts/agentic-runs/P12-M41-E41.2/replay-validation.json
    type: data
  - name: The two measurement tasks, ground truth and harness (D4 inputs)
    path: .ai-project/artifacts/measurement-tasks/P12-M41/
    type: data
  - name: Escalation Notice — bimodal baseline, host-not-sandbox (D6)
    path: .ai-project/artifacts/escalation-notices/2026-08-22T00_00_00Z__P12-M41-E41.2__escalation_notice.md
    type: report
completion_notice_timestamp: n/a — PSG §11.6 does not require a Completion Notice on the happy path, and the M41 suspension below does not reinstate one
review_decision_timestamp: n/a — pending Milestone Chat Stage-2. ACCEPT-BY-SILENCE IS SUSPENDED FOR M41 (HQ ruling, master ad6e3f1, 2026-08-21): acceptance must be EXPLICIT and COMMITTED; silence accepts nothing
---

# Delivery Notice: P12-M41-E41.2 — The Successful-Nothing Instrument and the Incumbent's Baseline

> ## ⚠ CORRECTION — 2026-08-22, made before Stage-2 and by this chat
>
> **This notice as first committed said a clean delivery is accepted by silence under PSG §11.6.
> That is FALSE for M41.**
>
> **Accept-by-silence is SUSPENDED for M41** — HQ ruling
> `.ai-project/artifacts/rulings/2026-08-21__ai-project-system-hq__ruling__m41-accept-by-silence-suspended.md`,
> merged to `master` at **`ad6e3f1`** on 2026-08-21, while more than one session may hold M41's
> Stage-2 authority. **An epic delivery under M41 is accepted only by an explicit, committed
> acceptance naming the delivery. Silence is not acceptance and must not be read as one.** The
> suspension lapses automatically when the CFO records which session holds M41's Stage-2.
>
> **How the error reached this notice, stated because the mechanism is this phase's own subject.**
> **My starter is stale on this point.** It was issued **2026-08-20**, the ruling landed **2026-08-21**,
> and the starter's line 44 still says a clean delivery is *"accepted by silence — no artifact on the
> happy path."* I restated it without checking whether it was still current.
>
> **That is not the starter's fault; it is mine.** `ad6e3f1` **is an ancestor of my own branch point
> `575b0fd`** — verified with `git merge-base --is-ancestor` — and the ruling file was **tracked on
> this branch the whole time.** `P11-GH-1`'s downward hazard says an amendment must reach a branch
> already in flight; here the amendment had **already arrived**, and I read the starter instead of
> the branch. **The backstop the starter names — check whether the governing material moved after
> your branch point — I ran against the E41.2 *spec* and not against the *rules the starter
> restates*.**
>
> **Nothing measured in this epic changes.** No verdict, count, run or artifact depends on the
> acceptance mechanism. **What changes is what this chat may infer from quiet: nothing.** I will not
> read elapsed time as acceptance, and I remain stopped at the gate.


> **On `merge_details`.** `pr_number` and `pr_url` are filled; the merge fields are unfillable at
> delivery time because this Epic Chat **stops before merge** by design. Known defect **`P10-GH-4`**,
> cited by artifact and defect rather than by ordinal. **Marked pending rather than invented.**

---

## The four easiest items to skip, discharged first

The starter names them; they are answered here before anything else.

### 1. **The two negative controls pass, with their counts shown**

| | Rounds | Files changed | Claims | Verdict |
|---|---:|---:|---|---|
| **E33.2 Run B** | **10** | **2** — `local_agent_runner/__init__.py`, `tests/test_public_api.py` | 0 asserted (vacuous, recorded) | **PASS** |
| **E33.4** | **10** | **3** | **4 / 4 resolved** | **PASS** |

*"Run B passed"* is not a result. The counts are.

### 2. **The check-list commit hash, proving the bar predates the runs**

**`c9c2fb43fb241eda82a7256acb438576a85b377b`** (`c9c2fb4`) — and it is **the first commit on this
branch**. The Hard Constraint is discharged by the branch's history, not by assertion.

### 3. **The advertised tool set is recorded with every run**

- **dev runs:** `read_file, write_file, edit_file, list_dir, run_command, git` — six tools, committed
  with the task. **So DEV RUN 2's zero rounds is a finding, not an artefact of an empty tool set.**
- **QA runs:** `read_file, list_dir`, sha256 `83ff08e7b3492fd38aee92bbe13fe2118535d1d9fa8f120372b362c64cfb69e7`,
  **captured at run time** by `bin/run-qa-agent` rather than read back afterwards.
- **replay cases:** recorded per case, and where a historical run's metadata captured no tool set the
  record says **`NOT RECOVERABLE`** rather than substituting the file's current content. E33.2 Run B
  is that case (S3).

### 4. **`epic_qa` was judged against the RULED per-lane floor, and Criterion 2 is intact**

**The version applied: `per-lane-ruled-2026-08-20`** — `rounds > 0` AND `claims resolve`, with
`files changed` **recorded but not scored**. The instrument stamps `floor_version` into **every**
verdict it emits, so the statement is carried by the artifacts and not only by this notice.

**Criterion 2 intact:** no run reached its floor through a mutating QA tool set.
`.ai-project/agents/qa-tools.json` was not modified by this epic; both QA runs record their
`tools_enabled` and the tool-set sha above.

---

## What was delivered

### D1 — the instrument

`bin/successful-nothing-instrument` **v1.0.3**. Three counters and a verdict derived from those and
nothing else: **tool rounds executed** (not intended), **files changed** (worktree for live runs,
transcript-derived and **declared a lower bound** for replays), **claims resolving** (three narrow
syntactic classes with five stated limits). **Exit code, status, tokens and duration are recorded and
never scored.** It is a measuring device, not a gate — **M46 formalizes the gate**, and there is no
CI wiring, no enforcement and no blocking hook here.

### D2 — the replay, in both directions

**Declared 5, reported 5. Three flagged, two passed.** The ERROR path was proven on a falsification
control rather than assumed, and the S3 counterfactual was measured: **a metadata-requiring harness
reports four cases and silently drops E33.2 Run B, the primary negative control.**

### D3 — the checks and the floor

Committed at `c9c2fb4`, before anything ran. **Not loosened afterwards.** The two corrections below
were to the instrument's *implementation*; the committed checks and floor are unchanged.

### D4 — two incumbent baselines, measured separately

`qwen3-coder:30b`, two runs per lane, **all four reported, none discarded, no best-of-N**, no
mechanical failures to exclude.

| Run | Lane | Floor | Rounds | Files | Claims | Directional check | Exit | Status | Tokens | Duration |
|---|---|---|---:|---:|---|---|---:|---|---:|---:|
| DEV 1 | `epic_dev` | **PASS** | 10 | 1 | 0/0 vacuous | **9 / 20 tests** | 2 | `max_iterations_exceeded` | 2888 | 121.1 s |
| DEV 2 | `epic_dev` | **FAIL** | 0 | 0 | 0/0 vacuous | **0 / 20 tests** | **0** | `completed` | 55 | **4.2 s** |
| QA 1 | `epic_qa` | **PASS** | 1 | 0 *(not scored)* | 0/0 vacuous | **3/5 catches, 0 false alarms, 0 unclear** | 0 | `completed` | 355 | 8.9 s |
| QA 2 | `epic_qa` | **PASS** | 1 | 0 *(not scored)* | 0/0 vacuous | **3/5 catches, 0 false alarms, 0 unclear** | 0 | `completed` | 355 | 7.3 s |

### D5 — the dispatch path

**An engine RESOLVES** — four dispatches, `local-agent-runner` `4ec1e8f` through the committed shim,
endpoint `runner-default`, **host**. **Docker present (29.7.2)** and `:392-397` **could not have
fired**, because `bin/ai-project-orchestrator` was never invoked. **The sandbox question is stated as
UNTESTED, not answered.**

### D6 — Escalation Notice, two items, neither blocking

---

## The headline finding

**DEV RUN 2 is a live successful-nothing, captured 2026-08-22, on the incumbent, on the lane M41 is
qualifying.** Exit **0**, `status: completed`, 4.2 seconds, 55 tokens, and `durations/parse.py`
byte-identical to the committed stub. It emitted its `read_file` call in `<function=…>` syntax
instead of the runner's protocol; the parser saw no protocol call and treated the message as a final
answer.

**That is E33.2 Run A's mechanism on a different model, and the instrument flagged an instance it was
not built against.** It is stronger evidence than the replay, because the replay's five outcomes were
known before the checks were written and this one did not exist.

**And S2 reproduced live and unprompted:** of the two dev runs, **the clean exit belongs to the one
that did nothing**.

---

## ⚠ The instrument was wrong twice. Both corrections are recorded in full.

**A tool that was correct on the first try is a claim; a tool with recorded corrections is evidence.**

1. **A pattern trusted without being falsified.** `is denied` matched the **content** of the runner's
   own permissions module, so a `read_file` on it scored as denied. **Found by a unit test. It
   changed no verdict** — which is exactly why the five-case replay could never have surfaced it.
2. **Wrong in the DANGEROUS direction, and only a live run found it.** C-C2 read the bare `` `/` ``
   — inside the run's entirely correct finding that `healthcheck.path` does not start with `/` — as
   a cited repository path, and returned **FAIL on a grounded, one-round, read-only run**. That is
   the mirror of `return FAIL` and **M40's F5 one level down: the run scored worse for stating a
   correct finding.** Fixed; **re-measured with all five replay verdicts and every count unchanged**;
   two regression tests, the second of which asserts the fix did not blind C-C2, because narrowing a
   check until it extracts nothing is not a fix.

**And two measurement contaminants, both found by running the harness rather than reading it, both
the epic's own subject reproduced inside its own apparatus:** `__pycache__` gave a do-nothing run
**three** changed files, and the dev adapter's own artifact directory gave a do-nothing run **one**.
The second had removed C-B's discriminating power on the dev lane entirely.

---

## Escalations raised — the positive statement

**Escalations WERE required. Two items, one notice**
(`.ai-project/artifacts/escalation-notices/2026-08-22T00_00_00Z__P12-M41-E41.2__escalation_notice.md`):

1. **The `epic_dev` baseline is BIMODAL on identical inputs** — `{PASS 9/20, FAIL 0/20}` — so
   E41.3's *"no worse on every objective check"* has no single number to attach to. Four aggregation
   rules give four different answers, and **choosing one after seeing the data is the failure the
   bar-committed-first rule exists to prevent.** The bar's shape is settled above this chat.
2. **These are HOST baselines, not sandbox baselines.** The fallback executes unsandboxed on the
   host, and so did these runs — they are **equivalent to** it rather than protected from it.
   **E41.3 must dispatch identically**, and whether qualification survives M42 is a milestone
   question raised now rather than discovered later.

**Not escalated, and each is a positive statement:** S5 is ruled and was applied with its version
stated; an engine resolved; Docker stayed available; no committed transcript was missing or
untracked; no check was weakened to make a control pass; Criterion 2 is intact; **no new `GH-` ID was
allocated.**

---

## Suite

| Repo | Before | After | Delta |
|---|---|---|---|
| `ai-project-system` | **549 passed / 0 failed** | **570 passed / 0 failed** | **+21** |
| `drivr` | **not touched by this epic** | — | — |

**All 21 are in `tests/test_successful_nothing_instrument.py`**, and **collection was confirmed
directly** — `pytest --collect-only` on the file reports 21, matching the total's delta exactly.
M38's warning applies: *"is the baseline still N?"* is not a guard on its own.

Invocation: `PYTHONPATH=. pytest -q`. **Bare `pytest` still fails collection.**

---

## Definition of Done — every item

- [x] The instrument records tool rounds, files changed and claims-resolution, and derives its
      verdict from those and nothing else
- [x] All five replay cases ran: Run A, E39.3, E39.3-RUN2 **FAIL**; Run B and E33.4 **PASS**; raw
      counts shown for all five
- [x] No case was unloadable; the ERROR path was proven on a control anyway, and the S3
      counterfactual was measured
- [x] The checks and floor were committed **before** the first measurement run — `c9c2fb4`, the
      branch's first commit — and the hash is cited above
- [x] `epic_qa` judged against the **RULED per-lane floor**, with the version applied stated and
      stamped into every verdict
- [x] **Criterion 2 intact**, and the record says so with the tool set and its sha
- [x] Two separate incumbent baselines, two runs each, **all four reported, none discarded**
- [x] The task definitions are committed with their inputs and **each carries a directional check
      with objective ground truth** — 20 pre-written tests for `epic_dev`; five machine-verified
      planted defects scored as catches and false alarms for `epic_qa`
- [x] The advertised tool set recorded with every run
- [x] The dispatch path stated; **engine-resolves answered YES (host)**; **Docker-present answered,
      with the stronger and more accurate statement that the fallback's code path never ran**
- [x] Suite green at **570**, delta **+21** attributed to named tests, collection confirmed
- [x] Structural diagram (Mermaid, in-repo, no ComfyUI) — record §7
- [x] Delivery Notice committed; work committed to `epic/P12-M41-E41.2`; **push verified at
      `origin`** by `git ls-remote`, not a local ref read; **PR #231 opened to `milestone/M41`**

---

## Non-Goals honoured

M46's gate not built. No candidate measured. **No model value, policy row or `DEFAULT_MODELS` entry
changed.** The lane not repaired — the fallback, the sandbox and `discover_runner()` are recorded as
observed and left to M42. **The floor was not re-tuned after seeing a result.** S5 not re-opened.
E39.1's decision not taken back — the QA verdict is data about the model, not a decision about
anything. **The committed transcripts were replayed and never edited.**

---

## Merge authorization

**This Epic Chat does not merge.** Accept/reject is the M41 Milestone Chat's.

**Acceptance must be EXPLICIT.** `PSG §11.6` default-accept is **suspended for M41** (HQ ruling,
`master` `ad6e3f1`, 2026-08-21) while more than one session may hold M41's Stage-2 authority.
**Silence accepts nothing here**, and this chat will not treat quiet or elapsed time as assent —
which is the ruling's own stated reason: silence cannot distinguish *"reviewed and clean"* from
*"never looked"* from *"the session died"* (SN-31 Decision 3).

**If merge authorization is given directly in this chat rather than through the parent's Stage-2
review, this chat will say so and confirm the bypass is intended before acting** — mode is what may
run, not what may be authorized. **M43 moves the merge to the parent (SN-31 Decision 4); until M43
delivers, the current rule is what operates here, and it has not been pre-applied.**
