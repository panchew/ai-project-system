---
artifact_type: delivery_notice
artifact_version: 1.0
timestamp: 2026-09-01T20:30:00Z
issuer_chat: Epic Execution Chat (P12-M41-E41.4)
issuer_role: Epic Agent
status: delivered
epic_id: P12-M41-E41.4
milestone_id: P12-M41
phase_id: P12
project_name: ai-project-system
merge_details:
  pr_number: 239
  pr_url: https://github.com/panchew/ai-project-system/pull/239
  merge_commit: <pending — this Epic Chat does not merge>
  merge_timestamp: <pending>
  merge_strategy: <pending — parent's call>
  target_branch: milestone/M41
duration:
  start_date: 2026-09-01
  end_date: 2026-09-01
  elapsed_days: 0
final_artifacts:
  - name: D1 — the transport
    path: .ai-project/artifacts/reference/local-review-backtest/e41-4-runs/transport.py
    type: code
  - name: D2 — 30 blinded run records (scoring material)
    path: .ai-project/artifacts/reference/local-review-backtest/e41-4-runs/blinded/
    type: data
  - name: D2 — 30 verbatim transport records with U8 credential visibility
    path: .ai-project/artifacts/reference/local-review-backtest/e41-4-runs/raw/
    type: data
  - name: D3 — blinded scores (committed BEFORE the mapping)
    path: .ai-project/artifacts/reference/local-review-backtest/e41-4-runs/scores.md
    type: report
  - name: D3 — opaque-ID<->model mapping (committed AFTER the scores)
    path: .ai-project/artifacts/reference/local-review-backtest/e41-4-runs/mapping.tsv
    type: data
  - name: D4 — both bars applied + four per-row recommendations
    path: .ai-project/artifacts/reference/local-review-backtest/e41-4-runs/d4-bars-and-recommendations.md
    type: report
  - name: D5 — Escalation Notice (two rows, neutral results)
    path: .ai-project/artifacts/escalation-notices/2026-09-01T00_00_00Z__P12-M41-E41.4__escalation_notice.md
    type: report
  - name: D7 — U7 fidelity probe (receive + faithful report, both rows)
    path: .ai-project/artifacts/reference/local-review-backtest/e41-4-runs/fidelity/
    type: data
  - name: Execution Record + Structural diagram
    path: docs/phases/P12__Completion_Fail_Closed_Defaults_and_the_Drivr_MVP/P12-M41-E41.4__record__verification-target-back-test.md
    type: report
completion_notice_timestamp: n/a — PSG §11.6 does not require a Completion Notice on the happy path
review_decision_timestamp: n/a — pending Milestone Chat Stage-2. ACCEPT-BY-SILENCE IS SUSPENDED FOR M41 (HQ ruling, master ad6e3f1, 2026-08-21): acceptance must be EXPLICIT and COMMITTED; silence accepts nothing
---

# Delivery Notice: P12-M41-E41.4 — The Verification-Target Back-Test

## What was delivered

**The transport E35.5 never had, thirty real remote-model runs on the frozen instrument,
blinded scoring with the scores committed before the mapping, and both bars applied
separately.**

| Deliverable | Result |
|---|---|
| **D1** transport | Built; prompt byte-for-byte after `<!-- PROMPT-BEGIN -->` (verified for all 5 packets); audit header never sent; verbatim capture + stop reason; U8 credential visibility per dispatch. Blinding re-verified: **ALL CLEAN**. |
| **D2** 30 runs | All captured: `claude-opus-5` (R01–R10), `gpt-5.6-sol` (R11–R20), `deepseek-v4-pro` (R21–R30). **Every run finish=stop, exit=0 — no truncation, no mechanical failure, no refusal.** |
| **D3** blinded scoring | 30/30 CATCH, 0 MISS, 0 SPLIT, 0 FALSE ALARM (incl. 0 on the packet-3 inverse control). Scores committed blind (`8fbc30a`), mapping published after (`91b3431`) — **the commit order is the control**. |
| **D4** both bars | **Absolute:** PASS for all three models (5/5, 0 FA). **Relative:** neither candidate strictly better than the baseline — identical on every check. Bars applied separately, labelled. |
| **D5** escalation | Two rows (`phase`, `milestone`) escalated to the CFO as neutral results. Positive statement: no other escalation required. |
| **D7** fidelity | Well-formed unguessable path round-trips EXACTLY on both `phase` and `milestone` routes; receive + faithful report CONFIRMED. |
| **D8** credentials | Recorded per run; zero values read; 0 leaks; E41.1's baseline not contradicted. |

## The measured result, in one paragraph

All three models — the never-measured `claude-opus-5` incumbent and the two remote
candidates `gpt-5.6-sol` and `deepseek-v4-pro` — scored **identically** on the frozen
E35.5 instrument: **30/30 CATCH, zero false alarms, zero splits**, packet 3 (the
inverse control) accepted by every run. All three **PASS the absolute pre-registered
bar**. On the **relative** bar, neither candidate is *strictly better* than the baseline
on any objective check — they are identical on all of them — so **neither row moves on
this evidence**. `qwen3.6:27b` is cited, not re-run. The neutrality is **escalated to
the CFO as a result, not as a failure.**

**Stated within `judgment.md`'s six cautions:** a 30/30 with zero variance means the
instrument did not *discriminate* among the three models, not that the models are
equivalent. The result measures recognition of known ground truth on curated,
single-turn, tool-free packets. It is reported as measured, and it is not over-read as
proving any model's live-review fitness, nor as supporting or contradicting the CFO's
line-up decision.

## Blinding control (U1), demonstrable from history

`8fbc30a` (blinded scores, no model identity) precedes `91b3431` (mapping + raw
records). Verified in the commit order. The scorer could not have known which model
produced which output at scoring time.

## Per-row recommendations (summary)

| Row | Candidate | Absolute bar | Relative bar | Recommendation |
|-----|-----------|--------------|--------------|----------------|
| `creation` | none (fable-5 cancelled) | n/a | n/a | **holds** at `claude-opus-5` |
| `phase` | `gpt-5.6-sol` | PASS | NOT cleared (identical) | **holds on this evidence; escalated (neutral)** |
| `milestone` | `deepseek-v4-pro` (pro, per v1.4.0) | PASS | NOT cleared (identical) | **holds on this evidence; escalated (neutral)** |
| `epic_manual` | — (waived by CFO) | n/a | n/a | lands on R6 surface confirmation (E41.1 §9.3) |

## Hard-constraint and binding-constraint compliance

- **Frozen instrument reused untouched** — packets, rubric, ground truth, blinding
  record, E35.5's ten runs: **not modified**. No packet change was required.
- **Two runs per packet per model, both scored, no best-of-N** — yes; every run scored.
- **Truncation via vendor stop reason** — recorded every run; none fired.
- **`qwen3.6:27b` cited, not re-run** — yes; the citation asymmetry is recorded beside
  it in the execution record.
- **No model substituted** — nothing was substituted for an unreachable model; all
  three measured targets answered.
- **Credentials never committed or printed** — presence recorded, values never read.
- **`P11-GH-2` layer/time/scope** — stated on every verification and in each artifact.

## Suite

`PYTHONPATH=. pytest -q` at the branch baseline (== `origin/milestone/M41`):
**569 passed / 0 failed** (34–36s), total, delta **0** — this epic adds no code under
test. Transport code lives under `.ai-project/artifacts/reference/` and is not part of
the suite.

## Merge authorization

**This Epic Chat does not merge.** Per the starter: merge authorization normally follows
the parent Milestone Chat's Stage-2 review. **Accept-by-silence is SUSPENDED for M41**
(HQ ruling, master `ad6e3f1`, 2026-08-21) — acceptance must be **explicit and
committed**. If merge authorization is given directly in this chat rather than through
the parent's Stage-2, this chat will state the bypass and confirm before acting.

## Completion

Work is committed to `epic/P12-M41-E41.4` (7 commits), pushed to `origin`, PR opened to
`milestone/M41`. This is the **Epic Delivery Notice**.

*"Epic E41.4 execution complete. Epic Delivery Notice produced. Awaiting Milestone Chat review and authorization."*