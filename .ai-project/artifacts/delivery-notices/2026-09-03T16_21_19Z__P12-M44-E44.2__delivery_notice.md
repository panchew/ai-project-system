---
artifact_type: delivery_notice
artifact_version: 1.0
timestamp: 2026-09-03T16:21:19Z
issuer_chat: Epic Execution Chat (P12-M44-E44.2)
issuer_role: Epic Agent
status: delivered
epic_id: P12-M44-E44.2
milestone_id: P12-M44
phase_id: P12
project_name: ai-project-system
merge_details:
  pr_number: 256
  pr_url: https://github.com/panchew/ai-project-system/pull/256
  merge_commit: <pending — this Epic Chat does not merge>
  merge_timestamp: <pending>
  merge_strategy: <pending — parent's call>
  target_branch: milestone/M44
duration:
  start_date: 2026-09-03
  end_date: 2026-09-03
  elapsed_days: 1
final_artifacts:
  - name: D1 — the decided-vs-evidenced convention, both faces, defined once in model-routing-policy.md
    path: .ai-project/artifacts/reference/token-measurement/model-routing-policy.md
    type: implementation
  - name: D2 — row P4's why cell rewritten to an allowance-decided account (E41.5's handoff discharged)
    path: .ai-project/artifacts/reference/token-measurement/model-routing-policy.md
    type: implementation
  - name: D3 — the guard gap disposed by test_row_p4_tier_cell_provenance_agrees_with_mapping_row (falsified)
    path: tests/test_model_config.py
    type: implementation
  - name: D4 — the distinguishability-without-inference statement
    path: .ai-project/artifacts/reference/token-measurement/model-routing-policy.md
    type: implementation
  - name: Execution record, including the Structural diagram (Mermaid, in-repo, no ComfyUI)
    path: docs/phases/P12__Completion_Fail_Closed_Defaults_and_the_Drivr_MVP/P12-M44-E44.2__record__decided-vs-evidenced-convention.md
    type: record
  - name: this Epic Delivery Notice
    path: .ai-project/artifacts/delivery-notices/2026-09-03T16_21_19Z__P12-M44-E44.2__delivery_notice.md
    type: report
---

# Epic Delivery Notice — E44.2 The Decided-vs-Evidenced Convention

## Summary

E44.2 is delivered. **D1:** the convention — how a row records *decided*, *configured*, and
*evidenced* so a reader can tell them apart from the record alone — is defined once, both faces, in
`model-routing-policy.md`'s new `## The decided-vs-evidenced convention (P12-M44-E44.2)` section,
the file the routing policy owns and cites. **D2:** row P4's `why` cell is rewritten to describe an
**allowance-decided** value, discharging E41.5's handoff verbatim; E41.4's neutral result is cited,
not restated; **no CFO-enumerated value moved.** **D3:** the guard gap is disposed by a new guard —
`test_row_p4_tier_cell_provenance_agrees_with_mapping_row` — which fails if the tier cell and its
mapping row describe different value-provenance; it was **falsified** (drift reintroduced → failed →
restored). **D4:** the distinguishability-without-inference statement is stated.

## Deliverables

- [x] **D1** — The convention, defined once, both faces, location stated (`model-routing-policy.md`)
- [x] **D2** — Row P4's language rewritten to describe an allowance-decided value; E41.5's note
      reads as consistent with it
- [x] **D3** — The guard gap disposed: rewritten language under a guard (falsified, then restored)
- [x] **D4** — The distinguishability-without-inference statement
- [x] Structural diagram (Mermaid, in-repo, no ComfyUI)
- [x] **Epic Delivery Notice**

## The row P4 rewrite (D2), shown

Row P4's `why` cell now reads: *"…**The tier stays paid frontier** — evaluated against local
inference 2026-07-31 and not moved (see the P4 note below). **The value is set by CFO allowance
decision (SN-41), not by measurement** (mapping row); the one measurement of the configured engine
(E41.4, 2026-09-01) is neutral toward the decision (see the P4 note below)."*

The **tier** and the **value** are now told apart in the cell itself: the tier stays paid frontier
by its original evidential reasoning, and the value is allowance-decided by SN-41, with E41.4
cited as the one measurement that neither supports nor contradicts it. **No CFO-enumerated value
moved** — the `default` cell, the mapping table's `milestone` cell, and `.ai-project.yml` are
untouched; only row P4's language changed.

## The guard (D3), and its falsification

`test_policy_mapping_agrees_with_yml_block` parses the mapping table only; row P4's `why` cell sat
outside its parse. The guard `test_row_p4_tier_cell_provenance_agrees_with_mapping_row` parses both
surfaces — the mapping table's `milestone` row and the tier cell — and fails if they describe
different value-provenance. **Fence-aware:** it reads only table rows, never the note's quoted
prose. **Falsified:** the drift was reintroduced (the `why` cell reverted to evidential derivation)
and the guard **failed**; the rewrite was restored and the guard **passes**. Both states recorded in
the execution record against this branch.

## Suite baseline

`PYTHONPATH=. pytest -q` → **741 passed / 0 failed**, re-measured on this branch 2026-09-03
(baseline 740 / 0 at the branch point `origin/milestone/M44` `57b8293`; **+1** = the D3 guard).
No skips introduced; no guard weakened.

## Method obligations discharged

- **`P11-GH-2`** — every claim in the record states its layer (repo file / test / this branch),
  time (2026-09-03), scope (this repo, this epic), and the ref it was measured against
  (`origin/milestone/M44` at `57b8293` branch point).
- **G2** — the row P4 facts re-derived on this branch, not inherited: mapping attribution, carried
  rows configured, E41.5 note present, E41.4 neutral result cited.
- **G1** — the mapping row's *"set by CFO allowance decision (SN-41), not by measurement"*
  attribution and E41.5's handoff quoted verbatim in the record; derivation removed.
- **Falsify before trusting** — the guard was proven by reintroducing the drift and watching it fail.
- **Fence-awareness** — the guard parses only table rows; the same phrase quoted in the notes cannot
  satisfy it. Stated in the guard's docstring and the record.
- **Itemize, never count** — the rows/faces are listed, not counted, throughout.
- **Every artifact committed. Untracked is undelivered.**
- **No CFO-enumerated value moved; E41.5's note not superseded.**

## Chat Closure

This Epic Execution Chat (P12-M44-E44.2) is execution-complete. All work is committed to
`epic/P12-M44-E44.2`, the push to `origin` is verified, and a PR to `milestone/M44` is opened.

**Epic E44.2 execution complete. Epic Delivery Notice produced. Awaiting Milestone Chat review and
authorization.**

**Next Step:** Parent Chat (M44 Milestone Chat) reviews at Stage-2; a clean delivery is accepted by
an acknowledgment that names the party that reviewed and accepted (PSG §11.6 — silence accepts
nothing). This Chat stops here and does not merge.