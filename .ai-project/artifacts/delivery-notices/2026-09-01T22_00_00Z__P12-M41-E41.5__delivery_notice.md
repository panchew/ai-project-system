---
artifact_type: delivery_notice
artifact_version: 1.0
timestamp: 2026-09-01T22:00:00Z
issuer_chat: Epic Execution Chat (P12-M41-E41.5)
issuer_role: Epic Agent
status: delivered
epic_id: P12-M41-E41.5
milestone_id: P12-M41
phase_id: P12
project_name: ai-project-system
merge_details:
  pr_number: <pending — updated in a follow-up commit once opened>
  pr_url: <pending>
  merge_commit: <pending — this Epic Chat does not merge>
  merge_timestamp: <pending>
  merge_strategy: <pending — parent's call>
  target_branch: milestone/M41
duration:
  start_date: 2026-09-01
  end_date: 2026-09-01
  elapsed_days: 0
final_artifacts:
  - name: D-A — Row P4 reconciled, beside the row
    path: .ai-project/artifacts/reference/token-measurement/model-routing-policy.md
    type: report
  - name: D-B — The discharge record (landed vs. planned vs. differing, with commits; both gates named MOOT; carry-forward handed over)
    path: docs/phases/P12__Completion_Fail_Closed_Defaults_and_the_Drivr_MVP/P12-M41-E41.5__record__discharge-and-reconciliation.md
    type: report
  - name: D-C — this Delivery Notice
    path: .ai-project/artifacts/delivery-notices/2026-09-01T22_00_00Z__P12-M41-E41.5__delivery_notice.md
    type: report
completion_notice_timestamp: n/a — PSG §11.6 does not require a Completion Notice on the happy path
review_decision_timestamp: n/a — pending Milestone Chat Stage-2. ACCEPT-BY-SILENCE IS SUSPENDED FOR M41 (HQ ruling, master ad6e3f1, 2026-08-21): acceptance must be EXPLICIT and COMMITTED; silence accepts nothing
---

# Delivery Notice: P12-M41-E41.5 — Terminal: Land What Cleared

## Summary

**This epic lands no configuration change, and that is the correct, verified result.** Spec
v2.0.0 (`c556c30`) found that the model line-up this epic was built to land had already been
configured outside M41's machinery, by PR #236 (`master` `3222f50`, 2026-08-27, HQ Ruling on
SN-40..46). Re-verified directly against the live files (not taken on the spec's own report):
confirmed true. This epic's remaining, real work — reconciling row P4's stated reasoning with how
its value is actually set, and recording the discharge — is delivered as D-A and D-B below. Both
gates that would have bound a configuration change are recorded as **moot**, not satisfied, and the
distinction is stated plainly. The three-row carry-forward (`phase`, `milestone`, `epic_manual`) is
**handed over with the facts**, not discharged — its status after #236 is recorded as the Phase
Chat's call, not assumed here.

## Deliverables

| Deliverable | Result |
|---|---|
| **D-A** — Row P4 reconciled | Note added beside row P4 in `model-routing-policy.md` (not inside it): holds apart the **decision** (2026-08-19, `Deepseek V4 Flash`), the **configured value** (2026-08-27, `deepseek-v4-pro` — a different engine, by a later CFO allowance decision), and the **measurement** (E41.4, 2026-09-01 — neutral, neither supports nor contradicts). Cites E41.4's result rather than restating it. Row P4's `default`/`why` cells and the mapping table are unchanged. |
| **D-B** — The discharge record | `P12-M41-E41.5__record__discharge-and-reconciliation.md`. Six of seven `models:` keys differ from what M41 planned to land, in value, route, or both (table, with commits) — only `hq` matches, because it never moved. Both gates named **MOOT**: Gate 1 because M42 is verified `status: planned`, not closed; Gate 2 because the moves cite a CFO allowance decision rather than a harness result, and E41.4's own harness result for `phase`/`milestone` was delivered five days **after** the landing it would have gated. The carry-forward is handed over with its facts (all three rows now hold configured-but-ungated values; enforcement resumes at P12's own close, per the CFO's 2026-08-31 decision), status left to the Phase Chat. |
| **D-C** — this Epic Delivery Notice | This document. |
| Structural diagram | In `P12-M41-E41.5__record__discharge-and-reconciliation.md`, Mermaid, in-repo, no ComfyUI. |

**Not deliverables here** (spec v2.0.0): the `.ai-project.yml` edit, `DEFAULT_MODELS`,
`chat-hierarchy.md`'s mapping, the `tests/test_model_config.py` refactor, and the notification —
all done by #236 or moot (`model_verification: advisory` means nothing arms).

## What was verified, not inherited (G2)

| Claim | Re-measured by this chat | Result |
|---|---|---|
| The line-up landed via #236, not via this milestone | `git show 3222f50 --stat`; diffed against `.ai-project.yml`, `chat-hierarchy.md`, `bin/ai-project-orchestrator`, `tests/test_model_config.py`, `model-routing-policy.md` as they stand today | **Confirmed** — all five surfaces carry the landed values, matching #236's diff |
| M42 is closed (Gate 1) | `git show HEAD:.../P12-M42__milestone-spec.md` frontmatter; searched for any `E42.x` spec/starter or M42 closure declaration | **NOT closed** — `status: planned`, no epic docs exist. Gate 1 recorded as **moot** (the landing bypassed it), not satisfied |
| `EXPECTED_MANUAL_ONLY_VALUE` refactor still needed (V3) | `grep` in `tests/test_model_config.py` | **Constant is gone.** Guard is `test_config_manual_only_key_is_well_formed`, per-key. V3 confirmed moot |
| E41.4's result and its date relative to #236 | `git log --oneline`; `d4-bars-and-recommendations.md` | **E41.4 merged `9f50e39`, 2026-09-01 — five days after #236 (`3222f50`, 2026-08-27).** Result: absolute PASS all three, relative bar cleared by none — identical on every check |
| Branch drift against the spec's citations (`P11-GH-1`/`P11-GH-2` discipline) | `git rev-list --count HEAD..origin/milestone/M41` and the reverse | **0 both directions** — this branch is exactly at its parent, no staleness |
| Suite green, delta stated | `PYTHONPATH=. pytest -q` | **569 passed / 0 failed** — matches E41.4's own recorded branch baseline exactly; delta from this epic: **0** |

## Hard-constraint and binding-constraint compliance

- **Every number measured and reported** — the landing table, the gate checks, and the suite count
  are all direct measurements, not restatements of the spec's or starter's claims.
- **No claim of gate satisfaction where none occurred** — both gates are named moot, with the
  specific reason each did not (and could not) bind the landing that already happened.
- **`decision made` and `value configured` kept visibly distinct** — D-A's whole content is that
  separation, applied to row P4's `milestone` cell, where a third fact (measured) also had to be
  held apart.
- **The carry-forward is handed over, not discharged** — its facts are stated; its status is left
  to the Phase Chat, per spec v2.0.0's explicit instruction.
- **No guard touched, none weakened** — this epic's only edit is to a reference document
  (`model-routing-policy.md`) outside the divergence guards' parse; the guards named in earlier spec
  versions (V2/V3) never applied to this epic's actual work, since no `models:` key moves here.

## Suite

`PYTHONPATH=. pytest -q` (bare `pytest` fails collection) at `epic/P12-M41-E41.5`: **569 passed / 0
failed**, ~34s. Matches E41.4's recorded branch baseline exactly. This epic adds no code under
test; its only content edit is to `.ai-project/artifacts/reference/token-measurement/model-routing-policy.md`, a reference artifact outside the suite.

## Merge authorization

**This Epic Chat does not merge.** Per the starter: merge authorization normally follows the
parent Milestone Chat's Stage-2 review. **Accept-by-silence is SUSPENDED for M41** (HQ ruling,
`master` `ad6e3f1`, 2026-08-21) — acceptance must be **explicit and committed**. If merge
authorization is given directly in this chat rather than through the parent's Stage-2, this chat
will state the bypass and confirm before acting. **This matters more here than in any other epic in
this milestone: this is the epic that changes the configuration file that governs model routing
policy** (albeit only its reference documentation, not the guarded surfaces themselves).

## Completion

Work committed to `epic/P12-M41-E41.5`, pushed to `origin`, PR opened to `milestone/M41` (number
recorded in a follow-up commit to this notice once created). This is the **Epic Delivery Notice**.

*"Epic E41.5 execution complete. Epic Delivery Notice produced. Awaiting Milestone Chat review and
authorization."*
