---
type: escalation-notice
milestone: M41
epic: E41.4
issued_by: Epic Execution Chat (P12-M41-E41.4)
issued_to: Milestone Chat (P12-M41)
date: 2026-09-01
status: pending
---

# Escalation Notice: no candidate clears the relative bar for `phase` or `milestone` (E41.4)

**Two rows, one result, escalated as a result — not as a failure.** Per the Question
Policy (*no candidate clears a row → the row holds, the result goes to the CFO*), the
measured result for both rows is neutrality: **`gpt-5.6-sol` and `deepseek-v4-pro` are
identical to the `claude-opus-5` baseline on every objective check of the frozen E35.5
instrument.** The relative bar's *strictly better on at least one* leg is unmet by
construction, so **neither candidate clears its row.** Nothing failed, no transport
blocked, no packet changed. The rows hold; the decision the CFO is asked to make is
whether the line-up's allowance for these two rows stands on this measurement's
neutrality.

---

## §1 — The two escalated rows

### `phase` → `gpt-5.6-sol` (measured: `openai/gpt-5.6-sol`)

| Check | `claude-opus-5` (baseline) | `gpt-5.6-sol` (candidate) | Bar |
|-------|----------------------------|---------------------------|-----|
| Catches | 10/10 | 10/10 | no worse — TRUE (identical) |
| Misses | 0 | 0 | no worse — TRUE (identical) |
| SPLITs | 0 | 0 | no worse — TRUE (identical) |
| False alarms | 0 | 0 | no worse — TRUE (identical) |
| Packet 3 inverse control | ACCEPT ×2, 0 FA | ACCEPT ×2, 0 FA | no worse — TRUE (identical) |
| **Strictly better on ≥1** | — | — | **FALSE — identical on all** |

**Relative bar: NOT cleared.** Escalated to the CFO as a neutral result.

### `milestone` → `deepseek-v4-pro` (measured: `opencode/deepseek-v4-pro`)

| Check | `claude-opus-5` (baseline) | `deepseek-v4-pro` (candidate) | Bar |
|-------|----------------------------|-------------------------------|-----|
| Catches | 10/10 | 10/10 | no worse — TRUE (identical) |
| Misses | 0 | 0 | no worse — TRUE (identical) |
| SPLITs | 0 | 0 | no worse — TRUE (identical) |
| False alarms | 0 | 0 | no worse — TRUE (identical) |
| Packet 3 inverse control | ACCEPT ×2, 0 FA | ACCEPT ×2, 0 FA | no worse — TRUE (identical) |
| **Strictly better on ≥1** | — | — | **FALSE — identical on all** |

**Relative bar: NOT cleared.** Escalated to the CFO as a neutral result.

> **Note for the CFO:** the `milestone` row was measured with the **`pro`** variant
> (spec v1.4.0; the value now in `.ai-project.yml`), **not** the `flash` variant E41.1
> §9.5 originally ruled. Both variants resolve on this host; the transport works
> (dispatches answered, finish=stop, exit=0).

---

## §2 — What did NOT block, so it is not part of this escalation

- **U2 transport:** both remote targets have working transports on this host. The
  `milestone` transport concern is **discharged** — a remote `deepseek-v4-pro` dispatch
  answered and was captured.
- **U5 truncation:** every run finished `stop`; no truncation ever needed to be
  distinguished from a miss.
- **U8 credential visibility:** recorded per dispatch; zero credential values read;
  E41.1's baseline not contradicted; no measurement fault.
- **U7 fidelity:** both rows receive and faithfully report the injected identity block
  (exact round-trip on the well-formed unguessable path probe).

---

## §3 — Positive statement

**An escalation was required (two rows above); no other escalation was required.**
No packet, rubric, or ground-truth change was needed. No mechanical failure occurred.
No model was substituted for an unreachable one. The frozen instrument sufficed
untouched.

---

## §4 — What the CFO is asked to decide

1. Whether the `phase` and `milestone` rows may land / hold on the CFO's SN-40..46
   allowance **given the back-test is neutral** (the measurement neither supports nor
   contradicts the move), or whether the rows should be held pending further evidence.
2. Whether the 30/30 absolute-PASS for all three models changes the relative-bar
   calculus in any way the CFO wishes to rule — this epic records the neutral result
   and takes no position.

**Full evidence:** `e41-4-runs/scores.md` (blinded), `e41-4-runs/mapping.tsv`,
`e41-4-runs/d4-bars-and-recommendations.md`, `e41-4-runs/raw/R01–R30`,
`e41-4-runs/fidelity/d7-fidelity-note.md`.