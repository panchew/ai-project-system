---
type: escalation-notice
milestone: M48
epic: E48.2
issued_by: Milestone Chat (P13-M48)
issued_to: Phase Chat (P13)
date: 2026-09-09
status: resolved
---

# Escalation Notice: E48.2 `+1` exhausted — remaining enumeration/regression omissions (E48.2)

## Trigger

**All authorized E48.2 rework is exhausted** — the original three-attempt budget plus the written
`+1` granted by Phase ruling `dbdbbaa`. The `+1` delivery received a Reject decision, so PSG §11.6
provides no further route; this notice asks for final Phase disposition. The reviewing party is
**P13 Phase Chat, OpenCode session (`remote:gpt-5.6-sol`)**.

## The two remaining findings (as received, verbatim by substance)

1. **High — regression obligations not synchronized with the ruling.** D3 (and the Starter) must
   explicitly cover: opt-out producing `skip` with `helper_exit: null`; helper-start failure
   producing `undetermined` with `helper_exit: null`; and an unknown returned integer producing
   `undetermined` while preserving the integer. The current D3 covers opt-out/disabled and omits both
   `undetermined` paths.
   (`spec:64, 134-143, 178, 206-207, 274-275`; `starter:117-118, 163`)

2. **Medium — several outcome enumerations still omit `undetermined`**, contrary to the ruling that
   every surface show all six:
   - suite-gate exit rule (`spec:162-165`) — lists `fail`/`skip`/`deferred`/`config-error`, not
     `undetermined`;
   - D2 mapping summary (`spec:175-177`) — `0→pass, 4→fail, 2→skip, 5→deferred, 3→config-error`,
     missing `other→undetermined`;
   - Acceptance Criteria (`spec:219-223`) — "Pass, fail, skip, deferred, and config-error" omits
     `undetermined`;
   - Starter gate summary (`starter:136-138`) — the same five, omitting `undetermined`.

## What the review verified as correct (not re-raised)

The core six-outcome schema and `helper_exit` nullability are correct; the known/unknown/opt-out/
helper-start mappings are correctly defined in D2; the visual correctly includes `other→undetermined`
and `no exit→null`; the immutable permalink resolves; and no closed item (direct-helper path,
delivery-report consumer, deterministic-only exit, canonical-ref procedure, bounded claims, diagram
routing) was redesigned.

## Why this is escalated rather than reworked

- **The `+1` is exhausted and no further rework is authorized.** The Phase decision is explicit:
  "The written `+1` is exhausted; no further rework is authorized. Milestone M48 must issue a new
  Escalation Notice for final Phase disposition."
- The two remaining defects are mechanical completeness gaps, not design questions: the four
  enumerations simply need `undetermined` appended (and D2's summary needs `other→undetermined`), and
  D3 needs two explicit regression lines asserting the `null` and preserved-integer semantics. This
  chat states them only to hand an accurate record to the parent; it does not apply them.

## Decision Needed

The P13 Phase Chat must give E48.2 a final disposition, from among (at least):

1. **Amend-then-close** (the E48.1 precedent): record in the M48 spec a parent disposition that
   appends `undetermined` to the four enumerated surfaces and adds the two D3 regression obligations
   (opt-out → `skip`/`null`; helper-start → `undetermined`/`null`; unknown integer → `undetermined`
   preserving the integer) by exact replacement, closing E48.2 planning without another child edit.
2. **Resolution-by-parent-carriage:** close E48.2 planning and carry the two omissions as
   parent-recorded corrections applied during E48.2 execution, with the executed Epic's DoD
   enumerating six outcomes and the three regression paths as authoritative.
3. **One further bounded grant** — only if the parent chooses to; the Phase decision already stated
   no further rework is authorized, so this option is listed for completeness, not expected.

## Impact

M48 planning is blocked at its second first-tier Epic. E48.2's "suite green" split underlies the
delivery-report gate every P13 acceptance reports against, and E48.4/E48.5/E48.6 are ordering-blocked
behind E48.1/E48.2's accepted contracts. E48.3 remains paused. The remaining work is a wording/
enumeration completeness fix at the planning layer — no code, run, or merge is involved, and none has
been or will be attempted by this chat.

## Resolution

**Resolved 2026-09-09 by P13 Phase Chat, OpenCode session (`remote:gpt-5.6-sol`).**

The Phase Chat selects **amend-then-close**. The authoritative parent disposition is commit
`2731df4` on `phase/P13`, synchronized into `milestone/M48` by merge `5e53a73`. It supersedes the
stale E48.2 v1.4.0/Starter summaries with exact replacements that:

- enumerate `pass | fail | skip | deferred | config-error | undetermined` everywhere;
- preserve unknown returned integers and represent non-invocation with truthful
  `helper_exit: null`;
- require separate regression cases for disabled and explicit opt-out, plus unknown returned
  integers and helper-start failure; and
- retain the already-correct core schema, detailed D2 mappings, visual, permalink, direct-helper
  path, delivery-report consumer, deterministic-only exit, canonical-ref procedure, bounded claims,
  and diagram routing.

E48.2 planning at `177ad8d`, read under M48 spec v1.4.0 and its synchronized Starter, is accepted by
the P13 Phase Chat. This is a parent disposition after exhausted rework, not another child attempt.
No E48.2 execution or merge is authorized by this resolution. E48.3 may now be submitted alone for
planning review.
