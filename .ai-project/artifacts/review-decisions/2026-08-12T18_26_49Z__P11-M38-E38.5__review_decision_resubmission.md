---
artifact_type: review_decision
artifact_version: 1.0
timestamp: 2026-08-12T18:26:49Z
issuer_chat: Milestone Agent (P11-M38)
issuer_role: Milestone Agent
decision: accept
epic_id: P11-M38-E38.5
milestone_id: P11-M38
phase_id: P11
project_name: ai-project-system
completion_notice_timestamp: 2026-08-12T19:00:00Z
supersedes: 2026-08-12T18_00_45Z__P11-M38-E38.5__review_decision.md
feedback: The scoped rework is accepted. The exact session-start source set was reconstructed and
  probed; retained structured evidence supports silent truncation; the full suite passes 489/489;
  and all previously accepted boundaries remain intact. Three factual/reproducibility annotations
  must be fixed before merge, but none requires another model run or Review Decision.
authorization:
  action: merge
  merge_instruction: Fix the three annotations on PR #198, report the correction commit to the
    Milestone Chat, and merge only after explicit human authorization. No further Review Decision is
    required unless the fixes expand beyond the stated annotations.
---

# Review Decision: P11-M38-E38.5 — Resubmission

## Decision: ACCEPT ✓ — three annotations to fix before merge

This decision supersedes the scoped REJECT at
`2026-08-12T18_00_45Z__P11-M38-E38.5__review_decision.md`.

PR **#198**, `epic/P11-M38-E38.5` → `milestone/M38`, is accepted on the substantive E38.5 result.
Do not merge until the three annotations below are fixed and explicit human merge authorization is
given.

## Scoped rework verified independently

Reviewed head: **`0f18e2fd4ddb4cb9869d96abd1b6c623aa463d95`**.

### 1. Exact-context measurement and retained evidence — ACCEPTED

The committed builder reconstructs the prescribed 37 source extracts from the milestone starter,
milestone spec, targeted phase-spec sections, PSG sections, and AOG sections. Independent dry
reconstruction produced:

```text
section byte sum: 147626
corpus with separators: 148562
complete probe prompt: 148871
source commit: 7f8b0c126e3701058d6e4d4c8cee22409acea46a
```

The raw response and `/api/ps` records support the result directly:

```text
/api/ps context_length: 32768
prompt_eval_count:      32767
eval_count:             1
done_reason:            length
response:               "END"
context token IDs:      38466
position-0 marker:      not returned
```

The tail instruction remained visible and elicited `"END"`; the position-0 marker did not. This is
machine-readable evidence of silent beginning truncation, not an inference from agent prose.

### 2. Suite and model identity — ACCEPTED

`.ai-project.yml` and `governance/systems/chat-hierarchy.md` both declare
`models.epic_manual: remote:claude-opus-5`. The prior false `deepseek-v4-flash` divergence and test
exclusions are removed.

Independent run on the resubmitted head:

```text
$ PYTHONPATH=. pytest -q
489 passed in 25.28s
```

### 3. Accepted-and-closed scope — PRESERVED

- `qwen3-coder:30b` remains the measured model.
- The loaded value comes from `/api/ps`; 262,144 is not substituted.
- E35.5's judgment result remains correctly attributed to `qwen3.6:27b`, absent from
  `opencode.json`.
- Capacity remains a fourth axis; G-P4-a/b/c are unaffected; row P4 is not decided here.
- The deployment and `model-routing-policy.md` are unchanged; G11 is not claimed; no M39/M40 surface
  was built.

## Annotation 1 — correct the 55-byte section-boundary residue

The script labels two extracts as exact sections but carries their following boundaries:

- `m38_milestones = 477..512` includes line 512, the next heading:
  `### M39: Trustworthy Completion Signal (P10-GH-7)`.
- `acceptance = 585..628` includes lines 627–628, the closing `---` plus blank line.

Those adjacent bytes total **55**. The prescribed session-start source content is therefore
**147,571 bytes**, while the measured probe source set is **147,626 bytes**. Record the latter
honestly as a strict 55-byte superset; do not continue calling it byte-exact.

**No GPU re-run is required.** The raw response carries 38,466 context token IDs and one generated
token. The prompt therefore encoded 38,465 input token IDs, 5,697 above the loaded window. All
non-source probe overhead plus the 55-byte residue totals only 1,300 bytes; removing it cannot erase
that measured margin. The intended source set still exceeds the loaded window by thousands of
tokens, so the capacity and silent-truncation result stands.

Fix the Delivery Notice's counts and wording. Preserve the original raw manifest and response as
run-produced evidence; annotate rather than rewriting their historical values.

## Annotation 2 — make the reproducer truly commit-anchored and portable

The manifest records source commit `7f8b0c1`, but the script reads every file from mutable
`BASE_REF = 'milestone/M38'`. After the branch advances, the recorded command no longer reconstructs
the tested bytes. The script also hard-codes one user's absolute repository path.

Pin extraction to the recorded full commit SHA and derive the repository root from the script or
`git rev-parse --show-toplevel`. Keep `milestone/M38` as descriptive provenance only. This annotation
must not alter the historical raw result files.

## Annotation 3 — align prose with the actual endpoint and structured fields

- The live probe used Ollama's native **`/api/generate`** endpoint, not `/v1`; correct the Summary.
  Section 5 may still explain that OpenCode's `/v1` transport cannot adjust the loaded denominator.
- `prompt_eval_count: 32767` is the number of prompt tokens processed after truncation. It does not by
  itself count the full input. Use the raw `context` array (**38,466 token IDs**, including the one
  output token) for the full encoded prompt and the margin calculation.
- The raw response file has 38,481 file lines but **38,466 context token IDs**. Correct the table's
  “38,481 lines of context tokens” wording.

## Merge path

Fix only these annotations on the existing E38.5 branch and PR #198, then report the correction commit
to the Milestone Chat. No further Review Decision or GPU run is required if the diff remains within
this scope.

After the annotations are verified, explicit human merge authorization is still required.
