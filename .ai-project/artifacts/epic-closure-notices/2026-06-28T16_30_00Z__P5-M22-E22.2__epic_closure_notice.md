---
type: epic-closure-notice
epic: P5-M22-E22.2
milestone: M22
issued_by: Milestone Chat (P5-M22 — Visual Artifacts)
issued_to: Phase Chat (P5)
date: 2026-06-28
status: complete
---

# Epic Closure Notice: P5-M22-E22.2 — Guidelines, templates, and agent integration (VA-1 full)

## Merge Confirmation
- PR: #93 — https://github.com/panchew/ai-project-system/pull/93
- Merge commit: `cb195eb` (`Merge epic/P5-M22-E22.2 into milestone/M22 (PR #93) — VA-1 full implementation`)
- Target branch: `milestone/M22` (pushed to `origin/milestone/M22`)
- Merged at: 2026-06-28 UTC
- Epic branch: deleted (local **and** remote, via `gh pr merge --delete-branch`).

## Authorization Reference
Accepted under **SN-13 default-accept**: the delivery met its Definition of Done, acceptance
criteria, and spec, so no Review Decision artifact was issued (the exception path did not trigger).
The Milestone Chat independently verified the delivery before merging — full suite re-run (259
passed, 1 skipped), the integration test confirmed to **skip** at `enabled: false`, and the helper's
disabled-path exit code (2) exercised directly. The merge was authorized by the CFO under the
`cfo_review_gate: enabled` PR-review gate.

## Final State
- Definition of Done fully satisfied (no carry-over); Delivery Notice produced
  (`P5-M22-E22.2__delivery-notice.md`).
- **AOG §16 "Visual Artifact Production"** added (version **2.1.0 → 2.2.0** + Changelog): SN-11
  per-level abstraction table, structural vs. generative modes, tool-capability gating, and
  commit-location guidance; opt-in on `visual_artifacts.enabled`.
- **`seed.md` Rule 4** elicits "What does success look like visually?" before the Project Brief
  converges — integrated **additively** into the existing "Rule 4 — Convergence Target" ("Visual
  success" Brief element + elicitation prompt); existing rules un-renumbered, Rule 5 intact.
- **`bin/ai-project-visual`** ships: a callable prompt→artifact ComfyUI helper that reads
  `enabled`/`comfyui_url`/`types` through E22.1's single source (`load_yml_config` /
  `resolve_visual_artifacts`, loaded via `SourceFileLoader`), submits a workflow, and writes the
  result; stdlib-only (`urllib`); clear non-zero exits (2 disabled, 3 misconfig, 4 runtime).
- **`governance/guides/visual-artifacts.md`** documents endpoint configuration, both modes, helper
  usage, and a worked example per chat level (+ guides README index row).
- **Integration test** `tests/integration/test_visual_artifacts_helper.py` exercises the helper and
  **skips when `enabled: false`**; two offline gate tests run unconditionally.
- Full test suite on the integrated `milestone/M22`: **259 passed, 1 skipped** — green.
- Working tree clean.

## Carry-Over
- None blocking. **Manual endpoint verification deferred (by design):** no live ComfyUI endpoint is
  configured in the governance source repo (endpoint availability is the CFO's responsibility per the
  SN-11 framework/CFO split). Generation against a live endpoint is the manually-verifiable path; the
  skip-on-disabled test keeps the suite green without one. Failure modes were verified directly
  (disabled → 2, unconfigured type / video-without-workflow → 3, unreachable endpoint → 4).

## Chat Closure
Epic P5-M22-E22.2 is complete and closed.

---

**Closed by the Milestone Chat (the executing Coding Agent had stopped per the canonical happy path
after producing its Delivery Notice).**
