---
type: epic-closure-notice
epic: P5-M22-E22.1
milestone: M22
issued_by: Milestone Chat (P5-M22 — Visual Artifacts)
issued_to: Phase Chat (P5)
date: 2026-06-28
status: complete
---

# Epic Closure Notice: P5-M22-E22.1 — Configuration and spec (VA-1 config layer)

> **Note:** Issued together with the E22.2 closure artifacts. The Milestone Chat accepted E22.1
> under SN-13 default-accept and merged it at the time; this Closure Notice is produced in the same
> closure batch as E22.2. The merge facts below are confirmed against the repository, not
> reconstructed from memory.

## Merge Confirmation
- PR: #92 — https://github.com/panchew/ai-project-system/pull/92
- Merge commit: `b002ac4` (`Merge epic/P5-M22-E22.1 into milestone/M22 (PR #92) — VA-1 config layer`)
- Target branch: `milestone/M22` (pushed to `origin/milestone/M22`)
- Merged at: 2026-06-28 UTC
- Epic branch: deleted (local **and** remote).

## Authorization Reference
Accepted under **SN-13 default-accept**: the delivery met its Definition of Done, acceptance
criteria, and spec, so no Review Decision artifact was issued (the exception path did not trigger).
The Milestone Chat independently verified the delivery (full suite re-run) before merging; the merge
itself was authorized by the CFO under the `cfo_review_gate: enabled` PR-review gate.

## Final State
- Definition of Done fully satisfied (no carry-over); Delivery Notice produced
  (`P5-M22-E22.1__delivery-notice.md`).
- `visual_artifacts` block documented in `governance/ai-project-yml-spec.md` (§3.1 full-schema
  reference, new §3.5 field definitions, §4 validation rules 18–24, §11 full example; version bumped
  **2.0.0 → 2.1.0** + Changelog). Validation single-sourced in the existing Python config handler
  `bin/ai-project-orchestrator` (`load_yml_config` → `validate_visual_artifacts` /
  `resolve_visual_artifacts`) — **no parallel validator**. Tests cover valid / invalid `enabled` /
  invalid `types` / malformed `comfyui_url` / absent / unknown-key (`tests/test_visual_artifacts_config.py`
  + orchestrator inline), plus a repo-config assertion in `tests/test_ongoing_artifacts.py`. The
  repo's `.ai-project.yml` carries the block with `enabled: false`.
- Full test suite on the integrated `milestone/M22`: **259 passed, 1 skipped** — green (the single
  skip is E22.2's endpoint test at `enabled: false`).
- Working tree clean.

## Carry-Over
- None blocking. The exact validation locus (Python loader vs. bash init validator) was the
  Epic-level decision the spec left open; execution chose the Python loader and documented why (the
  bash scaffold-validator never sees an opt-in default-off block, and structured checks — boolean,
  set-membership, URL — are single-sourced cleanly in Python). E22.2 consumes this contract read-only.

## Chat Closure
Epic P5-M22-E22.1 is complete and closed.

---

**Closed by the Milestone Chat (the executing Coding Agent had stopped per the canonical happy path
after producing its Delivery Notice).**
